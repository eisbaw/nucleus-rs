---
id: TASK-0044.01
title: 'M6 sub-task: openmp-rs backend crate (rayon + shared memory + barrier, sync)'
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 09:23'
updated_date: '2026-05-27 09:58'
labels:
  - M6
  - backend
dependencies: []
parent_task_id: TASK-0044
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land the fifth tier-1 backend crate `nucleus/backends/openmp-rs`: workers map to rayon threads, transport is shared memory, notify is barrier, sync-only (no async, no buffer). PRD §7.1 row 1.

This is the SIMPLEST of the three M6 backends — same capability surface as pthreads-sync (sync + shared-memory + barrier-style notify), differing only in worker substrate (rayon scope/par_iter instead of std::thread). Because the capability matrix matches pthreads-sync 1:1, every schedule that compiles against pthreads-sync MUST compile against openmp-rs unchanged; the cross-backend differential gains a third sync-shared-memory row.

## Closest template

`nucleus/backends/pthreads-sync` is the structural template:
- Same `emit(per_worker, names, sidecar, kernels_path, out_dir)` signature.
- Same single-binary output (no run.sh process orchestration).
- Same use of `backend-common`'s `multi_worker_walker`, `check_frame`, `project_skeleton`, `render`, `host_election` helpers.

The diff is the runtime substrate: rayon's `scope` / `par_iter` instead of std::thread::spawn + handles. The barrier becomes rayon's implicit join at scope-end (or `rayon::iter::IntoParallelRefIterator::join()`).

## Capabilities

```toml
schema_version  = 1
name            = \"openmp-rs\"
tier            = 1
transport       = \"shared-memory\"
notify          = [\"barrier\", \"blocking\"]
supports_async  = false
supports_buffer = false
max_buffer      = 1
worker_classes  = [\"default\"]
memory_regions  = [\"heap\"]
```

(Identical to pthreads-sync capability surface; only `name` differs.)

**PRD vs reality:** PRD §7.1 row openmp-rs lists notify=`barrier` and pthreads-sync notify=`condvar`. The actual `pthreads-sync/capabilities.toml` ships notify=`["barrier", "blocking"]`. This brief copies the actual cap-file convention (not the narrower PRD row), because the compat-check matches on the cap-file. If the implementer reads the PRD §7.1 row and tries to ship notify=`["barrier"]` alone, schedules currently compiling on pthreads-sync via the `"blocking"` capability arm will reject on openmp-rs. KEEP `["barrier", "blocking"]` and treat the PRD row as documentary/aspirational (a separate cycle may reconcile PRD §7.1 to the actual cap-file convention, but that is NOT this task's scope).

## Acceptance criteria

1. `nucleus/backends/openmp-rs/` crate exists with Cargo.toml + src/lib.rs + capabilities.toml.
2. `openmp_rs::emit(...)` returns the same SkeletonResult shape as pthreads-sync (project_dir / cargo_toml / main_rs / kernels_rs / run_sh).
3. Driver dispatch arm in `nucleus/driver/src/main.rs` lit (the existing match-string list grows from 4 to 5 backends; the 'unknown backend' help-text list updated).
4. capabilities.toml satisfies the capability-compat check the same way pthreads-sync does (i.e. rejects async/buffer/event schedules with the same typed ContractGap shape).
5. e2e-matrix.toml gains openmp-rs cells for all schedules that pthreads-sync already covers — at minimum: 01/naive, 02/split, 03/naive, 04/naive+blocked, 05/naive+blocked, 06/naive+blocked, 07/naive+blocked (the M3 set plus 04/06 added in M3 extensions). Each cell bit-identical against reference.bin.
6. Capability mismatches (async + buffer + event schedules — 05/distributed, 09/pipelined, 11/pipelined, 13/pipeline_parallel) are [[skip]] with the same TASK-0042-style reason text pthreads-sync uses.
7. Unit + integration tests pin the emit string for at least one multi-worker example (mirror pthreads-sync's multi_worker_emit pin test).

## Honest scope

- rayon is a new top-level dependency; needs justification (PRD §12.2 doesn't list it). The substrate is the only well-known shared-memory parallel library for Rust; the dependency is bounded (rayon → std). Note rayon's transitive deps (crossbeam, num_cpus) in the design notes.
- The 'OpenMP' in the name is metaphorical — there's no real C-OpenMP runtime, no #pragma omp parallel. The name signals 'parallel-for shared-memory' semantics, matching how rayon is used in practice.
- multi-worker semantics: where pthreads-sync's multi_worker arm spawns N std::threads with Condvar rendezvous, openmp-rs uses rayon::scope with channel-based rendezvous (or shared Mutex if the rendezvous shape forces it). The backend-common multi_worker_walker abstracts much of this — check whether its rendezvous_prefix mechanism extends to rayon or needs a new arm.

## Cross-reference

- Template: `nucleus/backends/pthreads-sync/src/lib.rs`, `nucleus/backends/pthreads-sync/src/multi_worker.rs`, `nucleus/backends/pthreads-sync/capabilities.toml`.
- Shared infrastructure: `nucleus/backend-common/` (consumed as a sibling per project memory project-backend-common-crate).
- Driver dispatch: `nucleus/driver/src/main.rs:673-766` (the match-on-backend block).
- M4 precedent (sibling backend crate): `nucleus/backends/pthreads-async/`, TASK-0042.01.
- e2e-matrix wiring: `nuc-nucleus/e2e-matrix.toml`.
- PRD: §7.1 row openmp-rs; §11 M6.

## Forward-carried context (orchestrator briefing for the implementer)

- The capability surface IS identical to pthreads-sync. Do NOT add 'event' to notify just because rayon CAN do work-stealing — the PRD pins the row as barrier+blocking only.
- The backend-common::multi_worker_walker abstracts the per-worker event walk; if rayon's rendezvous_prefix mechanism doesn't fit cleanly, file the design gap as a sub-task rather than fork the walker (per project memory project-backend-common-crate).
- include_str! coverage rule applies: any include_str! you add MUST be paired with a #[cfg(test)] mod or include! in the same crate (per memory feedback-include-str-compile-coverage, just check-include-str-coverage).
- Textual replace on rendered codegen strings is unsafe (per memory feedback-textual-replace-codegen-unsafe, just check-textual-replace-on-codegen).
- check-narrative-doc-lie is wired into just ci as of cycle 170b — any narrative additions to e2e-matrix.toml must use 'AT FILING TIME' verbs or carry the `# ALLOW narrative-doc-lie:` annotation.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 173 — SKELETON slice landed; phased-AC addendum

Cycle 173 delivered the SKELETON slice (architect-fold-back P2.1 addendum filed inline):

- AC#1 — `nucleus/backends/openmp-rs/` crate exists with Cargo.toml + src/lib.rs + capabilities.toml. **DONE.**
- AC#2 — `openmp_rs::emit(...)` returns the `EmitResult` shape (single-binary 5-field). **DONE** at the shape level; emit() always returns `EmitError::ContractGap` in the skeleton phase, so no actual EmitResult instance is constructed yet.
- AC#3 — Driver dispatch arm in `nucleus/driver/src/main.rs` lit (4→5 backends); BACKENDS help-text + 'unknown backend' help-text both extended. **DONE.**
- AC#4 — capabilities.toml satisfies the capability-compat check (smoke-tested via `nucleus build --backend openmp-rs ... 01-elementwise-add/naive ...` — compat-check passes, then ContractGap with precise forward-link surfaces). **DONE.**
- AC#7 — Unit tests pin emit-string (the cycle-173 skeleton tests pin (a) ContractGap variant + message contains `openmp-rs` + forward-links TASK-0044.01; (b) EmitResult shape via constructor-as-compile-pin). **DONE for skeleton scope.** Multi-worker emit-string pin lands with the codegen cycle.

OPEN ACs (deferred to the codegen cycle of TASK-0044.01):

- AC#5 — e2e-matrix cells for openmp-rs × {01/naive, 02/split, 03/naive, 04/naive+blocked, 05/naive+blocked, 06/naive+blocked, 07/naive+blocked}, each bit-identical against reference.bin. **PENDING** — every cell would currently ContractGap; matrix wiring lands with codegen.
- AC#6 — Capability-mismatch cells [[skip]] with TASK-0042-style reasons. **PENDING** — same dep as AC#5.

PHASED-AC POLICY (cycle 173 architect P2.1 fold-back): TASK-0044.01 ACs split into skeleton-cycle (AC#1-#4, AC#7) and codegen-cycle (AC#5-#6) buckets. Do NOT close TASK-0044.01 until the codegen-cycle ACs also land — leaving the brief as-is risks `feedback-ac-rewrite-on-done-task` recurrence (closing Done with un-met live ACs). This task stays In Progress.

## Cycle 191 — SINGLE-WORKER arm landed; phased-AC AC#5-subset + AC#6 closed

Cycle 191 landed the single-worker codegen arm (the cycle-173 phased-AC addendum's first remaining bucket):

- AC#5 (PARTIAL — single-worker subset DONE; multi-worker portion still PENDING under TASK-0044.01.01):
  - **16 [[required]] openmp-rs cells**, all bit-identical against reference.bin (verified `just e2e` × 3 stable samples post-cycle-191, total 150/127/0/23/0):
    01/naive, 02/naive, 03/naive, 04/{naive, blocked, blocked-nondiv}, 05/{naive, blocked},
    06/{naive, blocked}, 07/{naive, blocked}, 08/naive, 09/naive, 11/naive, 13/naive.
    (13/naive added in the cycle-191b architect P1 fold-back — cycle-191 draft missed it
    despite pthreads-sync parity; gap was real.)
  - 2 implicit-fan-out informational PASS cells the harness auto-discovered:
    05-stencil/reuse, (formerly 13/naive — now [[required]]). Bit-identical.
- AC#6 (DONE): 5 [[skip]] entries for capability-mismatch schedules (async + buffer + event),
  reason text mirrors pthreads-sync's TASK-0042 verbatim:
    05/distributed, 05/distributed-2d, 09/pipelined, 11/pipelined, 13/pipeline_parallel.
- AC#7 (PARTIAL): single-worker emit-string pin LANDED (tests/single_worker_emit.rs — two
  byte-identical-vs-pthreads-sync tests: empty event-list + 01-elementwise-add/naive real
  fixture). Multi-worker emit-string pin still pending under TASK-0044.01.01.

OPEN ACs (carry-forward to TASK-0044.01.01):
- AC#5 (multi-worker subset): 8 [[skip]] cells with 'openmp-rs multi-worker codegen pending'
  reason — 02/split, 03/distributed, 06/{distributed, distributed2},
  07/{distributed, distributed-2d}, 08/distributed, 13/batch_parallel. Promotion target on
  TASK-0044.01.01 close.
- AC#7 (multi-worker pin): mirror pthreads-async/tests/skeleton.rs::multi_worker_emit_for_02_split_succeeds
  shape when codegen lands.

## Cycle 191 — implementation details

- nucleus/backends/openmp-rs/src/lib.rs: replaced SKELETON ContractGap with single-vs-multi-worker
  dispatch. Single-worker arm: `pthreads_sync::render_single_worker_main` +
  `backend_common::project_skeleton::single_binary::{render_cargo_toml, render_run_sh}` (same
  delegation pattern as pthreads-async cycle 17). Multi-worker arm: typed
  EmitError::ContractGap forward-linking TASK-0044.01.01. ~150 LoC total.
- nucleus/backends/openmp-rs/Cargo.toml: added `pthreads-sync` (runtime dep) +
  `test-common` (dev-dep). No `rayon` dep yet — single-worker arm is pure delegation.
- nucleus/backends/openmp-rs/tests/skeleton.rs: rewrote — multi-worker ContractGap pin +
  EmitResult shape pin.
- nucleus/backends/openmp-rs/tests/single_worker_emit.rs (NEW): empty event-list +
  01-elementwise-add/naive byte-identical-vs-pthreads-sync tests.
- nuc-nucleus/e2e-matrix.toml: openmp-rs added to backends list; 16 [[required]] +
  8 multi-worker-pending [[skip]] + 5 cap-mismatch [[skip]] cells.

## Cycle 191b — review-gate hardening

Parallel review gate (qa-test-runner + mped-architect read-only) GO with finite findings,
all folded into the cycle commit (no defer-to-follow-up except the silent-sibling test
which is TASK-0044.01.02):

- QA P2: 4 narrative-doc-lie sites pre-cycle-191 that now drift on landing —
  driver/src/main.rs:60 (file-doc), :115 (--help text), nucleus/Cargo.toml:44 (workspace
  comment), driver/Cargo.toml:47 (dep comment). All updated to reflect single-worker
  LANDED + multi-worker-pending state.
- Architect P1: 13/naive openmp-rs was missing from [[required]] (gap from briefing's
  literal AC#5 list which elided example 13). Added.
- Architect P2: src/lib.rs honest-limitations list lied — '05/distributed' was put in
  multi-worker-pending list but is actually capability-mismatch. Reworked into two
  distinct bullets (multi-worker-pending vs cap-mismatch).
- Architect P3: driver/src/main.rs:780-790 arm comment lied about SKELETON state.
  Updated.
- Architect P3: Cargo.toml line 20 was forward-looking 'lands with multi-worker' —
  reworded to honest current-state ('NOT a current dependency').
- Architect P2 follow-up (NOT in this commit, filed as TASK-0044.01.02): port the
  cycle-36 `const_in_indexexpr_*` silent-sibling test from pthreads-async to openmp-rs.

## Lessons forward-carried

For TASK-0044.01.01 (multi-worker arm):
- Reviewer subagent inferences from task TITLES rather than greps are the
  `feedback-implementer-disclosure-mechanism-wrong` recurrence — always re-grep before
  claiming structural facts.
- 13/naive cap-mismatch missing in the cycle-191 draft was a literal-AC-text gap, not a
  cap-check defect. When the AC enumerates 'at minimum: X, Y, Z', sweep the full
  pthreads-sync [[required]] list for siblings before claiming parity.
- The narrative-doc-lie check pattern is sensitive: any 'has not yet landed' or 'will
  land' line needs either a 'Cycle-N filing:' / 'Cycle-N update:' prefix OR a verb shift
  to past tense + 'AT FILING TIME' marker. Predict the lint before submitting.

## Cycle 191 stats

- e2e: 150/127/0/23/0 (up from cycle-189's 120/110/0/10/0): +30 total, +17 PASS, +13 SKIP.
  Required-fail: 0. Three stable samples post-cycle-191b.
- Tests: dev 986/0/3 (was 985 pre-cycle), release 985/0/3 (was 984). +2 new openmp-rs
  single_worker_emit tests + 2 reworked skeleton tests = net +1 test crate count.
- LoC delta: openmp-rs lib.rs 111 → 229 (+118), single_worker_emit.rs new (+159),
  skeleton.rs 75 → 84 (+9), e2e-matrix.toml 1497 → 1733 (+236 across cells + comments).
- Filed follow-ups: TASK-0044.01.01 (multi-worker rayon-scope codegen, HIGH),
  TASK-0044.01.02 (silent-sibling const-in-IndexExpr regression pin for openmp-rs,
  LOW).

## Cycle 197b closure (orchestrator-direct, M4-capstone-pattern)

All phased ACs now closed. Sub-task ladder:
- TASK-0044.01.01 (multi-worker codegen) — Done cycle 196/196b (8 [[required]] cells bit-identical).
- TASK-0044.01.02 (const-in-IndexExpr sibling regression pin) — Done cycle 191b.
- TASK-0044.01.03 (host-excluding-barrier oracle widening) — LOW prio follow-up, To Do; NOT a parent-close blocker (cap-mismatch surface coverage hardening, not a regression risk).

AC closure summary (vs cycle-173 + cycle-191 + cycle-196b implementation notes):
- AC#1-#4 + AC#7-skeleton: cycle 173 (skeleton slice).
- AC#5 single-worker: cycle 191 (16 [[required]] cells bit-identical).
- AC#5 multi-worker (8 cells): cycle 196/196b — 02-split-add/split, 03-reduction/distributed, 06/distributed + distributed2, 07/distributed + distributed-2d, 08-histogram/distributed, 13-cnn-inference/batch_parallel.
- AC#6: cycle 191 ([[skip]] entries with TASK-0042-style reasons for async/buffer/event cells).
- AC#7 multi-worker emit-string pin: cycle 196 (`tests/multi_worker_emit.rs`).

E2E baseline at close: 210/190/0/20/0 (cycle 197b, 4 non-flake samples). openmp-rs holds 24 PASS cells across the matrix.

Honest scope held: rayon top-level dep documented in lib.rs preamble; capability surface kept ["barrier", "blocking"] per pthreads-sync convention (PRD §7.1 row deferred to a separate reconciliation cycle, NOT this task's scope). No 5th compensating pass landed silently (TASK-0337 promotion-trigger guard cleanly NOT-applicable for openmp-rs — sync + shared-memory + barrier surface inherits NEITHER the host-relay tower NOR the bufsync ordering profile).

Parent CLOSED. M6 fifth tier-1 backend row LANDED.
<!-- SECTION:NOTES:END -->
