---
id: TASK-0049.10.01
title: >-
  BLOCKER 1 root: purity in KernelSig + per-frame effectful indexed-output INPUT
  lowering (ex14 firmware BIN)
status: In Progress
assignee:
  - '@me'
created_date: '2026-06-03 21:28'
updated_date: '2026-06-03 21:52'
labels:
  - M11
  - backend
  - codegen
  - value-correctness
dependencies: []
parent_task_id: TASK-0049.10
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Slice A of TASK-0049.10 (per the parent's suggested A/B/C/D order). ROOT FIX for BLOCKER 1: the 'effectful' kernel marker is lost before the codegen contract, so an effectful indexed-output zero-input kernel (fe_capture/rf_receive: () -> i32[SAMPLES_PER_FRAME] effectful, fired as mic_in[frame] <-- fe_capture()) is structurally indistinguishable from a pure indexed compute and lowers in the embedded backend's render_fire to the zero-returning extracted stub kernels::fe_capture(). Result: firmware mic_in/bt_in are all zeros => cannot match the REAL reference.bin.

TRACTABILITY (orchestrator-verified 2026-06-03): ResolvedKernel already carries purity: Purity (algo/ir.rs:134). The gap is sidecar::KernelSig (the codegen contract the embedded backend reads via NameSidecar.kernel_sigs) copies ONLY params+ret, NOT purity (sidecar.rs:391, whose own docstring flags this exact omission as the DIVERGENCE HAZARD). So slice A is BOUNDED: (1) add purity (recommend deriving serde on Purity and storing purity: Purity, which directly discharges the DIVERGENCE HAZARD; is_effectful: bool is an acceptable alternative) to KernelSig and populate it in build_sidecar from ResolvedKernel.purity; (2) in embedded render_fire (render.rs ~502 'Indexed OUTPUT (pure compute)' arm), add an ADDITIVE sub-branch: an EFFECTFUL kernel with an indexed output AND no inputs lowers to a per-frame shim region-read into the indexed slice (e.g. mic_in[frame], sized by the row not the whole array) instead of the kernels::callee() stub call.

SCOPE: INPUT only. fe_emit/rf_transmit (effectful, output=None) per-frame OUTPUT drain is slice B (TASK-0049.10.B). Per-worker input partition + multi-output capture .resc is slice C. renode-multimcu recipe resolution + byte-exact gate is slice D. Do NOT claim byte-exact in slice A (not achievable until C+D land).

REGRESSION GUARD (load-bearing): the existing STRUCTURAL classification in render_fire (output None => save; whole-array output + empty inputs => load; indexed output => pure stub call) backs 7 M6 cells + M10 ex1/5/9 + 02-split-add. The purity-aware path MUST be purely ADDITIVE — it only adds a new arm for the indexed-output + effectful + no-inputs shape, which NO shipped cell exercises. e2e MUST stay 427/364/0/63/0 byte-for-byte.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 purity (or is_effectful) added to sidecar::KernelSig + populated in build_sidecar from ResolvedKernel.purity; DIVERGENCE HAZARD docstring updated to reflect that purity is now mirrored
- [ ] #2 embedded render_fire: an EFFECTFUL kernel with indexed output AND no inputs lowers to a per-frame shim region-read into the indexed slice (mic_in[frame]-style), NOT the kernels::callee() zero-stub; verified by a focused emit test on ex14 multi-MCU
- [ ] #3 purely ADDITIVE: pure indexed-output kernels still emit the stub call; NO regression — e2e stays 427/364/0/63/0 byte-for-byte; the 7 M6 + M10 ex1/5/9 + 02-split-add structural classification unchanged
- [ ] #4 proptest_serde kernel_sig strategy + sidecar_kernel_sigs_match_algoir round-trip/coverage test updated for the new field; just check (build/clippy/test/test-release) + e2e all GREEN
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Derive serde on Purity (ast.rs:87) under the feature gate, mirroring ResolvedType/ScalarType precedent.
2. Add pub purity: Purity to sidecar::KernelSig; populate in build_sidecar from rk.purity; update DIVERGENCE HAZARD docstring (sidecar.rs:392-399) + section-(d) comment (606-611) to reflect purity is now mirrored (no longer 'irrelevant').
3. Add a public render_indexed_place helper in backend-common/render/fire.rs that reuses the private classify_data_slice to return the lvalue place-expression for an indexed DataSlice (name[idx] for Scalar, name[start..start+sub_len] for SubArray) + element count — NO textual replace.
4. Additive branch in embedded render_fire indexed-output arm: if kernel is Effectful AND bindings.inputs.is_empty(), emit a per-frame shim region-read (alloc_in_region/dma_wait/null-guarded copy_nonoverlapping) into the indexed place's .as_mut_ptr(), sized by size_of_val of the indexed element. Else keep existing kernels::callee stub.
5. Fix ALL sidecar::KernelSig construction sites (9 test files + proptest strategy) for the new field; extend petri_to_events sidecar_kernel_sigs_match_algoir to assert purity matches AlgoIR; extend proptest_serde strategy to generate purity.
6. Focused emit test in embedded tests.rs on ex14 fe worker: assert fe_capture renders alloc_in_region/copy_nonoverlapping per-frame region read AND NOT kernels::fe_capture(; pin a pure indexed kernel still emits stub.
7. Full gate: build/clippy/test/test-release/e2e must stay 427/364/0/63/0.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Slice A IMPLEMENTED + full gate GREEN. (1) serde derived on Purity (ast.rs:87, mirrors ResolvedType/ScalarType). (2) sidecar::KernelSig grew pub purity: Purity, populated in build_sidecar from rk.purity; DIVERGENCE HAZARD docstring + section-(d) comment updated (purity now mirrored, no longer "irrelevant"). (3) new pub backend_common::render::render_indexed_place (fire.rs) returns the indexed LVALUE place-expr by reusing private classify_data_slice (Scalar=>D[idx], SubArray=>D[start..start+sub_len]) — NO textual replace. (4) embedded render_fire: additive arm before the pure-stub indexed arm: if indexed-output AND inputs.empty AND kernel_is_effectful(sidecar.kernel_sig.purity) => per-frame shim region read (alloc_in_region(0,size_of_val(&place)) + dma_wait(0) + null-guarded copy_nonoverlapping into place.as_mut_ptr()); else existing kernels::callee stub UNCHANGED. (5) kernel_is_effectful helper fails loud (ContractGap) on a missing sig. Construction-site sweep: 14 sidecar::KernelSig literals across 8 test files got purity: Purity::Pure (+Purity import); proptest_serde kernel_sig strategy generates both Purity variants; petri_to_events sidecar_kernel_sigs_match_algoir now asserts sig.purity==rk.purity. Focused test ex14_effectful_indexed_input_lowers_to_per_frame_region_read_not_stub: fe(fe_capture/mic_in)+rf(rf_receive/bt_in) emit alloc_in_region/copy_nonoverlapping into datum[..] and do NOT contain kernels::fe_capture(/kernels::rf_receive(; additivity pinned (dsp mix2/denoise STILL emit kernels:: stub). GATE: build OK, clippy clean (-D warnings), just test 0 FAILED, just test-release 0 FAILED, e2e 427/364/0/63/0 byte-identical. Verified emitted fe/src/lib.rs reads mic_in[((frame)*16)..((frame)*16)+16] per frame.
<!-- SECTION:NOTES:END -->
