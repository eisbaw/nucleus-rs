---
id: TASK-0048.10
title: >-
  Doc-correction sweep: reword the count-255 mechanism from 'clock-seeding
  iteration' to Renode SysTick quantization (TASK-0048.08 review P2)
status: Done
assignee:
  - '@claude'
created_date: '2026-05-28 23:42'
updated_date: '2026-05-29 00:03'
labels:
  - tech-debt
  - doc-lie
  - M10
dependencies: []
parent_task_id: TASK-0048
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect P2 from the TASK-0048.08 cycle-244 review gate (read-only). The Renode count fixture deterministically reports 255 (not 256) violations. The CODE is correct (no off-by-one; the Count arm's timing window + '> threshold' compare are byte-identical to the Log arm; reviews GO). But the EXPLANATION of WHY 255 is mechanistically WRONG and propagated into several durable doc sites. WRONG framing: 'iteration 0 is the clock-seeding iteration whose first monotonic_ns() returns 0, so its body resolves to 0 ns.' CORRECTED mechanism: `_check_elapsed = monotonic_ns().wrapping_sub(_check_start)`, so iteration 0's seeded `_check_start=0` is CANCELLED by the subtraction — seeding is NOT the cause. The real cause is Renode's coarse, non-cycle-accurate SysTick stepping: an iteration whose two clock reads fall in the same un-stepped quantum (CVR unchanged -> delta 0 -> elapsed 0) does not exceed 1ns and is not counted; which iteration (and whether any) depends on instruction layout, hence 255 OR 256. Sites to reword (per-string atomic edits, NOT sed-batch — feedback-sed-batch-tracker-md-substitution): (1) justfile renode-embedded-check-count WHY block ~lines 735-747 (tighten the 'clock-SEEDING first iteration' framing); (2) nuc-nucleus/examples/01-elementwise-add/schedules/embedded_check_count.sched.nuc docstring; (3) the 7 embedded_check_count reasons in nuc-nucleus/e2e-matrix.toml if they carry the seeding framing; (4) TASK-0048 + TASK-0049 forward-carry notes (M11 reasoning-from-wrong-model risk — highest harm). Verify with 'just check-narrative-doc-lie' + 'just clippy' + a re-run of 'just renode-embedded-check-count' (still 255-or-256). Recurring class: feedback-implementer-disclosure-mechanism-wrong + feedback-comment-doc-lie-recurring. LOW/docs-only. NB origin: the orchestrator's carried '256 exact, timing-independent' over-claim seeded the error (feedback-orchestrator-narrative-also-wrong) — the implementer empirically corrected the NUMBER (good) but mislabeled the CAUSE.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle plan (docs-only, batched with TASK-0362). Reword count-255 mechanism from 'clock-seeding iteration resolves to 0 ns' to: `_check_elapsed = monotonic_ns().wrapping_sub(_check_start)` CANCELS iter-0's seeded `_check_start=0` (seeding NOT the cause); real cause = Renode coarse non-cycle-accurate SysTick stepping — an iteration whose two clock reads fall in one un-stepped CVR quantum measures delta 0 -> elapsed 0 -> below 1ns -> not counted; which iteration (if any) is instruction-layout-dependent -> 255 OR 256. Sites: (1) embedded_check_count.sched.nuc docstring; (2) 7 e2e-matrix.toml reasons (verify exactly 7, keep identical via replace_all); (3) justfile WHY block ~736 + OK/FAIL echo strings 784/787 (extra sites found beyond brief; '(diagnostic since removed)' on the TOTAL_ITERS/FIRST_ELAPSED_NS cite); (4) TASK-0048 note ~190 + TASK-0049 note ~69 via --append-notes correction (not in-place md edit). NO code/regex/assertion change; e2e/Renode unaffected by construction. Gate: build+clippy+test+check-narrative-doc-lie.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE (docs-only, commit 81019ff; batched with TASK-0362 commit 1a4a6d7). Reworded the count-255 mechanism from the clock-seeding framing to Renode SysTick quantization across all sites. Corrected mechanism (verified accurate, not a new mislabel): `_check_elapsed = monotonic_ns().wrapping_sub(_check_start)` CANCELS iter-0's seeded `_check_start=0`, so seeding is NOT the cause; the real cause is Renode's coarse non-cycle-accurate SysTick stepping — an iteration whose two clock reads fall within one un-stepped SysTick counter quantum (CVR unchanged → delta 0) measures elapsed 0 ns and does not exceed 1ns; which iteration (if any) is instruction-layout-dependent, hence 255 OR 256. Did NOT overstate (SysTick DOES advance, just coarsely). Sites: (1) embedded_check_count.sched.nuc docstring; (2) 7 identical e2e-matrix.toml reasons (verified exactly 7 before+after, still byte-identical, via replace_all); (3) justfile renode-embedded-check-count WHY block + OK/FAIL diagnostic echoes (lines 784/787, EXTRA sites beyond the brief's list — same framing, corrected for consistency; the 25[56] assertion regex was NOT touched), plus appended '(diagnostic since removed)' to the stale TOTAL_ITERS/FIRST_ELAPSED_NS cite; (4) TASK-0048 + TASK-0049 tracker notes via --append-notes additive CORRECTION superseding the earlier seeding framing (per feedback-ac-rewrite-on-done-task: additive, not in-place md edit). Post-commit grep confirms 0 stale 'may resolve to 0 ns'/'NOT wired in this cycle'/'first monotonic_ns()==0' framing in source/config/recipe. Gate: build OK, clippy clean, test 1079/0/3 unchanged, check-narrative-doc-lie OK, justfile parses. e2e/Renode NOT re-run — no codegen/assertion logic changed (embedded_check_count cells are [[skip]] rows; reason strings are never executed); e2e/Renode output unaffected by construction. SUBTLETY: the only surviving 'clock-seeding' substring (justfile:736) is the corrective negation 'The exception is NOT the clock-seeding iteration', which is accurate.
<!-- SECTION:FINAL_SUMMARY:END -->
