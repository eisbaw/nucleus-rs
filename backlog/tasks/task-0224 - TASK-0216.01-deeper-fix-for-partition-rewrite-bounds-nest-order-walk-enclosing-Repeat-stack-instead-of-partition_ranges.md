---
id: TASK-0224
title: >-
  TASK-0216.01: deeper fix for partition-rewrite bounds nest-order (walk
  enclosing Repeat stack instead of partition_ranges)
status: To Do
assignee:
  - mped-architect-impl
created_date: '2026-05-21 20:11'
updated_date: '2026-05-22 13:28'
labels:
  - compiler
  - partition
  - M4
  - tech-debt
dependencies:
  - TASK-0216
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0216 left this as a scope-split: the architect's id-order != nest-order concern is latent today (single-partitioned-iter-var schedules give a 1-element bounds vec), but a future schedule with multiple nested partitioned iter-vars under different IterVar ids could violate IterTile::bounds outer-to-inner convention. The proper fix is to rewrite rewrite_partition_tiles_inner to walk the enclosing Repeat stack instead of iterating partition_ranges (which gives BTreeMap key-ascending = IterVar-id ascending = nest-order-coincidence, not nest-order-by-construction). The current code is documented at the call site (transfer_inject.rs) explaining the assumption + risk.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Modify rewrite_partition_tiles_inner to receive an enclosing-Repeat stack as input (or compute it via a wrapper walk) and build IterTile::bounds in nest order.
- [ ] #2 Add a synthetic-fixture test exercising 2+ nested partitioned iter-vars with non-monotonic IterVar ids (e.g. inner has id 3, outer has id 7) — proves the fix corrects the bounds order even when id-order would mislead.
- [ ] #3 Update transfer_inject.rs site comment to reflect 'fixed by walking enclosing Repeat stack' (removing the current TASK-0216.01 note).
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 45 attempt — BLOCKED (mid-refactor stream timeout, rolled back)

An mped-architect implementer attempted this task on 2026-05-22 and got partway through (modified nucleus/compiler/src/passes/transfer_inject.rs + nucleus/compiler/src/event.rs, ~+82/-34 LoC). The stream hit an API idle-timeout at 27 tool uses / 7.5 min; the partial change broke transfer_fanout_composes_with_partition_sidecar — Wait tiles ended up with empty (0..0) bounds for each worker instead of the partition slices (e.g. WorkerId(1) should get 0..2, WorkerId(2) 2..4, etc.).

Diagnosis: the new approach (walking enclosing Repeat stack) was correctly receiving the stack but did not finish wiring the per-worker partition slice INTO the rebuilt bounds — the new code path returned the tile shape without the slice substitution. The partial commit was rolled back via git checkout; gate restored to baseline 88/70/0/18.

Forward-carried lessons for the next attempt:
1. The existing -iteration path DOES correctly apply the per-worker slice (that's what  pins). The new Repeat-stack walk needs to preserve THAT semantic — the failure mode was substitution-not-applied, not order-incorrect.
2. Start by reading the cycle-45 attempt's transfer_inject.rs diff IN GIT REFLOG (git stash list / git reflog) — there may be useful structural skeleton even though the slice-application was incomplete. (If git reflog has discarded it, start fresh.)
3. Probably want to first ADD the synthetic non-monotonic-IterVar-ids fixture (AC#2), confirm it FAILS on current code (or proves the latent gap), THEN refactor — test-first reduces the risk of an incomplete refactor passing the existing tests but breaking the new one.
4. Task remains LOW priority + latent (no in-tree schedule exercises >1 nested partitioned iter-var). Safe to defer until a real schedule forces the issue or until a future cycle with fresher context tackles it. Cycle 45 burned 0 commits, restored cleanly.
<!-- SECTION:NOTES:END -->
