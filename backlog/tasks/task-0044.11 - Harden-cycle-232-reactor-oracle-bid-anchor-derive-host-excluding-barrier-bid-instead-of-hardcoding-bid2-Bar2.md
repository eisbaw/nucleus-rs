---
id: TASK-0044.11
title: >-
  Harden cycle-232 reactor oracle bid-anchor: derive host-excluding barrier bid
  instead of hardcoding bid=2/Bar2
status: Done
assignee:
  - '@mark'
created_date: '2026-05-28 07:33'
updated_date: '2026-05-28 09:00'
labels: []
dependencies:
  - TASK-0044.08
parent_task_id: TASK-0044
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Cycle-232 architect review P3-1 on TASK-0044.08. The two cycle-232 reactor tests (transpose_15_distributed_rows_event_host_excluding_barrier_mediated, transpose_15_distributed_rows_uds_equiv_tcp) hardcode the post-mediation anchor as struct Bar2 / let bar_2 / barrier_cross(..., 2) — i.e. they assume the host-excluding barrier is SyncTag(2)/bid=2.

The implementer disclosed this coupling but mischaracterized it as 'fails loud on a sync-tag renumber'. The architect verified it actually fails SILENT-PASS in one direction: all 3 host-INCLUDED barriers in 15-transpose/distributed-rows span all four compute workers {host,w0,w1,w2,w3}, so if a future sync-tag renumber moved the host-excluding barrier off bid=2 and a host-included barrier took bid=2, host would already carry a Bar2 shim + barrier_cross over w0..w3 at bid=2 post-mediation -> the post-mediation assertion passes WITHOUT the mediated host-excluding barrier being present. (This is a feedback-implementer-disclosure-mechanism-wrong instance: honest disclosure, wrong mechanism.)

NOT a current defect: the PRE-mediation half (Err(ContractGap), participant-set = WorkerId(1..4), bid-agnostic) carries the real load-bearing bite and stays robust, so the tests are over-specific, not broken. The cycle-231 openmp-rs/poll siblings already use participant-set anchors (Barrier::new(4) / {w0,w1,w2,w3}) which are robust.

Scope: in the two cycle-232 tests, derive the host-excluding barrier's bid from the unmediated barrier_participants (the tag whose participant set lacks the host) and interpolate it into the post-mediation anchor, instead of hardcoding 2. Then the post-mediation half also fails loud under a renumber.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
=== Cycle-234 plan (orchestrator-direct; per memory feedback-spawned-agents-refuse-code-edits this repo implements test-only changes in-thread + delegates only read-only review) ===

GOAL: de-hardcode the two cycle-232 reactor oracle tests so the
post-mediation half ALSO fails loud under a sync-tag renumber (today it
can SILENT-PASS — architect P3-1 on TASK-0044.08), and lift mp-tcp-event's
hardcoded WorkerId(1..4) participant literals to the dynamic derivation
the cycle-233 bufsync sibling already uses (architect P3-2).

KEY FACTS (verified this cycle):
- Event::Sync { participants: BTreeSet<WorkerId>, kind, sync: SyncTag }
  carries the participant set + stable tag DIRECTLY (TASK-0172). The
  host-excluding barrier is the Sync whose `participants` does NOT contain
  the elected host.
- worker_program.rs:520 `let bid = tag.0;` -> emitted `Bar{bid}` /
  `let bar_{bid}` / `wire::barrier_cross(..., {bid})` use bid == SyncTag.0
  (u64). So the post-mediation anchor's "2" is derivable as the
  host-excluding Sync's sync.0.
- Only nesting Event variant is Loop { body }, so the bid-finder must
  recurse into Loop bodies.
- Robust reference pattern: mp-tcp-bufsync tests/multi_worker_emit.rs
  :170-186 (derive host, loop over `used` skipping host, assert
  !contains(host) + each compute worker present) — participant-SET anchor,
  not literals / not hardcoded #N.

STEPS:
1. mp-tcp-event tests/multi_worker_emit.rs
   transpose_15_distributed_rows_event_host_excluding_barrier_mediated:
   a. Elect host + compute `used` BEFORE the pre-mediation assert block
      (currently host is elected after, at ~431). Reuse the unmediated
      projection already built (unmediated_pw).
   b. Replace the WorkerId(1..4) literal asserts (~409-417) with the
      participant-set form: !err_text.contains(host) + for w in used skip
      host assert contains(w).
   c. Add a local recursive fn find_host_excluding_bid(&[Event], host) ->
      Option<u64> over unmediated_pw; derive bid; interpolate {bid} into
      the struct Bar{bid} / let bar_{bid} / barrier_cross(..., {bid})
      anchors (~465, ~475-476) instead of literal 2.
2. mp-uds-event tests/multi_worker_emit.rs
   transpose_15_distributed_rows_uds_equiv_tcp:
   a. Same bid derivation; interpolate {bid} into the struct Bar{bid} /
      let bar_{bid} anchor (~503). (This test has no WorkerId-literal
      hardcode in its pre-mediation half, so only the bid anchor changes.)
3. Update the now-stale comments that assert "SyncTag(2)" / "bid=2" /
   "Bar2" as a hardcoded fact (mp-tcp-event ~457-463, ~471-473;
   mp-uds-event ~433, ~490-494) to present-tense derived form. Comment-doc
   lie audit: the comment must not keep claiming the literal where the
   code now derives it.
4. Gate: nix develop -c bash -c "just build && just clippy && just test &&
   just test-release && just e2e". Baseline to hold: test 1048/0/3,
   test-release 1047/0/3, e2e 280/246/0/34/0. Re-run the two touched tests
   by name to confirm non-vacuous (they still fail loud if the derived bid
   is wrong — sanity: assert the derived bid is Some, not None).
5. Commit per backend (mp-tcp-event, mp-uds-event) matching repo style;
   then tracker close + read-only review gate (qa-test-runner +
   mped-architect parallel) before marking Done.

NON-GOAL: not touching the openmp-rs/poll cycle-231 siblings (already
participant-set robust) nor the bufsync cycle-233 sibling (already
dynamic). Not widening to mp-tcp-event's other ContractGap branch tests.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== cycle 233b forward-carry (architect P3-2) ===
Additional harmonization item for this hardening task: the cycle-232
reactor tests (mp-tcp-event transpose_15_..._mediated, mp-uds-event
transpose_15_uds_equiv_tcp) hardcode the compute-worker IDs as literal
WorkerId(1), WorkerId(2), WorkerId(3), WorkerId(4) (mp-tcp-event
multi_worker_emit.rs ~410-413). The cycle-233 bufsync test instead derives
them dynamically (iterate name_workers, skip the elected host). The dynamic
form is strictly more robust under a future WorkerId renumber. When
hardening the bid anchor here, also lift the hardcoded WorkerId literals to
the dynamic derivation for cross-family consistency.

=== Cycle 234 outcome — DONE (orchestrator-direct; both review GO) ===

Commit: 2b6ce62 (backend tests: TASK-0044.11 cycle 234 — derive
host-excluding barrier bid + participant set).

SCOPE COMPLETE (both items):
- Bid de-hardcode (P3-1): new shared helper
  test_common::host_excluding_barrier_bid(per_worker, host) -> Option<u64>
  derives the host-EXCLUDING barrier's bid (the Event::Sync whose
  participants exclude the elected host; worker_program.rs emits
  bid == SyncTag.0) from the UNMEDIATED projection (post-mediation host
  joins, so no barrier is host-excluding — must read before mediation).
  Both reactor tests interpolate {bid} into struct Bar{bid} / let
  bar_{bid} / barrier_cross(_, {bid}) instead of literal 2.
- WorkerId-literal de-hardcode (P3-2): mp-tcp-event's pre-mediation
  participant assertion rewritten to the participant-SET form (host
  absent + every compute worker present), mirroring the cycle-233
  bufsync sibling. Post-mediation barrier_cross check now loops over
  every derived compute-worker name (strictly more coverage than the old
  ctrl_w0/ctrl_w3 pair). mp-uds-event's pre-mediation half had no
  WorkerId literals, so only its bid anchor changed.

GATE (orchestrator-run + independently re-run by qa-test-runner):
just build OK, just clippy clean (no doc_lazy_continuation), just test
1051/0/3, just test-release 1050/0/3 (-1 = known TASK-0291 dev/release
should_panic divergence), just e2e 280/246/0/34/0 across 2 runs (no
flake), check-textual-replace + check-include-str-coverage OK. 3 new
test-common unit tests pin the helper (host-absent -> Some(bid),
all-inclusive -> None, host-excluding-nested-in-Loop -> Some).

REVIEW GATE: qa-test-runner GO (8/8 arms match baseline, e2e
non-flaky x2). mped-architect GO, ZERO findings — independently verified
(a) the soundness invariant: TASK-0172 one-SyncTag<->one-participant-set
is REAL (assign_sync_tags sync_inject.rs:308 monotonic per-node tags +
barrier_participants mod.rs:245-251 first-writer-wins union), so the
derivation cannot return a host-included bid; (b) the bite is preserved
(Bar{derived_bid} only appears when host joins THAT barrier); (c)
silent-sibling sweep CLEAN — all 7 backends' host-excluding oracles are
participant-set/substring-robust, no remaining Bar<N>/WorkerId(1..4)
hardcodes; (d) doc-lie audit clean; (e) the test_common placement is
real dedup (both backends call it), not cosmetic.

GOTCHAS / SUBTLETIES (forward-carry):
- The derivation is sound ONLY because of TASK-0172 tag-uniqueness. If a
  future change ever let one SyncTag carry two participant sets, this
  helper would silently misattribute. The helper docstring + this note
  pin that dependency.
- The bid MUST be read from the UNMEDIATED projection. Reading it after
  apply_host_mediation_inject returns None (host joined every barrier) ->
  the .expect() fires. The helper docstring states this; both call sites
  derive before mediation.
- host_excluding_barrier_bid returns the FIRST host-excluding Sync found
  across workers in BTreeMap order. Fine for 15-transpose/distributed-rows
  (exactly one host-excluding barrier). A schedule with MULTIPLE distinct
  host-excluding barriers would need a participant-set-keyed lookup — not
  needed today, not built (no in-tree example exercises it).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 234 (commit 2b6ce62): de-hardcoded the two cycle-232 reactor oracle tests (mp-tcp-event + mp-uds-event) per architect P3-1/P3-2 on TASK-0044.08. New shared helper test_common::host_excluding_barrier_bid derives the host-excluding barrier's bid (SyncTag whose participants exclude host) from the unmediated projection; both tests interpolate {bid} into the Bar{bid}/barrier_cross anchors, and mp-tcp-event's participant assertion now uses the derived participant-SET form. Closes the silent-pass risk (a host-included barrier inheriting the old literal bid=2 would have satisfied the Bar2 substring without the mediated host-excluding barrier present). Gate held: test 1051/0/3, test-release 1050/0/3, e2e 280/246/0/34/0. Both review agents GO, zero findings; architect independently verified the TASK-0172 tag-uniqueness soundness invariant + clean silent-sibling sweep across all 7 backends.
<!-- SECTION:FINAL_SUMMARY:END -->
