---
id: TASK-0343.03
title: >-
  Algorithm-level cross-check for overlapping-write accumulator detection
  (TASK-0343 cycle-189 detector follow-up)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-26 23:37'
updated_date: '2026-05-29 18:47'
labels:
  - compiler
  - follow-up
  - accumulator
  - detection
dependencies: []
parent_task_id: TASK-0343
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0343 cycle 189 detects the overlapping-write accumulator pattern PURELY STRUCTURALLY at the codegen layer: per-worker, group Waits by data; for each data with N>=2 Waits where every pair_tile is whole-array, classify all as accumulate (collect_accumulate_waits in backend-common/src/multi_worker_walker/collect.rs).

For every shipped schedule today (08-histogram/distributed is the load-bearing case), the structural pattern is equivalent to the algorithm-level accumulator pattern (LHS appears in RHS: 'histogram[b] <-- bin_inc(histogram[b], ...)'). But an exotic schedule that emits multiple whole-array pushes for non-accumulator semantics (no obvious example today) would be silently mis-combined as element-wise sum.

Scope: add a cross-check to collect_accumulate_waits that consults the algorithm-IR for the LHS-appears-in-RHS shape on the destination data symbol BEFORE classifying as accumulate. The check is a tightening (no shipped cell affected); reject (typed EmitError) if the structural pattern fires but the algorithm-level shape is not accumulator.

Cross-references:
- TASK-0343 (parent): the cycle-189 structural-only landing.
- backend-common/src/multi_worker_walker/collect.rs::collect_accumulate_waits LIMITS docstring.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Implementation Plan (cycle starting 2026-05-29)

Defensive TIGHTENING. The structural overlapping-write-accumulator detector (collect_accumulate_waits, backend-common/src/multi_worker_walker/collect.rs) classifies N>=2 whole-array Waits on one data as element-wise accumulate. For every shipped schedule this equals the algorithm-level accumulator shape (LHS-in-RHS). Add a cross-check that consults the algorithm-IR and FAILS LOUD when structural-accumulate fires but the algo shape is NOT an accumulator.

### Where the check lives (dep direction)
backend-common DEPENDS ON nucleus-compiler. The cross-check needs BOTH collect_accumulate_waits (backend-common) AND the AlgoIR types (nucleus-compiler), so it MUST live in backend-common. New pub fn check_accumulator_consistency in backend-common/src/multi_worker_walker/collect.rs (co-located with the detector it reuses).

### Signature (DataId->name passed explicitly; sidecar has no name map)
NameSidecar keys by DataId but stores ResolvedType, NOT names. The DataId->name map is NameTables.data (driver-built). So:
  pub fn check_accumulator_consistency(algo_ir: &AlgoIR, per_worker: &BTreeMap<WorkerId, Vec<Event>>, sidecar: &NameSidecar, data_names: &BTreeMap<DataId, String>) -> Result<(), EmitError>

### Algorithm-accumulator detection (REUSE existing IrExpr walker)
Walk algo_ir.stmts (recursing IrStmt::For bodies). For each IrStmt::Dataflow { lhs, rhs }: accumulator iff lhs.name is in the set of RHS DataRef names. Reuse the existing IrExpr data-ref collector: promote pipeline.rs::collect_data_refs (private, BTreeSet sink) to pub fn collect_dataref_names in algo/ir.rs, re-export, delegate the pipeline.rs private to it (eliminates one of two identical private siblings). dataflow.rs has a Vec-sink sibling that needs order+dups (CopyEdge) so it stays distinct -- file consolidation follow-up.

### Reuse the structural detector verbatim
Per worker: collect_pair_tiles([events]) then collect_accumulate_waits(events, sidecar, &pair_tiles). For each detected (DataId, SeqTag) whose DataId->name is NOT in the algo-accumulator name set -> Err(EmitError::ContractGap | new precise variant) naming the data symbol.

### Driver wiring (backend-independent, gates once)
driver/src/main.rs after names = NameTables::from_acfg (line ~754), before match backend.as_str() (line ~768). algo via linked.algo (LinkedIR.algo: AlgoIR), per_worker + sidecar already built, names.data is DataId->name. One call, all backends.

### Tests (reject path MUST bite)
- Negative: try a real synthetic schedule first; if structural pattern only arises for genuine accumulators in constructible schedules, fall back to a hand-built unit test (synthetic AlgoIR non-accumulator Dataflow + per_worker with >=2 whole-array Waits) asserting Err. Document which + why.
- Positive: 08-histogram-shaped (LHS-in-RHS, >=2 whole-array Waits) returns Ok.

### Hard constraint
Tightening only. e2e 308/246/0/62/0 MUST stay bit-identical. 08-histogram/distributed (all 4 tier-1) MUST stay accepted. Gate: nix develop --command bash -c "just build && just clippy && just test && just test-release && just e2e" run e2e 2x.

## Implemented (cycle 2026-05-29)

### What landed
- nucleus-compiler/src/algo/ir.rs: NEW pub fn collect_dataref_names(e: &IrExpr, out: &mut BTreeSet<String>) -- the canonical IrExpr data-ref-name walker, promoted VERBATIM from link::pipeline private collect_data_refs. Re-exported via algo/mod.rs. link/pipeline.rs now delegates to it (one private sibling removed). dataflow.rs Vec-sink sibling intentionally NOT collapsed (order+dups for CopyEdge) -> filed TASK-0343.03.01.
- backend-common/src/render/error.rs: NEW EmitError variant AccumulatorShapeMismatch(String) + Display arm. ContractGap reused for the missing-DataId-name defensive path.
- backend-common/src/multi_worker_walker/collect.rs: NEW pub fn check_accumulator_consistency(algo_ir, per_worker, sidecar, data_names) -> Result<(), EmitError>. REUSES the EXACT structural detector (collect_pair_tiles + collect_accumulate_waits) per worker -- no duplicated detection logic. Builds the algo-accumulator name set via collect_accumulator_names (LHS-appears-in-RHS, descends IrStmt::For) using the canonical collect_dataref_names walker. Re-exported via mod.rs.
- driver/src/main.rs: WIRED after let names = NameTables::from_acfg (was line 754), before the --shim check and the match backend.as_str() dispatch. Gates ONCE, backend-independent. Passes &linked.algo, &per_worker, &sidecar, &names.data.
- backend-common/tests/accumulator_cross_check.rs: NEW 6 tests (negative reject + positive accept + 4 guards).

### GOTCHAS / decisions
- EmitError variant: ADDED AccumulatorShapeMismatch (did NOT reuse ContractGap) -- the failure is a detected INCONSISTENCY between two views, not a missing fact; a precise greppable variant is the honest surface. ContractGap reused only for the defensive missing-DataId->name path (that IS a contract regression).
- IrExpr data-ref collector: an existing helper existed (TWO copies, both private: link/pipeline collect_data_refs BTreeSet-sink + link/dataflow collect_dataref_names Vec-sink). REUSED by promoting the BTreeSet one to pub (verbatim) and delegating pipeline to it. Did NOT write a brand-new walker. The Vec-sink one stays (incompatible sink, order-sensitive) -> consolidation = TASK-0343.03.01.
- NEGATIVE test is HAND-BUILT unit inputs, NOT a real .sched.nuc fixture. WHY: the reject needs a data symbol that is BOTH structurally-classified accumulate (>=2 whole-array Waits to one worker) AND non-accumulator (LHS not in RHS). For every constructible shipped schedule the structural pattern only arises for genuine accumulators (LHS-in-RHS is exactly what makes a worker push the FULL array). A real fixture would risk perturbing the locked e2e baseline; AC explicitly endorses the hand-built fallback. Honest limit: the reject path is therefore NOT proven reachable by any real schedule -- it is a defensive guard against an exotic future schedule.
- DataId<->name bridge: NameSidecar keys by DataId but stores ResolvedType (NO names). The DataId->name map is NameTables::data (driver-built); passed explicitly. NO sidecar/wire-contract extension (as required).
- Insertion point precise: driver/src/main.rs immediately after `let names = nucleus_compiler::NameTables::from_acfg(&acfg);`.

### Failure-mode note for siblings (forward-carry to TASK-0343.01/.02/.04)
This check only ADDS a reject path. A false classification by the LHS-in-RHS test is a FALSE REJECT (fail-loud), never a silent miscompile -- safety preserved. Transitive-accumulator precision gap filed TASK-0343.03.02.

### Gate (all inside nix dev shell)
- just build: OK; just clippy: 0 warnings; check-textual-replace + check-include-str: OK
- just test (dev): 1110 passed / 0 failed / 3 ignored
- just test-release: 1109 passed / 0 failed / 3 ignored (1 fewer = the known debug_assert should_panic dev/release divergence)
- just e2e x2: 308/246/0/62/0 BOTH runs (deterministic); 08-histogram/distributed PASS across all 7 backends (incl. all 4 tier-1).

### Follow-ups filed
- TASK-0343.03.01 -- consolidate the two IrExpr data-ref walkers (silent-sibling hygiene).
- TASK-0343.03.02 -- transitive-accumulator precision (false-reject precision gap, not a safety gap).

### Outstanding before close
- Parallel read-only review gate (qa-test-runner + mped-architect) is an ORCHESTRATOR step (implementer subagents refuse edits per memory); to be run by orchestrator before final close.

## Status handoff (implementation complete; awaiting orchestrator review gate)

Committed: 53471ce backend-common+driver: TASK-0343.03 algorithm-level cross-check for overlapping-write accumulator detection.

All code conditions for Done are met (check fn exists + wired in driver + reject path test bites + 08-histogram/distributed stays accepted across all 7 backends + full local gate green: build/clippy/dev-test 1110/release-test 1109/e2e 308/246/0/62/0 x2 + structural checks). NOT flipped to Done: the parallel read-only review gate (qa-test-runner + mped-architect) is an orchestrator step per project CLAUDE.md and feedback-spawned-agents-refuse-code-edits; deferring the Done flip to the orchestrator after that gate. Two follow-ups filed: TASK-0343.03.01 (walker consolidation), TASK-0343.03.02 (transitive-accumulator precision).

=== Orchestrator review-gate close (cycle, phase3-backlog-ralph) ===
Parallel read-only gate: qa-test-runner + mped-architect, both independent, both GO.

VERIFIED-BY-ORCHESTRATOR (re-run, not transcribed): e2e 308/246/0/62/0 (qa ran x2 identical, deterministic); just test 1110/0; test-release 1109/0; clippy clean; the 6 new accumulator_cross_check tests pass INCLUDING the reject path; 08-histogram/distributed PASS on all 7 backends (the load-bearing positive case, unaffected by the tightening); commit scope clean (11 files, no stray cargo-fmt churn).

mped-architect P0 (highest-risk part): the collect_dataref_names promotion from private link::pipeline::collect_data_refs into nucleus_compiler::algo is BEHAVIOUR-PRESERVING — structurally byte-equivalent walker, same BTreeSet sink, all 3 pipeline call sites delegate identically; the link::dataflow Vec-sink sibling is CORRECTLY left distinct (order/dup-sensitive for CopyEdge/propagate_copy_edges) and the consolidation filed as TASK-0343.03.01.
mped-architect P1: the negative test bites NON-VACUOUSLY (traced end-to-end through collect_accumulate_waits: the structural detector genuinely fires on the negative inputs; reject is driven by the LHS-in-RHS discriminator, isolated by an identical-Wait-shape positive test). The check cannot false-reject any shipped schedule; the one precision gap (transitive accumulator) is fail-loud-only, never silent, and filed as TASK-0343.03.02.
mped-architect P2/P3/P4: driver wiring is a uniform, unskippable, hard-error gate before all backend dispatch; EmitError::AccumulatorShapeMismatch is a justified new greppable variant; no doc-lies (5 claims spot-checked true); determinism clean (BTreeMap/BTreeSet only).

REVIEW FOLD-BACK applied in-thread (commit 63673f6, comment-only; build+clippy+e2e re-run green, e2e 308/246/0/62/0): architect P2 nit — the driver comment claimed the gate reads the backend-independent per_worker, but for mp-tcp-event/mp-uds-event it reads the safe_push_reorder-transformed map; clarified that the INPUT differs per backend while the RESULT is backend-invariant (detector is order-insensitive).

NET: scope fully met + gate green + both reviews GO -> flipping to Done.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
TASK-0343.03 DONE (cycle): added backend_common::multi_worker_walker::check_accumulator_consistency (collect.rs) — a driver-layer pre-codegen fail-loud gate that reuses the EXACT structural detector (collect_pair_tiles + collect_accumulate_waits) the backends consume and cross-checks each structurally-detected accumulate DataId against the algorithm-IR LHS-appears-in-RHS shape; mismatch => typed EmitError::AccumulatorShapeMismatch. Wired once, backend-independently, in driver/main.rs before codegen dispatch. NO contract/sidecar extension; no detector duplication. Tightening: e2e 308/246/0/62/0 preserved (no shipped schedule hits the reject; 08-histogram/distributed stays accepted on all 7 backends). 6 unit tests (negative reject path is hand-built inputs since no constructible real schedule triggers structural-accumulate + non-accumulator without perturbing the locked baseline — honest, AC-endorsed). Both reviews GO. Follow-ups: TASK-0343.03.01 (consolidate the 2 dataref walkers), TASK-0343.03.02 (transitive accumulator precision).
<!-- SECTION:FINAL_SUMMARY:END -->
