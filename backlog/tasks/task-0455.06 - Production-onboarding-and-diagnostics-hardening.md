---
id: TASK-0455.06
title: Production onboarding and diagnostics hardening
status: In Progress
assignee: []
created_date: '2026-06-09 21:30'
updated_date: '2026-06-10 18:30'
labels:
  - production
  - docs
  - ux
dependencies: []
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
No external-style user has ever written a .nuc program from scratch; documentation is the thesis + PRD. For production use (including future-self months from now and any collaborator), four bounded pieces, all in-repo (explicitly NOT a website/marketing task):

(a) Getting-started tutorial: write an algorithm + schedule from scratch (not copied from examples/), run it on two backends, diff against a reference — committed and exercised by a just recipe so it cannot rot silently.
(b) Diagnostics UX audit: every user-facing typed error (LowerError, LinkError, EmitError, capability rejection, gate rejection, transfer-directive-missing) names the offending symbol + source location and gives a concrete fix hint. Produce an audit table (error variant -> names symbol? location? fix hint?) and file gaps. Builds on the fuzzy-match did-you-mean tasks TASK-0096 / TASK-0198.
(c) Language-stability note: what surface is stable at v0.1, what may change; the grammar appendix is the contract.
(d) CLI ergonomics audit: nucleus build flags, --emit-pn, exit codes, output-on-success discipline.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Tutorial committed and walked end-to-end by a just recipe (fails if the tutorial program stops compiling)
- [ ] #2 Diagnostics audit table committed; every gap filed as a follow-up task
- [ ] #3 Stability note committed (stable surface vs may-change, grammar appendix as contract)
- [ ] #4 Fresh-environment onboarding (nix develop -> tutorial -> green cell) verified and signed off by the user
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Four bounded pieces in docs/**, justfile (tutorial recipe), nucleus-compiler error-display strings + test pins, driver capabilities/error display:
(a) docs/tutorial/ (OUTSIDE examples/) with from-scratch algo+sched, run on 2 backends + diff; just recipe `tutorial` exercising it end-to-end; verify e2e harness enumeration root does NOT include docs/tutorial.
(b) docs/diagnostics-audit.md table (every user-facing error surface x {names symbol? location? hint? leaks tracker-IDs/debug?}); FIX cheap gaps in-place: strip TASK-NNNN from user-facing Display (keep in comments/nuc_trace!), replace Debug DataId(N) with resolved names where tables available; grep test pins for each changed string + update; FILE expensive span-threading gaps as follow-up tasks.
(c) docs/stability.md (stable@v0.1 vs may-change, grammar appendix = contract).
(d) CLI ergonomics audit (build flags, --emit-pn, exit codes, find_default_capabilities CWD-walk: document + make explicit).
Verify: cargo test -p nucleus-compiler -p nucleus; tutorial recipe green; clippy --workspace; just check-doc-links + check-doc-citation-staleness.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Architecture-review cross-links (2026-06-10), three concrete inputs for the diagnostics audit: (1) parse/lower/link errors carry spans and are well-surfaced (driver main.rs:160-286 — keep), but every post-link pass error is a formatted string with NO source span; the span substrate (Spanned<T>, TASK-0082) never reaches ACFG nodes/XferPlaceholders — thread spans or at least symbol+schedule-line provenance onto pass errors. (2) User-facing messages leak internal tracker IDs: TransferInjectError::SameSetSilentElisionRisk embeds TASK-0324 (transfer_inject/mod.rs:441-451), CumulativeWholeArrayFallback cites TASK-0366; EmitError::ContractGap prints DataId(3)-style debug. Strip tracker IDs from user-facing strings (keep in nuc_trace!). (3) find_default_capabilities walks up from CWD guessing repo layout (driver main.rs:724-748) — surprising outside the repo; make the resolution explicit for external users.

Cycle work (left In Progress for review gate + AC#4 user sign-off):
(a) Tutorial: docs/tutorial.md + docs/tutorial/{prog.algo.nuc,kernels.rs,input.bin,schedules/{naive,split}.sched.nuc} — from-scratch scale-and-bias c=3a+b (NOT copied from examples/). `just tutorial` (scripts/tutorial-run.sh) builds naive->pthreads-sync + split->mp-tcp-bufsync, runs both, cmp the two outputs: PASS byte-identical (256B). Verified output == 3a+b. docs/tutorial is OUTSIDE nuc-nucleus/examples/ so e2e harness (enumerates nuc-nucleus/examples/<NN>/ + e2e-matrix.toml only) does NOT pick it up — no e2e-matrix bookkeeping needed.
(b) docs/diagnostics-audit.md: full table of every user-facing error surface x {names symbol? location? hint? leaks internals?}. FIXED in-place (tracker-IDs stripped from USER-FACING Display/message strings, kept in code comments): contract.rs aggregate msg (TASK-0012), sync_inject UncoveredCrossPartitionReducer (0268/0365/0281), transfer_inject SameSetSilentElisionRisk (0324/0325) + its reason str (0326), CumulativeWholeArrayFallback (0366), sched/ir UnrollUnimplemented (0293), algo/parser until-clause (0341.02.01.03), backend-common mpi_plan check-loop (0045.03). Updated 6 test pins (tests_tiles, transfer_inject x2 blocks, sched_unroll_unimplemented, algo_parser) to assert tracker-ID-FREE + actionable. FILED expensive gaps: TASK-0455.06.01 (span-threading onto post-link pass errors), .02 (DataId->name in CumulativeWholeArrayFallback), .03 (transition-id->construct in Petri gate errors).
(c) docs/stability.md: stable@v0.1 vs may-change, grammar appendix = contract.
(d) docs/cli-reference.md: build flags, --emit-pn, exit codes (0/1 binary), output-on-success discipline, capability CWD-walk. Driver: made find_default_capabilities explicit — NUC_TRACE line on resolution + CWD-walk-naming failure msg.
README.md: added onboarding-docs pointer.
GOTCHA reported separately: (1) concurrent agent mid-flight broke backends build (render_cargo_toml sig) early in cycle — now landed, full build/tests green. (2) just check-doc-links (rustdoc) is PRE-EXISTING RED on HEAD (11 warnings in net_soundness/halo_inference/acfg/transfer_inject mod — NONE in my diff). Not mine to fix.
Verified: cargo test -p nucleus-compiler (all pass) + -p nucleus + -p backend-common; clippy --all-targets clean on 3 crates; just check-doc-citation-staleness + -bare OK; just tutorial PASS.
<!-- SECTION:NOTES:END -->
