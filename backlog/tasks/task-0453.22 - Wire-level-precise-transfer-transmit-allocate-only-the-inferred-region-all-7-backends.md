---
id: TASK-0453.22
title: >-
  Wire-level precise transfer: transmit/allocate only the inferred region (all 7
  backends)
status: To Do
assignee: []
created_date: '2026-06-07 00:04'
updated_date: '2026-06-10 12:55'
labels:
  - rigour
  - compiler
  - backend
  - transfer
dependencies:
  - TASK-0455.07
parent_task_id: TASK-0453
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The REAL comms-reduction work behind P2, separated out after the cycle-2 finding (TASK-0453.02). The transfer INFERENCE already computes a precise per-worker tile (partition.rs compute_partition_bounds_with_dim_prefix), but every backend Push emits {name}.clone() (WHOLE array; event_walker.rs:469) and every worker allocates the array full-shaped (vec![0; N]); the tile is consumed ONLY by receiver-side paste (wait.rs). So neither wire volume nor per-worker memory is reduced today, in affine OR non-affine cases. To actually cut communication: slice the Push source by the producer-side tile (band-shaped payload) AND have each receiver paste a band-shaped recv into a (band-shaped or full-shaped) buffer, coordinated across ALL 7 tier-1 backends + tier-2/3. RISK: the all-zero/wrong-bytes axis-mapping failure class; must stay byte-identical (differential rig) and keep the soundness gate verdict (gate counts tokens not bytes, so structurally safe, but message/marking must be re-validated). LARGE, cross-cutting; own multi-step project, NOT a single ping-pong. Quantify reduction (e.g. matmul blocks2d b: whole K*N -> column band per worker).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Push transmits only the inferred per-worker region (band-shaped payload) for the contiguous-prefix case, across all 7 tier-1 backends
- [ ] #2 Receiver pastes the band-shaped payload correctly; differential rig byte-identical; e2e baseline held
- [ ] #3 Soundness gate verdict unchanged (re-validated); quantified comms reduction on >=1 example
- [ ] #4 Non-prefix (b[k][j] blocks2d) handled precisely OR explicitly kept whole with documented residual
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Raised LOW->HIGH 2026-06-09: pulled into the production-usefulness push as leverage item 2 of epic TASK-0455 (stop whole-array broadcast on every cross-worker edge; precise per-worker regions are already computed by transfer inference and today govern only receiver-side gather correctness). TASK-0455.04 (runtime perf study) depends on this task.

Re-scoped 2026-06-10 per the architecture review: TASK-0455.07 (WireShape chokepoint) lands first and consolidates the >=5 sender emit sites + the render_wait_assign receiver basis behind one derivation; this task then flips WireShape from whole-array to the inferred region (single-site change) + per-backend validation + the coupled receiver _tmp offset-basis switch. Also in scope here per review: host-relay hops must thread the tile; MPI bsend sizing (mpi_plan plan.rs bsend_bytes) shrinks with the payload.

TASK-0455.07 LANDED (2026-06-10) — the WireShape chokepoint now exists at nucleus/backend-common/src/multi_worker_walker/wire_shape.rs. This task flips whole-array -> inferred region by editing ONLY the THREE sender helpers in WireShape (each documents the TASK-0453.22 flip point inline), then coupling the receiver _tmp basis: (1) WireShape::sender_value_expr(name) — today returns name.clone(); narrow to the recv_basis slice expression (in-process .push payload, shared walker). (2) WireShape::sender_encode_expr(name, ty) — today delegates to tcp_plan::encode::encode_expr over the whole symbol; narrow to encode only the recv_basis span (sync-TCP wire). (3) WireShape::sender_byte_len_expr(name) — today core::mem::size_of_val(&name); narrow to the recv_basis span byte length (embedded link_push/link_recv/dma arms). (4) WireShape::extent_elems/extent_bytes — today whole-array product; narrow to recv_basis span (MPI bsend_bytes auto-shrinks, no extra edit). RECEIVER coupling: when senders narrow, render_wait_assign (wait.rs) must index _tmp FROM 0 instead of from the destination offset — the recv_basis variants (Flat/Rows/NestedRows) currently paste _tmp[dst_off..] assuming a full-shaped payload; the flip rebases them to _tmp[0..span]. The sender-expr and receiver-basis are ALREADY derived together in one WireShape so this coupling is one coordinated edit, not 5 independent ones. STILL-OUT-OF-SCOPE residuals to thread in THIS task: (a) host-relay hops forward bytes verbatim (tcp_plan/relay.rs render_relay_phase __relay_payload; event_plan/relay.rs relay_one) — shape-agnostic today, but the relay-DEST receiver basis must use the narrowed span when the producer narrows; (b) apply_host_data_relay_inject / host_mediation must thread the tile to the relay-injected seqs. AC#3 (byte-identity) of THIS task is the differential floor; e2e value-identity verified byte-stable for 02-split-add/05-stencil/08-histogram x 6 backends under 0455.07 (whole-array preserved). Single-worker (single_worker_main) and the embedded host-IO size_of_val drains are NOT cross-worker payloads — leave untouched.

Wave-4 architect review addendum (P2.1, 2026-06-10): TWO payload-size deciders live OUTSIDE the WireShape chokepoint and must be line items of this flip — tcp_plan/relay.rs max_payload_bytes (~:196-215) and event_plan/plan.rs (~:489-507), both whole-array computations driving run.sh SO_SNDBUF/SO_RCVBUF sizing. After the flip they become conservative over-estimates (sound, but they defeat the footprint half of the win and mislead the buffer-sizing story); route them through WireShape extent or the narrowed region. Also from review: mpi_plan/plan.rs bsend sizing currently swallows a WireShape::derive Err via if-let-Ok (P2.2, being fixed pre-flip) — when flipping, make the bsend budget read the narrowed extent and keep the error path loud.
<!-- SECTION:NOTES:END -->
