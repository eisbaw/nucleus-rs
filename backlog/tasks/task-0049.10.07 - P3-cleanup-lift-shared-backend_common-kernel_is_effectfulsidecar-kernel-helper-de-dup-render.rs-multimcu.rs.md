---
id: TASK-0049.10.07
title: >-
  P3 cleanup: lift shared backend_common kernel_is_effectful(sidecar, kernel)
  helper (de-dup render.rs + multimcu.rs)
status: Done
assignee:
  - '@claude'
created_date: '2026-06-04 00:13'
updated_date: '2026-06-04 06:17'
labels:
  - backend
  - cleanup
  - silent-sibling
dependencies: []
parent_task_id: TASK-0049.10
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect review of TASK-0049.10.04 P3-1 (silent-sibling drift). After slice A (render.rs:594 kernel_is_effectful via ctx.sidecar) + slice C1 (multimcu.rs:363 kernel_is_effectful via sidecar), there are TWO byte-identical purity-classification impls: both read sidecar.kernel_sig(kernel).purity == Purity::Effectful with identical ContractGap-on-missing-sig logic, differing only in ctx.sidecar vs sidecar param. Low drift surface (both delegate to the shared NameSidecar::kernel_sig accessor; divergence = the == Effectful compare + error string) but a real silent-sibling recurrence risk on the NEXT purity change (e.g. a third Purity variant or a missing-sig policy change touching one not the other). FIX: lift one helper kernel_is_effectful(sidecar: &NameSidecar, kernel: KernelId) -> Result<bool, EmitError> into backend_common; render.rs (reaches via ctx.sidecar) + multimcu.rs delegate. Non-blocking; defends feedback-silent-sibling-defect on canonical code path.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPLEMENTED (commit 04ceaab). Lifted ONE backend_common::render::kernel_is_effectful(kernel: KernelId, sidecar: &NameSidecar) -> Result<bool, EmitError> with a single generic ContractGap message. render.rs deletes its local impl + calls via ctx.sidecar; multimcu.rs deletes its local impl + calls via sidecar. Removed now-dead Purity imports from both. Pure de-dup, no behavior change. 3 unit tests in render_guard_siblings.rs (effectful->true, pure->false, missing-sig->ContractGap). Gate: e2e 427/364/0/63/0 (one mp-tcp-event cell flaked on transient crates.io DNS during build; cleared on clean e2e re-run, NOT an embedded cell). Tests non-regressive. Renode byte-exact preserved (02 1024B + 14 512B BYTE-EXACT, exit 0).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE. Lifted shared backend_common::render::kernel_is_effectful; de-dups render.rs + multimcu.rs purity classifiers onto one canonical impl (defends feedback-silent-sibling-defect on the next purity change). Commit 04ceaab. Pure de-dup, no behavior change. e2e 427/364/0/63/0 (transient crates.io DNS flake on one mp-tcp-event build cell cleared on re-run); tests non-regressive (+3); Renode 02 1024B + 14 512B BYTE-EXACT, exit 0; cargo doc -p backend-common clean. COLD-SESSION NOTE: the unified ContractGap message is now generic ("effectful-IO lowering") — it no longer cites a specific call-site TASK number, by design.
<!-- SECTION:FINAL_SUMMARY:END -->
