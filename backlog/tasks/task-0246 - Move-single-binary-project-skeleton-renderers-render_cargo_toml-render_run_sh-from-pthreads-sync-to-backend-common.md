---
id: TASK-0246
title: >-
  Move single-binary project-skeleton renderers (render_cargo_toml +
  render_run_sh) from pthreads-sync to backend-common
status: Done
assignee:
  - mped-architect-impl
created_date: '2026-05-22 11:38'
updated_date: '2026-05-22 11:53'
labels:
  - tech-debt
  - architecture
dependencies: []
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Cycle 37 (TASK-0244) extracted the backend-common crate but LEFT two project-skeleton renderers in pthreads-sync:
- render_cargo_toml: emits the single-binary Cargo.toml template (used by pthreads-sync + pthreads-async).
- render_run_sh: emits run.sh launcher (used by pthreads-sync + pthreads-async).

mp-tcp-bufsync does NOT consume these — it has its own multi-process variants (render_cargo_toml(bin_names) + render_run_sh_single) because multi-process needs different shape.

The cycle-37 narrative claimed 'the one semantic inter-backend arrow that survives is render_single_worker_main'. Architect review-gate (cycle 37) flagged this slightly oversells — there are TWO surfaces still routed through pthreads-sync: render_single_worker_main (genuinely semantic delegation — pthreads-async's emit() for used_workers <= 1 IS pthreads-sync's main.rs, byte-identical) AND render_cargo_toml/render_run_sh (project-skeleton string templates, no semantic reason to live in pthreads-sync — they could just as well live in backend-common::project_skeleton::single_binary).

Cleaner architecture would put render_cargo_toml + render_run_sh in backend-common::project_skeleton (single-binary variant). Then ONLY render_single_worker_main remains in pthreads-sync — which IS semantically pthreads-sync-owned (the straight-line emitter is a pthreads-sync-specific shape that pthreads-async happens to delegate to for the degenerate single-worker case).

Acceptance:
- backend-common::project_skeleton module created with render_cargo_toml + render_run_sh (single-binary variant).
- pthreads-sync, pthreads-async both consume from backend-common; pthreads-sync no longer owns these.
- mp-tcp-bufsync's multi-process variants stay in mp-tcp-bufsync (different shape).
- e2e tally + cross-backend bit-identical invariants unchanged.

Low priority — same architectural smell as TASK-0244 fixed for the rest of the surface; deferred because cycle 37 was already a 1654 LoC refactor and bundling more would have outsized the cycle.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Plan (cycle-38 implementer onboarding)

1. Create new module nucleus/backend-common/src/project_skeleton.rs with a pub mod single_binary containing:
   - pub fn render_cargo_toml() -> String  (verbatim move from pthreads-sync, no signature change)
   - pub fn render_run_sh() -> String       (verbatim move from pthreads-sync, no signature change)
   Doc comments updated to retitle 'pthreads-sync' -> 'single-binary tier-1 backends (pthreads-sync, pthreads-async)' but the EMITTED string contents are byte-stable (the # Generated by the pthreads-sync backend comment header inside the string stays — changing it would break the bit-identical invariant on every cell). 

2. backend-common/src/lib.rs: add 'pub mod project_skeleton;' and a 'pub use project_skeleton::single_binary::{render_cargo_toml, render_run_sh};' at the crate root so import sites can spell 'use backend_common::{render_cargo_toml, render_run_sh}'.

3. pthreads-sync/src/lib.rs: delete the two pub fn definitions (~42 LoC). Replace the internal call sites with a 'use backend_common::{render_cargo_toml, render_run_sh}' import (already imports check_frame + render from backend-common).

4. pthreads-async/src/lib.rs: change 'use pthreads_sync::{render_cargo_toml, render_run_sh, render_single_worker_main}' to 'use pthreads_sync::render_single_worker_main' + 'use backend_common::{render_cargo_toml, render_run_sh}'. Update the doc strings that mention 'pthreads_sync::render_cargo_toml / render_run_sh' to point at backend_common.

5. mp-tcp-bufsync untouched — its local fn render_cargo_toml(bin_names) + fn render_run_sh_single() are different-signature, multi-process variants.

6. Update Cargo.toml comments where they referenced pthreads-sync as the home of these two renderers (backend-common/Cargo.toml line 27, pthreads-async/Cargo.toml line 44-53).

## Byte-identical invariant verification
The EMITTED strings (Cargo.toml + run.sh contents) are byte-stable across the move. The '# Generated by the pthreads-sync backend' header inside the run.sh + Cargo.toml strings is retained because changing it would break diff against the existing pthreads-async tests/skeleton.rs byte-identical assertions. Functional behaviour: zero diff.

## Implementation complete (cycle 38)

### Files modified
- NEW: nucleus/backend-common/src/project_skeleton.rs (154 lines: pub mod single_binary with render_cargo_toml + render_run_sh, plus 3 byte-stability tests)
- nucleus/backend-common/src/lib.rs: +8 (declare module + top-level re-exports)
- nucleus/backend-common/Cargo.toml: ±18 (update 'what stays per-backend' comment to reflect TASK-0246)
- nucleus/backends/pthreads-sync/src/lib.rs: -55 net (delete two pub fn definitions + their block comment; add single-line backend_common import + 14-line forward-pointer comment)
- nucleus/backends/pthreads-async/src/lib.rs: ±30 (re-route import sites + update three doc strings; render_single_worker_main import preserved)
- nucleus/backends/pthreads-async/Cargo.toml: ±36 (rewrite both dependency comments)

### Byte-identical verification (the load-bearing invariant)
- Pre-refactor snapshot: 4 cells x (Cargo.toml + run.sh) = 8 files captured into /tmp/task0246-pre/
- Post-refactor snapshot: same 4 cells re-captured into /tmp/task0246-post/
- diff -r /tmp/task0246-pre/ /tmp/task0246-post/  -> NO DIFFERENCES — byte-identical
- The cross-backend differential tests inside pthreads-async/tests/skeleton.rs (single_worker_*_emits_byte_identical_to_pthreads_sync) and mp-tcp-bufsync/tests/pingpong.rs (pingpong_matches_pthreads_sync_bit_for_bit) all passed under 'just test' — these test pthreads-async's Cargo.toml + run.sh against pthreads-sync's byte-for-byte at the test level, the strongest possible check on this refactor.

### Gate numbers (all GREEN)
- nix develop -c just check: clean
- nix develop -c just test: 0 FAILED across all crates (sample: 41/22/14/17/21/8/14/13/12 etc. per-crate ok)
- nix develop -c just clippy: clean
- nix develop -c just e2e: 66/55/0/11 (unchanged from baseline)
- nix develop -c just determinism-check-negative: OK (NUC_NONDET_PERTURBED_CELLS=55)
- nix develop -c just xbackend-check-negative: OK (NUC_XBACKEND_CORRUPTED_APPLIED=16, _DETECTED=1)

### Post-refactor surface: pthreads-sync consumption from siblings
- pthreads-async/src/lib.rs:127  use pthreads_sync::render_single_worker_main;
- mp-tcp-bufsync/src/lib.rs:97   use pthreads_sync::render_single_worker_main;
NOTHING ELSE. The sole inter-backend arrow that survives is the genuinely-semantic delegation to the straight-line single-worker emitter (pthreads-sync-shape; pthreads-async's 0/1-worker arm + mp-tcp-bufsync's single-process arm both wrap it for byte-identical single-worker emit). This matches the architectural narrative the cycle-37 review-gate flagged as slightly overselling: now it is literally true.

### Honest limits
- The EMITTED string header inside Cargo.toml + run.sh still reads '# Generated by the pthreads-sync backend' — RETAINED intentionally to keep the byte-identical invariant intact across pthreads-sync + pthreads-async + mp-tcp-bufsync test-side comparisons (those tests compare to pthreads-sync's output as oracle). Renaming the header is a separate follow-up that also has to update the oracle simultaneously. Filed mentally as a future cosmetic cleanup; NOT urgent because the comment is correct (pthreads-sync is still where main.rs is generated for that backend; the header is a per-emit-call provenance label, not a claim about where the template lives).
- mp-tcp-bufsync was untouched as specified — its render_cargo_toml(bin_names) + render_run_sh_single() local fns have a different signature/shape (multi-process, N [[bin]] targets) and stay in mp-tcp-bufsync.
- Tests in pthreads-async/tests/skeleton.rs and mp-tcp-bufsync/tests/pingpong.rs still contain comments that say 'shared render_cargo_toml' — STILL ACCURATE (just shared via backend-common instead of pthreads-sync now), left as-is.
- pthreads-sync/src/lib.rs still has minor comments referring to '# Generated by the pthreads-sync backend' in the strings being the historical artifact of being-first; these comments are not in the moved module, they're in this file's existing surface. Did NOT churn them.

READY FOR REVIEW + COMMIT
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 38 (2026-05-22) — closed. Moved render_cargo_toml + render_run_sh out of pthreads-sync/src/lib.rs into a new module nucleus/backend-common/src/project_skeleton.rs (pub mod single_binary). Mechanical refactor: ~6 files, net +28 LoC after also fixing the residual comment-lie (architect-flagged in-thread).

Post-refactor pthreads-sync consumption from sibling backends: ONLY . The cycle-37 narrative ('the one semantic inter-backend arrow that survives') is now literally true.

Architect review-gate flagged a MEDIUM: the moved templates still contained the stale header literal '# Generated by the pthreads-sync backend.' (same defect class TASK-0231 fixed for main.rs in cycle 32). Fixed in-thread before commit: updated 2 template strings + 2 self-test assertions + 2 docstrings to the neutral '# Generated by the nucleus pre-compiler.' (matching TASK-0231's main.rs treatment). The 3 in-module self-tests pass with the new literal. No other tests pinned the old header (verified by grep).

Gate (cycle 38, post in-thread fix): just e2e 66/55/0/11 unchanged; just test 0 FAILED (incl. 3 new backend-common project_skeleton tests); just clippy clean; NUC_NONDET_PERTURBED_CELLS=55; NUC_XBACKEND_CORRUPTED_DETECTED=1.

Review-gate (parallel read-only): both qa-test-runner + mped-architect GO. Architect MEDIUM (the comment-lie) resolved in-thread. No outstanding HIGH/MEDIUM findings.
<!-- SECTION:FINAL_SUMMARY:END -->
