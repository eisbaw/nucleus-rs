---
id: TASK-0340.04.04
title: >-
  Sibling-backend sweep: tighten Plan visibility over-widening in pthreads-async
  + mp-uds-event multi_worker (TASK-0340.04.01 generalization)
status: Done
assignee:
  - '@claude'
created_date: '2026-05-29 20:46'
updated_date: '2026-05-29 21:14'
labels:
  - tech-debt
  - hygiene
  - visibility
dependencies: []
parent_task_id: TASK-0340.04
---

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 pthreads-async multi_worker.rs: Plan + all its fields/methods that lib.rs never reaches (lib.rs goes through render_main_rs_multi, not Plan) tightened from pub(crate) to private; render_main_rs_multi stays pub(crate)
- [x] #2 mp-uds-event multi_worker/: same keep-vs-tighten split as TASK-0340.04.01 (KEEP pub(crate): Plan/build/used_workers/worker_name/render_worker_program/render_run_sh; private: per_worker/names/sidecar/host_worker/chan_ids/chan_caps/chan_pairs/pair_tiles/accumulate_waits/barrier_participants/data_name/non_host_workers/ChanId + pub(super) mod.rs methods used only by descendants)
- [x] #3 Pure visibility narrowing: build + clippy clean (no new dead_code/unused); e2e remains 308/246/0/62/0 (live baseline; do NOT trust a copied figure)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Plan (orchestrator in-thread; low-risk compiler-verified visibility hygiene; generalization of TASK-0340.04.01). pthreads-async: single-file multi_worker.rs, only child mod is tests (descendant); lib.rs reaches Plan ONLY via render_main_rs_multi (pub(crate) entry fn) -- NOT Plan directly -- so Plan + ALL its fields/methods are module-internal and can be PRIVATE; render_main_rs_multi stays pub(crate). mp-uds-event: exact twin of mp-tcp-event (same submodule structure encode/relay/walkers/worker_program + same lib.rs Plan usage) -> identical keep-vs-tighten split as TASK-0340.04.01. Compiler is the oracle; relax any item the build rejects. e2e codegen-inert.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
LANDED (commit cc41d2e) + review fold-back (18eb550). Parallel review gate both GO. qa-test-runner: build ok, clippy clean (forced-fresh re-lint of both changed crates -- no dead_code/unused/private_interfaces/unreachable_pub), dev 1116/0, release 1115/0, e2e 308/246/0/62/0 x2 byte-identical; confirmed both crates #[cfg(test)] mod tests still compile/pass accessing the now-private Plan as descendants. mped-architect: verified per-backend correctness adversarially -- pthreads-async render_main_rs_multi signature returns String (no Plan/RingId leak -> full privatization sound, no private_interfaces); mp-uds-event keep-vs-tighten split matches lib.rs reach exactly (incl. render_worker_program grandparent-reach needing pub(crate)); pthreads-sync (struct Plan private @139) + openmp-rs (@98) confirmed already-tight, untouched; diff touches only the 2 named files. One P2 folded back (18eb550): a silent-sibling doc-lie -- the tightening updated the Plan-visibility comment inside multi_worker.rs but missed a structurally-identical stale claim in pthreads-async/src/lib.rs:151 ("The Plan stays pub(crate)"), now false. Fixed + swept all 3 backends for other stale Plan-visibility comments (the two "Plan::build is pub(crate)" comments are accurate). AC#1/#2/#3 all met + independently verified. DONE.

GOTCHA recorded (silent-sibling discipline): a visibility-tightening commit must grep for EVERY comment asserting the old visibility level, not just the one at the definition site -- adjacent files (here lib.rs, two levels from the struct) carry duplicate "X is pub(crate)" narration that goes stale. Pre-commit: grep //.*Plan.*pub\(crate\) across the whole crate before claiming the doc-update complete.
<!-- SECTION:FINAL_SUMMARY:END -->
