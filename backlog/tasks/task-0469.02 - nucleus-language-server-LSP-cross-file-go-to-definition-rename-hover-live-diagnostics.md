---
id: TASK-0469.02
title: >-
  nucleus language server (LSP): cross-file go-to-definition, rename, hover,
  live diagnostics
status: To Do
assignee: []
created_date: '2026-06-13 10:40'
updated_date: '2026-06-13 12:17'
labels:
  - adoption
  - tooling
  - ux
  - diagnostics
dependencies: []
parent_task_id: TASK-0469
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Zero-friction editing inside the class. The shared name table between the two sublanguages makes an LSP unusually tractable and unusually valuable. Capabilities, in priority order: (1) live diagnostics surfacing the existing typed errors (unplaced kernel, unknown name, missing transfer directive, capability mismatch) as the user types; (2) go-to-definition across the algo/sched boundary (schedule place/loop/transfer names jump to the algorithm declaration); (3) hover showing inferred facts from the sidecars (per-worker bands, halo widths, synthesised transfers) — explainability inline; (4) cross-file rename of a kernel or loop variable, which today silently invalidates every schedule that references it (closes the rename-breaks-schedules risk called out in PRD section 13). Reuse the chumsky parser and existing pass machinery via a library entry point rather than re-implementing analysis; prefer a thin LSP shell over the compiler crate. Depends on 0469.01 only for editor packaging, not for analysis.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 LSP binary builds in-workspace and speaks enough of the protocol for diagnostics, definition, hover, rename
- [ ] #2 Diagnostics mirror the compiler typed errors with the same messages and locations (no second error catalogue)
- [ ] #3 Go-to-definition resolves schedule references to algorithm declarations across the two files
- [ ] #4 Rename of a kernel or loop variable updates both files consistently; a stale reference is impossible post-rename
- [ ] #5 Hover surfaces at least one inferred fact (band, halo, or transfer) from the sidecars
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
forward-carried from TASK-0455.06.04: nucleus-compiler now exposes parse_capabilities(&str) (the filesystem-free core of load_capabilities — same parse + Capabilities::validate path, byte-equivalent to disk-load, only cannot emit CapError::Io). The LSP / live-diagnostics path here will want in-memory capability parsing for editor feedback with no disk round-trip; reuse parse_capabilities rather than going through a Path. The driver-side cap_registry.rs (BACKEND_CAPS include_str! of all 10 matrices + accepting_backends) is also a ready substrate for an LSP did-you-mean-backend / quick-fix code action.
<!-- SECTION:NOTES:END -->
