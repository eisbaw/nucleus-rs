---
id: TASK-0340.13
title: >-
  Slice: split nucleus-compiler/src/passes/transfer_inject.rs (6128 LoC) — the
  largest mega-file in the repo, 3x the next slice
status: Done
assignee:
  - '@mark'
created_date: '2026-05-31 18:22'
updated_date: '2026-05-31 23:28'
labels:
  - tech-debt
  - hygiene
  - mega-file
  - transfer_inject
dependencies: []
parent_task_id: TASK-0340
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
transfer_inject.rs is allow-listed in justfile (~line 758) so it does NOT block just ci, but at 6128 LoC it is by far the largest mega-file (next slices: bufsync lib.rs 1997, mp-tcp-event multi_worker.rs 1695, acfg.rs 1440). It grew +303 LoC in TASK-0389 (producer-rank Wait sort) and accretes every gather/scatter/distributed cycle. Natural seams to carve into a transfer_inject/ submodule dir: (1) Pass A inject_in_sequence + splice_pushes_for_waits (per-sequence Wait hoist/dedup), (2) splice_pushes_global + producer_repeat_path / wait_repeat_path (global Push placement), (3) build_waits_for_op + producer_rank_by_data + same_set_elision_unsafe_reason (per-op Wait synthesis + ordering), (4) the halo-strip / N-S-W-E synth block (~4400-4600), (5) the large #[cfg(test)] module. Preserve byte-identity (e2e 371/314/0/57/0) and the check-mega-files allow-list must be UPDATED/removed for the file once split (see TASK-0340.10 for the post-split new-mega-file follow-up pattern). Mirror the TASK-0383 driver-split + TASK-0388 approach. Forward-carried from TASK-0389 QA review (file at 6128 LoC noted as accumulating debt against the eventual split).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0389.01 (DONE): transfer_inject.rs grew from 6128 to ~6382 LoC. New cohesive cluster to keep together when splitting: the FIFO host-Push/worker-Wait ordering machinery — producer_rank_by_data, splice_after_repeat, splice_after_producer (both now carry the append-after-existing-Pushes invariant), the producer-rank sort in splice_pushes_global, and the build_waits_for_op (dst,src,rank,data,seq) sort — plus their unit tests task0389_* and task038901_* (incl. the test-local helpers producer_op/wait_ph/push_textual_order). These form one logical "transfer ordering / FIFO correctness" sub-module; splitting them apart would scatter a single invariant (host Push textual order == producer_rank order == worker Wait order) across files and re-open the silent-sibling risk. The mega-file allow-list entry for transfer_inject.rs is still load-bearing until this split lands.

PLAN (cycle-orchestrator, in-thread): convert nucleus/nucleus-compiler/src/passes/transfer_inject.rs (6389 LoC) into a transfer_inject/ directory module. mod.rs keeps: file docstring+imports, TransferInjectError surface, the 3 shared walk-context types (InjectCtx/State/HoistSink) + impl State, impl ACFGNode/ACFG count helpers, and submodule decls. Submodules (each 'use super::*;', cross-module free fns marked pub(super)): inject (entry+inject_in_node*), sequence (inject_in_sequence+hoist_invariant_waits+splice_pushes_for_waits), ordering (FIFO cluster: producer_rank_by_data+splice_after_*+splice_pushes_global+build_waits_for_op+wait-synth helpers — kept TOGETHER per TASK-0389.01 forward-carry), partition, tiles (cumulative+halo_extend), elision, dedup, halo_strip. Tests split into tests_ordering.rs (FIFO task0389*/task038901*) + tests_tiles.rs (rest). KEY: shared structs stay in mod.rs so descendant submodules+tests reach private fields with NO pub(super) on fields — only free fns need pub(super). Remove transfer_inject.rs from check-mega-files allow-list. Byte-identity gate: e2e must stay 385 (NOT the stale 371 in the task title); 1211 unit-test lines + cargo test must stay green. Also update 2 stale prose citations 'transfer_inject::extend_xfer_tiles_inner' in tests/sidecar_halo.rs to new submodule path.

IMPLEMENTED (cycle-orchestrator, in-thread). transfer_inject.rs (6389 LoC) -> transfer_inject/ dir module, 11 files all <1000 LoC: mod.rs 706 (docstring+TransferInjectError+shared InjectCtx/State/HoistSink+impl ACFGNode/ACFG+decls), inject.rs 573, sequence.rs ~580, ordering.rs 799 (FIFO cluster kept together per TASK-0389.01: producer_rank_by_data+splice_after_*+splice_pushes_global+build_waits_for_op all here), partition.rs 716, tiles.rs 576 (cumulative+halo_extend), elision.rs 536, dedup.rs 158, halo_strip.rs 598, tests_ordering.rs 363 (6 FIFO tests task0389*/task038901*), tests_tiles.rs 866. Technique: shared structs stay in mod.rs so descendant submodules+tests reach private fields with NO field visibility changes; only cross-module free fns marked pub(super); each submodule 'use super::*;'. check-mega-files allow-list entry for transfer_inject.rs REMOVED (gate green, no stale-entry/new-megafile). Also de-line-numbered 5 stale prose line-refs the split would have turned into doc-lies (mod.rs lines 1689-1698/2501/dedup line-stamp 'still in this file'; tests_tiles.rs 1938/1980 — all were ALREADY stale pre-split, re-anchored to symbol/submodule names) + updated 2 sidecar_halo.rs citations to transfer_inject::tiles::extend_xfer_tiles_inner. GATE (orchestrator re-ran, authoritative): build OK, clippy OK (0 warn), test 116 ok/0 fail, test-release 116 ok/0 fail, e2e total:385 pass:328 fail:0 skipped:57 required-fail:0 (UNCHANGED baseline -> byte-identity preserved). fmt: repo is NOT fmt-enforced (clippy is the gate); new files made fmt-clean, pre-existing halo_inference.rs deviations left untouched. PENDING: parallel read-only review gate (qa-test-runner + mped-architect) before Done.

REVIEW GATE (parallel read-only, both GO): qa-test-runner independently re-ran the full gate — build OK, clippy 0/0, test 1206/0/3, test-release 1205/0/3 (1206vs1205 = the documented TASK-0291 debug_assert #[should_panic] delta, 0 fail both), e2e 385/328/0/57/0 byte-exact, check-mega-files OK (no stale entry/new mega-file), all 11 submodule files <1000 LoC. mped-architect GO, no P1/P2: FIFO cluster cohesion intact (producer_rank_by_data/splice_after_*/splice_pushes_global/build_waits_for_op all in ordering.rs + tests in tests_ordering.rs), all claimed docstring rewrites verified accurate, visibility minimal (only inject_transfers pub). Architect found 2 P3 doc-lies my de-line-numbering sweep MISSED (silent-siblings of the same fix); a proactive follow-up grep found a 3rd. All 3 fixed in ab24550 (partition.rs grep-witness '5 hits in this file', inject.rs 'line ~733', ordering.rs '(above)' — all relocation-induced). Pre-existing tangential doc-staleness (acfg/build.rs prose citations) filed as TASK-0340.14, not scope-crept. Commits: 1b3fb20 (split) + ab24550 (fold-back).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE. Split nucleus/nucleus-compiler/src/passes/transfer_inject.rs (6389 LoC, the repo's largest file) into a transfer_inject/ directory module: 11 files all <1000 LoC (mod.rs 696, ordering 799 [FIFO cluster, kept whole per TASK-0389.01], partition 716, halo_strip 598, sequence 580, tiles 578, inject 573, elision 536, dedup 158, tests_ordering 363, tests_tiles 868). Technique: shared walk-context structs stay in mod.rs so descendant submodules reach private fields with zero field-visibility changes; only cross-module free fns are pub(super); each submodule 'use super::*;'. check-mega-files allow-list entry removed. Byte-identity preserved (e2e 385/328/0/57/0 unchanged, qa-verified). Also de-line-numbered 5+3 stale prose location-refs the split would have made hard doc-lies. Both reviewers GO.
<!-- SECTION:FINAL_SUMMARY:END -->
