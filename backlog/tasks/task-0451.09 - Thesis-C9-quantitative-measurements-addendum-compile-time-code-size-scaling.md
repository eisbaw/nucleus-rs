---
id: TASK-0451.09
title: >-
  Thesis C9: quantitative measurements addendum (compile-time, code-size,
  scaling)
status: Done
assignee: []
created_date: '2026-06-05 20:01'
updated_date: '2026-06-06 07:01'
labels:
  - thesis
  - paper
  - results
  - measurements
  - deferred
dependencies:
  - TASK-0451.04
parent_task_id: TASK-0451
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
DEFERRED / LOW (filed per the planning decision: existing correctness/coverage results land first in C4; fresh quantitative measurements come later). Gather and report NEW objective metrics by running the toolchain: per-backend generated-code size (LoC / bytes), compiler/codegen wall-time per (example x schedule x backend) cell, schedule x backend matrix scaling, and any other cheap-to-collect quantitative signal. Add a Results subsection (and update Future-work to mark it done). HONEST FRAMING: the project disclaims performance as a goal, so present these as descriptive engineering characteristics (cost of genericity, codegen footprint), NOT as speed/scaling claims. Methodology must be reproducible (Nix-pinned, deterministic). Review gate per C7/C8 lenses for the new numbers. DoD: measurement methodology documented + reproducible; numbers codebase-verified; results subsection builds clean.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Quantitative-characteristics addendum landed GO. Measured from the live toolchain, framed as descriptive engineering characteristics (cost of genericity) NOT speed claims; runtime speed of emitted code stays out of scope. New ch9 sec:res-quant + 3 tables: (1) backend footprint via tokei — shared backend-common 5405 + middle-end 19385 vs thinnest tier-1 backend 161 lines (quantifies thin-backend claim); (2) generated-code footprint — shared-mem ~70-94 lines, message-passing ~716-1607 (wire+per-worker binaries); generated code O(program text), loop-structured (16x16 and 1024x1024 stencil both = 18 lines); (3) analysis-net scaling — gate net expanded over iteration space (~2 nodes/firing), matmul 16^3->8199 nodes; honest limitation: gate cost O(firings), would not scale to billions, symbolic looped analysis = future work; compile ~6-10ms/cell startup-dominated/indicative. Methodology reproducible in Appendix D sec:repro-quant. sec:fw-quant revised (static chars done; runtime study + symbolic gate open). Review: paper-accuracy re-ran EVERY measurement vs live codebase — all 13 footprint rows + 7 gen-code rows+bytes + 5 net-node counts EXACT; paper-correctness found 1 WRONG (net=2x firings not 1x, grep counts places+transitions) fixed + minor tightenings. Build exit 0, 118pp, zero undefined refs, biber clean, tables visually verified clean. Epic TASK-0451 now FULLY complete (all 10 children Done).
<!-- SECTION:NOTES:END -->
