---
id: TASK-0431.01
title: >-
  Strengthen 20-index-cast-permute to a non-identity permutation (discriminating
  value-oracle)
status: Done
assignee:
  - '@me'
created_date: '2026-06-03 03:32'
updated_date: '2026-06-03 05:46'
labels:
  - testing
  - scatter
  - oracle-strength
  - cycle-248
dependencies: []
parent_task_id: TASK-0431
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0431 cycle-248 architect P3-1. The 20-index-cast-permute example (build-clean witness for a bare iter-var i64 arg to an i32-param pure index kernel) uses IDENTITY idx/pass, so reference.bin == input.bin. The AC#2 load-bearing proof (build-clean across backends: pre-fix kernels::idx(i) is E0308) + the render-string unit pin DO carry the proof, so the AC is genuinely met. BUT the value-oracle is weak: a backend that copies input->output WITHOUT evaluating idx/pass would also byte-match. Strengthen idx to a non-identity bijection (e.g. reversal idx(i)=N-1-i — still a permutation so PRD 6.2.1 single-assignment holds) and recompute reference.bin through it, so the oracle ALSO catches a backend that emits the cast yet mis-evaluates the index. One-line kernel + reference + README change; re-run just e2e (expect the 7 cells still pass, now value-discriminating). Pointer: nuc-nucleus/examples/20-index-cast-permute/ (kernels.rs idx, reference/src/main.rs, README.md).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CYCLE 250 (orchestrator in-thread impl — trivial mechanical change, per loop "do not spawn a subagent per trivial task"; review gate still run).

CHANGE: strengthened the 20-index-cast-permute value-oracle from IDENTITY (idx(i)=i -> reference.bin == input.bin, satisfiable by a copy-only backend) to REVERSAL (idx(i)=N-1-i -> reference.bin == input REVERSED, value-discriminating). Files: kernels.rs (idx body (N as i32)-1-i), reference/src/main.rs (oracle out[N-1-i]=in[i], regenerated reference.bin via just regen-references), prog.algo.nuc + README.md + schedules/naive.sched.nuc (comments). pass stays identity. Also refreshed the stale in->src naming note to cite the TASK-0433 front-end reject (pre-TASK-0433 it emitted let mut in and failed rustc; now rejected at source). Commits: ccad86c (impl) -> e72e6b3 (NO-GO fold-back).

VERIFY (orchestrator-observed): reference.bin == reversed(input.bin) True, == input False (input non-palindromic). e2e 420/363/0/57/0 UNCHANGED (same cell count, stronger oracle); narrowed 20-index-cast-permute 7/7 PASS across all tier-1 backends vs the reversed oracle (each backend genuinely evaluates idx(i)=N-1-i, not copying); doc-citation + narrative-doc-lie fences green.

REVIEW GATE (cycle 250 parallel read-only): qa-test-runner GO + mped-architect initial NO-GO -> resolved.
qa NUMBERS (re-run): build OK; clippy clean; e2e 420/363/0/57/0 zero fail; 7/7 cells PASS; reference.bin == reverse(input) confirmed independently; full just ci EXIT 0 (negative arms bit correctly). qa P3 (doc-only): stale identity wording in prog.algo.nuc.
architect: GO on oracle math (idx(i)=N-1-i genuine bijection, no i32 overflow), kernel<->oracle agreement (oracle computes out[N-1-i] directly, independent of idx/pass), fixture discrimination (verified empirically), scope (only the one example dir; render-layer unit tests untouched), TASK-0433-note accuracy. P1 NO-GO: INCOMPLETE comment sweep — 6 surviving identity lies (prog.algo.nuc header/L23/L63/L74[WRONG FORMULA idx(i)=i]/L84 + naive.sched.nuc:3 identity+stale-in).
RESOLUTION (commit e72e6b3, orchestrator in-thread): all 6 enumerated lines corrected to reversal / idx(i)=N-1-i / src; full re-grep confirms only legitimate residual mentions remain (pass IS identity; contrastive "vs an identity oracle" describing the rejected weak alternative); 7/7 cells still PASS; doc fences green. NO-GO cleared by orchestrator line-by-line re-verification (proportionate for a precisely-enumerated comment-only finding). qa P3 subsumed by the same fix.

GOTCHA forward-carried: the comment sweep on an identity->reversal example must hit EVERY mention (header line, algorithm block, data-decl comment, KERNEL DECLARATION comment [carries the actual formula idx(i)=i -> wrong], for-loop comment, AND the separate schedule file) — partial sweeps leave a self-contradicting source + a wrong formula, the project #1 doc-lie defect class. The schedule .sched.nuc is a separate file easily missed.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE cycle 250. 20-index-cast-permute oracle strengthened identity->reversal (idx(i)=N-1-i); reference.bin now input-reversed and value-discriminating (a copy-only backend mismatches). e2e 420/363/0/57/0 unchanged, 7/7 PASS vs reversed oracle, just ci exit 0. Review: qa GO; architect NO-GO (incomplete comment sweep, incl a wrong idx(i)=i formula in the kernel decl comment) -> resolved e72e6b3, re-verified. Commits ccad86c + e72e6b3.
<!-- SECTION:FINAL_SUMMARY:END -->
