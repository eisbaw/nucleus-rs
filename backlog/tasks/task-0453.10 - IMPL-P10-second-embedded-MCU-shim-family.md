---
id: TASK-0453.10
title: 'IMPL P10: second embedded MCU shim family'
status: Done
assignee: []
created_date: '2026-06-06 22:55'
updated_date: '2026-06-07 09:49'
labels:
  - rigour
  - embedded
  - backend
dependencies: []
parent_task_id: TASK-0453
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Addresses L7/W3 (ch10:203-212; ch11:142-148). Add a second microcontroller-family shim against the committed hardware-abstraction trait, broadening hardware-portability evidence from one family to several. Bounded but real work (months/family). Residual: shim sprawl remains in principle unbounded - this is containment evidence, not elimination.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A second MCU-family shim compiles+runs at least one example byte-exact under its simulator
- [x] #2 Hardware-abstraction trait shown sufficient for two families
- [x] #3 Shim-sprawl-unbounded residual kept documented
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CYCLE-10 STEP-0 de-risk GO (2026-06-07): nRF52840 (Nordic Cortex-M4F, thumbv7em-none-eabihf — SAME Rust target already in .#embedded) is a genuinely-distinct second family. Renode 1.16.1 bundles platforms/cpus/nrf52840.repl + UART.NRF52840_UART (easyDMA). PROVED a minimal no_std nRF firmware emits a UARTE EasyDMA (TXD.PTR/MAXCNT/STARTTX/poll ENDTX) sentinel captured BYTE-EXACT by Renode headless (cruft/nrf-smoke-spike). Linker GOTCHA: FLASH@0x0 needs `-C link-arg=--nmagic` (page-align bumps the vector table off 0x0 otherwise; STM32 @0x08000000 dodged it). EasyDMA register interface is genuinely different from STM32 USART DR/ISR -> exercises the NucleusShim trait as a real portability seam. Implementing single-worker `--shim nrf52840` BIN path (ex1 byte-exact); multi-MCU nRF kept OUT (residual).

CYCLE-10 GO (2026-06-07): implemented second MCU-family shim `--shim nrf52840` (Nordic nRF52840 Cortex-M4F, UARTE EasyDMA) on the single-worker embedded-pattern BIN path, alongside the original `--shim stm32h7`. SHARED seam: NUCLEUS_SHIM_SRC trait + generic run<S> + kernel extraction reused unchanged; only the concrete shim (new src/skeleton/shim_nrf52840.rs NRF_UARTE_SHIM_SRC), memory.x (FLASH 0x0/1024K + RAM 0x20000000/128K, input window @0x20020000), and one linker flag (--nmagic, FLASH@0x0 vector-table fix) differ. Threaded ShimTarget{Stm32h7,Nrf52840} through emit_bin->emit_one_worker_bin->render_bin_main(BinSpec); nRF multi-worker rejected typed (UnsupportedFeature) before any write. VERIFIED byte-exact in Renode (just renode-embedded-nrf-all): ex1/ex5 1024B, ex9 64B vs reference.bin. STM32 path re-verified byte-exact (no regression). Gate GREEN: build/clippy/test 1452/test-release 1450/e2e 497-428-0-69-0 (unchanged, e2e-inert)/check-embedded(+nRF arm)/3 structural fences. Split bin.rs+bin_shape.rs to stay <1000 LoC. qa GO + architect GO (no P1; P2-1 reproducibility recipe added, P2-2 timing/diagnostic-path-mirrored-not-runtime-verified honestly documented + follow-up TASK-0453.10.01). EasyDMA TX register interface genuinely distinct from STM32 USART DR/ISR => trait proven as real portability seam. RESIDUAL kept: shim sprawl in-principle unbounded; second family cheap ONLY because shared Cortex-M target + bundled Renode model; nRF multi-MCU is out-of-tree.
<!-- SECTION:NOTES:END -->
