---
id: TASK-0049.09
title: >-
  ex14 embedded_multimcu_sync LIB cross-compile REGRESSED: TASK-0343.03
  accumulator cross-check rejects mic_in multi-Wait gather
status: To Do
assignee: []
created_date: '2026-05-31 11:06'
labels: []
dependencies: []
parent_task_id: TASK-0049
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRE-EXISTING regression found during TASK-0049.05 review (reproduced on clean master with 0049.05 changes stashed). `just check-embedded` ex14 (14-hearing-aid) arm now FAILS at the `nucleus build` step (driver transfer_inject), NOT cross-compile: "accumulator cross-check error: data symbol `mic_in` is structurally classified as an overlapping-write accumulator (>=2 whole-array Waits) but the algorithm-IR shows it is NOT an accumulator". This is the TASK-0343.03 defensive accumulator tightening firing on ex14 embedded_multimcu_sync (mic_in gathered to the host via multiple whole-array Waits with non-accumulator semantics). It post-dates TASK-0049.06 (which marked the ex14 3-worker LIB cross-compile DONE) — so a prior Done now rests on a red gate (silent-baseline-rot, project-e2e-gate-trust-caveats). 02-split-add LIB + the two single-worker check-embedded arms remain GREEN. Root-cause options: (a) the accumulator detector mis-classifies a fan-in GATHER (multiple producers -> host) as an overlapping-write accumulator; the cross-check correctly refuses to silently sum, but the schedule shape is legitimate gather, not accumulate; (b) ex14 embedded_multimcu_sync schedule needs adjusting. BLOCKS: TASK-0049.06 gate health + ex14 multi-MCU BIN value-correctness (the value-correctness arm of TASK-0049.05 / TASK-0049.02).
<!-- SECTION:DESCRIPTION:END -->
