---
id: TASK-0042.03
title: >-
  M4 sub-task: Example 9 (producer/consumer pipe) — algorithm + naive +
  pipelined + kernels.rs + reference impl + input.bin + reference.bin
status: To Do
assignee: []
created_date: '2026-05-21 13:10'
labels:
  - M4
  - examples
dependencies:
  - TASK-0134
parent_task_id: TASK-0042
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD §9 row 9: 'Producer/consumer pipe — Pipelining, buffer depth, async transfer'. Smallest self-contained fixture for the M4 capability set. Two-stage pipeline: producer kernel writes a stream of records, consumer kernel reduces/transforms. Schedule naive.sched.nuc on one worker; pipelined.sched.nuc on two workers with transfer X: async, buffer=N, notify=event + loop V: pipeline=D.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 examples/09-producer-consumer/{prog.algo.nuc, kernels.rs, reference/Cargo.toml, reference/src/main.rs, input.bin, reference.bin, README.md, schedules/{naive,pipelined}.sched.nuc} all present and self-consistent.
- [ ] #2 Numeric type i32 throughout (PRD §13 deterministic-integer-only); kernels.rs wrapping_add/wrapping_mul as per CNN example precedent; reference/ is std-only with independent implementation (policy §2).
- [ ] #3 Three-way independent agreement: kernels.rs <-> reference/ <-> reference.bin sha256 documented in README + verified at commit time.
- [ ] #4 On the EXISTING (sync) pthreads-sync backend with naive.sched.nuc, the example must build and produce reference-matching output bit-identical. (The pipelined.sched.nuc cell is blocked on TASK-0042's pthreads-async sibling and will enter [[required]] when that lands.)
<!-- AC:END -->
