---
id: TASK-0042.03
title: >-
  M4 sub-task: Example 9 (producer/consumer pipe) — algorithm + naive +
  pipelined + kernels.rs + reference impl + input.bin + reference.bin
status: Done
assignee:
  - mped-architect-impl
created_date: '2026-05-21 13:10'
updated_date: '2026-05-22 10:25'
labels:
  - M4
  - examples
dependencies:
  - TASK-0134
parent_task_id: TASK-0042
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD §9 row 9: 'Producer/consumer pipe — Pipelining, buffer depth, async transfer'. Smallest self-contained fixture for the M4 capability set. Two-stage pipeline: producer kernel writes a stream of records, consumer kernel reduces/transforms. Schedule naive.sched.nuc on one worker; pipelined.sched.nuc on two workers with transfer X: async, buffer=N, notify=event + loop V: pipeline=D.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 examples/09-producer-consumer/{prog.algo.nuc, kernels.rs, reference/Cargo.toml, reference/src/main.rs, input.bin, reference.bin, README.md, schedules/{naive,pipelined}.sched.nuc} all present and self-consistent.
- [x] #2 Numeric type i32 throughout (PRD §13 deterministic-integer-only); kernels.rs wrapping_add/wrapping_mul as per CNN example precedent; reference/ is std-only with independent implementation (policy §2).
- [x] #3 Three-way independent agreement: kernels.rs <-> reference/ <-> reference.bin sha256 documented in README + verified at commit time.
- [x] #4 On the EXISTING (sync) pthreads-sync backend with naive.sched.nuc, the example must build and produce reference-matching output bit-identical. (The pipelined.sched.nuc cell is blocked on TASK-0042's pthreads-async sibling and will enter [[required]] when that lands.)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
cycle-34 implementer (Opus 4.7) progress:

Files created under nuc-nucleus/examples/09-producer-consumer/:
  - prog.algo.nuc        (N=16; produce, transform pure kernels; load_input/save_output effectful)
  - kernels.rs           (produce = s.wrapping_mul(3); transform = r.wrapping_mul(7).wrapping_add(r))
  - schedules/naive.sched.nuc    (single host worker, no transfers)
  - schedules/pipelined.sched.nuc (3 workers: host, producer, consumer; transfer stream : async, buffer=4, notify=event; loop n : pipeline=4; seeds/result sync)
  - reference/Cargo.toml + src/main.rs (std-only, independent two-stage re-implementation)
  - input.bin            (64 bytes, seeds[i] = i+1 for i in 0..16)
  - reference.bin        (64 bytes, result[n] = (n+1)*24)
  - README.md            (with pinned sha256s and how-to-verify)

Pinned hashes:
  input.bin     sha256: 77d735ce838418aa151bd96b5b1e78ee63860892e0a95c00fe34178442be9b07
  reference.bin sha256: 354988c3e7d44caf339c24cbfa11ef1534e337751d99d7a4475202f2d6ccc86e

e2e-matrix.toml edits:
  - added "09-producer-consumer" to runnable_examples (line 65)
  - 3 new [[required]] cells: 09/naive × {pthreads-sync, mp-tcp-bufsync, pthreads-async} @ M3
  - 1 new [[required]] cell:  09/pipelined × pthreads-async @ M4 (the M4 headline)
  - 2 new [[skip]] cells:     09/pipelined × {pthreads-sync, mp-tcp-bufsync} @ M4 (capability mismatch cited verbatim)

nucleus/e2e/src/main.rs edits (required to land the first M4 cell):
  - real_manifest_has_no_coverage_gaps_at_every_milestone now iterates None..M4
  - required_counts_strictly_grow_per_milestone now asserts M1<M2<M3<M4 and M4 == full set; updated doc comment to reflect that M4 is the current top tier (with TASK-0042.03 cited as the trigger)

Verification gate (full sweep, all green):
  just e2e:        total: 60   pass: 51   fail: 0   skipped: 9   required-fail: 0
                   (was 54/47/0/7; +4 required pass, +2 skip)
  just test:       0 FAILED across all crates
  just clippy:     clean (no warnings under -D warnings)
  just determinism-check-negative: OK; NUC_NONDET_PERTURBED_CELLS=51 (>=1)
  just xbackend-check-negative:    OK; NUC_XBACKEND_CORRUPTED_DETECTED=1

Three-way independent agreement spot-check (n=0, n=15):
  n=0:  seeds=1  -> produce=3   -> transform=24  = 0x18  (first word of reference.bin: 18 00 00 00 ✓)
  n=15: seeds=16 -> produce=48  -> transform=384 = 0x180 (last word of reference.bin:  80 01 00 00 ✓)
  Closed form: result[n] = (n+1) * 24 (the reference does NOT use this form, by design).

AC closure:
  AC#1 (files present + self-consistent) — all 9 files present.
  AC#2 (i32, wrapping_*, std-only reference) — pinned in kernels.rs + reference/src/main.rs.
  AC#3 (three-way agreement, sha256 documented + verified) — README pins both sha256s; e2e diff is bit-identical against reference.bin across all 4 required cells.
  AC#4 (sync backend smoke test for naive) — pthreads-sync × naive PASS bit-identical. PLUS the pipelined cell, originally blocked at AC-write time, now PASSES on pthreads-async (TASK-0042.01 multi-worker emit landed cycles 26-31).

Note for review: tagged the pipelined headline cell as M4 (the milestone whose acceptance gate the cell first satisfies), which made it the first M4-tagged required entry in the manifest. Bumped the two e2e tests that enshrined "M3 is the current top tier" to "M4 is the current top tier" with strict-growth M1<M2<M3<M4. This is the honest milestone tag per the e2e-matrix.toml header convention (the tag is *the PRD §11 milestone whose acceptance gate owns this cell*) and matches the parent TASK-0042 labelling itself (M4).

READY FOR REVIEW + COMMIT.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 34 (2026-05-22) — closed. Created nuc-nucleus/examples/09-producer-consumer/ (9 new files): prog.algo.nuc, kernels.rs, schedules/{naive,pipelined}.sched.nuc, reference/{Cargo.toml, src/main.rs}, input.bin (64B), reference.bin (64B), README.md.

Algorithm: 2-stage pipeline. produce(s) = s.wrapping_mul(3); transform(rec) = rec.wrapping_mul(7).wrapping_add(rec). N=16. seeds[i] = i+1 (avoids degenerate zero-seed masking dropped-multiplier bugs).

Three-way independent agreement verified: kernels.rs <-> nucleus emit <-> reference/ all produce reference.bin (sha256 354988c3e7d44caf339c24cbfa11ef1534e337751d99d7a4475202f2d6ccc86e). README pins both input.bin (77d735ce8384...) and reference.bin sha256s.

e2e-matrix.toml: added 09-producer-consumer to runnable_examples; added 4 [[required]] cells (3 naive × {sync, mp-tcp, async} at M3 + 1 pipelined × pthreads-async at M4 — the first M4-tagged required cell ever) + 2 [[skip]] for pipelined × {sync, mp-tcp} (capability mismatch — async + buffer=4 + notify=event).

nucleus/e2e/src/main.rs: extended 2 milestone-invariant unit tests to acknowledge the new M4 tier. Cycle-34 review-gate caught a doc/assertion lie (comment said 'M3 <= M4 not strict' while assertion was strict m3 < m4); fixed in-thread before commit — comment now matches the strict assertion since the first M4 cell IS present.

Tally: 60 / 51 / 0 / 9 (was 54 / 47 / 0 / 7). +6 cells total (+4 pass, +2 skip). The cross-backend three-way differential now covers Example 9 naive on all three backends bit-identical; the pipelined headline exercises pthreads-async's distinctive async + buffer=4 + notify=event + pipeline=4 capability surface — the SECOND fixture in the matrix exercising this surface (alongside 13-cnn-inference/pipeline_parallel).

Gate (cycle 34): 0 FAILED test suites; clippy clean; e2e 60/51/0/9; NUC_NONDET_PERTURBED_CELLS=51; NUC_XBACKEND_CORRUPTED_DETECTED=1.

Review-gate (parallel read-only): both qa-test-runner + mped-architect GO. Architect flagged the M3<=M4 doc lie (MEDIUM, fixed in-thread) and reminded that AC#4 evidence (naive × pthreads-sync PASS bit-identical) should be captured in writing — done above ('Tally' line) + run output verified live in the close transcript.
<!-- SECTION:FINAL_SUMMARY:END -->
