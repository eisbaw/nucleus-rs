---
id: TASK-0048.06
title: >-
  embedded-pattern bin: verify input.bin byte-layout == EventList load order
  assumption when generalising the injected-input path to ex5/ex9
status: Done
assignee: []
created_date: '2026-05-28 14:29'
updated_date: '2026-05-28 21:37'
labels:
  - M10
  - backend
  - validation
dependencies: []
parent_task_id: TASK-0048
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Forward-carried gap from TASK-0048.02. The TASK-0048.02 real-input mechanism (B, Renode-injected axiSram region) relies on an ASSUMPTION: input.bin is the concatenation of the effectful-load arrays IN EventList load order, so the firmware's Usart1Shim cursor (advancing by each load's byte length) reads the right slice per load. For ex1 this is exact (a=words[0..N] loaded first, b=words[N..2N] loaded second). ex5 (single load load_image -> img_in) and ex9 (single load load_input -> seeds) are single-load so the cursor trivially starts at 0 and consumes the whole region — but this MUST be verified per example, not assumed, when TASK-0048.03 generates+runs their firmware. If any future M10 example has multiple loads whose input.bin layout does NOT match EventList order, the cursor reads the wrong slice and the reference.bin cmp will (correctly) fail. AC: (1) when TASK-0048.03 wires ex5/ex9, confirm each example's input.bin byte layout matches the EventList load order before trusting the cmp; (2) if a mismatch is possible, replace the implicit cursor with an explicit per-load offset derived from a stable source (sidecar/Event metadata), not kernel-body parsing.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle 240 (TASK-0048.03) confirmation: ex5 (single effectful load load_image -> img_in, input.bin=1024=one i32[H][W] array) and ex9 (single effectful load load_input -> seeds, input.bin=64=one i32[N] array) BOTH have exactly ONE effectful load, so the Usart1Shim cursor trivially starts at 0 and consumes the whole injected region — assumption holds trivially for both (architect-confirmed against generated load order). ex1 (input.bin=2048 = a[0..N] ++ b[N..2N]) remains the ONLY multi-load case so far. This task stays OPEN: the concatenation-order risk is real for any FUTURE M10 example with >1 effectful load; the remediation (explicit per-load offset from stable Event/sidecar metadata) is still wanted there.

Cycle 241 (2026-05-28) — VERIFICATION COMPLETE, closing Done. Part (1) 'confirm each M10 example's input.bin layout == EventList load order' verified EMPIRICALLY this session (not trusting the cycle-240 note):
- ex1 (01-elementwise-add): the ONLY multi-load case. TWO effectful loads in source order — a <-- load_input() (algo:48), b <-- load_input_b() (algo:49); effectful ordering is preserved (never reordered), so EventList load order = (a,b). input.bin=2048=2*256*4 => a=words[0..256], b=words[256..512]. PROOF (cheap empirical verification > narrative): re-ran `just renode-embedded 01-elementwise-add` — generated firmware injected input.bin, captured 1024 bytes over USART1, BYTE-EXACT identical to reference.bin, exit 0. A wrong cursor slice would corrupt the cmp; it passes, so the cursor read a then b correctly.
- ex5 (05-stencil): single effectful load img_in <-- load_image() (algo:67), input.bin=1024=one i32[H][W]. Cursor starts at 0, consumes whole region. Trivial.
- ex9 (09-producer-consumer): single effectful load seeds <-- load_input() (algo:88), input.bin=64=one i32[N]. Trivial.
Part (2) (explicit per-load offset for a FUTURE multi-load example whose input.bin order != EventList order) is NOT triggered by any in-tree example, so it is split out as a deferred-trigger follow-up: TASK-0048.07 (mirrors the TASK-0111->TASK-0347 close-deferred/reopen-on-fire pattern). No code changed this cycle (verification-only); e2e baseline 280/246/0/34/0 from earlier this session is unaffected.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Verified empirically that the embedded-pattern injected-input cursor (advance-by-load-length in EventList order) reads the correct input.bin slices for all 3 M10 examples. ex1 (only multi-load, a++b) PROVEN by a byte-exact Renode reference.bin cmp re-run this session; ex5/ex9 single-load trivial. The future-multi-load-mismatch remediation (explicit per-load offset from stable Event/sidecar metadata) is split to deferred-trigger TASK-0048.07. Verification-only, no code change.
<!-- SECTION:FINAL_SUMMARY:END -->
