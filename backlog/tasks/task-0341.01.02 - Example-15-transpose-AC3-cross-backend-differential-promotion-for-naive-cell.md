---
id: TASK-0341.01.02
title: 'Example 15-transpose AC#3: cross-backend differential promotion for naive cell'
status: In Progress
assignee:
  - '@orchestrator'
created_date: '2026-05-27 12:44'
updated_date: '2026-05-27 13:13'
labels:
  - examples
  - coverage
  - cross-backend-promotion
dependencies: []
parent_task_id: TASK-0341.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
=== Filed as TASK-0341.01 AC#3 fold-out, cycle 204 ===

15-transpose AC#1 (language-sanity, pthreads-sync naive) landed cycle 204. AC#3 wants the same naive cell promoted to [[required]] across the other 3 (or all 6) tier-1 backends.

EMPIRICAL OBSERVATION CYCLE 204: cycle 204's just e2e ran the naive cell on all 7 backends informationally; all 7 PASSed bit-identical against the same reference.bin (recorded in cycle-204 commit message). This is the expected behavior of the shared single-worker renderer for single-`host` schedules — the cell is structurally bit-identical by construction.

This task formalizes the promotion to [[required]] for the 4 substantively-coded tier-1 backends (pthreads-sync already required; pthreads-async / mp-tcp-bufsync / mp-tcp-event are the AC#3 promotion target). The 3 M6 skeleton backends (openmp-rs / mp-tcp-poll / mp-uds-event) ALSO already PASS informationally, so they MAY be promoted in the same cycle if the multi-sample non-flake property holds across all 7.

## Acceptance criteria

1. **Multi-sample non-flake verification**: run `just e2e` >=3 times, all 7 backends PASS bit-identical for `15-transpose | naive | *` each time (per TASK-0299 cycle-119 milestone-class re-run discipline).
2. **e2e-matrix.toml updates**: add `[[required]]` cells for `15-transpose | naive | <backend>` for at least pthreads-async, mp-tcp-bufsync, mp-tcp-event (AC#3 minimum); optionally also openmp-rs, mp-tcp-poll, mp-uds-event if the cycle scope allows.
3. **Architect review-GO**: e2e baseline jumps from 245/218/0/27/0 → 245/218/0/27/0 with the only delta being more `*` markers (the PASS count is unchanged, only the required-mark count grows). Numbers stable across the 3 samples.
4. **No code changes outside e2e-matrix.toml + tracker**: this is a promotion-only cycle. If a backend silently fails one of the 3 samples, file a precise follow-up naming WHICH backend + WHICH sample + WHY, and demote the cell back to informational pending root-cause.

## Honest scope LIMITS

- This is a low-risk cells-only promotion, not a feature cycle. Should land in <30 minutes if no flake surfaces.
- The 3 M6 skeleton backends (openmp-rs / mp-tcp-poll / mp-uds-event) PASS by construction (same shared single-worker renderer), but their multi-worker arms diverge from pthreads-sync. Promoting single-host cells is safe; multi-worker cells (which AC#2 = TASK-0341.01.01 will surface) need backend-by-backend judgment.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Cycle 205 closure (orchestrator-direct) ===

PROMOTION COMPLETE — all 7 tier-1 backends now [[required]] for `15-transpose | naive`.

DELIVERABLES:
- nuc-nucleus/e2e-matrix.toml: added 6 new [[required]] cells (mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event), all milestone=M6, all naive schedule. Updated the leading comment block to reflect AC#3 promotion provenance + 3-sample non-flake protocol citation.

MULTI-SAMPLE VERIFICATION:
- 8 samples total across cycles 204 + 205 — 15-transpose × 7 cells PASSed in all 8 samples (100%).
- Per-sample breakdown:
  - cycle 204 sample 1: 245/218/0/27/0
  - cycle 204 sample 2: 245/218/0/27/0
  - cycle 205 sample 1 (pre-promotion): 245/218/0/27/0
  - cycle 205 sample 2 (filtered): 245/218/0/27/0 — 15-transpose × 7 PASS
  - cycle 205 sample 3 (filtered): 245/218/0/27/0 — 15-transpose × 7 PASS
  - cycle 205 sample 4 (post-promotion): 245/217/1/27/1 — 1 flake in a NON-15-transpose cell
  - cycle 205 sample 5: 245/218/0/27/0
  - cycle 205 sample 6: 245/218/0/27/0
- 15-transpose × 7 row clean in 100% of samples; the 1 flake was in another cell and not introduced by this cycle.

e2e BASELINE UNCHANGED at 245/218/0/27/0 (only the `*` count grew by 6: was 1 required cell pthreads-sync, now all 7 backends required).

AC#1 (multi-sample non-flake): MET — 8 samples, 15-transpose × 7 cells PASS in 100% of samples.
AC#2 (e2e-matrix.toml updates): MET — 6 new [[required]] cells added, exceeding the AC#3 minimum (pthreads-async / mp-tcp-bufsync / mp-tcp-event); the 3 M6 skeleton backends (openmp-rs / mp-tcp-poll / mp-uds-event) also promoted.
AC#3 (architect review-GO): pending review gate (post-commit).
AC#4 (no code outside e2e-matrix + tracker): MET — only e2e-matrix.toml comment + 6 cells changed; no code.

HONEST FLAKE NOTE: 1 non-15-transpose cell flake observed in sample 4 (1/8 = 12.5%). This is below the cycle's scope; no fold-back. Forward-carried to TASK-0176 / TASK-0182 territory; no new task filed (existing ones cover it).

CYCLE 205 also unblocks closing PARENT TASK-0341.01: AC#3 of the parent is now MET via this child task; only AC#2 (TASK-0341.01.01) remains for the parent. Parent stays In Progress until TASK-0341.01.01 closes.
<!-- SECTION:NOTES:END -->
