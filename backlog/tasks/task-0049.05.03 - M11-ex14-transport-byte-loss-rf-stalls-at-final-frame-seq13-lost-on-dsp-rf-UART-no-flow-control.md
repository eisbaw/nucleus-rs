---
id: TASK-0049.05.03
title: >-
  M11 ex14 transport byte-loss: rf stalls at final frame (seq1[3] lost on
  dsp->rf UART, no flow-control)
status: To Do
assignee: []
created_date: '2026-06-04 03:50'
labels:
  - m11
  - tier3
  - renode
  - transport
dependencies: []
parent_task_id: TASK-0049.05
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
THE REAL ex14 multi-MCU byte-exact blocker (gates TASK-0049.10.08 AC#4), isolated 2026-06-04 AFTER TASK-0049.05.02 per-seq transport landed. Empirical facts: just renode-multimcu 14-hearing-aid embedded_multimcu_sync captures feUart=256B (4/4 frames) but rfUart=192B (3/4 frames) -> 448 != 512 -> recipe fails loud on the byte-count gate. CLEAN STALL (not corruption): rf's 3 completed frames are BYTE-EXACT vs reference.bin (cmp of rfUart[0..192] vs reference[256..448] passes). Window RULED OUT: identical 192B at RunFor 0.3s AND 5.0s (16x) -> not slowness, a true stall. NOT the multiplex: per-seq isolation (TASK-0049.05.02) gives every channel its own FIFO yet the number is unchanged, AND ex14 seq0+seq2 both carry mic_in so the old crossing was harmless anyway -> the architect's earlier 'streams cross and rf starves' trace was WRONG (feedback-orchestrator-narrative-also-wrong). ROOT (logical): rf blocks forever on link_recv(seq1) frame 3. dsp PROVABLY sent seq1[3] (fe completed all 4 frames => dsp reached push4[3]=E[3], which is AFTER push1[3]=B[3]) yet rf never received it. No logical deadlock in the dependency graph (frame-0 trace drains clean; pipeline is satisfiable). So the seq1[3] bytes are LOST at the Renode UART byte-delivery layer -> hypothesis: RX OVERRUN / no flow-control on the unbuffered STM32F7_USART (consistent with TASK-0049.01's durable finding that UARTBase.WriteChar DROPS chars not yet read / not queued). link_push is non-blocking (poll TXE, write TDR); a multi-byte burst with no per-byte/per-frame back-pressure relies on the fine global quantum (0.00001) interleaving sender-write and receiver-read; frames 0-2 align by luck, frame 3 the timing drifts and a byte is overwritten before rf reads it -> rf under-reads and blocks. CANDIDATE FIXES (pick empirically next cycle): (a) per-transfer RX-ready handshake / ACK so the sender does not transmit frame f+1 until the receiver consumed frame f (real flow-control; this is .05.02 item-1 'cyclic RX-ready handshake' territory); (b) check Renode STM32F7_USART RX buffer model + raise BufferState/quantum so a 256B burst is not overrun; (c) make link_recv poll-ready BEFORE the matched link_push fires (rendezvous). VERIFY NEXT CYCLE: rfUart reaches 256B and combined 512B; then the renode-multimcu cmp reveals whether per-frame VALUES are right (they look right for frames 0-2, so TASK-0049.02 may already be satisfied). DEP CHAIN CORRECTION: 0049.05.02(per-seq, DONE) is NOT the ex14 unblock; THIS task is the transport prerequisite, then 0049.10.08 AC#4. Needs interactive Renode debugging (USART watchpoints / byte trace) -> fresh focused context.
<!-- SECTION:DESCRIPTION:END -->
