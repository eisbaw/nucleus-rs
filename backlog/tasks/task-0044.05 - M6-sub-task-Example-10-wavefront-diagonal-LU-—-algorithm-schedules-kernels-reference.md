---
id: TASK-0044.05
title: >-
  M6 sub-task: Example 10 (wavefront / diagonal LU) — algorithm + schedules +
  kernels + reference
status: To Do
assignee: []
created_date: '2026-05-26 09:26'
labels:
  - M6
  - examples
dependencies: []
parent_task_id: TASK-0044
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land example 10 — wavefront / diagonal LU — per PRD §9 row 10: 'Diagonal dependency, ordering, no SIMD'. The first example with a 2D dependency pattern where each output element depends on its NW + N + W neighbors (or analog), forcing an anti-diagonal sweep ordering.

## What this stresses (per PRD §9)

- Algorithmic: 2D dependency where iteration (i, j) depends on iteration (i-1, j), (i, j-1), and (i-1, j-1). The natural execution order is anti-diagonal (one diagonal at a time).
- Scheduling: naive (single worker, row-major) + diagonal-parallel (workers process consecutive elements ALONG a diagonal in parallel; diagonals execute sequentially).
- 'No SIMD' marker in the PRD: each diagonal-step kernel is scalar-dependent on its neighbors; vectorization is intentionally hard. This stresses the algorithm/schedule split — same kernel, two radically different traversal orders, both produce identical output.

## Closest template

`nuc-nucleus/examples/05-stencil/` — the existing 2D stencil example. Reuse:
- 2D data declaration pattern (`data img : f32[H][W]`).
- 2D loop nest in the algorithm.

The diff is the DEPENDENCY DIRECTION: 05-stencil is a pure REDUCE-OVER-NEIGHBORS (each output independent), 10-wavefront is a RECURRENCE (each output depends on prior outputs of the same array). Wavefront is the first example with a true loop-carried dependency along TWO axes. This may stress the algorithm-sublanguage's `pure` rule (PRD §6.2.1 single-assignment) — verify the wavefront can be expressed without violating SSA, likely by declaring output(i, j) as a function of output(i-1, j) + input(i, j) using a fresh write per cell.

## Deliverables

- `nuc-nucleus/examples/10-wavefront/prog.algo.nuc` — algorithm: 2D recurrence with diagonal dependency.
- `nuc-nucleus/examples/10-wavefront/schedules/naive.sched.nuc` — one worker, row-major.
- `nuc-nucleus/examples/10-wavefront/schedules/diagonal.sched.nuc` — N workers process diagonals in parallel; diagonals execute sequentially.
- `nuc-nucleus/examples/10-wavefront/kernels.rs` — pure Rust kernel(s).
- `nuc-nucleus/examples/10-wavefront/reference/` — independent reference impl.
- `nuc-nucleus/examples/10-wavefront/input.bin`, `reference.bin`.
- `nuc-nucleus/examples/10-wavefront/README.md`.

## Acceptance criteria

1. `prog.algo.nuc` parses + lowers cleanly on pthreads-sync.
2. `naive.sched.nuc` lowers + emits + runs bit-identical against reference.bin on pthreads-sync + mp-tcp-bufsync.
3. `diagonal.sched.nuc` (or whatever the parallel schedule is named) lowers + emits + runs bit-identical against reference.bin on at least one tier-1 backend. ACCEPT a deferred-parallel-schedule outcome if diagonal partitioning needs new compiler machinery — file the gap as a sub-task.
4. kernels.rs is pure Rust, deterministic.
5. Reference impl is INDEPENDENT.
6. README documents: required schedules, which backends support each, the no-SIMD characteristic, honest limitations.
7. e2e-matrix.toml entries with right markers.

## Honest scope

- The single-assignment rule (PRD §6.2.1) MAY rule out expressing wavefront as a simple `out[i][j] = f(out[i-1][j], out[i][j-1], in[i][j])` if the compiler interprets that as a self-referential write. The implementer should EXPECT to discover this and either (a) reframe the algorithm to fit SSA (separate intermediate arrays per diagonal) OR (b) file a language-extension sub-task. The latter is a known v2 limitation — see TASK-0179 for the related in-array prefix scan case.
- 'Diagonal-parallel' means workers process N elements along the SAME diagonal in parallel; the next diagonal waits on the previous. This is a barrier-synchronization pattern between successive diagonals.
- If the parallel schedule cannot be expressed in v2's sublanguage, that's a HONEST FAILURE — file it, ship naive only, mark the parallel schedule as a v3 / future-grammar follow-up.

## Cross-reference

- Template: `nuc-nucleus/examples/05-stencil/` (2D iteration template).
- Related sublanguage limit: TASK-0179 (v2 algorithm sublanguage cannot express in-array prefix scan — close cousin of wavefront).
- PRD: §9 row 10; §6.2.1 single-assignment rule.

## Forward-carried context (orchestrator briefing for the implementer)

- BEFORE writing prog.algo.nuc, READ TASK-0179 (in-array prefix scan limitation) — wavefront may hit the same wall. If so, the honest outcome is the naive schedule + a precise sublanguage-extension follow-up task, NOT a hacked schedule.
- The 'diagonal' partition strategy MAY not fit any existing partition variant (partition_workers / partition_rows / partition_blocks2d all assume rectangular tiles). Diagonal traversal needs a triangular/anti-diagonal partition, which is a new variant. File `TASK-NNNN: partition=diagonal consumer pass` if the schedule needs it.
- check-narrative-doc-lie applies to README + e2e-matrix.toml additions.
- The example fundamentally tests the v2 model's expressiveness boundary. Be PREPARED for the answer to be 'wavefront's parallel schedule is post-v2'. That's a real, useful, honest outcome — not a failure of this task.
<!-- SECTION:DESCRIPTION:END -->
