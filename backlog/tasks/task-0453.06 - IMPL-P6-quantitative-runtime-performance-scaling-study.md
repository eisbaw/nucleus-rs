---
id: TASK-0453.06
title: 'IMPL P6: quantitative runtime performance & scaling study'
status: Done
assignee: []
created_date: '2026-06-06 22:54'
updated_date: '2026-06-07 05:44'
labels:
  - rigour
  - benchmark
dependencies: []
parent_task_id: TASK-0453
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Addresses Q1 (ch11:150-171). Measure emitted programs' wall-clock vs worker count/schedule/backend on real hardware with a disciplined methodology (worst-case inputs, controlled hardware, warm-up + variance). No guarantee risk; turns 'same values everywhere' into 'same values + here is what each decomposition costs'. Residual: cluster tier measured on available hardware only.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Reproducible benchmark harness with documented methodology (warm-up, variance, worst-case inputs)
- [ ] #2 Results table: wall-clock vs worker count across schedules/backends
- [x] #3 No correctness regression; measurement-only
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
CLOSED as DEFERRED (honest-stop, cycle-6 rigour epic P6). A rigorous runtime perf/scaling study is NOT defensibly feasible on this toolchain+hardware, so per the cycle guardrail NO study was shipped (the thesis keeps its honest "runtime out of scope" stance; ch11 sharpened instead, TASK-0453.16). AC#1/#2 (harness + wall-clock-vs-worker-count results table) NOT met by design; AC#3 (no correctness regression, measurement-only) trivially met (no code/example/manifest change). EMPIRICAL FEASIBILITY FINDINGS (probe in /tmp, not committed; matmul on Intel Ultra 7 165U / 14 hybrid cores / 30GB): (1) corpus-scale runtime is overhead-dominated -- naive matmul N=16 = 0.86ms median +/-30%, while 4096 madds compute = microseconds; no scaling signal. (2) signal needs large N -- naive N=512 = 103ms +/-8% compute-bound, compiles instantly (single-worker = buffer-free = symbolic gate fast path). (3) BUT multi-worker (communicating) schedules can NOT be compiled at large N: distributed-4w matmul compile peak RSS = 84/84/292 MB at N=16/32/64 (~N^3 growth), ~25GB OOM-killed at N=512; no gate-skip flag exists. So the compute-bound regime (N>=256) coincides with the gate-expansion residual (the documented communicating-case open problem) -- the decompositions one would want to time cannot be compiled at the sizes where their runtime exceeds the ~0.9ms startup floor. (4) message-passing backends run over loopback != cluster; single hybrid-core machine. CONCLUSION: the runtime study is coupled to the unsolved communicating-case gate-lifting, not merely awaiting a cluster -- re-openable when that compiler residual is lifted. GOTCHA caught (P2 discipline): an early probe abort at N=512 was MY bug (kernels.rs const N not updated to match prog), NOT a real obstacle -- corrected before drawing conclusions.
<!-- SECTION:FINAL_SUMMARY:END -->
