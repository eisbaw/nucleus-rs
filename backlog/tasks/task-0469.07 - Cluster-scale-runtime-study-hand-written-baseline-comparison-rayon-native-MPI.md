---
id: TASK-0469.07
title: >-
  Cluster-scale runtime study + hand-written baseline comparison (rayon / native
  MPI)
status: To Do
assignee: []
created_date: '2026-06-13 10:41'
labels:
  - adoption
  - perf
  - mpi
dependencies: []
parent_task_id: TASK-0469
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
HPC adoption gate; the residual beyond the loopback study. TASK-0455.04 (Done) delivered a single-laptop, loopback methodology + scaling curves and AC#4 explicitly retains the loopback caveat unless run on real multi-node hardware. A preferred language for HPC needs two things 0455.04 could not provide: (1) wall-clock on an ACTUAL multi-node cluster, not an oversubscribed loopback host; (2) comparison against hand-written baselines — the generated pthreads/rayon program vs a hand-rolled rayon version, and the generated MPI program vs a hand-rolled MPI version — so the cost of the genericity is quantified, not just its scaling shape. The symbolic gate (0455.01) already removed the compile-side obstacle for the dominant single-shot communicating class, so the large distributed decompositions one would want to time are now compilable. Keep the standing perf discipline: WORST-CASE inputs, warm-up/variance, machine spec, and an explicit cannot-claim list. Hardware-dependent: needs real cluster access (Helios or similar); mark env-blocked until secured.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Scaling curves on a real multi-node cluster for at least distributed matmul and a distributed stencil
- [ ] #2 Generated-vs-handwritten comparison reported for one shared-memory and one MPI baseline, on worst-case inputs
- [ ] #3 Overhead of genericity quantified as a ratio with variance, not a single friendly sample
- [ ] #4 If cluster hardware is unavailable, task parked env-blocked with the loopback caveat retained and the blocker recorded
<!-- AC:END -->
