---
id: TASK-0340.11.03
title: >-
  small-crates doc-link cleanup batch: embedded-pattern(7) + test-common(3) +
  pthreads-sync(2) + openmp-rs(1) + mp-tcp-poll(1) = 14 broken_intra_doc_links
status: Done
assignee:
  - '@mark'
created_date: '2026-05-30 03:30'
updated_date: '2026-05-30 04:02'
labels:
  - tech-debt
  - hygiene
  - doc-link
dependencies: []
parent_task_id: TASK-0340.11
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Sibling of TASK-0340.11.01 (nucleus-compiler bulk). The 5 SMALL doc-RED crates from the TASK-0340.11 full-workspace inventory, batched (all mechanical, same method). Goal: each of embedded-pattern, test-common, pthreads-sync, openmp-rs, mp-tcp-poll builds clean under RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc --no-deps -p <crate> (0 warnings) AND has ZERO dead cross-crate hrefs (grep target/doc, default + --document-private-items). Method proven on backend-common (5e7143c + cbbe0c9): backtick for cross-crate/field/private/TOML/angle-bracket targets; proper [link](crate::path) only same-crate. The -D lint is BLIND to dead cross-crate explicit-path hrefs — must grep. Does NOT add the just-ci gate arm (deferred until ALL crates incl nucleus-compiler + nucleus-e2e are green). e2e must stay 308/246/0/62/0 (doc-comment-only).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implementation plan (cycle): fix 14 broken_intra_doc_links across 5 leaf crates, doc-COMMENT-only (codegen-inert). Method = template 5e7143c/cbbe0c9: proper [link](crate::path) only for same-crate; backtick for cross-crate/private/field/TOML/angle-bracket. Inventory by RUSTDOCFLAGS oracle; verify by both dead-href greps (default + --document-private-items) + trap source-grep. DISPOSITION: ALL 14 turned out to be backtick fallbacks (ZERO proper same-crate links). embedded-pattern(10 fixes for 7 reported): NucleusShim/StubShim/USART1_SHIM_SRC/render_event/render_run_body are all PRIVATE-module (mod skeleton; mod render; both private) or pub(crate) fn -> private_intra_doc_links -> backtick. test-common(3): apply_block_transforms/apply_partition_workers/inject_check_frames all live in nucleus-compiler/src/passes/ = cross-crate -> backtick. pthreads-sync(2): FireBinding(nucleus_compiler::event)/ResolvedType(nucleus_compiler::algo::ir) cross-crate -> backtick. openmp-rs(1 link + 4 HTML): [[skip]] = e2e test-status literal (not item) -> `skip`; Slot<T>/Arc<Barrier> angle-bracket prose read as unclosed HTML tag -> backtick code spans. mp-tcp-poll(1): [[skip]] same -> backtick. All 5: oracle exit 0/0 warnings; both dead-href greps EMPTY; trap source-grep empty. Gate next.

=== Orchestrator review-gate close + fold-back (phase3 cycle) ===

Both read-only reviews ran on impl 3f9fa92. qa-test-runner GO: all 5 crates default-oracle exit 0/0 warnings; build/clippy/test/test-release green; e2e 308/246/0/62/0 x2 byte-identical; diff doc-only. architect GO-with-2xP2.

Fold-back applied in-thread (commit 3e31a8e):
- P2.1 SILENT SIBLING (the architect catch): pthreads-sync/src/lib.rs:284 had a [FireBinding] link on a PRIVATE fn (render_main_rs) that the batch left unconverted while fixing the IDENTICAL [FireBinding] at the crate-header //! (~line 18). Invisible to the default oracle (private fn not rendered); errors under --document-private-items. Backticked.
- P3: embedded-pattern/src/skeleton/bin.rs:6 ([[bin]]) + :26 ([super::NucleusShim]/[super::StubShim] wrong-path private links) backticked; test-common/src/lib.rs:43 prose "Bracketed"->"Code-spanned" (the stages it names are now backticks not brackets).

POST-FOLD-BACK STATE (verified): all 5 batch crates exit 0 under BOTH default AND --document-private-items; dead-href grep empty AND VALID (exit 0); build/clippy/test green; e2e 308/246/0/62/0. So the batch crates are now FULLY clean (stronger than the task's default-oracle DoD). Done justified.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE. All 5 leaf crates doc-clean (RUSTDOCFLAGS oracle exit 0 / 0 warnings) + ZERO dead cross-crate hrefs (both dead-href greps EMPTY: default AND --document-private-items, all 5) + trap source-grep EMPTY. 14 reported broken links resolved as 16 doc-comment edits (embedded-pattern needed 10 fixes for the 7 reported lines: NucleusShim x2 bare + render_run_body x2 + a triple-link line). Codegen-INERT: every changed line is /// or //! (git diff verified, non-doc-comment grep empty). Full gate GREEN: build + clippy(-D warnings) + test + test-release + e2e all pass; e2e 308/246/0/62/0 (baseline preserved, hard-match). PROPER-LINK vs BACKTICK TALLY: 0 proper same-crate links / 16 backtick fallbacks (100% backtick — these are LEAF crates whose doc references point OUT to nucleus-compiler/private-modules, the inverse of a root crate). Per-crate disposition: embedded-pattern 10x backtick (private mod skeleton/render + pub(crate) render_run_body); test-common 3x backtick (apply_block_transforms/apply_partition_workers/inject_check_frames all in nucleus-compiler/src/passes); pthreads-sync 2x backtick (FireBinding/ResolvedType cross-crate in nucleus-compiler); openmp-rs 1 link + 4 HTML backtick ([[skip]] test-status literal; Slot<T>/Arc<Barrier> angle-bracket-as-HTML-tag); mp-tcp-poll 1x backtick ([[skip]]). Crate-specific subtlety: openmp-rs [[skip]] is a doubled-bracket reference-style link to the e2e test-status word skip (not an item) -> `skip` code span; same on mp-tcp-poll. embedded-pattern subtlety: mod skeleton/mod render are PRIVATE, so even pub items inside (NucleusShim trait, StubShim struct) are unreachable under --no-deps and warn no-item-named, not private-item -> backtick is correct either way. No crate left unclean; no follow-up needed; scope did NOT expand to nucleus-compiler (.01) or nucleus-e2e (.02).
<!-- SECTION:FINAL_SUMMARY:END -->
