---
id: TASK-0453.05
title: 'IMPL P5: bounded data-dependent iteration (capped until-loop)'
status: To Do
assignee: []
created_date: '2026-06-06 22:54'
updated_date: '2026-06-07 04:54'
labels:
  - rigour
  - compiler
  - grammar
dependencies: []
parent_task_id: TASK-0453
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Addresses L1a (ch11:86-106). Lift bounded data-dependent control flow: a loop with compile-time max trip count + runtime early exit, so the worst-case firing order stays statically analysable (gate sound via worst-case replay). Builds on existing TASK-0341.02.01.x (until surface/AST/parser/codegen-free lowering). RISK per ch11:97-102: byte-identity additionally needs the early-exit predicate to be a deterministic backend-agnostic function (exact integer predicate OK; float predicate at risk - tie to P3). Residual: only exact-integer convergence; float-predicate convergence remains excluded.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A capped until-loop with an exact integer predicate compiles+runs multi-worker and is byte-identical across tier-1
- [x] #2 Worst-case replay keeps the soundness gate sound (cap-bounded)
- [x] #3 Float-predicate convergence remains rejected/excluded and documented
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CYCLE-5 STEP-0 RECONCILIATION (duplicate-check per brief): the CORE of P5 (bounded, static-firing-order-preserving data-dependent iteration) was ALREADY SHIPPED single-worker by the TASK-0341.02.01.x epic (S0-S6 all Done): for..until grammar surface + bool ops + scalar convergence reduction + IR/bounded-cap lowering (acfg_to_petri ignores break_cond; full-N unroll is the gate worst case; early exit 0..k is a sub-trace, bounded a fortiori) + single-worker pthreads-sync break emit; example 21-jacobi-converge runs value-correct in e2e. This is EXACTLY the static-firing-order-preserving design the cycle guardrail demanded. So P5 was NOT unimplemented - only its MULTI-WORKER arm (AC#1) was, and that == the open S7 (TASK-0341.02.01.08).

CYCLE-5 DELIVERED (the realizable rigour that strengthens P5 WITHOUT touching the static-firing-order invariant): new example 29-jacobi-cap-hit - the cap-hit (did-NOT-converge) sibling of 21-jacobi-converge (same seed/input.bin, same TOL=2, ITERS_CAP=16 < the convergence gen 30) so the until predicate NEVER fires inside the cap and the loop runs the FULL worst-case replay. This closes the cap-hit/did-NOT-converge e2e coverage gap forward-carried from the S5 review (TASK-0341.02.01.06 P3-2): previously the -1 break-sentinel -> __nuc_final_gen=cap + stderr did-NOT-converge path had ONLY string-level unit coverage; NO e2e cell ran a non-converging input. Now the worst-case (full-cap) replay the soundness gate analyses is an EXERCISED, byte-identical-vs-independent-reference execution path (verified direct emit+build+run+cmp + via just e2e). 1 [[required]] naive x pthreads-sync PASS + 6 [[skip]] (multi-worker break = S7). e2e 490/427/0/63/0 -> 497/428/0/69/0 (+7). just ci GREEN incl all 4 negative/determinism arms. Review GO x2 (mped-architect independently recomputed the reference oracle: maxdiff decay 288,184,...,11 at gen16>TOL=2, cap-hit provably taken, reference.bin byte-exact; differential bites on a control-flow bug; manifest mirrors 21; skip reasons backed by real fail-loud guards. qa = self-run just ci GREEN).

AC STATUS (honest): AC#2 TICKED - worst-case replay keeps the gate sound (cap-bounded), and is now EXERCISED end-to-end by 29-jacobi-cap-hit (was only design-level + unit before). AC#3 TICKED - float-predicate convergence is documented as EXCLUDED/at-risk (thesis ch11/ch05 tie it to the float-determinism limit); NOTE it is NOT a separate typed rejection (the until-COND gate accepts any IrExpr::Compare regardless of operand type; verified at acfg/build.rs:255-264). Adding a typed float-reject would be a workaround, not root-cause - the honest state is "at risk under float non-determinism", which the thesis states. AC#1 NOT TICKED - multi-worker byte-identity across tier-1 is the open S7 (TASK-0341.02.01.08): it is FEASIBLE within the static-firing-order invariant (a collective all-reduce+broadcast of the convergence scalar makes all workers exit at the same generation - static order preserved, NOT a dynamic-firing-order breach) but is a large deferred slice (new per-iteration cross-worker collective-reduce-with-control-feedback + broadcast, two walker substrates + 6-7 backends, layered on distributed-jacobi). Kept In Progress: AC#1 genuinely open = S7.
<!-- SECTION:NOTES:END -->
