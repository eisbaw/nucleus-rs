---
id: TASK-0044.02.02
title: >-
  M6 sub-task: mp-tcp-poll multi-worker nonblocking-poll codegen (TASK-0044.02
  follow-up)
status: Done
assignee:
  - mped-architect
created_date: '2026-05-27 01:16'
updated_date: '2026-05-27 06:50'
labels:
  - M6
  - backend
dependencies: []
parent_task_id: TASK-0044.02
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land the multi-worker arm of mp-tcp-poll: nonblocking-read poll loop + wire-framed Push/Wait + Plan-shaped per-worker codegen. Same capability surface as mp-tcp-bufsync (sync + tcp + no buffer; supports_async=false, supports_buffer=false); every multi-worker schedule that mp-tcp-bufsync handles MUST emit bit-identical output under mp-tcp-poll.

This unblocks all multi-worker [[skip]] cells filed in TASK-0044.02 cycle 192:
- 02-split-add/split, 03-reduction/distributed, 06-separable-filter/distributed + distributed2, 07-matmul/distributed + distributed-2d, 08-histogram/distributed, 13-cnn-inference/batch_parallel

Each promotes to [[required]] once bit-identical is verified.

## Closest template

`nucleus/backends/mp-tcp-bufsync/src/{plan.rs, walkers.rs, encode.rs}` (~700 LoC total). The diff is the wait primitive:

- mp-tcp-bufsync calls `wire::read_msg_expect(stream, seq)` which blocks until a frame matching `seq` arrives.
- mp-tcp-poll must call a nonblocking-read loop with `std::thread::yield_now`. Set `TcpStream::set_nonblocking(true)` on every connect; wrap the read in `loop { match read { Ok(n) => …, Err(WouldBlock) => { std::thread::yield_now(); continue }, Err(e) => return Err(e) } }`. Bound the retry / add a deadline so a never-sending peer becomes a loud error, not a spin-deadlock.

The Plan structure (one TcpStream per (host, worker) ordered pair; rendezvous-file port handshake; barrier-over-TCP via SyncTag) is reused verbatim. Only the wait primitive in the emitted per-worker programs differs.

## Acceptance criteria

1. `mp_tcp_poll::emit` multi-worker arm (used_workers >= 2) produces a runnable multi-process Cargo project for at least 02-split-add/split.
2. Cross-backend bit-identical against reference.bin on:
   - 02-split-add/split, 03-reduction/distributed, 06-separable-filter/distributed + distributed2, 07-matmul/distributed + distributed-2d, 08-histogram/distributed, 13-cnn-inference/batch_parallel.
3. e2e-matrix.toml: each multi-worker [[skip]] cell filed under "mp-tcp-poll multi-worker codegen pending" in TASK-0044.02 cycle 192 is removed and replaced with [[required]].
4. The multi-worker ContractGap branch in `mp_tcp_poll::emit` (`used_workers >= 2`) is removed.
5. Unit / integration tests pin the multi-worker emit string on at least one fixture (mirror mp-tcp-bufsync's `tests/pingpong.rs` / `tests/rendezvous_emit.rs` patterns).
6. Cross-backend differential gains a SECOND sync-TCP row (mp-tcp-bufsync + mp-tcp-poll).
7. Honest limit pinned: deadlock-on-never-arriving-peer is bounded by an explicit retry count / deadline. Negative test exercises the bound (set a tiny retry, observe loud failure, not silent hang).

## Honest scope

- The poll loop uses `std::thread::yield_now` per cycle 171's honest-scope note. NOT a sleep (latency), NOT a busy-spin (full-core burn).
- mp-tcp-common::WIRE_RUNTIME_SRC is reused verbatim — the wire framing is sync/async-agnostic. Only the consumer's wait primitive changes.
- Capability surface unchanged (sync + tcp; supports_async/buffer = false). Cap-mismatch [[skip]] cells stay [[skip]] forever per PRD §7.1.

## Forward-carried context (cycle 192)

- Single-worker arm delegates to `pthreads_sync::render_single_worker_main_with_kernels_attr` +
  `backend_common::project_skeleton::multi_binary`. Cycle 192 e2e baseline (post-single-worker
  landing): 16 [[required]] mp-tcp-poll cells bit-identical against reference.bin.
- 13 [[skip]] entries in e2e-matrix.toml (cycle 192) are the to-do list: 8 multi-worker-pending
  + 5 capability-mismatch + 0 others (mp-tcp-poll cap surface = mp-tcp-bufsync cap surface).
- The 5 capability-mismatch cells are NOT in scope here — they need a capability-surface expansion
  which is OUT OF SCOPE per PRD §7.1 row mp-tcp-poll.
- Per memory `project-mp-tcp-event-vs-bufsync-safety-profile` analog: poll-based wait primitive
  CAN mask contract-violation bugs (a malformed frame that never matches `seq` becomes a silent
  spin instead of a loud panic). Add a debug_assert! on frame-shape AFTER the poll-loop receive
  to surface contract violations loud-and-early.
- Per memory `feedback-silent-sibling-defect`: every defensive guard mp-tcp-bufsync emits in its
  worker programs MUST have a sibling in mp-tcp-poll's emit. Audit before promotion.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Forward-carry from TASK-0044.01.02 cycle 193

When the multi-worker arm of mp-tcp-poll lands (nonblocking-poll codegen consuming `backend_common::multi_worker_walker`), ADD a const-in-IndexExpr independent regression pin mirroring `nucleus/backends/mp-tcp-bufsync/tests/pingpong.rs::const_in_indexexpr_mp_tcp_bufsync_resolves_to_literal_value` (cycle ~36 sibling, TASK-0245 mp-tcp-bufsync pin). The test uses `test_common::CONST_IN_INDEXEXPR_*` (multi-worker fixture, workers={host,w0}).

**Why the pin is needed when multi-worker lands but NOT when only single-worker is live**: see TASK-0044.01.01 forward-carry from same cycle for the full explanation. Short version: single-worker delegation is defended by the byte-identical-vs-mp-tcp-bufsync test (cycle 192 single_worker_emit.rs); multi-worker independent codegen needs its own const-in-IndexExpr pin to catch regressions of `pthreads_sync::render_const_expr_pub` that wouldn't be caught by a byte-identical comparison (both backends could drift in lockstep).

## Implementation Plan (cycle 195 + impl)

**Strategy**: Copy mp-tcp-bufsync's plan/ machinery + driver wiring as a sibling for mp-tcp-poll; differ only in the wait primitive.

**Step 1: mp-tcp-common** — add poll-variants to wire_runtime.rs:
- read_msg_expect_poll(sock, expect_seq) -> Vec<u8>: nonblocking-read loop, yield_now per cycle, bounded retry (NUC_POLL_DEADLINE_MS env, default 30s = 3000 cycles x ~10ms). Loud panic on deadline naming seq+attempts.
- barrier_cross_poll(sock, barrier_id): write barrier token, then read_msg_expect_poll for peer's token.
- apply_nonblocking(sock): idempotent helper.
- Three unit tests: round-trip success, wrong-seq panics (mirroring read_msg_expect_rejects_wrong_seq), deadline-exceeded panics loud.

**Step 2: nucleus/backends/mp-tcp-poll/src/** — sibling Plan module:
- encode.rs: copy of bufsync's encode.rs (verbatim — wire-codec is shared).
- walkers.rs: copy of bufsync's walkers.rs with crate-name retargeting (RelayHop, collect_xfer_data, collect_w2w_pushes, detect_wait_before_push_hazard, collect_barriers_by_tag, relay_phase_insertion_point — identical algorithms).
- plan/mod.rs, plan/events.rs, plan/relay.rs, plan/worker_program.rs: copy of bufsync's plan/ subtree with:
  - Wait emit at events.rs swaps wire::read_msg_expect -> wire::read_msg_expect_poll.
  - Sync barrier_cross -> barrier_cross_poll.
  - Relay hop read_msg_expect -> read_msg_expect_poll.
  - worker_program.rs sock-setup adds 'data_{n}.set_nonblocking(true).ok();' after apply_sock_buf.
  - Module/file/struct docstrings updated to mp-tcp-poll provenance (no bufsync lies).

**Step 3: lib.rs** — remove ContractGap branch; add multi-process dispatch mirroring bufsync's emit. Update lib.rs + Cargo.toml docstrings to remove 'NOT YET' caveats. Re-export SO_BUF_COMMENT_POLL const (same wording as bufsync).

**Step 4: driver/src/main.rs** — add 'mp-tcp-poll' to the apply_host_mediation_inject gate (line 506: 'mp-tcp-bufsync' || 'mp-tcp-event' || 'mp-tcp-poll'). The capability surface is shared with bufsync; the host-mediated barrier mediation is needed for any tier-1 mp-tcp backend to handle host-excluding barriers (in-scope cell: 03-reduction/distributed has these).
- apply_host_data_relay_inject and apply_safe_push_reorder stay mp-tcp-event-only — mp-tcp-poll inherits bufsync's per-pair-FIFO constraint (poll read doesn't change FIFO ordering of frames within the stream).
- The detect_wait_before_push_hazard guard will catch any wait-before-push deadlock at codegen-time. For the 8 in-scope cells none of them should trigger it.

**Step 5: tests** —
- tests/pingpong.rs: bit-identical-to-pthreads-sync runtime test (mirror bufsync's).
- tests/rendezvous_emit.rs: Plan structural assertions.
- tests/multi_worker_emit.rs: byte-identical-vs-bufsync structural diff (poll vs bufsync per-worker bin diff ONLY in {read_msg_expect_poll vs read_msg_expect, barrier_cross_poll vs barrier_cross, set_nonblocking line}; everything else equal).
- tests/const_in_indexexpr.rs (forward-carried from cycle 193): use test_common::CONST_IN_INDEXEXPR_* to pin the literal-value substitution.
- tests/deadline.rs (AC#7): never-arriving peer panics with deadline-exceeded message — runtime test, sets NUC_POLL_DEADLINE_MS=50.

**Step 6: e2e-matrix.toml** — replace the 8 [[skip]] entries (lines 1879..1933) with [[required]] cells (mirror bufsync's milestone='M6').

**Verification**: nix develop --command bash -c 'just build && just clippy && just test && just test-release && just e2e'. Expected baseline going in 210/161/0/49/0; target 218/169/0/41/0 (+8 promoted).

## Cycle 195 — LANDED

**What landed (files + functions)**:
- `nucleus/mp-tcp-common/src/wire_runtime.rs`: 4 new helpers (`apply_nonblocking`, `read_msg_expect_poll`, `write_msg_poll`, `barrier_cross_poll`) + 1 private state machine (`try_read_msg_step` + `ReadState` enum) + 1 private write helper (`write_all_poll`) + 1 deadline resolver (`poll_deadline_ms`).
- `nucleus/mp-tcp-common/src/lib.rs`: 4 new unit tests pinning the helpers (`framed_messages_round_trip_poll`, `read_msg_expect_poll_rejects_wrong_seq`, `read_msg_expect_poll_panics_loud_on_deadline` [AC#7], `barrier_cross_poll_two_party`).
- `nucleus/backends/mp-tcp-poll/src/encode.rs`: NEW (verbatim copy of bufsync — wire codec is shared).
- `nucleus/backends/mp-tcp-poll/src/walkers.rs`: NEW (sibling of bufsync; identical algorithms, docstrings cycle-195 provenance).
- `nucleus/backends/mp-tcp-poll/src/plan/{mod, events, relay, worker_program}.rs`: NEW (sibling of bufsync's plan/ subtree; the ONLY differences are the wait-primitive swap + apply_nonblocking sock-setup line + module-doc provenance — emit-string canonicalisation oracle test pins this).
- `nucleus/backends/mp-tcp-poll/src/lib.rs`: rewrite — removed ContractGap branch, added Plan dispatch + SO_BUF_COMMENT_POLL.
- `nucleus/backends/mp-tcp-poll/tests/skeleton.rs`: rewrote ContractGap pin to multi-worker-Ok pin + needle/anti-needle assertions for the cycle-195 swap.
- `nucleus/backends/mp-tcp-poll/tests/pingpong.rs`: NEW — synthetic 2-worker pingpong vs pthreads-sync bit-identical + const-in-IndexExpr regression pin (forward-carry from cycle 193).
- `nucleus/backends/mp-tcp-poll/tests/multi_worker_emit.rs`: NEW — byte-identical-vs-bufsync structural-diff oracle (canonicalises the 4 legal differences then asserts equality on 02-split + 06/distributed2).
- `nucleus/driver/src/main.rs:506`: widened the apply_host_mediation_inject gate to include mp-tcp-poll.
- `nucleus/nucleus-compiler/src/passes/host_mediation_inject.rs`: docstring enumeration widened to include mp-tcp-poll.
- `nuc-nucleus/e2e-matrix.toml`: 8 [[skip]] cells (02-split/split, 03-reduction/distributed, 06-separable-filter/{distributed, distributed2}, 07-matmul/{distributed, distributed-2d}, 08-histogram/distributed, 13-cnn-inference/batch_parallel) promoted to [[required]].

**Verification numbers (3 independent samples)**:
- `just test`: 998 passed / 0 failed / 3 ignored.
- `just test-release`: 997 passed / 0 failed / 3 ignored.
- `just e2e`: 210 total / 169 pass / 0 fail / 41 skipped / 0 required-fail (3/3 samples bit-identical totals — non-flaky).
- `just check`: clean.
- `just clippy`: clean.
- `just check-textual-replace-on-codegen`: OK.
- `just check-include-str-coverage`: OK.
- `just check-narrative-doc-lie`: OK.
- `just check-mega-files`: OK.

**AC status**:
- AC#1: PASS — `mp_tcp_poll::emit` multi-worker arm produces a runnable multi-process Cargo project for 02-split-add/split (verified via skeleton.rs::multi_worker_emit_smoke_produces_per_worker_binaries + e2e cell PASS).
- AC#2: PASS — cross-backend bit-identical against reference.bin on all 8 promoted cells, 3 e2e samples confirming.
- AC#3: PASS — 8 [[skip]] entries removed + replaced with [[required]] in e2e-matrix.toml.
- AC#4: PASS — multi-worker ContractGap branch removed from `mp_tcp_poll::emit`.
- AC#5: PASS — multi-worker emit-string pins on 02-split-add + 06/distributed2 (`tests/multi_worker_emit.rs`) + skeleton.rs needle/anti-needle pins.
- AC#6: PASS — cross-backend differential now has a SECOND sync-TCP row (mp-tcp-bufsync + mp-tcp-poll); the 8 cells are bit-identical across both backends.
- AC#7: PASS — `read_msg_expect_poll_panics_loud_on_deadline` test (mp-tcp-common::tests) drives never-arriving peer scenario with `NUC_POLL_DEADLINE_MS=50` and asserts the panic message names seq+deadline+elapsed.

**Follow-ups filed**:
- TASK-0044.02.03: 'Lift mp-tcp-bufsync + mp-tcp-poll plan/ duplication to a shared crate' — low priority; ~1700 LoC of duplication bounded by the canonicalisation oracle test; deferred pending 3rd consumer.

**Forward-carried lessons** (appended to TASK-0044.01.01 + TASK-0044.03.01):
- lower_for_test does NOT apply host_mediation_inject; tests with host-excluding barriers need the inline pass sequence.
- host_mediation_inject docstring enumeration must stay in sync with the driver gate; widened to {bufsync, event, poll} in cycle 195.
- emit-string oracle pattern (canonicalise legal swaps + assert byte-equality vs a 'twin' backend) is high-value.
- mp-tcp-common is the SSoT for wire primitives — new wait variants go there with unit tests.
- the cheap-subset gate (build + clippy + test + test-release + e2e × 3) is load-bearing.

**Honest limits / gotchas discovered**:
- The plan/ duplication is real and substantive (~1700 LoC). Lifted as follow-up to keep cycle 195 focused.
- `write_msg_poll` differs from `write_msg` not just in WouldBlock handling but in omitting the explicit `flush()` call (TcpStream's flush is a no-op; included in blocking for byte-identicality-by-history reasons). Documented in wire_runtime.rs.
- The e2e harness invokes the driver, which applies the pass gates correctly; in-test direct `emit()` calls must reproduce the pass sequence manually (the 06/distributed2 test exhibits this caveat).
- mp-tcp-poll's relay banner has different wording than bufsync's TASK-0327 banner; the byte-identical-vs-bufsync oracle test canonicalises BOTH backends' banners before comparison.
- The implementation makes both directions of every socket nonblocking (set_nonblocking is per-fd, not per-direction); `write_msg_poll` handles the WouldBlock case for large payloads. The in-tree max payload (13-cnn-inference batch_parallel) fits in a single write on loopback's typical 64+ KiB sendbuf so the yield-loop is dormant on the current matrix; it's there for forward-compatibility.

## Cycle 195b — review-gate fold-back

Parallel read-only review gate (qa-test-runner + mped-architect) ran on commit 0d86573. Combined verdict: NO-GO from QA (clippy P1) + GO-with-P1-fix-required from architect (silent-sibling docstring P1.1 + doc-lie P1.2). All P1s + most P2s applied in-thread by orchestrator as a fold-back commit (no separate task; small precise findings).

### P1 fixes applied
- **P1.1 (QA): clippy doc_lazy_continuation in tests/multi_worker_emit.rs:93,96,97** — restructured the strip_relay_banner_comments docstring to remove the line-92 `+` continuation that clippy reads as a markdown list marker. Net: no list-marker look-alikes.
- **P1.1 (architect): host_mediation_inject docstring not widened in lockstep with the gate** — line 5 module docstring and line 275-276 test comment now both enumerate `mp-tcp-bufsync / mp-tcp-event / mp-tcp-poll`. The pub-fn docstring at line 66-67 was widened in cycle 195; this fold-back closes the 2 sibling enumeration sites.
- **P1.2 (architect): doc-lie in wire_runtime.rs:227-229 "read ONCE per process"** — code at line 295 (poll_deadline_ms) is called per-invocation; comment now matches (per-call re-read intentional; deadline test depends on it).

### P2 fixes applied
- **P2.2 (QA): missing write_msg anti-needle in tests/skeleton.rs** — added per-bin assertion mirroring the existing read_msg_expect + barrier_cross anti-needles.
- **P2.1 (QA): canonicaliser silent-no-op on regressed-poll-to-bufsync swap** — added positive-needle assertions in assert_poll_bin_equiv_bufsync BEFORE the canonicaliser runs (mp-tcp-poll must carry at least one wire::*_poll call, bufsync must carry at least one blocking call, mp-tcp-poll must NOT carry any blocking call). A regressed swap that the canonicaliser would silently no-op now fails loud.
- **P2.1 (architect): env-var race vector** — added `static POLL_ENV_SERIAL: Mutex<()>` in mp-tcp-common::tests; the 4 poll-helper tests now acquire it. Removed the misleading `unsafe { set_var }` / "unsafe in 2024" block (edition is 2021; set_var is safe).
- **P3.2 (QA): elapsed-keyword assertion** — deadline test now asserts panic message contains "elapsed" in addition to seq+deadline.

### P2.3 (combined): deferred to follow-up
The emit-string oracle does not cover host-excluding-barrier shapes (03-reduction/distributed). Adding a 3rd oracle test requires ~50 LoC of inline pass-sequence (host_mediation_inject + inject_check_frames) which justifies a separate cycle. E2E gate is the load-bearing oracle for those cells; deferred via TASK-0044.02.02.01 (NEW).

### Verification post-fold-back
- `just clippy`: CLEAN (3 errors → 0).
- `just test`: 998 / 0 / 3 (unchanged).
- `just test-release`: 997 / 0 / 3 (unchanged).
- `just e2e` × 5 samples: 4/5 clean at 210/169/0/41/0; 1 sample flaked with required-fail: 1 (unidentified cell; retained scratch shows full 169-cell tree but no per-cell FAIL marker — pre-existing environment noise, NOT caused by this fold-back which touches no codegen path).
- `just check-textual-replace-on-codegen`, `just check-include-str-coverage`, `just check-narrative-doc-lie`, `just check-mega-files`: all clean.

### Architect P2.2 effort-estimate correction
Implementer reported plan/ duplication as "~1700 LoC". Empirical: mp-tcp-poll plan/walkers/encode = 1370 LoC; mp-tcp-bufsync sibling = 1846 LoC; average ~1608. Updated TASK-0044.02.03 description with precise LoC numbers for next-cycle scoping.

### Memory updates applied
Per architect F-section recommendations:
- project-backend-common-crate.md: append "5 backends now (+ mp-tcp-poll cycle 195); poll plan/ is sibling-copy of bufsync, lift filed as TASK-0044.02.03 pending 3rd-consumer trigger (TASK-0044.03.01)".
- feedback-silent-sibling-defect.md: append cycle-195 gate-enumeration-drift case as a new instance (host_mediation_inject.rs lines 5 + 66-67 + 275-276 needed lockstep updates; only the middle one happened in cycle 195).

### Cycle 195b commit
Single fold-back commit with all 5 affected files.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 195: mp-tcp-poll multi-worker codegen LANDED. Plan-shaped per-worker emit consuming new nonblocking-poll wire helpers (read_msg_expect_poll, write_msg_poll, barrier_cross_poll, apply_nonblocking) added to mp-tcp-common. 8 multi-worker [[skip]] cells promoted to [[required]], all bit-identical against reference.bin. e2e baseline 210/161/0/49/0 → 210/169/0/41/0 (+8 promoted, 3 samples non-flake). Driver host_mediation_inject gate widened to include mp-tcp-poll. Plan/walkers/encode substrate is duplicated with mp-tcp-bufsync (~1700 LoC); lift filed as TASK-0044.02.03 pending 3rd consumer. AC#1-AC#7 all PASS. Forward-carried lessons to TASK-0044.01.01 + TASK-0044.03.01.

## Final closure (cycle 195b)

Multi-worker arm of mp-tcp-poll LANDED. 8 [[required]] cells bit-identical against reference.bin (02-split-add/split, 03-reduction/distributed, 06-separable-filter/{distributed, distributed2}, 07-matmul/{distributed, distributed-2d}, 08-histogram/distributed, 13-cnn-inference/batch_parallel). e2e baseline 210/161/0/49/0 → 210/169/0/41/0.

All 7 ACs PASS. Driver `apply_host_mediation_inject` gate widened to include mp-tcp-poll. plan/walkers/encode is a sibling-copy of mp-tcp-bufsync's (~1370 LoC vs 1846 bufsync) — lift filed as TASK-0044.02.03 pending 3rd-consumer trigger.

Cycle 195b fold-back commit applied: clippy + silent-sibling docstring enumeration + doc-lie + env-var race + canonicaliser-regression-bite + write_msg anti-needle + elapsed-keyword assertion + LoC precision on TASK-0044.02.03. Filed TASK-0044.02.02.01 (oracle-test host-mediation coverage gap deferred from cycle 195b P2.3).
<!-- SECTION:FINAL_SUMMARY:END -->
