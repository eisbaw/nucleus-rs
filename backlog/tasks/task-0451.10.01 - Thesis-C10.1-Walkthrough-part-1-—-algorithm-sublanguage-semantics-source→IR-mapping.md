---
id: TASK-0451.10.01
title: >-
  Thesis C10.1: Walkthrough part 1 — algorithm sublanguage semantics + source→IR
  mapping
status: Done
assignee: []
created_date: '2026-06-05 22:08'
updated_date: '2026-06-05 22:31'
labels:
  - thesis
  - paper
  - content
  - deep-dive
dependencies:
  - TASK-0451.04
parent_task_id: TASK-0451.10
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Write the first half of the compilation-walkthrough chapter + deepen ch5/ch6. (1) Algorithm sublanguage SEMANTICS in depth: shape-typing, single-assignment, pure vs effectful kernels, the affine/static firing model, why the class is restricted. (2) Pick ONE representative worked example (a stencil/separable-filter or histogram-combine that exercises partitioning + transfers + a non-trivial net) and trace it from Nuc source through EACH IR transform: AlgoIR/SchedIR -> LinkedIR (linking) -> ACFG (control-flow + transforms/tiling) -> transfer/halo inference -> Petri-net IR -> soundness gate -> per-worker projection -> EventList. For each pass: WHAT it computes, WHY, and the concrete IR snapshot for the worked example. Verify every mechanic against the LIVE compiler: nucleus/nucleus-compiler/src/pipeline.rs (pass order), the algo/sched/acfg/petri IR modules, passes/net_soundness.rs (the gate), sidecar.rs. Forward-ref the per-stage figures (drawn in .06.03). Duplicate-check vs existing ch5/ch6 -- EXTEND, do not restate. Standalone prose, no TASK/PRD refs. DoD: chapter part written; mechanics code-verified; just build clean exit 0.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle C10.1 landed. New chapter chapters/07b-compilation-walkthrough.tex (A Compilation, End to End) inserted after Backends via main.tex (label-based numbering; downstream chapters renumber). Traces 05-stencil/distributed source->EventList through every verified stage: algorithm semantics (shape-typing, single-assignment, pure/effectful, affine static firing) -> AlgoIR/SchedIR -> LinkedIR -> ACFG + transforms (block=64 tile, partition=rows bands 1..5/5..9/9..12/12..15 floor-with-spillover, halo width 1, reuse 3-window) -> sync+transfer inject (2 barriers; img_in host->worker band+halo async buf2 event, img_out sync) -> Petri net + soundness gate -> per-worker EventList (host + w0). EVERY mechanic verified against live code: pipeline.rs:120-139 pass order, compute_partition_bands test, net_soundness.rs, AND the actual pthreads-async generated main.rs (Ring::new(2)/(1), Barrier::new(5), img_in[0..96]/[64..160] halo slices, band loops, event order). Forward-refs fig:wt-acfg/bands/net/events (drawn in .06.03). Review gate: paper-accuracy GO (all 10 structural/quantitative claims VERIFIED vs code+codegen), paper-correctness NO-GO->GO (fixed 2 load-bearing soundness-framing P1s: conflict-freedom representativeness now scoped to the restricted no-data-dependent-routing net class not general Petri; static-firing holds-for-every-input now scoped to coordination not value-correctness; +3 P2 precision fixes; re-verified GO), paper-clarity advisory folded (faculty typo, half-open interval convention, band derivation, two-barriers, first-person aside, sync depth). PDF: just build exit 0, 71 pages, zero undefined citations (only fig:wt-* + other C6 figures undefined). DoD met.
<!-- SECTION:NOTES:END -->
