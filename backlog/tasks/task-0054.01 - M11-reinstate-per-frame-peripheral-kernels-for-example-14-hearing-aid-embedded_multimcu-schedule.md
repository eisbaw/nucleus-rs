---
id: TASK-0054.01
title: >-
  M11 reinstate per-frame peripheral kernels for example 14 (hearing-aid
  embedded_multimcu schedule)
status: In Progress
assignee:
  - '@Claude'
created_date: '2026-05-27 11:11'
updated_date: '2026-05-29 05:57'
labels:
  - M11
  - examples
dependencies: []
parent_task_id: TASK-0054
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0054 cycle 201 follow-up. The embedded_multimcu schedule at nuc-nucleus/examples/14-hearing-aid/schedules/embedded_multimcu.sched.nuc references per-frame peripheral kernels (fe_capture / rf_receive / fe_emit / rf_transmit) that the cycle-201 tier-1 bulk-IO algorithm does NOT declare. The schedule file is preserved AS-IS as the M11 design target; all 7 e2e cells are currently [[skip]]'d (TASK-0054.01 M11-deferred reason).

When M11 (multi-MCU Renode co-simulation) lands, this task delivers:

1. Algorithm extension to declare the per-frame peripheral kernels. Likely paths:
   (a) Second algorithm file (`prog.algo.M11.nuc` or similar) with the stateful peripheral kernels; the tier-1 vs M11 worlds share `mix2` + `denoise` but differ in IO.
   (b) Algorithm-level `tier` discriminator that selects between bulk-IO and per-frame-IO declarations at compile time.
   (c) Add fe_capture/rf_receive/fe_emit/rf_transmit kernels back to the SHARED algorithm; tier-1 schedules + kernels.rs would need to deal with them (likely stub them as no-op variants of load_*/save_*).
   
   Option (a) is cleanest but introduces a second algorithm file (more code to maintain). Option (b) is least disruptive but requires algorithm-sublanguage extension. Option (c) requires per-process state for the stateful kernels that breaks multi-process backends (mp-tcp-bufsync / etc.).

2. Stateful per-frame kernel implementations in kernels.rs (or a new kernels.M11.rs). The fe_capture/rf_receive kernels track a call-count via AtomicUsize and read frame[counter] from canned bins; fe_emit/rf_transmit write frame[counter] to accumulating buffers flushed on Drop. ONLY safe for single-process M11/Renode mode.

3. Renode multi-MCU substrate (M11 milestone scope — depends on TASK-0064 Renode integration). Three MCUs: FE_MCU + DSP_MCU + RF_MCU connected over SPI.

4. Promote all 7 currently-[[skip]]'d embedded_multimcu cells to [[required]] once the schedule lowers + runs bit-identical against an updated reference impl.

Status: LOW priority, deferred-not-cancelled. Parent: TASK-0054. Related: TASK-0064 (Renode flake), TASK-0048 (M10 STM32H7 Renode shim), TASK-0049 (M11 multi-MCU Renode co-simulation).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A per-frame embedded algorithm artifact exists (option (a): separate file, e.g. nuc-nucleus/examples/14-hearing-aid/prog.embedded.algo.nuc) declaring the four per-frame peripheral kernels fe_capture/rf_receive (() -> i32[SAMPLES_PER_FRAME] effectful) and fe_emit/rf_transmit ((i32[SAMPLES_PER_FRAME]) -> () effectful), reusing mix2/denoise; tier-1 prog.algo.nuc is UNTOUCHED
- [ ] #2 Per-frame dataflow uses an explicit 'mixed' intermediate (NO nested kernel call inside an argument expression — v2 codegen rejects denoise(mix2(...))); i32 + wrapping arithmetic for cross-backend determinism (PRD 10.1)
- [ ] #3 embedded_multimcu.sched.nuc's 'schedule for' points at the new algorithm and EVERY place'd kernel (fe_capture/fe_emit/rf_receive/rf_transmit/mix2/denoise) resolves to a declared kernel; the schedule parses (sched_parser green)
- [ ] #4 The new algorithm parses and lowers in isolation (algo_parser + algo_lower green); design decision (option a chosen over b/c) recorded with rationale; remaining parts (stateful per-frame kernels.rs impl, Renode multi-MCU substrate, e2e cell promotion) explicitly left as deep-codegen follow-ups with filed task refs
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Create nuc-nucleus/examples/14-hearing-aid/prog.embedded.algo.nuc (option (a), the cleanest per the AC). Port the OLD per-frame peripheral shape (commit 2cc372c) to i32 + wrapping arithmetic, reusing const N_FRAMES=4 / SAMPLES_PER_FRAME=16 and the data symbols mic_in/bt_in/spk_out/bt_out/mixed. Declare the 4 per-frame peripheral kernels fe_capture/rf_receive (() -> i32[SAMPLES_PER_FRAME] effectful) and fe_emit/rf_transmit ((i32[SAMPLES_PER_FRAME]) -> () effectful). Reuse mix2/denoise. Use the explicit mixed intermediate (NO nested denoise(mix2(...)) — v2 rejects nested kernel call in arg expr). Header records option-(a) rationale + scope/IO-model split. Tier-1 prog.algo.nuc UNTOUCHED.
2. Point embedded_multimcu.sched.nuc 'schedule for' at "../prog.embedded.algo.nuc" (algo_path is stored-verbatim/informational; the e2e harness hardcodes prog.algo.nuc and these cells stay [[skip]], so this is documentary). Update the schedule header lie ('does NOT lower against the current prog.algo.nuc') accordingly.
3. AC#4 evidence tests: algo_parser + algo_lower positive tests for prog.embedded.algo.nuc (parses + lowers in isolation). AC#3 evidence: sched_parser test that the embedded schedule still parses + every placed kernel resolves against the new algo (cross-resolve via link or name check).
4. Gate: nix develop --command bash -c 'just build && just clippy && just test && just test-release && just e2e'. e2e baseline 301/246/0/55/0 must not regress (7 embedded cells stay [[skip]]). Report actual numbers.
5. TASK-0192 (separate task): attempt admission into sched_lower/link/acfg; file precise TASK-0049 gap subtasks for suites that cannot admit; never weaken assertions.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Implemented (cycle: M11 entry) ===
DELIVERED (algorithm + lowering-admission only; deep-codegen parts deferred):
- NEW file nuc-nucleus/examples/14-hearing-aid/prog.embedded.algo.nuc (option (a), separate file — rationale recorded in its header: tier-1 stays byte-stable for 7 passing naive cells; per-frame kernels are stateful, breaking multi-process tier-1, ruling out option (c); option (b) needs a sublanguage extension that does not exist). Declares the 4 per-frame peripherals fe_capture/rf_receive (() -> i32[SAMPLES_PER_FRAME] effectful) + fe_emit/rf_transmit ((i32[SAMPLES_PER_FRAME]) -> () effectful), reuses mix2/denoise, i32 + wrapping, explicit 'mixed' intermediate (NO nested kernel call). tier-1 prog.algo.nuc UNTOUCHED.
- embedded_multimcu.sched.nuc 'schedule for' repointed to '../prog.embedded.algo.nuc'; stale header lie ('does NOT lower against the current prog.algo.nuc') + 'over SPI' both corrected (UART hub per TASK-0049.01).
- Evidence tests: parses_example_14_hearing_aid_embedded (algo_parser), lowers_example_14_hearing_aid_embedded (algo_lower), embedded_multimcu_places_resolve_against_embedded_algo (sched_parser — pure name-set cross-resolution of all 6 placed kernels against the new algo's declared kernels).

GOTCHAS / forward-carried from TASK-0054.01:
1. The e2e harness (nucleus/e2e/src/main.rs:1286 + :1766) HARDCODES ex_dir.join('prog.algo.nuc') as the algo for EVERY cell — it does NOT read the schedule's 'schedule for' path to pick the algo. So prog.embedded.algo.nuc is INVISIBLE to the e2e matrix today; promoting the 7 [[skip]]'d embedded_multimcu cells requires teaching the harness to honour 'schedule for' (or a per-cell algo override in e2e-matrix.toml) — that is part of the deep-codegen follow-up, NOT this slice. Filed observation, no separate task yet (subsumed by TASK-0054.01 parts 2-4 + TASK-0049).
2. sched AST's algo_path is stored-verbatim/informational (parser test algo_path_stored_verbatim_no_resolution pins this) — repointing 'schedule for' is safe and changes no lowering behaviour.
3. The two check-* gate recipes scan only nucleus/**/*.rs; a second example .algo.nuc read via std::fs (not include_str!) needs no compile-coverage shim.

GATE (full, all arms): build OK; clippy OK (0 warns); test 1085/0/3 (+6 vs 1079 baseline); test-release 1084/0/3 (+6 vs 1078); e2e 301/246/0/55/0 (UNCHANGED — 7 embedded cells stay [[skip]]).
<!-- SECTION:NOTES:END -->
