---
id: TASK-0340.04
title: 'Slice 5: split nucleus/backends/mp-tcp-event/src/multi_worker.rs (1695 LoC)'
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 12:27'
updated_date: '2026-05-26 12:47'
labels:
  - tech-debt
  - hygiene
dependencies: []
parent_task_id: TASK-0340
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0340 AC#2 slice 5: split mp-tcp-event/src/multi_worker.rs (1695 LoC, the second-largest remaining mega-file after cycle-179 mp-tcp-bufsync slice). Plan impl spans ~1030 LoC alone — same pattern as slice 4, requires multi-file impl Plan<'_> blocks per Rust convention. NOTE: do NOT silently fix the Err(_e) match-arm indentation skew at line 896 (the cross-backend asymmetry tracked by TASK-0340.01.01); slice 5 is a PURE MECHANICAL MOVE per the cycle-179b disclosure-honesty lesson.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 180 — slice 5 implementation plan

Split mp-tcp-event/src/multi_worker.rs (1695 LoC) into multi_worker/ directory of 5 files. Same shape as cycle-179 mp-tcp-bufsync slice-4 with naming adapted to mp-tcp-event's surface.

**File breakdown** (Plan-method line markers in current file):
- multi_worker/mod.rs (~340 LoC): module //! docstring lines 1-63 verbatim + use statements + ChanId type + struct Plan + impl Plan { build, worker_name, data_name, non_host_workers, worker_chans, peer_index_for, chan_peer_index, collect_pre_init, max_payload_bytes, barriers_used_by } + sub-module decls + render_run_sh + #[cfg(test)] mod tests (verbatim) + SO_BUF_COMMENT_EVENT const.
- multi_worker/worker_program.rs (~480 LoC): impl Plan<'_> { render_worker_program (492-692), emit_handshake (786-965), emit_reactor_and_chans (969-1033), emit_barrier_shims (1041-1128) }.
- multi_worker/relay.rs (~110 LoC): impl Plan<'_> { relay_schedule (705-718), render_relay_phase (735-778) }.
- multi_worker/walkers.rs (~340 LoC): detect_wait_before_push_hazard (1154-1229), collect_push_pairs (1242-1256), struct RelayHop (1265-1270), relay_phase_insertion_point (1312-1367), collect_w2w_pushes (1418-1425), collect_w2w_pushes_inner (1427-1482).
- multi_worker/encode.rs (~85 LoC): encode_decode_paths (1489-1523), scalar_fn_suffix (1525-1541), scalar_width (1543-1551).

**Visibility rules:**
- pub(crate): items lib.rs uses (Plan, ChanId, render_run_sh; build / worker_name / data_name / non_host_workers / render_worker_program — verify by grep).
- pub(super): items consumed across multi_worker/ sub-modules but not by lib.rs.
- private: items consumed in only one file.

**Walkers visibility:**
- detect_wait_before_push_hazard: called from mod.rs's build → pub(super).
- collect_push_pairs: called from mod.rs's build → pub(super).
- relay_phase_insertion_point: called from worker_program.rs's render_worker_program → pub(super).
- collect_w2w_pushes: called from relay.rs's relay_schedule → pub(super).
- collect_w2w_pushes_inner: called only from walkers.rs's collect_w2w_pushes → private.
- struct RelayHop: constructed in walkers.rs, returned via Plan::relay_schedule consumed in render_relay_phase → pub(super) (sibling-only).
- scalar_width: called in mod.rs's max_payload_bytes → pub(super).
- encode_decode_paths: called in worker_program.rs's emit_reactor_and_chans → pub(super).
- scalar_fn_suffix: called only from encode_decode_paths → private.

**Constraints:**
- Module docstring lines 1-63 stays VERBATIM in mod.rs (cycle 179 carry-over discipline).
- No silent indentation fix on line 896 Err(_e) — TASK-0340.01.01 follow-up tracks the cross-backend skew.
- All backend_common::* references in sub-modules use file-head use statements.
- multi_worker/mod.rs's impl block uses explicit <'a> (build returns Plan<'a>); sub-module impls use impl Plan<'_>.
- AC#6: no new TASK-NNNN / cycle-NNN citations in refactored files (lenient reading: existing anchors copied with their code OK).

**Verification:** just build && just clippy && just test && just test-release && just check-mega-files && just e2e (2 samples).

Plan recorded; proceeding to edit.

## Cycle 180 — slice 5 LANDED

Split nucleus/backends/mp-tcp-event/src/multi_worker.rs (1695 LoC) into multi_worker/{mod,worker_program,relay,walkers,encode}.rs along the existing impl-method + free-function seams. Sibling shape to cycle-179 mp-tcp-bufsync slice-4. AC#2 now 3/6.

**Per-file LoC after split** (vs old multi_worker.rs 1695):
- src/multi_worker/mod.rs              649  module //! docstring (verbatim) + use statements + ChanId type + struct Plan<'a> + impl<'a> Plan<'a> { build, worker_name, data_name, non_host_workers, worker_chans, peer_index_for, chan_peer_index, collect_pre_init, max_payload_bytes, barriers_used_by } + SO_BUF_COMMENT_EVENT const + render_run_sh + #[cfg(test)] mod tests
- src/multi_worker/worker_program.rs   578  impl Plan<'_> { render_worker_program, emit_handshake, emit_reactor_and_chans, emit_barrier_shims }
- src/multi_worker/relay.rs            104  impl Plan<'_> { relay_schedule, render_relay_phase }
- src/multi_worker/walkers.rs          355  detect_wait_before_push_hazard, collect_push_pairs, struct RelayHop, relay_phase_insertion_point, collect_w2w_pushes, collect_w2w_pushes_inner
- src/multi_worker/encode.rs            74  encode_decode_paths, scalar_fn_suffix, scalar_width
- (sum 1760; +65 vs pre-split 1695 is per-file module //! docstrings + use statements + 2 sub-module impl Plan<'_> braces; no behaviour change)

**Behaviour-equivalent change list** (empirically diffed per moved item; cycle-179b enumerated-disclosure discipline):
- (a) collect_pre_init: signature reflowed into 4 lines from 1 due to rustfmt forcing multi-line after pub(super) widening. Bodies byte-identical.
- (b) Plan::max_payload_bytes: bare scalar_width(...) call requalified to encode::scalar_width(...) — the function moved to sub-module encode.rs. Bodies otherwise byte-identical.
- (c) detect_wait_before_push_hazard, collect_push_pairs, relay_phase_insertion_point, collect_w2w_pushes, encode_decode_paths, scalar_width, worker_chans, peer_index_for, chan_peer_index, collect_pre_init, barriers_used_by, relay_schedule, render_relay_phase: visibility changed from fn (private) to pub(super) — required for sibling-module access from siblings of mod.rs. Bodies byte-identical.
- (d) struct RelayHop: visibility changed from private struct + private fields to pub(super) struct + pub(super) fields — required for relay.rs to construct and read RelayHop instances received from walkers::collect_w2w_pushes. Bodies byte-identical.

All four classes are mechanical visibility/path requalification consequences of the move. Bit-identical emit verified by e2e 112/102/0/10/0 reproduced across TWO independent samples (non-flake).

**Public surface preserved verbatim:**
- pub(crate) struct Plan<'a> + pub(crate) type ChanId + pub(crate) fn render_run_sh (lib.rs reaches these via use multi_worker::Plan; / multi_worker::render_run_sh).
- pub(crate) Plan methods used by lib.rs: build, worker_name, used_workers (field), render_worker_program.
- External consumers unchanged: lib.rs (only structural call site within the crate); the crate's outward surface (pub fn emit + pub struct EmitResult + pub use EmitError in lib.rs) untouched.

**Visibility scheme:**
- pub(crate): items lib.rs reaches (Plan, ChanId, render_run_sh; build, worker_name, used_workers field, render_worker_program).
- pub(super): items reached across multi_worker/ sub-modules (worker_chans, peer_index_for, chan_peer_index, collect_pre_init, barriers_used_by in mod.rs; encode_decode_paths + scalar_width in encode.rs; relay_schedule + render_relay_phase in relay.rs; detect_wait_before_push_hazard + collect_push_pairs + relay_phase_insertion_point + collect_w2w_pushes + struct RelayHop in walkers.rs).
- private: items consumed in only one file (max_payload_bytes in mod.rs; emit_handshake/emit_reactor_and_chans/emit_barrier_shims in worker_program.rs; collect_w2w_pushes_inner in walkers.rs; scalar_fn_suffix in encode.rs).

**Per-file //! orientation docstrings** added on the 4 new sub-module files (mod.rs's is the verbatim original module docstring lines 1-63). The 4 new //! docstrings repeat existing TASK-NNNN anchors only (TASK-0176, TASK-0233, TASK-0172, TASK-0327, TASK-0042.05 — all pre-existing in original code, verified by grep). Per parent TASK-0340 cycle-179b lenient AC#6 reading, this is permitted.

**Module docstring** (lines 1-63 of original) preserved BYTE-IDENTICAL in mod.rs.

**Multi-file impl<'a> Plan<'a> clippy compliance:**
- mod.rs: impl<'a> Plan<'a> (build returns Self bounded by 'a — explicit lifetime needed).
- worker_program.rs + relay.rs: impl Plan<'_> (no method signature in those impls references 'a; clippy::needless_lifetimes would fire otherwise). Same pattern as cycle-179 bufsync split.

**Justfile allow-list:**
Removed 'nucleus/backends/mp-tcp-event/src/multi_worker.rs' line from the printf list at justfile:457-466. Allow-list shrank from 12 to 11 entries. Direction A (new mega-file outside list) + direction B (stale entry) both OK per just check-mega-files.

**NOT FIXED in this slice** (deliberately, per brief gotcha (B) + cycle-179b lesson):
- The 12-space indentation on the emitted Err(_e) match-arm at the original line 896 (now in worker_program.rs's emit_handshake read_rendezvous_port format string). Stays as-is. Tracked separately by TASK-0340.01.01.
- The 'feedback-comment-doc-lie-recurring' pattern in the docstrings (e.g. the LAST-Sync vs FIRST-Sync narrative in relay_phase_insertion_point's docstring — those are pre-existing claims, not new). Audit out of scope.

**Verification gate (orchestrator-self-run, inside nix develop):**
- just build (workspace): clean.
- just clippy (-D warnings --all-targets): clean (after removing one unused-import warning on the initial extract — the ScalarType import was needed only inside encode.rs after the split; mod.rs's prelude had no remaining consumer).
- just test (dev): 969 / 0 / 3.
- just test-release: 968 / 0 / 3 (-1 vs dev is debug_assert-only #[should_panic] negative; expected per TASK-0291).
- just e2e: 112 / 102 / 0 / 10 / 0 — TWO independent samples, both matching cycle-179b baseline exactly (non-flake).
- just check-mega-files: OK both directions; allow-list shrank from 12 to 11 entries.

**Subtleties / forward-carries for slices 6+:**
- The Plan-with-multi-file-impl pattern is now landed for THREE backends (mp-tcp-bufsync slice-4 + mp-tcp-event slice-5; pthreads-async/multi_worker.rs at 1048 LoC is the cycle-179b non-audit candidate that would benefit from the same pattern). Slice 6+ targets (acfg.rs / link.rs / multi_worker_walker.rs) are NOT Plan-shaped; their natural seams will look different (algo lowering passes / linker / shared walker substrate).
- Slice 5 (this slice) was confirmed cleaner than slice 4 in disclosure-honesty terms: NO indentation skew fix, NO inline-vs-import drift across files (all backend_common::* are file-head use statements in every new sub-module — see worker_program.rs:14-20, walkers.rs uses none beyond crate::, etc).
- 11 mega-files >1000 LoC remain after slice 5. Three are the originally-named TASK-0340 audit targets (acfg.rs, link.rs, multi_worker_walker.rs); the remainder are the cycle-179b out-of-audit set.

**No new tracker tasks filed** — no latent defects discovered during the move. (The line-896 indentation skew is tracked by TASK-0340.01.01, filed in cycle 179b.)

**AC status post-slice-5:**
- AC#1: DONE (cycle 176 + 178; audit list still accurate).
- AC#2: 3/6 files split (render.rs + mp-tcp-bufsync/lib.rs + mp-tcp-event/multi_worker.rs). Remaining: acfg.rs, link.rs, multi_worker_walker.rs.
- AC#3: PENDING (slice 9 proptest dep + ≥3 properties on Petri-net IR passes).
- AC#4: PENDING (slice 10 e2e/main.rs report-formatter carve-out).
- AC#5: DONE; allow-list shrank further this slice (12 → 11).
- AC#6: DONE for this slice (per parent task's lenient reading; only pre-existing TASK-NNNN anchors repeated in new //! orientation docstrings).
- AC#7: PENDING (final cycle when AC#2 + AC#3 close).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## Slice 5 LANDED (cycle 180)

Split nucleus/backends/mp-tcp-event/src/multi_worker.rs (1695 LoC) into multi_worker/ directory of 5 cohesive sub-modules along impl-method + free-function seams. Sibling shape to cycle-179 mp-tcp-bufsync slice-4.

**Per-file LoC after split** (sum 1760; +65 vs pre-split = per-file //! docstrings + use statements + 2 sub-module impl Plan<'_> braces):
- multi_worker/mod.rs              649  module docstring (verbatim) + Plan struct + impl<'a> Plan<'a> { build, accessors, max_payload_bytes, barriers_used_by } + render_run_sh + #[cfg(test)] mod tests
- multi_worker/worker_program.rs   578  impl Plan<'_> { render_worker_program, emit_handshake, emit_reactor_and_chans, emit_barrier_shims }
- multi_worker/relay.rs            104  impl Plan<'_> { relay_schedule, render_relay_phase }
- multi_worker/walkers.rs          355  detect_wait_before_push_hazard, collect_push_pairs, RelayHop, relay_phase_insertion_point, collect_w2w_pushes(_inner)
- multi_worker/encode.rs            74  encode_decode_paths, scalar_fn_suffix, scalar_width

**Behaviour-equivalent change list** (empirical diff per moved item):
- (a) collect_pre_init signature reflowed across 4 lines (rustfmt-forced after pub(super) widening).
- (b) Plan::max_payload_bytes: scalar_width(...) requalified to encode::scalar_width(...) (function moved sub-module).
- (c) 13 items had visibility uplift fn → pub(super) (required for sibling-module access).
- (d) struct RelayHop + 4 fields uplifted from private to pub(super).
All four classes are mechanical move-consequences; function bodies byte-identical (line-by-line diff on every moved fn confirmed empty, excepting the 2 specific edits enumerated above).

**Public surface preserved verbatim** — lib.rs sees the same pub(crate) Plan struct, ChanId type, render_run_sh function, and pub(crate) methods (build, worker_name, used_workers field, render_worker_program) it saw before. Crate's outward pub fn emit + pub struct EmitResult unchanged.

**Justfile allow-list shrank from 12 to 11 entries** (mp-tcp-event/src/multi_worker.rs line removed; allow-list still positive-enumerated; both directions of just check-mega-files OK).

**Verification gate (orchestrator-self-run, inside nix develop):**
- just build (workspace): clean.
- just clippy (-D warnings --all-targets): clean.
- just test (dev): 969 / 0 / 3.
- just test-release: 968 / 0 / 3.
- just e2e: 112 / 102 / 0 / 10 / 0 — TWO independent samples both at the same totals line (non-flake).
- just check-mega-files: OK both directions.

**Subtleties / forward-carries:**
- Multi-file impl Plan<'_> pattern now landed for 3 of 4 backends (bufsync + event split; pthreads-async/multi_worker.rs at 1048 LoC is the non-audit candidate).
- Slice 5 was clean by self-audit (no silent indentation fix, no inline-vs-import drift) — cycle-179b enumerated-disclosure discipline applied directly. The 2 mechanical reflow + path-requalify edits (a) + (b) were flagged BEFORE review-gate, not after.
- Per-file //! orientation docstrings on the 4 new sub-module files repeat only EXISTING TASK anchors (TASK-0176, TASK-0233, TASK-0172, TASK-0327, TASK-0042.05 — all pre-existing in original code, grep-verified) — permitted under parent task's lenient AC#6 reading.

**NOT FIXED in this slice** (deliberately):
- The 12-space indentation on the emitted Err(_e) match-arm in emit_handshake's read_rendezvous_port format string. Tracked by TASK-0340.01.01.
- Pre-existing comment/doc claims that may have rotted (the relay_phase_insertion_point LAST-vs-FIRST-Sync narrative, the chan_peer_index host-relay claim). Out of scope; AC#6 forbids new audit work in this slice.

**AC status post-slice-5:**
- AC#1: DONE (cycle 176 + 178; audit list accurate).
- AC#2: 3/6 files split (render.rs + mp-tcp-bufsync/lib.rs + mp-tcp-event/multi_worker.rs). Remaining: acfg.rs, link.rs, multi_worker_walker.rs.
- AC#3: PENDING.
- AC#4: PENDING.
- AC#5: DONE; allow-list shrank further (12 → 11).
- AC#6: DONE for this slice (lenient reading per parent cycle-179b clarification).
- AC#7: PENDING (final cycle).

**No new tracker tasks filed.** No latent defects discovered during the move.
<!-- SECTION:FINAL_SUMMARY:END -->
