---
id: TASK-0340.10
title: >-
  check-mega-files FAILS on embedded-pattern lib.rs (1134) + skeleton.rs (1029)
  — new post-split-wave mega-files block just ci
status: Done
assignee:
  - '@mark'
created_date: '2026-05-29 16:26'
updated_date: '2026-05-29 17:31'
labels:
  - tech-debt
  - hygiene
  - just-ci
  - embedded-pattern
dependencies: []
parent_task_id: TASK-0340
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Discovered during TASK-0340.08.01 review gate (qa-test-runner + mped-architect, both independently confirmed; orchestrator re-ran 'just check-mega-files' = exit 1). The full hard gate 'just ci' is currently RED — NOT a regression from TASK-0340.08.01 (test-only) but a pre-existing latent breakage. Two embedded-pattern source files exceed the 1000-LoC check-mega-files threshold and are absent from the allow-list:

  - nucleus/backends/embedded-pattern/src/lib.rs      = 1134 LoC
  - nucleus/backends/embedded-pattern/src/skeleton.rs = 1029 LoC

Verified both were ALREADY over-threshold at parent commit 0aac495; they grew under the M11 work (TASK-0049.04 / 0049.06 — commits 0a80394 / a76cb6b / fb9e90e). The original TASK-0340 split-wave predates this growth, so these are silent-sibling mega-files the wave never covered (file-hygiene analogue of feedback-silent-sibling-defect). check-mega-files scopes to nucleus/**/src so these ARE in scope (unlike tests/ — see TASK-0342).

The cheap pre-commit gate (build/clippy/test/test-release/e2e) is GREEN; only the full 'just ci' check-mega-files arm bites. e2e baseline is 308/246/0/62/0 (independently re-run x2 this cycle; the 120/110 in memory and 210/190 in older commits are stale).

Recipe's own fix preference order (justfile check-mega-files): (1) split along module-docstring seams [preferred], (2) allow-list with one-line rationale, (3) defer/revert. Prefer (1) per TASK-0340 AC#2 — split lib.rs and skeleton.rs into cohesive sub-modules.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 check-mega-files passes (exit 0) with embedded-pattern/src/{lib.rs,skeleton.rs} each either split below 1000 LoC along docstring seams OR allow-listed with a one-line rationale
- [x] #2 just ci runs through the check-mega-files arm without failing on these two files
- [x] #3 If split: no behavioural change to emitted firmware (e2e baseline 308/246/0/62/0 preserved bit-identical; embedded codegen tests green)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Implementation Plan (cycle, implementer @mark)

Confirmed BEFORE state: `just check-mega-files` exits 1; lib.rs=1134, skeleton.rs=1029 (both >1000). tests.rs=956 (under, do NOT push code there). kernel_extract.rs=172.

PURE MECHANICAL MODULE SPLIT, zero behavioural change. Two seams:

SKELETON.RS -> directory module skeleton/{mod.rs,bin.rs}:
- mod.rs keeps LIB templates (module docstring, NUCLEUS_SHIM_SRC, NucleusShim trait, StubShim, render_count_statics, render_lib, render_cargo_toml) + the inline #[cfg(test)] mod tests (it exercises BOTH lib and bin templates).
- bin.rs gets the M10 BIN block (USART1_SHIM_SRC + consts, usart1_*/systick_init, Usart1Shim, CountSummary, render_bin_main, render_bin_cargo_toml, render_memory_x, render_build_rs, render_cargo_config).
- mod.rs adds `pub use bin::*;` so lib.rs call sites (skeleton::render_bin_main / USART1_SHIM_SRC / render_memory_x / render_build_rs / render_cargo_config / render_bin_cargo_toml + CountSummary) resolve UNCHANGED.
- render_count_statics shared by render_lib (mod.rs) + render_bin_main (bin.rs) -> make it pub(super) (or pub(crate)) so bin.rs can call it; NUCLEUS_SHIM_SRC already pub.
- The mod skeleton; declaration in lib.rs stays identical (Rust resolves skeleton.rs OR skeleton/mod.rs).

LIB.RS -> extract run<S> body lowering into sibling mod render (render.rs):
- Move render_run_body, collect_data_decls, collect_referenced_data, collect_arg_data, render_events, render_event, render_fire, first_data_input into render.rs as pub(crate)/pub(super); add `use render::*;`.
- Keep emit/emit_bin/result structs/render_lib_rs/render_main_rs/lower_kernels_and_run/CountCheckLoop+collect_count_check_loops/collect_pure_kernel_names*/is_pure_compute_fire/kernel_name/write_file in lib.rs.
- write_file decision: keep in lib.rs (used by emit/emit_bin); render.rs does not need it.

HARD GATES: every embedded-pattern/src/*.rs <1000 LoC; e2e baseline 308/246/0/62/0 EXACT; check-mega-files exit 0; NO emit-string test assertion changes; clippy clean (re-run); just test AND test-release.

Renode arm NOT runnable here (which renode fails). Will try `nix develop .#embedded` for check-embedded; report if unavailable. Pure code-movement changes no emitted bytes by construction.

## Gate results (all GREEN, baseline preserved)

check-mega-files: BEFORE=FAIL exit 1 (lib.rs 1134, skeleton.rs 1029 flagged direction-A). AFTER=OK exit 0 (no non-allow-listed file >1000; no stale allow-list entry). NO allow-list entry added — split-only fix (recipe preference #1).

Post-split LoC (every embedded-pattern/src/*.rs, all <1000):
- lib.rs        651 (was 1134) — emit/emit_bin/result-structs/render_lib_rs/render_main_rs/lower_kernels_and_run/CountCheckLoop+collect_count_check_loops/collect_pure_kernel_names*/is_pure_compute_fire/kernel_name/write_file
- render.rs     507 (NEW)      — render_run_body (pub(crate)) + private helpers collect_data_decls/collect_referenced_data/collect_arg_data/render_events/render_event/render_fire/first_data_input
- skeleton/mod.rs 501 (was skeleton.rs 1029) — LIB templates (NUCLEUS_SHIM_SRC, NucleusShim trait, StubShim, render_count_statics pub(super), render_lib, render_cargo_toml) + the inline #[cfg(test)] mod tests
- skeleton/bin.rs 562 (NEW)    — M10 BIN block (USART1_SHIM_SRC + consts, usart1_*/systick_init, Usart1Shim, CountSummary, render_bin_main, render_bin_cargo_toml, render_memory_x, render_build_rs, render_cargo_config)
- kernel_extract.rs 172 (unchanged)
- tests.rs        956 (UNCHANGED — 0 lines diff vs HEAD; the emit-string drift detector is byte-for-byte untouched)

just build: OK. just clippy (-D warnings): clean. just test (dev): 1104 passed / 0 failed. just test-release: 1103 passed / 0 failed (1 fewer = debug_assert-gated #[should_panic] compiled-out in release; expected, unrelated). just e2e: total 308 pass 246 fail 0 skipped 62 required-fail 0 == baseline EXACTLY.

embedded-pattern unit tests: 27 passed, IDENTICAL assertions (no test edited). Renode runtime arm NOT run (renode not installed: which renode fails). HOWEVER check-embedded DID run under nix develop .#embedded and PASSES (exit 0): real thumbv7em-none-eabihf cross-compile of examples 1+5, 02-split-add 2-worker LIB, and the REAL example-14 3-worker LIB — strongest available behavioural-invariance proof for this backend short of a Renode run.

## Gotchas / visibility widening
- render_count_statics: fn -> pub(super) so skeleton/bin.rs::render_bin_main can call the shared helper (lib path render_lib stays in skeleton/mod.rs).
- kernel_name: fn -> pub(crate) so render.rs::render_fire can resolve KernelId->name (also still used by collect_pure_kernel_names_in in lib.rs).
- render_run_body: fn -> pub(crate), re-imported into lib.rs via 'use render::render_run_body;' (called by lower_kernels_and_run). All other moved fns stay module-private in render.rs.
- write_file: STAYED in lib.rs (used by emit/emit_bin; render.rs does not need it).
- skeleton/mod.rs adds 'mod bin; pub use bin::*;' so every existing skeleton::* call site (lib.rs + inline tests via super::*) resolves UNCHANGED.
- Imports trimmed from lib.rs (now only used by render.rs): fmt::Write, backend_common::render::{data_name,render_fire_args_nostd,render_loop_bounds,RenderCtx}, nucleus_compiler::event::DataId.
- Intra-doc-link note: changed [`render_event`] -> [`render::render_event`] in emit() docstring. cargo doc --document-private-items shows the SAME warning categories as HEAD baseline (private-item links + NucleusShim/StubShim string-literal dangles that never resolved — those types exist only inside emitted-code string literals, not as host-crate items). NO new warning category; gate (just ci) does not run cargo doc anyway. No follow-up needed.
- git rename heuristic displays 'skeleton.rs => skeleton/bin.rs' (bin.rs inherited the largest verbatim block); actual git mv was to skeleton/mod.rs. Cosmetic only.

## Forward-carry to TASK-0049.05 (multi-MCU BIN slice)
The bin emit path now lives in TWO places: (1) the BIN string TEMPLATES (render_bin_main + Usart1Shim + Cargo/memory.x/build.rs/config templates) are in nucleus/backends/embedded-pattern/src/skeleton/bin.rs; (2) the emit_bin ENTRY POINT + its single-worker rejection guard + render_main_rs are still in lib.rs. The SHARED event lowering (render_run_body etc.) is in render.rs. TASK-0049.05 will widen emit_bin (lib.rs) + extend skeleton/bin.rs (new multi-machine .resc / UART-hub templates).

=== Orchestrator review-gate close (cycle, phase3-backlog-ralph) ===
Parallel read-only gate: qa-test-runner + mped-architect, both independent, both GO.

VERIFIED-BY-ORCHESTRATOR (re-run, not transcribed): check-mega-files exit 0 ('OK: no non-allow-listed ... exceeds 1000 LoC'); e2e 308/246/0/62/0 (qa ran x2 identical); just test 1104/0, test-release 1103/0; clippy clean; tests.rs 0-line diff (behavioural invariance); check-embedded (thumbv7em ARM cross-compile of ex1/ex5/02-split-add 2-worker/ex14 3-worker) PASS under .#embedded.

mped-architect P0: TRUE pure move — emit-string consts USART1_SHIM_SRC (231 lines) + NUCLEUS_SHIM_SRC (71 lines) byte-identical (the dangerous surface is clean); moved bodies byte-identical modulo disclosed visibility keywords.

REVIEW FOLD-BACK applied in-thread (commit bc3f363, doc+visibility only; full cheap gate re-run green incl e2e 308/246/0/62/0 + check-mega-files exit 0):
- P1 (architect, the recurring feedback-verbatim-copy-comment-doc-lie pattern — fired AGAIN, this time during a mega-file SPLIT not a sibling-copy): skeleton/bin.rs:32 + :41 said 'see lib.rs render_fire' but THIS commit moved render_fire into render.rs -> live doc-lie. Fixed both -> 'see render.rs render_fire'.
- P2 (architect, over-widening): kernel_name was widened to pub(crate); architect empirically proved private fn suffices (render is a crate-root DESCENDANT, so ancestor-private items are reachable without widening). Narrowed pub(crate)->private fn + corrected the docstring rationale (it had claimed pub(crate) was required).
- P3 honesty note (no action): implementer's 'only one intra-doc link changed' disclosure omitted a necessary+benign [NucleusShim]-> link downgrade at render.rs:383 (correct, just undisclosed).

Tracker-md verified clean: exactly 3 ACs, all ticked, no leftover spurious AC from the --ac/--remove-ac footgun.

NET: Done stands. just ci check-mega-files arm: RED -> GREEN.

Correction to the P3 note above (a backlog-CLI/shell footgun bit it): the prior line lost a word to bash command-substitution of UNESCAPED backticks inside the double-quoted --append-notes string. Intended P3 text: the implementer disclosed changing only ONE intra-doc link, but a SECOND necessary+benign change existed at render.rs:383 — the [NucleusShim] intra-doc link was downgraded to plain monospace NucleusShim (the type is not in render.rs scope post-move, so the link would be a broken-intra-doc-link clippy error). Correct change, just undisclosed. LESSON: in --append-notes, single-quote the string or escape every backtick; unescaped backticks run as shell commands.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Split nucleus/backends/embedded-pattern/src/{lib.rs (1134), skeleton.rs (1029)} below the 1000-LoC check-mega-files threshold along docstring seams (recipe fix #1; no allow-list entry). skeleton.rs -> skeleton/{mod.rs 501 (M9 LIB templates + render_count_statics pub(super) + inline tests),bin.rs 562 (M10 Renode BIN block)} with 'pub use bin::*;' keeping all skeleton::* call sites unchanged. lib.rs 651 -> shared run<S>-body lowering extracted to sibling render.rs 507 (render_run_body pub(crate); kernel_name promoted pub(crate) for render_fire). write_file stayed in lib.rs. Every embedded-pattern/src/*.rs now <1000 (lib 651, render 507, skeleton/mod 501, skeleton/bin 562, kernel_extract 172, tests 956 untouched). Pure code movement, ZERO behavioural change. check-mega-files FAIL(exit1)->OK(exit0); build/clippy clean; test 1104/0; test-release 1103/0; e2e 308/246/0/62/0 (baseline exact); 27 embedded unit tests pass with IDENTICAL assertions (tests.rs 0-line diff); check-embedded passes under .#embedded (real thumbv7em cross-compile ex1/ex5/02-split/ex14). Renode runtime arm NOT run (renode not installed) — safe by construction (no emitted bytes change). Commit 11b2359. AC#3 (no behavioural change / e2e bit-identical) satisfied by baseline-hold + unchanged emit-string tests; Renode runtime specifically not exercised here.
<!-- SECTION:FINAL_SUMMARY:END -->
