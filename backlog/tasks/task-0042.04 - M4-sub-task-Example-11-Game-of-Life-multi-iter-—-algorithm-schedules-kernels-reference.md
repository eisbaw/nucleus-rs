---
id: TASK-0042.04
title: >-
  M4 sub-task: Example 11 (Game of Life multi-iter) — algorithm + schedules +
  kernels + reference
status: Done
assignee:
  - mped-architect-impl
created_date: '2026-05-21 13:10'
updated_date: '2026-05-22 10:52'
labels:
  - M4
  - examples
dependencies:
  - TASK-0134
parent_task_id: TASK-0042
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD §9 row 11: 'Game of Life (multi-iter) — Multi-iteration stencil, double buffer'. Square grid, fixed number of iterations; classic stencil but iterated. Naive.sched.nuc single-worker; pipelined.sched.nuc with double-buffer-via-pipeline=2 demonstrating the buffer-N + pipeline-D match.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 examples/11-game-of-life/ full scaffold per example-13 precedent (algo + schedules + kernels.rs + reference/ + I/O bins + README).
- [x] #2 naive.sched.nuc green on pthreads-sync today; pipelined.sched.nuc enters matrix when pthreads-async backend lands.
- [x] #3 i32 deterministic; reference/ std-only independent impl; sha256 documented.
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle-35 impl: scaffold landed.

Files created:
- nuc-nucleus/examples/11-game-of-life/prog.algo.nuc
- nuc-nucleus/examples/11-game-of-life/kernels.rs
- nuc-nucleus/examples/11-game-of-life/schedules/naive.sched.nuc
- nuc-nucleus/examples/11-game-of-life/schedules/pipelined.sched.nuc
- nuc-nucleus/examples/11-game-of-life/input.bin (sha256 51d6bc9ada0a63f847969bcd9aa1691193e416276c424190ee7fba1e81a0871f)
- nuc-nucleus/examples/11-game-of-life/reference.bin (sha256 f2c2069c773a5cfe7242d3f406ee210dbc9cfd0482f83d8982b18e116ff01a52)
- nuc-nucleus/examples/11-game-of-life/reference/Cargo.toml + src/main.rs
- nuc-nucleus/examples/11-game-of-life/README.md
- nuc-nucleus/e2e-matrix.toml updated (+6 cells: 4 required, 2 skip).

Algorithm choice: Option A (1D additive cellular automaton, NOT Conway 2D). Per task spec; halo+2D is TASK-0117/0126 territory. README §'Algorithm choice (option A vs B)' documents the choice.

Single-Dataflow constraint: PRD §6.2.1 SSA is per-data-symbol globally. A separate 'for i { grid[0][i] <-- ident(seed[i]) }' seed-staging loop alongside the iteration loop would be TWO Dataflows on grid -> DoubleAssignment. Solution: fold the seed case into the iteration body via a kernel-side branch on t (step_or_seed(pl, pm, pr, sm, t)). Matches example 04-prefix-sum's pattern (boundary/carry logic in Rust kernels, not Nuc). Single Dataflow on grid.

Codegen fix: pthreads-sync's render_int_expr was emitting bare 'ITERS'/'N' as Rust identifiers for IndexExpr const references — Nuc consts in IndexExpr (grammar §1 line 91-93 allows it) hit the unresolved-ident path. Examples 01..09/13 only used loop variables in IndexExpr, so the bug was inert. Fix: route through RenderCtx and consult sidecar.consts (same priority order as render_const_expr). 5 call sites updated; signature change is local to pthreads-sync.

e2e tally:
- before: 60 cells, 48 PASS, 9 SKIP
- after:  66 cells, 55 PASS, 11 SKIP
- delta:  +6 cells (+4 PASS @ {naive×{3 backends}, pipelined×pthreads-async}, +2 SKIP @ pipelined×{pthreads-sync, mp-tcp-bufsync})

Pipelined cell (M4 headline) is bit-identical to reference.bin (sha256 f2c2069c773a5cfe7242d3f406ee210dbc9cfd0482f83d8982b18e116ff01a52). pthreads-async emits Ring<T> capacity 2 (buffer=2) for grid.

Gates green:
- just e2e          PASS (66 cells, required-fail=0)
- just test         PASS (0 failures)
- just clippy       PASS
- just determinism-check-negative   PASS (NUC_NONDET_PERTURBED_CELLS=55)
- just xbackend-check-negative      PASS (16 corrupted, 1 detected)
- required_counts_strictly_grow_per_milestone   PASS (still strict)

ACs:
- AC#1 examples/11-game-of-life/ full scaffold per example-13 precedent: DONE
- AC#2 naive.sched.nuc green on pthreads-sync today; pipelined.sched.nuc enters matrix when pthreads-async backend lands: DONE (pthreads-async already landed cycles 26-27)
- AC#3 i32 deterministic; reference/ std-only independent impl; sha256 documented: DONE

Honest scope caveat in pipelined.sched.nuc + README: with step_or_seed alone on 'compute', the per-(t, i) body is single-worker on compute. transfer_inject's same-worker skip (TASK-0214) means no per-iteration Xfer is emitted between consecutive step_or_seed applications on compute. The 'loop t : pipeline=2' directive's primary effect on this schedule is driving the IR-level initial_marking=2 on the grid Push/Wait pair (link gate TASK-0134) and pinning the M4 capability surface end-to-end — not introducing genuine stage-parallelism inside the iteration loop. The headline stage-parallelism fixture is example 09 (two compute kernels on two compute workers). Example 11 is the iterated-state companion.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 35 (2026-05-22) — closed. Created nuc-nucleus/examples/11-game-of-life/ (9 new files): prog.algo.nuc, kernels.rs, schedules/{naive,pipelined}.sched.nuc, reference/{Cargo.toml, src/main.rs}, input.bin, reference.bin, README.md.

Algorithm choice: Option A (1D toroidal additive automaton, NOT Conway's 2D rule). Conway's 8-neighbour 2D stencil needs halo regions across distributed placements (TASK-0117/0126), out of scope. 1D wrap-around is the smallest concrete realisation of 'iterated stencil' the existing backends carry.

Spec: N=32, ITERS=8, next[i] = wrapping_add(cur[(i-1+N) % N], wrapping_add(cur[i], cur[(i+1) % N])). Single-Dataflow SSA constraint (PRD §6.2.1) forced pushing the seed-vs-step branch into the Rust kernel step_or_seed(pl,pm,pr,sm,t) with explicit t==0 path. Matches the boundary-in-Rust pattern example 04-prefix-sum already uses.

Hashes: input.bin sha256 51d6bc9ada0a..., reference.bin sha256 f2c2069c773a... — both pinned in README.

UNEXPECTED root-cause compiler fix landed in lockstep:
- pthreads_sync::render_int_expr signature changed from (e: &IrExpr, subst: &BTreeMap<String,String>) to (e: &IrExpr, ctx: &RenderCtx<'_>) to resolve declared consts via sidecar.consts. Bare const idents inside IndexExpr (e.g. ITERS in grid[(t + ITERS) % (ITERS + 1)][i]) were previously emitted as raw Rust identifiers — a compile error inert in examples 01-09/13 but tripped by Example 11. New precedence: abs_subst > consts > bare-ident, matching render_const_expr.
- 5 internal call-sites updated. mp-tcp-bufsync + pthreads-async inherit the fix free through render_flat_index_pub / render_const_expr_pub (TASK-0245 audit filed to verify by grep + add a synthetic multi-backend test).

Honest scope: pipelined.sched.nuc places step_or_seed on a single 'compute' worker. Same-worker carveout (TASK-0214) means no per-iteration Xfer between consecutive step applications, so pipeline=2 does NOT introduce genuine stage-parallelism in this schedule. PRIMARY effect: drives IR-level initial_marking=2 on the cross-worker grid Push/Wait pair (link-gate TASK-0134 validates the buffer match) and pins the M4 capability surface end-to-end. The genuine 2-stage parallelism cell is Example 9.

Tally: 66 / 55 / 0 / 11 (was 60 / 51 / 0 / 9). +6 cells (+4 PASS incl. headline pipelined × pthreads-async, +2 skip).

Gate (cycle 35): just test 0 FAILED; just clippy clean; e2e 66/55/0/11; NUC_NONDET_PERTURBED_CELLS=55; NUC_XBACKEND_CORRUPTED_DETECTED=1; cross-backend byte-identical verified post-fix (01-elementwise-add/naive × {sync, async}: BYTE-IDENTICAL).

Review-gate (parallel read-only): both GO. Architect flagged ONE MEDIUM (file pthreads-async/mp-tcp-bufsync IndexExpr renderer audit) — filed as TASK-0245 before commit. QA caught an implementer-side typo (48 vs 51 PASS in 'before' tally) — no real regression, just a misrecollection.
<!-- SECTION:FINAL_SUMMARY:END -->
