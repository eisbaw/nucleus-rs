---
id: TASK-0453.04
title: 'IMPL P4: symbolic looped-net soundness analysis (gate scaling)'
status: In Progress
assignee: []
created_date: '2026-06-06 22:54'
updated_date: '2026-06-07 01:09'
labels:
  - rigour
  - compiler
  - petri
dependencies: []
parent_task_id: TASK-0453
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Addresses Q2/W5 (ch11:172-181 'most concrete compiler work'; ch09 gate-linear-in-firings; ch10:214-226). The soundness gate replays a net expanded over the iteration space (linear in firings) - cheap for the corpus, won't scale to billions of firings. Reason symbolically about the LOOPED net structure (TASK-0159 already carries loop-nest structure in the contract) so the gate cost matches the problem-size-independent footprint of generated code. Residual: symbolic analysis may be sound for a subclass only - state it.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Gate decides soundness on a rolled/looped net without full firing-space expansion for at least one example class
- [x] #2 Gate verdict matches the expanded-net verdict on the corpus (equivalence pin)
- [x] #3 Subclass scope of the symbolic analysis documented
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
STEP-0 premise VERIFIED empirically (cycle-3): the gate is linear-in-firings on BOTH net construction and replay. Isolated measurements (matmul naive, varying N): N=16 -> 8199 net nodes, acfg_to_net 0.94ms + check_net_sound 4.27ms; N=32 -> 65543 nodes, 4.68+26.9ms; N=64 -> 524295 nodes, 37.3+226.6ms. Net nodes ~= 2x firings; gate time scales ~linearly with firings. Premise holds -> proceeding to implement. DESIGN: symbolic analyze_net_soundness_symbolic(acfg) walks the ROLLED ACFG (O(program text), N-independent) and proves soundness WITHOUT expansion for the BUFFER-FREE subclass (no Xfer placeholders => pure per-worker control chains => unconditionally bounded+live+conflict-free, a real theorem). All 3 rejection modes (overflow/stall/free-choice) each REQUIRE a buffer place (only Xfer creates one), so buffer-free can never skip a rejection: the fast-path is sound-equivalent-or-stronger by construction. Everything else -> NeedsExpansion -> existing expanded check_net_sound (no precision loss). Covers single-worker incl. the cited matmul 16^3; distributed-with-transfers is the documented residual.

IMPLEMENTED + GATE GREEN (cycle-3). New pass passes/net_soundness_symbolic.rs: analyze_net_soundness_symbolic(acfg)->SymbolicSoundness{ProvenSound|NeedsExpansion}. Walks the ROLLED ACFG; ProvenSound iff buffer-free (no Xfer => only per-worker control chains). THEOREM (proven by elimination over the 3 rejection modes, all of which require a buffer place): buffer-free => bounded+deadlock-free+conflict-free => expanded check_net_sound==Ok. Driver (driver/src/main.rs gate site) takes ProvenSound as a FAST PATH skipping acfg_to_net+check_net_sound; NeedsExpansion falls back to the UNCHANGED expanded gate => sound-equivalent-or-stronger (never skips a rejection; every unsound net carries a buffer place). MEASURED: single-worker matmul driver build now FLAT ~5ms from N=16 (4096 firings) to N=256 (16.7M firings), vs expanded gate 0.26s already at N=64 + a ~33M-node net at N=256. Tests tests/net_soundness_symbolic.rs (8): AC#1 single-worker ProvenSound / distributed NeedsExpansion; AC#2 combined-verdict==expanded over an 8-cell corpus + ProvenSound-iff-buffer-free tie + ProvenSound=>expanded-Ok; iteration-count independence; multi-worker buffer-free + edge cases (promoted from architect probe). REVIEW GATE GO x2 (qa-test-runner + mped-architect): theorem verified against acfg_to_petri/boundedness/deadlock/net_soundness; no false-ProvenSound hole; P3 fixes applied (purged a stale phantom-test fingerprint, tightened boundedness.rs degenerate-order docstring to say nonzero BUFFER initial markings, promoted multi-worker/edge tests). just clippy/test/test-release GREEN; e2e baseline HELD 483/420/0/63/0. RESIDUAL (honest): distributed-with-transfers still uses the expanded linear-in-firings gate; lifting that needs a steady-state buffer-occupancy argument under the greedy order, deferred (would risk unsoundness if imprecise).
<!-- SECTION:NOTES:END -->
