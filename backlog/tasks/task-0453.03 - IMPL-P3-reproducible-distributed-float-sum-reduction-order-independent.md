---
id: TASK-0453.03
title: 'IMPL P3: reproducible distributed float-sum reduction (order-independent)'
status: In Progress
assignee: []
created_date: '2026-06-06 22:54'
updated_date: '2026-06-07 03:21'
labels:
  - rigour
  - compiler
  - float
dependencies: []
parent_task_id: TASK-0453
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Addresses L4 (ch10:161-177) + ch11:36-64 float determinism + existing TASK-0343.06. Add an order-independent reproducible float reduction (wide fixed-point superaccumulator or pre-rounding) so a distributed float SUM carries byte-identity. CRITICAL per ch11:53-60: the frozen REFERENCE oracle must adopt the same scheme (not purely additive - reduction lowering + kernel contract + reference move in lockstep). Make it opt-in. Residual: only the reproducible combine is bit-identical, not the naive IEEE left-to-right sum.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A distributed float-sum example is byte-identical across tier-1 backends
- [x] #2 Reference oracle uses the same reproducible scheme; differential rig agrees with oracle
- [x] #3 Opt-in; the rejection path for non-reproducible float-sum still exists for non-opt-in
- [x] #4 Residual (reproducible != naive IEEE sum) documented
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
STEP-0 DUPLICATE-CHECK (cycle-4): TASK-0453.03 and TASK-0343.06 OVERLAP and NEITHER is implemented (both To Do). TASK-0060 is Done but doc-only (integer-only policy; zero FP examples). Reconciliation: implement ONCE here (.03), satisfying both; will close 0343.06 as subsumed. STEP-0b PREMISE VERIFIED: float-sum IS rejected today at backend-common/src/multi_worker_walker/wait.rs:688 combine_form_for_scalar (float Sum -> EmitError::ContractGap citing non-associativity + PRD 10.1, pointing at TASK-0343.06). Float types f32/f64 ARE first-class (algo/tokens.rs, ast.rs ScalarType). KEY ARCH FINDING: the accumulate fan-in already emits combine statements in HOST EVENT-LIST ORDER, which TASK-0389 sorts by (dst,src,producer_rank,...) = worker-id-sorted, IDENTICAL across all 7 backends; per-worker partials are also backend-identical (same partition, same kernel loop). So a worker-id-sorted fold (TASK-0343.06 option a) is ALREADY the emission order -> admitting float-sum gives cross-backend byte-identity for free. CONSTRAINT: reference.bin is PER-EXAMPLE (e2e/src/main.rs:1778 ex_dir.join), shared across schedules; for float-sum naive(whole-array)!=distributed(worker-id fold) so they cannot share one reference.bin -> the fsum example must be DISTRIBUTED-ONLY (single-schedule examples are allowed: 10-wavefront/23/24 etc.). DESIGN: new opt-in CombineOp::Fsum (combine=fsum), float-only; combine=sum on float STAYS rejected (AC#3). New example 28-bin-fsum mirroring 27-bin-fmin (proven float fan-in path) but sum-combine, distributed-only; partition-aware LOCKSTEP oracle replicates per-worker ascending-i fold + worker-id-sorted host fold. Residual: fsum = fixed-fold reproducible (cross-backend, per-schedule), NOT naive IEEE sum, NOT schedule-invariant, sum-only, f32/f64.
<!-- SECTION:NOTES:END -->
