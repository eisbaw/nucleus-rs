---
id: TASK-0044.04
title: >-
  M6 sub-task: Example 8 (histogram) — algorithm + naive + distributed schedules
  + kernels + reference
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 09:25'
updated_date: '2026-05-26 23:42'
labels:
  - M6
  - examples
dependencies: []
parent_task_id: TASK-0044
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land example 8 — histogram — per PRD §9 row 8: 'Reduction with shared output array'. Distinct from example 3 (reduction-to-scalar) by reducing to an N-bin array via index-dependent accumulation.

## What this stresses (per PRD §9)

- Read-side: streaming over an input array.
- Write-side: SHARED OUTPUT ARRAY where each input value indexes into a different bin (non-trivial fan-in pattern).
- Schedules: naive (single worker) + distributed (partition the input + per-worker partial-histogram + final reduce). The DISTRIBUTED schedule is the load-bearing test of cross-worker partial-result combination on an ARRAY output.

## Closest template

`nucleus/examples/03-reduction/` — the existing reduction example. Reuse:
- Algorithm sublanguage pattern: declare `data input : T[N]`, `data output : T[BINS]`, a kernel that takes a slice, writes a bin index.
- Schedule pattern: `distributed` schedules partition the input dimension across workers; per-worker partial output is combined.

The diff is the OUTPUT SHAPE: 03-reduction has scalar output (one f32), 08-histogram has array output (BINS-element u32 array). Partial-histogram combination is element-wise add across worker-local arrays.

## Deliverables

- `nuc-nucleus/examples/08-histogram/prog.algo.nuc` — algorithm: input array of values, BINS-element output histogram, per-element kernel that computes bin index + atomically-equivalent increment.
- `nuc-nucleus/examples/08-histogram/schedules/naive.sched.nuc` — one worker, sequential.
- `nuc-nucleus/examples/08-histogram/schedules/distributed.sched.nuc` — N workers partition input, partial histograms, combine.
- `nuc-nucleus/examples/08-histogram/kernels.rs` — pure Rust kernel implementations.
- `nuc-nucleus/examples/08-histogram/reference/` — independent Rust reference impl.
- `nuc-nucleus/examples/08-histogram/input.bin` — canned deterministic input (u8 or u16 values; document the distribution).
- `nuc-nucleus/examples/08-histogram/reference.bin` — expected output bytes.
- `nuc-nucleus/examples/08-histogram/README.md` — what stresses, which schedules required, why.

## Acceptance criteria

1. `prog.algo.nuc` parses + lowers cleanly on pthreads-sync (smoke test against the existing M3 backend).
2. `naive.sched.nuc` schedule lowers + emits + runs bit-identical against `reference.bin` on pthreads-sync AND mp-tcp-bufsync.
3. `distributed.sched.nuc` schedule lowers + emits + runs bit-identical against reference.bin on at least one tier-1 backend that supports partition-then-reduce (pthreads-sync via TASK-0258 partition_rows, or pthreads-async via the M5 distributed machinery).
4. `kernels.rs` is pure Rust, deterministic, no floating-point non-associativity surprises (use integer bins; document any f32 use).
5. Reference impl in `reference/` is INDEPENDENT — not derived from kernels.rs. Both produce identical reference.bin (the differential oracle).
6. README documents: required schedules, which backends support each, what algorithmic pattern this stresses, honest limitations.
7. e2e-matrix.toml has entries for each (schedule, backend) cell with the right [[required]] / [[skip]] markers; reasons reference the right TASK IDs (e.g. TASK-0042 capability mismatch for async-only backends if applicable).

## Honest scope

- Histogram has a SUBTLE DETERMINISM HAZARD: if the bin-increment kernel sees a race (multiple workers updating the same bin), the output is non-deterministic. The whole point of the SCHEDULE is to AVOID this — per-worker partial histograms are combined by a single reducer. Make sure neither the algorithm nor any schedule expresses an unguarded shared-bin write.
- BINS should be small enough that partial-histogram-per-worker is cheap (e.g. 256 bins for u8 input). Document the BINS / N tradeoff.
- The distributed schedule's partial-combine is the cross-worker fan-in pattern; this exercises a different combinator shape than 03-reduction (per-bin sum across workers, not scalar accumulator chain).

## Cross-reference

- Template: `nuc-nucleus/examples/03-reduction/` (the scalar-reduction sibling).
- Related: `nuc-nucleus/examples/04-prefix-sum/` (the other multi-pass example).
- Cross-backend differential infrastructure: existing `nuc-nucleus/e2e-matrix.toml` patterns.
- PRD: §9 row 8.
- Memory project-cross-backend-differential — the bit-identical infrastructure.

## Forward-carried context (orchestrator briefing for the implementer)

- Do NOT introduce a new partition variant just for histogram. partition=workers (TASK-0258 partition_rows / partition_workers) is the canonical input-partition mechanism; reuse it.
- The reduce phase combines partial-histograms element-wise. If this needs a new ACFG combinator shape, file as a separate sub-task rather than expand example 8's scope.
- 08-histogram joins the e2e matrix; check-narrative-doc-lie applies to any [[skip]] reason text added (memory feedback-comment-doc-lie-recurring + just check-narrative-doc-lie).
- The example's kernels.rs + reference/ must both compile via the existing pthreads-sync renderer + e2e harness; do not add new harness machinery.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 186 — language-sanity slice landed; AC#3 BLOCKED on filed prerequisite TASK-0343

Cycle 186 (orchestrator-led, in-thread per memory feedback-spawned-agents-refuse-code-edits): landed AC#1, AC#2, AC#4-#7. AC#3 (distributed bit-identical on at least one tier-1 backend) is BLOCKED on a substantive new compiler-level gap surfaced empirically by the distributed.sched.nuc probe — TASK-0343 filed cycle 186.

Files landed (mechanical-template-based from 03-reduction):
- nuc-nucleus/examples/08-histogram/prog.algo.nuc — algorithm with rectangular masked-accumulator pattern; data-dependent indexing pushed to kernel level per PRD 6.2.2 / 6.2.4.
- nuc-nucleus/examples/08-histogram/kernels.rs — pure bin_inc + effectful load_input / save_output; same Vec aggregate-typed I/O caveat as 03-reduction (TASK-0103 gap).
- nuc-nucleus/examples/08-histogram/reference/ — independent reference impl using direct-index hist[v] += 1 (structurally different from kernels.rs masked-accumulate; differential oracle).
- nuc-nucleus/examples/08-histogram/schedules/naive.sched.nuc + distributed.sched.nuc — naive on host only; distributed with 4 compute workers + partition workers.
- input.bin (1024 bytes, N=256 i32 LE) — non-uniform fixture, 5 distinct bin counts.
- reference.bin (64 bytes, BINS=16 i32 LE) — sum=256.
- README.md — required vs stretch schedules, I/O format, algorithm structure, contract-check limitation, cross-references including TASK-0343.
- nuc-nucleus/e2e-matrix.toml — 08-histogram added to runnable_examples + 4 required naive cells (one per tier-1 backend) + 4 skip distributed cells (referencing TASK-0343).

Empirical verification (orchestrator-self-run, inside nix develop, before commit):
- AC#1 (parses + lowers cleanly on pthreads-sync): VERIFIED via direct nucleus invocation. Contract-pass diagnostics for aggregate-typed I/O matches the 03-reduction template (TASK-0012 known gap).
- AC#2 (naive bit-identical on pthreads-sync AND mp-tcp-bufsync): VERIFIED. Both backends + ALSO pthreads-async AND mp-tcp-event all BIT-IDENTICAL — exceeded AC#2 scope; promoted all 4 to required from this cycle.
- AC#3 (distributed bit-identical on at least one tier-1 backend): BLOCKED. All 4 backends lower + emit + run but produce wrong output (uniform 4-per-bin across all 16 bins, sum=64) instead of reference (5 distinct counts, sum=256). Root cause filed as TASK-0343: cross-worker array-output accumulator combine emits 4 sequential overwrites (last-write-wins) instead of element-wise sum. The OVERLAPPING-WRITE accumulator pattern (every worker writes histogram across all bins) is a substantively new ACFG/codegen shape vs the DISJOINT-WRITE accumulator of 03-reduction (each worker writes a unique partials slot). NOT a workaround — the gap is filed precisely + the cell is honestly skip in e2e-matrix.
- AC#4 (kernels.rs pure, deterministic, no FP): VERIFIED. i32 throughout, wrapping_add documents overflow contract; integer equality check, no division/modulo.
- AC#5 (reference impl independent): VERIFIED. kernels.rs uses masked-increment per cell; reference uses direct-index per input element. Structurally different access patterns.
- AC#6 (README documents required schedules, backends, pattern, limitations): VERIFIED. README explicitly disclosure-anchors the AC#3 BLOCKED state + TASK-0343 reference.
- AC#7 (e2e-matrix entries with right required/skip markers): VERIFIED. 4 required naive + 4 skip distributed; skip reasons cite TASK-0343 with precise diagnosis.

Verification gate (full):
- just build / clippy: clean.
- just test (dev):       979/0/3.
- just test-release:     978/0/3.
- just e2e:              112/102/0/10/0 -> 120/106/0/14/0 (+8 total cells, +4 pass, +4 skip; baseline cycle 185 -> cycle 186).
- just check-textual-replace-on-codegen / check-include-str-coverage / check-mega-files / check-narrative-doc-lie: all clean.
- 08-histogram cells specifically (via nucleus-e2e --example 08-histogram): 4 PASS + 4 SKIPPED, total 8/4/0/4/0.

Lessons forward-carried to TASK-0343 (the BLOCKED AC#3 implementer):
- The empirical-mismatch shape is uniform N-over-NUM_WORKERS-over-BINS for a partition that happens to be uniform-modulo-BINS in the last worker; the actual fixture is non-uniform-globally but the post-partition distribution in partition 3 (i in 192..256, (i+5) mod 16) gives the [4,4,...,4] shape from the last-worker-wins effect.
- The gap is uniform across all 4 tier-1 backends — pure substrate-level issue, no backend-specific divergence. Fix must land in the shared transfer_inject / acfg / sync_inject layer, not in backend-specific code.
- The DISJOINT-WRITE vs OVERLAPPING-WRITE accumulator distinction is the key compiler-level invariant: LHS index = partition variable means disjoint; LHS index independent of partition variable means overlapping. The compiler must detect this distinction (it is syntactic — read off the ACFG statement).

Honest-failure framing per phase3-backlog-ralph discipline:
- AC#3 is NOT closed. The task stays In Progress with AC#3 unchecked. AC-text NOT rewritten to match what shipped (feedback-ac-rewrite-on-done-task). The honesty disclosure lives in the prog.algo.nuc + distributed.sched.nuc docstrings + README + TASK-0343 description.
- The cycle CLOSED the language-sanity slice (the minimum AC#1 + AC#2 shape) and FILED the precise prerequisite for AC#3. That is the phase3-backlog-ralph 'blocked > fake-complete' shape.

## Cycle 186 review gate — Anthropic rate-limit truncation; orchestrator self-review (independence-of-result lost)

Cycle 186's parallel review gate (qa-test-runner + mped-architect read-only subagents) hit an Anthropic rate-limit truncation ("You've hit your limit · resets 9pm") before producing any review output. Per memory feedback-api-overload-during-review-gate, the orchestrator self-performed the review inline. Gate content preserved (numbers re-verified, architecture audited, diagnosis cross-checked); independence-of-result lost (the same actor wrote the commit and reviewed it).

Non-flake re-confirmation of e2e: second independent sample reproduced 120/106/0/14/0 bit-identical totals. No deviation.

Self-review summary (no fold-back commit needed — all P3-class, recorded here for the durable record):
- P3.1 (algorithm shape): prog.algo.nuc rectangular masked-accumulator nest matches 04-prefix-sum exclusive_add template. PRD 6.2.1 + 6.2.4 satisfied.
- P3.2 (AC#3 diagnosis): empirically verified pre-commit — emitted main.rs lines 127-130 emit 4 sequential `histogram = slot_N.wait()` overwrites. TASK-0343 anchored at correct compiler layer.
- P3.3 (reference independence): kernels.rs uses masked-increment per (i,b) cell; reference uses direct-index per i. Different control flow producing same result — bona fide differential oracle (not derivable one from the other without rewriting).
- P3.4 (e2e-matrix wiring): 4 required + 4 skip cells, correct M6 milestone, skip reasons cite TASK-0343 + pass check-narrative-doc-lie.
- P3.5 (AC-rewrite avoidance): AC#3 text unchanged; disclosure anchored at 6 sites (prog.algo.nuc + distributed.sched.nuc + README + TASK-0343 + commit body + this notes block).
- P3.6 (forward-carry quality): TASK-0343 description contains the diagnosis + substrate-uniformity + DISJOINT-vs-OVERLAPPING distinction + regression-prevention test spec. Future TASK-0343 implementer can act cold.
- P3.7 (sibling-defect sweep): runnable_examples count + e2e cell delta + required/skip split — all consistent (+8 cells = 4 required + 4 skip).

Review-gate-independence-lost is recorded as a cycle-186 caveat that the next session SHOULD re-establish: when the gate becomes available again, re-run qa-test-runner + mped-architect read-only on commit 1da7c69 to add the independent-result signal. The cycle's WORK is sound by self-review; the cycle's PROCESS is incomplete on the independence axis.

Stop signal recognised: phase3-backlog-ralph stop condition "Budget/context is thin enough that quality would degrade" fires when an Anthropic rate-limit truncates the review gate mid-cycle. Fresh session at >=9pm Europe/Copenhagen for the next keystone (TASK-0343 or another pick).

## Cycle 186c — process-backstop review gate GO; P2 fold-back applied

Cycle 186c (orchestrator-led, in-thread): re-ran the parallel read-only review gate that cycle 186b had to skip (rate-limit). Both arms returned GO independently:

- qa-test-runner: GO. Re-reproduced all gate numbers bit-identically: just test 979/0/3 dev; just test-release 978/0/3; just e2e 120/106/0/14/0 across TWO independent samples (non-flake). 08-histogram cell matrix 4 PASS + 4 SKIPPED reproduced. AC#3 BLOCKED mismatch shape `[4,4,4,4,4,4,4,4,4,4,4,4,4,4,4,4]` empirically reproduced; emitted main.rs lines 127-130 cited verbatim ("histogram = slot_0.wait()" / "histogram = slot_1.wait()" / "histogram = slot_2.wait()" / "histogram = slot_3.wait()" — 4 sequential whole-array assignments, no element-wise sum). No P1/P2/P3 findings.

- mped-architect: GO with 2 P2 + 2 P3 findings.

Cycle 186c fold-back (P2.1 + P2.2 + P3.1 applied inline; P3.2 deferred):

- P2.1 (stale "pthreads-sync + mp-tcp-bufsync" narrative drift) — rewrote 2 source-tree docstring sites that lagged the actual landed state (4 tier-1 backends, not 2):
  - prog.algo.nuc:41-48 "What this example does NOT demonstrate ... Distributed schedule" block: rewrote to reflect cycle-186 actual outcome (all 4 backends bit-identical naive; distributed lowers+emits+runs on all 4 but produces wrong output last-write-wins; TASK-0343 filed; cell skip across all 4 backends).
  - schedules/naive.sched.nuc:3-7 module docstring: rewrote "smoke test on pthreads-sync AND mp-tcp-bufsync" to "BIT-IDENTICAL across ALL FOUR tier-1 backends ... exceeded AC#2's pthreads-sync + mp-tcp-bufsync minimum; all 4 promoted to required from cycle 186".

- P2.2 (predictive narrative "if it lowers cleanly cycle 186 it joins the matrix") — same prog.algo.nuc block as P2.1; predictive language replaced with past-tense empirical-outcome wording.

- P3.1 (TASK-0343 citation missing from source-tree files; only README cites the ticket) — rewrote schedules/distributed.sched.nuc:26-38 "Honest open question" block. New text:
  - Status at cycle 186 close: empirically-known facts (lowers / emits / runs; output WRONG on all 4 backends).
  - Symptom-pin: cycle-186 mismatch shape `[4,4,...,4]` (sum=64) vs reference (sum=256).
  - Diagnosis: 4 sequential `histogram = slot_N.wait()` overwrites; OVERLAPPING-WRITE vs DISJOINT-WRITE accumulator distinction with explicit reference to 03-reduction's partials[w] DISJOINT-WRITE counter-example.
  - Filed as TASK-0343 with the compiler-layer diagnosis + substrate-level-uniformity observation + regression-prevention test spec.

- P3.2 (kernels.rs overflow-rationale tone overstated for committed fixture max-count=25 vs i32::MAX) — DEFERRED. Tone-only nit per architect; not load-bearing for cycle close; not worth the fold-back cycle on its own. If a future kernel-edit cycle touches kernels.rs, sweep this line.

Verification gate (cycle 186c is doc-only):
- just build: clean (cargo cache-warm rebuild ~0.05s).
- just check-narrative-doc-lie: clean.
- just test / test-release / e2e: NOT re-run for doc-only edit (gate numbers 979/0/3 + 978/0/3 + 120/106/0/14/0 stand from cycle 186 + 186b + cycle-186c QA re-run).

AC delta for TASK-0044.04 post-cycle-186c:
- AC#1, AC#2, AC#4, AC#5, AC#6, AC#7: DONE (unchanged from cycle 186).
- AC#3: BLOCKED on TASK-0343 (unchanged from cycle 186; disclosure quality IMPROVED by cycle-186c fold-back).

Cycle 186c process-completion: the parallel read-only review gate has now been INDEPENDENTLY run on the cycle-186 commits (1da7c69 + 8c629b8) — independence-of-result restored, gate-content already verified by cycle 186b orchestrator self-review. The cycle-186 work-product is sound; cycle 186c hardens the 6-site disclosure consistency that cycle 186b's 5th and 6th anchor sites (commit body + tracker notes) had pinned correctly but cycles 186's source-tree docstrings (sites 1 and 2) had partially missed.

NEW lesson — when an orchestrator-self-reviewed cycle is later re-reviewed independently, expect the self-review to have under-counted source-tree docstring drift: the orchestrator who wrote the docstrings during the cycle has a recency-bias blind spot for the gap between what they wrote and what the landed reality became (cycle-186 prog.algo.nuc / naive.sched.nuc both pre-dated the empirical-promotion across 4 backends). A fresh-eyes architect catches this immediately. Generalises feedback-implementer-disclosure-mechanism-wrong to the orchestrator-self-review case: AC#2-vs-shipped framing drift is a recurring shape.

## Cycle 189 close — AC#3 unblocked via TASK-0343 (cycle 189 landed)

TASK-0343 cycle 189 lifted the overlapping-write accumulator fan-in codegen-layer gap that was blocking AC#3 of this task. All 4 tier-1 backends now emit element-wise wrapping_add accumulate on the host-side cross-worker fan-in instead of the pre-cycle-189 last-write-wins overwrite sequence.

All 4 08-histogram/distributed cells PROMOTED to [[required]] in nuc-nucleus/e2e-matrix.toml (cycle 189). All 4 BIT-IDENTICAL vs reference.bin.

AC#3 now satisfied:
- AC#3 minimum was 'at least one tier-1 backend'; cycle 189 covers all 4 tier-1 backends bit-identical (exceeds minimum).
- Cross-backend differential gate enforces ongoing bit-identity.

This task closes as Done in cycle 189.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
All 7 ACs satisfied across cycles 186 (AC#1+#2+#4-#7 landed) and 189 (AC#3 unblocked via TASK-0343).

**Cycle 186** (orchestrator-led): landed the language-sanity slice — algorithm + naive schedule + kernels + reference + e2e-matrix entries. AC#1 (parses+lowers), AC#2 (naive bit-identical on at least pthreads-sync + mp-tcp-bufsync — actually all 4 tier-1 backends), AC#4 (pure deterministic kernels), AC#5 (independent reference impl), AC#6 (README), AC#7 (e2e-matrix entries with right markers). AC#3 BLOCKED on filed TASK-0343 codegen gap.

**Cycle 189** (orchestrator-led): TASK-0343 landed — overlapping-write accumulator fan-in lifted at the shared backend-common layer (collect_accumulate_waits + render_wait_assign(accumulate)). All 4 08-histogram/distributed cells PROMOTED [[skip]] → [[required]] bit-identical. AC#3 satisfied (exceeds minimum 'at least one tier-1 backend' — covers all 4).

e2e baseline: 112/101/0/11/0 (pre-189) → 120/110/0/10/0 (post-189; +9 pass, -1 skipped, 0 fail, 0 required-fail).
<!-- SECTION:FINAL_SUMMARY:END -->
