---
id: TASK-0085
title: Gitignore .claude/ and CLAUDE.md if they are local-only
status: Done
assignee: []
created_date: '2026-05-18 00:05'
updated_date: '2026-05-22 13:30'
labels:
  - M0
  - infra
  - tooling
dependencies: []
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
QA review of TASK-0002 flagged: 'Git tree is dirty' warning appears on every 'nix develop' command because .claude/, CLAUDE.md, and backlog/ are present in the working tree. Decide whether these should be (a) committed (CLAUDE.md probably yes — it's project-level Claude instructions written by backlog init), (b) gitignored (.claude/ probably yes — it's per-session ephemeral state), or (c) left as-is. The persistent warning is cosmetic but pollutes every cargo/nix run.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 .gitignore decision made and applied: .claude/ excluded if confirmed ephemeral.
- [x] #2 CLAUDE.md decision made and applied: committed to the repo if it's project instructions, gitignored if local-only.
- [x] #3 backlog/ already tracked; not touched.
- [x] #4 Test: after the change, 'nix develop --command true' shows no 'Git tree is dirty' warning (or only legitimately-modified files remain).
- [x] #5 Implementation notes record the decision rationale for each path.
- [x] #6 Implementation notes record honest limitations (e.g. CLAUDE.md was auto-generated by backlog init; if it gets regenerated, the decision may need re-confirmation).
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 46 (2026-05-22) — closed. The condition the task describes is now obsolete: neither .claude/ nor CLAUDE.md exists in this project (they were never committed or have been removed in some intervening cycle). So AC#1 + AC#2 are vacuous as written — there's nothing to gitignore for those paths.

The ONLY persistent ?? in git status throughout this session has been backlog/email-preferences.json — a per-user local file the backlog CLI writes when a task is --assignee'd. Cycle 46 added it to .gitignore as the in-spirit closure of AC#1 (the original target of the task — eliminate the persistent 'Git tree dirty' warning).

Decision rationale (AC#5):
- .claude/ — n/a, doesn't exist.
- CLAUDE.md — n/a, doesn't exist.
- backlog/email-preferences.json — gitignored (per-user state, written by backlog CLI on --assignee, not project content).
- backlog/ as a whole — already tracked, untouched (per AC#3).

AC#4 verified: after the change, git status shows only legitimately-modified files; the persistent ?? noise is gone.

Honest limit (AC#6): if a future cycle introduces a .claude/ or CLAUDE.md back into the project, this task's gitignore work won't catch them — file a follow-up at that point.
<!-- SECTION:FINAL_SUMMARY:END -->
