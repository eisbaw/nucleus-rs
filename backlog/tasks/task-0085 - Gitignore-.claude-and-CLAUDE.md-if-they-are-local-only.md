---
id: TASK-0085
title: Gitignore .claude/ and CLAUDE.md if they are local-only
status: To Do
assignee: []
created_date: '2026-05-18 00:05'
labels:
  - M0
  - infra
  - tooling
dependencies: []
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
QA review of TASK-0002 flagged: 'Git tree is dirty' warning appears on every 'nix develop' command because .claude/, CLAUDE.md, and backlog/ are present in the working tree. Decide whether these should be (a) committed (CLAUDE.md probably yes — it's project-level Claude instructions written by backlog init), (b) gitignored (.claude/ probably yes — it's per-session ephemeral state), or (c) left as-is. The persistent warning is cosmetic but pollutes every cargo/nix run.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 .gitignore decision made and applied: .claude/ excluded if confirmed ephemeral.
- [ ] #2 CLAUDE.md decision made and applied: committed to the repo if it's project instructions, gitignored if local-only.
- [ ] #3 backlog/ already tracked; not touched.
- [ ] #4 Test: after the change, 'nix develop --command true' shows no 'Git tree is dirty' warning (or only legitimately-modified files remain).
- [ ] #5 Implementation notes record the decision rationale for each path.
- [ ] #6 Implementation notes record honest limitations (e.g. CLAUDE.md was auto-generated by backlog init; if it gets regenerated, the decision may need re-confirmation).
<!-- AC:END -->
