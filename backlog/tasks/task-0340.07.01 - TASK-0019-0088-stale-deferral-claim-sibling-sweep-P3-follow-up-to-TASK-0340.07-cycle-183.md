---
id: TASK-0340.07.01
title: >-
  TASK-0019/0088 stale-deferral-claim sibling sweep (P3 follow-up to
  TASK-0340.07 cycle 183)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-26 15:29'
updated_date: '2026-05-28 04:50'
labels:
  - tech-debt
  - hygiene
  - follow-up
dependencies: []
parent_task_id: TASK-0340.07
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Cycle 183 (slice 8 of TASK-0340) rewrote 4 stale 'TASK-0019/0088 deferred' doc-claims in nucleus-compiler/src/link/mod.rs as part of the link.rs split. Silent-sibling sweep surfaced ~5 additional source-tree sites carrying the same defect class (TASK-0019/0088 are both Done; 'deferred to TASK-NNNN' wording is stale):

- nucleus/nucleus-compiler/src/capabilities.rs L2, L273 — describes capabilities pass IS TASK-0019 (structural anchor, MAY be fine — re-read).
- nucleus/nucleus-compiler/src/sched/ir.rs L52, L90 — 'Filed as follow-up (TASK-0019 in M1 per task spec)' + 'concern (TASK-0019), not a lowering concern'. Both are 'deferred' framings on a now-Done task.
- nucleus/nucleus-compiler/src/acfg/types.rs L290, L336 — 'codegen-time capability check (TASK-0019+) resolves it against' / 'to TASK-0019+ at codegen time when the backend is in hand' (the 'TASK-0019+' form is stale; TASK-0019 is Done).
- nucleus/nucleus-compiler/src/passes/transfer_inject.rs L378 — 'codegen-time TASK-0019+ rejects' (stale 'TASK-0019+' form).
- nucleus/nucleus-compiler/tests/transfer_inject.rs L26 — 'deferred to codegen-time (TASK-0019+)' (stale form).

NOT folded into cycle-183 commit because each site needs per-site re-reading to distinguish 'structural anchor naming the task that birthed the pass' (KEEP) from 'stale deferral claim presenting it as still-deferred' (REWRITE). 5+ sites > single-cycle fold-in scope per slice-8 brief 'If the fix is structurally larger than the split itself, file a follow-up task and leave a precise comment at the site' rule.

Recommended fix shape (mirrors cycle-183 R1/R5/S1/S2 rewrites in link.rs/acfg_to_petri.rs/event.rs):
- For 'deferred to TASK-NNNN' framings where TASK-NNNN is Done: rewrite to 'consumed by [Done pass name] (TASK-NNNN, Done)' or similar present-tense framing.
- Keep 'TASK-NNNN spec named this surface' / 'TASK-NNNN birthed this pass' anchors AS-IS — they are historical not deferred.

This is the same defect class as cycle-178b (mp-tcp-bufsync 'does NOT yet consume' claim across 3 sites) and cycle-182b (render_call_arg silent-sibling sweep 12 sites) — pattern: a doc-claim was TRUE at writing time, the supporting machinery landed, and the doc-claim went silently stale. Per memory feedback-comment-doc-lie-recurring + feedback-silent-sibling-defect.

Acceptance Criteria:
- Each of the ~6 sites listed re-read in context; rewrite if stale-deferral, keep if structural-anchor.
- grep -rn 'TASK-0019+' nucleus/ --include='*.rs' returns 0 hits (the '+' suffix form is unambiguously stale once TASK-0019 itself is Done).
- grep -rn 'TASK-0088' nucleus/ --include='*.rs' returns at most the link/mod.rs anchor (currently the only src-tree site after cycle 183).
- No new TASK-NNNN/cycle-NNN citations introduced (lenient AC#6 reading).
- Gate green: build / clippy / test / test-release / e2e baseline preserved (currently 112/102/0/10/0).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 229 (orchestrator-direct). Per-site re-read classification (TASK-0019 + TASK-0088 confirmed Done):
Verified the actual mechanism first: check_schedule_compat (TASK-0019, Done) runs in the BUILD DRIVER (nucleus/driver/src/main.rs:629), AFTER backend selection, BEFORE codegen — NOT 'at codegen time', and TASK-0019 alone is the gate (the '+' is obsolete). The accurate kernel 'not checked at this backend-agnostic pass; checked once the backend is in hand' stays true.
REWRITE (stale future/deferral framing on a Done task): acfg/types.rs:290 + :336, passes/transfer_inject.rs:378 (//! block), tests/transfer_inject.rs:26 — all 4 'TASK-0019+' plus-form; sched/ir.rs:52 'Filed as follow-up (TASK-0019...)'; sched/ir.rs:90 light clarification to anchor.
KEEP (structural/historical anchors, present-tense): capabilities.rs:2 (module birth) + :273 ('variant set named in TASK-0019'); tests/capabilities.rs:2 + :149 ('TASK-0019 notes' MSRV rationale); link/mod.rs:67 ('TASK-0088 closed as') + :73 ('crate::capabilities (TASK-0019, Done)') — both already cycle-183 present-tense.
NOTE: the task md cites e2e baseline 112/102/0/10/0 — itself stale; actual current baseline 280/246/0/34/0 (doc-only change → e2e invariant).

## Cycle 229 review gate (parallel read-only) — GO after P2 fold-back
qa-test-runner (independent): diff is DOC-ONLY (only ///,//! lines in 4 .rs + tracker md); just clippy clean (NO doc_lazy_continuation despite new multi-line //! continuations — recurring lint did not fire); build OK; just test 0 fail (doctests 0/0, no stray fenced block); just test-release 0 fail; just e2e 280/246/0/34/0; grep TASK-0019+ = 0 hits; grep TASK-0088 = exactly 1 (link/mod.rs). GO.
mped-architect (read-only): verified the core mechanism — check_schedule_compat (capabilities.rs:361, TASK-0019) invoked at driver/src/main.rs:629 AFTER backend selection, BEFORE codegen dispatch; three-way reject (async/buffer>1/notify=event) accurate; gate validates only EXPLICIT notify. 5 of 6 rewrites clean; KEPT anchors correct; no missed sites; ACs satisfied.
  P2 (precise-but-wrong, the exact trap this task class hunts): my acfg/types.rs:290 rewrite claimed Default is 'resolved by the backend at codegen' — but NO backend reads the acfg TransferPolicy.notify field. Empirically confirmed by orchestrator grep: zero consumers in backends/*/src; field is written (transfer_inject.rs:3613 from schedule, host_data_relay_inject.rs:1190) + asserted in tests + defaulted (types.rs:357), never read at codegen. Two distinct NotifyMode enums exist (capabilities.rs:88 = gate's Event/Poll/Barrier/Blocking/Irq; acfg/types.rs:301 = policy's Default/Event/Poll).
  FOLD-BACK: reworded types.rs:288 to state the CONTRACT honestly — Default carries no constraint, always satisfiable, choice nominally the backend's BUT no tier-1 backend currently specialises (carried-but-unread at codegen today); gate validates only explicit notify=. Re-ran build+clippy clean. Doc-only fix; e2e/test unaffected.
NOTE: AC cites stale e2e baseline 112/102/0/10/0; actual current baseline 280/246/0/34/0 (doc-only -> e2e invariant).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE cycle 229. Stale-deferral-claim sweep: TASK-0019 (capability check) + TASK-0088 both Done, so 6 doc-comments describing capability checking as 'deferred to codegen-time (TASK-0019+)' were stale. Per-site re-read classification (verified the real mechanism first: check_schedule_compat, TASK-0019 Done, runs in the build driver after backend selection / before codegen). Rewrote 6 stale sites (acfg/types.rs x2, passes/transfer_inject.rs, tests/transfer_inject.rs, sched/ir.rs x2) to present-tense references to the driver capability gate; KEPT 6 structural/historical anchors (capabilities.rs x2, tests/capabilities.rs x2, link/mod.rs x2). ACs met: grep TASK-0019+ = 0 hits; grep TASK-0088 = 1 (link anchor). Parallel review gate caught a P2 (my acfg/types.rs:290 rewrite invented an unimplemented 'backend resolves Default at codegen' step — no backend reads the field) — folded back in-cycle to honest contract framing. Gate green: build/clippy (no doc_lazy_continuation)/test/test-release + e2e 280/246/0/34/0 unchanged (doc-only).
<!-- SECTION:FINAL_SUMMARY:END -->
