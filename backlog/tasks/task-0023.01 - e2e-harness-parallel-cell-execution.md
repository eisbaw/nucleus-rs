---
id: TASK-0023.01
title: 'e2e harness: parallel cell execution'
status: Done
assignee:
  - mped-architect-impl
created_date: '2026-05-18 03:19'
updated_date: '2026-05-22 19:40'
labels:
  - M3
  - tooling
dependencies: []
parent_task_id: TASK-0023
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0023 ships sequential cell execution. cargo build saturates cores for one cell at a time; running cells in parallel mostly contends for the build cache. As the matrix grows past pthreads-sync (M3+), evaluate spawning N=ncores worker tasks with separate target dirs per cell.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Parallel execution opt-in via flag (e.g. --jobs N).
- [x] #2 Per-cell scratch + target dirs do not collide.
- [x] #3 Exit-status semantics unchanged: required-cell FAIL is non-zero.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Implementation plan (cycle-51):

1. Add `--jobs N` / `-j N` flag to `parse_args`; default 1 (preserves byte-for-byte sequential behaviour, gate untouched when flag absent).
2. Validate N >= 1; cap at a sane upper bound (e.g. 64) to avoid pathological fork bombs.
3. Refactor the two cell-loops in `run_inner` (run-mode at ~L2510 and determinism-mode at ~L2383) to dispatch via a small helper `execute_cells_parallel(jobs, planned, run_fn)` that:
   - if jobs==1: same path as today (sequential loop, in-order progress lines).
   - if jobs>1: spawns `min(jobs, planned.len())` worker threads via std::thread + std::sync::mpsc + an `Arc<Mutex<VecDeque<idx>>>` work-queue. Workers pull next index, run the cell, push (idx, result) over the mpsc.
4. Buffer per-cell progress lines so completion-ordered emission stays parseable; format `[i/N]` lines as today but emit them at completion time (so output is not interleaved mid-cell).
5. Collect results in original `planned` order (re-sort by idx at end) — the summary tables, gate signals (`NUC_NONDET_PERTURBED_CELLS=`, `NUC_XBACKEND_CORRUPTED_APPLIED/_DETECTED=`), and exit-code logic are all order-insensitive (they iterate `results`), but stable order keeps the summary table deterministic.
6. Update `print_help` to document `--jobs N` / `-j N`.
7. Add a unit test for the arg parser (`--jobs 4`, `-j 4`, bad value → error).

Why std::thread + mpsc and not rayon: harness has zero deps on rayon today; cell count is small (<=88); contention is on subprocess spawn, not on Rust-level work; std lib is sufficient and keeps the dependency footprint minimal (MPED principle: smallest dep that fits).

Send/Sync concerns:
- Paths is composed of String + PathBuf, trivially Send+Sync — pass as Arc<Paths>.
- PlannedCell / CellResult / DetCellResult contain only owned data; trivially Send.
- run_cell / check_cell_determinism each spawn their own cargo subprocesses; the per-cell scratch is unique by construction (TASK-0182's run-id segment). No shared mutable state.
- The two negative-gate env reads (NUC_NONDET_TEST, NUC_XBACKEND_NEGATIVE) are per-cell std::env::var reads against a constant env — re-entrant safe.
- Cargo's per-workspace lock on the nucleus driver target dir will SERIALISE the brief `cargo run --bin nucleus` lock-acquire phase but not the work; per-cell `cargo build` runs in disjoint scratch trees with their own target dirs.

Risks / honest limits:
- Memory: each concurrent `cargo build --release` of an emitted project costs ~200-500MB peak. With --jobs 4 on a 14-core 30GB host: comfortable. Higher --jobs values risk OOM on smaller hosts; help text will note this.
- Output ordering: per-cell progress lines emit in completion order, not planned order. Summary table re-sorts.
- The two negative recipes (`determinism-check-negative`, `xbackend-check-negative`) currently invoke the harness sequentially. Adding `--jobs N` is opt-in — those recipes keep their current sequential invocation, so their per-cell discipline is unchanged.

Verification:
- Pre-fix wall-clock baseline (sequential `just e2e`).
- Post-fix gates: `just e2e` (sequential — must stay 88/70/0/18), `--jobs 4` (same tally, faster), determinism-check-negative, xbackend-check-negative, just test, just clippy.

DO NOT mark Done. DO NOT commit.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0182 (Done, commit 5735b19): the shared-tree CWD race that blocked safe concurrent harness runs is FIXED via a process-wide run-id path segment (nucleus/target/e2e-matrix/<run-id>/<cell>, e2e-determinism/<run-id>/<cell>__<label>), computed once in Paths::discover. This is a PREREQUISITE for parallel-cell execution and is now in place: WITHIN a single harness process all cells already get unique stable paths under one run-id, so parallelising cells inside one process is safe w.r.t. scratch-dir collisions WITHOUT further per-cell isolation work. Deliberately chose run-id over an flock lock precisely so this task is not serialised. NOTE: TASK-0182 added gate-only NUC_E2E_FORCE_SHARED_RUN_ID (test seam) and a 24-way concurrency stress test (tests/concurrency_stress.rs) that can be extended to assert the in-process parallel executor stays infra-race-free. Also: the end-of-run finalize_run_scratch cleans the per-run tree on success — a parallel executor must keep the single end-of-run cleanup (do NOT clean per-cell, sibling cells share the run-id root).

## Cycle 51 attempt — partial (plan landed, implementation did not)

Stream idle-timeout at 29 tool uses / 9.6 min during cycle 51. The implementer wrote a detailed implementation plan into the tracker (above) but did not get to ship any source changes before the timeout. No source files modified — gate state unchanged from baseline 88/70/0/18.

The plan above is FORWARD-CARRIED for the next attempt. Specifically:
- The std::thread + mpsc + Arc<Mutex<VecDeque>> work-queue design is sound; smallest dep footprint that fits.
- The dispatch helper `execute_cells_parallel(jobs, planned, run_fn)` would refactor TWO cell-loops in run_inner (run-mode L2510 + determinism-mode L2383).
- Send+Sync analysis: Paths is Arc'able (String + PathBuf); PlannedCell + CellResult + DetCellResult are owned-data only; run_cell / check_cell_determinism are subprocess-spawn isolated by TASK-0182's run-id scratch path segment.
- Output ordering: buffer per-cell progress lines + emit at completion to avoid interleave, but re-sort results by original idx for summary-table determinism.

Cycle 51 burned ~10 min of subagent time + a bogus stray `=` file (cleaned). Task remains To Do for a future implementer to execute the plan. Estimated effort: ~3-5 KiB of harness changes + 1 unit test for the arg parser.

## Cycle 52 — Stages 1+2+3+4 LANDED (all 4 stages)

File: nucleus/e2e/src/main.rs

Changes:
- Imports (L55-67): + std::collections::VecDeque, + std::sync::{Arc, Mutex}, + std::thread.
- Args (L290-323): added `jobs: usize` field with custom Default::default impl preserving the existing field defaults; introduced `MAX_JOBS = 64` constant.
- parse_args (L353-389): added `--jobs N` / `-j N` arm with strict validation — usize parse, reject 0, cap at MAX_JOBS, "requires a value" error. All loud at parse time.
- print_help (L387-405): documents --jobs N / -j N + per-cell ~200-500MB peak warning + completion-order progress caveat.
- Paths (L460): `#[derive(Clone)]` added — required so workers can stash an Arc<Paths> snapshot.
- PlannedCell (L857): `#[derive(Clone)]` added — required so the work-queue can clone owned cells across worker threads.
- New helper `execute_cells_parallel<R, F>` (~L2358-2530): jobs=1 → strictly sequential equivalent of pre-flag for-loop (default `just e2e` path stays byte-identical). jobs>=2 → spawns `min(jobs, planned.len())` threads pulling indices from `Arc<Mutex<VecDeque<usize>>>`; sends `(idx, R, completion_line)` over mpsc; main thread prints completion lines in COMPLETION order; results re-sorted to PLANNED order before return. Worker panic surfaces via `resume_unwind` rather than silent drop. Sequential branch is deliberately a plain for-loop (no Mutex/mpsc on the default path) precisely so jobs=1 is provably equivalent to the pre-flag behaviour.
- run_inner (L2533+L2599): BOTH cell-loops (det-mode + run-mode) refactored to call `execute_cells_parallel` with closures that return `(R, completion_line)`. The two log banners now show `(jobs=N)`.
- Tests (L3074-3185): added 9 unit tests for the arg parser — default=1, --jobs and -j accepted, MAX_JOBS boundary, reject 0 / non-integer / negative / above-MAX / missing-value, composes with other flags.

Stage 1 fixup: pre-existing `let args = Args { ... };` literal at the gate-test site (line 3243) was missing the new `jobs` field → switched to `..Args::default()` pattern (other test sites already used spread). No semantic change.

Verification (all from `nix develop`):
- `just clippy` → clean (0 warnings, -D warnings).
- `just test` → all 30+ workspace test crates green; e2e crate runs 43 tests including the 9 new ones.
- `just e2e` (sequential default) → **88/70/0/18, 34.6s** (unchanged from baseline; required-fail=0).
- `cargo run --release --bin nucleus-e2e -- --jobs 4` → **88/70/0/18, 13.9s** (**~2.5x wall-clock speedup**, same tally).
- `just determinism-check-negative` → OK + `NUC_NONDET_PERTURBED_CELLS=70` (sequential default; falsifier bites).
- `just xbackend-check-negative` → OK + `NUC_XBACKEND_CORRUPTED_APPLIED=16` / `DETECTED=1` (sequential default; falsifier bites).
- `--check-determinism --jobs 4` under `NUC_NONDET_TEST=1` → `NUC_NONDET_PERTURBED_CELLS=70` (parallel falsifier bites IDENTICALLY).
- `--jobs 4` under `NUC_XBACKEND_NEGATIVE=1` → `APPLIED=16, DETECTED=1` (parallel falsifier bites IDENTICALLY).

Honest limits:
- The two negative recipes (`determinism-check-negative`, `xbackend-check-negative`) keep their existing sequential invocation in the justfile — opt-in parallelism only. Their per-cell discipline is unchanged.
- Live progress lines under `--jobs >=2` emit in completion order, not planned order — humans eyeballing live CI logs see cells finish out of order. The summary table is still planned-order and gate signals are order-insensitive, so this is purely cosmetic.
- Per-cell memory under `--jobs 4` peaks around 800MB-2GB total (each concurrent `cargo build --release`). MAX_JOBS=64 is a safety cap, NOT a recommendation — `--jobs 4` is the documented sweet spot for a 14-core / 30GB host.
- `cargo build` of an emitted project briefly holds its target-dir lock; with --jobs >=2 those locks are on disjoint target dirs (per-cell scratch is unique via TASK-0182 run-id) so no serialisation point inside the parallel critical path.

DO NOT mark Done. DO NOT commit. Ready for review-gate.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 52 (2026-05-22) — closed. All 3 ACs met.

Implementation: nucleus/e2e/src/main.rs only (~470 LoC delta). New CLI flag --jobs N / -j N (default 1, MAX_JOBS=64); execute_cells_parallel<R, F> helper using std::thread + Arc<Mutex<VecDeque<usize>>> + mpsc; both cell-loops in run_inner (run-mode + determinism-mode) route through it. 9 new arg-parser unit tests. No new deps.

AC closure:
- AC#1: parallel execution opt-in via --jobs N / -j N flag. CLOSED.
- AC#2: per-cell scratch + target dirs do not collide. CLOSED via TASK-0182's run-id+cell path segmentation (already in place).
- AC#3: exit-status semantics unchanged. CLOSED — sequential path is byte-identical (jobs<=1 branch is a plain for-loop, no thread spawn); parallel path collects results via sparse Vec<Option<R>> indexed by original cell idx so summary table + gate signals are planned-order; live progress lines emit in completion order (cosmetic-only, documented).

Performance: --jobs 4 wall-clock 13.5s vs sequential 34.6s (~2.6x speedup). User CPU 50s vs 38s (better core utilization).

Falsifier integrity preserved under --jobs N: NUC_NONDET_PERTURBED_CELLS=70 + NUC_XBACKEND_CORRUPTED_DETECTED=1 both bite identically.

Gate (cycle 52): just test 0 FAILED + 9 new arg-parser tests pass; just clippy clean; just e2e (sequential default) 88/70/0/18 UNCHANGED.

Review-gate (parallel read-only): both qa-test-runner + mped-architect GO. Architect verified worker-panic propagation (std::panic::resume_unwind on join Err — no silent loss), MAX_JOBS=64 cap as a loud-fail validator (not silent clamp), and the Clone derives on Paths + PlannedCell are over owned-data only (no shared mutable state). Two MEDIUMs noted both acceptable: (a) path-isolation between sibling cells is contract-level (TASK-0182's run-id) not type-enforced; (b) child cargo build process stdout under --jobs >=2 may interleave (acceptable; gate signals are summary-table-driven).

Cycle 51 attempt that hit a stream timeout left its detailed plan in this task's tracker; cycle 52 executed that plan directly.
<!-- SECTION:FINAL_SUMMARY:END -->
