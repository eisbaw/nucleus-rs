---
id: TASK-0044.02.03
title: Lift mp-tcp-bufsync + mp-tcp-poll plan/ duplication to a shared crate
status: In Progress
assignee:
  - '@mark'
created_date: '2026-05-27 05:10'
updated_date: '2026-05-28 09:57'
labels: []
dependencies: []
parent_task_id: TASK-0044.02
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Context

TASK-0044.02.02 cycle 195 landed mp-tcp-poll multi-worker codegen by COPYING mp-tcp-bufsync's `src/{encode.rs, walkers.rs, plan/{mod.rs, events.rs, relay.rs, worker_program.rs}}` substrate verbatim and swapping the wait primitive (`wire::*_poll` vs blocking, `apply_nonblocking` setup line). Same precedent as the original backend-common lift (TASK-0244) — fail-fast prove a second consumer, then lift.

The two backends now have ~1700 LoC of duplicated plan/walkers/encode logic. The poll/bufsync difference lives EXCLUSIVELY in the emit layer:

- wait-primitive call-site swap (3 sites in events.rs + 1 in relay.rs)
- apply_nonblocking line after apply_sock_buf in worker_program.rs
- module-doc / banner-text provenance lines

Everything else (host election, xfer registry, slice-paste tile derivation, accumulator classification, FIFO-shape hazards including TASK-0330 / TASK-0332 guards) is identical.

## Scope

Lift the shared substrate to a new `nucleus/backend-common/src/tcp_plan/` (or sibling crate `nucleus/mp-tcp-plan/`) parameterised over a small trait:

```rust
trait WirePrimitives {
    fn read_msg_expect_call(cv: &str, seq: u64) -> String;
    fn write_msg_call(cv: &str, seq: u64, enc: &str) -> String;
    fn barrier_cross_call(cv: &str, bid: u64) -> String;
    fn sock_setup_extra(sock: &str) -> Option<String>; // apply_nonblocking line
    const BACKEND_NAME: &'static str;
    const TASK_TAG: &'static str; // for provenance lines
}
```

mp-tcp-bufsync impl returns `wire::read_msg_expect(&mut {cv}, {seq})` etc; mp-tcp-poll impl returns `wire::read_msg_expect_poll(&mut {cv}, {seq})` and `Some("wire::apply_nonblocking(&{sock});")`.

## Acceptance criteria

1. Both backends' plan/walkers/encode source surface collapses to a thin shim over the lifted code.
2. Every test that pins bufsync emit-string shape still passes.
3. Every test that pins poll emit-string shape (cycle-195 multi_worker_emit + pingpong) still passes.
4. e2e baseline unchanged (210/169/0/41/0 going into the lift cycle; SAME after).
5. byte-identical-vs-bufsync emit test (poll's tests/multi_worker_emit.rs canonicalisation) survives — the lift must preserve byte-identicality MODULO the same legal swaps that test already discharges.

## Risk + safety profile

- The duplication is bounded by the byte-identical canonicalisation test in poll's tests/multi_worker_emit.rs — any future drift between the two substrates surfaces there before it hits e2e.
- The lift is OPTIONAL pending a third consumer (e.g. mp-uds-bufsync or a future blocking-UDS sibling). Two consumers prove the abstraction is well-shaped; three consumers prove it's load-bearing.

## Notes

- TASK-0044.02.02 cycle 195 commit message references the duplication explicitly + this follow-up id.
- Same pattern as backend-common itself (TASK-0244 cycle 37) which started life duplicated 3-4 ways before lift.
- See memory `project-backend-common-crate.md` for the broader pattern.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 195b — LoC precision update (review-gate architect P2.2)

Empirical line counts captured cycle-195b:
- mp-tcp-poll plan/walkers/encode total: **1370 LoC** (events 361 + mod 206 + relay 180 + worker_program 310 + walkers 221 + encode 92).
- mp-tcp-bufsync sibling total: **1846 LoC** (events 493 + mod 280 + relay 228 + worker_program 388 + walkers 376 + encode 81).
- Average: ~1608 LoC. Bufsync carries more docstring/banner narrative; substantive emit logic is closer to ~1370.

Architect P2.2 (cycle 195b): the implementer's "~1700 LoC" was a 6% overstatement against the bufsync side and ~25% overstatement against the poll side. Use the precise numbers above for next-cycle scoping. The lift's worker_program.rs surface is the largest delta (388 vs 310) and the rendezvous-port handshake renderer is the bulk of that delta.

Lift-task scoping when 3rd consumer arrives (TASK-0044.03.01, mp-uds-event multi-worker): build the WirePrimitives trait against ~1370 LoC of behaviour-bearing code, not ~1846; the latter includes provenance/banner text that lifts as separate strings.

## Cycle-235 implementation plan (implementer @mark)

Lift target: NEW module `nucleus/backend-common/src/tcp_plan/` parameterised over a `WirePrimitives` trait (associated-fn/const only; `Plan<'a, W: WirePrimitives>` carries `PhantomData<fn() -> W>`). Both backends' src/{encode.rs,walkers.rs,plan/{mod,events,relay,worker_program}.rs} collapse to a thin shim defining the per-backend WirePrimitives impl + a `type Plan<'a> = backend_common::tcp_plan::Plan<'a, ThisWire>` alias and re-export of the shared walkers/encode.

EXACT divergence map (verified by reading every file, not trusting the brief):
1. events.rs: 3 emitted wire-call sites (barrier_cross x2 [var cv + literal ctrl_host], write_msg, read_msg_expect). poll appends _poll. -> trait read_msg_expect_call/write_msg_call/barrier_cross_call build the call STRUCTURALLY (no textual replace).
2. worker_program.rs: (a) poll emits 4 extra `wire::apply_nonblocking(&sock);` lines after apply_sock_buf -> trait sock_setup_extra(sock)->Option<String>. (b) EMITTED file-header docstring differs: bufsync 'mp-tcp-bufsync backend (TASK-0036,' vs poll 'mp-tcp-poll backend (TASK-0044.02.02,' -> trait FILE_HEADER_BACKEND + FILE_HEADER_TASK consts. (NOT in the brief's map; caught by reading.)
3. relay.rs: hop emit uses read_msg_expect/write_msg (poll _poll). EMITTED relay-phase banner comment block wording genuinely differs -> trait relay_banner(pad)->String supplied per backend. SO_BUF const differs (poll adds a 3rd line) -> trait so_buf_comment()->&str.
4. walkers.rs: ZERO emitted-code diff; the ContractGap MESSAGE strings differ (compile-time errors, NOT emitted). Pinned substrings (bufsync tests): loop guard needs {BACKEND_NAME}/TASK-0330/TASK-0327/Loop/'FLAT outside any loop'; wait-before-push needs {BACKEND_NAME}/Wait/Push/TASK-0332/(host-relay|circular); mod.rs Sync gap needs 'exclude the host worker'. Canonical shared message parameterised by BACKEND_NAME retains all required substrings for BOTH (poll has no loop/wait-before-push msg tests). encode.rs: ZERO diff.
5. mod.rs: Plan struct + build + accessors identical modulo docstring + the Sync-gap ContractGap (BACKEND_NAME).

Byte-identity verification: (a) unit-level tripwire = poll's tests/multi_worker_emit.rs assert_poll_bin_equiv_bufsync legal-swap oracle (kept green WITHOUT editing expectations); (b) e2e 5-tuple unchanged; (c) direct nucleus build --out DIR spot-check (NOT post-just-e2e scratch, which is deleted on success).

Baseline e2e to preserve: 280/246/0/34/0 (orchestrator-stated; AC's 210/169/0/41/0 is stale). Measuring fresh baseline before edits.

## Cycle-235 implementation progress (lift LANDED, gate in flight)

Created nucleus/backend-common/src/tcp_plan/{mod,wire_primitives,plan,events,relay,worker_program,walkers,encode}.rs — the shared generic substrate Plan<'a,W: WirePrimitives>. Both backends reduced to a single shim file each: backends/mp-tcp-{bufsync,poll}/src/plan.rs (WirePrimitives impl + type alias). Deleted (git rm) each backend's old src/{encode.rs,walkers.rs,plan/{mod,events,relay,worker_program}.rs}.

WirePrimitives final shape (8 items): consts BACKEND_NAME / FILE_HEADER_BACKEND / FILE_HEADER_TASK; fns read_msg_expect_call(cv,seq)->String, write_msg_call(cv,seq,enc)->String, barrier_cross_call(cv,bid)->String, sock_setup_extra(sock)->Option<String>, so_buf_comment()->&str, relay_banner(pad)->String. Plan carries PhantomData<fn()->W>; all W methods associated (no self); zero-cost monomorphisation.

Gate so far (all GREEN inside nix dev shell): just build OK; just clippy OK (clean — hit the doc_lazy_continuation trap on the shim module-docs' wrapped '+ worker-program' lines, fixed by rephrasing '+' joins to commas/'and'); just test = 1051/0; just test-release = 1050/0 (expected -1 debug_assert should_panic per TASK-0291); check-textual-replace-on-codegen OK (all call strings built structurally via format!, zero .replace); check-include-str-coverage OK.

Byte-identity evidence at unit layer: bufsync pingpong_matches_pthreads_sync_bit_for_bit (compiles+RUNS generated project) PASS; poll {split_02,separable_filter_06_distributed2,transpose_15_distributed_rows}_poll_equiv_bufsync (the AC#5 legal-swap canonicalisation oracle) PASS; poll single_worker_* byte-identical-to-bufsync PASS; ContractGap message-pin tests (loop_body_w2w_push, wait_before_push) PASS — canonical shared messages parameterised by W::BACKEND_NAME retain all pinned substrings.

GOTCHA chased: first bufsync test run FAILED — pingpong's generated w0.rs had an unclosed delimiter. Root cause: hand-transcription error in the giant \x20-escaped read_rendezvous_port raw string (sleep line + 2 closer braces had wrong \x20 counts: 14/12/10 vs correct 16/14/12). Fixed by extracting exact \x20 counts from the HEAD git blob and matching. Verified ALL 54 \x20-bearing emitted lines byte-identical to original via awk count|content diff. LESSON for the sibling lift TASK-0044.03.02: diff every \x20-heavy raw string against the original blob (awk count+content) BEFORE trusting; hand-transcription of these is the highest byte-drift risk.

e2e gate currently running; will report the actual 5-tuple. Not marking Done — orchestrator runs independent review.

## Cycle-235 GATE RESULTS (code committed fd1bb0d; status left In Progress for independent review)

ALL gate arms GREEN inside nix dev shell:
- just build: clean
- just clippy: clean (-D warnings; doc_lazy_continuation trap hit on shim module-doc wrapped '+' lines, fixed pre-commit)
- just test (dev): 1051 passed / 0 failed
- just test-release: 1050 passed / 0 failed (expected -1 vs dev = debug_assert-gated #[should_panic] per TASK-0291)
- just check-textual-replace-on-codegen: OK (every wire call built structurally via format!; zero .replace on codegen strings)
- just check-include-str-coverage: OK
- just e2e: **280 / 246 / 0 / 34 / 0** — IDENTICAL to pre-lift baseline (measured fresh before edits = 280/246/0/34/0). BYTE-IDENTICAL emit confirmed.
- just determinism-check: 280/246/0/34 stable
- just xbackend-check-negative: 33 applied / 10 detected — 'differential correctly bit on injected mp-tcp corruption' (cross-backend falsifier intact through the lift)

Independent byte-identity spot-check (direct nucleus build, NOT post-e2e scratch): 06-separable-filter/distributed2 × {mp-tcp-bufsync, mp-tcp-poll} → all 5 worker bins (host+w0..w3) byte-identical modulo the documented legal swaps (3 wire calls, apply_nonblocking lines, file-header, relay banner) + SO_BUF run.sh comment. Verified each divergence routes through W: file-header (TASK-0036 vs TASK-0044.02.02), SO_BUF (2-line vs 3-line), apply_nonblocking (0 vs 2-per-worker/8-host).

Silent-sibling check: grep confirms NO leftover local def of any lifted symbol (RelayHop / collect_w2w_pushes / render_events / render_worker_program / Plan / XferId / encode_expr / collect_barriers_by_tag / relay_phase_insertion_point) in either backend src — both route exclusively through backend_common::tcp_plan. ONE implementation; drift structurally impossible now.

Per-AC status (AC e2e baseline 210/169/0/41/0 is STALE — current tree baseline is 280/246/0/34/0):
- AC#1 (both backends collapse to thin shim over lifted code): MET — each backend = 1 shim file src/plan.rs (~75-90 LoC) + lib.rs mod line; ~3240 LoC of duplication deleted.
- AC#2 (bufsync emit-string pins pass): MET — all bufsync tests green (multi_worker_emit, pingpong, host_relay_emit via reuse_codegen/rendezvous/check_frame, loop_body_w2w_push, wait_before_push).
- AC#3 (poll emit-string pins pass): MET — poll multi_worker_emit (3 *_poll_equiv_bufsync) + pingpong + single_worker + skeleton all green.
- AC#4 (e2e unchanged): MET — 280/246/0/34/0 == baseline.
- AC#5 (byte-identical-vs-bufsync canonicalisation oracle survives): MET — poll tests/multi_worker_emit.rs assert_poll_bin_equiv_bufsync passes UNEDITED (expected strings not touched).

No follow-up tasks needed (no stubs/shortcuts; the lift is complete and byte-clean). NOT marking Done — orchestrator runs the independent review gate (qa-test-runner + mped-architect).
<!-- SECTION:NOTES:END -->
