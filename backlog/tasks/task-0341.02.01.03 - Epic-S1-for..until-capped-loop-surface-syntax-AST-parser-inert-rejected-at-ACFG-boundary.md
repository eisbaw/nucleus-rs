---
id: TASK-0341.02.01.03
title: >-
  Epic S1: for..until capped-loop surface syntax + AST + parser (inert, rejected
  at ACFG boundary)
status: To Do
assignee: []
created_date: '2026-06-03 10:13'
updated_date: '2026-06-03 11:47'
labels:
  - grammar-extension
  - frontend
  - grammar-algo
dependencies:
  - TASK-0341.02.01.02
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Land the surface syntax + AST node + parser for the bounded early-exit loop, INERT: parsed and represented but REJECTED at the ACFG-build boundary with a typed error pointing at the epic (acfg/build.rs NonConstLoopBound neighbourhood). Surface form (architect-preferred, design review cycle-254): for IDENT : 0 .. N until COND { Stmt* } where N is the COMPILE-TIME cap (keeps the loop statically bounded — the cap keystone) and COND is the HALT predicate (converged), e.g. until diff <= tol. Architect chose until over while: COND-as-halt reads as author intent, and while carries unbounded-loop baggage that invites misreading the cap as not-a-real-bound. Grammar ForStmt ::= for Ident : ConstExpr .. ConstExpr (until Expr)? { Stmt* } is LL(1)-clean (until/brace is single-token lookahead). The loop var IDENT MUST stay (double-buffering needs it for generation indexing) — this is why a pure while/iterate form was rejected. Depends on S2 (relational/bool ops) so COND is grammatical. First landed example uses a COND that does NOT need a scalar reduction (decouple from S3) — e.g. a pre-existing scalar predicate — proving the loop-control surface independently. Anchor a malformed-COND diagnostic at the until token (TASK-0434 VAR-anchoring precedent) to avoid reintroducing the diffuse-span problem. Inert => zero e2e change; 16-jacobi converge example stays SKIPPED with a precise reason.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 for i : 0..N until COND { } parses into a new AST node carrying var, bounds, COND, body; positive + negative parser tests
- [ ] #2 Lowers to a new IR node then REJECTED at the ACFG boundary with a typed error naming the epic (not a panic)
- [ ] #3 Malformed-COND diagnostic anchored at the until token; e2e baseline unchanged (inert)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FORWARD-CARRIED from S2 (TASK-0341.02.01.02, cycle-256, commit e626916): the bool-expression grammar S1 depends on is NOW LANDED. ast Expr::Compare + CmpOp, ir IrExpr::Compare + IrCmpOp, parser RelExpr level (relational below additive, single non-associative comparison). S1 CONSUMER GUIDANCE:
1. The for..until COND must lower through the BOOL-ACCEPTING path (lower_rvalue, which has the Expr::Compare arm), NOT lower_index_expr / eval_const_expr (those REJECT a comparison with the new span-anchored LowerErrorKind::ComparisonNotAllowedHere - that is the where-allowed bool rule, enforced POSITIONALLY).
2. There is NO bool LHS/RHS type-checker today: lower_rvalue accepts a Compare as any rvalue (incl a kernel arg), not gated to bool LHS. If S1 needs to ENSURE the until-COND is bool-typed (reject `for i:0..N until x` where x is i32), that type gate must be ADDED in S1 - it does NOT exist yet.
3. Bool-DATA e2e codegen (Vec<bool> buffer / input.bin layout) was DEFERRED from S2 to here (S1 until-cond is the real first consumer). The Compare node currently has NO backend render path for a bool RHS - render_int_expr/render_const_expr return a typed EmitError on a Compare (defense-in-depth backstop). S1/S5 must add the bool render path.
4. Anchor the until-COND diagnostic at the until token per TASK-0434 precedent (epic P2).
<!-- SECTION:NOTES:END -->
