---
id: TASK-0453.01.01
title: >-
  diff-fuzz follow-ups: per-command timeout (hang->FAIL) + multi-worker
  partition generation
status: Done
assignee: []
created_date: '2026-06-06 23:22'
updated_date: '2026-06-10 19:31'
labels:
  - rigour
  - testing
dependencies: []
parent_task_id: TASK-0453.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Named residuals from TASK-0453.01 architect review (cycle-1). (1) P3.2: diff_fuzz has NO per-command timeout, so a backend DEADLOCK hangs the harness rather than failing loud (a hang reads as 'still running', not FAIL) - add a wall-clock timeout on the run phase converting a hang into a reported failure. The compile-time soundness gate is the real liveness guard; this is value-correctness, but given the repo's deadlock history a timeout is worth adding. (2) The generated subclass is single-cross-worker-boundary (host+w0); generating a multi-compute-worker partition form (place ... on {w0,w1} / partition=workers) is the most pointed extension and would stress the distributed transfer-inject path the curated corpus exercises.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 run phase has a wall-clock timeout; a hung backend is reported as FAIL with the program dump
- [x] #2 generator optionally emits a multi-compute-worker partitioned form proven green across tier-1
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Owner of: per-command timeout + multi-worker partition gen.
(1) Add a wall-clock timeout helper (spawn+wait+kill on Linux) wrapping every Command in run_backend (nucleus build, cargo build, run). A hang->FAIL with the program dump; never a stall. Default budget generous (build can be slow on cold cache), env-overridable knob DIFF_FUZZ_TIMEOUT_SECS.
(2) Add a multi-compute-worker partitioned shape variant: place compute on {w0,w1} + loop i : partition=workers, modelled on 03-reduction/05-stencil distributed scheds. Prove green across all 7 tier-1.
Cross-ref: TASK-0455.05 is the sole co-owner; partition gen lives HERE (this task), reductions/2D/until live in 0455.05.
Files: only nucleus/e2e/src/bin/diff_fuzz.rs (+ tests inline). Validate: cargo test -p e2e; cargo build -p e2e --bins; seeded fuzz run; clippy; check-mega-files.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPL landed (e2e/src/bin/diff_fuzz/ module split). AC#1: per-command wall-clock timeout on every build/run via exec.rs run_timed — child in own process group, SIGKILL the whole group on deadline (so bash run.sh forked workers are torn down), typed Timed::Timeout -> phase-tagged FAIL with partial-output tail; env knob DIFF_FUZZ_TIMEOUT_SECS (default 600s, zero rejected). Found+fixed a real deadlock in the timeout path itself: draining the captured pipe BEFORE the group kill blocked read_to_end forever on a forked grandchild holding the write end; reordered to kill-then-drain (exec::tests::group_kill_reaps_forked_children pins it). AC#2: multi-compute-worker partition=workers family (family/partition.rs) — place stage on {w0..wk} + loop w:partition=workers, two-operand map a/c, modelled on 03-reduction distributed; harness-verified 7/7 byte-exact (prog-seed 4: NW=3 PS=17). This task is the SOLE OWNER of multi-worker partition generation; TASK-0455.05 consumes it (cross-ref recorded there too). 44 diff-fuzz unit tests green; clippy -p e2e --all-targets clean; check-mega-files green.

Validated within the same seed=12345 k=120 run (101/120 GREEN, 0 FAIL): partition_workers family (this task owns it) exercised 19x, all 7/7 byte-exact; for_until family 15x, all 1/1 (pthreads-sync). Per-command timeout machinery exercised on every build/run (none tripped — all builds completed well under the 600s budget); the timeout-path deadlock that the exec unit tests caught is fixed.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Both halves landed: (1) per-command wall-clock timeout on every spawned phase (process_group(0) + deadline + kill-group-THEN-drain; a real pipe deadlock in the drain-first order was found and fixed, pinned by two unit tests incl. a forked-grandchild reap test the review verified empirically); DIFF_FUZZ_TIMEOUT_SECS knob, phase-tagged FAIL with output tail. (2) multi-compute-worker partition=workers family, 7-backend byte-exact (19 programs in the seeded run + hand-verified). The curated-harness sibling gap (bare .output() spawns in run.rs/determinism.rs) is filed as TASK-0466. Landed b376165; architect GO.
<!-- SECTION:FINAL_SUMMARY:END -->
