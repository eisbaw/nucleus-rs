---
id: TASK-0046.02
title: >-
  M8 — lift shared multi-worker MPI Plan into backend-common (de-dup
  mpi-blocking/mpi-nonblocking)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-31 09:46'
updated_date: '2026-06-01 17:40'
labels:
  - M8
dependencies: []
parent_task_id: TASK-0046
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0046 copied the multi-worker Plan logic (host election, rank assignment, channel-id collection, barrier participant analysis, non-whole-world-barrier + check-frame loud rejects, the render_worker_events walk) from mpi-blocking/src/multi_worker.rs into mpi-nonblocking/src/multi_worker.rs verbatim (mirror, to bound landing risk). The ONLY material difference between the two backends is the rendezvous prelude (blocking Send/Recv vs buffered Ibsend/Imrecv) + the buffered-send buffer attach. This duplication is a silent-sibling hazard. Lift the shared Plan into backend-common (precedent: tcp_plan/event_plan substrates, TASK-0244) with a thin per-backend prelude/transport hook, so a fix to the Plan logic cannot skip one backend.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Shared multi-worker MPI Plan lives in backend-common; mpi-blocking and mpi-nonblocking both consume it via a thin prelude/transport hook
- [x] #2 Byte-identity preserved: just check-mpi and just check-mpi-nonblocking both still pass byte-exact after the lift
- [x] #3 No duplicated Plan::build / render_worker_arm / collect_chan_peers across the two MPI backends
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPLEMENTATION PLAN (TASK-0046.02 lift shared MPI Plan to backend-common)

Onboarding done: read tcp_plan {mod,wire_primitives,plan}.rs + mp-tcp-bufsync/plan.rs shim + event_plan/mod.rs (sibling), both MPI multi_worker.rs FULLY, both lib.rs + tests.rs, both Cargo.toml + backend-common Cargo.toml/lib.rs. Confirmed: imports of the two multi_worker.rs are byte-identical; all walker symbols the Plan needs are already pub (collect_pair_tiles, collect_barriers_by_tag, collect_accumulate_waits, collect_pre_init_sets, collect_let_at_wait_data, render_worker_events, WalkerCtx, RendezvousId). backend-common has NO mpi/rsmpi dep and must not gain one (prelude is EMITTED text).

VARIATION SURFACE (the trait, name MpiRendezvous) -- the SOLE delta confirmed by diffing both files:
 1. BACKEND_NAME const ("mpi-blocking" / "mpi-nonblocking"): used in 3 EmitError messages (no-used-worker ContractGap, check-frame UnsupportedFeature reject, non-whole-world-barrier UnsupportedFeature reject) + the header banner + the rank-count eprintln + the file-header backend-name.
 2. header_dispatch_lines(): the //! Push/Wait => ... banner comment lines (blocking Send/Recv vs buffered Ibsend/Imrecv prose).
 3. prelude(): the rendezvous + WorldBar wrapper-type source text (blocking VecChan/ScalarChan vs buffered).
 4. emit_universe_init(out, plan): the lines from `let [mut] universe = mpi::initialize()...` through `let world = universe.world();`. Blocking: plain `let universe` + world. Nonblocking: `let mut universe` + MPI_Buffer_attach (set_buffer_size via bsend_bytes heuristic + NUC_MPI_BSEND_BYTES env) + world. This single seam captures BOTH the `mut` difference AND the buffer-attach block.

SHARED (lives in backend_common::mpi_plan): Plan struct + build (used_workers, host election rank0, rank assignment 1..N, check-frame reject, chan_ids from collect_pair_tiles, barrier_participants, non-whole-world reject, accumulate_waits), render_worker_arm (pre-init + chan bindings + barrier bindings + render_worker_events walk rendezvous_prefix="mpi"), chans_used_by, collect_chan_peers, record_peer, chan_id, rank_for, barriers_used_by, worker_name, data_name, collect_pre_init, ChanDecl, Dir, has_check_frame, mpi_equivalence_scalar, rust_type_of, render_array_init, rust_scalar_zero, scalar_bytes, bsend_bytes (shared but only consulted by the nonblocking impl via the trait seam).

bsend_bytes/scalar_bytes: these are mpi-nonblocking-only TODAY but harmless+correct to keep in shared Plan; the nonblocking emit_universe_init reads plan.bsend_bytes(). Keep them in the shared Plan so the heuristic is one source of truth too.

DESIGN: backend_common/src/mpi_plan/{mod.rs, rendezvous.rs (trait), plan.rs (Plan<W>)} mirroring tcp_plan layout. Each backend reduces to src/multi_worker.rs = `impl MpiRendezvous for <Wire>` + `type Plan<a> = backend_common::mpi_plan::Plan<a,Wire>` + render_main_rs_multi delegating to Plan::build(...).emit(). Delete the duplicated Plan/build/render_worker_arm/collect_chan_peers/helpers from BOTH backends.

GATE: full `just build && just clippy && just test && just test-release && just e2e` before each commit; e2e must stay 385/328/0/57/0. AC#2 = `just check-mpi && just check-mpi-nonblocking` byte-exact under .#mpi. cargo doc warning baseline 10 to check if moving doc-linked items.

COMMIT UNITS: (a) add backend_common::mpi_plan substrate; (b) reduce mpi-blocking to shim; (c) reduce mpi-nonblocking to shim. Full gate before each commit.

OUTCOME (lift LANDED, full gate GREEN, both byte-identity arms PASS):

WHAT LANDED: new module backend_common::mpi_plan {mod.rs 62, rendezvous.rs 80, plan.rs 735}. Trait MpiRendezvous = SOLE variation axis: const BACKEND_NAME (3 EmitError diagnostics + emitted file-header + rank-count eprintln); write_header_dispatch_lines (the //! Push/Wait banner prose); prelude() (rendezvous + WorldBar wrapper-type source); write_universe_init (let-universe..let-world block — blocking takes world directly, buffered attaches MPI_Bsend buffer first). Shared Plan<W> owns: build (used_workers, host election rank0, rank 1..N, check-frame reject, chan_ids, barrier_participants, non-whole-world reject, accumulate_waits), emit, render_worker_arm, chans_used_by, collect_chan_peers, record_peer, chan_id, rank_for, barriers_used_by, worker_name, data_name, collect_pre_init, bsend_bytes/scalar_bytes, ChanDecl, Dir, has_check_frame, mpi_equivalence_scalar, rust_type_of/render_array_init/rust_scalar_zero.

DELETED FROM EACH BACKEND (now in shared substrate): struct Plan + build + render_worker_arm + chans_used_by + collect_chan_peers + record_peer + chan_id + rank_for + barriers_used_by + worker_name + data_name + collect_pre_init + ChanDecl + Dir + has_check_frame + mpi_equivalence_scalar + the 3 type helpers (BOTH backends); + bsend_bytes + scalar_bytes (mpi-nonblocking). Each multi_worker.rs reduced to: impl MpiRendezvous for {Blocking,Buffered}Rendezvous + type Plan alias + render_main_rs_multi wrapper.

LoC: mpi-blocking 711->147, mpi-nonblocking 888->246 (shims 393 total); shared substrate 877 lives ONCE (was duplicated ~600-line Plan twice). Net -329 lines AND the behaviour-bearing Plan de-duplicated.

COMMITS (on master, NOT pushed):
 362a2a9 backend-common: TASK-0046.02 add shared SPMD MPI Plan substrate (mpi_plan)
 c9e5d6a mpi-blocking: TASK-0046.02 reduce multi_worker to a thin MpiRendezvous shim
 cefc976 mpi-nonblocking: TASK-0046.02 reduce multi_worker to a thin MpiRendezvous shim

GATE (actual numbers): just build OK; just clippy clean (-D warnings, independently re-run); just test 1238 passed/0 failed; just test-release all green/0 failed; just e2e total 385 pass 328 fail 0 skipped 57 required-fail 0 (EXACT baseline, MPI not in matrix so unchanged total = no tier-1 regression); check-textual-replace-on-codegen OK; check-include-str-coverage OK; check-mega-files OK (plan.rs 735 < 1000); cargo doc 10 links-to-private warnings UNCHANGED from baseline (the mpi_blocking/mpi_nonblocking [multi_worker] links are pre-existing, ZERO new from mpi_plan).

AC#2 byte-identity (the acceptance signal) — BOTH PASS under .#mpi (OpenMPI 5.0.10):
 just check-mpi: 6 single -n1 + 4 multi-worker -n N all-ranks-live byte-exact vs reference.bin.
 just check-mpi-nonblocking: 3 async schedules (05-distributed, 05-distributed-2d real w<->w 2x2 halo, 11-pipelined) x {default, forced-rendezvous} byte-exact + deadlock-immune.
 ALSO emit-text A/B (git-stash HEAD vs reduced): 02-split + 03-reduction/distributed (blocking) and 02-split + 05-stencil/distributed (nonblocking) main.rs BYTE-IDENTICAL; committed emit sha256 matches pre-reduction snapshot exactly.

AC DISPOSITION (all 3 genuinely MET):
 AC#1 MET: shared Plan in backend-common; both backends consume via thin MpiRendezvous hook + type Plan alias.
 AC#2 MET: check-mpi + check-mpi-nonblocking both byte-exact after the lift (above).
 AC#3 MET: grep-verified no struct Plan / fn build / render_worker_arm / collect_chan_peers in either backend src.

GOTCHAS / SUBTLETIES (for sibling MPI tasks + reviewer):
 - The universe-init seam: I chose ONE trait method (write_universe_init) spanning let-universe..let-world INCLUSIVE because that single region captures BOTH the `let universe` vs `let mut universe` mut-difference AND the buffer-attach block. let-size and everything after stays shared. Cleaner than a separate buffer-attach-line method (which would have left the mut-difference unhandled).
 - bsend_bytes() is called UNCONDITIONALLY in the shared emit() (passed to write_universe_init) but its result is consumed ONLY by the buffered impl; the blocking impl takes _bsend_bytes and discards it. Tiny wasted compute, byte-identical-safe, one source of truth for the heuristic. NOT a behaviour change.
 - Doc-link trap avoided: kept `mod multi_worker;` in both lib.rs, so the pre-existing [multi_worker] doc-link warnings are unchanged (not newly introduced/removed). New mpi_plan intra-doc links all resolve (0 new warnings).
 - Comment-doc-lie discipline: no blocking-specific claim leaked into the shared substrate. The shared plan.rs docstring says "via MPI" generically; the blocking-vs-buffered specifics live in each shim docstring. The non-whole-world reject comment was de-backend-specified ("check-mpi*" not "check-mpi").
 - NO new backend-common dependency: mpi::... is EMITTED text (resolves in generated crate), exactly like tcp_plan/event_plar wire strings.

FOLLOW-UPS: none filed — clean lift, no stubs/shortcuts/new gaps.

=== Orchestrator parallel read-only review gate (cycle close) — both GO ===

Ran the mandatory parallel read-only gate on commit range 5ce8337..cefc976 (qa-test-runner + mped-architect). Numbers below are REVIEWER-RE-RUN, not implementer-claimed.

qa-test-runner GO: just build clean; just clippy clean x2 (forced recompile of backend-common + both mpi backends + 12 dependents, no doc_lazy_continuation); just test 1238 passed / 0 failed / 3 ignored; just test-release 1237 passed / 0 failed / 3 ignored (the -1 is the known debug_assert-gated should_panic compile-out, TASK-0291); just e2e total 385 / pass 328 / fail 0 / skipped 57 / required-fail 0 on TWO consecutive runs (non-flake, byte-identical); just check-mpi PASS (8 cells byte-exact under mpiexec -n1 and -n N); just check-mpi-nonblocking PASS (7 cells incl the real w<->w 2x2 halo 05-stencil/distributed-2d x {default, forced-rendezvous}).

mped-architect GO (0 P1 / 0 P2 / 2 P3): EMPIRICALLY proved byte-identity via a git-worktree A/B at 5ce8337 vs cefc976 -- diff -rq clean on SIX full generated project trees (both backends, incl 05-distributed-2d real-halo + game-of-life buffer-attach path), strongest possible AC#2 evidence. Confirmed silent-sibling residue gone (grep: struct Plan/build/render_worker_arm/collect_chan_peers/chans_used_by/barriers_used_by/bsend_bytes/emit_prelude all return nothing in both backend src/ trees; both loud-reject guards exist exactly once). _bsend_bytes discard verified benign (no bsend/Buffer_attach/Ibsend text in ANY blocking output; positive control confirms it IS in nonblocking). No comment-doc-lie from moved docstrings. AC#1/#2/#3 genuinely met, not AC-gamed. No mpi/rsmpi dependency added to backend-common.

P3-1 (resolved, no code change): implementer disclosure said cargo-doc shows 10 warnings; the real count is 14, and it is UNCHANGED pre-lift vs post-lift (warning-set diff empty). No regression; the 10 was a stale forward-carried figure (TASK-0410/0411 era). Recording the accurate 14-unchanged here per do-not-transcribe-claimed-numbers discipline.

P3-2 (resolved, no code change this cycle): backend_common::mpi_plan::mod.rs pub use plan::{ChanId, Plan} re-exports ChanId with no external consumer, BUT this EXACTLY mirrors the deliberately-kept sibling event_plan/mod.rs:70. Narrowing only mpi_plan would diverge the two substrates. Left consistent; symmetric cleanup filed as TASK-0412 (drop ChanId from BOTH pub use lines together).

OUTCOME: TASK-0046.02 Done is justified by independent review. Byte-identity proven (not just argued), gate green (re-run), silent-sibling hazard CLOSED. Net -317 LoC; shared SPMD MPI Plan now lives once in backend_common::mpi_plan behind the MpiRendezvous trait.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
TASK-0046.02 DONE: lifted the verbatim-duplicated multi-worker SPMD MPI Plan into backend_common::mpi_plan (trait MpiRendezvous = sole variation axis: BACKEND_NAME const + header banner + rendezvous prelude + universe-init/buffer-attach). mpi-blocking 711->147 LoC, mpi-nonblocking 888->246 LoC; shared substrate 877 LoC lives once (was a ~600-line Plan duplicated twice). Silent-sibling hazard CLOSED: a future Plan-logic fix lands in one place, cannot skip a backend. All 3 ACs met. Commits 362a2a9 + c9e5d6a + cefc976 (master, not pushed). Gate: build/clippy clean; test 1238/0; test-release green; e2e 385/328/0/57/0 (exact baseline, no tier-1 regression); check-mpi + check-mpi-nonblocking both byte-exact under .#mpi; emit-text A/B byte-identical (committed emit sha256 == pre-reduction snapshot); cargo doc 0 new warnings. PURE DE-DUP, no behaviour change. No follow-ups filed.
<!-- SECTION:FINAL_SUMMARY:END -->
