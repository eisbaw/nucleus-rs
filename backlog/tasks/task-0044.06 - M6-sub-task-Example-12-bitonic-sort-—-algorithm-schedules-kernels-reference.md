---
id: TASK-0044.06
title: >-
  M6 sub-task: Example 12 (bitonic sort) — algorithm + schedules + kernels +
  reference
status: To Do
assignee: []
created_date: '2026-05-26 09:26'
labels:
  - M6
  - examples
dependencies: []
parent_task_id: TASK-0044
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land example 12 — bitonic sort — per PRD §9 row 12: 'Static communication pattern, fits SDF'. The first example with a DATA-DEPENDENT IN-PLACE sort using a fixed-shape communication pattern that fits the synchronous-dataflow (SDF) model — every stage knows in advance which element pairs to compare-exchange.

## What this stresses (per PRD §9)

- Algorithmic: bitonic sort network has a static compare-exchange pattern that depends only on input SIZE, not on input VALUES. Despite being a sort (data-dependent at the comparator level), the COMMUNICATION pattern is fully scheduled at compile time — making it a clean fit for SDF.
- Scheduling: naive (single worker, log²(N) stages) + parallel (N/2 workers per stage, each handles one compare-exchange pair). The PARALLEL schedule is the load-bearing test of static-but-irregular communication.
- 'Fits SDF' marker: this is the canary example for whether v2's schedule sublanguage can EXPRESS a stage-by-stage compare-exchange network. If it can't cleanly, that's a meaningful expressiveness boundary.

## Closest template

`nuc-nucleus/examples/02-split-add/` — the existing multi-worker compare/transfer example. Reuse:
- Multi-worker pattern with explicit data flow between worker pairs.
- input.bin / reference.bin oracle pattern.

The diff: bitonic sort is MULTI-STAGE — each of the log²(N) stages has a different pairing of elements. The naive schedule is a sequential loop over stages; the parallel schedule has all N/2 compare-exchanges within a stage in parallel, then barrier, then next stage.

## Deliverables

- `nuc-nucleus/examples/12-bitonic-sort/prog.algo.nuc` — algorithm: power-of-2 length input array, log²(N)-stage bitonic network.
- `nuc-nucleus/examples/12-bitonic-sort/schedules/naive.sched.nuc` — one worker, sequential stages.
- `nuc-nucleus/examples/12-bitonic-sort/schedules/parallel.sched.nuc` — N/2 workers per stage, intra-stage parallel, inter-stage barrier.
- `nuc-nucleus/examples/12-bitonic-sort/kernels.rs` — compare-exchange kernel + stage-index → pair-mapping kernel.
- `nuc-nucleus/examples/12-bitonic-sort/reference/` — independent reference impl (or use Rust std `sort` as the oracle).
- `nuc-nucleus/examples/12-bitonic-sort/input.bin`, `reference.bin`.
- `nuc-nucleus/examples/12-bitonic-sort/README.md`.

## Acceptance criteria

1. `prog.algo.nuc` parses + lowers cleanly on pthreads-sync.
2. `naive.sched.nuc` lowers + emits + runs bit-identical against reference.bin on pthreads-sync + mp-tcp-bufsync.
3. `parallel.sched.nuc` (if expressible) lowers + emits + runs bit-identical on at least one tier-1 backend.
4. kernels.rs deterministic. Sort COMPARATOR is total + stable for equal keys (or document why stability doesn't matter for the test input).
5. Reference impl independent (e.g. Rust std `sort_unstable` on the input, or a hand-rolled merge-sort).
6. README documents required schedules + the SDF-fit claim.
7. e2e-matrix.toml entries.

## Honest scope

- Bitonic sort needs input length = 2^k. Pick a single fixed length (e.g. 1024) and document.
- Comparator MUST be total + deterministic. If input has duplicates, the comparator's tie-breaking determines the output bit-pattern. Use distinct integers for the canned input to sidestep the issue, OR document the tie-break rule.
- The PARALLEL schedule has N/2 workers — that's MANY workers for N=1024 (512 workers). The pthreads-sync backend may have a thread-count limit; consider a smaller N for the test (e.g. 16 elements, 8 workers max) or use a coarser worker assignment (e.g. 4 workers each handling N/8 pairs per stage).
- The stage-by-stage barrier may exercise existing sync_inject machinery in a new way; expect to discover whether the schedule grammar can express 'log²(N) stages of varying parallelism' cleanly, or whether it needs unrolling.

## Cross-reference

- Template: `nuc-nucleus/examples/02-split-add/` (multi-worker compare/transfer).
- Related: `nuc-nucleus/examples/07-matmul/` (multi-stage / blocked work).
- PRD: §9 row 12.

## Forward-carried context (orchestrator briefing for the implementer)

- The 'fits SDF' claim is the load-bearing one. If you find the SDF fit needs an extension (e.g. parametric stage-count loops with per-stage worker remapping), file the gap precisely — this is a thesis-level result, not a flaw.
- Start with a SMALL fixed length (8 or 16 elements) for the first cut. Scaling to 1024 is a smoke-test cell; the COMPILATION boundary is what's interesting, not the runtime.
- check-narrative-doc-lie + textual-replace + include_str! coverage all apply.
- If you find the schedule sublanguage cannot express log²(N) variable-parallelism stages, ship the naive schedule + file the grammar-extension follow-up. Do NOT hack a workaround.
<!-- SECTION:DESCRIPTION:END -->
