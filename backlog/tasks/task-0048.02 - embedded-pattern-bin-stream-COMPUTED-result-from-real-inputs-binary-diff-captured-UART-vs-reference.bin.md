---
id: TASK-0048.02
title: >-
  embedded-pattern bin: stream COMPUTED result from real inputs + binary-diff
  captured UART vs reference.bin
status: Done
assignee:
  - '@me'
created_date: '2026-05-28 12:53'
updated_date: '2026-05-28 14:50'
labels:
  - M10
  - backend
  - validation
dependencies: []
parent_task_id: TASK-0048
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Follow-up to TASK-0048.01 (which proved the EMISSION pipeline with zero-filled stub inputs => deterministic ASCII line 'NUC-EX1 len=1024 checksum=0'). This task makes the generated STM32H7 firmware compute on REAL input data and stream the actual result for a binary diff vs reference.bin. Two pieces: (1) a real input path — the load_input/load_input_b effectful hooks currently no-op (alloc_in_region returns null, dma_wait no-ops), so a/b stay zero; needs either a Renode-injected input region (sysbus WriteBytes / a memory file backend) or embedding input.bin bytes into the firmware. (2) stream the raw output bytes (or a strengthened framing) over USART1 and binary-diff the captured bytes against reference.bin (replace the grep-q ASCII assertion with a byte-exact diff), per PRD §10.3 point 3. Note the carried Renode caveat: STM32F7_USART hardwires TXE=true so raw streaming is fine but back-pressure is unvalidated.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 load_input/load_input_b effectful hooks fill a/b with REAL non-zero data (Renode-injected input region via sysbus, or firmware-embedded input.bin bytes); a/b are no longer zero at compute time.
- [x] #2 Firmware streams the raw computed output bytes (or a byte-faithful framing) over USART1.
- [x] #3 Recipe binary-diffs the captured bytes against a host-computed reference.bin (fail-loud), replacing the grep-q ASCII line assertion (PRD §10.3 point 3).
- [x] #4 The assertion BITES: a deliberately corrupted compute/stream is detected by the diff (negative check).
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. DE-RISK FIRST (mandatory): prove the input-injection mechanism end-to-end in-sandbox with a throwaway STM32H7 firmware before any codegen. Evaluate (B) Renode-injected region (sysbus LoadBinary input.bin into axiSram @0x24000000 — UNMAPPED in memory.x so the linker never collides; firmware reads first word back over USART1, cmp vs input.bin word0) vs (A) firmware-baked static (include_bytes!). Pick the one that proves deterministic in-sandbox; document why.
2. Wire the chosen real-input path into render_fire's LOAD lowering (alloc_in_region + load dma_wait). The load hook fills a/b with REAL data. Keep the lowering structural (no String::replace on rendered exprs).
3. Switch the SAVE path (Usart1Shim::dma_push) to stream RAW output bytes (the c array, 1024 bytes) over USART1 verbatim instead of the ASCII summary. Keep docs honest (drop the checksum=0 / zero-input claims).
4. Update tests/renode/embedded-ex1/run.resc + the renode-embedded-ex1 recipe: inject input.bin (if mechanism B), capture raw USART1 bytes to a file, strip any framing, cmp byte-exact vs reference.bin, fail loud. Run TWICE (determinism).
5. Prove AC#4 BITES: temporary corrupt of compute/stream detected by cmp; revert; report how.
6. Gate before every commit: nix develop --command 'just build && just clippy && just test && just test-release && just e2e' (baseline 280/246/0/34/0); .#embedded just check-embedded (M9 intact); .#renode just renode-embedded-ex1 (cmp vs reference.bin, twice); structural gates.
7. File follow-ups (generalise input path to ex5/ex9 on TASK-0048.03; real DMA shim on parent; anything deferred). Commit per logical unit, no AI co-author.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 239 implementation (real injected input + byte-exact reference.bin diff) — commit f297c11

### Mechanism choice: (B) Renode-injected region, NOT (A) firmware-baked static
DE-RISKED FIRST with a throwaway STM32H7 firmware (mandatory, mirroring cycle-237). Two proofs in-sandbox before any codegen:
1. firmware reads word0 from axiSram @ 0x24000000 after .resc did 'sysbus LoadBinary @input.bin 0x24000000' -> captured 'INPUT0=7' == input.bin a[0] (Renode log showed 'Loading block of 2048 bytes length at 0x24000000' BEFORE the CPU started; load ordering correct).
2. full path: firmware read a[0..N]+b[0..N], computed c, streamed RAW 1024 bytes -> cmp -s identical to reference.bin. Both halves (input + raw-output capture) proven, then scratch deleted.
WHY (B) over (A): architecturally honest (input arrives from a memory region the platform models, closer to the real DMA-from-peripheral shim of parent AC#1); no fixture baked into flash; fully deterministic in-sandbox. The KEY enabler is axiSram @ 0x24000000 (512K): mapped in platforms/cpus/stm32h743.repl but NOT in our memory.x (which is only FLASH@0x08000000 + DTCM RAM@0x20000000), so the linker never places stack/.bss there -> the injected fixture cannot collide. (A) remains a viable fallback if a future target lacks an unmapped-but-modelled region.

### Files (commit f297c11)
- skeleton.rs USART1_SHIM_SRC: Usart1Shim gained input_cursor field; alloc_in_region returns NUC_INPUT_REGION+cursor and advances by bytes (the REAL input path); dma_push streams RAW bytes (dropped the ASCII summary + usart1_put_u32 + checksum); dma_wait no-op (synchronous injection). Docs rewritten to the new behaviour.
- lib.rs render_fire effectful-input arm: emits 'let __src = shim.alloc_in_region(0, size_of_val(&name)); shim.dma_wait(0); if !__src.is_null() { copy_nonoverlapping(__src, name.as_mut_ptr() as *mut u8, size_of_val(&name)); }'. SHARED lib+bin via render_run_body. Null-guard keeps the StubShim lib zero-filled (M9 intact). Module + emit_bin docs updated.
- tests.rs + skeleton.rs tests: ex01_bin_* and render_bin_main_* now assert the load-fill + raw-stream + input-region + cursor + NUC-EX1/checksum ABSENCE.
- driver/src/main.rs: stm32h7 arm comment updated (real input load + raw stream).
- tests/renode/embedded-ex1/run.resc: injects $input into axiSram before LoadELF; takes a 3rd $input var.
- justfile renode-embedded-ex1: passes $input=@input.bin; byte-count guard + cmp -s vs reference.bin (fail loud) replacing grep-q.

### Per-AC status
- AC#1 DONE: load_input/load_input_b fill a/b with REAL injected data (cursor reads a=region[0..1024], b=region[1024..2048]); a/b non-zero at compute (a[0]=7,b[0]=169 -> c[0]=176).
- AC#2 DONE: firmware streams the raw computed output bytes over USART1 (dma_push raw-byte loop).
- AC#3 DONE: recipe cmp -s captured vs reference.bin (1024 bytes), fail-loud, replaced grep-q.
- AC#4 DONE: bite PROVEN — corrupting the GENERATED firmware's kernel (wrapping_add->wrapping_sub), recompiling, re-running made cmp fail at byte 1. Out-of-tree experiment, cleaned.

### Gate (actual)
- Host subset: build OK; clippy 0 warnings; just test ok (embedded-pattern 19 tests); test-release ok; e2e = 280/246/0/34/0 (== baseline, no regression).
- check-embedded (M9 lib intact, .#embedded) OK; check-textual-replace-on-codegen OK; check-include-str-coverage OK.
- renode-embedded-ex1 (.#renode): captured 1024 bytes, cmp -s BYTE-EXACT vs reference.bin, exit 0 — run TWICE (deterministic).

### Gotchas / subtleties (for the next slice)
- Renode load ordering: 'sysbus LoadBinary @input.bin <addr>' in the .resc executes before RunFor, so the region is populated before the CPU boots. dma_wait can be a no-op (synchronous). A real async DMA shim (parent AC#1) would NOT have this luxury.
- The cursor assumes input.bin = loads concatenated in EventList order. Exact for ex1 (a then b). Single-load examples (ex5 load_image, ex9 load_input) trivially fit. Pinned as TASK-0048.06 for the ex5/ex9 generalisation — verify per example, do not assume.
- The 'region' arg of alloc_in_region is ignored (always region 0 / the single injected region). Multi-region placement (place_data in tcm_per_core) is M10-shim/M11.
- __src is reused as a binding name across the two loads (shadowing) — fine in Rust.
- endianness: input.bin/reference.bin are LE i32; the Cortex-M7 is LE; the firmware does a raw byte copy (no swap needed) and a raw byte stream out — LE preserved end-to-end. A BE target would need a swap.
- read_volatile on the byte loads in dma_push: prevents the compiler reordering/eliding the loads across the MMIO writes in usart1_putc.

### Rejected approaches
- Adding a new trait method (e.g. fill_from_input): rejected to keep the NucleusShim surface STABLE (the parent-note item 1 says the M10 shim relies on the alloc_in_region+dma_wait load hooks). Reused alloc_in_region's existing returned-pointer contract instead.
- Streaming a framed payload (length prefix + checksum) then stripping before cmp: rejected — raw bytes are simpler and Renode's USART file backend captures them faithfully (proven in de-risk); the byte-count guard + cmp is a sufficient and honest assertion.

=== Cycle 239b review gate (orchestrator) — GO/GO + doc-nit hardening ===
Parallel read-only gate on commit f297c11:
- qa-test-runner: GO. e2e 280/246/0/34/0 TWICE (no flake); clippy 0; test 1070/0/3, test-release 1069/0/3; check-embedded OK (M9 lib intact, only known TASK-0012 aggregate-I/O warnings); renode-embedded-ex1 byte-exact vs reference.bin TWICE (1024 bytes); structural gates OK. Disk 94G free.
- mped-architect (read-only): GO, no P1/P2. INDEPENDENTLY confirmed the M9-lib-drift risk is clean (generated the lib at f297c11; StubShim::alloc_in_region null => null-guarded copy skipped => arrays stay zero-filled; check-embedded + direct thumbv7em cross-compile both pass with the new shared load lowering). INDEPENDENTLY proved AC#4 bite (corrupted input.bin byte0 7->8 out-of-tree; cmp failed at byte 1). Verified reference.bin == wrapping(a+b) for all 256 words. Doc-lie sweep clean (surviving NUC-EX1/checksum strings are negative assertions). TASK-0048.06 real + correctly scoped.
Two P3 doc nits fixed IN-THREAD this cycle (comment-doc-lie class) + re-verified (build/clippy/test green, renode recipe byte-exact again):
- lib.rs module-doc honest-limitations: reworded the zero-filled-inputs claim to the accurate mechanism (StubShim::alloc_in_region returns null => the null-guarded input-fill copy is skipped => arrays stay zero-filled), not "the input-fill hook does nothing".
- skeleton.rs Usart1Shim::alloc_in_region: added a code comment at the cursor-advance site pinning the input.bin==EventList-load-order ASSUMPTION + the TASK-0048.06 reference (was only in shim docstrings + the tracker, not at the cursor site).
P3-3 (dma_wait no-op on synchronous-injection load path) accepted by design (forward-compatible with a real async DMA shim); no action.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
M10 value-correctness LANDED for example 1 (PRD §10.3 pt 3). The generated STM32H7 firmware now computes on REAL input.bin data (Renode injects it into axiSram @0x24000000 — modelled in the .repl but absent from memory.x, so no linker collision; the Usart1Shim alloc_in_region cursor reads a then b) and streams the RAW 1024 computed output bytes over USART1; the renode-embedded-ex1 recipe cmp -s byte-exact vs reference.bin, fail-loud. Both reviews GO. All 4 ACs met + independently verified: real injected inputs (a[0]=7,b[0]=169->c[0]=176), raw-byte stream, byte-exact reference.bin diff, and the diff BITES (corrupted input -> cmp fails at byte 1, proven out-of-tree). M9 lib intact (shared load lowering null-guarded for StubShim; check-embedded passes; lib stays zero-filled). Host e2e baseline unchanged 280/246/0/34/0. Honest limits: ex1 only (ex5/ex9 = TASK-0048.03; load-order assumption per-example = TASK-0048.06); region arg ignored / single injected region (multi-region = M11); real async DMA/IRQ shim still stub (parent AC#1); Renode hardwires USART TXE=true so TX back-pressure is unvalidated.
<!-- SECTION:FINAL_SUMMARY:END -->
