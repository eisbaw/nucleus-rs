---
id: TASK-0382.02
title: >-
  Doc-citation fence: no-line-number prose symbol/test-residence checking
  (TASK-0382.01 gap 2)
status: In Progress
assignee:
  - '@mark'
created_date: '2026-05-31 21:22'
updated_date: '2026-06-01 00:55'
labels:
  - tooling
  - ci
  - doc-lie
  - robustness
  - cycle-221-followup
dependencies:
  - TASK-0382.01
parent_task_id: TASK-0382
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Gap (2) deferred from TASK-0382.01 (which landed gap (1), the partial-path PREFIX resolver). 

The cycle-221 check-doc-citation-staleness-bare fence (and its TASK-0382.01 partial-path extension) only sees citations of the form <path>.rs:N — a basename or partial-path WITH a line number. It is structurally BLIND to the BARE-BASENAME-AS-LOCATION prose variant: a claim like "pinned by bin_rejects_multi_worker_* in tests.rs" or "the splice_after_repeat cell in transfer_inject.rs" that names a file (no :N) and asserts a SYMBOL / TEST resides there. 

This is the exact lie-shape the TASK-0383 mega-file split freshly produced (a cell named in prose moved to a sibling module while the prose kept the old basename) and that was caught ONLY by manual silent-sibling grep — see commit ec50108 (TASK-0389.01 fold-back fixed a stale splice_after_repeat cell name). 

Catching it mechanically needs symbol/test-name RESIDENCE checking (does the named test/fn still live in the cited basename, crate-scoped?), NOT a line-count range check. The HARD part is zero-FP: prose ambiguity about what token is a symbol name vs an ordinary word, and which .rs basename a bare mention refers to. Design carefully (e.g. restrict to the back-tick-quoted-ident + "in <base>.rs" co-occurrence shape, crate-scope the residence grep, SKIP on any ambiguity) or keep deferred. 

NEITHER this nor gap (1) is an FP risk on the current fence — purely additive coverage. Lower priority than keeping the fence zero-FP. Out of TASK-0382.01 scope by explicit decision (genuinely harder, separate zero-FP risk profile).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== forward-carried from TASK-0391 (cycle-231, commit f800f8c) ===

Concrete zero-FP edge case for THIS fence to account for: the cross-crate bare lib.rs:N cites in nucleus/backend-common/src/check_frame.rs (currently at 146/162-163/193/208/214-215, all TASK-0052.04 pre-extraction pthreads-sync/mp-tcp-bufsync pointers) are exactly the bare-basename-as-location prose shape this task targets -- BUT their line numbers are pre-extraction-HISTORICAL (stale-by-design), so a residence/symbol checker MUST SKIP them (the existing bare fence already does, via the crate-name-within-WIN-lines prose guard). Mirror that skip.

Writing lesson too: TASK-0391 de-line-numbered the MAINTENANCE-CONTRACT comment because an enumerated source-line list inside a comment is itself the rot these fences exist to catch; prefer a grep locator over an enumerated list in any fence comment you add.

Cycle-231 (orchestrator in-thread per feedback-spawned-agents-refuse-code-edits). INVESTIGATION FINDINGS (durable):

1. The literal scope as filed (symbol/cell FILE-RESIDENCE: "`sym` in `base.rs`") is MIS-TARGETED + zero-yield + high-FP:
   - The tight prose shape (backtick-ident + backtick-`<base>.rs` co-occurrence in a comment, no :N) has ZERO instances in the current tree.
   - The bare-basename mention space is DOMINATED by ~50k codegen/test STRING LITERALS (scratch.join("kernels.rs") etc., NOT prose) -- kernels.rs alone 23880 mentions. Any loose residence matcher false-positives on these.
   - The actual motivating defect ec50108 was NOT a file-residence lie: it was a stale e2e-CELL NAME (gather_2out_loop renamed to 18-multigather/distributed) + a task038901_* glob. Validating cells needs e2e-matrix.toml, not fn-residence.

2. FEASIBLE ON-TARGET SUBSET (delivering this): the project`s task<NNNN>_<desc> unit-test naming convention is an UNAMBIGUOUS zero-FP token class (task\d{3,}, back-tick-quoted = always a test ref; never an English word nor a codegen literal; definitions fn task... are never back-ticked). Stale test-pin citation IS a documented recurrence (memory feedback-verbatim-copy-comment-doc-lie, cycle-197 one of 3 firings). NONE of the existing doc fences (narrative-doc-lie / citation-staleness / -bare / doc-links) catch back-ticked test-NAME refs in source docstrings.

PLAN: add justfile recipe check-doc-test-name-staleness; every back-ticked task<NNNN> ref in .rs must be a literal PREFIX of a defined fn task... (prefix => abbreviated cites task0306_ac3 and glob families task0299_* both resolve; a renamed/deleted test stops being a prefix => FAIL). Workspace-global (test names globally unique by convention). Wire into just ci after check-doc-citation-staleness-bare. PROTOTYPE PROVEN: RUN1 current tree OK/zero-FP (39 refs all resolve, 67 defined fns); RUN2 injected `task999999_*` FAILs with precise loc; RUN3 reverts OK.

RESIDUAL (stays open / follow-ups): general file-residence variant deferred with above rationale; stale e2e-CELL-name variant (validate vs e2e-matrix.toml) to be filed as a precise follow-up.

=== Cycle-231 DELIVERY + architect-review CORRECTION (review a89db02; commit 424c413) ===

DELIVERED (feasible zero-FP SUBSET, not the full task): check-doc-test-name-staleness justfile fence + ci wiring -- validates back-ticked task<NNNN> unit-test cites resolve to a defined fn (literal-prefix rule). qa GO; architect GO on the recipe; e2e 385/328/0/57/0 unchanged; bite-proven.

STAYS IN PROGRESS (architect NO-GO on marking Done): the task as filed asks for general symbol FILE-RESIDENCE checking; the delivered fence is a strict subset, NOT that.

CORRECTION to my cycle-231 investigation notes above (architect P2, independently verified -- disclosed per honest-failure discipline, not hidden):
1. The ~50k / kernels.rs-23880 codegen-literal figure was WRONG-for-context: it came from a grep -r scanning GITIGNORED generated trees the fence never sees. In the fence actual (rg, gitignore-respected) scan space it is ~154 double-quoted .rs literals / ~613 kernels.rs / ~2259 bare .rs mentions. The FP-surface conclusion still holds (no codegen literal starts with task\d) but the number was ~40x inflated.
2. The residence-prose shape does NOT have ZERO instances. The back-ticked-ident + back-ticked-base.rs co-occurrence has ~16 in-tree instances -- a MIX of true residence claims (relay_one defined in runtime.rs; is_whole_array_recv in wait.rs) and NON-residence co-occurrences (symbol EMITTED into a generated main.rs; CALLED from events.rs). So residence checking is neither zero-yield nor infeasible -- the genuine hard part is disambiguating defined-in vs called-from vs emitted-into for zero-FP. THAT is why it stays deferred (not emptiness, as I wrongly wrote).

REMAINING DELIVERABLE on this task: the residence-prose checker (~16-instance shape + the disambiguation challenge). NEW sibling classes filed this cycle: TASK-0393 (mod::name test-pin class ~643 refs -- the shape cycle-197 actually was) + TASK-0392 (e2e-cell-name). Hygiene follow-up: TASK-0394 (tmp/ not gitignored => all fences scan scratch).
<!-- SECTION:NOTES:END -->
