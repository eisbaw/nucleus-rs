---
id: TASK-0341.02.01.01
title: >-
  Epic S0 SPIKE: hand-wired capped-loop early-exit over 16-jacobi cumulative
  buffer (generation-k vs generation-N)
status: Done
assignee:
  - Mark Ruvald Pedersen
created_date: '2026-06-03 10:12'
updated_date: '2026-06-03 10:37'
labels:
  - grammar-extension
  - spike
  - feasibility
  - examples
dependencies: []
parent_task_id: TASK-0341.02.01
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
THROWAWAY feasibility spike, architect-mandated as the FIRST slice of the data-dependent-loop epic (design review cycle-254). Do NOT touch the grammar/parser. Hand-construct (in a test, bypassing the parser) an IR/Event with a compile-time-capped loop plus a HARD-CODED early-exit at iteration k, over the EXISTING 16-jacobi cumulative double-buffer, single-worker (pthreads-sync). Verify the gathered OUTPUT is generation-k, NOT generation-N. This answers the ONE question that can fundamentally invalidate the whole epic: does early-exit read the right buffer? The risk: 16-jacobi double-buffers via a modular index (t+ITERS)%(ITERS+1) computed from the STATIC bound; if early-exit stops at k<N the current generation sits in buffer k%2 and the final gather must read the LAST-EXECUTED generation, not generation-N. If the spike fails, all surface-syntax investment (S1/S2) is premature. Rationale (architect): substrate-first (TASK-0082 precedent) is correct only when the substrate is the boring part; here S1 plumbs toward a keystone (S4) with a genuine could-be-broken question, so prove the risky core empirically first (CLAUDE.md recurring-defect item 5: cheap empirical verification beats narrative). ALSO verify the architect assumption left unchecked: trace the shipped 16-jacobi .nuc double-buffer modulus and confirm whether it uses the same symbol as a would-be cap N (sidecar.rs cumulative classifier ~704-760; the index-shape-based classifier should be untouched by a break, but confirm).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Hand-wired capped loop + early-exit-at-k over the existing 16-jacobi cumulative buffer compiles and runs single-worker
- [ ] #2 Gathered output asserted == generation-k (the last-executed generation), NOT generation-N — bites if the final-read indexes the wrong buffer
- [ ] #3 Written finding: does early-exit read the right buffer? GO or NO-GO for the rest of the epic, with the buffer-modulus arithmetic documented
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Cycle-255 SPIKE RESULT (provisional, pre-review) ===
Question: on early-exit at k<N, does the 16-jacobi final gather read generation-k or generation-N? ANSWER: generation-N (WRONG/stale-zero).

EMPIRICAL (cruft/spike-0341-02-01-01/jacobi_earlyexit_spike.rs, replicates the EXACT generated index arithmetic, verified against the shipped pthreads-sync src/main.rs): with H=W=8, ITERS=4, early-break after generation k=2:
  - codegen reads field[ITERS=4] = ALL-ZERO (slice 4 never written on early-exit)
  - correct generation-2 read = nonzero ([39,61,69,74] at interior row 1)
  - they DIFFER -> the fixed-iteration final-read is structurally wrong under early-exit.

ARCHITECT ASSUMPTION CORRECTED: field is NOT a 2-slice k%2 double-buffer. It is a FULL history array i32[ITERS+1][H][W] (generated: vec![0; (ITERS+1)*H*W], generation t at slice stride H*W). The prev-gen index (t+ITERS)%(ITERS+1) is only used as the READ index inside the step; each generation WRITES its own slice field[t]. The final extraction (prog.algo.nuc:161, generated main.rs:22) reads the HARD-CODED constant slice field[ITERS].

DESIGN CONSEQUENCE (forward-carried to S4/S5): the convergence variant must read the RUNTIME-current generation slice field[k] where k = the break generation (the loop var t at break), NOT the compile-time field[ITERS]. Because the encoding is full-history, generation-k IS available and correct (slices 0..k all valid; only k+1..N are unwritten) — no double-buffer aliasing. So the fix is purely: make the final-extraction slice index runtime (the captured break-generation), and the lowering must thread that value to the extraction loop.
SCALING NOTE: full-history buffer is (N+1)*H*W; a large convergence cap N makes it large. A 2-slice double-buffer is the natural OPTIMIZATION (not required for correctness) — file as an S4/S5 follow-up if N is large.
VERDICT: GO for the epic — the question is answered and the issue is concrete + fixable in S4/S5.

=== Cycle-255 REVIEW GATE: qa GO + architect GO ===
qa-test-runner GO: workspace baseline UNTOUCHED (just build clean; just test 1290/0; e2e 420/363/0/57/0; tree clean — spike confined to cruft/, outside the cargo workspace, no build leakage); spike independently reproduced (exit 0, codegen field[4] all-zero vs correct field[2] nonzero); generated 16-jacobi main.rs confirms vec![0;320] full-history buffer + hard-coded field[(4)*64+...] extraction.
architect GO: finding faithful + correctly interpreted; the correction to the design-review Caveat-1 is right (full-history array INVALIDATES the 2-slice-double-buffer aliasing concern, does not merely relocate it).
P3 CORRECTION FOLDED (architect): the cross-iteration READ index (t+ITERS)%(ITERS+1) needs NO change under early-exit — at t>=1 it always evaluates to t-1 (the immediately-prior, always-written slice), independent of break point. So the S4/S5 fix is FINAL-EXTRACTION-ONLY (read runtime field[k]); do NOT touch the step read index. Also confirmed: cumulative classifier (sidecar.rs:704-762) is index-shape based, so a break/until is classifier-invariant.
P2 FINDINGS FILED/FOLDED: (a) codegen-invariant drift-pin -> NEW task S0b TASK-0341.02.01.09 (real workspace test; cruft spike does not protect against substrate drift); (b) scaling re-framed as a Petri-unroll KEYSTONE concern (folded into S4 notes), not S5 perf.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
GO. Spike answered the gating question empirically: on early-exit at k<N the 16-jacobi final gather reads generation-N (hard-coded field[ITERS]), which is the WRONG, unwritten/stale-zero slice — proven by a hand-wired single-worker reproduction of the exact generated index arithmetic (cruft/spike-0341-02-01-01). Corrected the design-review buffer model: field is a FULL (ITERS+1)-slice history array, not a 2-slice double-buffer, so generation-k is always materialized and the fix is final-extraction-only (read runtime field[k], not static field[ITERS]); the step read index needs no change. De-risks the keystone; epic proceeds S2->S1->S3->S4->S5. Follow-ups filed: S0b drift-pin (.09, S4 dep) + scaling-vs-Petri-unroll re-frame folded into S4. qa GO + architect GO.
<!-- SECTION:FINAL_SUMMARY:END -->
