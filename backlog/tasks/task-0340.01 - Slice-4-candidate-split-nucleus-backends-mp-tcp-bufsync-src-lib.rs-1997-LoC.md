---
id: TASK-0340.01
title: 'Slice 4 candidate: split nucleus/backends/mp-tcp-bufsync/src/lib.rs (1997 LoC)'
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 11:31'
updated_date: '2026-05-26 11:59'
labels:
  - tech-debt
  - hygiene
dependencies: []
parent_task_id: TASK-0340
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0340 AC#2 slice 4: split mp-tcp-bufsync/src/lib.rs (currently 1997 LoC, largest remaining mega-file after cycle-178 render.rs split). Heavy host/worker emission seams already labelled by section headers. NOTE: do NOT touch the doc-comment claims about reuse-codegen consumer status (cycle-178b folded the carried lie back; verify any moved comments don't re-introduce it).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Slice 4 implementation plan (cycle 179)

Target: nucleus/backends/mp-tcp-bufsync/src/lib.rs (1997 LoC) → multi-file module tree, zero behaviour change.

Seam map (chosen from existing '// ---' headers + cohesive groups):
- src/lib.rs (~285 LoC): module docstring (verbatim), use imports, pub use re-exports, EmitResult, emit(), SO_BUF_COMMENT_BUFSYNC, KERNELS_MOD_ATTR_FOR_SRC_BIN, write_file, mark_executable, trailing #[allow(unused_imports)] use io as _io, plus mod declarations.
- src/plan/mod.rs (~250 LoC): XferId, struct Plan<'a>, impl Plan { build, worker_name, data_name, non_host_workers, ctrl_var }.
- src/plan/worker_program.rs (~360 LoC): impl Plan { render_worker_program }.
- src/plan/events.rs (~470 LoC): impl Plan { render_events }.
- src/plan/relay.rs (~180 LoC): struct RelayHop, impl Plan { data_conn_var, relay_schedule, render_relay_phase, collect_pre_init, render_run_sh, max_payload_bytes }.
- src/encode.rs (~70 LoC): scalar_width, encode_expr, decode_expr, scalar_fn_suffix.
- src/walkers.rs (~250 LoC): collect_xfer_data, relay_phase_insertion_point, collect_w2w_pushes, collect_w2w_pushes_inner, detect_wait_before_push_hazard, collect_barriers_by_tag, collect_pre_init_sets.

Visibility rule: pub(crate) for items called from sibling sub-modules under src/ (e.g. lib.rs → plan, plan → walkers). Never widen to pub. RelayHop stays pub(crate) inside plan/relay.rs even though walkers.rs constructs it — walkers will get a pub(crate) view of RelayHop via the plan module exposing it pub(crate).

Wait — RelayHop is constructed by collect_w2w_pushes (which I'm placing in walkers.rs). Two options:
  (A) Keep RelayHop in plan/relay.rs and re-export pub(crate); walkers.rs depends on plan.
  (B) Move RelayHop to walkers.rs (or shared location).

Option A creates a circular-feeling dep (walkers → plan → walkers via collect_w2w_pushes call in build). But Rust allows it: only the TYPE is shared. Better: move collect_w2w_pushes_inner + RelayHop together. Since RelayHop is essentially the output of collect_w2w_pushes, I'll keep collect_w2w_pushes in walkers.rs and put RelayHop ALSO in walkers.rs (pub(crate)). Then plan/relay.rs imports it via super::walkers::RelayHop.

Final visibility: all moved items pub(crate) (or pub(super) where it fits within the plan/ subdir).

Constraint preservation:
- public surface: pub use EmitError, pub use NameTables, pub struct EmitResult, pub fn emit — all stay in lib.rs verbatim.
- module-level //! docstring stays verbatim in lib.rs (cycle 178b just cleaned these).
- No new TASK-NNNN / cycle-NNN citations (AC#6).
- After split, remove 'nucleus/backends/mp-tcp-bufsync/src/lib.rs' from justfile:458 allow-list.

Verification gate:
- just build, just clippy, just test, just test-release, just check-mega-files (both directions), just e2e.
- e2e baseline target: 112/102/0/10/0 (cycle 178 + 178b).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
## Slice 4 LANDED (cycle 179)

Split nucleus/backends/mp-tcp-bufsync/src/lib.rs (1997 LoC) into 7 cohesive sub-modules; zero behaviour change; e2e baseline preserved.

**Per-file LoC after split:**
- src/lib.rs                315   module docstring (verbatim from cycle 178b) + emit() + EmitResult + 2 consts + write_file/mark_executable + trailing use io as _io
- src/encode.rs              81   scalar_width / encode_expr / decode_expr / scalar_fn_suffix
- src/walkers.rs            394   RelayHop + collect_xfer_data + relay_phase_insertion_point + collect_w2w_pushes + detect_wait_before_push_hazard + collect_barriers_by_tag + collect_pre_init_sets
- src/plan/mod.rs           243   Plan struct + impl Plan { build, worker_name, data_name, non_host_workers, ctrl_var } + sub-module decls
- src/plan/worker_program.rs 388   Plan::render_worker_program (per-worker src/bin/<name>.rs body emit)
- src/plan/events.rs        491   Plan::render_events (event-walk codegen)
- src/plan/relay.rs         222   Plan::{data_conn_var, relay_schedule, render_relay_phase, collect_pre_init, render_run_sh, max_payload_bytes}
- (sum 2134; +137 vs pre-split 1997 is per-file module docstrings + use statements + sub-module declarations; no behaviour change)

**Public surface preserved verbatim:**
- pub use backend_common::EmitError; (lib.rs)
- pub use nucleus_compiler::NameTables; (lib.rs)
- pub struct EmitResult { ... } (lib.rs)
- pub fn emit(...) -> Result<EmitResult, EmitError> (lib.rs)
- 4 external consumers unchanged: nucleus/driver/src/main.rs, mp-tcp-event/lib.rs (doc-comment only), mp-tcp-poll/lib.rs (doc-comment only), and the 7 in-crate test files (all import 'use mp_tcp_bufsync::{emit, NameTables}').

**Visibility:**
- All cross-module items: pub(crate) (consistent with cycle 178 render.rs pattern).
- Module-level //! docstring stays verbatim in lib.rs (no doc-claim rewriting — cycle 178b just cleaned the doc-lies in this crate; do not re-introduce).
- All multi-file impl blocks use 'impl Plan<'_>' (clippy::needless_lifetimes — only plan/mod.rs's build needs <'a>).

**Justfile allow-list:**
Removed 'nucleus/backends/mp-tcp-bufsync/src/lib.rs' from justfile:457-468 printf list. Direction A (new mega-file outside allow-list) + direction B (stale allow-list entry) both verified OK by 'just check-mega-files'.

**Verification gate (orchestrator-self-run, inside nix develop):**
- just build (workspace): clean.
- just clippy (-D warnings --all-targets): clean.
- just test (dev): 969 / 0 / 3.
- just test-release: 968 / 0 / 3 (-1 vs dev is debug_assert-only #[should_panic] negative, expected per TASK-0291).
- just e2e: 112 / 102 / 0 / 10 / 0 — TWO independent samples, both matching cycle 178b baseline exactly (non-flake).
- just check-mega-files: OK both directions.

**Subtleties / gotchas (forward-carries for slices 5+):**

1. **Multi-file impl<'a> Plan<'a>** clippy rejected explicit lifetime in 3 of 4 sub-module impl blocks (events.rs / worker_program.rs / relay.rs). Only plan/mod.rs's build() actually names 'a in a signature (returns Self bounded by 'a). Elided to impl Plan<'_> on the other three. Slice 5 (mp-tcp-event/multi_worker.rs) likely identical pattern.

2. **RelayHop co-location.** RelayHop is constructed by collect_w2w_pushes (walker) and consumed by Plan::render_relay_phase (codegen). Placed in walkers.rs (with its producer) rather than in plan/relay.rs (with its consumer) to avoid a circular feel: relay.rs imports it pub(crate) via 'use crate::walkers::RelayHop'. Pure Rust permits either; co-located with producer chosen because the producer is the only construction site.

3. **collect_w2w_pushes_inner** kept as private fn (not pub(crate)) since it's only called from collect_w2w_pushes within the same file. No cross-module call site existed.

4. **The trailing #[allow(unused_imports)] use io as _io;** is preserved in lib.rs verbatim. It exists so the 'use std::io;' is materially referenced even though io is only used transitively via the re-exported EmitError variants (KernelsReadFailed/WriteFailed) — removing it would trigger an unused-import warning on the std::io import.

5. **NO doc-claim rewriting.** Module-level //! docstring stays byte-identical to cycle 178b (which fixed the previous doc-lies). Every moved fn/struct/doc-comment is preserved verbatim — including the long collect_w2w_pushes docstring with its cycle-148/151/163/166 narrative, the detect_wait_before_push_hazard docstring, and the relay_phase_insertion_point docstring. AC#6 verified by no new TASK-NNNN / cycle-NNN strings introduced.

6. **No new tracker tasks filed.** No latent defects discovered during the move. Slice 5 (mp-tcp-event/multi_worker.rs) is the obvious next candidate per parent task notes.

7. **Allow-list verification quirk.** I did not empirically verify that direction-B (stale-allow-list) would fail loud if I had FORGOTTEN to remove the bufsync entry. The cycle-177 implementer did verify; trusting their inductive testimony plus the recipe's positive-enumeration form. Honest-limit disclosure.

**AC status post-slice-4:**
- AC#1: DONE (cycle 176; audit list still accurate).
- AC#2: 2/6 files split (render.rs + mp-tcp-bufsync/lib.rs). Remaining: mp-tcp-event/multi_worker.rs, acfg.rs, link.rs, multi_worker_walker.rs (4 files).
- AC#3: PENDING.
- AC#4: PENDING.
- AC#5: DONE (cycle 176 + 177); allow-list shrank further this slice.
- AC#6: DONE for this slice.
- AC#7: PENDING (final slice when AC#2 + AC#3 close).
<!-- SECTION:FINAL_SUMMARY:END -->
