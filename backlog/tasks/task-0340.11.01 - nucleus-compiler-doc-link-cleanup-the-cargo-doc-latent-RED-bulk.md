---
id: TASK-0340.11.01
title: nucleus-compiler doc-link cleanup (the cargo-doc latent-RED bulk)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-30 02:55'
updated_date: '2026-05-30 04:45'
labels: []
dependencies: []
parent_task_id: TASK-0340.11
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Out-of-scope sibling of the backend-common slice (TASK-0340.11, backend-common arm DONE @cycle landing this note). The whole-workspace `cargo doc --no-deps --workspace` cascade-aborts first on `could not document nucleus-compiler`: the bulk of the ~50 broken/private intra-doc-links live in the nucleus-compiler crate. Fix them so `RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc --no-deps -p nucleus-compiler` builds clean (0 warnings). Sanctioned patterns (proven on the backend-common slice): proper intra-doc-link when the target is reachable (incl. cross-crate `crate_name::path::Item` and enum-variant `crate::Enum::Variant` forms); backtick code span for unreachable targets (struct FIELDS, PRIVATE items, cross-crate items not importable, Cargo-TOML syntax like `[[bin]]`, and `<T>`/`<lv>`-style angle-bracket prose that rustdoc reads as HTML tags). private_intra_doc_links is a DISTINCT lint from broken_intra_doc_links. ONCE BOTH this task AND the backend-common slice are clean: ADD a `cargo doc --no-deps --workspace` arm (under `-D rustdoc::broken_intra_doc_links`) to `just ci` — this is the deferred final step that closes the silent-breakage hole the `feedback-visibility-tighten-doclink-trap` memory documents. Do NOT add the gate arm while nucleus-compiler is still RED.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== CORRECTION (orchestrator, cycle of commit cbbe0c9) — SUPERSEDES the cross-crate-link guidance in this task's Description ===

RETRACT the Description sentence that lists "cross-crate `crate_name::path::Item`" among the sanctioned PROPER-LINK forms. That is WRONG and was a propagated comment-doc-lie from the backend-common slice (the FireBinding "resolved" claim, falsified by architect review + orchestrator href-grep).

CORRECT RULE for this nucleus-compiler arm:
- A cross-crate intra-doc-link to a DEPENDENCY (e.g. nucleus_compiler -> some other crate) does NOT resolve under `cargo doc --no-deps`. rustdoc renders it as a literal-path href (href="othercrate::path::Item") that 404s, and emits NO warning — broken_intra_doc_links is structurally BLIND to it. So the `-D broken_intra_doc_links` oracle alone will PASS a dead cross-crate link.
- Therefore: for any target NOT in the crate being documented, use a BACKTICK code span, NOT a [link](other_crate::path). Proper links are only honest for SAME-crate targets (crate::path::Item, crate::Enum::Variant) and items reachable in the doc invocation.
- nucleus-compiler is the ROOT crate here, so most of its own types ARE same-crate (crate::...) and CAN be properly linked; only references OUT to its dependencies need backtick.

VERIFICATION the oracle alone does NOT give you (architect P2): after the per-crate doc build, grep the generated HTML for dead hrefs:
  grep -rho 'href="[a-z_]*::[^"]*"' nucleus/target/doc/nucleus_compiler/   # MUST be empty
Any href containing `::` is a guaranteed-dead cross-crate link. Run this under BOTH default and --document-private-items. When the deferred `cargo doc --no-deps --workspace` arm is finally ADDED to just ci, its DoD MUST include this dead-href grep assertion (the lint cannot catch this class).

ALSO (architect P3, for the eventual --workspace gate's lint-set choice): backend-common still carries 2 PRE-EXISTING `unescaped backtick` warnings that surface only under `-W rustdoc::all` (NOT under the two gated lints): check_frame.rs:177 and event_plan/transport.rs:84. Decide deliberately whether the --workspace gate includes rustdoc::all or just the two intra-doc-link lints; if rustdoc::all, those 2 need fixing first.

Feed-forward from sibling TASK-0340.11.03 (small-crates batch, all 5 done): RATIO INSIGHT for the nucleus-compiler bulk. The 5 leaf crates were 100% backtick fallback (16/16) — they are leaves whose doc references point OUT to nucleus-compiler/backend_common = cross-crate = backtick. nucleus-compiler is the ROOT crate, so the INVERSE holds: most of ITS own types are SAME-crate (crate::Foo / crate::module::Bar / crate::Enum::Variant) and CAN be proper [link](crate::path) that resolves + navigates. Only nucleus-compiler doc references OUT to a dependency (rare: e.g. external crate items) need backtick. So for the 43-link bulk: prefer proper crate:: links by default, reach for backtick only for (a) struct FIELDS (rustdoc cannot link fields), (b) genuinely private items (private_intra_doc_links), (c) cross-crate-OUT refs, (d) angle-bracket prose <T>/Vec<T> read as HTML tags, (e) [[skip]]-style test-status literals / Cargo [[bin]] TOML. CRITICAL trap still applies even in the root crate: NEVER write [`X`](other_crate::path) to "fix" a cross-crate link — it renders a literal-path href that 404s under --no-deps and the -D broken_intra_doc_links lint is BLIND to it. Always run BOTH dead-href greps (default + --document-private-items) after the oracle goes 0-warnings.

=== METHOD CORRECTION from TASK-0340.11.03 review (forward-carried to this nucleus-compiler bulk) ===

The dead-href grep `grep -rho 'href="[a-z_]*::[^"]*"' target/doc/<crate>/` is ONLY meaningful when rustdoc EXIT=0. An empty grep on a FAILED (exit-101) rustdoc build is VACUOUS — no HTML was emitted, so there is nothing to grep — NOT a clean result. In .03 both the implementer AND the qa-reviewer reported the 5 crates "clean under --document-private-items" when 2 of them (embedded-pattern, pthreads-sync) were actually exit-101 (private-module broken links) → vacuously-empty grep. The architect caught it. So the verification protocol is: (1) run the oracle, ASSERT exit==0 FIRST; (2) only THEN trust the dead-href grep. For nucleus-compiler: assert exit 0 under default AND --document-private-items before grepping; treat any non-zero exit as RED regardless of grep output.

STATUS UPDATE: all 5 .03 leaf crates (embedded-pattern, test-common, pthreads-sync, openmp-rs, mp-tcp-poll) are now clean under BOTH default AND --document-private-items (fold-back 3e31a8e). backend-common is clean (5e7143c/cbbe0c9). Remaining for the gate arm: THIS (nucleus-compiler, 43) + nucleus-e2e exit-101 triage (TASK-0340.11.02). The just-ci cargo-doc arm lands only when ALL crates are exit-0 (NOT just grep-empty). Decide whether the arm uses --document-private-items: if NOT (default --workspace), private-item links don't block it; if so, every crate must be --document-private-items exit-0 too.

IMPLEMENTED (orchestrator-direct, phase3 cycle). nucleus-compiler doc-link bulk cleared: ~47 doc diagnostics across 26 files fixed. Oracle now EXIT 0 / ZERO warnings under BOTH default AND --document-private-items; dead-href grep empty (non-vacuous, exit-0-asserted-first) in both modes.

Fix taxonomy (proper crate:: link vs backtick fallback):
- PROPER same-crate links: DataId/WorkerId/IterVar/KernelId -> crate::event::*; IrExpr/IndexedRef -> crate::algo::ir::*; AlgoAst/KernelDecl -> crate::algo::ast::*; SchedAst -> crate::sched::ast::*; LinkedIR -> crate::LinkedIR; ParseError -> crate::error::ParseError; build_acfg -> crate::acfg::build_acfg; check_deadlock_free -> crate::passes::deadlock::check_deadlock_free; acfg_to_events(fn) -> crate::passes::petri_to_events::acfg_to_events; Event::Loop -> crate::event::Event::Loop; HaloInferenceError variants qualified Enum::Variant; SchedLowerError::DuplicateWorkerClass CORRECTED to SchedLowerErrorKind::DuplicateWorkerClass (variant lives on the Kind enum).
- struct-FIELD links via Self::field: loop_bounds -> [loop_bounds](Self::loop_bounds) (matches sidecar.rs:130 precedent; rustdoc CAN link fields via Self::field, contra the backend-common NameTables::worker note).
- BACKTICK fallback (unlinkable): cross-crate-OUT (backend_common::multi_worker_walker[::*], wire::barrier_cross); private/pub(crate) fns (lower_stmt, push_wait_pair_covers, walk, find_outer_of_2d, contains_repeat, collect_op_workers, compute_partition_bands, classify_index, iv_is_partitioned, collect_iter_var_refs, eval_const_int, expr_mentions, map_one_chumsky_error, chumsky_message, brace_balanced_recovery, tiles_may_overlap, map_band_error, *_from_nonempty, Accum::*, is_failed*); variant-FIELD Repeat::range; TransferPolicy::buffer (struct field); angle-bracket prose <T>/<String> read as HTML; Cargo/test literal [[skip]].
- AMBIGUOUS: crate::link is both fn+mod -> disambiguated to the fn via [crate::link](crate::link()).
- REDUNDANT explicit links removed (3): Spanned/Event/SyncTag where bare label already resolves.

GOTCHA (forward-carry to TASK-0340.11.02 + the eventual gate-arm decision): the default `cargo doc --no-deps -p <crate>` oracle UNDERCOUNTS -- it does NOT surface broken links that live inside PRIVATE items doc-comments. Only --document-private-items does. This bulk needed a SECOND pass: after the default oracle went 0-warnings, --document-private-items surfaced 4 MORE private-only sites (algo/lower.rs:264 + sched/lower.rs:680 [failed_decls], partition_rows.rs:404 map_band_error, transfer_inject.rs:3584 <String> HTML). So the per-crate inventory must be run under BOTH modes; treat the default-only count as a floor. The .03 vacuous-grep method-correction still applies: assert EXIT==0 before trusting the dead-href grep.

GATE (codegen-inert, doc-comment-only diff -- verified: only changed non-doc line in whole diff is the task-md status flip): build clean; clippy -D warnings clean; test 1122/0/3; test-release 1121/0/3; e2e 308/246/0/62/0 (byte-identical to backend-common-slice baseline). NOT marking Done until orchestrator review gate (qa-test-runner + mped-architect) returns GO.

REMAINING for the just-ci cargo-doc --workspace arm: only TASK-0340.11.02 (nucleus-e2e exit-101 triage). backend-common + 5 small crates + nucleus-compiler all now exit-0 both-mode.

REVIEW-GATE CLOSE (both GO) — phase3 cycle, impl commit f7bc624.

qa-test-runner GO (numbers INDEPENDENTLY re-run, not transcribed from implementer): doc oracle EXIT 0 / 0 warn under BOTH default AND --document-private-items (PRIVATE build force-cleaned: rm -rf target/doc/nucleus_compiler, 239 HTML files confirm real tree, not cache); dead-href grep empty + NON-VACUOUS (exit-0 asserted first) both modes; build clean; clippy -D warnings clean incl. ZERO doc_lazy_continuation (force re-lint of all 12 crates); test 1122/0/3; test-release 1121/0/3; e2e 308/246/0/62/0 IDENTICAL across 2 runs; codegen-inert verified independently (every changed .rs line is ///|//!). Honest caveat: per-cell emit bytes not diffed (harness deletes target/e2e-matrix on success); identical totals + pure-doc diff make a per-cell regression implausible but it is inference not byte-proof.

mped-architect GO: P1 dead-cross-crate-href mode (the exact defect the backend-common sibling shipped) EMPIRICALLY ABSENT — whole-tree grep ](backend_common|mio|serde|nucleus_|wire):: = empty; bare [dep::*] = empty; only ::-bearing rendered hrefs are legit https://docs.rs/serde external links. All 14 NEW proper-links start crate::/Self:: and render real relative hrefs. Both content claims verified TRUE: (a) DuplicateWorkerClass IS a variant of SchedLowerErrorKind not SchedLowerError; (b) [loop_bounds](Self::loop_bounds) RESOLVES to struct.NameSidecar.html#structfield.loop_bounds — Self::field intra-doc links work since Rust 1.48, which CORRECTS the stale backend-common note claiming fields are unlinkable. No 5th private-only broken site (private build exit 0 + empty grep).

P3 (cosmetic, non-blocking, NOT actioned): (1) architect flagged halo_inference.rs:502 classify_index backtick as unneeded — CORRECTION: 502 is a PUBLIC fn doc linking a PRIVATE fn, so the link form genuinely triggered -W private_intra_doc_links; the backtick was REQUIRED, not optional (the other classify_index sites are in private-fn docs, no public->private warning). Architect narrative slightly off here; my code correct (recurring reviewer-narrative-can-be-wrong). (2) pre-existing link/backtick style mix at untouched in-crate sites — out of scope.

ALL ACs (implicit: oracle 0-warnings both modes + gate-inert) MET. Task DONE.
<!-- SECTION:NOTES:END -->
