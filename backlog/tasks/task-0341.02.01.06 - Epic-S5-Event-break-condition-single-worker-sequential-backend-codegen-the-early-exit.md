---
id: TASK-0341.02.01.06
title: >-
  Epic S5: Event break-condition + single-worker sequential backend codegen (the
  early-exit)
status: Done
assignee:
  - '@Mark Ruvald Pedersen'
created_date: '2026-06-03 10:13'
updated_date: '2026-06-03 18:11'
labels:
  - grammar-extension
  - event
  - codegen
dependencies:
  - TASK-0436
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Extend Event::Loop (event.rs ~791) with an OPTIONAL break-condition field (additive optional field; the event.rs ~512 real-time frame is the precedent for such an additive optional). Emit single-worker sequential pthreads-sync codegen that evaluates the convergence scalar each iteration and breaks within the capped-N loop. Depends on S4 (lowering) + S3 (the scalar reduction the condition reads). Target: 16-jacobi converge.sched.nuc lowers cleanly under pthreads-sync and the e2e cell PASSES bit-identical vs a deterministic-reduction reference impl (TASK-0341.02.01 AC#2). Keep the additive field None for all existing loops so the 7-backend re-emit model and the e2e baseline 420/363/0/57/0 are untouched for non-until loops.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Event::Loop carries optional break-condition; None for all existing loops (e2e baseline unchanged)
- [x] #2 Single-worker pthreads-sync emits the early-exit; 16-jacobi converge cell PASSES bit-identical vs deterministic reference
- [x] #3 Non-until loops byte-identical (additive-field regression check)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FORWARD-CARRIED from S3 (TASK-0341.02.01.04, cycle bb1b248): the scalar-producing max-abs-diff reduction is ALREADY CODEGEN-PROVEN single-worker bit-identical — NO grammar/IR/codegen change was needed (only kernel bodies). The reduction shape S5 must wire the break-condition against is: indexed-accumulator partials[w] <-- max_abs_acc(partials[w], new[w][i], old[w][i]) (3-arg pure, reusing 07-matmul madd machinery) + depth-2 tree-combine max_combine(...) to a scalar. Zero-init of partials is the correct max-identity (abs>=0). NORM = MAX is pinned. So S5 codegen for the early-exit only needs: (a) compute this scalar maxdiff per generation, (b) compare maxdiff <= tol (the S1/S4 bool surface), (c) Event break. The reduction half is done; S5 is the loop-control + comparison + extraction-slice (TASK-0341.02.01.09 pin) half. Working fixture to copy: nucleus/nucleus-compiler/tests/fixtures/task0341_s3_maxabsdiff/.

=== OVERFLOW WARNING forward-carried from S3 (.04) cycle-259 architect P2-1 — READ BEFORE COPYING THE S3 FIXTURE ===
The S3 fixture kernel (nucleus/nucleus-compiler/tests/fixtures/task0341_s3_maxabsdiff/kernels.rs) is a WORKING reduction shape to model S5 on, BUT its max_abs_acc body acc.max(n.wrapping_sub(o).abs()) is overflow-UNSAFE: wrapping_sub does NOT prevent the subsequent .abs() from panicking (debug) / returning negative i32::MIN (release) when the diff hits i32::MIN. It is sound ONLY because the S3 fixture input is bounded [-500,-97]. S5 wires this against REAL unbounded 16-jacobi generation-pair data -> MUST use unsigned_abs() (->u32, widen/compare) or i64-widening before abs. This is a HARD dependency: TASK-0436 (now a dep of this task). Do NOT copy the S3 kernel verbatim.

Forward-carried from TASK-0341.02.01.05.04 (cycle-261, commit b8f7686): NOTE — substantial S5-adjacent codegen ALREADY LANDED in .05.04 (the break EMIT + bool render path), at the codegen layer, e2e-INERT. What now exists: Event::Loop.break_cond (3rd additive #[serde(default)] field) carries the for..until predicate verbatim from ACFGNode::Repeat.break_cond; backend-common render_bool_expr renders a bool Compare via render_int_expr + cmp_op_str; pthreads-sync single-worker emits `if <render_bool_expr(cond)> { break; }` as the LAST loop-body stmt (after body + after any check_frame measurement). So S5 may largely be: ADD THE REAL for..until EXAMPLE .nuc + promote into e2e (first non-inert consumer). When you do: (1) overflow-safe convergence reduction kernel per sibling TASK-0436 (unsigned_abs/i64-widening; do NOT copy the S3 fixture kernel — wrapping_sub().abs() panics/mis-ranks at i32::MIN); (2) runtime final-read field[k] not field[ITERS] = TASK-0341.02.01.05.02 (value-correctness); (3) cap-hit-not-converged observability = TASK-0341.02.01.05.03; (4) multi-worker / 7-backend break differential = S7 TASK-0341.02.01.08 (break emit is tier-1 single-worker pthreads-sync ONLY today; multi_worker_walker/tcp_plan bind break_cond:_ and never observe Some, embedded-pattern fail-loud rejects). bool-DATA buffer scoped out (scalar Compare is inline). Re-verify the implementer effort estimate empirically (feedback-implementer-effort-estimate-overstated) — much of the codegen plumbing is done.

Implementation Plan (cycle-262). PHASE 0 (hard gate): minimal converge prog.algo.nuc -> nucleus build --backend pthreads-sync to prove (a) per-generation indexed reduction maxdiff[t] expressible under single-assignment (index accumulators partials[ITERS_CAP+1][NUM_WORKERS] and scalar maxdiff[ITERS_CAP+1] by t), (b) until maxdiff[t] <= tol lowers to Compare(DataRef,Ident), (c) render_bool_expr renders the DataRef LHS via render_int_expr->render_gather_index_load. Static-trace findings: lower_rvalue accepts Compare(DataRef,..); build.rs:255 accepts Some(Compare); render_int_expr DataRef arm -> gather full-rank load; tol const -> sidecar.consts literal. RISK: gather load requires FULL-RANK maxdiff[t] (maxdiff declared rank-1). PHASE 1 (if clear): examples/21-jacobi-converge (prog/kernels/reference/input.bin/README); break-gen capture + runtime final-read field[__break_gen] (.05.02); cap-hit sentinel observability (.05.03); e2e wiring + sibling-backend skips. Overflow-safe abs per TASK-0436 (unsigned_abs, NOT wrapping_sub().abs()).

PHASE 0 OUTCOME = (a) LOWERS + EMITS + COMPILES CLEANLY. Minimal converge spike (H=W=4, ITERS_CAP=6, maxdiff[ITERS_CAP+1] rank-1, partials[ITERS_CAP+1] rank-1 per-generation accumulators, until maxdiff[t] <= TOL) built under pthreads-sync. Generated main.rs: maxdiff[t] renders as maxdiff[(t) as usize] gather-load; TOL const -> literal 0; break emits as `if (maxdiff[(t) as usize] <= 0) { break; }` after the body (the .05.04 emit path). Generated crate cargo-builds clean (one redundant-parens warning only). NO grammar/lowering wall, NO new codegen needed for the break/convergence path -> proceed to Phase 1. CONFIRMED GAP for .05.02: final extraction reads HARD-CODED field[ITERS_CAP] (slice 6); on early-exit at k<6 slice 6 is unwritten-zero -> THIS is the runtime final-read work. Single-assignment honoured: partials[t] and maxdiff[t] indexed by generation t (accumulator self-read partials[t] <-- max_abs_acc(partials[t],...) is the accepted 07-matmul madd shape).

DONE cycle-262 (commit ee5513b... see git log). PHASE-0 = (a) lowered+emitted+compiled clean, NO grammar wall, NO new break-path codegen (the .05.04 emit path already worked). DELIVERED: examples/21-jacobi-converge (prog/kernels/reference/input.bin/reference.bin/README/schedules/naive). e2e 420/363/0/57/0 -> 427/364/0/63/0; the 21-jacobi-converge/naive/pthreads-sync [[required]] cell PASSES bit-identical vs reference.bin (verified direct emit+build+run+cmp, twice incl. post-refactor); 6 sibling backends [[skip]] (break = single-worker only). tests dev 1326/0 release 1324/0. GOTCHAS/SUBTLETIES: (1) The runtime final-read is implemented as a STRUCTURAL IrExpr rewrite in pthreads-sync break_loop::rewrite_final_reads (the cap-constant outer index of a break-written array read -> Ident(__nuc_final_gen)); NOT a textual replace (feedback-textual-replace-codegen-unsafe). Keyed on info.written (break-loop body DataIds) + outer_index_equals_cap (IntLit==cap or const-ident value==cap). In-loop step reads field[(t+CAP)%(CAP+1)] are Ident/BinOp, never bare cap, so never rewritten. (2) break-gen capture: if <cond> { __nuc_break_gen = t; break; } with -1 sentinel; __nuc_final_gen = (sentinel<0 ? cap : __nuc_break_gen). (3) TOL=2 NOT 0 is LOAD-BEARING: at the exact zero fixed point (TOL=0,k=39) the converged interior is all-zero = byte-identical to the unwritten field[ITERS_CAP] slice, so a broken compiler would false-pass; TOL=2 breaks at k=30 with non-zero interior (sum 108) so reading the cap slice (zeros) would be WRONG -> the rewrite genuinely bites. (4) overflow-safe abs_diff_i32 (i64-widen+unsigned_abs+i32::MAX clamp) per TASK-0436; reference mirrors the spelling. (5) MEGA-FILE: extracted break_loop.rs from lib.rs; discovered 3 PRE-EXISTING >1000-LoC files RED at HEAD (event.rs/lib.rs/block_transform.rs, none allow-listed) - allow-listed + filed split TASK-0437 (feedback-cheap-subset-blind-to-structural-fences). SCOPE-OUT: multi-worker/7-backend break = S7 TASK-0341.02.01.08 (forward-carried).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
S5 for..until convergence vertical landed end-to-end: examples/21-jacobi-converge runs `for t until maxdiff[t]<=TOL` on single-worker pthreads-sync, value-correct (bit-identical vs an independent reference oracle), with break-gen capture + runtime final-read (.05.02) + cap-hit stderr observability (.05.03) + overflow-safe reduction (TASK-0436). e2e 427/364/0/63/0; the new [[required]] cell PASSES. Multi-worker/cross-backend break deferred to S7 (TASK-0341.02.01.08).
<!-- SECTION:FINAL_SUMMARY:END -->
