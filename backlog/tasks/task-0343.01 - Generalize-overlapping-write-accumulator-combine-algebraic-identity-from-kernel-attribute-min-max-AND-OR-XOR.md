---
id: TASK-0343.01
title: >-
  Generalize overlapping-write accumulator combine: algebraic identity from
  kernel attribute (min/max/AND/OR/XOR)
status: Done
assignee: []
created_date: '2026-05-26 23:37'
updated_date: '2026-06-05 09:50'
labels:
  - compiler
  - M6
  - follow-up
  - accumulator
dependencies: []
parent_task_id: TASK-0343
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0343 cycle 189 hardcoded sum (wrapping_add) as the overlapping-write fan-in combine identity. This works for 08-histogram's i32 accumulator. The general case (PRD §10.1 bit-determinism + future schedules with min/max accumulators) needs the kernel to declare its algebraic identity. Concretely: add an attribute like 'kernel bin_inc : ... pure combine=sum' (or infer from the body when possible) and consume in render_accumulate_assign instead of hardcoding wrapping_add.

Scope:
- Grammar: combine=<name> attribute on kernel decl.
- Algorithm-IR sidecar: per-kernel combine identity (sum/min/max/and/or/xor).
- backend-common::render_accumulate_assign: map identity to Rust op (wrapping_add / min / max / & / | / ^).
- Defensive: reject a fan-in classified as accumulate whose owning kernel has no combine attribute declared (today the sum-only carve-out forgives this — the user gets bit-correct sum because every shipped accumulator IS sum).

Cross-references:
- TASK-0343 (parent): the cycle-189 sum-only landing.
- backend-common/src/multi_worker_walker/wait.rs::accumulate_op_for_scalar (the hardcoded match arm).
- PRD §10.1 (bit-determinism invariant — informs which identities are safe per scalar type).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
PROGRESS (cycle landing TASK-0343.01.01, GO): the zero-identity subset of this generalization is DONE — combine=<sum|or|xor> kernel attribute threaded grammar->sidecar->codegen, strict accumulate-fan-in reject (no silent sum assumption), demonstrated by new 25-bin-parity distributed XOR-combine bit-identical across 7 tier-1 backends (e2e 441->455). REMAINING scope of this parent = the non-zero combine identities min/max/and (identity-aware accumulator init across the ~4 duplicated init sites), tracked as child TASK-0343.01.02 (depends on .01.01). This parent stays To Do/open until .01.02 lands.

DONE: the combine-identity generalization is COMPLETE across all six ops named in the title. TASK-0343.01.01 landed sum/or/xor (zero identity) + the combine= attribute pipeline + strict reject + 25-bin-parity XOR example. TASK-0343.01.02 landed min/max/and (non-zero identity) + single-shared identity-aware init helper (render_array_init_for_combine) across all 8 init sites + 26-bin-min MIN example, bit-identical 7 backends, full just ci GREEN, e2e 469/406/0/63/0. Both via independent orchestrator review gates (GO/GO). Remaining accumulator follow-ups are SEPARATE scope: float/bool combine (TASK-0343.02), max/and e2e cells (TASK-0343.01.03, low), transitive-accumulator detection (TASK-0343.03.02).
<!-- SECTION:NOTES:END -->
