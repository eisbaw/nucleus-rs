---
id: TASK-0341.03.03
title: 'Example 17-spmv AC#4: cross-backend differential promotion for naive cell'
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-27 16:54'
updated_date: '2026-05-27 17:40'
labels:
  - examples
  - cross-backend
  - M6-follow-up
dependencies: []
parent_task_id: TASK-0341.03
priority: medium
---

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Cycle 211 plan (orchestrator-direct, per memory feedback-spawned-agents-refuse-code-edits + cycle-205/207 precedent on sibling cross-backend promotion sub-tasks).

SCOPE: AC#1 + AC#2 + AC#3 (all three ACs of this leaf task).

PREDICTED OUTCOME: clean promotion. Single-host naive schedule
goes through the shared single-worker renderer (pthreads_sync::
render_single_worker_main / _with_kernels_attr per the variant
split in cycle-205/207's narrative blocks); cross-backend
RUNTIME-OUTPUT bit-identical by construction. The only source of
failure would be a cross-backend scaffolding bug, which the
2-sample cycle-210 evidence already rules out.

PLAN:

1. Establish PRE-CYCLE e2e baseline by running `just e2e` once
   (capture totals + the 6 informational 17-spmv rows).

2. Run TWO MORE fresh e2e samples (samples #2 and #3 of the
   AC#2-required 3-sample non-flake check). Per memory
   project-cross-backend-differential cycle-205/207 precedent,
   3 fresh samples taken AT PROMOTION TIME suffice; the cycle-210
   2 samples are NOT counted toward AC#2's "3 samples" minimum.

3. Edit nuc-nucleus/e2e-matrix.toml to:
   - Promote 6 entries from informational to [[required]] for
     17-spmv | naive x {mp-tcp-bufsync, pthreads-async,
     mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}.
   - Update the narrative comment block above the existing
     [[required]] pthreads-sync entry to cite cycle 211 +
     reference the cycle-205/207 precedent + record the variant
     split (no-attr vs _with_kernels_attr).
   - DO NOT alter the existing pthreads-sync [[required]] entry
     (it stays from cycle 210).
   - Stay verbatim where the wording is the same as cycle-205/207
     (this avoids inventing claim drift).

4. Run `just e2e` a FOURTH time (post-promotion verification +
   AC#2-required 3rd sample). The total pass count should be
   UNCHANGED (266) but pass count should not change (still 232
   if all 6 promoted cells pass; pass count = total - skipped -
   fail). What WILL change: the 6 promoted rows now have `*`
   markers (required cells). The total remains 266.

5. Run the cheap gate before commit:
   nix develop --command bash -c "just build && just clippy
   && just check-textual-replace-on-codegen
   && just check-include-str-coverage
   && just check-narrative-doc-lie
   && just check-mega-files
   && just test && just test-release"
   (these all should be unchanged from cycle 210 since this is
   a pure-tracker / pure-e2e-matrix edit).

6. Tick TASK-0341.03.03 ACs #1, #2, #3 + mark Done.
   Parent TASK-0341.03 AC#4 was already ticked cycle 210 with
   the cycle-205-precedent-fold-out pattern (the AC text says
   "all 4 tier-1 backends"; cycle 211 closes the broader
   7-backend promotion sub-task, exceeding the AC minimum).

7. Commit: tracker + e2e-matrix.toml. Commit-message records
   pre-cycle 266/232/0/34/0 -> post-cycle 266/232/0/34/0 with
   the required-fail-count parity check.

8. Read-only review gate (qa-test-runner + mped-architect) in
   parallel, before considering the cycle closed.

VERIFICATION SUMMARY:
- Pre-cycle: 266/232/0/34/0 (2 cycle-210 samples + 1 cycle-211 sample).
- Mid-cycle: 266/232/0/34/0 (3 cycle-211 samples = AC#2 minimum).
- Post-promotion: 266/232/0/34/0 (4th sample after the matrix edit).
  Required-fail count MUST stay 0; total + pass MUST stay 266/232.

FORWARD-CARRY LESSONS from cycle-205b + 207b (architect P2 review-gate
fold-back):
- AC#4 of parent (TASK-0341.03) was filed cycle 178 saying "all 4
  tier-1 backends" — by cycle 211 there are 7 tier-1 backends with
  substantive multi-worker codegen. The wider 7-backend promotion
  EXCEEDS the AC minimum; the AC text wording-vs-reality delta is
  the same shape as cycle-205b/207b. Do NOT rewrite the parent AC
  in place (memory feedback-ac-rewrite-on-done-task); a clarification
  addendum may be added if cycle-211 architect review surfaces this
  as a finding.
- Verbatim-copy doc-lie risk (memory feedback-verbatim-copy-comment-doc-lie):
  the cycle-211 narrative block in e2e-matrix.toml copies from the
  cycle-207 16-jacobi block. Verify each cited fact applies to
  17-spmv (e.g. "ITERS=4 fixed" is jacobi-specific, must NOT appear).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Filed as TASK-0341.03 AC#4 fold-out, cycle 210 ===

17-spmv AC#1 (language-sanity, pthreads-sync naive) landed cycle 210. 6 other tier-1 backends (mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event) PASSed informationally in the cycle-210 e2e run (266/232/0/34/0 × 2 samples, non-flake). AC#4 promotes those cells to [[required]] for the cross-backend differential matrix.

## Acceptance criteria

1. **6 cells promoted**: e2e-matrix.toml gains 6 new [[required]] entries for 17-spmv | naive × {mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}, milestone=M6.
2. **Non-flake re-verification**: at least 3 fresh e2e samples taken AT PROMOTION TIME (not relying on cycle-210's 2 samples) confirm 100% PASS rate across the 6 promoted cells. Sample count matches cycle-205/207 precedent.
3. **Pre-cycle baseline + post-cycle update**: commit message records before/after totals so the promotion is auditable.

## Predicted outcome

Single-`host` naive schedule => shared single-worker renderer => RUNTIME-OUTPUT bit-identical across all 7 backends by construction. The only source of failure would be a cross-backend scaffolding bug (Cargo.toml / run.sh / project_skeleton family), which the 2-sample cycle-210 evidence already rules out.

Same precedent as TASK-0341.01.02 (cycle 205) and TASK-0341.02.03 (cycle 207).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 211 closure. 17-spmv | naive promoted to [[required]] across the 6 remaining tier-1 backends (mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event), milestone=M6. All 3 ACs ticked.

EMPIRICAL VERIFICATION (cycle 211):
- 4 fresh `just e2e` samples (samples #1-#3 pre-promotion, sample #4 post-promotion) all 266/232/0/34/0; non-flake.
- Sample #4 confirms the 6 newly-promoted rows render with the required-cell marker (`*`) and contribute 0 required-failures (required-fail count stays 0).
- 2 cycle-210 samples + 4 cycle-211 samples = 6 total samples with 100% PASS on the 17-spmv x 7 row.
- Cheap gate (`just build && just clippy && just check-textual-replace-on-codegen && just check-include-str-coverage && just check-narrative-doc-lie && just check-mega-files`): all green.
- `just test` + `just test-release` unchanged from cycle 210 (no code edits in cycle 211; pure tracker + e2e-matrix.toml edit).

DELIVERABLES (cycle 211):
- nuc-nucleus/e2e-matrix.toml: 6 new [[required]] entries for 17-spmv | naive x {mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}; narrative comment block above the existing pthreads-sync entry updated to cite cycle 211 + record the variant split (`render_single_worker_main` vs `_with_kernels_attr`).
- Tracker: this task ticked Done.

VARIANT SPLIT CROSS-CHECKED (per memory feedback-verbatim-copy-comment-doc-lie):
The narrative block copies the cycle-207 (16-jacobi) shape. Each cited fact verified against the actual call sites in nucleus/backends/*/src/lib.rs via grep:
- no-attr `render_single_worker_main`: pthreads-async (lib.rs:127, :313), openmp-rs (lib.rs:143, :250), mp-tcp-event (lib.rs:170, :332). 3 backends.
- `_with_kernels_attr`: mp-tcp-bufsync (lib.rs:103, :189), mp-tcp-poll (lib.rs:90, :180), mp-uds-event (lib.rs:126, :321). 3 backends.
- pthreads-sync owns both entry points (lib.rs:931 no-attr; :945 `_with_kernels_attr`; the no-attr variant delegates to `_with_kernels_attr("")` at :936).
All 3 facts in the narrative block are accurate as of cycle 211.

FORWARD-CARRY LESSONS (cycle 211):
- Cross-backend promotion sub-tasks (this, cycle-205 TASK-0341.01.02, cycle-207 TASK-0341.02.03) are mechanically identical: 3 fresh AC#2-required samples + matrix edit + 4th sample for post-promotion verification + commit. The pattern is now well-established across 3 examples (15, 16, 17).
- Parent AC text "all 4 tier-1 backends" is stale wording from cycle 178 — the actual achieved promotion is 7 backends. Same shape as cycle-205b/207b architect P2 findings; record any review-time AC drift as a clarification addendum (memory feedback-ac-rewrite-on-done-task) rather than in-place rewrite.
- Verbatim-copy doc-lie risk (memory feedback-verbatim-copy-comment-doc-lie): the narrative block copies cycle-207's wording but the example-specific facts (M=N=8, NNZ=3, the IndexExpr.Atom grammar gap, the masked-accumulator workaround) differ — preserved in the cycle-211 version.

NO FOLLOW-UPS FILED. TASK-0341.03 (parent) has AC#3 still open via TASK-0341.03.02; cycle-211 leaves it untouched.

=== Cycle 211b clarification addendum (architect review-gate fold-back P1.1) ===

Cycle-211 architect read-only review surfaced P1.1: the e2e-matrix.toml narrative block (lines 580-582 at HEAD 21adf2b) said "Non-flake verified across 5 e2e samples (cycles 210 + 211)" — but the commit message + this final summary say "6 total samples" (2 in cycle 210 + 4 in cycle 211). Off-by-one arithmetic contradiction, hits memory feedback-comment-doc-lie-recurring.

Cycle-211b fold-back fixes the matrix narrative: `5` -> `6 (2 in cycle 210 + 4 in cycle 211)` to make the breakdown explicit + greppable. The fold-back is matrix-comment-only; no test/e2e re-run needed (the underlying 6-sample evidence stands).

QA architect re-grep of variant-split citations (P3.1): all 7 line numbers verified accurate (pthreads-async/lib.rs:127, openmp-rs/lib.rs:143, mp-tcp-event/lib.rs:170, mp-tcp-bufsync/lib.rs:103, mp-tcp-poll/lib.rs:90, mp-uds-event/lib.rs:126; pthreads-sync/lib.rs:931 + :945). No fold-back needed for those.

P3.2 verbatim-copy doc-lie scan: clean (cycle-211 narrative correctly dropped jacobi-specific facts and substituted spmv-specific ones). No fold-back needed.

P3.3 precedent-task-id non-greppability: low-impact (no fix). Earlier sibling blocks (15-transpose / 16-jacobi) have the same pattern.
<!-- SECTION:FINAL_SUMMARY:END -->

- [x] #1 #1 6 cells promoted: e2e-matrix.toml gains 6 new [[required]] entries for 17-spmv | naive x {mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}, milestone=M6.
- [x] #2 #2 Non-flake re-verification: at least 3 fresh e2e samples taken AT PROMOTION TIME (not relying on cycle-210's 2 samples) confirm 100% PASS rate across the 6 promoted cells. Sample count matches cycle-205/207 precedent.
- [x] #3 #3 Pre-cycle baseline + post-cycle update: commit message records before/after totals so the promotion is auditable.
<!-- AC:END -->
<!-- AC:END -->
