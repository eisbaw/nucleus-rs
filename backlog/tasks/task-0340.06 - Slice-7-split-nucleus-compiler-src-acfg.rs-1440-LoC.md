---
id: TASK-0340.06
title: 'Slice 7: split nucleus-compiler/src/acfg.rs (1440 LoC)'
status: Done
assignee:
  - '@claude-implementer'
created_date: '2026-05-26 14:10'
updated_date: '2026-05-26 15:00'
labels:
  - tech-debt
  - hygiene
dependencies: []
parent_task_id: TASK-0340
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0340 AC#2 slice 7: split nucleus-compiler/src/acfg.rs (1440 LoC, 0.57 comment ratio — HIGHEST in workspace). First nucleus-compiler (IR/pass-layer) split; previous slices 3-6 were backend codegen. The high comment density makes doc-audit risk amplified per feedback-comment-doc-lie-recurring; the cycle-181b deixis-lie discipline (just generalised in memory) must be applied prophylactically.

## Proposed seams (already named by section/fn boundaries — verify against current line numbers before splitting)

- acfg/mod.rs (~150 LoC): module-level //! docstring (verbatim L1-142 of pre-split) + pub mod declarations + pub use re-exports preserving the EXACT lib.rs surface (currently lib.rs:36 has 'pub use acfg::{...}' with 13 public symbols + pub(crate) eval_const).
- acfg/types.rs (~690 LoC): TYPE DEFINITIONS section L143-831. Operation + DataflowDag + DataflowEdge + SyncPlaceholder + NotifyMode + TransferPolicy + XferPlaceholder + XferRole + ACFGNode + ACFG (+ all their inherent impls + From conversions).
- acfg/errors.rs (~86 LoC): ERRORS section L832-918. LoopBoundEnd + BuildAcfgError (+ Display + Error impls).
- acfg/build.rs (~464 LoC): BUILD entry + helpers section L919-1383. pub fn build_acfg + struct BuildCtx + collect_iter_var_names + build_seq + build_stmt + build_arg_bindings + bind_arg + build_dataflow + build_effect + resolve_worker_set + collect_dataref_access + collect_dataref_access_expr + pub(crate) eval_const.
- acfg/methods.rs (~57 LoC): HELPER METHODS section L1384-end. impl ACFGNode + impl ACFG helper methods.

## Discipline (cycle-178 + 181b doc-audit discipline mandatory — comment density 0.57 amplifies risk)

THIS IS THE NUCLEUS-COMPILER LAYER, NOT BACKEND CODEGEN. The shape differs from slices 3-6:

- High comment ratio (0.57): each sub-module will carry MORE doc-prose than the backend slices did.
- The module-level //! docstring (L1-142) cites multiple TASK-NNNN ids + PRD sections + previous-cycle behaviour claims. Must move VERBATIM (AC#6 — no new citations introduced; existing ones move along) BUT every claim must be re-verified before the commit lands.
- THREE audit dimensions per cycle-181b memory generalisation:
  (1) Numeric citations (line N, file:line): `grep -nE ':[0-9]+' <new sub-files>' and walk each hit.
  (2) Behaviour-time-bound claims: `grep -nE '(does NOT yet|will be|TODO|future work|deferred|in this cycle)' <new sub-files>' and verify each against current state.
  (3) File/module deixis (NEW from cycle 181b): `grep -nE '(in this file|in this module|this function|the helper (above|below)|nearby|earlier in this file)' <new sub-files>' and verify each post-split — the entities referenced may have moved across the new sub-module boundary.

When a claim was TRUE pre-split but FALSE post-split (deixis class), REWRITE the docstring (do not just relocate) before the commit lands. The cycle-181b discovered ONE such 'in this file' lie in ctx.rs that the architect's 5-citation list did NOT flag; expect similar surprises in acfg.rs given its higher comment density.

## ID-assignment / determinism caveat (acfg.rs-specific)

acfg.rs's module-level //! docstring claims 'ID assignment is deterministic from LinkedIR input: names are sorted lexicographically, then 0,1,2…'. This is LOAD-BEARING for the equivalence-by-hashing follow-up (TASK-0190/0191) and the PartialEq tests. The split must NOT change determinism (mechanical move) but the implementer must verify post-split: a deterministic golden-output regression test under `just test` would bite if this claim broke.

## Justfile allow-list

Edit justfile: REMOVE 'nucleus/nucleus-compiler/src/acfg.rs' from the positive-enumeration list (allow-list 10 → 9). Direction-B (staleness) would fail loud if forgotten.

## Verification gate

Inside nix develop:
just build && just clippy && just test && just test-release && just e2e && just check-mega-files

Hard requirements:
- Build + clippy clean.
- just test (dev): 969 / 0 / 3 (matches cycle-181b baseline).
- just test-release: 968 / 0 / 3 (matches cycle-181b baseline).
- just e2e: 112 / 102 / 0 / 10 / 0 EXACTLY, TWO samples (non-flake).
- just check-mega-files: OK both directions.

## Cross-references

- Previous slices: cycles 178 (render.rs), 179 (mp-tcp-bufsync/lib.rs), 180 (mp-tcp-event/multi_worker.rs), 181 (multi_worker_walker.rs). All 4 backend-side; this is the first compiler-side.
- Forward-carries from cycle 181b: memory feedback-comment-doc-lie-recurring now carries the deixis-lie discipline; apply prophylactically.
- Existing pub use at lib.rs:36 — 13 symbols + pub(crate) eval_const. Surface must be byte-identical post-split via mod.rs re-exports.
- 13+ external call sites: passes/{sync_inject, partition_workers, partition_rows, halo_inference, partition_blocks2d, host_data_relay_inject, sidecar, block_transform, petri_to_events, host_mediation_inject}.rs + pthreads-sync/tests/check_frame_codegen.rs. All resolve via the unchanged 'nucleus_compiler::acfg' or 'crate::acfg' path; zero churn expected at call sites.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle-182 implementation plan: split acfg.rs (1440 LoC) into acfg/{mod,types,errors,build}.rs (4 files). Section-aligned with the existing // --- dividers: mod.rs = L1-128 docstring + use/pub-mod/pub-use, types.rs = L143-830 type defs + co-located impl ACFGNode/ACFG inherent helpers (L1384-1440 — cycle-178 precedent: small impls inline with types unless cross-sub-module callers exist; methods.rs sub-module skipped), errors.rs = L832-917, build.rs = L919-1382. eval_const pub(crate) re-exported via mod.rs. Three-dimension doc audit performed before commit.

Cycle-182 results landed in this commit:

Per-file LoC (vs pre-split acfg.rs 1440):
- acfg/mod.rs        141 (verbatim //! docstring L1-128 + use/pub-mod/pub-use)
- acfg/types.rs      778 (L143-830 type defs + co-located impl ACFGNode/ACFG helpers ex-L1384-1440)
- acfg/errors.rs      94 (L832-917 errors + Display + Error)
- acfg/build.rs      487 (L919-1382 entry + helpers + pub(crate) eval_const)
- (sum 1500; +60 vs pre-split = per-file //! orientation docstrings + use statements + pub mod decls + pub use re-exports; no behaviour change)

Three-dimension doc audit on new sub-files (cycle-181b discipline):
- Dimension 1 (numeric citations): 0 hits. (Pre-split had 0 too; no regression.)
- Dimension 2 (behaviour-time-bound claims): 2 hits in types.rs (multi-firing fused blocks deferred — VERIFIED still true: Operation::dataflow.edges has exactly 1 edge per construction; 'pair so the receiver knows where the data is coming from' — false positive on 'coming'). 0 hits needed rewriting.
- Dimension 3 (file/module deixis): 0 hits.

Doc-claim REPAIRS (NOT verbatim move — three pre-split lies fixed before landing):
1. mod.rs L70-77 (was acfg.rs L70-77 module docstring): 'precise per-tile halo synthesis ... that tightening is not done here ... so it is filed rather than half-implemented' — claim that the consumer is 'filed' was stale (TASK-0260/halo_inference + TASK-0263/transfer_inject extension have landed). Rewrote to: 'the consumer machinery lives in TASK-0260 / TASK-0263; that tightening is not done here — it lives in transfer_inject and halo_inference (the ACFG layer just exposes the index expressions verbatim so those passes can act on them)'. Anchors TASK-0260/TASK-0263 are pre-existing in halo_inference.rs/transfer_inject.rs, not new project anchors.
2. types.rs L113-114 (was acfg.rs L223-224, DataflowEdge data_in_access doc): 'new consumers (per-Fire value bindings — TASK-0156; precise per-tile halo synthesis — deferred follow-up)' — 'deferred follow-up' stale (consumer landed via transfer_inject TASK-0263). Rewrote: 'precise per-tile halo synthesis — TASK-0260 / TASK-0263'. Same anchor-leniency justification.
3. build.rs L250-255 (was acfg.rs L1146-1151, bind_arg doc): 'pthreads-sync's render_call_arg currently rejects it' — function no longer exists in pthreads-sync; rejection migrated to backend_common::render::fire::render_fire_arg returning EmitError::UnsupportedFeature. Rewrote to name the current location.

Behaviour-equivalent change list (enumerated-disclosure discipline per cycle-179b):
- (a) Module structure: acfg.rs (single 1440-LoC file) -> acfg/{mod,types,errors,build}.rs (4 files). Public surface BYTE-IDENTICAL via mod.rs pub use re-exports.
- (b) Method sub-module skipped: impl ACFGNode + impl ACFG convenience helpers (count_operations / count_repeats / max_repeat_depth / operation_count / repeat_count) co-located inside types.rs at the tail (cycle-178 precedent: keep small inherent impls with their types when they have no cross-sub-module callers; only consumers are tests via the ACFG/ACFGNode types, which come along).
- (c) eval_const visibility preserved pub(crate) and re-exported at the acfg module root via 'pub(crate) use build::eval_const' so the single cross-module caller (link.rs:1213 'crate::acfg::eval_const') sees an unchanged import path.
- (d) Three doc-claim rewrites above (NOT mechanical-move).
- (e) Justfile allow-list: removed 'nucleus/nucleus-compiler/src/acfg.rs' entry (allow-list count 10 -> 9). check-mega-files OK both directions confirmed.
- No code-body edits; no visibility uplifts beyond eval_const's pub(crate) preservation; no fn/struct/enum text changes other than docstrings.

Verification gate (orchestrator-self-run, inside nix develop --command bash -c):
- just build: clean.
- just clippy (-D warnings --all-targets): clean.
- just test (dev): 969 passed / 0 failed / 3 ignored — MATCHES cycle-181b baseline EXACTLY.
- just test-release: 968 passed / 0 failed / 3 ignored — MATCHES cycle-181b baseline EXACTLY.
- just e2e sample 1: 112 / 102 / 0 / 10 / 0 — MATCHES baseline.
- just e2e sample 2: 112 / 102 / 0 / 10 / 0 — non-flake confirmed.
- just check-mega-files: OK both directions; allow-list shrank 10 -> 9.
- just check-textual-replace-on-codegen: OK.
- just check-include-str-coverage: OK.

Determinism caveat (acfg.rs //! 'ID assignment is deterministic from LinkedIR input'): VERIFIED preserved. The PartialEq-dependent test sets (acfg.rs, sync_inject.rs, block_transform.rs, transfer_inject.rs, partition_workers.rs, partition_rows.rs, partition_blocks2d.rs, acfg_to_petri.rs, petri_to_events.rs, transfer_inject_hoist.rs) all pass with identical counts before/after.

AC delta TASK-0340 post-cycle-182:
- AC#1: DONE.
- AC#2: 5/6 files split (render.rs cycle 178, mp-tcp-bufsync/lib.rs cycle 179, mp-tcp-event/multi_worker.rs cycle 180, backend-common/multi_worker_walker.rs cycle 181, nucleus-compiler/acfg.rs cycle 182). Remaining originally-named: nucleus-compiler/link.rs (1290 LoC).
- AC#3: PENDING (slice 9).
- AC#4: PENDING (slice 10).
- AC#5: DONE (allow-list shrank 10 -> 9; direction-B verified clean).
- AC#6: DONE for slice 7. Three doc-claim rewrites fall under cycle-181b extension of cycle-178 'rewrite when the claim was true at writing time but false now' rule. Pre-existing anchors (TASK-0260/TASK-0263) used.
- AC#7: PENDING (final cycle).

Forward-carries to slices 8+ (link.rs 1290 LoC):
- link.rs is the second and final nucleus-compiler-layer mega-file in the originally-named scope.
- Doc-audit dimensions all apply equally to link.rs.
- link.rs calls 'crate::acfg::eval_const' at link.rs:1213-1214 — preserved by mod.rs's 'pub(crate) use build::eval_const' re-export; no churn at this call site.

Cycle-182 was clean by self-audit (cycle-180 pattern continued — no silent reflows; no indentation fixes; three doc-claim rewrites enumerated explicitly above; no behaviour-equivalent change misclassified as 'verbatim'). Two TaskCreate system-reminders during the cycle were ignored (project uses backlog CLI, not TaskCreate).

## Cycle 182b — architect P2 fold-back (silent-sibling render_call_arg sweep + Xfer doc-claim rewrite)

Parallel review gate after cycle-182 slice 7 returned GO with two P2 doc-lie findings the implementer's Dim-2 grep missed:

**P2.1 (architect): silent-sibling render_call_arg sweep.** Implementer fixed claim 3 in build.rs:250-255 ('pthreads-sync render_call_arg' -> 'backend_common::render::fire::render_fire_arg') but left 12 structurally-identical sibling references untouched. Pattern: feedback-silent-sibling-defect. Sites swept this cycle:
- nucleus-compiler/src/event.rs:352 (1 site)
- nucleus-compiler/src/sidecar.rs:45, 175, 371 (3 sites)
- nucleus-compiler/tests/petri_to_events.rs:1338, 1347, 1387, 1397, 1489, 1560, 1569, 1579 (8 sites; replace_all of symbol + targeted qualifier updates)

The architect listed 7 test-file sites; my pre-fix grep found 8 — minor count discrepancy disclosed.

L1397 in petri_to_events.rs ALSO had a stale file-path + line citation ('lib.rs render_call_arg ~636-642' — both 'lib.rs' wrong post-move AND line 636-642 wrong post-rename). Rewrote to 'backend-common's render/fire.rs render_fire_arg scalar-cast arm' (symbol-anchored per cycle-138 line-stamp lesson — prefer function-name anchors over line numbers since they're stable across edits).

**P2.2 (architect): stale Xfer doc-claim in mod.rs:43-44.** Original 'placeholder. Empty payload at M1; populated by TASK-0018' contradicts current truth (XferPlaceholder has substantive role/seq/src/dst/policy fields per types.rs:371). Rewrote to match the structurally-symmetric Sync claim at L38-42, naming the current payload + the transfer_inject pass that populates it:

  'Xfer — transfer placeholder inserted by the transfer-injection pass (TASK-0018). Carries an XferPlaceholder with the matched-pair endpoint (XferRole, seq tag, src/dst worker, TransferPolicy). Empty in a freshly built ACFG (no Xfer nodes are emitted by build_acfg); transfer_inject walks the tree and inserts them when distributed kernel placements demand cross-worker data movement.'

The implementer's Dim-2 grep vocabulary ('does NOT yet|will be|TODO|future work|deferred|in this cycle|NOT yet|coming') did NOT probe for 'M1' or 'empty payload' or 'placeholder'. Forward-carry for slice 8: extend Dim-2 vocabulary to include milestone-version pins ('M1'/'M2'/'M3'/...) and structural-stage claims ('placeholder', 'stub', 'skeleton').

**Verification gate (orchestrator-self-run, post-182b):**
- just build: clean.
- just clippy: clean.
- just test (dev): 969 / 0 / 3.
- just test-release: 968 / 0 / 3.
- just e2e sample 1: 112 / 102 / 0 / 10 / 0.
- just e2e sample 2: 112 / 102 / 0 / 10 / 0 (non-flake).

**Diff stat:**
- nucleus-compiler/src/acfg/mod.rs: +8/-2
- nucleus-compiler/src/event.rs: +2/-1
- nucleus-compiler/src/sidecar.rs: +10/-8
- nucleus-compiler/tests/petri_to_events.rs: +9/-8
- Total: +29/-19 across 4 files.

**Architect P3 forward-carries (not folded back this cycle):**
- P3 (panic-not-diagnostic on build.rs:269, 364, 381 — pre-existing, surfaced incidentally by audit): defer to existing panic-audit thread.
- P3 (effort-estimate of slice 8 — accurate, no action).
- P3 (direction-B staleness self-claim asserted not independently verified by architect): cycle 181b precedent — orchestrator self-reproduced check-mega-files both directions OK; not raising to a separate cycle.

Status -> Done (TASK-0340.06 closes on this cycle).
<!-- SECTION:NOTES:END -->
