---
id: TASK-0023.03.02
title: 'e2e harness: per-cell perf_threshold_pct in e2e-matrix.toml (Stage 3)'
status: Done
assignee: []
created_date: '2026-05-22 19:59'
updated_date: '2026-05-22 20:36'
labels:
  - tooling
dependencies: []
parent_task_id: TASK-0023.03
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0023.03 Stage 3 — depends on Stage 2 (the comparator). Adds per-cell perf gating to the manifest:

- [ ] `[[required]]` and `[[skip]]` entries in `nuc-nucleus/e2e-matrix.toml` accept an optional `perf_threshold_pct = N` (default = None = no gate).
- [ ] When `--baseline` is set, cells with a threshold whose delta exceeds N% are flagged as REGRESSION rows in the delta table.
- [ ] Exit-code semantics: a regression on a `[[required]]` cell with a threshold = HARD FAIL (non-zero exit). Skip-row regressions are informational.
- [ ] The matrix-toml deserializer (`Matrix` / `RequiredEntry`) needs the optional field plumbed in. Default-None preserves byte-identical bare-run behaviour.
- [ ] Unit tests: a matrix entry with threshold=50, baseline=200ms, current=350ms (75% slower) MUST fail; current=250ms (25% slower) MUST pass.

Out of scope: absolute-millis thresholds (relative-pct only for the first cut; absolute can be a follow-on if needed). Determinism-mode (separate followup).

Stage 1 (cycle-54) already lands the JSON schema and writer; Stage 2 is the comparator. Stage 3 is the gate.
<!-- SECTION:DESCRIPTION:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 56 (2026-05-22) — closed. Stage 3 of perf-regression infra (per-cell threshold gating) landed cleanly. Trilogy complete.

Implementation: nucleus/e2e/src/main.rs only (~+422/-38 LoC). RequiredEntry + SkipEntry gain serde(default) perf_threshold_pct: Option<f64>. PlannedCell carries the threshold. DeltaRow gains regression: bool + required: bool. compute_delta_rows joins planned cells against deltas. render_delta_table flags REGRESSION (bright red, required+breach), regression (dim red, skip+breach), threshold-no-breach (dim). compare_against_baseline returns the required-cell breach count. run_inner OR-merges required_failed | perf_regressions>0 into exit code.

AC closure (all 5):
- AC#1: serde-default Option<f64> in both entry types. CLOSED.
- AC#2: REGRESSION row rendered when delta > threshold AND --baseline set. CLOSED.
- AC#3: required-cell breach = HARD FAIL exit; skip-band informational. CLOSED + architect-verified the filter is exactly `required && regression`.
- AC#4: default-None preserves byte-identical bare-run behavior. CLOSED + e2e 88/70/0/18 unchanged.
- AC#5: 5 new unit tests cover fail (75%>50%), pass (25%<50%), skip-band informational-only, serde-default absence, serde-present-parses. CLOSED.

Gate (cycle 56): just e2e 88/70/0/18 UNCHANGED; --baseline no-threshold delta table behavior preserved (exit 0); just test 0 FAILED + 5 new tests pass (e2e crate 64→69); just clippy clean.

Honest limits (architect-verified):
- The breach detection uses strict `>` (delta exactly equal to threshold passes).
- `[[skip]]` cells never run a payload — threshold on skip is mainly placeholder for future un-skip flip.
- Matrix .toml UNTOUCHED — this cycle ships the MECHANISM, not its application.
- Falsifier seams compose (OR-merge) without short-circuiting (NUC_XBACKEND_NEGATIVE and NUC_NONDET_TEST run unconditionally; threshold gate is late-stage layered).

Review-gate: both qa-test-runner + mped-architect GO. Architect MEDIUM noted but non-blocking (no integration test combining required_failed + perf_regressions concurrently — single-file SoC). QA suggested a one-off live --baseline-against-threshold gate but the 5 unit tests cover the HARD-FAIL exit path structurally.

The 3-stage perf-regression trilogy (TASK-0023.03 + .01 + .02) is now complete. TASK-0023.03 (parent) status set Done in cycle 54; .01 + .02 close in cycle 55 + 56. The remaining sub-task TASK-0023.03.03 (determinism-mode --emit-timings) is independent infra and stays To Do.
<!-- SECTION:FINAL_SUMMARY:END -->
