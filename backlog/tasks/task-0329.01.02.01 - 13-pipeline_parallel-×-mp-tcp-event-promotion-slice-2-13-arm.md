---
id: TASK-0329.01.02.01
title: 13/pipeline_parallel × mp-tcp-event promotion (slice 2 13-arm)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-26 04:26'
updated_date: '2026-05-26 06:38'
labels:
  - M6
  - backend
  - mp-tcp-event
  - host-relay
  - compiler-pass
  - forward-carried-from-TASK-0329.01.02
dependencies: []
parent_task_id: TASK-0329.01.02
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Background

Cycle 163 (TASK-0329.01.02 slice 2 cycle) landed Option (B2) — the compiler-level `apply_host_data_relay_inject` ACFG pass — and promoted `09-producer-consumer/pipelined × mp-tcp-event` from [[skip]] to [[required]] bit-identical. Per parent task AC#4 P3.2 review fold-back, cycle 163 deliberately did NOT attempt 13 in the same cycle:

> P3.2 review fold-back: 09 is 2 workers × 1 transfer per iter (structurally simpler). 13 is 4 workers × multiple inter-stage transfers per iter (structurally complex). If 09 lands cleanly but 13 reveals a multi-Push-per-iter projection bug, file 13 as its own follow-up rather than blocking the slice.

09 landed clean (e2e 112/101/0/11/0 bit-identical, 3 non-flake samples). This task is the 13-arm follow-up.

## Hypothesis to test (falsifiable)

The cycle-163 `apply_host_data_relay_inject` pass is backend-agnostic at the ACFG layer; the driver wires it for mp-tcp-event only. The pass's pair-matching (Push and Wait siblings in the same Sequence, both non-host) and per-pair host-routing should compose cleanly across multiple pairs in the same Repeat body — and the pass has a regression test for the same-sequence multi-pair case (`multiple_pairs_same_sequence_each_rewritten_independently`).

But: 13's structure is qualitatively richer — 3-stage pipeline (conv_block_1 → conv_block_2 → classifier) means 2+ inter-stage transfer pairs per outer-batch iteration, with potentially different DataIds, tiles, and SeqTags. The pass should handle this naturally IF every pair is in the same Sequence. **Falsifier**: if any pair is split across nested Sequences / a Sequence boundary that the pass doesn't cross, the pair predicate doesn't fire and the TASK-0330 guard hits.

## Acceptance criteria

### AC#1: empirical verification

Attempt the cycle: re-run `just e2e` with the existing pass; verify whether 13/pipeline_parallel × mp-tcp-event compiles cleanly. If TASK-0330 guard fires, capture the (data, dst, seq) tuple and trace which Sequence boundary the unmatched Wait/Push sit in.

### AC#2: structural fix (if needed)

Two possible fixes per the failure mode:

(a) **Cross-sequence pair matching**: extend the pass to pair Push (in one Sequence) with Wait (in a sibling/parent Sequence) by walking outward. This is more invasive but matches transfer_inject's actual placement rules (Wait hoisted via `hoist_invariant_waits` can sit in a different scope than its Push).

(b) **Pre-pass normalization**: a separate pass that moves any Wait into the same Sequence as its Push (or vice versa) so that B2's pair predicate matches. Less invasive on B2 but introduces a new pass with its own correctness contract.

Pick whichever has the smaller blast radius given the empirical failure shape.

### AC#3: e2e promotion

`13-cnn-inference/pipeline_parallel × mp-tcp-event` promoted from [[skip]] to [[required]]. Bit-identical against `13-cnn-inference/reference.bin` sha256 d893337208d7b46923581ecdea8e326e07e8c7e1204a13d867807d6795f7b861.

### AC#4: regression pin

If a structural fix is needed (AC#2), add a unit test fixture mirroring 13's exact pair-distribution shape so future cycles don't silently regress on it.

## Cross-reference

- Parent: TASK-0329.01.02 cycle 163 implementation notes (B2 decision rationale + scope-limit refinement).
- Sibling: TASK-0329.01.01 cycle 162 (slice 1, Option D).
- TASK-0330 (defensive guard).
- `nucleus/nucleus-compiler/src/passes/host_data_relay_inject.rs` (the cycle-163 pass).
- `nucleus/driver/src/main.rs` (driver wiring for mp-tcp-event).

## Forward-carried lessons from cycle 163 (slice 2 / 09-arm)

These are the lessons the next implementer should INHERIT, not rediscover:

### L1 — Scope limit: pass operates on in-Repeat-body pairs ONLY

Cycle 163's first draft rewrote EVERY non-host-pair Push/Wait — top-level AND in-Repeat-body. The e2e gate caught a 05-stencil/distributed-2d × mp-tcp-event regression: top-level halo Push/Wait pairs (handled by cycle-149 flat host-relay via `relay_one` + tile-aware `render_wait_assign`) got an EXTRA host hop that broke tile-paste semantics. Host's local `img_in` is a fresh zero-init `Vec` that does NOT carry the source worker's slice; pushing 'the whole array' onward shipped zeros.

**Fix**: scope the pass to in-Repeat-body pairs only via an `inside_repeat` walker flag. Top-level pairs are LEFT ALONE; the cycle-149 flat host-relay handles them. This is the load-bearing correctness invariant of the pass.

**For 13-arm**: any extension MUST preserve this scope limit. A cross-sequence pair-matching extension (AC#2a) needs to still skip top-level (depth-0) pairs even if walking into nested Sequences.

### L2 — Existing walker machinery handles new host endpoints for free

The B2 choice was made over B1 (new `ACFGNode::Relay` variant) because the existing `backend-common/multi_worker_walker` machinery (`render_worker_events`, `collect_pre_init_sets`, `collect_worker_rendezvous`, `collect_pair_tiles`) ALREADY recurses into Loop bodies and ALREADY allocates per-worker locals + chan IDs for Wait/Push endpoints. Adding host as a Wait+Push endpoint via B2 produces correct host-side codegen WITHOUT touching the renderer.

This is load-bearing for 13: as long as the pass produces well-formed Xfer pairs (with hose seq → host wait, host push → dst wait), the renderer Just Works on host's per-iter Loop body.

### L3 — `pipeline_depth_for_seq` mirroring is mandatory

The pass mirrors any `pipeline_depth_for_seq` entry from the original seq onto both fresh seqs. Without this, mp-tcp-event's `Plan::build` chan_caps lookup ContractGaps on the fresh seqs because `build_sidecar` builds `transfer_buffer_for_seq` from `pipeline_depth_for_seq`. 09 has `buffer=4` on `transfer stream`; 13 likely has `buffer=3` (per the schedule's pipeline depth). Either way, the mirroring is mechanical and correct.

### L4 — Idempotence via the pair predicate

After one application, every fresh hop has host as an endpoint, failing the `src != host && dst != host` predicate. Second application is a structural no-op. Test: `apply_host_data_relay_inject_idempotent`. For 13: any extension must preserve this property.

### L5 — Match-exhaustiveness B1 vs B2 trade-off

If the AC#2 fix path requires changing the ACFG type (e.g. introducing a new variant after all), the cycle-163 B1 audit numbers apply: ~17 distinct `match` sites on `ACFGNode` repo-wide (`rg 'match.*node.*\{' nucleus-compiler/src/passes/`). B2 was picked precisely to avoid this. Strongly prefer additional B2-flavoured rewrites (more Xfer mutation) over B1-flavoured (variant introduction) unless the rewrite cannot be expressed structurally.

### L6 — `feedback-orchestrator-narrative-also-wrong` applies

Cycle 163's first draft followed the parent design narrative ('Option B interleaved per-Push host-relay') literally and rewrote top-level pairs too. The narrative was incomplete: the actual TASK-0330 surface is in-Repeat-body, not all non-host pairs. The e2e gate caught it. **For 13-arm**: don't trust the design narrative blindly — verify against the matrix at every cycle boundary.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Forward-carried lessons from TASK-0329.01.02 cycle 163 (slice 2 / 09-arm)

These are inherited verbatim from the cycle-163 implementation; do NOT rediscover them.

### L1 — Scope limit: pass operates on in-Repeat-body pairs ONLY

Cycle 163's first draft rewrote EVERY non-host-pair Push/Wait — top-level AND in-Repeat-body. The e2e gate caught it: 05-stencil/distributed-2d × mp-tcp-event (slice-1's promoted cell) regressed FAIL/diff because top-level halo pairs (cycle-149-handled) got an extra zero-byte host hop.

**Fix in cycle 163**: `inside_repeat` flag threaded through walker; only Sequences inside a Repeat body get rewritten. Top-level Sequences are walked for recursion but their direct Xfer children are not rewritten.

**For 13-arm**: any extension must preserve this scope limit. If 13's failure mode requires cross-sequence pair matching (pair Push in nested Sequence with Wait in parent Sequence), STILL skip top-level (depth-0) pairs. Test pin: `mixed_top_level_and_in_repeat_pairs_only_in_repeat_rewritten` in `host_data_relay_inject.rs` is the regression canary.

### L2 — Existing walker machinery handles new host endpoints for free

`backend-common/multi_worker_walker` (`render_worker_events`, `collect_pre_init_sets`, `collect_worker_rendezvous`, `collect_pair_tiles`) ALREADY recurses into Loop bodies and ALREADY allocates per-worker locals + chan IDs for Wait/Push endpoints. The B2 choice over B1 was made precisely to exploit this: adding host as Wait+Push endpoint produces correct host-side codegen WITHOUT touching the renderer.

**For 13-arm**: as long as the (extended) pass produces well-formed Xfer pairs, the renderer Just Works. No backend-side changes needed for the projection.

### L3 — `pipeline_depth_for_seq` mirroring is mandatory

Without mirroring the original seq's pipeline depth to both fresh seqs, mp-tcp-event's `Plan::build` chan_caps lookup ContractGaps on the fresh seqs (`build_sidecar` builds `transfer_buffer_for_seq` from `pipeline_depth_for_seq`). 13's schedule likely has `pipeline=3` + `buffer=3` so the mirroring matters.

### L4 — Idempotence via the pair predicate

After one application, every fresh hop has host as an endpoint, failing the `src != host && dst != host` predicate; second application is a structural no-op. Test: `apply_host_data_relay_inject_idempotent`. For 13-arm: preserve this property.

### L5 — B1 vs B2 match-exhaustiveness audit

Repo has ~17 distinct `match` arms on `ACFGNode` (cycle 163 `rg 'match.*node.*\{' nucleus-compiler/src/passes/`). B2 was picked precisely to avoid the exhaustiveness audit at every site. Strongly prefer additional B2-flavoured rewrites (more Xfer mutation) over B1-flavoured (variant introduction) unless the rewrite cannot be expressed structurally.

### L6 — `feedback-orchestrator-narrative-also-wrong` applies

Cycle 163's first draft followed the parent design narrative ('Option B interleaved per-Push host-relay') literally and rewrote top-level pairs too. The narrative was incomplete; the actual TASK-0330 surface is in-Repeat-body only. e2e gate caught the gap. **For 13-arm**: verify against the e2e matrix at every cycle boundary; don't trust design narratives blindly.

### Cross-reference

- Cycle 163 commit (TBD after this task closes) — see TASK-0329.01.02 Implementation Notes.
- Pass module: `nucleus/nucleus-compiler/src/passes/host_data_relay_inject.rs` (read the cycle-163 scope-limit refinement docstring before extending).
- Driver wiring: `nucleus/driver/src/main.rs` (`apply_host_data_relay_inject` between cycle-160 host_mediation and the capability check).

## Cycle 163b — parallel review fold-back corrections to L1-L6

The cycle-163 forward-carried lessons L1-L6 above were written by the implementer at end-of-cycle. The cycle-163b parallel review gate (qa-test-runner + mped-architect, read-only) caught two narrative errors that the next implementer should NOT inherit blindly.

### L3 mechanism correction (QA P3.1 fold-back)

L3 above attributes the failure mode of NOT mirroring `pipeline_depth_for_seq` to a compile-time `chan_caps` ContractGap in mp-tcp-event's `Plan::build`. **This is wrong**, and the next implementer should NOT remove the mirroring 'because chan_caps doesn't actually use pipeline_depth_for_seq'.

The actual mechanism:

- `chan_caps` (mp-tcp-event `multi_worker.rs:199-214`) is built from `sidecar.transfer_buffer_for_seq`, which is populated by `sidecar.rs:collect_transfer_buffers` walking each `XferPlaceholder` directly using `x.policy.buffer`. After the pass runs, the fresh Xfers ARE in the ACFG with cloned `policy.buffer`, so `chan_caps` automatically has entries for the fresh seqs. The ContractGap-on-fresh-seqs failure mode CANNOT fire.
- The load-bearing consumer of `pipeline_depth_for_seq` is `passes::acfg_to_petri::buffer_place_for` (see `acfg_to_petri.rs:486-497`), which uses the depth as the petri-net buffer place's `initial_marking` to model producer-runs-ahead semantics (TASK-0134, PRD §8.2). Without mirroring, the fresh hops' buffer places start with 0 tokens instead of D — the failure mode is a petri-model-vs-runtime mismatch / pipeline-stall surface, NOT a compile-time `chan_caps` ContractGap.

The cycle-163 pass source docstring + the in-code Phase-3 comment have both been corrected (commit at cycle-163b fold-back). The empirical observation (mirroring is required for bit-identity on 09) is unchanged — what's corrected is which downstream consumer the mirror serves.

### Residual class enumeration (architect P2.4 fold-back)

The cycle-163 pass docstring's residual-class narrative was hand-wavy ('e.g. residual in-loop w2w Pushes that could arise from future schedules'). Architect P2.4 enumerated the precise residuals; the docstring has been updated (cycle-163b). For the 13-arm implementer, the most-likely-to-surface residual is:

- **(R-singleton)** A `Push` (or `Wait`) without its matching sibling endpoint in the SAME `Sequence` — e.g. when `transfer_inject::hoist_invariant_waits` has hoisted one endpoint into a different scope. The current pass's pair-finding skips this; if 13's inter-stage Wait is hoisted to a parent Sequence, the pair predicate doesn't fire and TASK-0330 guard hits.

If 13 trips this residual, the AC#2(a) cross-sequence pair-matching extension is the right fix; if 13's pairs are all same-Sequence (which is more likely for the per-iter inter-stage shape), no extension is needed and just running existing pass + promoting the cell is sufficient.

### Affirmative bufsync structural finding (architect P2.1 fold-back)

The cycle-163 bufsync docstring at `mp-tcp-bufsync/src/lib.rs:1684` was evasive on AC#5 ('different failure profile → gain-of-zero risk'). The actual structural answer is now pinned at that site:

> the B2 rewrite splits one non-host pair into two pairs each of which is a single-pair stream with its own monotonically-allocated seq. The per-pair FIFO invariant `wire::read_msg_expect` relies on is therefore preserved per resulting hop — the pass does NOT introduce a latent seq-mismatch panic surface on future capability-compatible schedules.

For 13-arm: if 13's e2e cell is ever moved to bufsync (capability gate relaxation), the pass would be safe to enable; the bufsync docstring carries the audit forward.

## Cycle 165 implementation notes (TASK-0329.01.02.01 13-arm follow-up)

### Outcome

**AC#1 (empirical verification): PASS.** Promoted the [[skip]] cell to [[required]] and re-ran `just e2e` directly — `13-cnn-inference/pipeline_parallel × mp-tcp-event` compiled cleanly and emitted bit-identical output against `reference.bin` (sha256 d893337208d7b46923581ecdea8e326e07e8c7e1204a13d867807d6795f7b861) on the FIRST sample. No TASK-0330 guard fire, no e2e differential failure. The cycle-163 `apply_host_data_relay_inject` pass already handles 13's 4-worker 3-pair-per-iter shape WITHOUT structural change.

**AC#2 (structural fix): NOT NEEDED.** AC#1's empirical verification was clean on first promotion attempt; the cycle-163b residual `(R-singleton)` enumeration warned this MIGHT surface if `transfer_inject::hoist_invariant_waits` had split a pair across scopes, but it did NOT for 13's schedule. All three inter-stage transfer pairs (feat1: w_stage1→w_stage2, feat2: w_stage2→w_stage3, output: w_stage3→host) sit in the SAME Sequence inside the per-batch Repeat body, so the existing same-Sequence `rewrite_sequence_children` pair-matching predicate fires on every non-host pair (the two host-endpoint pairs — input and output — are left alone correctly).

**AC#3 (e2e promotion): PASS bit-identical.** Cell promoted from [[skip]] to [[required]] in `nuc-nucleus/e2e-matrix.toml` (matrix line ~1218). Bit-identity verified by the e2e harness's existing byte-diff path (`nucleus-e2e/src/main.rs:1582` "first byte differs at offset" — never fired). Non-flake: 4 consecutive e2e samples all PASS at 112/102/0/10/0 (sample times for the promoted cell: 2.79s, 2.69s, 2.68s, 2.96s — well within budget, stable).

**AC#4 (regression pin): ADDED.** Even though AC#2 wasn't taken, added `task_0329_01_02_01_13_cnn_inference_pipeline_parallel_shape` unit test in `nucleus-compiler/src/passes/host_data_relay_inject.rs` mirroring 13's exact 3-pair (feat1, feat2, output) + 1 input pair distribution inside a Repeat body. The pin locks in: (a) the input host→w_stage1 pair is LEFT ALONE (host endpoint), (b) feat1 w_stage1→w_stage2 is rewritten to 4 hops, (c) feat2 w_stage2→w_stage3 is rewritten to 4 hops, (d) the output w_stage3→host pair is LEFT ALONE (host endpoint), (e) 4 distinct fresh seqs monotonically allocated > max original seq. If a future cycle changes the pass to over-rewrite or under-rewrite this distribution, the unit test fails fast at `cargo test` time (before the e2e gate).

### Measured gate numbers (HEAD c6794a7 → cycle-165 HEAD pending)

- `just build`: green (no warnings).
- `just clippy`: green (no warnings).
- `just test`: 962 passed / 0 failed / 3 ignored (was 961; +1 task_0329_01_02_01_13_cnn_inference_pipeline_parallel_shape).
- `just test-release`: 961 passed / 0 failed / 3 ignored (was 960; +1 same).
- `just e2e`: **112/102/0/10/0** (was 112/101/0/11/0; +1 required, -1 skipped). 4 non-flake samples.

### File touchpoints

- `nuc-nucleus/e2e-matrix.toml` lines 1182–1223: matrix-comment addendum ("Cycle-165 PROMOTION...") + cell flipped from [[skip]] to [[required]] (reason= line removed).
- `nucleus/nucleus-compiler/src/passes/host_data_relay_inject.rs` (a) module docstring "Honest scope" section updated from "09 only this cycle" to "09 in cycle 163; 13 in cycle 165" with the empirical pair-shape finding pinned and (b) new unit test `task_0329_01_02_01_13_cnn_inference_pipeline_parallel_shape` added.

### Honest limits

- The unit test pin is structural-only (it tests the ACFG-level rewrite, not the projection or the runtime). The empirical bit-identity is the load-bearing evidence; the unit test is the cheap regression canary.
- Did NOT investigate the cycle-163b `(R-singleton)` shape with hoist_invariant_waits actually splitting a pair across scopes — that surface, if a future schedule shape triggers it, is what TASK-0329.01.02.01 follow-ups would file (see "Forward-carry" below).
- Did NOT extend the pass to cross-Sequence pair matching — not needed for 13, and AC#2 expressly avoids it absent failure evidence.

### Lessons forward-carried

**L7 (NEW)**: cycle-163b's residual `(R-singleton)` worry did NOT empirically fire for 13. The hypothesis that `transfer_inject::hoist_invariant_waits` might split inter-stage pairs across scopes was structurally sound but empirically incorrect for the 13/pipeline_parallel shape (and presumably for similar pipelined-loop shapes where the consumer immediately follows the producer in the algorithm AST). If a FUTURE schedule shape (e.g., a pipelined loop with significant Sync or Operation activity BETWEEN producer and consumer that might trigger hoist) trips R-singleton, the (a) cross-sequence-pair-matching extension in `host_data_relay_inject.rs` is the right fix. Until that empirical shape exists, do NOT pre-build the extension.

**L8 (NEW)**: when the implementation hypothesis ("cycle-163 pass already handles 13") is empirically TRUE on first try, the AC#4 regression-pin discipline still applies even though AC#2 wasn't taken. A 3-pair-in-Repeat unit-test pin defends against a quieter regression: a future cycle that subtly narrows the pair-matching predicate (e.g., adds an "only one non-host pair per Sequence" optimisation by accident) would fail the pin at unit-test time, not e2e time. Cheap to add; high leverage.

### Cross-reference

- Parent: TASK-0329.01.02 (Done cycle 163; this 13-arm follow-up closes its AC#4 P3.2 per-cell sub-split).
- Sibling: TASK-0329.01.01 (slice 1 Option D, separate cell).
- TASK-0329.01 (parent of parent): once this lands, the parent's AC#3 fully covers both trigger cells; AC#4 cross-ref with TASK-0332 still has a TASK-0332 sibling cell (05/distributed-2d × mp-tcp-event, lift-after-host-mediated-relay model — different surface).
- Memory `feedback-implementer-disclosure-mechanism-wrong`: applied — disclosed the empirical mechanism (same-Sequence pair-matching predicate fires on 13's shape) rather than speculating about why the cycle-163 pass works.
- Memory `feedback-orchestrator-narrative-also-wrong`: applied — verified the empirical claim by 4 non-flake e2e samples + a structural unit test pin, not by algebra.
- Memory `project-cross-backend-differential.md`: e2e baseline bumps from 112/101/0/11/0 (cycle 164b) → 112/102/0/10/0 (cycle 165).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 165: 13-cnn-inference/pipeline_parallel × mp-tcp-event PROMOTED from [[skip]] to [[required]] bit-identical against reference.bin sha256 d893337208d7b46923581ecdea8e326e07e8c7e1204a13d867807d6795f7b861. AC#1 verified empirically on FIRST promotion sample (cycle-163 apply_host_data_relay_inject pass already handles 13's 4-worker 3-pair shape without structural change); AC#2 not needed (no structural fix); AC#3 PASS bit-identical across 4 non-flake e2e samples; AC#4 regression-pin task_0329_01_02_01_13_cnn_inference_pipeline_parallel_shape added (defends against silent pair-matching narrowing). e2e baseline: 112/101/0/11/0 (cycle 164b) → 112/102/0/10/0 (cycle 165). Tests 961/0/3 → 962/0/3 (dev) and 960/0/3 → 961/0/3 (release). All 6 forward-carried lessons L1-L6 from cycle 163 preserved (in particular L1 scope limit and L3 pipeline_depth mirroring); 2 new lessons L7 (R-singleton empirically did not fire for 13) and L8 (AC#4 pin discipline applies even when AC#2 isn't taken) added to notes. Parent TASK-0329.01.02 AC#4 P3.2 13-arm sub-split now closed.
<!-- SECTION:FINAL_SUMMARY:END -->
