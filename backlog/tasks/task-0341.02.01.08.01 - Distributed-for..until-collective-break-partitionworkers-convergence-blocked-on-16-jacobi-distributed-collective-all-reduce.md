---
id: TASK-0341.02.01.08.01
title: >-
  Distributed for..until collective-break (partition=workers convergence):
  blocked on 16-jacobi/distributed + collective all-reduce
status: To Do
assignee: []
created_date: '2026-06-10 22:44'
labels:
  - grammar-extension
  - multi-worker
  - cross-backend
  - honest-blocked
dependencies: []
parent_task_id: TASK-0341.02.01.08
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Remaining scope of epic S7 (TASK-0341.02.01.08) after cycle-S7 landed the SINGLE-WORKER 7-backend for..until differential (12 [[skip]] -> [[required]], e2e 504/443/0/61/0). The DISTRIBUTED (partition=workers / partition=rows) convergence schedule — where the break is a COLLECTIVE all-reduce + broadcast of the global convergence scalar so every worker exits at the SAME generation by construction (AC#1 invariant) — is honest-BLOCKED.

WHY BLOCKED (verified cycle-S7, both empirically and by code-trace):
1. A distributed Jacobi convergence schedule inherits the 16-jacobi/DISTRIBUTED stencil blockage. 16-jacobi/distributed is itself honest-BLOCKED on ALL 7 tier-1 backends today (5 on the 3D wait_slice gap TASK-0341.02.02.01.01, 2 on the in-Loop w2w Push gap TASK-0330). Until that fixed-iteration distributed base passes, NO distributed convergence cell can be [[required]].
2. The convergence scalar `partials[t]` becomes an overlapping-write accumulator fan-in needing combine=max (verified: building a partition=rows converge schedule trips "data symbol partials is an overlapping-write accumulator fan-in but its owning kernel declares NO combine = <op> identity" — the all-reduce shape).
3. The global decision (maxdiff[t] <= TOL) must be BROADCAST back into the Repeat body so every worker branches on the IDENTICAL value — an in-Repeat-body cross-worker scalar edge. The design memory flags this as the in-Loop w2w class that star backends handle via host-relay (B2) and that bufsync/poll REJECT loud (per-pair FIFO makes the splice-point lift unsafe). So the per-backend reach will be tiered: shared-memory + event backends via host-relay first; bufsync/poll likely stay honestly skipped.

WHAT IS DONE (do NOT redo): the multi-worker walkers correctly stay fail-loud on break_cond:Some (pinned by backend-common/tests/multi_worker_break_cond_rejected.rs — the falsifier that bites if a future cycle lifts the guard without supplying the collective break). Lifting that guard is THIS task; it must REPLACE the falsifier with a positive pin (all workers break on the same received global decision at the same generation), not merely delete it.

DEPENDENCY CHAIN: this task is GATED on 16-jacobi/distributed landing (TASK-0341.02.02 cluster: wait_slice N-D TASK-0341.02.02.01.01 + host-relay for in-Loop w2w TASK-0330 / TASK-0341.02.02.01.02). Do not start until the fixed-iteration distributed Jacobi passes on at least the shared-memory + event backends.

ACs (carry from parent TASK-0341.02.01.08): (1) collective all-reduce+broadcast on the global scalar, every worker branches identically; (2) replace the fail-loud falsifier with a positive collective-break pin + KEEP a negative test that a LOCAL-only break predicate is rejected; (3) distributed converge PASSES bit-identical on the in-reach backends (the sound subset; document the honestly-skipped backends with precise reasons).
<!-- SECTION:DESCRIPTION:END -->
