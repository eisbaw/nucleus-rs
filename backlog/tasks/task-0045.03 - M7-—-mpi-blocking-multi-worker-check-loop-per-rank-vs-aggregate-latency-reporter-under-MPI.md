---
id: TASK-0045.03
title: >-
  M7 — mpi-blocking multi-worker check-loop (per-rank vs aggregate latency
  reporter under MPI)
status: To Do
assignee: []
created_date: '2026-05-31 08:00'
labels:
  - M7
  - backend
  - mpi
dependencies: []
parent_task_id: TASK-0045
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Forward-linked from TASK-0045.01: the single-worker arm's NucCheckCountReporter was spot-verified under MPI -n 1 (rank 0 prints its tally, value-correct). The multi-worker arm REJECTS check frames loud (EmitError::UnsupportedFeature) because the per-rank-vs-aggregate reporter semantics need design and NO shipped multi-worker schedule carries a check loop (split/distributed schedules have no 'check loop' directive), so this never fires on the example set today.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A multi-worker schedule carrying a check loop frame (on_violation=count/log/panic) lowers under MPI instead of the current typed UnsupportedFeature reject in mpi-blocking/src/multi_worker.rs
- [ ] #2 The per-rank-vs-aggregate semantic is DECIDED and documented: MPI processes do not share memory, so the count static is per-rank; an aggregate program-wide tally would require an MPI_Reduce of the per-rank counts before the rank-0 reporter prints (PRD §6.3.5 'tallied across the program run')
- [ ] #3 Verified under mpiexec -n N (all ranks live) on a multi-worker check schedule (needs a new example schedule — none exists today) — the reporter output matches the decided semantic
<!-- AC:END -->
