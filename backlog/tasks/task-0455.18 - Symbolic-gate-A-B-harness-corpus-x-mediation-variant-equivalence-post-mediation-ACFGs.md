---
id: TASK-0455.18
title: >-
  Symbolic-gate A/B harness: corpus x mediation-variant equivalence
  (post-mediation ACFGs)
status: Done
assignee: []
created_date: '2026-06-10 18:03'
updated_date: '2026-06-11 04:30'
labels:
  - production
  - validation
  - gate
dependencies: []
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Wave-6 review P2.1: the driver gates the POST-mediation ACFG (host-mediation + host-data-relay injections run before the soundness gate on star-topology backends), but tests/symbolic_comm_ab.rs enumerates the corpus only through run_pre_mediation_passes — so the production gate input for mp-tcp-*/mp-uds-event (mediated Syncs, 4-hop relay Xfer pairs incl. in-Repeat ones) is never A/B-d against the expanded replay. Soundness is unaffected (the theorem is shape-generic; unknown shapes land in NeedsExpansion -> real expanded gate), but the empirical falsifier is narrower than ideal.

Work: extend the harness to also run each corpus cell through the two inject passes with the elected host for each mediation-capable backend variant (mirror the driver chain exactly — the driver-mirrors-election rule), and assert symbolic==expanded on those post-mediation nets too. Keep the gated-count floor in step.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Post-mediation corpus variants A/B-d: symbolic verdict == expanded verdict on every mediated cell
- [x] #2 Harness header claim and floors updated consistently
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Plan (TASK-0455.18):
1. Extend tests/symbolic_comm_ab.rs: add a post-mediation A/B arm.
2. Derive distinct (star,relay) mediation variants from the 10 backends/*/capabilities.toml at runtime (not a hardcoded list) - ALL_BACKENDS<->dir completeness-pin precedent. The non-(false,false) variants are the new ones to apply.
3. For each gated corpus cell, for each non-identity variant: elect host via acfg_to_events + the canonical rule MIRRORED inline (nucleus-compiler cannot dep backend-common; pin source), then apply_host_mediation_inject (+ apply_host_data_relay_inject when relay), then assert symbolic verdict == expanded verdict on the post-mediation net.
4. Update harness header (remove the open-extension wording, document post-mediation arm) and add a post-mediation gated floor + keep pre-mediation floor.
5. If a REAL verdict divergence appears: STOP, document, file a task; do NOT touch production.
6. clippy -p nucleus-compiler; run test 3x for determinism; report pre/post totals.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implemented (cycle, TASK-0455.18). Extended nucleus-compiler/tests/symbolic_comm_ab.rs with a POST-mediation corpus A/B arm closing wave-6 review P2.1.

Design:
- distinct_mediation_variants() DERIVES the (star,relay) combinations from the 10 backends/*/capabilities.toml at runtime via load_capabilities (the exact driver load), excluding the identity (false,false) variant (no passes => gate input IS the pre-mediation ACFG already A/B-d). NOT hardcoded — same completeness discipline as task0455_09 ALL_BACKENDS<->dir set-equality. Derived set = 2 variants: (true,false) [mp-tcp-bufsync, mp-tcp-poll], (true,true) [mp-tcp-event, mp-uds-event].
- elect_host_mirroring_driver() mirrors the driver chain byte-faithfully: acfg_to_events -> used=non-empty-event-list workers (BTreeSet) -> canonical election rule MIRRORED INLINE from backend_common::host_election::elect_host_from_name_workers (nucleus-compiler cannot dep backend-common; arrow runs the other way). Source pinned in docstring (driver-mirrors-election rule).
- apply_mediation() mirrors the Some(h) branch: apply_host_mediation_inject(h) then conditionally apply_host_data_relay_inject(h) with the SAME h; None=>acfg pass-through mirrored.
- New test post_mediation_corpus_combined_verdict_equals_expanded_verdict: for each gated corpus cell x each non-identity variant, asserts symbolic verdict == expanded verdict on the post-mediation net. A real divergence is flagged LOUDLY as a P1 (no production fix in this test, per brief).

Header updated: removed the open-extension/wave-6-P2.1 gap wording; documents the new post-mediation arm.

Floors: pre-mediation gated>=55 unchanged (current 62). NEW post-mediation floor mediated_cells>=100 (current 124=62x2), plus non-vacuity floors mediated_changed>0 (10), mediated_proven>0 (116), mediated_fellback>0 (8).

Totals (deterministic 3x):
- PRE-mediation: enumerated=71 gated=62 skipped=9 proven_sound=58 (buffered=21) needs_expansion=4.
- POST-mediation: variants=2 mediated_cells=124 proven_sound=116 needs_expansion=8 (net changed for 10 cells). ZERO verdict mismatches (no P1 finding).

Verification: extended harness 8/8 GREEN 3x identical; clippy -p nucleus-compiler --tests -D warnings clean (forced rebuild, no doc_lazy_continuation). File 853 LoC (<1000 mega-file gate; no sibling split). NO production/driver code touched; test_support.rs untouched.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Post-mediation corpus arm landed: every gated cell x every distinct (star, relay) combination derived from the 10 capability files, mediated with the driver-mirrored election (rule pinned inline + verified line-by-line faithful by the architect), symbolic==expanded on all 124 mediated pairs (116 proven / 8 expanded-equal), zero divergences, deterministic 3x, floors enforced. The wave-6 P2.1 falsifier gap is closed; header claim discharged. Landed 5fde1c2 (+39c1534 citation-fence fix); architect GO; full ci green.
<!-- SECTION:FINAL_SUMMARY:END -->
