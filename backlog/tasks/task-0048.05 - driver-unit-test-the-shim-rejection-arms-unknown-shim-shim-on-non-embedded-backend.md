---
id: TASK-0048.05
title: >-
  driver: unit-test the --shim rejection arms (unknown shim + --shim on
  non-embedded backend)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-28 13:42'
updated_date: '2026-05-28 21:34'
labels:
  - M10
  - backend
  - testing
dependencies: []
parent_task_id: TASK-0048
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Defensive follow-up from TASK-0048.01 cycle-238b review-gate (architect P3.3). The two --shim fail-fast guards in nucleus/driver/src/main.rs (unknown --shim value => typed error; --shim on a non-embedded backend => typed error, guard placed before the backend match) are currently covered only by manual runs — cmd_build has NO test module, so a future refactor could silently drop a guard (silent-sibling risk). Add a driver-level test (in-crate #[cfg(test)] mod in main.rs, or an integration test) asserting each rejection arm produces the typed nucleus error + non-zero exit. Cheap; defends the fail-fast contract.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A test exercises an unknown --shim value (e.g. --shim bogus) on --backend embedded-pattern and asserts a typed error + failure exit (not a panic, not silent ignore).
- [x] #2 A test exercises --shim stm32h7 on a NON-embedded backend (e.g. pthreads-sync) and asserts the typed wrong-backend error + failure exit.
- [x] #3 Tests run inside the standard just test / just ci gate (no new shell/toolchain needed).
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle 241 (2026-05-28) — LANDED, both reviews GO. Added nucleus/driver/tests/task0048_05_shim_rejection.rs (commit a05d269): two subprocess integration tests spawning the real `nucleus` binary on 01-elementwise-add/naive. (1) unknown_shim_on_embedded_pattern_rejects_typed_nonzero: `--shim bogus --backend embedded-pattern` => non-zero exit + 'unknown --shim `bogus`' + 'backend embedded-pattern' + NOT a panic. (2) stm32h7_shim_on_non_embedded_backend_rejects_typed_nonzero: `--shim stm32h7 --backend pthreads-sync` => non-zero exit + '--shim is only meaningful for the embedded-pattern backend' + 'pthreads-sync' + NOT a panic. Both guards reject before any generated-project codegen, so the tests are cheap. Mirrors cli_reuse_strict.rs subprocess template; docstring cites main.rs by grep-able symbol (`--shim`), not line numbers. GATE (orchestrator-re-run + qa-test-runner independent): clippy clean (--all-targets), test 1074 passed / 0 failed / 3 ignored, e2e 280/246/0/34/0 required-fail:0. REVIEW: qa-test-runner GO (confirmed guard strings originate in main.rs:757/992, not test-local; non-vacuous). mped-architect GO (faithful, no AC-gaming, clean on comment-doc-lie + panic-not-diagnostic classes); two P3 non-blockers noted (tempdir uses only process::id() — disambiguated by distinct tag; exit-code-value not asserted, only !success() — acceptable per AC wording 'failure exit'). No hardening required.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Driver-level regression pins for the two --shim fail-fast guards (TASK-0048.01 cycle-238b architect P3.3 silent-sibling defense). One new test file, two subprocess integration tests asserting typed-error + non-zero exit for (1) unknown --shim on embedded-pattern and (2) --shim stm32h7 on a non-embedded backend; each also asserts the rejection is NOT a panic. Test-only, no production change. Gate: clippy clean, test 1074/0/3, e2e 280/246/0/34/0. Both review-gate agents GO. All 3 ACs met.
<!-- SECTION:FINAL_SUMMARY:END -->
