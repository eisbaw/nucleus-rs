---
id: TASK-0340.12
title: >-
  Repo-wide cargo fmt --check skew: ~60 pre-existing deviations not in just ci
  gate
status: Done
assignee:
  - '@mark'
created_date: '2026-05-30 02:22'
updated_date: '2026-05-30 06:47'
labels:
  - tech-debt
  - hygiene
dependencies: []
parent_task_id: TASK-0340
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Discovered during TASK-0340.02 (impl b1ec0e7). cargo fmt --all -- --check flags ~60 files repo-wide (backend-common/src/event_plan/worker_program.rs, tcp_plan/relay.rs, all backends multi_worker.rs, nucleus-compiler tests proptest_petri/contract/link, e2e/src, test-common, embedded-pattern, etc.). These are pre-existing (NOT introduced by 0340.02 — verified fire.rs:162 byte-identical to HEAD) and stem from a rustfmt version/config skew between the pinned toolchain and whatever last ran cargo fmt. just ci does NOT include a fmt-check arm (only check/clippy/test/test-release/e2e + structural checks + negative/determinism), so the tree builds and lints green despite the skew. Decide: (a) one-shot cargo fmt --all sweep commit, or (b) add fmt-check as a gate arm to prevent re-drift, or (c) accept and document. Low priority hygiene.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Cycle 211d (orchestrator-direct, mechanical sweep). DECISION: option (a) one-shot `just fmt` sweep of the 26 version-skew files; REJECT option (b) fmt gate arm (preserves the deliberate TASK-0069 non-gated-fmt decision: fmt is dev-side informational, clippy -D warnings is the CI hard gate). Evidence: all hunks are benign rustfmt-default drift (line-wrap, call-arg layout, import grouping); none touch string literals / emitted codegen. Verify e2e stays 308/246/0/62/0 (proves emitted output byte-identical) + tests/clippy green; confirm git diff is fmt-only; then `just fmt-check` exits 0.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Orchestrator accuracy correction (resolving a reviewer contradiction empirically) ===

The two review subagents on the parent task TASK-0340.02 DISAGREED on this skew, so the orchestrator re-ran the commands directly. Resolution:

ACCURATE METRIC: the skew is 26 unique files / 63 hunks (NOT "~60 files" — that conflated the hunk count with the file count). Full file list spans backend-common (event_plan/worker_program.rs, render/fire.rs, tcp_plan/relay.rs, several tests), all backends' multi_worker.rs (mp-tcp-event, mp-uds-event, openmp-rs, pthreads-sync, pthreads-async), embedded-pattern (lib.rs, render.rs, skeleton/mod.rs, kernel_extract.rs, tests.rs), nucleus-compiler (contract.rs + 3 tests), e2e (main.rs, tests.rs), test-common, driver test.

REPRODUCE: `just fmt-check` (= `cd nucleus && cargo fmt --all -- --check`) -> exit 1, 63 "Diff in" hunks. rustfmt 1.8.0-stable, no rustfmt.toml in tree (pure rustfmt-default version skew).

PITFALL (this is what made one reviewer wrongly report "0 files / cargo-fmt is clean"): the cargo workspace is rooted at nucleus/, NOT the repo root. Running `cargo fmt --all -- --check` from the REPO ROOT finds no manifest and silently checks nothing (exit 1, zero diff output) — a false "clean". Always run fmt/build/clippy from nucleus/ (or via the just recipes, which cd there). The implementer's original mechanism (`cargo fmt --all -- --check`) was CORRECT; only the count needed fixing. Do NOT reword this task to claim cargo-fmt passes and only bare rustfmt shows skew — that is the false reading.

NOT GATED (claim verified): no fmt arm is in `just ci` (justfile ci: recipe ~183-196). fmt-check is deliberately excluded per justfile:43-45 / TASK-0069 closure (fmt is dev-side informational; clippy -D warnings is the CI hard gate). So this skew is by-design-tolerated, not a gate failure. PRE-EXISTING: the skewed-file SET is byte-identical at parent vs HEAD (qa-test-runner A/B), so TASK-0340.02 did not introduce it.

SCOPE when picked up: a single `just fmt` (cargo fmt --all) sweep fixes all 26 files, but touches them wholesale — schedule it on a quiet boundary (not interleaved with a content change) so the fmt-only diff stays reviewable. Low value given fmt is non-gated by design; this task mainly exists so the drift is tracked, not silently growing.

=== Cycle 211d outcome — DONE via option (a) one-shot sweep (commit ca2f228) ===

Resolved: 26 nucleus/*.rs files reformatted via just fmt. DECISION: option (a) sweep; option (b) (fmt arm in just ci) REJECTED to preserve the deliberate TASK-0069 non-gating decision (fmt dev-side informational, clippy -D warnings is the CI hard gate); option (c) rejected because leaving 63 hunks keeps just fmt-check permanently red and erodes its signal.

GATE (orchestrator-run, on the swept tree): build OK, clippy -D warnings clean, just test 0 failed, just test-release 108 ok/0 failed, just e2e 308/246/0/62/0 (bit-identity preserved — emitted codegen byte-identical), just fmt-check now exit 0.

REVIEW GATE — both GO:
- qa-test-runner: 26 .rs-under-nucleus only; fmt-check exit 0; emit-string PIN suites 57 passed/0 failed (the precise mechanism that would catch any emitted-codegen drift); token-level diff proves formatting-only. Did not re-run full e2e (orchestrator already observed 308/246/0/62/0).
- mped-architect: extracted ALL 110 double-quoted literals from -/+ lines, sorted+diffed -> EMPTY (conclusive: no emitted string changed); no doc/comment/ALLOW/SAFETY annotation mangled; independently re-ran e2e 308/246/0/62/0; decision sound; residual drift handled (rustfmt pinned via rustChannel in flake.nix, so drift only returns on a deliberate channel bump).

P3 findings investigated by orchestrator:
1. Architect flagged the TASK-0069 attribution as possibly belonging to TASK-0256. VERIFIED FALSE ALARM (read both summaries): the justfile comment correctly splits it — recipe=TASK-0256, non-gating-DECISION=TASK-0069; TASK-0256s own summary says "intentionally NOT wired into just ci (per TASK-0069 closure)". Attribution is accurate; no fix. (Instance of feedback-implementer-disclosure-mechanism-wrong, reviewer-subagent variant.)
2. METHODOLOGICAL LESSON (architect): for a fmt sweep the DECISIVE inertness test is the token-level string-literal-invariance diff, NOT e2e-green-alone (e2e green is necessary, not sufficient — it would miss a change to an untested internal control-flow/diagnostic branch). The string-literal-extract-and-diff is the gold-standard check; e2e + emit-pin tests corroborate.

RECURRING-BY-DESIGN: TASK-0276 (Done, cycle 91) was the PRIOR fmt-drift sweep (~330 lines then, e2e was 92/77/0/15/0). Because fmt is non-gated (TASK-0069), drift re-accumulates whenever code lands without just fmt; periodic sweeps recur. NOT a duplicate of TASK-0276; the current instance.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE cycle 211d (commit ca2f228). One-shot just fmt sweep of 26 version-skew-drifted nucleus/*.rs files; option (a) chosen, option (b) fmt-gate-arm rejected to preserve the deliberate TASK-0069 non-gating decision. Semantically inert: full gate green (e2e 308/246/0/62/0 bit-identical) + both review subagents GO (architect string-literal-invariance diff EMPTY = conclusive proof no emitted codegen changed). fmt-check now exit 0. Recurring-by-design (prior instance TASK-0276 cycle 91); drift will re-accumulate while fmt stays non-gated.
<!-- SECTION:FINAL_SUMMARY:END -->
