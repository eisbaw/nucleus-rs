---
id: TASK-0048.03
title: >-
  embedded-pattern bin: generalise --shim stm32h7 emit + Renode recipe to
  examples 5 and 9
status: To Do
assignee: []
created_date: '2026-05-28 12:53'
labels:
  - M10
  - backend
  - validation
dependencies: []
parent_task_id: TASK-0048
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Follow-up to TASK-0048.01 (example 1 only). PRD §11 M10 names examples 1, 5, 9 as the embedded-representative set. Generalise the bin-emit path (emit_bin) + the renode-embedded-ex1 just recipe to also cover example 5 (05-stencil, 2D blur3 — already cross-compiles as a LIB via check-embedded, so the bin scaffolding should transfer) and example 9 (09-producer-consumer). The deterministic UART framing currently hardcodes the 'NUC-EX1' prefix in the Usart1Shim dma_push; parameterise it per-example (or make the recipe assert per-example expected len/checksum). Watch: example 9 may be multi-worker / use cross-worker Push/Wait/Sync which emit_bin currently rejects with an M11 forward link — confirm the naive single-worker schedule shape before assuming it fits the M10 single-MCU bar.
<!-- SECTION:DESCRIPTION:END -->
