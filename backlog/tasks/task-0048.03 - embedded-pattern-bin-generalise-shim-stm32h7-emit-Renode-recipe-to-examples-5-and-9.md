---
id: TASK-0048.03
title: >-
  embedded-pattern bin: generalise --shim stm32h7 emit + Renode recipe to
  examples 5 and 9
status: Done
assignee:
  - '@me'
created_date: '2026-05-28 12:53'
updated_date: '2026-05-28 15:56'
labels:
  - M10
  - backend
  - validation
dependencies: []
parent_task_id: TASK-0048
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Follow-up to TASK-0048.01 (example 1 only). PRD §11 M10 names examples 1, 5, 9 as the embedded-representative set. Generalise the bin-emit path (emit_bin) + the renode-embedded-ex1 just recipe to also cover example 5 (05-stencil, 2D blur3 — already cross-compiles as a LIB via check-embedded, so the bin scaffolding should transfer) and example 9 (09-producer-consumer). The deterministic UART framing currently hardcodes the 'NUC-EX1' prefix in the Usart1Shim dma_push; parameterise it per-example (or make the recipe assert per-example expected len/checksum). Watch: example 9 may be multi-worker / use cross-worker Push/Wait/Sync which emit_bin currently rejects with an M11 forward link — confirm the naive single-worker schedule shape before assuming it fits the M10 single-MCU bar.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 emit_bin + the renode recipe cover example 5 (05-stencil naive): cross-compiles for thumbv7em + Renode captures a deterministic UART line.
- [x] #2 Example 9 (09-producer-consumer): confirm the naive schedule's single-worker shape fits the M10 single-MCU bar; if it is multi-worker (Push/Wait/Sync), defer to M11 (TASK-0049) with a forward link rather than forcing it through emit_bin.
- [x] #3 The UART framing prefix (currently hardcoded 'NUC-EX1' in Usart1Shim::dma_push) is parameterised per example.
- [x] #4 M9 lib path (check-embedded) + the ex1 bin path (renode-embedded-ex1) remain green.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. ONBOARD: read both examples (algo/sched/kernels/input/reference), the backend (lib.rs emit_bin/render_fire/render_run_body/collect_data_decls, skeleton.rs USART1_SHIM_SRC + load-order ASSUMES comment, kernel_extract.rs limits, tests.rs), the renode-embedded-ex1 recipe + run.resc, check-embedded. DONE.
2. CONFIRM single-worker: both ex5+ex9 naive schedules are `workers={host}` (verified). Neither hits emit_bin's multi-worker reject.
3. AC#3 reality: the NUC-EX1 prefix was REMOVED in TASK-0048.02 (raw-byte stream now). There is no per-example prefix to parameterise — the stream is already example-agnostic raw bytes. Record this; AC#3 is satisfied-by-obsolescence.
4. Empirically GENERATE ex5 + ex9 bins via `nucleus build --backend embedded-pattern --shim stm32h7`; inspect emitted main.rs (esp. ex9 two-stage: stream intermediate array + produce/transform both indexed-output pure Fires; verify kernel_extract handles both). DIAGNOSE any gap (real bug vs unsupported shape -> typed EmitError + filed task).
5. Cross-compile each generated bin under .#embedded (thumbv7em). Lower-risk: ex5 (already a LIB in check-embedded). Risk: ex9 (never through embedded codegen).
6. PARAMETERISE the recipe: rename renode-embedded-ex1 -> `renode-embedded EX=01-elementwise-add` (default preserves ex1 gate). The .resc + size/cmp logic is already example-agnostic (expected size derived from each example's reference.bin). Only --algo/--sched + cmp reference path vary. Rename tests/renode/embedded-ex1/ -> embedded/ for doc honesty (dir name was ex1-specific).
7. Add ex5/ex9 shape tests to tests.rs (bin emit) mirroring ex01_bin_* (single-source drift detection).
8. LOAD-ORDER (TASK-0048.06): confirm ex5 (single load_image) + ex9 (single load_input) each have exactly ONE effectful load => cursor starts at 0, consumes whole region. Verify against generated load order, record, update TASK-0048.06.
9. GATE: host subset (build/clippy/test/test-release/e2e; e2e baseline 280/246/0/34/0 = hard fail on regression), check-embedded, structural gates, renode-embedded EX={01,05,09} (new ones twice for determinism). Report ACTUAL numbers.
10. Commit per logical unit; honest notes; ex9 honest outcome (land or file precise gap).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Implementation Notes (recorded by orchestrator — implementer was interrupted by an API socket error pre-commit, after a coherent dirty tree; orchestrator verified + committed as 09446b1)

### What landed
- Parameterised the Renode recipe: renode-embedded-ex1 -> `renode-embedded EX="01-elementwise-add"` (positional default preserves the prior ex1 gate). Renamed tests/renode/embedded-ex1/ -> tests/renode/embedded/ (run.resc generalised: injects $input, loads $bin, RunFor+quit). One recipe, not three (PRD §12.3 anti-bloat). Per-example: only --algo/--sched + the cmp reference path vary; expected size derived from each reference.bin; byte-count guard + `cmp -s` fail-loud.
- ex5 (05-stencil, 2D blur3 9-pt stencil) + ex9 (09-producer-consumer, two-stage produce->transform with intermediate `stream` array) both emit, cross-compile thumbv7em, run in Renode on real injected input.bin, cmp BYTE-EXACT vs reference.bin. ex9 is the first example through embedded codegen beyond the M9 set — it works (kernels extracted verbatim, no brace-matcher truncation; result=24*seeds verified by architect).
- +2 new #[test] fns (ex5 + ex9 bin-shape, ~27 assertions) — genuine drift detectors (invoke emit_bin on real lowered events, assert exact emitted loop-body lines).

### Review gate (cycle 240b) — GO/GO
- qa-test-runner: GO. build clean; clippy 0; just test 1072/0/3; test-release 1071/0/3; e2e 280/246/0/34/0 TWICE (no flake); check-embedded OK (M9 lib intact); renode-embedded {01,05,09} all byte-exact vs reference.bin (ex5/ex9 deterministic across 2 runs); structural gates OK.
- mped-architect (read-only): GO, no P1. Independently verified ex9 codegen structurally correct (NOT coincidental), ex5 blur3 fully extracted, M9 lib byte-UNTOUCHED (only comment/doc lines changed in lib.rs/skeleton.rs/Cargo.toml — zero functional change), AC#3 legitimate (satisfied-by-prior-work: NUC-EX1 framing removed in .02; not AC-gaming), recipe anti-bloat correct, new tests genuine.
- Two P2 doc nits folded IN-THREAD (recurring comment-doc-lie class): lib.rs:68-70 "Scope (M9)" reworded to distinguish the M9 check-embedded gate set (1+5) from the backend capability + the M10 bin runtime set (1/5/9); lib.rs irq_barrier "UNEXERCISED by examples 1 and 5" -> "by the naive single-worker examples (1, 5, 9)". Re-verified build/clippy/test green.
- P3-1 (commit-message test-count phrasing "27 tests / +8"): the embedded-pattern lib unit suite shows 27 passing in the gate run; the precise NEW delta is 2 #[test] fns. Architect counted 21 via `cargo test -p embedded-pattern`; figure is not load-bearing. Recorded for honesty (commit msg is immutable).

### Load-order (TASK-0048.06): ex5 + ex9 confirmed single-load (cursor consumes whole region from 0); ex1 remains the only multi-load case; .06 stays OPEN for future multi-load examples.

### Gotchas / rejected
- The raw-byte stream (since .02) is already example-agnostic, so generalising needed NO firmware-emit change for the output path — only the recipe + per-example generate inputs. AC#3 ("parameterise the NUC-EX1 prefix") was thus satisfied by .02 already removing the prefix.
- ex9 `stream` is an intermediate (produce writes, transform reads) declared as a fixed [i32;N] local alongside seeds+result; lowers fine.
- The save Fire drains only `result`, so dma_push streams exactly 64 bytes (== reference.bin); the byte-count guard catches any over/under-stream.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
M10 bin emit + Renode value-correctness generalised to the full PRD §11 M10 set (examples 1, 5, 9). One parameterised recipe `renode-embedded EX=<example>` (default ex1; tests/renode/embedded-ex1 -> embedded) generates each example firmware, cross-compiles thumbv7em, runs in Renode on real injected input.bin, and cmp BYTE-EXACT vs reference.bin. ex5 (2D blur3) + ex9 (two-stage produce/transform) both land value-correct; ex9 is the first example through embedded codegen beyond the M9 set and works. Both reviews GO; e2e baseline unchanged 280/246/0/34/0; M9 lib byte-untouched (only docs); +2 genuine drift-detector tests. Implementer was interrupted by an API error pre-commit; orchestrator verified the coherent tree (all gates green, all 3 examples byte-exact, ex5/ex9 deterministic x2) and committed/closed. AC#1 ex5 DONE, AC#2 ex9 single-worker confirmed + landed (no M11 defer needed — naive sched is single-host), AC#3 satisfied (raw stream is example-agnostic since .02), AC#4 M9 lib + ex1 path green. Honest limits: real STM32H7 DMA/IRQ shim still stub (parent AC#1); load-order assumption holds for single-load ex5/ex9, multi-load future = TASK-0048.06; Renode hardwires USART TXE=true so TX back-pressure unvalidated.
<!-- SECTION:FINAL_SUMMARY:END -->
