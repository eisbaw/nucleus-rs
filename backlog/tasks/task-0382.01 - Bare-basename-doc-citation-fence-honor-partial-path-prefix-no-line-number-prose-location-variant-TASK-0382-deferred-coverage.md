---
id: TASK-0382.01
title: >-
  Bare-basename doc-citation fence: honor partial-path prefix + no-line-number
  prose-location variant (TASK-0382 deferred coverage)
status: Done
assignee:
  - '@me'
created_date: '2026-05-31 04:04'
updated_date: '2026-05-31 21:48'
labels:
  - tooling
  - ci
  - doc-lie
  - robustness
  - cycle-221-followup
dependencies: []
parent_task_id: TASK-0382
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0382 cycle-221 landed check-doc-citation-staleness-bare: a crate-scoped, prose-aware resolver for bare <file>.rs:N citations (zero-FP, wired into just ci). Two SAFE coverage gaps were deferred (both currently fall to SKIP — neither can produce a false POSITIVE, they just go unvalidated):

(1) PARTIAL-PATH citations like multi_worker/mod.rs:N, sched/ir.rs:N, multi_worker_walker/event_walker.rs:N. The cycle-221 resolver discards the path prefix and resolves the BASENAME only, so partial-path forms hit SKIP:ambiguous (e.g. nucleus-compiler has algo/ir.rs AND sched/ir.rs) or SKIP:basename-not-in-crate (mod.rs find-name is ambiguous/none). A prefix-honouring resolver (resolve <prefix>/<base>.rs relative to <crate-root>/src/, then range-check) would recover this coverage with NO loss of zero-FP — the interior slash is MORE disambiguating, not less. Concrete uncaptured-but-valid in-tree examples: multi_worker/mod.rs:273-284 (mp-tcp-event tests), sched/ir.rs:382 (nucleus-compiler tests).

(2) BARE-BASENAME-AS-LOCATION with NO :N (TASK-0382 Implementation Notes variant). A prose claim like "pinned by bin_rejects_multi_worker_* in tests.rs" names a file but no line; the cycle-221 fence only sees <base>.rs:N forms, so it is structurally blind. Catching it needs symbol/test-name RESIDENCE checking (does the named test still live in the cited basename?), not a line-count. This is the lie-shape TASK-0383 split freshly produced and that was caught only by manual silent-sibling grep. Harder to make zero-FP (prose ambiguity about what is a symbol name); design carefully or keep deferred.

NEITHER gap is an FP risk on the current fence; this is purely additive coverage. Lower priority than keeping the fence zero-FP.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Land gap (1) PARTIAL-PATH citations in check-doc-citation-staleness-bare; defer gap (2) as its own follow-up task.

GAP (1) DESIGN (zero-FP preserved — every ambiguity SKIPs):
1. Widen capture + re-extract regex from [A-Za-z0-9_]+\.rs:N to [A-Za-z0-9_]+(/[A-Za-z0-9_]+)*\.rs:N — captures an optional interior-slash path prefix; no-prefix bare form is a zero-repetition match (byte-identical behaviour).
2. base=${cite%%:*} now holds the path-bearing token (e.g. sched/ir.rs). If it contains a slash, resolve by SUFFIX: find $croot -path */$base (0->SKIP not-in-crate; 1->range-check; >1->SKIP ambiguous). No slash -> unchanged -name resolution.
3. Cross-crate-prose guard (WIN=3) unchanged; suffix match is strictly MORE disambiguating than basename.

Segment class deliberately WITHOUT hyphen (matches task spec). Consequence: a cite whose FIRST segment is a hyphenated crate name (nucleus-compiler/src/...) is captured as compiler/src/... and SKIPs (segment-boundary mismatch under -path) — SAFE, just unvalidated; documented as honest limit.

RECOVERED coverage (in-tree, now VALIDATE): sched/ir.rs:382, sched/ir.rs:409, sched/parser.rs:573-575 (all in range). SAFE SKIPs: multi_worker/mod.rs:273-284 (file gone post TASK-0340.04 split -> 0 matches), driver/src/main.rs (prose-guard + not-in-citing-crate), acfg/build.rs cited in backend-common (not in croot), tests/emit.rs + tests/multi_worker.rs (prose-guard names pthreads-sync), compiler/src/* (segment mismatch).

VERIFY: temporary stale partial-path fixture (sched/ir.rs:999999) must FAIL with STALE; remove after. just check-doc-citation-staleness-bare must print OK on full tree (zero new FP). Full just ci CI_EXIT=0, e2e 385/328/0/57/0 byte-identical-unchanged (build-tooling-only).

GAP (2) (no-:N prose symbol-residence) filed as separate follow-up with dep edge on this task; update DEFERRED comment block to reflect gap(1) LANDED + new task id.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
LANDED gap (1) PARTIAL-PATH (cycle-221 follow-up). justfile check-doc-citation-staleness-bare:
- Regex (capture + re-extract) widened [A-Za-z0-9_]+\.rs:N -> [A-Za-z0-9_]+(/[A-Za-z0-9_]+)*\.rs:N. No-prefix bare form is a zero-repetition match (byte-identical). base=cite%%:* now holds the path-bearing token.
- Resolution: case base in */* -> find croot -path */base (SUFFIX); * -> find croot -name base (unchanged). 0->SKIP not-in-crate, 1->range-check, >1->SKIP ambiguous. WIN=3 cross-crate-prose guard unchanged.

ZERO-FP: full-tree just check-doc-citation-staleness-bare prints OK (no new STALE). Suffix is strictly MORE disambiguating than basename.

BITE PROVEN: temp fixture // sched/ir.rs:999999 appended to nucleus-compiler/src/trace.rs -> recipe FAILED EXIT=1 with: STALE (line past EOF): sched/ir.rs:999999 cited in ./nucleus/nucleus-compiler/src/trace.rs:125 -> resolves crate-scoped to ./nucleus/nucleus-compiler/src/sched/ir.rs (1164 lines); cited line 999999 is past EOF. Fixture removed (trace.rs zero diff vs HEAD), re-run OK.

COVERAGE RECOVERED (now VALIDATE, were SKIP:ambiguous before): sched/ir.rs:382 + sched/ir.rs:409 (->sched/ir.rs 1164 lines), sched/parser.rs:573-575 (->parser.rs 921), nucleus-compiler/src/passes/acfg_to_petri.rs:486-497 (->555), nucleus-compiler/src/algo/ir.rs:256-260 (->772). Traced through recipe logic: all reach range-check, all in range.

CORRECT SAFE SKIPS (no FP): multi_worker/mod.rs:273-284 + :286-297 -> 0-match (file GONE post TASK-0340.04 split; moved-file staleness OUT of mechanized scope by design); driver/src/main.rs:406 (cited in backend-common/pthreads-async; prose-guard names driver + not in citing croot); acfg/build.rs:276 (cited in backend-common; target is nucleus-compiler, not in croot); tests/emit.rs:79 + tests/multi_worker.rs:334 (prose-guard names pthreads-sync); compiler/src/* (segment-boundary mismatch under -path).

GAP (2) (no-:N prose symbol/test-residence) FILED as TASK-0382.02 (depends-on TASK-0382.01). DEFERRED comment block updated: gap(1) LANDED + points at TASK-0382.02; NO doc-lie remains.

GATE: full just ci CI_EXIT=0. REAL e2e 385/328/0/57/0 BYTE-IDENTICAL-UNCHANGED (build-tooling-only). FQ + bare fences both OK. All 4 negative/determinism arms bit correctly.

LIMITS/GOTCHAS: (a) segment class [A-Za-z0-9_]+ (no hyphen, per spec) -> hyphenated FIRST segment (nucleus-compiler/src/...) captured as trailing run (compiler/src/...) and SKIPs on segment-boundary mismatch; SAFE but loses validation of those 2 cites; documented as honest CAVEAT in recipe. (b) find -path *//base requires the path to contain /<prefix>/<base> with segment boundaries; partial-segment crate aliases never collide. (c) POSIX-sh: cite token contains no glob chars so rest=rec%:cite is a literal strip; -path glob is single-quoted in the recipe. (d) The mp-tcp-event/mp-uds-event multi_worker/mod.rs:N cites are now STALE-by-move (file restructured) but correctly SKIP; if re-anchoring is wanted that is a separate doc-hygiene call (NOT silently rewritten here per instruction).
<!-- SECTION:NOTES:END -->
