---
id: TASK-0049.05
title: >-
  M11 backend slice B: embedded-pattern multi-MCU BIN + Renode multi-machine
  .resc + real UART-hub transport shim
status: Done
assignee:
  - Mark
created_date: '2026-05-29 06:57'
updated_date: '2026-05-31 11:07'
labels: []
dependencies: []
parent_task_id: TASK-0049
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Follow-up to the slice-A lib emit. Generalise emit_bin (single-worker, lib.rs:273) to emit N firmware bins (one per worker/MCU) + a multi-machine .resc wiring them on an emulation CreateUARTHub, mirroring tests/renode/multimcu-uart-smoke/. Replace the stub transport with a real UART-hub shim: Push -> TX over USART1 (poll TXE, write TDR); Wait -> RX poll-on-RXNE + read RDR; Sync -> irq_barrier. MUST emit the receiver-RX-enabled-before-sender-TX start-gating discipline from TASK-0049.01 (UARTBase.WriteChar drops pre-RX-enable bytes) in the generated .resc. Then value-correctness (bit-identical vs tier-1 reference) — gated on TASK-0049.03 (harness schedule-for) + TASK-0049.02 (stateful kernels).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Forward-carried from TASK-0049.04 (M11 backend slice A, LIB path DONE) ===

The STRUCTURAL multi-worker emit landed on the LIB path. The BIN slice (this task) inherits these shapes — do NOT rediscover them:

1. TRANSPORT LOWERING SHAPE (already implemented in render_event for the lib; the bin shares lower_kernels_and_run/render_run_body, so the SAME run-body lowering applies once the bin guard lifts):
   - Event::Push { data, seq } -> shim.dma_push(seq, <data>.as_ptr() as *const u8, core::mem::size_of_val(&<data>))  [mirrors the save->dma_push template]
   - Event::Wait { data, seq } -> shim.dma_wait(seq)  [data received into a zero-init receive LOCAL]
   - Event::Sync { sync } -> shim.irq_barrier(sync as u32)
   chan = SeqTag.0 (Push/Wait pair shares seq), tag = SyncTag.0 (matches across participants). NO channel allocator — straight from the events.

2. RECEIVE-LOCAL MECHANISM: collect_referenced_data (lib.rs) now includes Push AND Wait data ids, so the run-body data decls declare a zero-init fixed [T;N] local for every Waited (and Pushed) symbol — the receiving worker has the symbol in scope WITHOUT loading it. For the BIN with a REAL Usart1-hub shim, dma_wait must actually poll RX + fill that local (the lib StubShim no-ops it, so zero-init is honest only for compile-only). The local is already declared; the bin shim just needs to populate it.

3. N-PROJECTS LAYOUT: the lib emits one project per used worker (single -> out_dir root; multi -> out_dir/<worker_name>/, name from NameTables.worker). The bin must mirror this (N firmware bins, one per MCU class), PLUS the .resc wiring them on a CreateUARTHub. BinEmitResult will need the same N-projects refactor emit() got (currently emit_bin returns a single BinEmitResult; multi-worker needs Vec).

4. THE BIN GUARD IS STILL UP (emit_bin lib.rs:~273): retained deliberately + pinned by bin_rejects_multi_worker_with_m11_forward_link. Lifting it is THIS task. Its message already forward-links here (TASK-0049.05).

5. BLOCKERS THE LIB SLICE HIT (and the bin will too) — see TASK-0049.06: the REAL example-14 embedded_multimcu schedule is rejected at check_schedule_compat (async+buffer=3+notify=event + heterogeneous worker classes + sram_shared/dsp_tcm regions) and its mix2/denoise have Vec<i32> bodies + fe_*/rf_* have no kernels.rs bodies. The bin slice will need (A) the synchronous-vs-async transport + worker-class capability decision and (B) no_std-clean per-frame kernels resolved BEFORE ex14 firmware can build. The UART-hub start-gating discipline (receiver RX-enabled before any sender TX — TASK-0049.01) is the cross-MCU analogue of the Sync barrier and MUST be emitted in the generated .resc.

6. EMPIRICAL-VERIFICATION LESSON: TASK-0192 proved lower/link/acfg admit ex14 via lower_for_test (IR passes only); that did NOT exercise the driver-level capability gate. The bin slice MUST test through the real driver path (or check_schedule_compat directly), not just lower_for_test, or it will repeat the slice-A surprise.

=== Forward-carried from TASK-0049.04 slice-A review (architect P2 — bin slice MUST handle) ===
Two latent transport-correctness traps that are INERT against the compile-only StubShim (so slice A correctly left them) but BITE under a real UART-hub shim:
1. DMA channel-id namespace COLLISION. The effectful load/save Fires hardcode chan=0 (embedded-pattern lib.rs ~:1021/1024/1051/1054) while cross-worker Push/Wait derive chan from SeqTag. In the generated host run(), channel 0 simultaneously carries load_input dma_wait(0), the Push of seq-0 data, AND save_output dma_push(0)+dma_wait(0) — three unrelated transfers on one channel. Against the no-op StubShim this is inert + compiles; under a real shim it is NOT a faithful encoding. The bin slice needs a channel ALLOCATOR or a disjoint namespace separating effectful-IO channels from cross-worker-transport channels.
2. SyncTag -> u32 TRUNCATION. The lowering emits `shim.irq_barrier({tag} as u32)` casting a u64 SyncTag to u32. Harmless for small monotonic tags today; a silent truncation surface for the bin/real shim — widen the shim tag type or guarantee the tag range.
Also carry the slice-A receive-local mechanism (Wait declares a zero-init fixed-[T;N] local then dma_wait; real shim must actually fill it via RX) + the TASK-0049.01 receiver-RX-enabled-before-sender-TX start-gating discipline the generated .resc MUST emit.

Forward-carried from TASK-0049.07/0363 (cycle): (1) [from 0049.07] The no_std sub-array Fire-arg lowering's array length comes from TWO independent sidecar tables — sub_len from sidecar.data_types (data trailing dims), N from sidecar.kernel_sigs (kernel-sig [T;N] param) — with NO contract/link cross-validation. render_fire_arg now fails LOUD (EmitError::ContractGap) at emit time when they disagree, but only for FixedArray (no_std). The deep BIN slice's cross-MCU UART-hub transport carries the SAME two-table shape-agreement assumption across the wire: the sender's sub_len and the receiver kernel-sig N must match, and that is still NOT checked at contract/link level — only per-worker at codegen. Carry a transport-shape agreement check (or at least the same emit-time fail-loud) into the multi-machine .resc / UART-hub shim. (2) [from 0363] Per-worker emitted projects are cargo-built (crate name from Cargo.toml) so the dotted-stem rustc crate-name bug does NOT affect them — but the --kernels file feeding the contract check IS dotted (kernels.embedded.rs); rustc_check now sanitises --crate-name so phase-1 compile-check actually runs for that file (previously always-failed = a silent erosion of the syntax safety-net). If the BIN slice introduces a NEW direct rustc invocation deriving a crate name from any dotted path, sanitise it the same way (sanitise_crate_name in contract.rs).

## Forward-carried from TASK-0049.03 (harness schedule-for resolver, landed 2026-05-29)

The e2e harness now resolves each cell's algorithm from the schedule's `schedule for "<path>"` directive via `resolve_algo_path(sched_path)` in e2e/src/main.rs (both run_cell ~1371 and check_cell_determinism ~1870). embedded_multimcu*.sched.nuc point at ../prog.embedded.algo.nuc, so once the embedded BIN cells are promoted from [[skip]] they will AUTOMATICALLY drive prog.embedded.algo.nuc — no further harness change needed for algo selection.

GOTCHAS this BIN slice must still clear before promoting any embedded cell (the harness capability alone is NOT sufficient):
1. PER-CELL kernels-file override is STILL MISSING. The harness hardcodes ex_dir/kernels.rs; embedded_multimcu_sync uses kernels.embedded.rs (NOT kernels.rs). Filed as the sibling prereq TASK-0049.08 (see TASK-0049.03 final summary). resolve_algo_path solved ALGO selection only — kernels selection is the structurally-identical untouched sibling.
2. resolution base = sched file's PARENT dir; NOT canonicalised (the '..'-bearing path ex_dir/schedules/../prog.embedded.algo.nuc flows through fine — matches the downstream existence check + nucleus build today). Do not add canonicalize; it would fail on not-yet-existing targets.
3. determinism-mode reports a malformed sched as Skipped (not Failed) — mirrors that site's missing-fixture convention. run_cell reports Failed(Compile). If the BIN slice wants embedded cells in determinism mode too, that asymmetry is by-design, not a bug.

Forward-carried from TASK-0049.08 (cycle): the e2e harness PER-CELL FIXTURE-SELECTION capability is now COMPLETE — both halves landed. ALGO selection = resolve_algo_path (TASK-0049.03); KERNELS selection = kernels_filename_for_algo (TASK-0049.08, this cycle). When you promote an embedded_multimcu / embedded_multimcu_sync cell from [[skip]] to drive ex14 end-to-end, the harness will AUTOMATICALLY: (1) resolve algo from the sched's 'schedule for "../prog.embedded.algo.nuc"' directive, and (2) derive kernels.embedded.rs from that algo's variant. NO further harness fixture-selection work needed for that. GOTCHA: kernels_filename_for_algo is convention-driven (prog<variant>.algo.nuc <-> kernels<variant>.rs) and FALLS BACK to kernels.rs for off-convention names rather than failing loud — so if ex14's embedded sched ever points at an algo whose stem is NOT prog-prefixed, you'd silently get kernels.rs and the fixture-existence check would catch it only if kernels.rs is absent (it isn't for ex14). Keep the embedded algo named prog.embedded.algo.nuc so the convention holds. Both halves are proven by pure unit tests ONLY; the FIRST true end-to-end exercise of the embedded fixture pair is YOUR task — verify both files are actually selected (e.g. assert the emitted crate compiled against kernels.embedded.rs, not just that the cell PASSed).

ORCHESTRATOR TRIAGE (2026-05-31, post-M8/TASK-0046 cycle): toolchain CONFIRMED AVAILABLE — .#renode shell gives renode/renode-test 1.16.1 (cached in nix store), .#embedded gives the thumbv7em cross-compile, and the tests/renode/multimcu-uart-smoke/ fixture (run.resc + src + memory.x + build.rs) exists as the multi-machine CreateUARTHub template to mirror. So this task is NOT env-blocked; it is a deep pure-implementation slice (N firmware bins + multi-machine .resc + real UART-hub shim) carrying the enumerated transport-correctness traps (DMA chan-id collision, SyncTag->u32 truncation, transport-shape two-table agreement, receiver-RX-before-sender-TX start-gating). DELIBERATELY NOT STARTED this cycle: it warrants a fresh context (the M8 cycle that preceded it was a large tier-2 MPI build + review). Recommended first step next cycle: read embedded-pattern/src/lib.rs emit_bin (~:273 guard) + render_run_body + the multimcu-uart-smoke run.resc, then de-risk via the REAL driver path (check_schedule_compat), NOT lower_for_test (the slice-A surprise lesson). Value-correctness for ex14 also needs TASK-0049.06 capability + no_std-kernel resolution.

IMPL PLAN (cycle 2026-05-31, orchestrator in-thread; fresh context). De-risk done by reading the emitted multi-worker LIBs (target/embedded-m11/02-split-add/{host,w0}) + stm32h743.repl (9 UARTs modelled: usart1/2/3/6, uart4/5/7/8, lpuart1).

FINDINGS:
- 02-split-add/split topology: host Pushes a(seq0),b(seq1)->w0; w0 computes c=add(a,b); w0 Pushes c(seq2)->host; host saves c. Barriers tag0/tag1(x256 in-loop)/tag2 are CONTROL-only — every data dep is carried by a Push/Wait edge, so with a BLOCKING-RX Wait the barriers are safe no-ops (to be VERIFIED empirically by the byte-exact Renode diff, not assumed).
- Trap#1 (chan collision) CONFIRMED real: host save_output lowers to dma_push(0) and host Push a(seq0) ALSO lowers to dma_push(0) — one channel namespace cannot route IO-stream vs cross-worker-send to different USARTs. FIX (structural): split the trait namespace — transport Push/Wait -> link_push(seq)/link_recv(seq); effectful IO stays dma_push(0)/alloc_in_region (unchanged, so M9 LIB + M10 single-worker bin byte-stable).
- Renode UARTHub is BROADCAST -> one hub per worker-PAIR; seq->USART is per-peer (all host<->w0 seqs multiplex one host.usartK<->w0.usartK hub, FIFO per direction, opposite directions temporally disjoint here). Reserve usart1 for the host effectful-output capture stream; transport USARTs from usart2.. pool.
- Start-gating generalises the smoke fixture for DAGs: each MCU enables RX on all its receive USARTs FIRST; .resc boots receivers-before-early-senders (w0 then host). Cyclic topologies need a real handshake (filed follow-up if hit).

STEPS:
A. Trait namespace split: add link_push/link_recv + widen Sync barrier tag to u64 (trap#2 SyncTag->u32). render.rs Push->link_push, Wait->link_recv (BLOCKING fill), Sync->barrier(u64). Effectful load/save arms UNCHANGED. Update LIB multi-worker + ex14-sync tests to assert link_*; verify M9/M10 single-worker byte-stable.
B. emit_bin -> MultiBinEmitResult{workers:Vec} + N-projects layout (single->root unchanged; multi->out_dir/<worker>/) + guard lift + driver wiring + bin_shape test (guard-pin -> N-bin topology-pin). Verify N bins cross-compile under .#embedded (02-split-add -> 2 bins).
C. Real multi-MCU shim: per-seq->USART table (computed from global seq->(src,dst) peer map), link_push=TX(poll TXE,write TDR), link_recv=BLOCKING RX(poll RXNE,read RDR, fill size_of_val bytes), barrier=no-op(documented). Generate multi-machine .resc (CreateUARTHub per peer-pair + Connect + receivers-first boot gating + SetGlobalQuantum + RunFor). just renode-multimcu recipe: cross-compile N bins, inject host input.bin, run, cmp BYTE-EXACT vs reference.bin (02-split-add).

Verify each: nix develop --command bash -c "just build && just clippy && just test && just test-release"; cross-compile under .#embedded; Renode run under .#renode. If C blocks (gating/barrier/framing), A+B are honest progress; file C blocker precisely. ex14 value-correctness stays gated on TASK-0049.02.

CYCLE OUTCOME (2026-05-31, commits e8ae76d + 0f4c4f7, review GOx2). DELIVERED + VERIFIED:
- emit_bin -> MultiBinEmitResult: ONE Renode-runnable no_std firmware per worker (single->root w/ Usart1Shim UNCHANGED M10; multi->out_dir/<worker>/ w/ concrete MultiMcuShim) + generated out_dir/multimcu.resc. Driver wiring + N-projects layout mirror emit().
- REAL UART-hub transport: link_push->USART TX (poll TXE, write TDR), link_recv->BLOCKING RX (poll RXNE, read RDR, fill receive local). Sync->irq_barrier (no-op; see below).
- Trap#1 (chan collision) FIXED structurally: link_push/link_recv are a DISTINCT trait namespace from effectful dma_push(0)/alloc_in_region, so host save_output (USART1 stream) and host Push a (usart2) route to different USARTs.
- Trap#2 (SyncTag->u32 truncation) FIXED: irq_barrier tag widened u32->u64, no cast.
- multimcu::TransportPlan = single source of truth for shim + .resc: one UARTHub per worker-pair (broadcast-bus-safe), seq->USART per peer (usart1 reserved for output capture), receivers-first boot order = the RX-before-TX start-gating (TASK-0049.01) holding BY CONSTRUCTION for a pipeline DAG.
- VALUE-CORRECTNESS PROVEN: just renode-multimcu cross-compiles host+w0 under .#embedded, co-simulates 2 STM32H7 machines over a UARTHub under .#renode, captured USART1 BYTE-EXACT identical to 02-split-add reference.bin (1024 bytes). Reproduced x2 by qa-test-runner. Gate: build/clippy/test(1191)/test-release(1190)/e2e 350/293/0/57/0 (unchanged); embedded-pattern 27 tests; 02-split-add LIB cross-compile green.

DEFERRED (filed):
- ex14 value-correctness: gated on TASK-0049.02 (stateful per-frame kernels) AND blocked by TASK-0049.09 (PRE-EXISTING ex14 LIB accumulator-crosscheck regression from TASK-0343.03 tightening, reproduced on clean master, post-dates 0049.06 Done).
- TASK-0049.05.01: fail-loud guard for a control-only Event::Sync NOT subsumed by a data edge (the no-op barrier is VERIFIED-correct for 02-split-add where Syncs are data-edge-redundant, but unconditional; a non-redundant barrier would silently miscompile — mitigated today because the tier-3 acceptance is always the byte-exact diff).
- TASK-0049.05.02: transport-faithfulness residuals (cyclic/non-DAG receivers-first handshake + transport-shape two-table agreement across the wire + multi-input .resc injection).
- TASK-0388: check-mega-files scope omits nucleus/driver/src (main.rs 1242 LoC).

GOTCHAS for the next session:
- Renode UARTHub is BROADCAST (delivers each TX byte to all OTHER connected UARTs, not the sender) -> one hub per worker-pair is the collision-free unit; opposite directions on one hub must not temporally overlap (they do not on these DAG pipelines).
- The no-op barrier is the load-bearing simplification: it is correct ONLY because every cross-worker DATA dep is carried by a BLOCKING link_recv. Do NOT assume it for a schedule with standalone control barriers (TASK-0049.05.01).
- check-embedded MUST be run as `nix develop .#embedded --command just check-embedded` (it cross-compiles DIRECTLY; the thumbv7em std is only in .#embedded). renode-multimcu enters .#embedded internally so it runs from the default shell.
- ex14 LIB is RED at the nucleus build step (transfer_inject accumulator cross-check), NOT cross-compile (TASK-0049.09).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE (2026-05-31, e8ae76d + 0f4c4f7, review GOx2). The M11 multi-MCU embedded BIN machinery is complete and value-correct end-to-end: emit_bin emits N Renode-runnable no_std firmwares + a generated multi-machine .resc with a REAL UART-hub transport shim (link_push=USART TX, link_recv=blocking RX, collision-free per-peer hub/USART allocation, receivers-first start-gating). PROVEN by just renode-multimcu: the 02-split-add 2-worker schedule co-simulates as 2 STM32H7 machines over a UARTHub and the captured USART1 output is BYTE-EXACT vs reference.bin (1024 bytes, reproduced x2). Both transport-correctness traps fixed (chan-collision via the link_* namespace split; SyncTag u32->u64). ex14-specific value-correctness remains GATED on TASK-0049.02 (stateful kernels) + TASK-0049.09 (pre-existing ex14 LIB accumulator regression). Residual faithfulness/soundness work tracked as children TASK-0049.05.01 (control-only-Sync fail-loud guard) + TASK-0049.05.02 (cyclic-topology gating + transport-shape agreement). Single-worker M10 path byte-stable.
<!-- SECTION:FINAL_SUMMARY:END -->
