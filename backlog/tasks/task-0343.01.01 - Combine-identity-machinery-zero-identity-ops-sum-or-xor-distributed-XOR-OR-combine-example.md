---
id: TASK-0343.01.01
title: >-
  Combine-identity machinery + zero-identity ops (sum/or/xor) + distributed
  XOR/OR-combine example
status: Done
assignee:
  - '@mark'
created_date: '2026-06-05 03:57'
updated_date: '2026-06-05 05:22'
labels:
  - compiler
  - M6
  - accumulator
  - codegen
  - grammar
dependencies: []
parent_task_id: TASK-0343.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Land the first, low-regression slice of the combine-identity generalization (parent TASK-0343.01). Today the overlapping-write accumulator fan-in combine is HARDCODED to wrapping_add (sum) in backend-common multi_worker_walker/wait.rs::render_accumulate_assign / accumulate_op_for_scalar. This slice makes the combine identity DECLARED by a kernel attribute and threads it to codegen for the THREE algebraic identities that share additive-identity ZERO (sum, or, xor) — so the existing zero-init (render_array_init_for / rust_scalar_zero, duplicated in backend-common render/types.rs + openmp-rs + mpi_plan + pthreads-sync) needs NO change. The non-zero-identity ops (min->MAX, max->MIN, and->all-ones) require identity-aware init across those ~4 sites and are DEFERRED to TASK-0343.01.02.

NEW codegen/IR surface (the depth this exercises): a 'combine=<op>' kernel attribute end-to-end (grammar + AST + parser + sidecar) plus NEW emitted code — the or arm emits 'name[_k] = name[_k] | _tmp[_k];' and xor 'name[_k] = name[_k] ^ _tmp[_k];' (operator form), distinct from sum's '.wrapping_add(...)' method form. Plus a SOUNDNESS gate: make the accumulate-fan-in reject STRICT — an accumulate fan-in whose owning kernel declares no combine identity is EmitError (today the sum-only carve-out silently assumes sum).

GRAMMAR GOTCHA (verified 2026-06-05): 'combine' is ALREADY a kernel identifier in 03-reduction and 23-dot-product. The attribute keyword MUST be contextual (positional, after purity) and MUST NOT be added to algo/tokens.rs reserved words, or those examples break. Kernel-name position (after 'kernel') and attribute position (after purity, before ';') never overlap, so contextual parse is unambiguous.

DETt-deterministic note: or and xor are associative+commutative -> order-independent across worker arrival -> bit-identical (PRD 10.1), strengthening the determinism story vs float sum.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Grammar+AST+parser: optional contextual 'combine = <sum|or|xor>' attribute on a kernel decl; KernelDecl carries Option<CombineOp>; 'combine' is NOT a reserved word (03-reduction & 23-dot-product still parse, kernel named 'combine' still legal)
- [ ] #2 Sidecar: NameSidecar carries a DataId->CombineOp map (or kernel->combine resolvable to the accumulator data) computed at sidecar build from the accumulator's Dataflow RHS kernel; render_accumulate_assign consults it instead of hardcoding wrapping_add
- [ ] #3 Codegen: or emits element-wise '|' , xor '^', sum unchanged '.wrapping_add(...)'; verified by a backend-common unit test asserting the emitted string for each op
- [ ] #4 SOUNDNESS reject: check_accumulator_consistency (driver gate.rs) fails loud (EmitError) when an accumulate-fan-in accumulator's owning kernel declares NO combine identity; negative test proves it bites; existing histogram kernels get combine=sum (ALL fan-in accumulator kernels: 08-histogram bin_inc + scatter/textbook inc, 19-histogram-unconstrained inc — grep every site, silent-sibling)
- [ ] #5 New example (e.g. 25-bin-parity XOR or 25-bin-union OR) with a distributed schedule mirroring 08-histogram/distributed (i-band partition, whole-array replicate, host element-wise combine); bit-identical across ALL 7 tier-1 backends; +7 e2e required cells registered in e2e-matrix.toml
- [ ] #6 Full gate green: nix develop -c 'just build && just clippy && just test && just test-release && just e2e'; e2e baseline 441/378/0/63/0 PRESERVED on existing cells and grows by the new example's cells; counts recorded from an actually-observed run (not implementer-claimed)
- [ ] #7 min/max/and (non-zero combine identity + identity-aware init) explicitly DEFERRED to TASK-0343.01.02 and rejected with a typed EmitError pointing there; no silent fallthrough
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPLEMENTATION PLAN (in progress, mark):
1. AST: add CombineOp{Sum,Or,Xor} + Option<CombineOp> on KernelDecl (ast.rs). [done]
2. Parser: contextual `combine = <op>` after purity; min/max/and rejected pointing TASK-0343.01.02; unknown rejected; combine stays a legal kernel NAME (not reserved). [done]
3. lower.rs -> ResolvedKernel.combine; ir.rs PartialEq incl combine. [done]
4. sidecar: KernelSig.combine + NameSidecar.combine_for_data (DataId->CombineOp) built from each accumulator Dataflow-RHS callee kernel combine attr. [done]
5. wait.rs render_accumulate_assign consults combine_for_data: sum->.wrapping_add (method), or->| , xor->^ (operator); absent => fail-loud ContractGap. [done]
6. driver gate check_accumulator_consistency: strict reject when detected accumulator has no combine_for_data entry. [done]
7. histogram kernels get combine=sum (08 bin_inc + scatter/textbook inc + 19 textbook inc) — wrapping_add => byte-identical. [done]
8. New example 25-bin-parity (XOR-combine) naive+distributed, +7 e2e required cells. [pending]
9. Tests: parser (sum/or/xor/min/max/and/unknown/combine-name), render per-op strings, gate negative (no-combine reject), proptest serde roundtrip. [done except example e2e]
DECISION: only sum|or|xor accepted in parser; min/max/and rejected at parse with TASK-0343.01.02 pointer (no enum variant) — simplest no-silent-fallthrough.

IMPLEMENTATION COMPLETE (mark, pending orchestrator review-gate + Done flip).

Commits (5):
- 2ba0d9c grammar: contextual combine=<sum|or|xor> attr (AST/parser/lower/ir/mod)
- ee383a8 sidecar: KernelSig.combine + NameSidecar.combine_for_data
- 2e1d062 codegen: thread combine to accumulate emit + strict gate + histogram combine=sum
- 737a666 example: 25-bin-parity distributed XOR-combine vertical (+7 required cells)
- 26bade3 refactor: split sidecar collectors sub-module (sidecar.rs 1190->924, under mega-fence)

PER-AC STATUS:
#1 Grammar+AST+parser: MET. CombineOp{Sum,Or,Xor} + Option<CombineOp> on KernelDecl; contextual keyword (not reserved); 03-reduction/23-dot-product parse + e2e PASS with kernel named combine. Tests: combine_attribute_sum_or_xor_parse, combine_named_kernel_still_legal_without_attribute, parses_example_03_reduction (combine field None assertions).
#2 Sidecar combine_for_data + render consult: MET. DataId->CombineOp built from each accumulator Dataflow-RHS callee kernel combine attr; render_accumulate_assign consults it (no hardcoded wrapping_add).
#3 Codegen per-op emit: MET. sum->.wrapping_add (method), or->| , xor->^ (operator); array+scalar. Tests: accumulate_emit_array_op_strings_per_combine_op, accumulate_emit_scalar_op_strings_per_combine_op.
#4 SOUNDNESS reject: MET. check_accumulator_consistency + render both fail loud when detected accumulator has no combine_for_data entry. Negative tests: accumulator_rejects_lhs_in_rhs_accumulator_without_combine_identity (gate), accumulate_emit_no_combine_declared_returns_contract_gap (render). All histogram fan-in kernels got combine=sum (08 bin_inc + scatter/textbook inc + 19 textbook inc — grep-swept).
#5 New example bit-identical 7 backends +7 cells: MET. 25-bin-parity distributed XOR-combine; e2e PASS all 7 tier-1; emitted `parity[_k] = parity[_k] ^ _tmp[_k]` verified across all 7 transport shims; output==reference.bin (parity = 08-histogram counts mod2, cross-checked). +7 required distributed cells.
#6 Full gate green + baseline: MET (with caveat). cheap subset (build/clippy/test/test-release) GREEN; e2e ACTUAL 455/392/0/63/0 (was 441/378/0/63/0; +14 = 7 required distributed + 7 auto-discovered naive; required-fail 0; all existing cells preserved). CAVEAT: just check-mega-files RED on embedded-pattern/src/multimcu.rs (1478 LoC) — PRE-EXISTING (1478 at baseline, unrelated to this task; the cheap subset is blind to it per memory feedback-cheap-subset-blind-to-structural-fences). My change REMOVED sidecar.rs as an offender (split to collectors sub-module). Filed multimcu.rs as TASK-0450.
#7 min/max/and deferred to TASK-0343.01.02: MET. Rejected at parser with TASK-0343.01.02 pointer (no silent fallthrough); TASK-0343.01.02 already exists. Tests: combine_attribute_min_max_and_rejected_pointing_at_followup.

GOTCHAS HIT:
- proptest_serde sidecar_field_completeness_guard + namesidecar_strategy needed the new field (break-to-update teeth fired as designed).
- ~15 test fixtures construct sidecar::KernelSig literally — all needed combine:None (silent-sibling swept via grep of purity-bearing literals; one had purity as a var not Purity::Pure, missed by the regex, fixed manually).
- build_sidecar combine_for_data growth pushed sidecar.rs over the mega-fence -> split required (collectors sub-module).
- pre-existing mega-file RED (multimcu.rs) is the cheap-subset-blind fence; filed TASK-0450.

NOT DONE (deliberate): min/max/and (TASK-0343.01.02, already filed); multimcu.rs split (TASK-0450, filed). Did NOT flip task Done / write final-summary — left for orchestrator review-gate.

ORCHESTRATOR REVIEW GATE (independent, parallel qa-test-runner + mped-architect) — GO/GO. Numbers verified by the gate, NOT implementer-claimed: e2e 455/392/0/63/0 reproduced 3x (qa 2x + architect 1x) non-flake, required-fail 0, baseline 441/378/0/63/0 preserved (+14 = 7 distributed + 7 naive 25-bin-parity); just test 1387/0 (dev), test-release 1385/0 (load-bearing 2-test should_panic delta exercised); clippy clean -D warnings (re-run); 25-bin-parity/distributed byte-identical across all 7 tier-1 backends sha256 84c93922abbe1636... == reference.bin (architect also recomputed reference.bin from scratch in Python, matches). Soundness reject bites in BOTH gate (check_accumulator_consistency, AccumulatorShapeMismatch) and render (render_accumulate_assign, ContractGap) with negative tests; silent-sibling sweep COMPLETE (08 bin_inc + 08-scatter/textbook inc + 19 inc got combine=sum; all other accumulators are disjoint-slice partitions correctly exempt — zero false rejects); contextual combine keyword (not reserved) verified, 03-reduction/23-dot-product still parse; min/max/and rejected at parser pointing TASK-0343.01.02. Commits 2ba0d9c ee383a8 2e1d062 737a666 26bade3 70974d3. KNOWN: just check-mega-files RED on embedded-pattern/src/multimcu.rs (1478 LoC) — PRE-EXISTING (RED at base 376fb8c), unrelated, filed TASK-0450; this cycle was net-positive on the fence (sidecar.rs 1190->924).
<!-- SECTION:NOTES:END -->
