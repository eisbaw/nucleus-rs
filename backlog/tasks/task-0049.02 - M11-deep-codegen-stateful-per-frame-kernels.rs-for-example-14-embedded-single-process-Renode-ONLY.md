---
id: TASK-0049.02
title: >-
  M11 deep-codegen: stateful per-frame kernels.rs for example 14 embedded
  (single-process/Renode ONLY)
status: To Do
assignee: []
created_date: '2026-05-29 06:00'
updated_date: '2026-05-31 11:09'
labels:
  - M11
  - examples
  - backend
dependencies: []
parent_task_id: TASK-0049
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0054.01 part 2 (deferred deep-codegen). The per-frame embedded algorithm (nuc-nucleus/examples/14-hearing-aid/prog.embedded.algo.nuc, landed TASK-0054.01) declares fe_capture/rf_receive/fe_emit/rf_transmit but has NO Rust bodies. This task writes a stateful kernels.rs (or kernels.embedded.rs): fe_capture/rf_receive track a per-kernel call cursor (AtomicUsize) reading frame[cursor] from canned bins; fe_emit/rf_transmit write frame[cursor] to accumulating buffers flushed on Drop (tier-1) or at program-exit (tier-3 bin, no Drop on a spinning firmware — see TASK-0048.08 note). SAFE ONLY in single-process M11/Renode mode: a shared call cursor breaks the multi-process tier-1 backends (mp-tcp-bufsync gives each process its own kernels.rs instance with no shared cursor). Front-end (sched_lower/link/acfg) already admits the schedule (TASK-0192). Depends on: e2e-harness algo selection (sibling task) so the build can pick prog.embedded.algo.nuc.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Forward-carried from TASK-0049.03 (landed 2026-05-29)

The e2e harness can now drive a per-cell non-default algorithm (resolve_algo_path honours `schedule for "<path>"`; embedded_multimcu*.sched.nuc -> ../prog.embedded.algo.nuc). So once stateful kernels land, the algo-selection half of getting embedded cells to RUN is already done. REMAINING harness gap for embedded promotion: per-cell kernels-file override (kernels.embedded.rs vs the hardcoded kernels.rs) — filed as TASK-0049.08. The harness still hardcodes ex_dir/kernels.rs.

Forward-carried from TASK-0049.05 (M11 BIN slice, DONE + value-correct 2026-05-31): the multi-MCU BIN machinery is now COMPLETE and proven value-correct end-to-end for 02-split-add (just renode-multimcu => 2 co-simulated STM32H7 machines over a UARTHub, captured USART1 BYTE-EXACT vs reference.bin). So once THIS task lands stateful per-frame kernels AND TASK-0049.09 (the pre-existing ex14 LIB accumulator-crosscheck regression) is resolved, ex14 multi-MCU value-correctness should be reachable via `just renode-multimcu 14-hearing-aid embedded_multimcu_sync` with NO further BIN-codegen work — the emit_bin N-bin path, the real link_push/link_recv UART transport, the per-peer UARTHub allocation, and the receivers-first start-gating are all in place. CAVEAT to verify when you get there: ex14 has 3 workers (fe/dsp/rf) => the receivers-first boot-order heuristic + the per-peer USART allocation will exercise paths 02-split-add (2 workers, single peer) did NOT; and if any ex14 Event::Sync is a control-only barrier NOT subsumed by a data edge, the no-op irq_barrier would miscompile (TASK-0049.05.01) — the byte-exact diff is the safety net, so a mismatch will fail loud rather than silently pass.
<!-- SECTION:NOTES:END -->
