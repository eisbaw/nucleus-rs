---
id: TASK-0044.02
title: >-
  M6 sub-task: mp-tcp-poll backend crate (OS processes + TCP + nonblocking poll,
  sync)
status: In Progress
assignee:
  - '@orchestrator'
created_date: '2026-05-26 09:24'
updated_date: '2026-05-27 02:06'
labels:
  - M6
  - backend
dependencies:
  - TASK-0044.02.01
parent_task_id: TASK-0044
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land the sixth tier-1 backend crate `nucleus/backends/mp-tcp-poll`: workers are OS processes, transport is TCP loopback, notify is nonblocking poll, no buffer (sync, single-frame in flight). PRD §7.1 row 5.

This is a SYNC-side sibling of mp-tcp-bufsync: same multi-process + TCP-loopback substrate, but where mp-tcp-bufsync uses BLOCKING recv to wait, mp-tcp-poll uses NONBLOCKING I/O + a busy/yield poll loop. Capability-wise they look identical (both reject async/buffer/event); the SHIPPED RUNTIME differs only in the wait primitive. This matters for the milestone story: it demonstrates the capability matrix is genuinely orthogonal — same sync capability surface, two different I/O strategies.

## Closest template

`nucleus/backends/mp-tcp-bufsync` is the structural template:
- Same multi-binary output (per-worker bins + wire.rs + run.sh).
- Same `nucleus/mp-tcp-common` consumer (wire framing, port-picker, socket-option setting).
- Same emit() signature returning the per-worker bin list.

The diff is the wait primitive: where bufsync calls `wire::read_msg_expect()` blocking, mp-tcp-poll calls a nonblocking read in a `loop { match read { WouldBlock => std::thread::yield_now(), ... } }` loop. The wire framing + port-picker + run.sh + SO_*BUF setup all unchanged.

## Capabilities

```toml
schema_version  = 1
name            = \"mp-tcp-poll\"
tier            = 1
transport       = \"tcp\"
notify          = [\"poll\", \"barrier\", \"blocking\"]
supports_async  = false
supports_buffer = false
max_buffer      = 1
worker_classes  = [\"default\"]
memory_regions  = [\"heap\"]
```

(Note: 'poll' is added to notify — distinguishes from 'event' which means mio/epoll readiness. Decide whether capability-compat needs to treat 'poll' as a superset of 'blocking' or as a sibling; mp-tcp-bufsync currently accepts notify=[barrier, blocking] for the same schedule set.)

## Acceptance criteria

1. `nucleus/backends/mp-tcp-poll/` crate exists with Cargo.toml + src/lib.rs + capabilities.toml.
2. `mp_tcp_poll::emit(...)` returns the same EmitResult shape as mp-tcp-bufsync (project_dir / cargo_toml / worker_bins / kernels_rs / wire_rs / run_sh).
3. Driver dispatch arm in `nucleus/driver/src/main.rs` lit (5→6 backends).
4. capabilities.toml satisfies the capability-compat check the same way mp-tcp-bufsync does.
5. e2e-matrix.toml gains mp-tcp-poll cells for at least the same set mp-tcp-bufsync covers as [[required]] — at minimum: 01/naive, 02/split, 03/naive, 04/naive+blocked, 05/naive+blocked, 06/naive+blocked, 07/naive+blocked. Each cell bit-identical against reference.bin.
6. Capability-mismatch cells [[skip]] with TASK-0042-style reasons (same set bufsync skips for async/buffer).
7. Unit + integration tests: at minimum a smoke test that emits + builds + runs run.sh on 02-split, asserting bit-identical output. Pin emit-string on at least one multi-worker example mirroring mp-tcp-bufsync's pin tests.
8. Honest limitation note: busy-poll BURNS CPU. Pick a yield strategy (std::thread::yield_now vs busy spin vs short sleep) and document the tradeoff.

## Honest scope

- `nucleus/mp-tcp-common` already provides wire framing + port picker + SO_*BUF logic; ensure mp-tcp-poll consumes it cleanly without per-backend forking.
- The 'poll' notify variant is new in the capability schema. **Precursor: TASK-0044.02.01** lands the schema-validator widening BEFORE this task; the dependency edge is recorded on the tracker. If TASK-0044.02.01 reveals the validator does NOT hard-code the notify list (i.e. accepts any string), the precursor becomes docs-only and unblocks this task immediately.
- Hazard: a nonblocking-read loop that NEVER sees data (peer never sends) becomes a spin-deadlock with no error. Make sure the read loop has a bounded retry count or a deadline-driven timeout (project memory project-mp-tcp-event-vs-bufsync-safety-profile is the analog warning for the async sibling — sync poll has a similar mask-failure-modes hazard).
- run.sh: same shape as bufsync (host first, then non-host workers, propagate exit).

## Cross-reference

- Template: `nucleus/backends/mp-tcp-bufsync/src/lib.rs`, `nucleus/backends/mp-tcp-bufsync/src/multi_worker.rs`, `nucleus/backends/mp-tcp-bufsync/capabilities.toml`.
- Shared transport infrastructure: `nucleus/mp-tcp-common/src/wire.rs` (the lift; consumed by mp-tcp-bufsync AND mp-tcp-event).
- Shared backend infrastructure: `nucleus/backend-common/` (see project memory project-backend-common-crate).
- Driver dispatch: `nucleus/driver/src/main.rs:694-707` (the mp-tcp-bufsync arm shape).
- PRD: §7.1 row mp-tcp-poll.

## Forward-carried context (orchestrator briefing for the implementer)

- DO NOT add this to capabilities-toml.md schema's hardcoded list of notify variants without checking compat-check first; the schema validator may not know 'poll' yet.
- The schedules that compile for mp-tcp-bufsync MUST compile for mp-tcp-poll (capability superset). The differential gains a fourth backend column for the same M3 set.
- Hardening rule: any new file that contains `String::replace` on a rendered Rust expression must carry the `// ALLOW textual replace: <reason>` annotation or be reworked structurally (memory feedback-textual-replace-codegen-unsafe, just check-textual-replace-on-codegen).
- include_str! coverage rule applies (memory feedback-include-str-compile-coverage, just check-include-str-coverage).
- TCP port allocation: use the existing __nuc_pick_port helper (mp-tcp-bufsync emits it); do not introduce a python-based picker (TASK-0038 forward-carry — non-reproducible in dev shell).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Forward-carry from TASK-0044.02.01 cycle 172

Schema-side blocker DISCHARGED. The notify allow-list at `nucleus/nucleus-compiler/src/capabilities.rs:91` (NotifyMode::Poll) and `:114` (From<NotifyKind> impl) already accept `notify="poll"` end-to-end; check_schedule_compat at `:394-402` walks the path cleanly. Schema doc at `docs/capabilities-toml.md:68` already lists `poll`. All landed at cycle-77 via the SUPPORTED_SCHEMA_VERSIONS work.

Cycle 172 added the positive-sibling pin test at `nucleus/nucleus-compiler/tests/capabilities.rs::compat_positive_notify_poll_accepts_poll_request` so a future regression of the From-impl breaks the accept path's test coverage (was a silent-sibling gap pre-172).

Remaining surface for TASK-0044.02: backend crate + capabilities.toml emission + driver dispatch arm + e2e cells. NO schema-side prerequisite work remains.

## Cycle 174 — SKELETON slice landed; phased-AC addendum (architect P3.2 fold-back)

Cycle 174 delivered the SKELETON slice. Mirrors the cycle-173 openmp-rs slice shape. The cycle-173 architect P2.1 phased-AC addendum on TASK-0044.01 is mirrored here so the same close-time AC-gaming risk is pre-empted.

- AC#1 — `nucleus/backends/mp-tcp-poll/` crate exists with Cargo.toml + src/lib.rs + capabilities.toml. **DONE.**
- AC#2 — `mp_tcp_poll::emit(...)` returns the same EmitResult shape as mp-tcp-bufsync (multi-binary 6-field). **DONE** at the shape level; emit() always returns ContractGap in the skeleton phase.
- AC#3 — Driver dispatch arm in `nucleus/driver/src/main.rs` lit (5→6 backends); BACKENDS help-text + 'unknown backend' help-text both extended. **DONE.**
- AC#4 — capabilities.toml satisfies the capability-compat check (smoke-tested via `nucleus build --backend mp-tcp-poll ... 01-elementwise-add/naive ...` — compat-check passes, then ContractGap with precise forward-link surfaces). **DONE.** First cap-file in the repo to ship notify=["poll", ...]; cycle-172 already verified the schema validator accepts it.
- AC#7 — Unit tests pin emit-string (cycle-174 skeleton tests: ContractGap variant + message contains `mp-tcp-poll` + forward-links TASK-0044.02; EmitResult shape via constructor-as-pin). **DONE for skeleton scope.** Multi-worker emit-string pin lands with codegen.

OPEN ACs (deferred to the codegen cycle of TASK-0044.02):

- AC#5 — e2e-matrix cells for mp-tcp-poll × {01/naive, 02/split, 03/naive, 04/naive+blocked, 05/naive+blocked, 06/naive+blocked, 07/naive+blocked}, each bit-identical against reference.bin. **PENDING** — every cell would currently ContractGap; matrix wiring lands with codegen.
- AC#6 — Capability-mismatch cells [[skip]] with TASK-0042-style reasons. **PENDING** — same dep as AC#5.
- AC#8 — Honest limitation note + yield-strategy commitment. **HALF-DONE** — the lib.rs docstring header (`mp-tcp-poll/src/lib.rs:34-46`) documents the busy-poll CPU-burn / spin-deadlock hazards + commits to `std::thread::yield_now`; the runtime SHIP of that yield primitive lands with codegen.

PHASED-AC POLICY (cycle 174 architect P3.2 fold-back, mirroring cycle-173 TASK-0044.01 P2.1): TASK-0044.02 ACs split into skeleton-cycle (AC#1-#4, AC#7) and codegen-cycle (AC#5-#6, AC#7-multi-worker, AC#8-runtime) buckets. Do NOT close TASK-0044.02 until the codegen-cycle ACs also land — `feedback-ac-rewrite-on-done-task` recurrence guard.

## Cycle 192 — SINGLE-WORKER arm landed; phased-AC AC#5-subset + AC#6 closed

Cycle 192 landed the single-worker codegen arm (the cycle-174 phased-AC addendum's first remaining bucket):

- AC#5 (PARTIAL — single-worker subset DONE; multi-worker portion still PENDING under TASK-0044.02.02):
  - **16 [[required]] mp-tcp-poll cells**, all bit-identical against reference.bin (verified `just e2e` × 3 stable samples post-cycle-192, total 180/144/0/36/0):
    01/naive, 02/naive, 03/naive, 04/{naive, blocked, blocked-nondiv}, 05/{naive, blocked},
    06/{naive, blocked}, 07/{naive, blocked}, 08/naive, 09/naive, 11/naive, 13/naive.
  - 1 implicit-fan-out informational PASS cell (05-stencil/reuse) — bit-identical.
- AC#6 (DONE): 5 [[skip]] entries for capability-mismatch schedules (async + buffer + event),
  reason text mirrors mp-tcp-bufsync's TASK-0042 verbatim:
    05/distributed, 05/distributed-2d, 09/pipelined, 11/pipelined, 13/pipeline_parallel.
- AC#7 (PARTIAL): single-worker emit pin LANDED (tests/single_worker_emit.rs — two
  byte-identical-vs-mp-tcp-bufsync tests: empty event-list + 01-elementwise-add/naive real
  fixture). Multi-worker emit-string pin still pending under TASK-0044.02.02.
- AC#8 (CITATION CORRECTED cycle-192b): lib.rs docstring header at `mp-tcp-poll/src/lib.rs:50-65` 
  (was 34-46 pre-cycle-192 when the file was 119 LoC; cycle-192 expanded the doc-comment block,
  shifting the busy-poll/spin-deadlock/yield_now-commitment section down). Stamp restored.

OPEN ACs (carry-forward to TASK-0044.02.02):
- AC#5 (multi-worker subset): 8 [[skip]] cells with 'mp-tcp-poll multi-worker codegen pending'
  reason — 02/split, 03/distributed, 06/{distributed, distributed2},
  07/{distributed, distributed-2d}, 08/distributed, 13/batch_parallel. Promotion target on
  TASK-0044.02.02 close.
- AC#7 (multi-worker pin): mirror mp-tcp-bufsync's tests/pingpong.rs + tests/rendezvous_emit.rs
  shape when codegen lands.
- AC#8 (runtime yield-primitive SHIP): the lib.rs docstring commits to std::thread::yield_now;
  runtime emission lands with TASK-0044.02.02.

## Cycle 192 — implementation details

- nucleus/backends/mp-tcp-poll/src/lib.rs: replaced SKELETON ContractGap with single-vs-multi-worker
  dispatch. Single-worker arm: `pthreads_sync::render_single_worker_main_with_kernels_attr` +
  `backend_common::project_skeleton::multi_binary::{render_cargo_toml, render_run_sh_single}`
  (same delegation pattern as mp-tcp-bufsync cycle 24 / mp-tcp-event cycle 41). Multi-worker arm:
  typed EmitError::ContractGap forward-linking TASK-0044.02.02. ~230 LoC total.
- nucleus/backends/mp-tcp-poll/Cargo.toml: added `pthreads-sync` + `mp-tcp-common` (runtime
  deps) + `test-common` + `mp-tcp-bufsync` (dev-deps). The `mp-tcp-common` dep is the
  wire-v0 framing module — emitted verbatim even on the single-process path for shape uniformity
  with multi-process builds (mp-tcp-bufsync precedent).
- nucleus/backends/mp-tcp-poll/tests/skeleton.rs: rewrote — multi-worker ContractGap pin (now
  creates a real kernels.rs stub since emit() reads kernels.rs upfront, mirroring mp-tcp-bufsync's
  structure) + EmitResult shape pin.
- nucleus/backends/mp-tcp-poll/tests/single_worker_emit.rs (NEW): empty event-list +
  01-elementwise-add/naive byte-identical-vs-mp-tcp-bufsync tests.
- nuc-nucleus/e2e-matrix.toml: mp-tcp-poll added to backends list; 16 [[required]] +
  8 multi-worker-pending [[skip]] + 5 cap-mismatch [[skip]] cells.
- nucleus/driver/src/main.rs + Cargo.toml + nucleus/Cargo.toml: narrative comments updated
  to reflect single-worker LANDED + multi-worker-pending state.

## Cycle 192 — review-gate findings

Parallel review gate (qa-test-runner + mped-architect read-only) BOTH GO with finite findings:

- QA: GO, no P1/P2. P3 advisories.
- Architect: GO with P2.1 stale-citation fix (TASK-0044.02 notes line 115 cited
  mp-tcp-poll/src/lib.rs:34-46 but cycle-192 expanded the doc-block so the busy-poll section
  is now at lines 50-65). Applied this update.
- Architect P2.2 advisory: ContractGap message at lib.rs:186 should reference TASK-0044.02.02
  AC#7 (deadlock-bound contract). Applied — gap message now points readers at the precise AC.
- Architect P3.1 / P3.2 / QA P3 informational-promotion: noted but not in scope this cycle.

## Lessons forward-carried

For TASK-0044.02.02 (multi-worker arm):
- AC#7 deadlock-bound: bound the nonblocking-read retry / add a deadline. Never-sending peer
  must surface as a LOUD error (debug_assert! after the poll-loop receive), not a silent spin.
  Per memory `project-mp-tcp-event-vs-bufsync-safety-profile`, the poll-based primitive masks
  contract violations the same way the per-seq demux mp-tcp-event uses does. Use
  `std::thread::yield_now` as the yield primitive — NOT a sleep (latency), NOT busy-spin
  (full-core burn).
- Per memory `feedback-silent-sibling-defect`: every defensive guard mp-tcp-bufsync emits in
  worker programs MUST have a sibling in mp-tcp-poll's emit. Cross-grep before promotion.
- The single-worker arm's byte-identical-vs-mp-tcp-bufsync invariant is preserved when the
  multi-worker arm lands; multi-worker bit-identical against mp-tcp-bufsync is the cross-backend
  differential gate (Plan structure / wire framing reused).

## Cycle 192 stats

- e2e: 180/144/0/36/0 (up from cycle 191b's 150/127/0/23/0): +30 total, +17 PASS, +13 SKIP.
  Required-fail: 0. Three stable samples.
- Tests: dev 988/0/3 (was 986 pre-cycle), release 987/0/3 (was 985). +4 new mp-tcp-poll tests
  (2 single_worker_emit + 2 skeleton).
- LoC delta: mp-tcp-poll lib.rs 119 → 234 (+115), single_worker_emit.rs new (+186),
  skeleton.rs 75 → 99 (+24), e2e-matrix.toml +228 across cells + comments.
- Filed follow-ups: TASK-0044.02.02 (multi-worker nonblocking-poll codegen, HIGH priority,
  7 ACs including explicit deadlock-bound negative test).

## Cycle-191 sibling pattern observation (for orchestrator memory)

Cycle 191 (openmp-rs) and cycle 192 (mp-tcp-poll) are the same SHAPED slice — single-worker
delegation to shared renderers + 16 [[required]] + 13 [[skip]] cells + 1 multi-worker
follow-up. Net delta per cycle: +30 e2e cells. The remaining M6 skeleton (mp-uds-event,
TASK-0044.03) follows the SAME pattern but as a sibling of mp-tcp-event (multi-binary,
EmitResult has runtime_rs in addition to wire_rs). If TASK-0044.03 also lands its
single-worker arm cleanly, e2e baseline becomes 210/161/0/49/0 and all three M6 backends
have their single-worker arms live.
<!-- SECTION:NOTES:END -->
