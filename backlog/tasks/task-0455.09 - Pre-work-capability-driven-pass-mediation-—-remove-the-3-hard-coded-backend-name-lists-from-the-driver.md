---
id: TASK-0455.09
title: >-
  Pre-work: capability-driven pass mediation — remove the 3 hard-coded
  backend-name lists from the driver
status: Done
assignee: []
created_date: '2026-06-09 21:58'
updated_date: '2026-06-10 10:51'
labels:
  - production
  - driver
  - capabilities
dependencies: []
parent_task_id: TASK-0455
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
From the 2026-06-09 architecture review (Risk 4, verified verbatim at the first site). Which compiler passes run is decided by string-matching backend names in the driver: driver/src/main.rs:400-404 (host-mediation gate: mp-tcp-bufsync|mp-tcp-event|mp-tcp-poll|mp-uds-event), :467 (host data-relay gate: mp-tcp-event|mp-uds-event), :639 (safe push reorder gate: mp-tcp-event|mp-uds-event). The encoded facts (star topology, no native worker-to-worker channel, per-seq demux vs strict FIFO) are CAPABILITY data, and capabilities.toml is loaded a few lines later (main.rs:492-511). Every new platform must remember up to three lists; a miss is ContractGap at best, silent topology mismatch at worst — the silent-sibling class. Must land before TASK-0455.02 (embedded multi-MCU is the next topology) and before any new backend.

Work: extend the capabilities schema (docs/capabilities-toml.md + validator) with the topology/mediation flags; drive all three gates from the loaded capabilities; delete the name lists. Discipline: driver must mirror backend election EXACTLY (standing memory feedback-driver-must-mirror-backend-election-exactly — the host_election helper precedent). Fold in the adjacent waste the review found: the driver re-projects the ACFG up to three extra times for host election (main.rs:420, 473, 645) — collapse to one projection per build. Acceptable intermediate step: a single const table beside the capability load, then the full schema migration.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Zero backend-name string matching in driver pass selection (grep witness recorded)
- [x] #2 Capabilities schema documents the new topology/mediation flags; validator rejects missing/unknown; all 10 backends declare them
- [x] #3 ACFG projection for host election happens at most once per build
- [x] #4 e2e + mpi gates green; negative check: a deliberately-wrong flag on one backend is caught loudly, not silently mis-mediated
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Re-grep the 3 gates + cap-load in driver/main.rs (DONE: gates at lines 400-404, 467, 639; cap-load 492-502).
2. Add 3 capability flags to schema: star_topology_host_mediation (bufsync/tcp-event/poll/uds-event), host_data_relay (tcp-event/uds-event), reorderable_push (tcp-event/uds-event). Named for MEANING.
3. Declare in all 10 capabilities.toml to reproduce OLD name-list selection EXACTLY.
4. Validator: reject contradictory flag set (data-relay/reorderable-push imply host-mediation; deny if relay/reorder without mediation).
5. Load caps EARLIER (before the 3 gates) so gates read caps not backend name. Collapse 3 ACFG re-projections to one.
6. Delete the 3 name lists.
7. Exhaustive equivalence unit test over 10 backends: new-selection == old-selection table. Negative-arm test for contradictory flags.
Verify: cargo test -p nucleus-compiler capabilities + driver tests + clippy both crates.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CYCLE WORK (wave-2, left In Progress for batched gate):

Three capability flags added (named for MEANING per the 3 gates they replace):
  - star_topology_host_mediation -> apply_host_mediation_inject (bufsync/tcp-event/poll/uds-event)
  - host_data_relay              -> apply_host_data_relay_inject (tcp-event/uds-event)
  - reorderable_push             -> apply_safe_push_reorder (tcp-event/uds-event)
All three are #[serde(default = false)] (back-compat: off-tree/pre-task files parse, select no mediation). All 10 in-tree capabilities.toml declare them explicitly.

Validator: new CapError::InconsistentTopologyFlags rejects host_data_relay OR reorderable_push without star_topology_host_mediation (first cross-field rule in the schema). Made Capabilities::validate() pub so in-memory negative tests hit the real path.

Driver: cap load MOVED up (before the 3 gates) so gates read caps not backend name. 3 name lists DELETED. The 2 host-election preview projections in the mediation chain COLLAPSED to ONE (reuse pre-mediation host h for both mediation+relay). Safe-push gate already reused the authoritative per_worker projection (no extra preview there).

PROOF of collapse correctness: corpus sweep test host_election_is_invariant_under_mediation_so_one_projection_suffices asserts pre-mediation host == post-mediation host for every shipping (algo,sched) pair (>=20 cells). Equivalence test capability_selection_matches_old_name_lists_for_all_backends is exhaustive over all 10 backends (new==old tuple). Old name lists reproduced verbatim as the oracle.

GOTCHAS / decisions:
  - Driver package name is "nucleus" (NOT nucleus-driver) — backends/* dirs hold capabilities.toml.
  - task0422_01_inv2_post_mediation.rs has its OWN copy of the name lists (MEDIATED_BACKENDS const) — NOT touched (not in ownership; behavior identical so it still passes). Could be migrated to caps in a follow-up but out of scope.
  - dispatch.rs match-on-backend-name for emit() routing is registered-backend dispatch, NOT pass selection — left as-is (out of scope).
  - Reordering cap-load before the mediation passes cannot change user-facing error order: the 3 mediation passes are infallible (return ACFG/per_worker directly, no Result).
  - Fixed a PRE-EXISTING stale doc claim in docs/capabilities-toml.md ("No schema_version field today" — added by TASK-0120).

VERIFY (targeted, NOT full gate — orchestrator runs e2e batched):
  cargo test -p nucleus-compiler --test capabilities  -> 29 passed
  cargo test -p nucleus --test task0455_09_capability_pass_selection -> 6 passed
  cargo test -p nucleus (all driver tests) -> all pass incl task0422_01 inv2
  cargo test -p nucleus-compiler --lib -> 192 passed
  cargo clippy -p nucleus-compiler --all-targets -D warnings -> clean
  cargo clippy -p nucleus --all-targets -D warnings -> clean

OPEN/FOLLOW-UP: migrate task0422_01_inv2_post_mediation.rs MEDIATED_BACKENDS to read caps (de-dup the last name-list copy, in a test outside this task ownership).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Driver pass-mediation is capability-driven: 3 meaning-named flags (star_topology_host_mediation, host_data_relay, reorderable_push) in schema+validator+docs+all 10 capabilities.toml; the 3 backend-name lists deleted. Equivalence PROVEN twice: 10-backend old-vs-new unit table (dev+release) + architect independently re-derived the table from the toml diffs; runtime confirmed by wave gate e2e 497/428/0/69/0. Cross-field validator rejects inconsistent flags (negative tests both paths). Host-election projections collapsed 2->1 with a corpus-sweep invariance pin (>=20-cell non-vacuity floor) — strictly stronger than before per review. Out-of-tree caps-file authority semantics documented (6ac4bb9). Landed c2e245a; follow-up TASK-0465 migrates the last name-list copy (test oracle). Architect GO.
<!-- SECTION:FINAL_SUMMARY:END -->
