---
id: TASK-0451.06
title: 'Thesis C6: Figures (TikZ) — the core figure set'
status: To Do
assignee: []
created_date: '2026-06-05 20:00'
labels:
  - thesis
  - paper
  - figures
  - tikz
dependencies:
  - TASK-0451.01
parent_task_id: TASK-0451
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Author the core TikZ figures (paper/figures/*.tex), co-evolving with the content chapters (content tasks reference these labels). FIGURE SET: (1) the compilation pipeline — parse -> typed algorithm IR -> link -> ACFG -> transforms -> transfer/halo inference -> Petri-net IR -> soundness gate -> per-worker projection -> presentation codegen -> out/ (the central figure); (2) algorithm x schedule x backend composition (one algorithm, many schedules, many backends); (3) the two boundaries (algorithm/schedule; middle-end/presentation); (4) an example dataflow DAG (stencil/separable filter); (5) the ACFG of a worked example (op/repeat/sync/xfer nodes); (6) an example Petri net (places = data slots/channels/barriers, transitions = firings/xfers/syncs) + a firing-order replay; (7) the soundness-gate flow (boundedness/deadlock/conflict-free -> compile error on failure); (8) per-worker projection -> EventList; (9) the target ladder (tier 1/2/3 + cost classes); (10) the bit-identical differential rig (source -> N backends -> diff vs reference.bin); (11) backend x example coverage heatmap; (12) MPI SPMD rank dispatch (match rank -> per-rank events); (13) embedded multi-MCU UART-hub Renode co-simulation (link_push/link_recv); (14) capability-matrix / orthogonality schematic. Prefer self-contained, legible, print-safe (grayscale-friendly) TikZ; consider externalisation via the justfile figures recipe. CONSTRAINT: figures must be technically faithful to the implementation (verified in C8 visual review). DoD: all figures compile in the PDF; just build clean.
<!-- SECTION:DESCRIPTION:END -->
