---
id: TASK-0340.04.03
title: >-
  Tighten stale 'forward-linking TASK-0329' claim in
  mp-tcp-event/multi_worker/mod.rs:1-63 module docstring (TASK-0329.01.02 has
  landed)
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 12:59'
updated_date: '2026-05-26 22:48'
labels:
  - tech-debt
  - docs
  - comment-doc-lie
dependencies: []
parent_task_id: TASK-0340.04
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Cycle-180b architect P3.3: module-level docstring on nucleus/backends/mp-tcp-event/src/multi_worker/mod.rs:48-53 still reads:

'**Host-excluding barriers**: same transport limitation as mp-tcp-bufsync — one CTRL stream per (host, worker) pair, so a barrier whose participants exclude host cannot be lowered. Fail-loud with a typed ContractGap forward-linking TASK-0175 (filed forward as TASK-0329 for host-mediated barrier mediation, analogous to cycle-148/149's data lift).'

Per memory project-cross-backend-differential, TASK-0329.01.02 (Option B interleaved per-Push host-relay via ACFG pass) LANDED cycle 163-164b for in-Repeat-body w2w Push pairs. The 'forward-linking' framing is stale.

Tightened wording (suggested):

'**Host-excluding barriers**: same transport limitation as mp-tcp-bufsync — one CTRL stream per (host, worker) pair, so a barrier whose participants exclude host cannot be lowered. Fail-loud with a typed ContractGap. Data-arm partially discharged by TASK-0329.01.02 (cycle 163-164b host-mediated data-relay for in-Repeat-body w2w Push). Sync-arm host-mediated mediation still pending (TASK-0329).'

Spans both mp-tcp-event and mp-tcp-bufsync (same forward-linking pattern likely repeats in mp-tcp-bufsync/lib.rs module docstring); audit both as a pair.

This is a PRE-EXISTING comment-doc-lie not introduced by slice 5; the docstring was preserved verbatim per the verbatim-move discipline.

## Acceptance criteria

1. Audit both mp-tcp-event/multi_worker/mod.rs:1-63 AND mp-tcp-bufsync/src/lib.rs:1-74 for the 'TASK-0329 forward-linking' claim and similar stale framings.
2. Update wording to reflect post-TASK-0329.01.02 reality (in-Repeat-body w2w Push DATA-arm landed; Sync-arm still pending).
3. Verify the new wording via grep against the actual codegen sites (memory feedback-comment-doc-lie-recurring + feedback-implementer-disclosure-mechanism-wrong: do not infer mechanism from task title; check actual code).
4. just build && just clippy clean; just e2e 112/102/0/10/0 preserved (docstring-only edit; should be zero source-byte impact).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 187 — landed in-thread (orchestrator, doc-only)

Audited both files per AC#1; updated both module-level docstrings per AC#2; mechanism verified against the actual ContractGap sites per AC#3.

Empirical findings on the audit:
- nucleus/backends/mp-tcp-event/src/multi_worker/mod.rs:48-53 had the stale "filed forward as TASK-0329 for host-mediated barrier mediation, analogous to cycle-148/149's data lift" framing flagged by cycle-180b architect P3.3. Rewrote to enumerate the actual lift-history (TASK-0327 cycle 149 top-level + TASK-0329.01.02 cycles 163-164b in-Repeat-body) + name TASK-0329 specifically as the CTRL-arm pending ticket. Matches the framing already in the same file at line 230 (the actual ContractGap construction site) and in plan/mod.rs:137-138.

- nucleus/backends/mp-tcp-bufsync/src/lib.rs:52-66 (the sibling) had a CLOSELY-RELATED stale-claim — "worker-to-worker mesh is TASK-0175" — that did NOT use the literal "forward-linking TASK-0329" phrase but is structurally the same defect (the TASK-0175 ticket was split into TASK-0327 + TASK-0329 cycles 148/149; the docstring still claimed the original combined ticket was the open one). Updated to the same enumerated lift-history framing. Sibling-defect sweep (per memory feedback-silent-sibling-defect) caught it.

Mechanism verification (per memory feedback-implementer-disclosure-mechanism-wrong):
- Greppped both files for TASK-0175, TASK-0327, TASK-0329 references before editing.
- Confirmed the actual ContractGap construction sites (mp-tcp-event/multi_worker/mod.rs:230 + mp-tcp-bufsync/src/plan/mod.rs:137-138) already use the correct "DATA arm lifted as TASK-0327; CTRL arm tracked as TASK-0329" wording. The module-level docstrings were the lagging anchor sites.
- Did NOT modify the literal ContractGap error message string (which the cycle-186-vintage NB at mod.rs:235-239 explicitly pins as test-tied to multi_worker_emit::host_excluding_barrier_is_typed_contract_gap; rewriting the wire message would silent-break the test).
- TASK-0329.01.02 cycle 163-164b reference cross-checked against memory project-cross-backend-differential entry ("Slice 2 (TASK-0329.01.02) Option B2 host-mediated data-relay").

Verification gate (cycle 187 is doc-only):
- just build: clean (cache rebuild ~2.81s after editing 2 module-level docstrings).
- just clippy: clean.
- just check-narrative-doc-lie: clean.
- just test / test-release / e2e: NOT re-run for module-docstring-only edits (zero behaviour change; gate numbers from cycle 186 / 186b / 186c stand: 979/0/3 dev + 978/0/3 release + 120/106/0/14/0 e2e).

AC#4 of this task says "just e2e 112/102/0/10/0 preserved" — that target is STALE since cycle 186 promoted 08-histogram naive × 4 backends, lifting the baseline to 120/106/0/14/0. The spirit of AC#4 (zero behaviour change, e2e baseline preserved) is satisfied; the literal numbers in AC#4 were authored before cycle 186 landed. Per memory feedback-ac-rewrite-on-done-task, do NOT rewrite AC#4 — record the actual current-baseline-preserved verification here.

Forward-carry of cycle 187 lesson — pre-existing comment-doc-lies in module-level docstrings can take MULTIPLE cycles to surface: the cycle-180b architect P3.3 flagged the mp-tcp-event mod.rs site in May 2026; the bufsync lib.rs SIBLING defect required cycle-187 explicit sibling-sweep to find. Per memory feedback-silent-sibling-defect: any time you fix a stale comment-doc-lie at one anchor site, grep for the same ticket/framing across ALL siblings (here: paired backends mp-tcp-event ↔ mp-tcp-bufsync).

## Cycle 187b — fold-back of cycle 187 (TASK-0329 status was misread; cycle 187 introduced fresh comment-doc-lies)

Cycle 187's combined review gate (architect P1 finding) caught a serious defect: cycle 187 propagated a FALSIFIED "TASK-0329 is filed and still pending" claim across both rewritten module docstrings. TASK-0329 was actually marked **Done** 2026-05-26 07:53 (updated `host_mediation_inject.rs` pass landed cycle 160), 5 hours BEFORE this task (TASK-0340.04.03) was filed at 2026-05-26 12:59. The task's description ITSELF carried the stale claim ("Sync-arm host-mediated mediation still pending (TASK-0329)") into the implementer's hands; the implementer (orchestrator, cycle 187) trusted the prompt without empirically verifying TASK-0329's status.

The architect surfaced TWO defect classes in cycle 187:
- (a) Cycle 187 introduced 2 NEW stale-pending docstring claims at mp-tcp-event/multi_worker/mod.rs:59 + mp-tcp-bufsync/src/lib.rs:72 (my edits, claimed "TASK-0329 still pending"). Direct introduction of the defect class the cycle was filed to fix.
- (b) Cycle 187 left 5 PRE-EXISTING sibling anchor sites untouched, all carrying stale "TASK-0329 tracked/forward-linked/blocked" framings:
  - mp-tcp-event/multi_worker/mod.rs:238 (in-file sibling 180 LoC below the L59 edit)
  - mp-tcp-event/src/lib.rs:79 + :125
  - mp-tcp-bufsync/src/plan/mod.rs:138
  - driver/src/main.rs:49 + :753 (the worst — :49 claims "13/pipeline_parallel × mp-tcp-event is blocked on TASK-0329" but that cell was PROMOTED bit-identical cycle 165 via TASK-0329.01.02.01)

The cycle 187 commit body's claim that mp-tcp-event/src/lib.rs "already had correct distinguished CTRL-arm framing" was itself a comment-doc-lie about a comment-doc-lie. Ironic given cycle 187's purpose.

Cycle 187b fold-back applied (cycle 187 + 187b should be considered together):

Code rewrites (replaced stale "TASK-0329 still pending" anchors with "TASK-0329 Done cycle 160 via apply_host_mediation_inject; backend ContractGap is now defense-in-depth" framings):
- mp-tcp-event/multi_worker/mod.rs:48-67 (replaces cycle 187 rewrite)
- mp-tcp-event/multi_worker/mod.rs:230-244 (in-file sibling at ContractGap construction site)
- mp-tcp-bufsync/src/lib.rs:62-78 (replaces cycle 187 rewrite)
- mp-tcp-bufsync/src/plan/mod.rs:132-143 (pre-existing sibling at ContractGap construction site)
- mp-tcp-event/src/lib.rs:71-87 + :118-128 (pre-existing siblings; commit body's "already correct" claim falsified)
- driver/src/main.rs:45-62 (the M4 four-way differential framing; now reflects actual cycle-165 four-way bit-identical state)
- driver/src/main.rs:753-761 (per-backend dispatch comment)

Empirical verification:
- TASK-0329 status: `backlog task view TASK-0329 --plain` confirms Status: ✔ Done (updated 2026-05-26 07:53; cycle 160 — apply_host_mediation_inject pass at nucleus-compiler/src/passes/host_mediation_inject.rs:70).
- 13/pipeline_parallel × mp-tcp-event status: `awk '/13-cnn-inference.*pipeline_parallel/' nuc-nucleus/e2e-matrix.toml` confirms PROMOTED bit-identical cycle 165 (TASK-0329.01.02.01); current matrix entry is [[required]] not [[skip]].
- After cycle-187b sweep: `grep -rn 'TASK-0329 and still pending|tracked as TASK-0329|filed as TASK-0329|forward.*TASK-0329|blocked on TASK-0329' nucleus/ --include='*.rs'` returns ZERO matches.

Verification gate (cycle 187b is doc-only):
- just build: clean.
- just clippy: clean.
- just test (dev):       979/0/3 (preserved cycle 186/187/188 baseline).
- just test-release:     978/0/3 (preserved cycle 186/187/188 baseline).
- just e2e:              120/106/0/14/0 (preserved).
- just check-narrative-doc-lie / check-mega-files / check-textual-replace-on-codegen / check-include-str-coverage: clean.

NEW lesson — orchestrator may trust their own pre-existing TASK DESCRIPTION as authoritative without empirical re-verification. The cycle-187b root cause: TASK-0340.04.03's description (filed cycle 180b on 2026-05-26 12:59) suggested wording that included the stale "Sync-arm host-mediated mediation still pending (TASK-0329)" phrase. TASK-0329 had been marked Done 5 hours EARLIER (07:53) but the cycle-180b author did not re-check TASK-0329's status when writing the suggested wording. The cycle-187 implementer (also orchestrator) then trusted the suggested wording verbatim. This generalises memory feedback-implementer-disclosure-mechanism-wrong to the TRACKER level: any task description authored at time T0 may carry claims (e.g., "X is pending") that become falsified at later time T1 — including in HOURS, not weeks. Mitigation: every implementer onboarding step MUST include "re-verify the task description's claims against current tracker state (backlog task view <ticket-cited> --plain) before writing code". A cycle whose entire purpose is fixing stale claims is most susceptible to introducing fresh ones — the irony is the defect class, not an exception.

Cycle 187b also annotates this disclosure into the long-term memory entry feedback-implementer-disclosure-mechanism-wrong.md.

TASK-0340.04.03 status: remains Done (cycle 187 + cycle 187b together implement the AC's spirit; AC text NOT rewritten). The "TASK-0329 forward-linking" framing has been swept from ALL 7 anchor sites identified by cycle 187b architect review.

Cycle 187b commit hash: pending.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 187 doc-only fold-back: rewrote module-level docstrings on mp-tcp-event/multi_worker/mod.rs:48-53 AND mp-tcp-bufsync/src/lib.rs:52-66 to enumerate the actual TASK-0175 lift history (TASK-0327 DATA arm cycles 148/149 + TASK-0329.01.02 DATA arm cycles 163-164b; TASK-0329 CTRL arm pending) instead of the stale 'forward-linking TASK-0329' framings. Sibling-defect sweep caught the bufsync lib.rs site that the task description did not pre-flag. Mechanism verified against actual ContractGap sites; wire message literal preserved per its test-pinned NB. Doc-only; gate numbers preserved (979/0/3 + 978/0/3 + 120/106/0/14/0).
<!-- SECTION:FINAL_SUMMARY:END -->
