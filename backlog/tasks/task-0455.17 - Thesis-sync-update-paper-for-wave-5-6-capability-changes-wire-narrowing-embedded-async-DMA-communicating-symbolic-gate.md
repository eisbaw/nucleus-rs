---
id: TASK-0455.17
title: >-
  Thesis sync: update paper for wave 5-6 capability changes (wire narrowing,
  embedded async DMA, communicating symbolic gate)
status: Done
assignee: []
created_date: '2026-06-10 17:19'
updated_date: '2026-06-10 20:17'
labels:
  - production
  - paper
dependencies: []
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The production push changed three things the thesis documents as limitations/facts (the TASK-0453 IMPL/THESIS ping-pong discipline applies — every implementation change gets an honest thesis revision, residuals kept):

1. ch10 sec:disc-shortcomings "over-communication floor": every cross-worker edge transmits the whole array — NOW FALSE for contiguous-span edges (TASK-0453.22: -71.9 percent bytes on 05-stencil/distributed, -50 percent on 07-matmul/distributed). Residuals to keep honest: strided arms + embedded still whole-array; per-worker ALLOCATION still full-shaped (the footprint half is open, TASK-0455.14).
2. ch07 tab:capabilities + ch11: embedded-pattern declared sync/buffer=1/blocking — NOW async/buffer=2/notify=event with a byte-exact double-buffered DMA ring in Renode (TASK-0455.02). Residual: depth-2 only, modelled-sync engine caveat, nRF multi-MCU absent.
3. ch09 sec:res-quant-net + ch11 sec:fw-quant: the communicating-case gate fell back to expanded replay (the coupled blocker for the runtime study) — NOW the single-shot matched-pair subclass is proven symbolically (TASK-0455.01: N=512 distributed matmul at 7.2MB flat RSS; A/B verdict-equality over the corpus). Residuals: loop-carried + pipelined shapes still expand; the runtime-study coupling argument in sec:fw-quant needs re-scoping (the wall is down for the dominant shapes).

Verify every number against the landed code (paper-accuracy discipline); rebuild PDF green; keep all residuals as documented limitations.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 All three sections revised to match landed capability, residuals kept honest, numbers code-verified
- [x] #2 PDF rebuilds green; paper-accuracy spot-check on the changed claims
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Onboard: read tracker final summaries for TASK-0453.22, 0455.01, 0455.02, 0455.05, 0455.14 to harvest VERIFIED numbers (wire cuts, gate RSS, embedded async depth-2, e2e baseline, diff-fuzz families).
2. Read the six paper sections to be revised: ch10 sec:disc-shortcomings; ch07 tab:capabilities + tier-3; ch11 sec:fw-quant + sec:fw-affine; ch09 sec:res-quant-net + sec:res-tiers; ch08 sec:val-differential generative arm.
3. Revise prose surgically: wire-narrowing (over-communication floor no longer absolute, residuals strided/embedded/full-shaped alloc); embedded async DMA depth-2 (tab:capabilities row async/buffer=2/notify=event + tier-3 prose, residual depth-2/modelled-sync/no-nRF-multi-MCU); communicating symbolic gate (single-shot matched-pair proven symbolic, residual loop-carried/pipelined expand, re-scope fw-quant runtime coupling); generative-arm widening (5 families/6 ops/multi-worker, boundary moved outward not vanished, for_until single-backend).
4. Update e2e/MPI baseline figures where cited (504/431/0/73/0, 14/14 MPI).
5. Rebuild PDF green from paper/ dir; grep changed claims for cross-section consistency; record page-count delta.
6. backlog notes + leave In Progress.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
TASK-0455.17 DONE (paper/** only; build green). Revised six paper sections for wave 5-6 capability changes, every number code-verified against gates/manifest not narrative.

WIRE NARROWING (ch10 over-comm paragraph rewritten; new ch09 sec:res-quant-comm subsection; ch11 sec:fw-affine inference paragraph): over-communication floor no longer absolute. Contiguous-span cross-worker edges transmit only the span on all tier-1 + MPI. MEASURED (TASK-0453.22 static counts): 05-stencil/distributed 8192->2304B = 71.9pct cut; 07-matmul/distributed 12288->6144B = 50pct cut. Residuals KEPT: strided/non-prefix/accumulator/data-dependent edges + embedded stay whole-array; per-worker ALLOCATION still full-shaped (footprint half open, TASK-0455.14 cited as the rebasing work).

EMBEDDED ASYNC DMA (ch07 tab:capabilities embedded row + caption + tier-3 prose; ch09 sec:res-tiers; ch10 cheap-tier paragraph): embedded-pattern row flipped to Notify=event,poll,barrier,blocking / Async=yes / Buffer=2 (verified against committed capabilities.toml). Tier-3 prose adds the depth-2 producer-side DMA descriptor ring + IRQ-event completion; embedded examples 3->4 (new 09 embedded_dma_buffered, byte-exact 64B 3-MCU STM32H7 Renode, renode-multimcu-gate 4th arm). Residuals KEPT: depth-2 only (buffer>=3 rejects), modelled-sync engine (occupancy-2 is a state property not temporal overlap), nRF multi-MCU absent.

COMMUNICATING SYMBOLIC GATE (ch09 sec:res-quant-net rewritten two->three regimes + sec:res-quant-time; ch11 sec:fw-quant both paragraphs): single-shot matched-pair communicating subclass now proven SYMBOLICALLY (the dominant distributed shapes). N=512 distributed matmul gates at ~7.2MB flat RSS (+~350KB over N=16 across 32768x firings; expanded net = several hundred million nodes / 100+GB). Residuals KEPT: loop-carried (16-jacobi) + pipelined shapes still expand. Re-scoped the fw-quant runtime-study coupling: wall down for dominant shapes, coupling now narrow (loop-carried/pipelined only).

GENERATIVE ARM (ch08 sec:val-differential + ch10 sec:disc-threats): widened 1->5 families (pipeline1d, stencil2d halo, reduction over all SIX combine ops, partition_workers, for_until). "one-dimensional element-wise pipelines" stale text replaced; boundary moved outward not vanished; for_until honestly single-backend (pthreads-sync only). Stale "extend to multi-dim/reduction/data-dependent" tail (those now done) replaced with floating-point + multi-backend-for_until as the remaining work.

E2E BASELINE: preamble macros nmatrixtotal/pass/skip 497/428/69 -> 504/431/73 (RE-DERIVED by counting e2e-matrix.toml [[required]]=431 + [[skip]]=73 = 504 directly, the single-source manifest, not narrative). Skip-category sub-counts in sec:res-coverage updated: capability-mismatch 20->24 (+4 sync backends rejecting embedded_dma_buffered), embedded-fixture 35 + language 14 unchanged. MPI 12 cells/11 examples (8 blk + 4 nonblk) -> 14 cells/12 examples (10 blk + 4 nonblk), verified by awk over the manifest. Honesty fix: 07-matmul/distributed MPI cell is a 1D ROW-band partition (schedule comment), so changed "2D-tiled matrix multiply" -> "row-partitioned matrix multiply" in ch07+ch10; added separable-filter to the blocking-arm shape list.

VERIFICATION: latexmk exit 0; 129 pages (was 125 at HEAD => +4 page delta); 0 LaTeX warnings, 0 undefined refs/citations. tab:capabilities widened past textwidth from the 4-item embedded Notify list (new 37.68pt overfull) -> fixed by \footnotesize + tabcolsep=4pt; all table-region overfull cleared. Remaining 3 overfull boxes (no_std sentence 8.1pt, purity-annotation 11.5pt, six 1.9pt) are ALL pre-existing (git-confirmed untouched by this diff). Cross-section consistency greps all clean (504/431/73, 14 MPI, 4 embedded examples, 2 MCU families, 5 gen families/6 ops, 71.9pct/50pct, 7.2MB all consistent).

TOUCHED (paper/** only): paper/preamble.tex, paper/chapters/07-backends-and-target-ladder.tex, 08-validation.tex, 09-results.tex, 10-discussion.tex, 11-future-work.tex. NO git add/commit. Left In Progress per brief.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Thesis synced to waves 5-7 reality, every figure re-derived from gates/manifests (the agent A/B-d page counts via stash-rebuild): ch10 over-communication rewritten (whole-array floor no longer absolute; -71.9%/-50% measured), new ch09 sec:res-quant-comm, ch11 re-scoped; tab:capabilities embedded row flipped with depth-2 ring + Renode 4th-arm evidence; ch09/ch11 communicating-gate re-scope (single-shot proven, N=512 flat ~7MB, loop-carried/pipelined third regime kept; runtime-study coupling PARTIALLY broken and says so); ch08 generative arm updated to 5 families/6 ops/timeouts. All residuals preserved (alloc footprint, strided/embedded whole-array, fsum scope, loopback caveat). Review caught 3 one-clause doc-vs-code lies (ch06 whole-array sibling, ch08 phantom missing-timeout, ch07 interrupt-not-spin) — fixed in fold-in b12c58e; bonus honesty catch: 07-matmul matrix comment mislabeled 1D as 2D, fixed. PDF 129pp green. Revision triggers recorded (0455.14 -> ch10/ch11 clauses; S7 -> for_until). Landed a6d75a1 + b12c58e; architect conditional-GO discharged.
<!-- SECTION:FINAL_SUMMARY:END -->
