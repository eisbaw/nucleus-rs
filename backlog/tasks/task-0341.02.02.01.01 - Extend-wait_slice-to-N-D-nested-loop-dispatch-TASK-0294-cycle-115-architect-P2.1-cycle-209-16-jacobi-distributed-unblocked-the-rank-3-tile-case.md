---
id: TASK-0341.02.02.01.01
title: >-
  Extend wait_slice to N-D nested-loop dispatch (TASK-0294 cycle-115 architect
  P2.1; cycle-209 16-jacobi/distributed unblocked the rank-3 tile case)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-27 16:01'
updated_date: '2026-05-30 09:24'
labels:
  - compiler
  - backend-common
  - wait_slice
  - M5-follow-up
  - gap-probe-followup
dependencies:
  - TASK-0294
parent_task_id: TASK-0341.02.02.01
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
=== Filed as TASK-0341.02.02.01 AC#3 honest-BLOCKED follow-up, cycle 209 ===

Cycle 209 landed the halo_inference (B') refinement and re-tested 16-jacobi/distributed across all 7 tier-1 backends. The (B') predicate correctly classifies the halo_inference error as advisory; compilation proceeds past halo_inference. The NEXT-LAYER gap fires:

5 backends (pthreads-sync, pthreads-async, mp-tcp-event, openmp-rs, mp-uds-event):

    EventList/sidecar contract gap: Wait of data DataId(0): tile rank 3 and data dim rank 3 exceed the 2D row-loop slice-paste's supported shape (rank <= 2 on both). No shipped schedule constructs this today; see TASK-0294 cycle-115 architect P2.1 — extend `wait_slice` to N-D nested-loop dispatch or file a follow-up before shipping a schedule that does

2 backends (mp-tcp-bufsync, mp-tcp-poll):

    EventList/sidecar contract gap: mp-tcp-bufsync: TASK-0330 defensive guard — worker-to-worker Push (data=DataId(0), dst=WorkerId(2), seq=SeqTag(0)) found INSIDE an Event::Loop body. The cycle-148 host-relay (TASK-0327) emits the relay block FLAT outside any loop, so a nested w2w Push would either over-count or mis-order.

## Root cause (5-backend variant — THIS TASK)

`backend-common/src/multi_worker_walker/wait.rs:282-293` carries a rank-3+ guard:

    if tile.bounds.len() > 2 || (tile.bounds.len() >= 2 && ty.dims.len() > 2) {
        return Err(EmitError::ContractGap(format!(
            "Wait of data {data:?}: tile rank {} and data dim rank {} \
             exceed the 2D row-loop slice-paste's supported shape ..."
        )));
    }

16-jacobi/distributed constructs a rank-3 tile (partition=rows on the spatial y-axis of a 3D `field[ITERS+1][H][W]` data symbol). The guard's docstring claim 'No shipped schedule constructs this today' was true at cycle 115's landing but became false at cycle 209's landing.

## Proposed shape

Extend `wait_slice` from the current 2-case dispatch (`Flat` 1D + `Rows` 2D) to an N-D nested-loop emit:

- Add a `WaitSlice::NestedRows { outer_axes: Vec<(usize, usize)>, ... }` variant carrying the per-axis (lo, hi) range for each partition-bound axis.
- `render_wait_assign` emits N nested `for` loops, one per partition-bound axis, with the innermost loop body being the existing copy_from_slice.
- The Flat (rank-0 partition) and Rows (rank-1 partition, the 2D case) paths stay — both are special-cases of NestedRows; the optimisation is in the rendered output, not the IR shape.

## Acceptance criteria

1. wait_slice supports rank-3 tile / rank-3 data (the 16-jacobi/distributed shape).
2. Existing wait_assign_slice unit tests still pass.
3. 16-jacobi/distributed × pthreads-sync PASSES bit-identical against reference.bin. PROMOTE the e2e-matrix [[skip]] entry to [[required]] with cycle citation.
4. Sibling promotion: cells on pthreads-async, mp-tcp-event, openmp-rs, mp-uds-event also PASS bit-identical (the gap is in the shared backend-common::wait_slice path; the 5 backends inherit the fix uniformly). Promote those 4 [[skip]] entries to [[required]] in the same cycle.

## Out of scope (separate task)

- mp-tcp-bufsync + mp-tcp-poll: those 2 backends hit TASK-0330 (w2w Push inside Loop body), NOT the wait_slice gap. The w2w-in-Loop gap is architectural (the flat host-relay model can't yet handle in-Loop w2w transfers); see memory project-mp-tcp-event-vs-bufsync-safety-profile. Separate follow-up.

## Regression pins to add alongside the fix

1. Negative-arm unit test pin: a synthetic rank-3 (tile, data) pair that the OLD guard rejected, now passes through the new path. Add to backend-common/tests/wait_assign_slice.rs.
2. Forward-compatibility pin: rank-4+ shape (a 13-cnn-inference batch_parallel-on-4D shape) — either supported by the same N-D path, or still rejected LOUD with an updated error message that doesn't carry the now-false 'No shipped schedule constructs this today' narrative.

## Companion / linkage

- Parent: TASK-0341.02.02.01 (halo_inference (B') refinement; cycle 209). Without (B'), the wait_slice gap is masked by the upstream halo_inference reject.
- Related: TASK-0294 (cycle-115 2D row-loop slice-paste; this task is the architect P2.1 forward-carry from there).
- The wait.rs comment block at lines 273-281 contains a now-false 'No shipped schedule constructs this today' narrative that this cycle (209) is updating to flag the cycle-flip; the next cycle picking up this task should rewrite the block fully once the N-D path lands.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add WaitSlice::NestedRows variant carrying per-axis (lo, hi, stride) plus inner_lo_off/inner_hi_off; keep Flat + Rows byte-identical.
2. Extend wait_slice: derive each data axis range from its tile bound (positional bounds[k] <-> dims[k]) defaulting to full (0..dim) for axes with no bound; validate each range against its dim (loud ContractGap OOB); rank<=2 keeps the existing Flat/Rows arms verbatim; rank>=3 (or rank-2-tile-on-rank-3+-data) builds NestedRows. Degenerate all-full -> Ok(None).
3. Add render_wait_assign arm for NestedRows: N-1 nested for loops over outer axes computing flat base offset, innermost copy_from_slice over [base+inner_lo .. base+inner_hi]. Underscore-prefixed locals (_a0,_a1,...,_base) inside a { } block.
4. Rewrite the now-false 2D-max doc comments (module doc, WaitSlice enum doc, render_wait_assign doc, wait_slice guard block) to describe the N-D path truthfully.
5. Add regression pins to tests/wait_assign_slice.rs: rank-3 middle-axis-partitioned (16-jacobi shape), rank-3 fully-banded, rank-4 forward-compat. Keep all existing tests green.
6. Promote the 5 [[skip]] e2e entries (pthreads-sync/async, mp-tcp-event, openmp-rs, mp-uds-event) to [[required]]; leave mp-tcp-bufsync/poll skipped (TASK-0330). Update block-leading comment.
7. Full gate: build/clippy/test/test-release/e2e; e2e must reach 308/251/0/57/0 with bit-identity on all 5.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Cycle 209b architect P3.1 citation correction ===

The original brief cited `backend-common/src/multi_worker_walker/wait.rs:282-293` for the rank-3 guard. The actual location is:
- Comment block: lines 273-293 (cycle-115 rationale + cycle-209 update).
- `if` conditional + ContractGap return: lines 294-305.

The actual blocker conditional is `if tile.bounds.len() > 2 || (tile.bounds.len() >= 2 && ty.dims.len() > 2) {`. Use the 294-305 range when scoping the patch.

=== Cycle 209b architect P2.2 fold-back (related) ===

The runtime error format-string at wait.rs:295-304 (post-cycle-209b edit) was rewritten in cycle 209b to drop the stale "No shipped schedule constructs this today" claim and cite TASK-0341.02.02.01.01 directly. The implementer of this task should re-rewrite the message once the N-D path lands (the cycle-209b rewrite is interim; once N-D is in place, rank > 2 should be DEFAULT and the ContractGap path should describe what remains genuinely unsupported, if anything).

HONEST-BLOCKED (this cycle). The task premise is INCORRECT: lifting the rank-3 wait_slice guard does NOT promote 16-jacobi/distributed to bit-identical. Empirical reproduction (pthreads-sync):

1. Confirmed the guard fires on field=DataId(0), tile rank 3 / data dim rank 3 (the exact error the task names).
2. Captured the actual rank-3 tile by instrumenting wait_slice: bounds=[(t,0..5),(y,0..8),(x,0..8)] vs dims=[5,8,8] — the WHOLE-ARRAY (all-axes-full) case, which already collapses to Ok(None). No in-tree schedule constructs a BANDED rank-3 tile, so the N-D nested-loop path this task proposes would have ZERO e2e coverage.
3. With the guard temporarily returning Ok(None) for the rank-3 tile, codegen COMPLETES but the emitted binary DEADLOCKS (timeout, no output.bin).

The operative blocker is a transfer-placement deadlock, NOT the wait_slice gap: transfer_inject emits the w2w field Push/Wait INSIDE the innermost for x loop with per-worker-VARIABLE counts (unequal bands: w0/w1=12 exchanges, w2/w3=6, on single-element Slots). w0 blocks forever waiting for a 7th push from w2 that never comes. Full analysis filed as TASK-0341.02.02.01.02 (the REAL blocker); this task now --depends-on it.

DECISION: did NOT land a speculative wait_slice N-D extension. Rationale: (a) it promotes zero cells (the deadlock is upstream and backend-independent); (b) it would add untested codegen surface (no schedule exercises the banded N-D path) — counter to the project norm against shipping untested codegen; (c) the whole-array rank-3 case that 16-jacobi DOES construct already works via the existing Ok(None) degenerate-full collapse, IF the upstream deadlock were fixed.

This task should be REVISITED only after TASK-0341.02.02.01.02 lands and we know whether the hoisted-exchange design constructs a rank-3 BANDED tile (which WOULD need N-D dispatch) or stays whole-array (which would NOT). Re-scope at that point. Tree left at clean baseline; no code changed; e2e baseline 308/246/0/62/0 unchanged. The 5 [[skip]] e2e-matrix entries were NOT promoted (they would have failed/deadlocked).

=== Cycle 211b review-gate outcome (orchestrator) ===

Parallel read-only gate: qa-test-runner GO (commit b680817 tracker-only; workspace builds; wait_assign_slice 11/11; e2e structurally unchanged at 308/246/0/62/0) + mped-architect GO. The architect DECISIVELY verified the BLOCKED finding rather than trusting the narrative:
- Reproduced the deadlock: guard bypassed -> pthreads-sync codegen completes, emitted program exits 124 (timeout), 5 threads blocked on the Slot Condvar (futex) at 3s. A loud compile error becomes a silent runtime hang -> promotes ZERO cells. Central conclusion holds.
- Discharged the key epistemic risk (feedback-implementer-disclosure-mechanism-wrong): dumped EVERY (data,bounds,dims) across all 5 wait_slice backends. The ONLY rank-3 tile for field=DataId(0) is whole-array, 48/48 identical (bounds=[(t,0..5),(y,0..8),(x,0..8)] vs dims=[5,8,8]). Zero banded rank-3 tiles. The banded tiles are all rank-2 on DataId(2) (seed broadcast) taking the existing Rows/Flat path. So the N-D wait_slice path would ship with ZERO coverage -- the implementer did NOT over-generalize from one sample.
- Family cross-validation: bufsync/poll TASK-0330 guard message names the identical in-Loop-w2w root. All 7 backends share ONE upstream transfer_inject placement defect; bufsync/poll fail loud (compile guard), the 5 slot/event backends fail silent (deadlock).

Architect P3 fold-back APPLIED in-thread (commit 5ee22a5): the wait.rs rank-3 guard comment + ContractGap message AND the 5 e2e-matrix.toml jacobi/distributed skip reasons all carried the now-false "wait_slice/.01 is the blocker" diagnosis. Corrected to cite .02 (transfer_inject deadlock) as the operative blocker; kept the guard latent-purpose (future banded rank-3 tile); unified the 2 bufsync/poll reasons (same in-Loop-w2w root, loud-vs-silent failure). Comment/string-only; clippy clean; e2e routing unchanged.

STATE: left In Progress, depends-on TASK-0341.02.02.01.02 (architect P2-a: In Progress acceptable; .01 stated mechanism is dead for THIS fixture, whether N-D is ever needed rides on .02 AC#4 -- if the once-per-t hoist later yields a BANDED rank-3 tile). No active .01 work until .02 lands.

FORWARD-CARRIED from TASK-0341.02.02.01.02 (cycle 212): .01 IS now CONFIRMED co-required for 16-jacobi/distributed — the cycle-211b hedge ("co-required ONLY IF the hoist yields a banded rank-3 tile") resolves to YES, with a SECOND prerequisite.

Empirical chain (pthreads-sync, all reverted after measuring):
1. Built the w2w-exchange placement hoist (transfer_inject: hoist the field Wait+Push out of the for y/for x partition spatial loops to the for t body; send-then-recv order after the band compute). This FIXED the deadlock (exit 124 -> exit 0) — the per-worker-variable-count + recv-before-send rendezvous deadlock is a real, fixable placement bug.
2. BUT the whole-array ACCUMULATE combine produces WRONG VALUES: output == EXACTLY 4x reference (4 = worker count). Proven cell-by-cell (ref[1,1]=14 -> out=56=4*14, etc.).
3. ROOT CAUSE: the whole-array-accumulate fan-in (wrapping_add over the WHOLE field, from same-set-elision + collect_accumulate_waits classifying whole-array tiles as accumulate, TASK-0343) is correct ONLY for DISJOINT single-pass partitions (05-stencil/distributed). For 16-jacobi field is CUMULATIVE across t: after t=0 all workers SHARE field[0]; summing whole local fields double-counts the shared history field[0..t-1] across all N workers => xN.

CORRECT model (what .01 + a combine change must deliver):
- Each worker exchanges ONLY its newly-computed BAND ROWS of field[t] => a BANDED rank-3 tile (y-band sliced, x full, t full). This is exactly the N-D nested-loop wait_slice dispatch .01 is about.
- The combine must be COPY (slice-paste OVERWRITE), NOT accumulate (wrapping_add). collect_accumulate_waits/render_wait_assign currently FORCE accumulate on whole-array tiles; the banded model needs the copy path. So .01 has a co-prerequisite: a partition-band cross-iteration w2w exchange must be classified COPY, not accumulate. (Distinct from the disjoint-fan-in accumulate of TASK-0343.)
- The placement hoist (transfer_inject) is ALSO needed and is the .02 deliverable — but .02 is BLOCKED on .01 because landing the hoist alone (with the wrong whole-array-accumulate combine) yields run-but-xN-wrong, which is worse than an honest deadlock for a [[skip]] cell.

The wait.rs guard comment block (lines ~308-349) still says the 16-jacobi rank-3 tile is WHOLE-ARRAY (cycle-211b). That is the tile shape under the WRONG accumulate model; under the CORRECT banded-copy model it is BANDED. When .01 lands the N-D dispatch + copy-combine, rewrite that comment to reflect the banded reality (memory feedback-comment-doc-lie-recurring).

=== Cycle 212 review-gate (read-only architect) — two refinements for the N-D banded tile ===

1. WRITE-BAND, NOT HALO-READ-RANGE (architect P2-3). When .01 builds the banded rank-3 copy tile, key it on the per-worker WRITE band [source_lo, source_hi), NOT the halo-expanded read range [source_lo-halo, source_hi+halo). The halo-expanded ranges OVERLAP across workers (w0 reads y 0..4, w1 reads y 2..6 -> share rows 2,3); a slice-paste COPY over halo rows would double-write the boundary rows and corrupt them. The write bands (w0=1..3, w1=3..5, w2=5..6, w3=6..7) are disjoint and cover interior rows 1..6; copying those is correct.

2. COPY APPLIES TO HOST GATHER TOO (architect P1-2/P1-3; keep in sync with TASK-0330 + .03). The copy-not-accumulate fix (TASK-0341.02.02.01.03) is needed for BOTH the w2w exchange AND the workers->host field gather. The architect proved the xN on the EXTRACTED 16-jacobi output enters at the host gather of identical full field[ITERS] slices (not the w2w history re-summation). So .01's banded path must also drive the host receiver's field Wait so the host pastes disjoint write-bands rather than accumulating 4 full slices.

CYCLE 213 DONE (commit cc8c4d7). N-D nested-loop dispatch LANDED in wait.rs: nd_banded_slice + WaitSlice::NestedRows. A full-leading + one-banded + full-trailing rank-N tile emits per-leading-index contiguous copy_from_slice over the banded span (16-jacobi field[5][8][8] x partition=rows(y) write-band [(t,0..5),(y,band),(x,0..8)] => one for _a0 in 0..5 loop, per-t y-band span). All-axes-full collapses to Ok(None) (whole-array). Multi-banded rank>=3 + rank-mismatch fail LOUD (nd_banded_slice ContractGap). Unit pins added to tests/wait_assign_slice.rs: nd_banded_rank3_emits_nested_rows_slice_paste (positive emit round-trip), nd_banded_rank3_full_collapses_to_whole_array, nd_banded_rank3_two_banded_axes_returns_contract_gap, rank_mismatch_tile_returns_contract_gap (replaces the old rank_3_or_higher guard test). AC#1 (rank-3 support) MET. AC#2 (existing tests pass) MET. AC#3/#4 (bit-identical + sibling promotion) MET - 5 backends promoted [[required]] (verified by cmp + just e2e 308/251/0/57/0). The old wait.rs rank-3+ ERR guard is gone; the now-false "No shipped schedule constructs this" narrative was already removed cycle-209b and the guard comment rewritten cycle 213.
<!-- SECTION:NOTES:END -->
