---
id: TASK-0049.10.06
title: >-
  BLOCKER 2 root: thread data-DECLARATION order into codegen contract for
  per-worker input.bin layout (DataId is alphabetical, not decl order)
status: In Progress
assignee:
  - '@me'
created_date: '2026-06-03 23:35'
updated_date: '2026-06-04 01:38'
labels:
  - M11
  - backend
  - codegen
  - transport
  - value-correctness
dependencies: []
parent_task_id: TASK-0049.10
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
EMPIRICAL FINDING from TASK-0049.10.04 (slice C1): the per-worker input.bin partition offset CANNOT be computed correctly from the codegen contract today. The reference generator (nuc-nucleus/examples/14-hearing-aid/reference/src/main.rs:74-87) writes input.bin in DATA-DECLARATION order: mic block (mic_in) first, then bt block (bt_in). But DataId is assigned ALPHABETICALLY (acfg/build.rs:60-66 enumerates linked.algo.data.keys(), and algo.data is a BTreeMap<String,_> keyed by name), so for ex14 bt_in=DataId(0), bt_out=1, mic_in=2, mixed=3, spk_out=4. Sorting loaded symbols by DataId therefore yields bt_in BEFORE mic_in => fe(mic_in)=offset 256, rf(bt_in)=offset 0 -- the REVERSE of the reference input.bin layout (mic at 0, bt at 256). So slice C1 Mechanism A (DataId-order concatenation) is UNSOUND for any schedule where >1 worker loads input (cross-worker input partition). Declaration order is LOST at AlgoIR construction (AST->IR stores data into BTreeMap<String,ResolvedData>) and is NOT present in NameSidecar (data_types: BTreeMap<DataId,ResolvedType>) or NameTables (data: BTreeMap<DataId,String>). ROOT FIX: preserve data-declaration order as a Vec<DataId> (or Vec<String>) through AST->IR->ACFG->NameSidecar so the embedded backend can build the global input.bin layout in the SAME order the reference generator hand-writes it. Then multimcu::compute_input_offsets sorts loaded symbols by that declaration-order index instead of DataId. SCOPE: compiler-layer (nucleus-compiler AlgoIR + ACFG + sidecar) + backend consume. SLICE C1 DISPOSITION (TASK-0049.10.04): classification fix landed (fe AND rf recognized as loaders, both get $input -- AC#1 done); single-loader offset 0 landed + 02-split-add regression-safe; the CROSS-WORKER multi-loader offset FAILS LOUD (EmitError) referencing THIS task rather than emitting a byte-wrong offset. AC#2 (fe=0/rf=256 emit) is BLOCKED on this task.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 data-declaration order preserved as an ordered list (Vec<DataId> or Vec<String>) threaded AST->IR->ACFG->NameSidecar (or NameTables); deterministic, no HashMap iteration
- [x] #2 embedded multimcu::compute_input_offsets sorts loaded input symbols by declaration-order index (NOT DataId); ex14 fe(mic_in)=offset 0, rf(bt_in)=offset 256 matching reference.bin; the cross-worker fail-loud in compute_input_offsets is removed and replaced with the real offset
- [x] #3 BLOCKER 3 (TASK-0049.10.05) output-capture ordering also consumes declaration order if it derives a per-saver order; no DataId-order reversal
- [x] #4 just renode-multimcu 14-hearing-aid embedded_multimcu_sync progresses past the input-partition rejection; e2e stays 427/364/0/63/0
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FORWARD-CARRIED from TASK-0049.10.04 review (architect P3-2, effort-estimate-overstated recurrence): the root signal is NOT lost as deeply as "thread decl order through all of AST->IR->ACFG->NameSidecar" implies. AlgoAst.items: Vec<SpItem> (ast.rs:284) ALREADY preserves declaration order verbatim; it is discarded only at the AST->IR lowering bucketing step (algo/lower.rs), and AlgoIR.data is the name-keyed BTreeMap (ir.rs:276, whose own doc ir.rs:266-267 admits "if source order becomes important later we can switch to an index-vec"). So .06 likely needs to CAPTURE the already-ordered AlgoAst.items into ONE new ordered field (e.g. a Vec<DataId> decl-order index on AlgoIR or NameSidecar) at lowering, then carry that single field downstream to the embedded backend offset computation — NOT a from-scratch order-threading through every layer. Start from AlgoAst.items. The reference input.bin layout (mic_in@0 ++ bt_in@256) AND output reference.bin layout (spk_out ++ bt_out) are BOTH declaration order, so .06 unblocks both C1.AC#2 (input offsets) and C2 (output capture ordering, TASK-0049.10.05).

Implementation plan (cycle, implementer):
1. AlgoIR: add `pub data_decl_order: Vec<String>` (decl order of data symbols), documented; Default/Eq derive unaffected.
2. lower_data: push name.clone() into ir.data_decl_order on the success path right after ir.data.insert (only successfully-lowered decls; lower_algo iterates ast.items in source order).
3. NameSidecar: add `pub data_decl_order: Vec<DataId>` with #[serde(default)], mirroring partition_worker_ranges wire-compat.
4. build_sidecar (h): map linked.algo.data_decl_order names through acfg.name_data to DataIds; panic-with-context on desync (mirrors data_types (a) block).
5. multimcu compute_input_offsets: replace >=2-loader fail-loud with real computation ordered by sidecar.data_decl_order position; per-symbol byte size = alloc_len(did) * scalar_byte_width; worker base = offset of its FIRST loaded symbol; guard non-contiguous multi-loader as typed EmitError. Add scalar byte-width helper (no hardcode 4). Single-loader early-return stays offset 0.
6. Rewrite compute_input_offsets doc + input_base_offset field doc (no longer fail-loud).
7. Add direct unit test: ex14-shaped (decl_order [mic_in,bt_in], DataId reversed bt_in=0/mic_in=2) -> fe=0/rf=256; flip decl_order -> offsets flip (bite).
8. Update proptest_serde namesidecar_strategy + field-completeness guard.
Sibling sites: only sidecar.rs:729 literal + proptest strategy need the field; all other NameSidecar uses go via Default.

IMPLEMENTATION COMPLETE (commit ead198b).

What landed:
- AlgoIR.data_decl_order: Vec<String> (algo/ir.rs); pushed in lower_data on the SAME success path as data.insert (algo/lower.rs) so it is exactly the source decl order of data that lowered.
- NameSidecar.data_decl_order: Vec<DataId> with #[serde(default)] (sidecar.rs); built in build_sidecar block (j) by mapping decl-order names through acfg.name_data, panic-with-context on desync (mirrors data_types block (a)).
- ScalarType::fixed_byte_width() -> Option<usize> (algo/ast.rs): EXACT fixed widths; None for usize/isize so consumers fail loud rather than guess host-vs-32bit-target width. NOTE: did NOT fold backend-common mpi_plan::scalar_bytes (it is a deliberate over-estimate budgeting usize=8 + Bool harmlessly; different semantics/target — folding would be a behavior-changing risk out of scope).
- multimcu::compute_input_offsets (now pub(crate) for the unit test): replaced >=2-loader fail-loud with real computation ordered by data_decl_order position; per-symbol byte size via new symbol_byte_size helper (alloc_len * fixed_byte_width); worker base = offset of FIRST loaded symbol; typed EmitError on non-contiguous multi-loader, usize/isize width, decl-order-miss, and overflow. Single-loader early-return offset 0 untouched.
- Doc rewrites: compute_input_offsets doc + input_base_offset field doc (were doc-lies once fail-loud removed).

Tests:
- NEW tests/input_offsets.rs (4 tests): ex14-shaped two loaders with REVERSED DataIds (bt_in=0/mic_in=2), decl_order [mic_in,bt_in] -> fe=0/rf=256; FLIP decl_order -> offsets flip (THE BITE proving it reads decl_order not DataId); single-loader stays 0; decl-order-miss fails loud.
- bin_shape ex14 test flipped from ..._fails_loud_pending_decl_order to ..._emits_decl_order_offsets: lowers the REAL ex14 (prog.embedded.algo.nuc + kernels.embedded.rs) through the full pipeline and asserts emitted fe/main.rs has NUC_INPUT_BASE 0 and rf/main.rs has 256. This is the end-to-end AC#2/AC#4 proof on real sources.

Sibling sweep: all AlgoIR{} literals (4 src test fixtures in reuse_inference/halo_inference, 4 in tests/sidecar_*) + the 2 relevant NameSidecar{} literals (build_sidecar return + proptest namesidecar_strategy) updated. proptest field-completeness guard + strategy extended (bundled the new field with cumulative_data into a nested tuple to stay under the ~12 arity ceiling). All other NameSidecar/AlgoIR uses go via Default/lowering.

Gate (actual numbers): build green; clippy green; just test 1343/0/3; just test-release 1341/0/3 (the 2-test dev/release delta is the known debug_assert #[should_panic] divergence, not a regression); e2e 427/364/0/63/0 (inert — all 7 embedded_multimcu cells skip); check-textual-replace + check-include-str-coverage green; just renode-multimcu 02-split-add split BYTE-EXACT 1024 bytes.

AC status:
- AC#1 MET: decl order threaded AST->IR->ACFG->NameSidecar, deterministic Vec, no HashMap iteration.
- AC#2 MET: compute_input_offsets orders by decl-order index; ex14 fe(mic_in)=0/rf(bt_in)=256; cross-worker fail-loud removed + replaced. Proven by unit test (bite) + real-ex14 emit test (NUC_INPUT_BASE 0/256).
- AC#3 NOT TICKED (honest): the decl_order signal now EXISTS on NameSidecar for TASK-0049.10.05 to consume, but .05 has not landed its output-capture consumer yet. .05 forward-note appended. Tick when .05 consumes it.
- AC#4 MET: emit progresses PAST the input-partition rejection (bin_shape test emits successfully; the renode 14-hearing-aid recipe now fails at a LATER link stage — schedule names fe_capture/rf_receive but the recipe hardcodes prog.algo.nuc/kernels.rs which lack those place directives; that is the slice-D hardcode, explicitly out of scope). e2e stays 427/364/0/63/0.

GOTCHAS for next cycle (slice C2 / TASK-0049.10.05 / slice D):
1. ScalarType is NOT Copy — fixed_byte_width takes &self.
2. The proptest tuple was AT the ~12 arity ceiling; the new field is nested with cumulative_data. Adding another serde field will need similar nesting.
3. The renode-multimcu recipe HARDCODES prog.algo.nuc + kernels.rs (NOT the *.embedded.* variants the embedded_multimcu_sync schedule needs). ex14 full byte-exact via the recipe is blocked on fixing that hardcode (slice D) AND TASK-0049.02 stateful kernels — NOT on this contract work.
4. compute_input_offsets is now pub(crate) (was private) purely for the unit test; matches verify_control_sync_subsumed visibility.
5. usize/isize input widths intentionally fail loud (target word size ambiguous); if a real schedule ever loads a usize input on the 32-bit target, decide the target width explicitly there.

REVIEW GATE GO/GO (cycle 2026-06-04, commit ead198b). qa-test-runner INDEPENDENTLY reproduced: test 1343/0/3, test-release 1341/0/3, e2e 427/364/0/63/0 (x2, non-flake), renode-multimcu 02-split-add BYTE-EXACT 1024B, clippy clean under -D warnings; new input_offsets.rs (4 tests incl the decl-order BITE) + flipped bin_shape ex14 emit test all pass. mped-architect: all 8 checks PASS, no P1/P2. Two P3 (both optional, NOT folded): P3-1 symbol_byte_size recomputed in the contiguity loop (O(n) redundant, irrelevant at current symbol counts); P3-2 cosmetic note that 427/364/0/63/0 inertness rests on the 7 embedded_multimcu cells being [[skip]] (harness hardcodes prog.algo.nuc, TASK-0049.03) so the offset layout gets ZERO run-and-diff coverage today — only emit-string + unit-test guards; the renode tier-3 run-and-diff is the real integration gate, blocked on slice D + TASK-0049.02. AC#1/#2/#4 met+verified; AC#3 is a forward-constraint on TASK-0049.10.05 (output-capture ordering) — signal now exists on NameSidecar.data_decl_order, .05 ticks #3 when it consumes it. Task stays In Progress until .05 lands (honest: full AC set not satisfiable here).

AC#3 DISCHARGED by TASK-0049.10.05 (slice C2, commit 27cab88): the per-saver OUTPUT-capture order is computed by multimcu::compute_output_capture sorting saved symbols by NameSidecar.data_decl_order index (NOT alphabetical DataId), mirroring compute_input_offsets. ex14 yields capture order [spk_out(fe), bt_out(rf)] = reference.bin spk@0++bt@256 (decl order spk_out line 80 before bt_out line 81), NOT the DataId reversal (bt_out=1 < spk_out=4). Pinned by tests/output_capture.rs incl a decl-order BITE test (DataIds fixed/reversed, flip data_decl_order -> capture order flips; empirically verified to FAIL under a DataId-order keying). No DataId-order reversal.
<!-- SECTION:NOTES:END -->
