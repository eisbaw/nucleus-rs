---
id: TASK-0437.01
title: 'Split pthreads-sync/src/lib.rs mega-file (canonical backend, production-heavy)'
status: Done
assignee:
  - '@me'
created_date: '2026-06-05 14:07'
updated_date: '2026-06-05 15:49'
labels:
  - hardening
  - tech-debt
  - mega-file
dependencies: []
parent_task_id: TASK-0437
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
pthreads-sync/src/lib.rs is ~1044 LoC and production-heavy: unlike block_transform.rs (split in TASK-0437 by a mechanical inline-test carve-out), this file has NO inline test mod to extract. Its bulk is emit / render_main_rs / render_array_init / render_events_in PLUS the PUBLIC render_single_worker_main* API that five sibling backends depend on (pthreads-async / mp-tcp-bufsync / openmp-rs / mp-tcp-poll / mp-uds-event). Splitting therefore requires moving PRODUCTION fns into cohesive sub-modules along the module-doc Event:: arm seams (Fire / Loop / Sync-Push-Wait, per the docstring) while PRESERVING the public API EXACTLY (re-export the render_single_worker_main* surface from lib.rs so downstream `use backend_common::... / pthreads_sync::...` paths keep resolving). This is a distinct risky-refactor boundary from the block_transform carve-out (phase3 anti-pattern: never batch across a risky refactor boundary), hence deferred to its own cycle. The allow-list entry for nucleus/backends/pthreads-sync/src/lib.rs in justfile:check-mega-files STAYS until this lands; remove it (direction-B guard) only after the split drops the file under 1000 LoC. Gotchas carried forward from TASK-0437: (1) edition-2021 foo.rs + foo/ dir coexistence works (block_transform.rs stayed the module file, tests.rs went under passes/block_transform/); for a production split prefer either lib.rs as the module file + sub-files under a pthreads_sync_emit/ dir OR named sibling modules declared from lib.rs. (2) `use super::*` resolves to the parent module after a move. (3) the direction-B allow-list removal is MANDATORY and is checked by `just ci` (not by the cheap pre-commit subset). (4) clippy re-lints moved code — watch doc_lazy_continuation on any /// you touch and needless_borrow.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0437 cycle review (architect P3-2): when you split pthreads-sync/src/lib.rs and REMOVE its check-mega-files allow-list entry, also fix/drop the stale rationale comment on that bullet — it currently says pthreads-sync is "~flat at 1035" but the file is 1044 LoC. Removing the entry deletes the comment, so this resolves naturally; just do not copy the stale "1035" figure forward.

Implementation plan (cycle): carve the cohesive "Event rendering" section (lib.rs ~507-957: render_events/render_events_in/render_event) VERBATIM into new sibling src/events.rs; declare `mod events;` + `pub(crate) use events::{render_events, render_event};` from lib.rs for the break_loop consumer (which calls crate::render_events + crate::render_event with ZERO change). render_events_in stays private to events.rs. Public render_single_worker_main*/emit/EmitResult API stays untouched in lib.rs. Remove the stale allow-list entry + the "~flat at 1035" rationale bullet from justfile:check-mega-files (direction-B). Prove fn bodies byte-identical; run just ci once.

RESULTS (cycle done): lib.rs 1044->609 LoC (under the 1000 fence); new events.rs 471 LoC holding render_events/render_events_in/render_event. CONTENT-PRESERVATION PROOF: extracted HEAD lib.rs lines 511-957 and diffed against the three-fn region of events.rs -> IDENTICAL (zero diff); no fn body changed, pure move. Re-export added near mod events;: pub(crate) use events::{render_event, render_events}; break_loop.rs was NOT modified (its crate::render_events / crate::render_event calls resolve through the re-export). render_events_in stayed private to events.rs. Pruned now-unused lib.rs imports (event::{IterVar, ViolationKind}; check_frame::{emit_count_branch, emit_log_branch, sanitize_loop_var}; render::{data_name, render_const_expr, render_fire_args, render_fire_output_assign, render_loop_bounds, render_reuse_*}); kept RenderCtx + render_array_init_for_combine (still used by render_main_rs). Public API render_single_worker_main / _with_kernels_attr / _with_signature / emit / EmitResult / EmitError / NameTables UNTOUCHED, still in lib.rs (five sibling backends depend on it). justfile: removed the pthreads-sync/src/lib.rs allow-list entry AND deleted the stale "~flat at 1035" rationale bullet (did NOT carry the figure forward); event.rs entry intact. just check-mega-files OK both directions. full just ci: EXIT 0 (GREEN) — clippy clean (-D warnings), all 4 negative/determinism arms bit correctly (xbackend-corruption, required-coverage typo, etc.). CAVEAT: my own tail-60 truncation dropped the literal e2e-totals + test-count lines from the saved log; did NOT re-run e2e (efficiency mandate). Proof rests on exit-0 + byte-identical-body diff: a pure move cannot change e2e totals or test counts; any divergence would have failed the gate. Parent TASK-0437: this was its remaining (pthreads-sync) half; block_transform half landed in TASK-0437 already, so the parent is now fully addressed — FLAGGED for orchestrator to tick/close after the independent review gate (not closing it myself). GOTCHA for next person: re-export sufficed for break_loop with zero change; ViolationKind/IterVar/the render_* set were ONLY used by the moved arm and had to be pruned from lib.rs or clippy -D warnings fails.
<!-- SECTION:NOTES:END -->
