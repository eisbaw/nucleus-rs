---
id: TASK-0044.03.02.02
title: >-
  event_plan: golden-snapshot unit pin for the emit_handshake transport block
  (one host + one non-host bin per backend)
status: Done
assignee:
  - '@claude'
created_date: '2026-05-29 23:53'
updated_date: '2026-05-30 01:15'
labels:
  - testing
  - hardening
  - event_plan
  - regression-pin
dependencies: []
parent_task_id: TASK-0044.03.02
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect P3-2 on the TASK-0044.03.02 event_plan lift: the most transport-divergent emitted surface — the wholesale emit_handshake block (TCP port-in-file rendezvous vs UDS path-as-rendezvous + sun_path 104-byte cap message) — has NO unit golden. The strongest unit oracle (mp-uds-event/tests/multi_worker_emit.rs::assert_uds_bin_equiv_tcp) DELIBERATELY strips the entire transport prelude before comparing (documented structural slack); the per-backend tests only .contains()-check handshake markers (e.g. UnixListener::bind), not a full-string golden. So a future hand-edit to emit_handshake could drift without a unit test firing — the only regression net today is the e2e gate + a manual git-worktree A/B. Bytes are CORRECT today (cycle byte-identity A/B proven on both backends incl. the rendezvous+relay path). Hardening: add a full-string golden snapshot of one host bin + one non-host bin per backend that pins the handshake block verbatim. Keep e2e bit-identical (test-only).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Orchestrator-direct. In-crate unit test (emit_handshake is pub(crate), only callable in-crate) calling T::emit_handshake directly with fixed inputs (host: "host"/true/["w0"]; non-host: "w0"/false/["w0"]) — the most direct pin of the architect-flagged surface. Golden = exact captured output, embedded as a raw-string const, asserted full-string. Bootstrap via temporary capture test -> /tmp -> embed -> delete capture. Tests added to each backend src/multi_worker.rs #[cfg(test)] mod tests (which already reaches crate::plan). e2e bit-identical (test-only). Closes the gap: a behavior-preserving edit to emit_handshake is caught by NOTHING today (e2e checks runtime output not emitted source; emit-pinning tests strip the prelude).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CYCLE CLOSE (orchestrator-direct + parallel review gate, both GO). Commit 11815c8; doc fold-back d64d982.

APPROACH: in-crate unit test per backend (emit_handshake is pub(crate), callable only in-crate) calling crate::plan::{Tcp,Uds}EventTransport::emit_handshake directly with fixed inputs (host: "host"/true/["w0"]; non-host: "w0"/false/["w0"]) and asserting full-string equality against an inline raw-string GOLDEN const. Goldens BOOTSTRAP-CAPTURED from live emit (temp capture test -> /tmp -> python-generated inline consts -> capture test deleted), so zero transcription error. Storage: inline r#"..."# consts (NOT include_str! -- a .txt include_str! in src/ trips check-include-str-coverage; tests/ cannot reach pub(crate) emit_handshake). This CLOSES the real gap: a behavior-preserving edit to emit_handshake is caught by NOTHING today (e2e checks runtime output.bin not emitted source; the integration emit-pinning tests deliberately strip_transport_prelude; per-backend tests are .contains()-only).

REVIEW GATE:
- qa-test-runner GO: build OK; clippy clean -D warnings (no needless_raw_string_hashes false-flag -- the goldens contain " so the hashes ARE needed); dev 1122/0/3, release 1121/0/3 (+2 goldens, exact; delta-1 = TASK-0291 skew); e2e 308/246/0/62/0 byte-identical x3 (md5-matched verdict grid); check-textual-replace + check-include-str-coverage PASS (goldens are inline, impose no coverage obligation); both emit_handshake_golden tests run+pass (not ignored/filtered).
- mped-architect GO: pin BITES (assert_eq compares LIVE emit output vs FROZEN const, not live==live/empty==empty); golden correctness confirmed (UDS golden carries check_uds_path_len + UDS_PATH_CAP=104 in both branches; TCP host golden carries the rdv_tmp->fs::rename atomic TOCTOU-closing publish); no visibility widening (transport structs stay pub(crate), test reaches them in-crate); justification real. 1-WORKER vs 2-worker analyzed: 1-worker ACCEPTED -- the host branch loop body has no cross-iteration state/separator (each iter is self-contained writeln!s), and the non-host branch ignores non_host_names entirely (uses only wname); a wrong-block-count bug is non_host_names.len()-driven (plan layer) -> caught by e2e at runtime as a missing-peer connection failure. A 2-worker golden would only double size to pin an iteration-coupling that structurally cannot exist (filed mentally as optional, NOT a task).

P3 fold-backs applied in-thread (d64d982): (1) docstring over-promised a "capture recipe in the notes" (rot-prone after close) -> inlined a concrete 3-line regenerate recipe in the docstring itself; (2) "non_host_names carries the peer list" was imprecise -> reworded to "full list of non-host workers (host branch loops it; non-host branch ignores it)". Both fixes applied to BOTH backends (silent-sibling). Re-gated: build/clippy clean, golden tests pass, e2e 308/246/0/62/0.

GOTCHA (forward lesson for any future emit_handshake / transport golden): goldens captured with 1-worker inputs pin the TEMPLATE only because the host per-worker loop has no inter-iteration coupling; if a future emit ever adds cross-iteration state or a between-peer separator, the golden MUST be regenerated with >=2 workers to catch it.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE. Closed architect P3-2 from the event_plan lift: the transport-divergent emit_handshake block had no unit golden, so a behavior-preserving edit to it was caught by nothing (e2e checks runtime output not emitted source; integration tests strip the prelude). Added an in-crate emit_handshake_golden test per backend (commit 11815c8) calling emit_handshake directly with call-site-faithful inputs and asserting full-string equality against bootstrap-captured inline raw-string goldens (host + non-host). The pin BITES (live-vs-frozen). UDS golden carries check_uds_path_len/104-cap; TCP carries the tmp+rename atomic publish. No visibility widening (transport structs stay pub(crate)). Both reviews GO; e2e 308/246/0/62/0 x3 codegen-inert; dev 1122/0/3 release 1121/0/3. 2 P3 doc-precision fold-backs applied (d64d982). AC#1-3 met (in-crate emit_handshake call golden chosen over the AC literal "bin golden" -- equivalent + more focused on the flagged surface, decoupled from full-project emit which the existing tests already pin minus the stripped prelude).
<!-- SECTION:FINAL_SUMMARY:END -->
