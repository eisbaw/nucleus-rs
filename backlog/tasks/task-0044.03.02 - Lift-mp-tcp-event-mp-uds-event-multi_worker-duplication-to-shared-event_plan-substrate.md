---
id: TASK-0044.03.02
title: >-
  Lift mp-tcp-event + mp-uds-event multi_worker/ duplication to shared
  event_plan substrate
status: Done
assignee:
  - '@claude'
created_date: '2026-05-27 09:42'
updated_date: '2026-05-29 23:54'
labels: []
dependencies: []
parent_task_id: TASK-0044.03
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Context

TASK-0044.03.01 cycle 197 landed mp-uds-event multi-worker by COPYING mp-tcp-event's multi_worker/ subtree (~3000 LoC) with the TCP→UDS transport swap. The two backends now have:

- encode.rs: BYTE-IDENTICAL between mp-tcp-event and mp-uds-event.
- walkers.rs + relay.rs: differ ONLY in error-message branding (`mp-tcp-event` → `mp-uds-event`).
- worker_program.rs: bulk of the TCP→UDS call-site swaps (UnixListener vs TcpListener, UnixStream vs TcpStream, path-as-rendezvous vs port-in-file).
- mod.rs: Plan struct + dispatcher; structurally identical.

## Scope (distinct from TASK-0044.02.03)

This lift is DISTINCT from TASK-0044.02.03's bufsync+poll lift because the underlying Plan substrate is different:

- TASK-0044.02.03: bufsync/poll = blocking/nonblocking-poll FIFO Plan (~1700 LoC duplication).
- THIS TASK: event/uds = per-seq-demux mio reactor Plan (~3000 LoC duplication).

Two independent lift opportunities. Both follow the same shape: parameterise a small trait over the transport-specific details (Listener/Stream type, rendezvous mechanism, wire framing) and have both backends consume the shared crate.

## Suggested approach

1. New crate or backend-common module: `event_plan` (or `backend-common::event_plan`).
2. Trait `EventTransport`:
   - `type Listener`, `type Stream`
   - `fn listener_bind_call(...)` -> emit string (TcpListener::bind(addr) vs UnixListener::bind(path))
   - `fn stream_connect_call(...)` -> emit string
   - `fn mio_stream_type()` -> emit string ("mio::net::TcpStream" vs "mio::net::UnixStream")
   - `fn rendezvous_dir_strategy()` -> RendezvousDirStrategy enum (TASK-0044.03.01 cycle-197b architect P3.1 — the `/tmp`-mktemp swap surface lifts cleanly here)
3. mp-tcp-event: impl EventTransport for TcpTransport.
4. mp-uds-event: impl EventTransport for UdsTransport.

## Forward-carried from cycle 197b architect P2.1

Cycle-197b architect review explicitly named this lift trigger. encode.rs being verbatim + walkers.rs/relay.rs only differing in branding makes this lift ready RIGHT NOW (no architectural unknowns).

## Acceptance criteria

1. Shared event_plan crate / module created with EventTransport trait.
2. mp-tcp-event consumes via TcpTransport impl; emit output byte-identical to pre-lift.
3. mp-uds-event consumes via UdsTransport impl; emit output byte-identical to pre-lift.
4. e2e baseline preserved: 210/190/0/20/0 (cycle 197 post-cycle 197b baseline).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
PHASE 0 (done): captured live e2e baseline (running). Read blueprint tcp_plan/* + both event backends multi_worker/* + lib.rs consumers + emit-pinning tests. Verified duplication map: encode.rs byte-identical; relay.rs 1 brand string; walkers.rs 16 brand/doc lines; mod.rs ~145 lines (docs/brand + render_run_sh /tmp swap); worker_program.rs genuine transport divergence in emit_handshake + stream-type consts in reactor/barrier-shim + imports block + file header.

DESIGN: new backend-common module event_plan/, parameterised over EventTransport trait. NOTE this is a SEPARATE substrate from tcp_plan (event Plan is reactor/chan_caps/peer_index based, not FIFO). Plan<a, T: EventTransport> carries PhantomData<fn() -> T>; all trait methods associated (no self).

EventTransport trait surface:
- const BACKEND_NAME (ContractGap diagnostics)
- const FILE_HEADER_LINE (full //! Generated by... first line; TCP/UDS wording genuinely differs)
- const SO_BUF_COMMENT (run.sh commentary)
- const CTRL_STD_STREAM_TY (std::net::TcpStream vs std::os::unix::net::UnixStream)
- const DATA_MIO_STREAM_TY (mio::net::TcpStream vs mio::net::UnixStream)
- fn emit_role_imports(out, is_host) - role-specific imports block (TCP has use std::fs + io::Write; UDS does not)
- fn emit_handshake(out, wname, is_host, non_host_names) - the BIG wholesale per-backend block (rendezvous + stream setup), built structurally from args, NO Plan dep
- fn render_run_sh_post(shared) -> Result<String,EmitError> - UDS /tmp swap; TCP identity

PHASING (commit per unit, gate byte-identical each):
1. Scaffold event_plan/ + EventTransport trait + register in lib.rs.
2. Lift encode.rs (verbatim) + relay.rs (brand via BACKEND_NAME) + walkers.rs (brand via const, neutral docs). Wire both backends shims; gate.
3. Lift Plan struct/build (mod.rs) + worker_program.rs with transport behind EventTransport methods. Each backend plan.rs supplies impl. Gate per backend.
4. Collapse each backend multi_worker/ to thin plan.rs shim + re-exports so lib.rs + in-crate tests resolve unchanged.

ORACLE: byte-identical emit BOTH backends. mp-uds-event tests/multi_worker_emit.rs already does cross-backend byte-diff (post-prelude-strip) - strongest unit oracle. e2e totals must equal captured baseline. test-release load-bearing.

HONEST PARTIAL fallback if hard tier intractable byte-identically: land easy tier (encode/relay/walkers+scaffold), file worker_program/mod follow-up with dep edge.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Forward-carried from TASK-0044.02.03 (cycle 235, mp-tcp-bufsync+poll lift LANDED)

The sibling sync-TCP lift completed; lessons that bear on this event-backend lift:

1. **Trait shape that worked**: `WirePrimitives` with 3 consts (BACKEND_NAME for ContractGap diagnostics, FILE_HEADER_BACKEND + FILE_HEADER_TASK for the EMITTED `//! Generated by the X backend (TASK-Y,...)` header) + 5 fns (read_msg_expect_call/write_msg_call/barrier_cross_call build the call STRUCTURALLY via format!, sock_setup_extra(sock)->Option<String>, so_buf_comment()->&str, relay_banner(pad)->String). `Plan<'a, W>` carries `PhantomData<fn() -> W>`; ALL trait methods associated (no self) so no W value is threaded. Lives in backend-common/src/tcp_plan/.

2. **Canonical EmitError lives in backend-common ALREADY** — both backends `pub use backend_common::EmitError;`. In lifted code use `crate::EmitError` (= backend_common::EmitError) directly. `NameTables` is `nucleus_compiler::NameTables`. backend-common's only dep is nucleus-compiler — the lift adds NO new dep (wire::... strings are EMITTED text, not calls). Same will hold for mp-tcp-event/mp-uds-event.

3. **The #1 byte-drift risk is hand-transcription of \x20-escaped raw codegen strings** (the rendezvous/connect_retry blocks). My first build FAILED with an unclosed-delimiter because a sleep line + 2 closer braces had wrong \x20 counts. MITIGATION (do this proactively): after lifting any \x20-heavy raw string, run `awk '/x20/{n=gsub(/\\x20/,"");print n"|"$0}'` on BOTH the original git blob and the lifted file and `diff` the count|content sequences — must be 0 diff. Cheaper than chasing a compile error.

4. **Byte-identity verification technique (3 layers, all independent)**: (a) e2e 5-tuple unchanged; (b) the per-bin legal-swap canonicalisation oracle each backend already ships (event backends have their own equiv tests); (c) DIRECT `nucleus build --algo .. --sched .. --backend X --out DIR` for both backends + diff after reverting documented legal swaps — do NOT inspect post-`just e2e` scratch (deleted on success per memory project-e2e-harness-scratch-lifecycle).

5. **doc_lazy_continuation WILL fire** when you reword module-docs — a wrapped `//!` line beginning with `+`/`-`/`*` at column 5 is read as a Markdown list marker. I hit it on a '+ worker-program assembly' wrap; fixed by using commas/'and'. Re-run `just clippy` independently (it is -D warnings).

6. **ContractGap messages are NOT emitted code** — they are compile-time diagnostics, only weakly pinned by per-backend unit tests' substring asserts. A canonical shared message parameterised by W::BACKEND_NAME is fine as long as it retains every pinned substring (grep each backend's tests/*.rs for the asserts FIRST). mp-tcp-event/mp-uds-event likely have analogous host-excluding-barrier + wait-before-push message pins (TASK-0044.08/09/10/11 family) — audit those before collapsing.

IMPLEMENTATION COMPLETE (full lift, not a partial) — commit a9e8f4f. Left In Progress for orchestrator independent review gate (qa-test-runner + mped-architect) per implementer contract.

WHAT WAS LIFTED (backend_common::event_plan, 1721 LoC single source of truth):
- transport.rs: the EventTransport trait (BACKEND_NAME, FILE_HEADER_PREFIX, CTRL_STD_STREAM_TY, DATA_MIO_STREAM_TY, SO_BUF_COMMENT consts; emit_role_imports / emit_handshake / render_run_sh_post fns). All associated (no self); Plan carries PhantomData<fn()->T>.
- encode.rs (verbatim), walkers.rs (branded via T::BACKEND_NAME, neutral docs), plan.rs (Plan struct+build+all accessors+peer-index routing), relay.rs (host-relay codegen), worker_program.rs (render_worker_program + emit_reactor_and_chans + emit_barrier_shims + render_run_sh; handshake delegated to T::emit_handshake, stream-types via consts).

WHAT STAYED PER-BACKEND (the EventTransport impl in each backends src/plan.rs):
- TcpEventTransport: TCP port-in-file rendezvous handshake (TASK-0176 TOCTOU-safe), TcpListener/TcpStream/mio::net::TcpStream, render_run_sh_post=identity.
- UdsEventTransport: UDS path-as-rendezvous handshake + sun_path 104-byte cap check (AC#9), UnixListener/UnixStream/mio::net::UnixStream, render_run_sh_post=/tmp-mktemp swap (single ALLOW-annotated String::replace on a FIXED bash-literal block, fail-loud on needle-miss).
- Each backends src/multi_worker.rs is now a thin re-export shim (pub(crate) use crate::plan::{Plan, render_run_sh}) carrying the in-crate Branch-A tests (which call Plan::build directly).
Justification: handshake/stream-types/run.sh are the GENUINE transport divergence; supplying the handshake wholesale (verbatim per backend) beats a clever unification that shifts one byte (oracle is byte-identity).

BYTE-IDENTITY (hard gate) PROVEN 3 WAYS:
1. per-backend emit-pinning tests green: mp-tcp-event {multi_worker_emit 10, host_relay_emit 3, loop_body_w2w_push 3, in-crate Branch-A 2}; mp-uds-event {multi_worker_emit 4 incl 3 cross-backend structural-twin uds-equiv-tcp oracles, single_worker_emit 2, skeleton 2, in-crate 2}.
2. DIRECT nucleus build emit on 4 cells/backend (06/distributed2 w2w-relay, 02/split, 16-jacobi single-worker, 15-transpose host-excluding-mediated) git-stash A/B diffed vs pre-lift -> BYTE-FOR-BYTE IDENTICAL both backends; re-confirmed post-commit.
3. e2e 308/246/0/62/0 across 3 samples == captured pre-change baseline (NB: task md cited STALE 210/190/0/20/0; live baseline is 308/246/0/62/0 per memory + my pre-change capture).

GATE: build OK, clippy OK (-D warnings), dev test 1110/0, test-release OK, e2e 308/246/0/62/0 (x3 non-flake). check-textual-replace-on-codegen + check-include-str-coverage OK.

GOTCHAS / LESSONS:
- doc_lazy_continuation + doc-quote fired 4x as the cycle-235 sync-TCP-lift notes predicted: a wrapped /// line starting with + (list-marker) or >= (blockquote >) at col 5 trips it. Fixed by rewording (+ -> plus/and; ">= 2 invariant" -> "len() >= 2 invariant"). Always independently re-run just clippy.
- The event Plan is a SEPARATE substrate from tcp_plan (reactor/chan_caps/peer_index vs FIFO) — same trait-over-Plan SHAPE, different internals; do NOT try to merge them.
- Self-caught one doc-link-lie I introduced during a const rename (FILE_HEADER_FMT -> FILE_HEADER_PREFIX left one stale [`...FILE_HEADER_FMT`] intra-doc link in worker_program.rs module doc); fixed + amended.
- Field visibilities: Plan fields are pub (consumed cross-module within backend-common); accessors that backends do not need are pub(crate). This is WIDER than the post-TASK-0340.04 mp-tcp-event tightening (which made mod.rs Plan items private). Flagged as a candidate for a visibility-tightening sweep follow-up (see follow-ups) — but Plan now lives in backend-common where the consumers are sibling modules, so some widening is structural.

REJECTED APPROACH: parameterising render_run_sh_multi with a RendezvousDirStrategy enum (cycle-197b architect P3.1). Chose render_run_sh_post trait method instead — keeps the shared template untouched + byte-identity-safe + de-duplicates the swap. Forward-carried the typed-slot idea into TASK-0288 notes.

PRE-EXISTING LIMITATION (NOT introduced here, preserved verbatim): mp-uds-event has no integration-layer host-excluding-barrier ContractGap pin (mp-tcp-events host_excluding_barrier_is_typed_contract_gap is mp-tcp-event-only); the cycle-197 work deliberately left this as a follow-up "if the mediation gate is loosened". The cross-backend structural-twin oracle (transpose_15_distributed_rows_uds_equiv_tcp) gives strong byte-comparison coverage. Not filing a new task (would be scope creep on a pre-existing carried gap); noted honestly.

FORWARD-CARRIES: (a) TASK-0288 notes updated re render_run_sh_post being the new home of the UDS run.sh swap. (b) A future 3rd event backend would add a third EventTransport impl only (the lift is now ready for it) — no task exists yet to carry into.

CYCLE CLOSE (orchestrator review gate, both reviewers GO). Implementation commit d7599de. NO fold-back code change required (no P1/P2).

REVIEW GATE RESULT:
- qa-test-runner GO: build OK; clippy clean (-D warnings, FORCED recompile so not a warm-cache false-clean; no doc_lazy_continuation/dead_code/unused/unreachable_pub/private_interfaces from the module move); dev test 1120/0/3, release 1119/0/3 (delta=1 = TASK-0291 should_panic skew); e2e 308/246/0/62/0 byte-identical x3; check-textual-replace-on-codegen + check-include-str-coverage PASS; per-backend emit oracles all green (mp-tcp-event 21/0 incl multi_worker_emit/host_relay_emit/loop_body_w2w_push; mp-uds-event 15/0 incl the cross-backend uds_equiv_tcp byte-comparison oracles, 0 ignored/filtered); commit touches ZERO example fixtures/snapshots.
  NOTE: implementer self-reported dev 1110/0; QA RE-MEASURED 1120/0/3 (no failures either way; the implementer undercounted, recorded fact is the reviewer number).
- mped-architect GO: INDEPENDENTLY reproduced byte-identity via git-worktree A/B against parent 1d84805 -- 5 schedules x 2 backends = 10 byte-for-byte-identical diffs INCLUDING the rendezvous+relay path (verified emitted bins contain UnixListener/mio UnixStream/check_uds_path_len/rendezvous_dir). Verified clean: the ONE sanctioned String::replace (mp-uds-event/src/plan.rs ~296-328) operates on a FIXED bash-literal, //ALLOW-annotated, FAIL-LOUD typed EmitError::ContractGap on needle-miss (not panic, not silent); emit_handshake built structurally via writeln!/format!; no comment-doc-lie in the shared module; no reverse dependency arrow (backend-common Cargo.toml dep = only nucleus-compiler); determinism clean (all BTree*); both lib.rs + tests resolve through the new shims; AC#4 honestly verified against the LIVE 308/246/0/62/0 (the md AC#4 cites a STALE 210/190/0/20/0, flagged by implementer).

THREE P3 (all non-blocking) DISPOSED:
- P3-1 (over-wide pub walker/encode re-exports in event_plan/mod.rs; SILENT SIBLING: tcp_plan has identical pattern) -> filed TASK-0044.03.02.01 (covers BOTH siblings; orchestrator grep confirmed neither re-export set is consumed outside its module). Per silent-sibling discipline: filing the sibling pair explicitly IS the always-file-the-gap move.
- P3-2 (emit_handshake -- the most transport-divergent emitted surface -- has no unit golden; uds_equiv_tcp oracle strips the prelude; per-backend tests only .contains()-check markers; regression net today is e2e + manual A/B) -> filed TASK-0044.03.02.02 (golden snapshot hardening).
- P3-3 (host-excluding-barrier pin asymmetry framing): architect AGREES dont file -- the lift SUBSUMED the silent-sibling risk (ContractGap logic now single-source in event_plan/plan.rs; uds path differs only in T::BACKEND_NAME; both backends DO have the expect_err mediated host-excluding test). Documentation nit only; no action.

LoC: net -1097 (-3773 duplicate / +2676 = 1721 shared single-source + ~955 shims); the ~3000-LoC silent-sibling-defect surface RETIRED.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE. Lifted ~3000 LoC of duplicated multi-worker codegen from mp-tcp-event + mp-uds-event into a shared backend-common::event_plan substrate parameterised over an EventTransport trait (mirroring the proven WirePrimitives/tcp_plan blueprint, TASK-0044.02.03). Trait surface: consts BACKEND_NAME/FILE_HEADER_PREFIX/CTRL_STD_STREAM_TY/DATA_MIO_STREAM_TY/SO_BUF_COMMENT + fns emit_role_imports/emit_handshake(wholesale rendezvous block, built structurally)/render_run_sh_post. Each backend reduced to a thin src/plan.rs shim (TcpEventTransport / UdsEventTransport) + a multi_worker.rs re-export shim carrying its in-crate tests. Encode lifted verbatim; relay/walkers branded via T::BACKEND_NAME with backend-neutral docs; plan/worker_program carry the transport variation behind the trait. The one transport divergence kept per-backend wholesale: UDS /tmp-mktemp run.sh swap (sun_path 104-byte cap) via a FIXED-bash-literal String::replace that is //ALLOW-annotated + fail-loud (typed EmitError on needle-miss). HARD GATE -- byte-identical emit for BOTH backends -- proven THREE ways: per-backend emit-pinning + cross-backend uds_equiv_tcp byte-comparison oracles (qa GO), independent architect git-worktree A/B (10 identical diffs incl rendezvous+relay path), e2e 308/246/0/62/0 x3. AC#1-4 all met (AC#4 verified vs the LIVE baseline, not the stale md figure). Both reviews GO; no P1/P2. Net -1097 LoC; the ~3000-LoC silent-sibling-defect surface RETIRED. Commit d7599de. Two non-blocking P3 follow-ups filed: TASK-0044.03.02.01 (visibility tighten, event_plan+tcp_plan sibling pair), TASK-0044.03.02.02 (emit_handshake golden snapshot).
<!-- SECTION:FINAL_SUMMARY:END -->
