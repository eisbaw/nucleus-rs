---
id: TASK-0340.01.01
title: >-
  Align mp-tcp-event sibling rendezvous-port format string after cycle-179
  mp-tcp-bufsync indentation fix
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 12:22'
updated_date: '2026-05-26 22:27'
labels:
  - tech-debt
  - hygiene
  - cross-backend
dependencies: []
parent_task_id: TASK-0340.01
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Cycle 179 (commit 6315b1b) split mp-tcp-bufsync/lib.rs and in passing
realigned the embedded rendezvous-port format string in
plan/worker_program.rs:244-265 — the `Err(_e) =>` match-arm gained 2
spaces (12 → 14) to match its sibling `Ok(s) =>` at 14. This corrects
a long-standing 2-space skew (since TASK-0176) but it was NOT disclosed
in the slice-4 commit message ("pure mechanical split, no behaviour
change").

Sibling backend `nucleus/backends/mp-tcp-event/src/multi_worker.rs:896`
still carries the OLD 12-space indentation on `Err(_e) =>`. The
asymmetry is a CROSS-BACKEND DRIFT introduced by slice 4:

- mp-tcp-bufsync: emit `Err(_e) =>` at 14 spaces (post-179).
- mp-tcp-event:   emit `Err(_e) =>` at 12 spaces (still cycle-178b form).

Both are functionally identical (Rust ignores whitespace in function
bodies); the emit-string drift is observable by:
  diff <(nucleus build … mp-tcp-bufsync …) <(nucleus build … mp-tcp-event …)
on the rendezvous-port helper section.

## Acceptance criteria

1. Decision recorded: realign mp-tcp-event's `Err(_e) =>` to 14 spaces
   (matching the post-179 mp-tcp-bufsync form), OR revert mp-tcp-bufsync
   to the pre-179 12-space form. Both options preserve binary behaviour.
2. If realign: edit
   `nucleus/backends/mp-tcp-event/src/multi_worker.rs` Err-arm block at
   line 896 onward to +2 spaces per line, then re-run `just e2e` (must
   stay 112/102/0/10/0 bit-identical).
3. If revert: edit
   `nucleus/backends/mp-tcp-bufsync/src/plan/worker_program.rs` Err-arm
   block at lines 244-265 to -2 spaces per line. Same e2e gate.
4. Document the chosen direction in the commit message and update
   slice 4's note in TASK-0340.01.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 188 — landed in-thread (orchestrator)

Picked direction A per AC#1 (realign mp-tcp-event to match the post-179 mp-tcp-bufsync form), but extended scope because empirical audit revealed BOTH backends carried residual partial-fix inconsistencies that cycle 179 had not completely addressed.

Empirical audit findings (before edit):
- mp-tcp-event/multi_worker/worker_program.rs (was multi_worker.rs:896 pre-cycle-180; now multi_worker/worker_program.rs:346 post-split) — Err arm body indent profile 12/14/14/16/18/18/16/14/14/12/12, NOT matching the bufsync post-179 form.
- mp-tcp-bufsync/src/plan/worker_program.rs:254 — Err arm at 14 (post-179 fix), but L262 std::thread::sleep at 14 and L263 closing Err arm at 12 — STRUCTURALLY INCONSISTENT with siblings (Ok arm closing L253 at 14, attempt += 1 L255 at 16). The cycle-179 fix bumped only L254 Err opening, not its body's closing brace or std::thread::sleep peer.

The TASK-0340.01.01 description framed the issue as "mp-tcp-event behind bufsync's post-179 fix". The deeper truth: cycle 179 was itself a partial fix. Both backends carried different shapes of pre-existing structural inconsistency.

Resolution chosen (deeper than AC#1's literal text): per skill discipline 'never implement a workaround in place of a root fix' (CLAUDE.md), brought BOTH backends to the same structurally-correct indent profile (rather than mirroring bufsync's residual partial-fix shape into mp-tcp-event).

Edits applied:
- mp-tcp-event/multi_worker/worker_program.rs L345-355: +2 spaces on each line of the Err arm block (L346 opening through L355 closing) AND L345 closing-Ok-arm-brace (also at 12 pre-edit, sibling of bufsync L253:14). L356 closing-match-brace stays at 12 to match match opening (L338:12) — initial draft over-corrected this and was reverted before commit.
- mp-tcp-bufsync/src/plan/worker_program.rs L262-263: +2 spaces on std::thread::sleep (was 14, now 16 matching attempt at 16) and closing Err arm brace (was 12, now 14 matching closing Ok arm brace at 14). Completes the partial cycle-179 fix to a fully-structurally-consistent shape.

Both files now have IDENTICAL indent profiles for the read_rendezvous_port function (L338-358 in mp-tcp-event matches L246-266 in mp-tcp-bufsync byte-for-byte after de-anchoring the prefix offsets).

Mechanism verification (per memory feedback-implementer-disclosure-mechanism-wrong + feedback-comment-doc-lie-recurring):
- Counted leading `\x20` escape occurrences via `awk gsub` on each line (precise count, not visual inspection that miscounts at scale).
- Verified before/after profiles match symbolically — both files produce identical emit strings for the read_rendezvous_port function.
- Searched for tests that pin the emit string byte-for-byte (`grep -rn 'read_rendezvous_port\|rendezvous_dir.join' nucleus/ --include='*.rs'`) — no source-side pins; only target/ build artifacts referenced the function. Safe to edit.

Verification gate (cycle 188 is emit-string-only — same shape as cycle 179 + cycle 187):
- just build: clean.
- just clippy: clean.
- just test (dev):       979/0/3 (matches cycle 186/187 baseline).
- just test-release:     978/0/3 (matches cycle 186/187 baseline).
- just e2e:              120/106/0/14/0 (matches cycle 186/187 baseline).
- just check-narrative-doc-lie / check-mega-files / check-textual-replace-on-codegen / check-include-str-coverage: clean.

The e2e baseline preserved cycle 188 confirms the edit is BIT-IDENTICAL at the output.bin level (whitespace in the emit string changes the generated Rust source whitespace, which does not affect compiled binary behaviour — same shape as cycle 179 indentation fix). AC#4's literal "112/102/0/10/0" target is stale (pre-cycle-186); the spirit (e2e baseline preserved) is satisfied at the current 120/106/0/14/0 baseline.

Forward-carry of cycle 188 lesson — when a sibling-defect-class task description identifies ONE site, the orchestrator should empirically audit the FULL surrounding structure before editing. Cycle 188 surfaced TWO additional partial-fix gaps that the task description did not flag (mp-tcp-event L345 closing Ok arm; mp-tcp-bufsync L262+L263 sibling-mis-indent within Err arm). Per memory feedback-silent-sibling-defect: silent siblings are the #1 recurring source of "we thought we fixed this". The same applies in the small: when fixing an indent at one anchor, count the indents at ALL siblings (within-file and cross-file) before declaring closure.

Also: this cycle is bundled with cycle 187 (TASK-0340.04.03 doc-lie sweep, commits 728f27c + 1cb26bc) for a single combined review gate, per skill cost discipline ("don't spawn one per trivial task; batch tightly-related small tasks into one brief").
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 188 emit-string indentation realignment: brought BOTH backends (mp-tcp-event/multi_worker/worker_program.rs L345-355 + mp-tcp-bufsync/src/plan/worker_program.rs L262-263) to the same structurally-correct indent profile in the read_rendezvous_port emit string. Direction A of AC#1 (realign mp-tcp-event) chosen, scope extended to also complete cycle-179's partial fix on bufsync (L262 std::thread::sleep + L263 closing Err arm). Empirically-verified: both files now have byte-identical indent profiles for the function. Gate numbers preserved: 979/0/3 dev + 978/0/3 release + e2e 120/106/0/14/0. Bundled with cycle 187 for single combined review gate.
<!-- SECTION:FINAL_SUMMARY:END -->
