---
id: TASK-0042.02
title: 'M4 sub-task: mp-tcp-event backend crate (mio + epoll)'
status: In Progress
assignee:
  - mped-architect-impl
created_date: '2026-05-21 13:10'
updated_date: '2026-05-22 12:54'
labels:
  - M4
  - backend
dependencies:
  - TASK-0134
parent_task_id: TASK-0042
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD §7.1 row mp-tcp-event: OS processes + TCP loopback + mio (epoll-based readiness) + ring buffer; supports_async=true; supports_buffer=true. Codegen produces a binary per worker; mio reactor multiplexes inbound transfers; outbound writes are non-blocking with a per-(src,dst) ring.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 backends/mp-tcp-event/{Cargo.toml, capabilities.toml, src/lib.rs} land; capabilities.toml: transport=tcp, notify=[event], supports_async=true, supports_buffer=true, max_buffer=<chosen>.
- [ ] #2 EventList contract upheld; mio dependency justified (PRD §12 'one well-known crate' allowance); honest limitation note for mio polling overhead vs tokio.
- [ ] #3 Generated run.sh launches N worker processes and the host; uses TCP loopback ports; computes SO_SNDBUF/SO_RCVBUF for the chosen buffer size as TASK-0038 does for mp-tcp-bufsync.
- [ ] #4 e2e: examples 9, 11 on mp-tcp-event × pipelined bit-identical; cross-backend differential vs pthreads-async sibling AND vs reference.bin (three-way independent agreement).
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle 41 (2026-05-22): Stages 1+2 shipped (single-worker delegation + skeleton + driver + workspace + capabilities). Stages 3+4 (multi-worker mio reactor + ring buffer codegen + corresponding e2e [[required]] cells) DEFERRED to TASK-0042.05.

## What landed
- nucleus/backends/mp-tcp-event/{Cargo.toml, capabilities.toml, src/lib.rs} (74+33+385 = 492 LoC).
- nucleus/Cargo.toml workspace member entry.
- nucleus/driver/Cargo.toml dep + dispatch arm + help text in nucleus/driver/src/main.rs.
- nuc-nucleus/e2e-matrix.toml: backend added to backends list; 14 single-worker [[required]] cells; 7 multi-worker [[skip]] cells with precise forward-link to Stage 3.

## Capabilities
- transport=tcp, notify=[event], supports_async=true, supports_buffer=true, max_buffer=64 (same conservative cap as pthreads-async).

## Verification gate
- just test: 0 failures.
- cargo clippy --workspace --all-targets -- -D warnings: clean.
- just e2e: 88 cells, 70 PASS, 0 FAIL, 18 SKIPPED, 0 required-fail. (Baseline before this cycle: 66/55/0/11.)
- just determinism-check-negative: OK (70 of 88 cells perturbed).
- just xbackend-check-negative: OK (16 mp-tcp-bufsync cells corrupted, 1 detected).

## AC closure
- AC#1 (crate + capabilities + emit signature): CLOSED.
- AC#2 (EventList contract + mio dependency + honest limitation note): DEFERRED to TASK-0042.05 (mio not yet a dependency since multi-worker codegen is ContractGap; honest limitation note is in the lib.rs preamble + the [[skip]] reason strings).
- AC#3 (run.sh launches N worker processes + TCP loopback + SO_*BUF sizing): DEFERRED to TASK-0042.05 (the single-worker run.sh ships in this cycle but it does not exercise multi-worker port allocation).
- AC#4 (e2e examples 9, 11 on mp-tcp-event × pipelined bit-identical): DEFERRED to TASK-0042.05. The pipelined cells are [[skip]] today; will become [[required]] when Stage 3 lands.

## Honest limits
- mp-tcp-event multi-worker codegen is ContractGap. The capability surface is real (driver/check_schedule_compat accepts), but emit() refuses to produce a binary on used_workers >= 2. This is fail-loud (typed EmitError::ContractGap with a precise forward-link to TASK-0042.05), not a wrong binary.
- Single-worker emit is byte-identical to pthreads-sync's by construction (shared renderer); the cross-backend bit-identical differential holds on this path BUT it is a tautology — all four tier-1 backends share the same single-worker code path. The genuinely independent test is the multi-worker path, which Stage 3 will deliver.

## Files
- /home/mpedersen/topics/mark_thesis/nucleus/backends/mp-tcp-event/Cargo.toml (new)
- /home/mpedersen/topics/mark_thesis/nucleus/backends/mp-tcp-event/capabilities.toml (new)
- /home/mpedersen/topics/mark_thesis/nucleus/backends/mp-tcp-event/src/lib.rs (new)
- /home/mpedersen/topics/mark_thesis/nucleus/Cargo.toml (modified)
- /home/mpedersen/topics/mark_thesis/nucleus/driver/Cargo.toml (modified)
- /home/mpedersen/topics/mark_thesis/nucleus/driver/src/main.rs (modified)
- /home/mpedersen/topics/mark_thesis/nuc-nucleus/e2e-matrix.toml (modified)
- TASK-0042.05 filed as Stage 3/4 follow-up.

## Status
In Progress (NOT Done — Stage 3 + Stage 4 [[required]] migration both pending).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 41 (2026-05-22) — PARTIAL closure. Stages 1+2 landed; Stages 3+4 deferred to filed sub-task TASK-0042.05.

**Stage 1 (skeleton + workspace + dispatch)**: LANDED.
- New crate nucleus/backends/mp-tcp-event/{Cargo.toml, capabilities.toml, src/lib.rs} (492 LoC). capabilities.toml: transport=tcp, notify=[event], supports_async=true, supports_buffer=true, max_buffer=64.
- Workspace registration in nucleus/Cargo.toml.
- Driver dispatch arm + help text in nucleus/driver/src/main.rs.

**Stage 2 (single-worker delegation)**: LANDED.
- emit() for used_workers <= 1 delegates to pthreads_sync::render_single_worker_main + multi-process project skeleton from mp_tcp_bufsync (Cargo.toml with [[bin]] entries + run.sh launcher + wrap_single_worker for the 0/1-used-worker case). Emitted src/bin/nuc-generated.rs is byte-identical to mp-tcp-bufsync's by construction (shared renderers within the multi-process layout family); arithmetic body byte-identical to all 3 prior tier-1 backends (one shared single-worker renderer).

**Stage 3 (multi-worker mio reactor + ring buffer)**: NOT LANDED. emit() returns EmitError::ContractGap with precise forward-link to TASK-0042.05 (Stage 3 of TASK-0042.02) on used_workers >= 2. Honest fail-loud — typed error, not a panic or wrong binary.

**Stage 4 (e2e wiring)**: PARTIAL. 22 mp-tcp-event cells added to nuc-nucleus/e2e-matrix.toml:
- 15 [[required]] (every single-worker schedule where delegation is structural).
- 7 [[skip]] with precise forward-link to TASK-0042.05 for the multi-worker schedules (02-split/split, 03-reduction/distributed, 05-stencil/distributed, 11-game-of-life/pipelined, 13-cnn-inference/{batch_parallel, pipeline_parallel}, 09-producer-consumer/pipelined).

The headline 13/pipeline_parallel × mp-tcp-event cell AC#4 names is currently [[skip]]; it cannot be wired [[required]] until TASK-0042.05 lands Stage 3.

**AC#1**: CLOSED.
**AC#2**: deferred to TASK-0042.05 (mio dep + EventList contract upheld + honest limitations re polling overhead).
**AC#3**: deferred to TASK-0042.05 (multi-worker run.sh with TCP ports + SO_*BUF).
**AC#4**: deferred to TASK-0042.05 (e2e pipelined × mp-tcp-event bit-identical for Examples 9, 11, 13).

Tally: 88 / 70 / 0 / 18 (was 66 / 55 / 0 / 11). +22 cells (+15 required PASS, +7 SKIP).

Honest implementer caveat preserved: single-worker bit-identical is a TAUTOLOGY (all 4 tier-1 backends share one single-worker renderer; the 15 passes verify harness wiring, capability check, and delegation, but do NOT independently verify any mp-tcp-event codegen). The truly falsifiable test of mp-tcp-event's independence lands with TASK-0042.05's pipelined cells.

Review-gate (parallel read-only): both qa-test-runner + mped-architect GO. Architect LOW (the ContractGap message cited only the parent task; cycle-41 in-thread fix added the freshly-filed TASK-0042.05 sub-id). QA LOW (SKIP-reason text overclaimed 'byte-identical to the three shipped tier-1 backends' but the LAYOUT differs — multi-process src/bin/ vs single-binary src/main.rs; cycle-41 in-thread fix scoped this to 'byte-identical to mp-tcp-bufsync within the multi-process layout family').
<!-- SECTION:FINAL_SUMMARY:END -->
