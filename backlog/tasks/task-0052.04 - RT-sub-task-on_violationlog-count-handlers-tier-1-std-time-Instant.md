---
id: TASK-0052.04
title: 'RT sub-task: on_violation=log/count handlers (tier-1 std::time::Instant)'
status: Done
assignee:
  - '@mped'
created_date: '2026-05-21 15:29'
updated_date: '2026-05-21 16:57'
labels:
  - M4
  - real-time
  - backend
dependencies:
  - TASK-0052.02
parent_task_id: TASK-0052
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD §6.3.5. TASK-0052.02 wired on_violation=panic for both tier-1 backends (pthreads-sync + mp-tcp-bufsync). on_violation=log and on_violation=count handlers were deferred this cycle - the projection pass currently materialises Panic only (and the codegen errors loudly on Log/Count to avoid silent loss of the assertion). This task adds: log: eprintln! once per violation, execution continues, message names loop_var+measured+threshold, stdout unchanged so cross-backend differential keeps holding. count: atomic counter increment per violation, one-line summary to stderr at run end via Drop on a guard struct, stdout unchanged. Both go to STDERR not stdout so the differential continues to compare stdout/output-bin only.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 pthreads-sync + mp-tcp-bufsync codegen emit eprintln-once for on_violation=log; cross-backend differential remains green (stdout unchanged on violation).
- [x] #2 pthreads-sync + mp-tcp-bufsync codegen emit an atomic counter + Drop-guard summary for on_violation=count; cross-backend differential remains green.
- [x] #3 Positive run: a kernel that violates under log/count handler runs to completion, exits 0, and prints exactly one summary line on stderr; cross-backend differential still byte-identical on stdout.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Codegen change in two backends + shared helpers in pthreads-sync exposed pub for mp-tcp reuse. Design choice A (per-loop AtomicU64 static + Drop guard struct) chosen over B (atexit/thread-local — not pure-std; bare-metal-hostile for tier-3 Renode) and C (Mutex<HashMap> registry — over-engineering). Log path: per-iteration eprintln, stderr-only so cross-backend differential (stdout/output.bin) stays byte-identical. Count path: file-scope AtomicU64 static per check loop + shared NucCheckCountReporter Drop guard struct emitted once per file + per-loop guard local in fn main; Drop prints summary at run end gated by n>0 so clean runs stay silent on stderr too. Sanitizer (sanitize_loop_var) is defensive — current grammar enforces valid-ident loop_vars at parse time. Tests: emit-string assertions on both backends pin the contracted shape (drift between backends fails one of the two suites loudly); AC#3 cargo-build-and-run pair for pthreads-sync Log (tight threshold, exit 0, logs to stderr, stdout unchanged) and Count (tight threshold, exit 0, summary once at end; generous threshold, no summary). Two-loops codegen test confirms exactly one reporter struct + Drop impl for N>1 Count loops in one schedule. mp-tcp-bufsync codegen mirrors via pub imports — single implementation, no drift.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle complete (commit c37baf6)

## What landed
- pthreads-sync: Log + Count handlers replace the TASK-0052.02 ContractGap returns in the Event::Loop arm. New file-scope helpers (pub for mp-tcp reuse): CountCheckLoop struct, sanitize_loop_var, collect_count_check_frames (recursive event walker), emit_count_reporter_struct.
- pthreads-sync render_main_rs: emits per-Count-loop AtomicU64 static + (once-per-file) NucCheckCountReporter struct + Drop impl at file scope BEFORE fn main; emits per-loop guard local INSIDE fn main before pre_init.
- mp-tcp-bufsync render_worker_program: SAME shape via pub imports from pthreads-sync. Single codegen implementation, no drift.
- mp-tcp-bufsync Event::Loop arm: Log + Count branches replace the TASK-0052.02 ContractGap return.
- 8 new tests: 3 pthreads-sync codegen-string (Log, Count, Count-two-loops); 3 pthreads-sync AC#3 cargo-build-and-run (Log tight, Count tight, Count generous-no-summary); 2 mp-tcp-bufsync emit-string (Log, Count).

## Independently re-run gate (post-commit c37baf6)
- cargo test --workspace: 539 pass / 0 fail / 2 ignored (was 531; +8).
- cargo clippy --workspace --all-targets -- -D warnings: clean.
- just e2e: 36 cells: 29 pass / 0 fail / 7 skipped (baseline unchanged).
- just determinism-check: byte-identical x2 across 29 passing cells.
- just determinism-check-negative: bites (29 cells perturbed).
- just ci: exit 0; xbackend-check-negative bites (14 corrupted, 1 detected).

## Per-AC status
- AC#1 DONE: both tier-1 backends emit eprintln for on_violation=log; cross-backend differential remains green (e2e 29/0/7 unchanged; determinism byte-identical).
- AC#2 DONE: both tier-1 backends emit AtomicU64 static + NucCheckCountReporter Drop guard for on_violation=count; differential green.
- AC#3 DONE: Log tight-threshold cargo-build-and-run exits 0, prints eprintln on stderr, stdout unchanged; Count tight-threshold exits 0, prints exactly one summary line on stderr at Drop time; Count generous-threshold (no violations) prints nothing on stderr (n>0 gate).

## Design choice resolution (A vs B vs C)
A chosen: per-loop file-scope AtomicU64 + per-file Drop-guard struct. Std-idiomatic, no unsafe, lock-free fetch_add, predictable codegen, test-pinnable. B rejected: atexit + thread-local — atexit is libc-only (not in pure-Rust std), bare-metal-hostile for tier-3 Renode forward-carry. C rejected: Mutex<HashMap> global registry — over-engineering; each check loop has its own static at codegen time, so locking is dead weight.

## Honest limitations / gotchas
- No tier-1 e2e cell uses on_violation=log / =count today. The codegen-string tests + AC#3 cargo-build-and-run pair are the lower-bound coverage; the differential continues to compare only the 29 existing cells (none of which use check loop). Future cell with explicit Log/Count would close this gap.
- The sanitize_loop_var is defensive — current chumsky grammar (parser.rs) enforces loop_var must be a valid Rust ident at parse time, so the sanitizer is a no-op on every realistic input today. Documented in the helper comment.
- Multi-worker pthreads-sync still rejects check_frame with TASK-0052.05 ContractGap; mp-tcp-bufsync also rejects multi-worker check_frame on top of strip-mining (TASK-0181). On_violation handler choice in multi-worker context is filed as TASK-0052.05 future work (Count across processes is the harder design question — per-process counter vs aggregated).
- The Count Drop guard fires when fn main returns normally. On panic (e.g. in another part of the program) Rust still runs Drops via unwind, so the summary is robust. On std::process::exit() Drop is skipped (per std docs); not used in our generated code today, but a future codegen that calls exit() would lose the Count summary — fail-loud documentation worth carrying forward.
- The Count counter is process-local in mp-tcp-bufsync: each worker process gets its own NUC_CHECK_COUNT_<id> + its own Drop guard + its own summary line. For a multi-worker schedule both lines would appear on stderr (one per process). Aggregation is a TASK-0052.05 follow-up if/when multi-worker check_frame is enabled.

## Forward-carries
- TASK-0042.01 (pthreads-async): the SAME Log/Count handler contract applies. pthreads-async will need to mirror the codegen pattern; the pub helpers (collect_count_check_frames, emit_count_reporter_struct, sanitize_loop_var) are already reusable.
- TASK-0048 (Renode STM32H7): no std::time::Instant on bare-metal; no eprintln to host stderr — would need UART log or RTT. Drop on a static guard at MCU 'shutdown' is also tricky (the MCU just halts). Carry-over from TASK-0052.02 still stands; Log/Count would need backend-specific re-routing.
- TASK-0052.05 (multi-worker pthreads-sync + mp-tcp-bufsync check_frame): per-process counters work today; cross-process Count aggregation is the open design question.

## Doc-lie audit (recurring class)
- mp-tcp src/lib.rs emit-comment block (TASK-0052.02 hardening) previously claimed bit-identical without a test enforcing it. This cycle: every claim ('SAME shape', 'single codegen implementation') is pinned by either (a) the pthreads-sync emit-string test + the mp-tcp emit-string test asserting the same literal substrings, or (b) the cross-backend differential remaining green. No new doc-lies introduced.

## Review-gate close (cycle close, post-implementer-commit c37baf6+1876be9)

mped-architect read-only review: GO (one MEDIUM finding + one MEDIUM tech-debt).
qa-test-runner read-only review: GO clean (539/0/2 stable x2; AC#3 cargo-build-and-run real subprocess tests confirm exit 0 + stderr-only side effects).

Working-tree state at review start: clean (only `backlog/email-preferences.json` cruft). The reversion saga from TASK-0213 + TASK-0052.02 cycles did NOT recur this cycle.

Findings folded in-thread:

1. **(MEDIUM) Stale forward-carry on TASK-0042.01.** The TASK-0052.02 forward-carry note on TASK-0042.01 said "Log/Count NOT YET wired; codegen MUST emit ContractGap" — both halves obsolete after TASK-0052.04. Without correction the next pthreads-async implementer would emit ContractGap instead of using the now-established shared helpers. Appended a CORRECTION block to TASK-0042.01 with the corrected codegen contract: import + use `collect_count_check_frames` / `emit_count_reporter_struct` / `sanitize_loop_var` / `CountCheckLoop` from pthreads-sync; emit Panic + Log + Count branches per established pattern; multi-worker still rejects with ContractGap until TASK-0052.05 lands.

2. **(MEDIUM) Code-clone risk understated.** The commit-message + lib.rs comments claim "Single codegen implementation [...] No drift between backends" — but four emit-string templates (static AtomicU64 decl, guard local, Log eprintln, Count fetch_add) are textually duplicated. The shared helpers cover collector/struct-emitter/sanitizer; the four templates are tripwire-detection by test, not structural prevention. Acceptable for two backends; needs extraction when pthreads-async lands as third. Filed as TASK-0222.

Doc-lie audit (recurring class):
- pthreads-sync + mp-tcp-bufsync Log/Count emit comments: accurate per architect.
- `CountCheckLoop` / `sanitize_loop_var` / `collect_count_check_frames` / `emit_count_reporter_struct` docstrings: accurate.
- TASK-0048 forward-carry on Log/Count (added in earlier cycle): accurate, untouched.
- TASK-0042.01 forward-carry: had stale Log/Count claim; CORRECTED in-thread this cycle.

Gate (re-run independent post-correction; backlog-only edits, no code touched):
- cargo test workspace: 539 pass / 0 fail / 2 ignored (unchanged from implementer's claim, verified by QA).
- cargo clippy --workspace --all-targets -- -D warnings: clean (no code changed).
- just e2e: 36 cells: 29 / 0 / 7 (unchanged baseline).

Follow-ups filed:
- TASK-0222: extract the 4 emit-string templates into shared helpers when pthreads-async lands as 3rd tier-1 backend. Closes the "no drift" overclaim by replacing tripwire-detection with structural impossibility.

LOW findings noted (not actioned this cycle):
- Log per-event message shape differs from Count summary shape — doc hygiene if downstream log-grepping appears.
- writeln!(...).ok() pattern is project-wide convention; no change.
- No e2e cell exercises on_violation=log/count — could add an opt-in cell as TASK-0223 later; the current AC#3 cargo-build-and-run pair is sufficient cycle-internal coverage.
<!-- SECTION:NOTES:END -->
