---
id: TASK-0438.02
title: >-
  Embedded-pattern backend: divergent DMA-async vs PIO-sync transport codegen
  (read TransferPolicy.transport)
status: In Progress
assignee:
  - '@mark'
created_date: '2026-06-04 07:02'
updated_date: '2026-06-05 00:39'
labels: []
dependencies:
  - TASK-0438.01
parent_task_id: TASK-0438
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Second sub-slice of TASK-0438. Make the embedded-pattern backend emit a STRUCTURALLY DISTINGUISHABLE code path for a DMA-async transport edge vs a PIO-sync one, driven by the `TransferPolicy.transport` field threaded in TASK-0438.01.

Sites (from parent pre-brief): nucleus/backends/embedded-pattern/src/render.rs:348-391 (link_push/link_recv lowering) + skeleton/multimcu.rs:238-258 (concrete UART byte loop) + multimcu.rs:202-335 TransportPlan::build. The TransportPlan/per-channel context must learn the mode so render diverges.

DMA-async path: emit a descriptor-arm + IRQ-wait stub (poll DMA-complete flag, or wfi-loop — decide Q#4 here; spin is acceptable for byte-exact differential, document the choice). The byte movement may still ride the SAME USART fabric (reuse UARTHub) — the point is the EMITTED Rust structurally differs (DMA arm + completion wait), not that a real silicon DMA engine runs.
PIO-sync path: keep the existing volatile byte-by-byte loop (skeleton/multimcu.rs usart_putc/usart_getc).

HONEST CONSTRAINT: if the DMA path needs a real Renode DMA engine to remain byte-exact, that is high-risk (cf. deferred TASK-0048.12). Prefer a DMA-SHAPED emit that is still correct over the existing transport (the M10-precedent honest-sync-shim approach). Document precisely what is modelled vs real.

Must keep 02-split-add + 14-hearing-aid (no mode= -> default Pio) BYTE-EXACT.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 a transfer edge with mode=dma emits structurally distinct Rust (descriptor arm + completion/IRQ wait) vs mode=pio (volatile byte loop); pinned by an emit-oracle/string-shape unit test
- [ ] #2 the choice of IRQ-wait strategy (wfi vs spin) is decided and documented in a code comment + task notes
- [ ] #3 02-split-add + 14-hearing-aid remain BYTE-EXACT under renode-multimcu-gate (default PIO unchanged)
- [ ] #4 honest comment/doc: what is a real transport vs a modelled DMA shape; no overclaim that a silicon DMA engine runs unless it actually does
- [ ] #5 cheap gate green (record counts) + clippy clean
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
forward-carried from TASK-0438.01 (architect review P3.3): `sync, mode=dma` and `async, mode=pio` cross-combinations currently parse and resolve WITHOUT rejection — transport mechanism (pio/dma) is treated as orthogonal to the sync/async dataflow semantic. This is defensible (a DMA engine can do a caller-blocking transfer) but was NOT an explicit decision. When you implement divergent codegen, EXPLICITLY decide whether any (sync|async)x(pio|dma) combination is invalid and should be a typed capability/lower error, rather than silently inheriting acceptance. If all 4 combos are valid, document why; if not, add the guard + negative test.
Also forward-carried: TransferPolicy.transport is the field to branch on (passes/transfer_inject/ordering.rs policy_from_directive). The serde-on-AST-enum layering departure for TransportMode is documented in sched/ast.rs — if you prefer the NotifyKind->NotifyMode mirror pattern, the conversion point is trivial.

DUPLICATE-CHECK (hard rule, grep-witness 2026-06-05): premise REAL, NOT shipped by .01. .01 threaded the hint to TransferPolicy.transport (acfg/types.rs:363, written at passes/transfer_inject/ordering.rs:793 `p.transport = *m`), but ZERO backend reads it — embedded-pattern "transport" refs are all the inter-MCU UART sense. NameSidecar (sidecar.rs:144) carries transfer_buffer_for_seq (BTreeMap<SeqTag,u64>, populated by collect_transfer_buffers sidecar.rs:886 + build_sidecar:693) but NO transport field. So the backend cannot diverge today. DESIGN (orchestrator pre-brief): (1) MIRROR the buffer-sidecar pattern: add transfer_transport_for_seq: BTreeMap<SeqTag,TransportMode> to NameSidecar + collect_transfer_transports walk. (2) Render divergence at embedded-pattern/src/render.rs:348-391 (Event::Push->link_push / Event::Wait->link_recv): branch on sidecar per-seq transport. PIO/absent: emit existing shim.link_push/link_recv (volatile byte loop). DMA: emit an INLINE descriptor-arm + completion-spin shape (structurally visible per AC#1), backed by new shim hooks. (3) Shim hooks: NucleusShim trait (skeleton/mod.rs:60 real Rust trait + StubShim mod.rs:125; AND the emitted BIN MultiMcuShim string multimcu.rs:215-274). DECISIONS PRE-MADE: (a) SPIN not wfi (AC#2) — the modelled shim completes synchronously so nothing fires an IRQ; wfi would deadlock, spin-on-done-flag is the honest model. (b) all 4 (sync|async)x(pio|dma) combos VALID/orthogonal (transport mechanism vs dataflow semantic), documented, NO new reject guard (matches current acceptance; forward-carried P3.3). (c) bytes still ride the SAME UART fabric (modelled DMA shape, NOT silicon DMA — honest per M10 precedent, AC#4). SCOPE: .02 = thread + diverge + emit-oracle unit test (AC#1) + keep 02-split-add/14-hearing-aid byte-exact (AC#3, default PIO). The 22-dma-pio-demo example + DMA Renode byte-exact is .03, NOT .02.

IMPLEMENTATION PLAN + RESULT (cycle 2026-06-05):
Part A (sidecar thread): mirrored transfer_buffer_for_seq exactly — added NameSidecar.transfer_transport_for_seq: BTreeMap<SeqTag,TransportMode> (sidecar.rs), collect_transfer_transports() walk, populated in build_sidecar beside (f) as (f). Updated proptest_serde.rs (exhaustiveness destructure guard + namesidecar_strategy generator, bundled into the nested tuple to stay under the prop arity ceiling).
Part B (divergent render): render.rs Push/Wait arms branch on ctx.sidecar.transfer_transport_for_seq.get(seq).copied().unwrap_or(Pio). Pio/absent emits the UNCHANGED shim.link_push/link_recv (comment + call byte-identical). Dma emits shim.dma_link_arm/dma_link_recv_arm + a completion SPIN loop (while !shim.dma_link_poll(chan) { core::hint::spin_loop(); }).
Part C (shim hooks): added 3 DEFAULT trait methods to NUCLEUS_SHIM_SRC (the only NucleusShim definition is this emitted string; there is NO separately-compiled trait in the crate — onboarding said mod.rs:60 real trait but it is inside the string const). dma_link_arm/dma_link_recv_arm delegate to link_push/link_recv (same UART fabric, modelled DMA shape NOT silicon DMA, AC#4); dma_link_poll returns true (synchronous-completion model). Defaults => StubShim/MultiMcuShim/Usart1Shim need NO override. Updated the shim_trait_declares_all_methods test + the NUCLEUS_SHIM_SRC doc.
Part D (AC#1 oracle): new src/tests/transport_mode_render.rs (3 tests) calling render_run_body directly: dma seq -> arm+spin, NOT link_push; absent seq -> plain PIO, NO dma_link_*; explicit Pio == absent byte-for-byte.

DECISIONS (AC#2/cross-combo/AC#4): SPIN not wfi — modelled shim completes synchronously in the arm, no DMA-complete IRQ fires, wfi would deadlock; documented at the emitted spin loop + the dma_link_poll doc. All 4 (sync|async)x(pio|dma) combos VALID/orthogonal — NO reject guard added (forward-carried P3.3); transport mechanism is independent of dataflow sync/async. Bytes ride the SAME UART — modelled DMA shape, never a silicon DMA engine (real engine = TASK-0048.12).
<!-- SECTION:NOTES:END -->
