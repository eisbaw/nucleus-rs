---
id: TASK-0340.14
title: >-
  Refresh cross-file prose citations to transfer_inject internals after the
  TASK-0340.13 submodule split
status: Done
assignee:
  - '@mark'
created_date: '2026-05-31 23:28'
updated_date: '2026-05-31 23:33'
labels:
  - tech-debt
  - hygiene
  - doc-citation
dependencies: []
parent_task_id: TASK-0340
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The TASK-0340.13 split moved several transfer_inject free functions into submodules (build_waits_for_op -> ordering, record_access_per_dim -> partition, extend_xfer_tiles_inner -> tiles, etc.). Four prose code-span citations in nucleus/nucleus-compiler/src/acfg/build.rs (lines ~420, 447, 492, 517) still name them as 'transfer_inject::build_waits_for_op' / 'transfer_inject::record_access_per_dim' (single-backtick prose, not intra-doc links, so cargo doc is NOT broken — they were never resolvable import paths because the fns were always private). These are now module-path-imprecise. Refresh to 'transfer_inject::ordering::build_waits_for_op' etc. (or drop the module qualifier and keep the bare fn name, which stays greppable). Surfaced by mped-architect review of TASK-0340.13 (classified out-of-scope/no-action for that task). Low priority, prose-hygiene only. Also: architect flagged a trace.rs reference to a 'trace_block_deferral' symbol but a grep found no such symbol or reference — verify and drop the note if it is a phantom.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
INVESTIGATED — NO CODE CHANGE WARRANTED (not AC-gaming; the 'refresh' was found unnecessary on investigation). Scope is actually 15 prose code-span sites across 5 files (architect spot-checked only acfg/build.rs ×4): transfer_inject::record_access_per_dim ×9 (halo_inference.rs ×6, common.rs ×1, acfg/build.rs ×2), transfer_inject::build_waits_for_op ×2 (acfg/build.rs), transfer_inject::hoist_invariant_waits ×4 (host_data_relay_inject.rs ×3, event_plan/walkers.rs ×1). FINDING: these are NOT stale/lies. (1) All three fns were PRIVATE 'fn' (verified at pre-split 953c5e9:transfer_inject.rs:1548/2639/4028), NEVER 'pub fn' — so transfer_inject::<fn> was NEVER an importable path, just prose naming convention; the split (fn -> pub(super) in a private submodule) changed nothing about resolvability. (2) The refs remain TRUE — each fn is still within the transfer_inject module tree. (3) check-doc-citation-staleness-bare gate is GREEN on all of them. Contrast: the intra-file 'in this file'/'(above)'/line-number refs fixed in ab24550 BECAME FALSE post-split (real lies); these cross-file module::fn refs did not. DECISION: closing as accurate-as-is rather than manufacturing 15-site churn across 5 files (incl. the deliberately-pristine halo_inference.rs) for optional submodule-qualifier precision on already-true prose — and a partial fix (only the 4 acfg/build.rs the architect named) would be a feedback-silent-sibling-defect. If a human wants max precision later, the fix is all-15-or-none: 'transfer_inject::partition::record_access_per_dim' / '::ordering::build_waits_for_op' / '::sequence::hoist_invariant_waits'.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE (investigation outcome: no change needed). The cross-file prose citations transfer_inject::<fn> to the 3 split-relocated functions are accurate-but-submodule-unqualified prose code-spans, NOT doc-lies: the fns were always private (never importable paths) and remain within transfer_inject's module tree; the doc-citation gate is green. Refreshing would be 15-sites-across-5-files optional precision polish with silent-sibling risk if partial, on prose that is already true — declined as poor value. Re-open only if submodule-qualifier precision is explicitly desired (then do all 15, not a subset).
<!-- SECTION:FINAL_SUMMARY:END -->
