---
id: TASK-0046.01
title: M8 — 09-producer-consumer/pipelined over mpi-nonblocking (needs Comm_split)
status: To Do
assignee: []
created_date: '2026-05-31 09:46'
updated_date: '2026-06-01 18:31'
labels:
  - M8
dependencies:
  - TASK-0045.02
parent_task_id: TASK-0046
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0046 AC#3 names examples 9 AND 11. Example 11 (+ bonus 05-stencil/distributed{,-2d}) landed byte-exact under mpi-nonblocking. Example 9 (09-producer-consumer/pipelined) is BLOCKED: it lowers to a non-whole-world barrier with participant set {producer,consumer} excluding the host (empirically reproduced cycle M8-entry). mpi-nonblocking inherits mpi-blockings whole-world-barrier-only guard and rejects it LOUD, forward-linked to TASK-0045.02 (Comm_split + sub-communicator barrier, unproven). Once TASK-0045.02 lands the Comm_split machinery, wire it into mpi-nonblockings emit and add 09-producer-consumer/pipelined to just check-mpi-nonblocking (mpiexec -n 3, byte-exact + forced-rendezvous), closing TASK-0046 AC#3.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 mpi-nonblocking emits a sub-communicator barrier (Comm_split) for the host-excluding {producer,consumer} barrier instead of rejecting it
- [ ] #2 09-producer-consumer/pipelined runs mpiexec -n 3 all-ranks-live byte-exact vs reference.bin under default + forced-rendezvous in just check-mpi-nonblocking
- [ ] #3 TASK-0046 AC#3 (examples 9 AND 11) is then fully met
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FORWARD-CARRIED FROM TASK-0045.02 (Comm_split landed, commits cd68c80 + 48c71a7):

The shared Comm_split machinery is DONE and serves mpi-nonblocking automatically. It lives in backend_common::mpi_plan (the substrate both MPI backends consume), so there is NO per-backend wiring to do. Example 9 ALREADY EMITS + RUNS value-correct today:

- nucleus build --backend mpi-nonblocking with the 09-producer-consumer/pipelined schedule now SUCCEEDS (exit 0, no reject). The generated src/main.rs carries, OUTSIDE the match-rank arms, a split_0 = world.split_by_color(if [1, 2].contains(&world.rank()) {...}) with the comment "barrier participants {consumer, producer}", and the consumer/producer arms bind bar_<tag> = SubcommBar::new(&split_0) for the host-excluding barrier (host = rank 0 excluded) while keeping WorldBar for the whole-world barriers in the same program.
- It runs BYTE-EXACT vs the example reference.bin under mpiexec --oversubscribe -n 3 (all ranks live), under BOTH the default eager protocol AND forced-rendezvous (--mca btl_*_eager_limit 128). Verified this cycle; probe scratch was cleaned (rm -rf nucleus/target/mpi-ex9-probe) so re-generate to a fresh --out dir.

REMAINING FOR THIS TASK: just ADD the "09-producer-consumer/pipelined/3" spec to the check-mpi-nonblocking targets loop in justfile (same shape as the existing 05/11 specs: n=3, both arms via the existing rndv default+rendezvous loop), DELETE the "NOT here: 09 ..." caveat comment above the recipe, tick the ACs, close TASK-0046 AC#3. NO codegen work left — the Comm_split lowering is already correct; this is gate-wiring + a doc-comment delete.

HOW IT IS INVOKED: there is no separate helper to call. Plan::build classifies barriers (whole-world vs strict-subset) and Plan::emit_subcomm_splits emits the split(s) before the rank dispatch; mpi-nonblocking gets it via its existing type Plan alias over backend_common::mpi_plan::Plan with BufferedRendezvous + render_main_rs_multi (both unchanged). The only mpi-nonblocking-side text is the SubcommBar wrapper already added to its prelude this cycle (byte-identical to mpi-blocking).
<!-- SECTION:NOTES:END -->
