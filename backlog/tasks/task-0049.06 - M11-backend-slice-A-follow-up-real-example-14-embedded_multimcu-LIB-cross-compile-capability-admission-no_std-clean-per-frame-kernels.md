---
id: TASK-0049.06
title: >-
  M11 backend slice A follow-up: real example-14 embedded_multimcu LIB
  cross-compile (capability admission + no_std-clean per-frame kernels)
status: Done
assignee:
  - '@Claude'
created_date: '2026-05-29 07:16'
updated_date: '2026-05-29 09:00'
labels:
  - M11
  - backend
dependencies: []
parent_task_id: TASK-0049
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0049.04 landed the STRUCTURAL multi-worker LIB path (lift guard + N projects + Push/Wait/Sync -> stub-shim hooks) and proved it cross-compiles under .#embedded with the no_std-clean 02-split-add/split fixture (host+w0, 2 libs). It did NOT cross-compile the REAL 3-worker example-14 embedded_multimcu schedule (TASK-0049.04 AC#3 as literally worded), because that schedule is blocked by two gaps OUTSIDE the backend structural change, discovered empirically this cycle:

(A) CAPABILITY GATE. The driver/e2e path runs check_schedule_compat BEFORE backend emit. embedded_multimcu demands async + buffer=3 + notify=event + worker_classes {fe_core,dsp_core,rf_core} + memory_regions {sram_shared,dsp_tcm}. embedded-pattern capabilities.toml declares only default class, heap region, sync, no async/buffer/event. Admitting those into capabilities.toml would be a LIE against the synchronous StubShim (forbidden). The sync-vs-async transport decision + heterogeneous worker classes belong here / TASK-0049.05. NOTE: TASK-0192 proved lower/link/acfg ADMIT the schedule via lower_for_test (IR passes only); it did NOT exercise the capability gate, which is why this surfaced only at the driver path.

(B) KERNEL BODIES. prog.embedded.algo.nuc declares mix2/denoise as PURE (indexed-output Fires -> verbatim-extracted into mod kernels), but kernels.rs gives them Vec<i32> bodies (NOT no_std-clean). The effectful fe_capture/rf_receive/fe_emit/rf_transmit have NO Rust bodies in kernels.rs at all. So even past the gate + lifted guard, the generated lib could not cross-compile. no_std-clean stateful per-frame kernels overlap TASK-0049.02 (stateful kernels.rs) but that task is scoped single-process/Renode; this needs a no_std-clean variant for the embedded LIB.

ACCEPTANCE: the real example-14 embedded_multimcu schedule emits 3 per-worker no_std libs (fe/dsp/rf) that cross-compile under .#embedded (extend check-embedder M11 arm from the 02-split fixture to ex14). Requires a decided answer to (A) [most likely: a synchronous embedded_multimcu_sync sibling schedule using sync transfers + a default-or-extended worker-class capability surface, mirroring the M10 synchronous-shim-as-honest-DoD precedent] and a no_std-clean kernels variant for (B).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A synchronous sibling schedule schedules/embedded_multimcu_sync.sched.nuc exists: 3 workers (fe/dsp/rf) on the DEFAULT worker class (NO neon128/TCM/named classes — honest for the synchronous StubShim), schedule for ../prog.embedded.algo.nuc, SYNC transfers (no async/buffer/notify=event), no named memory_regions beyond embedded-pattern's declared set; kernel placements preserved. It PASSES check_schedule_compat against embedded-pattern's UNCHANGED capabilities.toml (NO capability widening — async embedded_multimcu stays correctly rejected). M10 synchronous-DoD-precedent rationale recorded in the schedule header
- [x] #2 A no_std-clean kernels variant (e.g. kernels.embedded.rs, selected via the driver --kernels flag) provides mix2/denoise as fixed-array [i32; SAMPLES_PER_FRAME] in/out (NO Vec/alloc; mirror ex5 blur3 / ex1 add no_std style) and satisfies the kernels contract for the effectful fe_capture/rf_receive/fe_emit/rf_transmit (which map to shim hooks, bodies not extracted) — whatever check_kernels_contract requires, no_std-clean. tier-1 kernels.rs is UNTOUCHED
- [x] #3 check-embedded's M11 arm is extended to cross-compile the REAL ex14 embedded_multimcu_sync: 3 per-worker no_std libs (fe/dsp/rf) each cargo check --target thumbv7em-none-eabihf CLEAN (passing --kernels for the embedded kernels file). This real cross-compile is the genuine acceptance — string-render is NOT acceptance
- [x] #4 e2e stays green & deterministic: the new schedule file is [[skip]]'d for all 7 backends in e2e-matrix.toml (mirror embedded_multimcu skips; harness hardcodes prog.algo.nuc+kernels.rs per TASK-0049.03). Additive skips (+7) are expected and NOT a regression (pass + required-fail unchanged); report the new totals line. Full gate green before commit
- [x] #5 TASK-0049.04 AC#3 closed HONESTLY via an addendum (NOT in-place AC rewrite — see feedback-ac-rewrite-on-done-task): note it is met via the synchronous sibling (real ex14 algorithm + per-frame kernels, 3 libs cross-compile); the async embedded_multimcu remains deferred to the bin slice TASK-0049.05. Mark TASK-0049.04 Done IFF its other ACs still hold
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
GAP C FIRST (empirical, cheap): the SubArray Fire-ARG path (backend-common/src/render/fire.rs:225) emits `name[start..start+len].to_vec()`, which needs alloc/Vec — NOT no_std. mix2/denoise are array-typed pure kernels whose row-of-2D args (mic_in[frame]) classify as SubArray. Hypothesis: array-typed pure-kernel ARGS do NOT lower no_std-clean today. Verify by building the sync sibling + cross-compiling; if it fails on .to_vec(), that is the genuine THIRD blocker (overlaps TASK-0361). Plan: 1) GAP A: write schedules/embedded_multimcu_sync.sched.nuc (3 default-class workers fe/dsp/rf, sync transfers, no async/buffer/notify/named regions), schedule for ../prog.embedded.algo.nuc; passes check_schedule_compat vs UNCHANGED capabilities.toml. 2) GAP B: kernels.embedded.rs no_std-clean mix2/denoise as [i32;SAMPLES_PER_FRAME] in/out + bodies for the effectful kernels iff check_kernels_contract needs them. 3) GAP C empirical: just check-embedded extended arm for ex14_sync; cross-compile. If .to_vec() blocks, decide: either fix the SubArray arg path to emit a no_std array-copy when the param is a fixed-array kernel sig (real fix, not workaround) OR file blocker. 4) e2e [[skip]] +7 for embedded_multimcu_sync. 5) AC#3 addendum on TASK-0049.04. Gate before every commit.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
LANDED, both reviews GO. Commits 54e607f (GAP C render_fire_args_nostd in backend-common) + a76cb6b (sync sibling schedule + kernels.embedded.rs + check-embedded arm 3 + contract.rs TASK-0363 note) + b4ec6f3 (e2e-matrix 7 skips) + fb9e90e (review fold-back). GOTCHAS / subtleties for the next person: (1) GAP C (array-typed pure kernels) needed REAL shared-codegen work, not just embedded-pattern: the prior proven embedded kernels (ex1 add, ex5 blur3) are SCALAR-arg; mix2/denoise are array-typed (i32[16] in/out) and the shared render_fire_args lowered sub-array args as slice.to_vec() (needs alloc). Fix = new render_fire_args_nostd (SubArrayForm::FixedArray, slice.try_into().unwrap()) in backend-common; tier-1 render_fire_args UNCHANGED + byte-identical (e2e pass=246 held). (2) The honest path for the capability gate is the SYNC SIBLING (embedded_multimcu_sync: default class, sync transfers, no async/buffer/event/regions) — NOT widening capabilities.toml (that would lie vs the synchronous StubShim). check_schedule_compat checks worker-class NAMES, so named classes fe_core/dsp_core/rf_core are rejected; the sibling uses the default class. (3) kernels.embedded.rs selected via the driver --kernels flag; mix2/denoise MUST be literal [i32; 16] NOT [i32; SAMPLES_PER_FRAME] because kernel_extract copies only the pub fn span verbatim — a const ref would be E0425 once extracted into mod kernels (no const in scope). (4) TASK-0363: a kernels file with a DOT in its stem (kernels.embedded.rs) makes contract.rs rustc_check fail (rustc derives an invalid crate name) — NON-FATAL (driver warns + proceeds, all 3 libs compile) but it defeats phase-1 compile-check for that file. (5) LATENT PANIC: render_fire_args_nostd .try_into().unwrap() is not statically proven (sub_len from data dims vs N from kernel sig, uncrossvalidated) — TASK-0049.07. PROCESS NOTE: a sustained API 529 interrupted the implementer subagent PRE-COMMIT (then the resume also 529d); the orchestrator verified the uncommitted work + ran the full gate + committed in-thread; the qa-test-runner + mped-architect review subagents then RECOVERED and gave independent GO (so review independence was NOT lost in the end). Architect P2.1/P3.1/P3.3 fold-back applied in fb9e90e.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE, both reviews GO. The real example-14 multi-MCU hearing-aid cross-compiles as 3 per-worker no_std libs (fe/dsp/rf) under .#embedded, closing slice-A TASK-0049.04 AC#3. Delivered: (a) GAP-C fix render_fire_args_nostd in backend-common (array-typed pure-kernel no_std fixed-array lowering; tier-1 path byte-identical, e2e pass=246 held); (b) synchronous sibling schedule embedded_multimcu_sync (default class + sync transfers, capabilities.toml UNCHANGED, async embedded_multimcu stays correctly rejected — honest M10-precedent DoD); (c) no_std kernels.embedded.rs via --kernels; (d) check-embedded arm 3 + e2e 7 skips. Gate: build/clippy clean, test 1088/0/3, test-release 1087/0/3, e2e 308/246/0/62/0 (pass+required-fail unchanged), check-embedded all arms incl ex14 3 libs Finished. Follow-ups filed: TASK-0363 (dotted-filename rustc_check, non-fatal), TASK-0049.07 (latent try_into-unwrap panic -> typed EmitError). Commits 54e607f + a76cb6b + b4ec6f3 + fb9e90e.
<!-- SECTION:FINAL_SUMMARY:END -->
