---
id: TASK-0341.02.02.01
title: >-
  halo_inference (B) partition-policy-aware rule: refine fatality predicate from
  enclosing-scope ivs to index-expression-referenced ivs (cycle-208
  16-jacobi/distributed gap probe)
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-27 15:11'
updated_date: '2026-05-27 16:10'
labels:
  - compiler
  - halo-inference
  - partition-aware
  - gap-probe-followup
dependencies: []
parent_task_id: TASK-0341.02.02
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
=== Filed as TASK-0341.02.02 AC#3 honest-BLOCKED follow-up, cycle 208 ===

Cycle 208 attempted 16-jacobi/distributed × pthreads-sync (partition=rows on the spatial y-loop, sync transfers — capability-compatible). The compiler rejects with:

```
halo-inference error (under partitioned iv): kernel call `jacobi5_or_seed` reads `field` at axis 0 with a non-affine index expression. Halo inference accepts only `iv + b` (b constant) — see PRD §13 / TASK-0260 module docs.
```

## Root cause

`apply_halo_inference_partition_aware` in `nucleus/nucleus-compiler/src/passes/halo_inference.rs:471-484` decides per-error fatality with:

```rust
if scope.iter().any(|iv| iv_is_partitioned(linked, iv)) {
    return Err(err);
}
```

`scope` is the FULL enclosing iter-var nest at the error site — `[t, y, x]` for the failing read inside `for t { for y { for x { jacobi5_or_seed(...) } } }`. `y` is partitioned ⇒ the predicate fires fatal.

But the failing index expression `(t + ITERS) % (ITERS + 1)` is at axis 0 of `field`, references only iv `t`, and `t` is NOT partitioned. The non-affine wrap on the time axis is harmless under spatial partition — no halo strip is needed on a non-partitioned axis. The (B) rule is over-conservative.

## Why the cycle-95 rationale (TASK-0275) predates this case

The cycle-95 comment justifies the (B) rule by citing 11-game-of-life: `grid[(t + ITERS) % (ITERS + 1)]` would newly-reject under a naive strict promotion. But 11-game-of-life uses pipelined.sched.nuc (no partition); no iv in scope is partitioned -> advisory; preserved. The (B) rule's coarse 'any-iv-in-scope-partitioned' predicate happens to work for that example because there is NO partition at all. The 16-jacobi/distributed case is the first encountered shape where a non-affine index on a non-partitioned axis SHARES the enclosing scope with a partitioned iv on a DIFFERENT axis.

## Proposed refinement

Record (or compute on demand) the set of ivs referenced by the failing index expression -- call it ivs_used_in_index. The fatality predicate becomes:

    // Refined (B'): only the ivs the failing INDEX EXPRESSION references
    // determine fatality, not the full enclosing scope.
    if ivs_used_in_index.iter().any(|iv| iv_is_partitioned(linked, iv)) {
        return Err(err);
    }

Edge case: ivs_used_in_index empty (purely-constant index that nonetheless failed const-fold) -> preserve current advisory behaviour (no halo to emit; no risk).

The walker in classify_index already computes ivs_used via collect_iter_var_refs; the patch is:
1. Augment the four error variants (DataDependentStride, StridedAccessNotSupported, MultipleIterVarsInIndex, NonAffineIndex) with an ivs_referenced: Vec<String> payload OR
2. Pair the error with its iv set in the returned errors_with_scope triple (cleaner -- does not change the error variant API surface).

Option 2 is preferred. HaloErrorWithScope becomes (HaloInferenceError, Vec<String> /* enclosing scope */, Vec<String> /* ivs in the failing index */).

## Acceptance criteria

1. Refinement lands: apply_halo_inference_partition_aware uses ivs_used_in_index instead of scope for the fatality predicate. Walker plumbs the iv set through.
2. Regression pin: existing 11-game-of-life shape (no partition + Mod wrap) stays advisory; existing partitioned-iv-on-the-failing-axis shapes (e.g. a stride-2 strided read in a partitioned outer loop) stay fatal. Add negative unit tests at both edges.
3. 16-jacobi/distributed × pthreads-sync PASSES bit-identical against reference.bin. PROMOTE the e2e-matrix [[skip]] entry to [[required]] with cycle citation.
4. Cross-backend sibling promotion: open a sub-task for promoting to the other tier-1 backends once AC#1-3 land on pthreads-sync.

## Honest scope LIMITS

- This refinement only affects apply_halo_inference_partition_aware. The strict apply_halo_inference entry point (used by no current caller per the cycle-95 module docs) is unchanged.
- Multi-axis partition (partition=blocks2d over a 2D outer iv) is unaffected -- that scope still has a partitioned iv, and any failing index in such a scope is correctly fatal if it references the blocks2d outer iv on either axis.
- The MultipleIterVarsInIndex case has multiple ivs referenced; under the refinement, fatal if ANY of them is partitioned (else advisory). That is the natural extension.

## Companion / linkage

- Parent: TASK-0341.02.02 (16-jacobi AC#3 honest-BLOCKED).
- Related: TASK-0275 (cycle 95 (B) rule promotion); module docs at nucleus/nucleus-compiler/src/passes/halo_inference.rs:447-484.
- 11-game-of-life is the regression-pin example for 'no partition + Mod wrap stays advisory'.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Cycle 209 result: (B') refinement LANDED + AC#3 honest-BLOCKED ===

AC#1 (refinement lands): DONE. apply_halo_inference_partition_aware now consults `ivs_in_index` (populated lexically from collect_iter_var_refs at each error-push site) instead of the FULL enclosing scope. Per-variant rule split:
- NonAffineIndex / StridedAccessNotSupported / MultipleIterVarsInIndex / UnknownLoopVar: precise (ivs_in_index).
- DataDependentStride / UnknownKernelInCall: conservative scope-fallback (the lexical iv walk does not surface every iv a data-dependent address depends on).

Implementation: extended HaloErrorWithScope from (HaloInferenceError, Vec<String>) to a triple. Walker plumbs the iv set through at all 6 error-push sites. Module-level docs updated to describe (B) -> (B') refinement.

AC#2 (regression pin): DONE. 4 new in-mod tests:
- bprime_modwrap_nonpartitioned_axis_stays_advisory (16-jacobi shape, new behavior).
- bprime_modwrap_partitioned_axis_stays_fatal (complement, fatal preserved when the gap matters).
- bprime_strided_on_partitioned_iv_stays_fatal (symmetric for StridedAccessNotSupported variant).
- bprime_modwrap_no_partition_at_all_stays_advisory (11-game-of-life regression pin, advisory preserved under no partition).

AC#3 (16-jacobi/distributed × pthreads-sync bit-identical): HONEST-BLOCKED. Post-cycle-209 empirical re-test of all 7 tier-1 backends shows the (B') refinement DID unblock halo_inference (every backend progresses past), and the next-layer gaps surfaced precisely:
- 5 backends (pthreads-sync, pthreads-async, mp-tcp-event, openmp-rs, mp-uds-event): blocked by the 3D wait_slice gap (backend-common::multi_worker_walker::wait::wait_slice rank-3 guard, the TASK-0294 cycle-115 architect P2.1 forward-carry). 16-jacobi's `field[ITERS+1][H][W]` produces a rank-3 per-worker tile.
- 2 backends (mp-tcp-bufsync, mp-tcp-poll): blocked by TASK-0330 (in-Loop w2w Push; flat host-relay model can't handle in-Loop w2w transfers).

AC#3 closed honest-BLOCKED; both next-layer gaps are precise and existing. New follow-up filed: TASK-0341.02.02.01.01 (extend wait_slice to N-D nested-loop dispatch) -- the 5-backend gap. The 2-backend gap (TASK-0330) does NOT get a new task (existing TASK-0330 covers it).

AC#4 (cross-backend sibling promotion task): superseded. The cross-backend AC of the original brief assumed AC#3 landed on pthreads-sync first; with AC#3 honest-BLOCKED, the promotion sweep moves into TASK-0341.02.02.01.01's AC#4 (which promotes all 5 wait_slice-using backends in one fix).

Cycle-209 forward-carried lessons (recorded onto TASK-0341.02.02.01.01):
- The wait.rs comment at lines 273-292 contained 'No shipped schedule constructs such a (tile, data) shape today' — TRUE at cycle 115, FALSE at cycle 209. Updated in cycle 209 to flag the cycle-flip; the implementer of .01.01 should rewrite once the N-D path lands.
- The cycle-209 empirical-then-empirical-again pattern (verify the gap, fix the gap, re-test, observe the next-layer gap, file precisely) is the load-bearing 'gap probe' shape per memory feedback-orchestrator-narrative-also-wrong cycles 149-150.

Cycle-209 gate (pre-commit):
- just build: clean
- just clippy: clean
- just test: 1019 / 0 / 3 (was 1015 — +4 (B') regression pins).
- just test-release: clean
- just check-textual-replace-on-codegen: clean
- just check-include-str-coverage: clean
- just check-narrative-doc-lie: clean (after one fold-back: rephrased 'blocked by TASK-0330' to past-tense + AT FILING TIME marker to satisfy the cycle-169 hygiene rule).
- just check-mega-files: clean
- just e2e: 259/225/0/34/0 (preserved — the 7 [[skip]] cells remain skipped with updated reasons citing the new gap layer).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 209: halo_inference (B) partition-policy-aware rule refined to (B'). Per-error fatality predicate now consults the failing index expression's referenced ivs (precise) for the four affine-arithmetic variants, and falls back to enclosing scope (conservative) for DataDependentStride and the UnknownKernelInCall invariant-guard variant. 4 in-mod regression-pin tests added at both edges. Empirical re-test of all 7 tier-1 backends on 16-jacobi/distributed: every backend progresses past halo_inference (the upstream blocker is gone); two distinct next-layer gaps fire (3D wait_slice on 5 backends, TASK-0330 in-Loop w2w Push on 2). AC#3 honest-BLOCKED; TASK-0341.02.02.01.01 filed for the 3D wait_slice extension. e2e 259/225/0/34/0 preserved. Tests 1015/0/3 -> 1019/0/3.
<!-- SECTION:FINAL_SUMMARY:END -->
