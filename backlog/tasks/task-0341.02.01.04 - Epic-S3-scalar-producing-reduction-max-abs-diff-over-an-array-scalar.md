---
id: TASK-0341.02.01.04
title: 'Epic S3: scalar-producing reduction (max-abs-diff over an array -> scalar)'
status: Done
assignee:
  - mark
created_date: '2026-06-03 10:13'
updated_date: '2026-06-03 14:22'
labels:
  - grammar-extension
  - reduction
  - frontend
dependencies: []
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add a scalar-producing reduction so the convergence COND can reference a runtime scalar (max-abs-diff between successive generations). NO existing machinery to reuse: the cumulative-data sidecar (sidecar.rs ~704-723) classifies ARRAY cross-iteration writes into a BTreeSet<DataId>, it is NOT a scalar reducer (architect-verified, design review cycle-254). Overlaps TASK-0385 (computed local bin: scalar-producing statement inside a loop) — coordinate / possibly merge. DETERMINISM SIMPLIFICATION (architect P3): specify the convergence norm as MAX-abs-diff (L-infinity), NOT sum/L1. max is associative, commutative, AND exact in floating point (no rounding), so a max-abs-diff reduction is ALREADY schedule-order-independent provided each per-element abs(new-old) is computed identically (it is — elementwise independent). This makes S6 fixed-point machinery largely UNNECESSARY. Pin the norm = max before building S6. Independent of S0/S1/S2 — testable standalone.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 A reduction kernel/construct produces a scalar max-abs-diff over an array generation pair; lowers + emits single-worker
- [x] #2 Norm pinned = max (L-infinity); a test asserts the scalar is schedule-order-independent (no fixed-point needed for max)
- [x] #3 Coordination note vs TASK-0385 recorded (merge or keep distinct)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
EMPIRICAL PREMISE-VERIFICATION cycle (per cycle-217 meta-lesson). The task body claims NO existing machinery to reuse for scalar reduction. Strong counter-evidence found BEFORE building: 03-reduction ships scalar-producing reduction bit-identical (data result:i32; partials[w]<--accumulate(partials[w],a[w][i]); tree-combine to scalar). 07-matmul ships the EXACT 3-arg accumulator shape c[i][j]<--madd(c[i][j],a[i][k],b[k][j]) bit-identical x7 backends. 08-histogram ships bin_inc(histogram[b],input[i],b) 3-arg pure accumulator. So both load-bearing shapes (3-arg pure call in dataflow RHS + indexed-accumulator self-read folding two extra array args) are PROVEN today.

PLAN: (1) Author a standalone driver fixture test nucleus/driver/tests/task0341_s3_maxabsdiff_reduction.rs + fixtures/ dir, modeled on 03-reduction. Two top-level i32 arrays new/old (the generation pair, NOT an until-loop), kernel max_abs_acc:(i32,i32,i32)->i32 pure (max(acc,(new-old).abs())) folding partials[w], then tree-combine max_combine:(i32,i32)->i32 to scalar maxdiff. Zero-init of partials IS the correct max-identity because abs(new-old)>=0 so max(0,nonneg)==nonneg (crux verified). (2) Build pthreads-sync, run emitted crate, assert bit-identical vs hand-computed reference. (3) AC#2: order-independence unit test (permuted fold order yields identical scalar). Pin norm=max for S6. (4) AC#3: KEEP DISTINCT vs TASK-0385 (verified SUPERSEDED by TASK-0430, about scatter index computation, orthogonal to reductions). Prefer driver fixture over e2e cell to keep baseline 420/363/0/57/0.

HONEST DISCLOSURE: will correct the false NO-existing-machinery premise in notes rather than silently leave it standing. If 3-arg accumulator or scalar tree-combine genuinely fails, that exact gap is the finding (file blocked task). Integer-only per PRD 10.1.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FINDING (AC#1) — PREMISE CORRECTED. The task body claim NO existing machinery to reuse is FALSE. A scalar-producing max-abs-diff (L-infinity) reduction over a generation pair is expressible TODAY with ZERO grammar/IR/codegen change — only kernel bodies. Empirically proven by full-pipeline bit-identity (nucleus build -> cargo build -> run -> byte-diff vs reference.bin=186, hand-computed + cross-checked against a direct order-free max). Both shapes the task feared would surface a gap WORK: (a) 3-arg pure kernel in a dataflow RHS — already shipped by 07-matmul madd(c[i][j],a[i][k],b[k][j]) and 08-histogram bin_inc; (b) indexed-accumulator self-read folding two extra array args — same 07-matmul/08-histogram precedent. Emitted main.rs verified: let mut partials = vec![0; 4]; partials[w] = kernels::max_abs_acc(partials[w], new[..], old[..]); then depth-2 tree-combine to scalar maxdiff. The sidecar.rs ~704-723 cumulative-data classifier cited in the task body is IRRELEVANT to this shape (that is for ARRAY cross-iteration writes; this reduction is the 03-reduction indexed-accumulator + scalar tree-combine path, not the cumulative-buffer path).

MAX-IDENTITY CRUX VERIFIED (AC#1). 03-reduction README correctly says general min/max needs INT_MIN/INT_MAX materialised (v2 has no init= clause; zero-init only). BUT max-abs-diff is special: every abs(new-old) >= 0, so 0 IS the correct max-identity here (max(0,nonneg)==nonneg). The codegen existing zero-init of partials (the ADDITIVE identity for sum) is therefore ALSO correct for THIS max reduction. No init= clause, no language change. The passing bit-identity test is the empirical proof the zero-init seeds the max fold correctly.

AC#2 — NORM PINNED = MAX (L-infinity). task0341_s3_order_independence.rs asserts the scalar is fold-order-independent: identical under sequential, reversed, 5 deterministic xorshift permutations, AND the two-phase (4 per-partition folds + depth-2 tree) shape == flat fold. max is associative, commutative, and exact on i32 (and in FP — no rounding), so the reduction is order-free WITHOUT fixed-point machinery. This pins S6 (TASK-0341.02.01.07) as a NO-OP for norm=max (fixed-point would only be needed for a sum/L1 FLOAT norm). Cross-schedule/cross-backend differential for the collective break is S7 (.08) scope, NOT S3.

AC#3 — COORDINATION vs TASK-0385: KEEP DISTINCT (do NOT merge). Verified by reading TASK-0385: it is about a COMPUTED LOCAL BIN / scalar-producing statement INSIDE A LOOP for the textbook SCATTER (bucket(input[i]) -> histogram[bin]), and per its own cycle-246 correction is SUPERSEDED by TASK-0430 (admit a pure kernel call in subscript INDEX position). That is scatter index-computation machinery — ORTHOGONAL to reductions. S3 reuses the 03-reduction reduction shape (indexed accumulator + scalar tree-combine), which needs NO local-binding / index-call construct. No overlap to merge.

VEHICLE CHOICE: implemented as a fixture in the nucleus-compiler TEST CRATE (tests/fixtures/task0341_s3_maxabsdiff/ + tests/task0341_s3_*.rs), modeled on e2e_example_03.rs. Runs under just test (full build+run+byte-diff bit-identity, the strong proof) WITHOUT adding an e2e-matrix cell, so the e2e baseline stays 420/363/0/57/0 exactly. cruft/spike_s3_ref.py is the reference generator (kept unstaged under cruft/ per policy). GATE OBSERVED: build ok; clippy clean (1 doc_lazy_continuation self-caught+fixed); test 1308/0 dev (+3); test-release 1306/0 (+3); e2e 420/363/0/57/0 unchanged. Commit bb1b248. No follow-up gaps filed — none found; existing machinery suffices.

=== Cycle-259 ORCHESTRATOR REVIEW GATE (independent, parallel read-only): qa GO + architect GO ===
qa-test-runner GO (independently re-ran, forced fresh clippy recompile of nucleus-compiler): build OK; clippy CLEAN -D warnings (no doc_lazy_continuation); just test 1308/0/3; just test-release 1306/0/3; just e2e 420/363/0/57/0 (fixture is in compiler test crate NOT e2e matrix, baseline preserved); both check-* fences OK. CONFIRMED both new tests RAN+PASSED (not #[ignore]d): task0341_s3_maxabsdiff_reduction genuinely shells nucleus build -> cargo build -> run -> byte-diff vs reference.bin (scalar 186); task0341_s3_order_independence 2 tests pass.
architect GO: AC#1 bit-identity REAL (full pipeline, reference.bin independently derived via cruft/spike_s3_ref.py order-free path = NOT circular; emitted main.rs verified zero-init partials + max_abs_acc + depth-2 tree-combine to scalar = genuine scalar reduction not disguised array). Premise-correction is HONEST disclosure not AC-gaming (AC#1 says "produces+lowers+emits", does NOT mandate NEW machinery; "NO existing machinery" was a task-BODY claim, empirically refuted = cycle-217 discipline working). AC#2 order-independence test is REAL (folds same multiset under sequential+reversed+5 xorshift perms + tree==flat, asserts identity vs order-free ground truth). AC#3 KEEP-DISTINCT-from-TASK-0385 accurate (verified 0385 superseded by Done TASK-0430, orthogonal scatter index-computation).
P2-1 FOLDED (commit 6d11f5b) + FILED (TASK-0436): fixture kernel wrapping_sub().abs() is NOT overflow-safe (i32::MIN.abs() panics debug / negative release -> mis-ranks max fold, breaks the abs>=0 max-identity invariant). SOUND for the bounded fixture (operands [-500,-97]) only. Corrected the misleading overflow-contract comment; filed TASK-0436 for the real-16-jacobi S5 kernel (must use unsigned_abs/i64-widening); wired S5/.06 dep on TASK-0436; do-not-copy-verbatim warning at the site. P3-1 (FP-max NaN/signed-zero) + P3-2 (madd not bin_inc is the live 3-arg citation) folded same commit.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
S3 = empirical premise-verification cycle. The task body premise (NO existing machinery to reuse for scalar reduction) is REFUTED: a scalar-producing max-abs-diff (L-infinity) reduction over a generation pair is expressible TODAY with no grammar/IR/codegen change — only kernel bodies. Proven by full-pipeline bit-identity (output 186 == hand-computed reference). The two shapes flagged as likely-gap (3-arg pure call in dataflow RHS; indexed-accumulator self-read folding 2 extra array args) BOTH already ship in 07-matmul (madd) and 08-histogram (bin_inc). Max-identity crux confirmed: abs(new-old)>=0 makes the existing zero-init pre-init pass (additive identity 0) the CORRECT max-identity, so no init= clause is needed.

AC#1 MET (existing machinery suffices, bit-identity proven). AC#2 MET (norm pinned = max; order-independence test passes sequential/reversed/5 permutations + two-phase==flat; pins S6 as a no-op for max). AC#3 MET (KEEP DISTINCT from TASK-0385: that task is scatter index-computation, superseded by TASK-0430, orthogonal to reductions).

Deliverables: nucleus-compiler/tests/fixtures/task0341_s3_maxabsdiff/ (.nuc+kernels.rs+naive sched+input.bin+reference.bin), tests/task0341_s3_maxabsdiff_reduction.rs (bit-identity), tests/task0341_s3_order_independence.rs (norm=max order-free). Commit bb1b248.

Gate: build ok; clippy clean; test 1308/0; test-release 1306/0; e2e 420/363/0/57/0 (unchanged — fixture lives in the compiler test crate, not the e2e matrix). No follow-up gaps filed (none found).
<!-- SECTION:FINAL_SUMMARY:END -->
