---
id: TASK-0341.02.01.04
title: 'Epic S3: scalar-producing reduction (max-abs-diff over an array -> scalar)'
status: To Do
assignee: []
created_date: '2026-06-03 10:13'
labels:
  - grammar-extension
  - reduction
  - frontend
dependencies: []
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add a scalar-producing reduction so the convergence COND can reference a runtime scalar (max-abs-diff between successive generations). NO existing machinery to reuse: the cumulative-data sidecar (sidecar.rs ~704-723) classifies ARRAY cross-iteration writes into a BTreeSet<DataId>, it is NOT a scalar reducer (architect-verified, design review cycle-254). Overlaps TASK-0385 (computed local bin: scalar-producing statement inside a loop) — coordinate / possibly merge. DETERMINISM SIMPLIFICATION (architect P3): specify the convergence norm as MAX-abs-diff (L-infinity), NOT sum/L1. max is associative, commutative, AND exact in floating point (no rounding), so a max-abs-diff reduction is ALREADY schedule-order-independent provided each per-element abs(new-old) is computed identically (it is — elementwise independent). This makes S6 fixed-point machinery largely UNNECESSARY. Pin the norm = max before building S6. Independent of S0/S1/S2 — testable standalone.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A reduction kernel/construct produces a scalar max-abs-diff over an array generation pair; lowers + emits single-worker
- [ ] #2 Norm pinned = max (L-infinity); a test asserts the scalar is schedule-order-independent (no fixed-point needed for max)
- [ ] #3 Coordination note vs TASK-0385 recorded (merge or keep distinct)
<!-- AC:END -->
