---
id: TASK-0341.02.01.02
title: >-
  Epic S2: relational + boolean operators (bool-valued expressions) in the algo
  expression grammar
status: To Do
assignee: []
created_date: '2026-06-03 10:12'
labels:
  - grammar-extension
  - frontend
  - grammar-algo
dependencies: []
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Add relational operators (<=, <, ==, !=, >, >=) producing bool, plus the bool-expression layer, to the algorithm expression grammar (docs/grammar-algo.md section 1 currently has only +,-,*,/,% — no relational/bool-valued operators; bool exists ONLY as a scalar data TYPE, not an expression result). MUST PRECEDE S1 (architect design review cycle-254, P1 dependency inversion): the for..until COND clause in S1 needs a GRAMMATICAL bool expression to parse; an inert S1 that parses COND as an opaque bool-expr is NOT landable until this exists (the parser would fail at <= today, not parse-then-reject). Independent of S0/S1/S3 — testable standalone (a bool-typed dataflow or a parse-only bool expr). Wire through algo/parser.rs expr_parser (now: relational below additive precedence), algo/ast.rs Expr (new BinOp relational variants or a Cmp node), ir.rs IrExpr, and decide the scalar bool result representation. Anchor any malformed-relational diagnostic per the TASK-0434 span-anchoring precedent.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Relational operators <=,<,==,!=,>,>= parse with correct precedence (below additive) and produce a bool-typed expression
- [ ] #2 Round-trips through AST + AlgoIR; bit-identical determinism check stays green (new ops only reachable where used)
- [ ] #3 Negative tests: malformed relational expr gives a span-anchored diagnostic
<!-- AC:END -->
