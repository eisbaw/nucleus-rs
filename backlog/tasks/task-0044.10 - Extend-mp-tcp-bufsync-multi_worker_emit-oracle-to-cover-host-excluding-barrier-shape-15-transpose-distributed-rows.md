---
id: TASK-0044.10
title: >-
  Extend mp-tcp-bufsync multi_worker_emit oracle to cover host-excluding-barrier
  shape (15-transpose/distributed-rows)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-28 07:33'
updated_date: '2026-05-28 07:51'
labels: []
dependencies:
  - TASK-0044.08
parent_task_id: TASK-0044
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Silent-sibling completeness task surfaced by the cycle-232 architect review of TASK-0044.08 (P2-1). mp-tcp-bufsync ALSO has the host-excluding-barrier fast-emit-oracle hole that cycle 231 (openmp-rs, mp-tcp-poll) and cycle 232 (mp-tcp-event, mp-uds-event) closed for the other backends.

Empirically confirmed cycle 232 (orchestrator, probes reverted):
- mp-tcp-bufsync carries the host-excluding-barrier ContractGap guard at nucleus/backends/mp-tcp-bufsync/src/plan/mod.rs:163-173 (same shape as the poll/event/uds guards).
- mp-tcp-bufsync IS in the apply_host_mediation_inject gate (nucleus/driver/src/main.rs:525) but NOT in the apply_host_data_relay_inject gate (event/uds only). So its oracle is MEDIATION-ONLY, mirroring the mp-tcp-poll cycle-231 template, NOT the reactor mediation+data-relay pattern.
- No mp-tcp-bufsync test (check_frame_emit, host_relay_emit, loop_body_w2w_push, pingpong, rendezvous_emit, reuse_codegen_emit, wait_before_push) exercises a host-excluding barrier or calls apply_host_mediation_inject. Grep for 'apply_host_mediation_inject' / 'exclude the host' across that tests/ dir returns zero matches.

Scope: add a 15-transpose/distributed-rows oracle test to a mp-tcp-bufsync test file that inline-applies apply_host_mediation_inject (mediation-only; NO data-relay), asserts the UNMEDIATED emit returns Err(ContractGap 'exclude the host worker') with the rejected barrier's participants = the 4 compute workers (host absent), and post-mediation emit Ok with the formerly-host-excluding barrier now host-inclusive. Mirror the mp-tcp-poll cycle-231 test transpose_15_distributed_rows_poll_equiv_bufsync.

7-backend family-completeness map (the host-excluding-barrier oracle):
openmp-rs DONE (231), mp-tcp-poll DONE (231), mp-tcp-event DONE (232), mp-uds-event DONE (232), pthreads-sync covered pre-existing (partial-barrier fixture pthreads-sync/tests/multi_worker.rs:465), pthreads-async filed TASK-0044.09, mp-tcp-bufsync = THIS task. Family is complete only when BOTH TASK-0044.09 AND this task land.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== cycle 233 implementation (Done) — 7-backend family COMPLETE ===
Added test transpose_15_distributed_rows_host_excluding_barrier_bufsync in a NEW file nucleus/backends/mp-tcp-bufsync/tests/multi_worker_emit.rs (commit 8c2ab7f). New file (not appended to host_relay_emit.rs) because that file's name/docstring is specific to the cycle-148 DATA-relay phase, which this MEDIATION-ONLY oracle does NOT exercise; multi_worker_emit.rs matches the sibling-backend naming (poll/event/uds/openmp all use multi_worker_emit.rs). Mirrored the poll harness (repo_root/scratch_dir + inline pass-sequence); no Cargo.toml change needed (nucleus-compiler + backend-common + pthreads-sync are [dependencies], visible to integration tests).

Confirmed driver pass-order: bufsync IS in the host-mediation gate (driver/src/main.rs:525) but NOT the data-relay gate (:592, event/uds only) — so the test applies apply_host_mediation_inject ONLY, mirroring mp-tcp-poll. Host election via backend_common::elect_host_from_name_workers (driver-mirroring rule).

Pre-mediation assertion BITES: UNMEDIATED bufsync emit returns Err(ContractGap). Empirical message: 'barrier #2 participants {WorkerId(1), WorkerId(2), WorkerId(3), WorkerId(4)} exclude the host worker; ...'. Host is WorkerId(0), ABSENT from the rejected set; the 4 compute workers ARE present. Asserted host's WorkerId NOT in err_text + every compute worker IS — participant-SET anchor, NOT the hardcoded '#2' bid (renumber-safe). Bite-verified: inverting the host-absent check to host-present FAILS the test.

Post-mediation: emit Ok; host bin carries wire::barrier_cross(&mut ...). Bite-verified: replacing that needle with a non-existent symbol FAILS the test.

Proves mediation is load-bearing (not vacuous, unlike 03-reduction/distributed where both barriers already include host).

7-BACKEND host-excluding-barrier oracle family is now COMPLETE: openmp-rs (multi_worker_emit.rs, 231), mp-tcp-poll (multi_worker_emit.rs, 231), mp-tcp-event (multi_worker_emit.rs, 232), mp-uds-event (multi_worker_emit.rs, 232), pthreads-sync (multi_worker.rs:465 pre-existing host-excluding {w0,w1} partial-barrier fixture), pthreads-async (multi_worker_codegen.rs, TASK-0044.09 / cycle 233), mp-tcp-bufsync (multi_worker_emit.rs, this / cycle 233). Verified via grep across all 7 backends' tests/.

Gate: just test 1048/0/3, just test-release 1047/0/3, just e2e 280/246/0/34/0 (test-only — e2e unchanged).

Forward-carry to TASK-0044.11: the cycle-232 reactor tests' bid-hardcode (Bar2/bid=2) hardening is still open — NOT touched here (scope boundary respected). Note the empirically-observed bufsync ContractGap message DOES hardcode 'barrier #2', so any future test that DID anchor on '#2' would silent-pass under a sync-tag renumber; this test deliberately avoids it.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Added transpose_15_distributed_rows_host_excluding_barrier_bufsync (NEW multi_worker_emit.rs, commit 8c2ab7f). MEDIATION-ONLY (bufsync in driver host-mediation gate :525, not data-relay :592). Pre-mediation Err(ContractGap 'exclude the host worker') participants {WorkerId(1..4)} host(WorkerId(0)) absent; post-mediation Ok + host bin wire::barrier_cross. Participant-set anchor (not '#2' bid); both halves bite-verified. 7-backend host-excluding-barrier oracle family now COMPLETE (all 7 verified via grep). Gate: test 1048/0/3, test-release 1047/0/3, e2e 280/246/0/34/0 unchanged.
<!-- SECTION:FINAL_SUMMARY:END -->
