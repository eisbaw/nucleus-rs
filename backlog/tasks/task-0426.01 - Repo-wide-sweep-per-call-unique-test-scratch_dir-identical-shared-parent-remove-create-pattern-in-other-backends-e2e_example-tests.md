---
id: TASK-0426.01
title: >-
  Repo-wide sweep: per-call-unique test scratch_dir (identical shared-parent
  remove/create pattern in other backends + e2e_example tests)
status: In Progress
assignee:
  - '@me'
created_date: '2026-06-02 16:37'
updated_date: '2026-06-10 10:00'
labels:
  - testing
  - test-flake
  - silent-sibling
  - cycle-242
dependencies: []
parent_task_id: TASK-0426
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Forward-carried from TASK-0426 (silent-sibling sweep). TASK-0426 hardened the 3 mp-tcp-bufsync test scratch_dir helpers (per-call-unique {name}-{pid}-{counter} path, drop shared-parent remove_dir_all). The STRUCTURALLY IDENTICAL racy pattern (`let dir = target.join(name); let _ = remove_dir_all(&dir); create_dir_all(&dir)` on a shared parent) exists in 12+ other test files: pthreads-sync/tests/{emit,multi_worker}.rs, pthreads-async/tests/check_frame_emit.rs, openmp-rs/tests/multi_worker_emit.rs, mp-tcp-poll/tests/{pingpong,multi_worker_emit}.rs, mp-tcp-event/tests/host_relay_emit.rs, mp-uds-event/tests/multi_worker_emit.rs, mp-tcp-bufsync/tests/{pingpong,rendezvous_emit,host_relay_emit}.rs, and nucleus-compiler/tests/e2e_example_0{1..7}.rs. None have been OBSERVED flaky, but they carry the same latent race class. SCOPE: apply the same per-call-unique-path hardening (or extract a shared test-common scratch_dir helper) across all sites. NOTE from TASK-0426: the original ENOENT flake was NOT reproducible in 16 runs and static analysis did not confirm the documented shared-parent mechanism for unique-named subdirs in a single cargo-test process — so this sweep is defense-in-depth / consistency, not a confirmed-bug fix. If the flake recurs, capture the exact failing ABSOLUTE path + the two racing operations (e.g. via a logging wrapper or strace) to pin the true mechanism before assuming it is the scratch-dir pattern. Pointer: TASK-0426 commit 0e70cdd as the template.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
CYCLE re-audit (post-cycle-242): TASK-0426.01 was already landed Done in cycle 242/243 (commits a2b3681..58ca843). Re-opened and assigned to verify the sweep is still INTACT and to re-derive the denominator structurally (per coverage-audit-undercount memory), catching any site that regressed or was ADDED since cycle 242. Method: (1) grep remove_dir_all across nucleus/ -> reconcile vs cycle-242 list; (2) grep the FULL scratch-construction signature (unique_scratch_dir | fn scratch_dir | target.join | /target/ | create_dir_all | remove_dir_all | temp_dir | tempdir) across ALL test trees in ownership; (3) classify every hit as converted / already-clean / CAT-A-safe / skipped; (4) inspect every NEW test file added since cycle 242; (5) targeted cargo test + clippy on touched/representative crates. Forward-note connect/retry observations to TASK-0461 (read-only; backend src not in ownership).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CYCLE-242 IMPL PLAN (impl session):

Structural re-derivation: grep -rln remove_dir_all --include=*.rs nucleus/ => 44 files (43 listed by orchestrator + the already-fixed template check_frame_emit.rs). Reconciled against orchestrator categories A-D; corrections noted below.

APPROACH (shared helper, not 30x inline copies): add unique_scratch_dir(parent: &Path, leaf: &str) -> PathBuf to nucleus/test-common/src/lib.rs (per-call {leaf}-{pid}-{atomic counter}, create_dir_all once, never remove). test-common is a dev-dependency of ALL 11 backend crates (verified Cargo.toml) and is usable from #[cfg(test)] mod tests in src (mpi/embedded). The 7 nucleus-compiler e2e_example_*.rs files do NOT depend on test-common and neither does driver; rather than add a new crate dependency to the product crate dev-deps for a 5-line helper, apply the inline template fix there. Recorded trade-off.

CAT B (per-call scratch_dir(name) helper) -> route through test_common::unique_scratch_dir: mp-tcp-bufsync/{pingpong,host_relay_emit,rendezvous_emit}, mp-tcp-poll/{pingpong,multi_worker_emit}, mp-tcp-event/host_relay_emit, mp-uds-event/multi_worker_emit, openmp-rs/multi_worker_emit, pthreads-sync/{emit,multi_worker,check_frame_codegen}, pthreads-async/check_frame_emit (also fix its comment-doc-lie: docstring CLAIMS UNIQUE but code did not). e2e_example_01..07 -> inline template fix (7 files).

CAT C/D (fixed-leaf inline scratch + remove_dir_all) -> route through test_common::unique_scratch_dir(parent, leaf) for backend tests/ + src/tests: mpi-blocking/src/tests (4 sites), mpi-nonblocking/src/tests (4), embedded-pattern/src/{tests,tests/bin_shape} (centralize via the scratch_leaf-taking emit_* helpers), mp-tcp-bufsync/{wait_before_push,loop_body_w2w_push}, mp-tcp-event/{multi_worker_emit,loop_body_w2w_push}, openmp-rs/skeleton, mp-tcp-poll/skeleton, mp-uds-event/skeleton, pthreads-async/{skeleton,multi_worker_codegen}. CAT D inner-subdir removes (uds_out/async_out etc) become harmless once stem is unique; drop them.

CAT A (already pid-unique) -> verify only, leave: driver/tests/{cli_reuse_strict,emit_pn,task0048_05_shim_rejection,task0348_zero_init_invariant,task0371_partition_insufficient_work_reject}, e2e/src/{tests,main}, e2e/tests/concurrency_stress.

GATE: just build/clippy/test/test-release + one e2e; counts must be UNCHANGED.

PROGRESS: all edits applied. Added test_common::unique_scratch_dir helper + unit test. Converted CAT B (12 backend helpers + 7 e2e_example inline) and CAT C/D (mpi-blocking x5, mpi-nonblocking x5, embedded src/tests x3 + bin_shape x3 via shared replace_all, mp-tcp-bufsync wait_before_push x2 + loop_body x3, mp-tcp-event multi_worker_emit x8 + loop_body x3, openmp-rs skeleton x2 + single_worker_emit x2, mp-tcp-poll skeleton x1 + single_worker_emit x2, mp-uds-event skeleton x1 + single_worker_emit x2, pthreads-async skeleton x4 + multi_worker_codegen x3). CAT A (driver tests x5, e2e/src/tests, e2e/src/main, e2e concurrency_stress) verified pid-unique (fresh_outdir/nondet_tmp/run-id) — left unchanged. CORRECTION: pthreads-async/check_frame_emit docstring CLAIMED unique but code did not (comment-doc-lie) — fixed. Running gate next.

GATE (impl session, in dev shell): build OK; clippy clean (-D warnings, --all-targets re-run + read); cargo test --workspace 1269 passed / 0 failed (dev); cargo test --workspace --release 1267 passed / 0 failed (release; dev-release delta 2 = pre-existing debug_assert should_panic per TASK-0291); e2e 385/328/0/57/0 (matches baseline, test-helper-only change). NET test delta = +1 (git diff HEAD shows exactly one added #[test], zero removed) = the new test_common unit test; all other counts unchanged. NOTE: the 1256/1254 baseline cited in TASK-0426 is stale (pre-dates later test-adding cycles e.g. TASK-0429); current pre-change tree was 1268/1266, now 1269/1267.

COMMITS: a2b3681 (test-common helper) -> f517ef2 (CAT B 12 backend helpers) -> 1cd13d5 (7 e2e_example inline) -> 17c8359 (CAT C/D inline). Committed on master, not pushed.

REJECTED APPROACH (recorded for feed-forward): considered a single shared helper for ALL sites incl the 7 nucleus-compiler e2e_example tests + driver tests. REJECTED for those two crates: nucleus-compiler and driver do NOT depend on the test-common dev-dep, and adding a new dev-dependency to the product crate graph for a 5-line helper is heavier than an inline copy. Driver was already Cat A (pid-unique) so needed nothing. So: shared helper for the 11 backend crates (all already dev-dep test-common), inline copy for the 7 e2e_example files.

GOTCHA: unique_scratch_dir CREATES the dir, so a few inline sites that previously did remove+create+kernels-stub-write had a now-redundant create_dir_all dropped (harmless either way; emit() also create_dir_alls its out dir). The inner subdirs (out/poll/async/gen/unmediated/...) need no per-subdir uniqueness once the stem is unique — verified by reading each.

STATUS: implementer work complete, all 4 commits landed, gate green. Left In Progress pending the parallel read-only review gate (qa-test-runner + mped-architect) per project norm — orchestrator to run that before flipping to Done.

REVIEW GATE (cycle 243, orchestrator-independent): qa-test-runner GO + mped-architect GO.

qa GATE NUMBERS (re-run, not transcribed from implementer): build OK; clippy clean x2 (forced test-common rebuild, doc_lazy_continuation did NOT fire); test 1269 dev / 1267 release (0 failed, dev-release delta 2 = TASK-0291 debug_assert should_panic); e2e 385/328/0/57/0 UNCHANGED; net test delta vs parent 5a2db4e = exactly +1 (the unique_scratch_dir unit test) verified via worktree at parent — no test dropped/renamed-out.

architect COMPLETENESS: PASS. Independently re-grepped remove_dir_all across nucleus/; every racy pid-less scratch-CREATION site under the profile-independent CARGO_MANIFEST_DIR target/ parent converted; every unchanged site genuinely safe (driver fresh_outdir {tag}-{pid}; e2e/src/tests.rs nondet_tmp pid+nanos+tag, mutex-serialized; e2e run-id harness; Category-D inner-subdir removes now ride a unique stem). Doc-lie fix in pthreads-async/check_frame_emit.rs confirmed real+complete. Helper correctness PASS (atomic across threads + pid across processes; created-once/never-removed sound). Honesty PASS (defense-in-depth caveat carried verbatim, no overclaim).

FOLD-BACK applied in-thread (commit 58ca843), both findings resolved:
- P2 (single-source-of-truth): the 3 original parent-TASK-0426 mp-tcp-bufsync helpers (check_frame_emit, multi_worker_emit, reuse_codegen_emit) carried inline copies of the helper body; retrofitted to consume test_common::unique_scratch_dir. No duplication remains.
- P3 (doc staleness): pthreads-async/multi_worker_codegen.rs emit_02_split_main_rs docstring de-staled (old remove/create-race narrative -> create-once/never-removed).
Fold-back gate: build OK, clippy clean, test 1269 dev / 1267 release (0 failed, unchanged), e2e unaffected.

GOTCHA forward-carried: e2e/src/tests.rs and the driver tests use a DIFFERENT-but-equally-safe convention (pid+nanos wall-clock nonce in temp_dir(), serialized under nondet_env_lock) than the backend helper (pid+AtomicU64 under target/); both eliminate the race class. Do not assume one canonical helper covers the whole repo — temp_dir-rooted paths are intentionally separate from CARGO_MANIFEST_DIR-rooted target/ scratch.

CYCLE RE-AUDIT (2026-06-10) — RESULT: sweep is FULLY INTACT, ZERO new conversions required. No files edited.

DENOMINATOR RE-DERIVATION (structural, per coverage-audit-undercount memory):
- grep -rln remove_dir_all --include=*.rs nucleus/ => 21 files (down from cycle-242 44; the drop is consistent with conversions, since converted sites no longer call remove_dir_all). Of the 21: 7 are e2e_example_0{1..7} (inline pid+nonce idiom, comment-only remove_dir_all mention in docstring), task0341_s3 (inline idiom), 5 driver CAT-A files (fresh_outdir pid-unique in temp_dir), e2e/{src/main,src/tests,src/bin/diff_fuzz,tests/concurrency_stress} (CAT-A / diff_fuzz GC), check_frame_emit/single_worker_emit/pthreads-async check_frame_emit (docstring mention; route through helper), contract.rs (temp_dir pid+nanos), test-common/src/lib.rs (the helper itself).
- Full scratch-signature grep across ALL ownership test trees (backends/*/tests, nucleus-compiler/tests, e2e/tests, backends/*/src/tests): EVERY target/-rooted scratch-creation site routes through test_common::unique_scratch_dir OR the documented inline pid+nonce idiom (nucleus-compiler, no test-common dep). Enumerated 95 *.rs test files via find; classified all.

DISPOSITION BY FILE-CLASS:
- CONVERTED-AND-INTACT (route through test_common::unique_scratch_dir): all 12 backend scratch_dir(name) helpers (mp-tcp-bufsync {check_frame_emit,multi_worker_emit,host_relay_emit,pingpong,rendezvous_emit,reuse_codegen_emit,emit_io_errors,wait_before_push,loop_body_w2w_push}, mp-tcp-event {emit_io_errors,host_relay_emit,loop_body_w2w_push,multi_worker_emit}, mp-tcp-poll {emit_io_errors,multi_worker_emit,pingpong,single_worker_emit,skeleton}, mp-uds-event {emit_io_errors,multi_worker_emit,single_worker_emit,skeleton}, openmp-rs {emit_io_errors,multi_worker_emit,single_worker_emit,skeleton}, pthreads-async {check_frame_emit,emit_io_errors,multi_worker_codegen,skeleton}, pthreads-sync {check_frame_codegen,emit,multi_worker}) + all backend src/tests (mpi-blocking x5, mpi-nonblocking x5, embedded-pattern src/tests + bin_shape + bin_shape_nrf, all via the scratch_leaf-taking helpers). VERIFIED present.
- INLINE-IDIOM-INTACT (pid+nonce, created-once-never-removed; nucleus-compiler has no test-common dep — the cycle-242 recorded trade-off): e2e_example_01..07 + task0341_s3_maxabsdiff_reduction (NEW file post-cycle-242, already adopted the e2e_example_03 idiom correctly — verified lines 74-82).
- ALREADY-CLEAN / NO-SCRATCH (NEW files since cycle-242, IR/parser/codegen-string only, construct no scratch dir): task0341_s3_order_independence, task0341_s6_break_gen_order_independence, task0436_overflow_safe_abs, pthreads-sync/{break_cond_codegen,reuse_marker}, e2e/tests/determinism. Confirmed via empty scratch-signature grep.
- CAT-A SAFE, UNCHANGED (verified, OUTSIDE ownership anyway): driver/tests/{cli_reuse_strict,emit_pn,task0048_05_shim_rejection,task0348_zero_init_invariant,task0371_partition_insufficient_work_reject} use fresh_outdir(tag)=temp_dir/nucleus-{tag}-{pid} (pid-unique; remove_dir_all is benign same-pid pre-create cleanup, dev/release disjoint by pid). nucleus-compiler/tests/{capabilities,contract} use temp_dir pid+counter / pid+nanos (the separate temp_dir convention, RAII Drop cleanup). pthreads-sync/check_frame_codegen.rs cargo_build_and_run builds into target/check-frame-binaries/{leaf} where leaf=project_dir.file_name() already rides the unique {name}-{pid}-{counter} stem (no remove_dir_all). pthreads-sync/emit.rs out_dir=scratch.join(emit) rides the helper stem.
- mp-tcp-common: HAS NO tests/ dir (only src/). N/A.

VERIFICATION (targeted, in dev shell from nucleus/):
- cargo test -p test-common => 6 passed / 0 failed (incl unique_scratch_dir_returns_distinct_existing_paths_per_call).
- cargo test -p nucleus-compiler --test task0341_s3_maxabsdiff_reduction => 1 passed / 0 failed (inline-idiom scratch works end-to-end, bit-identical).
- cargo clippy -p test-common --all-targets -D warnings => BLOCKED by a TRANSIENT doc_lazy_continuation error in nucleus-compiler/src/passes/block_transform.rs:18-19 (a dependency, EXCLUDED from this ownership; line number shifted 19->18 between two runs => another agent is mid-editing that file RIGHT NOW). test-common own code is clippy-clean (the error is purely in the linked nucleus-compiler dep; cargo test compiled nucleus-compiler fine since rustc is laxer on docs than clippy). NOT my error, NOT in my ownership; reported not chased per instructions.

GOTCHA forward-carried: unique_scratch_dir CREATE-ONCE-NEVER-REMOVE means per-run scratch accumulates forever (TASK-0461 saw ~812 pingpong dirs; same in target/e2e-determinism/run-*). This is the INTENDED steady-state of the race-elimination design, not a regression — reclamation must be a separate on-success GC (see TASK-0461 AC#3 forward-note).

CONCLUSION: nothing to convert; cycle-242/243 sweep verified complete and intact; 8 files added since cycle-242 all either adopt the convention or need no scratch. Task work is a clean re-audit confirming no regression + no new sibling.

FINAL (re-audit close): No source edits. The cycle-242/243 repo-wide sweep is verified COMPLETE and INTACT against a freshly re-derived structural denominator. Every target/-rooted test scratch-creation site routes through test_common::unique_scratch_dir (backend tests + backend src/tests) or the documented inline pid+nonce idiom (7 e2e_example + task0341_s3, which have no test-common dep); temp_dir-rooted sites (driver fresh_outdir, capabilities/contract) use the separate pid-unique convention. 8 test files added since cycle-242 either already adopt the convention (task0341_s3_maxabsdiff) or construct no scratch dir (task0341 order-independence x2, task0436, pthreads-sync break_cond/reuse_marker, e2e determinism). Targeted verify: test-common 6/6, nucleus-compiler task0341_s3_maxabsdiff_reduction 1/1. clippy -p test-common is currently BLOCKED only by a transient doc_lazy_continuation in nucleus-compiler/src/passes/block_transform.rs (another agent mid-edit, EXCLUDED ownership, line shifted between runs) — not my change. Forward-note on connect/retry + scratch-rot appended to TASK-0461. Leaving In Progress for the orchestrator wave gate.

Wave-1 attempt (2026-06-10): implementer agent died on an API socket error BEFORE making any code edit (verified: zero test-file modifications in git status; only this note-trail). Re-queued as a wave-2 implementer with the same brief + file ownership.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Repo-wide silent-sibling sweep complete + review-gate GO (qa + architect, both orchestrator-independent). Converted every racy pid-less test scratch-creation site (a fixed leaf under the profile-independent CARGO_MANIFEST_DIR-rooted target/ parent with remove_dir_all+create_dir_all) to a per-call-unique dir, via a new shared test_common::unique_scratch_dir(parent, leaf) -> parent/{leaf}-{pid}-{counter} (created once, never removed). 12 backend scratch_dir(name) helpers + the 3 original parent-TASK-0426 bufsync helpers route through it; 7 nucleus-compiler e2e_example tests inline the same (no test-common dep); Category C/D fixed-leaf inline sites converted. Category-A sites (driver fresh_outdir, e2e nondet_tmp + run-id harness) verified already pid-unique and left unchanged. A pre-existing doc-lie (pthreads-async/check_frame_emit.rs claimed unique dirs over a pid-less leaf) was found and fixed. Defense-in-depth, NOT a reproduction-verified fix (original ENOENT flake unreproducible in 16 runs, carried from TASK-0426): removes the race CLASS by construction. Gate: build OK, clippy clean, test 1269 dev / 1267 release (0 failed), e2e 385/328/0/57/0 unchanged, net +1 test (helper unit test). Commits: a2b3681 (helper), f517ef2 (B), 1cd13d5 (e2e_example), 17c8359 (C/D), 58ca843 (review fold-back: de-dup 3 bufsync helpers + de-stale docstring).
<!-- SECTION:FINAL_SUMMARY:END -->
