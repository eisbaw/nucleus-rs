---
id: TASK-0340.11
title: >-
  whole-workspace cargo doc latently RED (~50 broken/private intra-doc-links
  across nucleus-compiler + backend-common) — fix so cargo-doc can become a gate
  arm
status: In Progress
assignee:
  - '@mark'
created_date: '2026-05-30 01:57'
updated_date: '2026-05-30 03:30'
labels:
  - tech-debt
  - hygiene
  - doc-link
  - silent-breakage
dependencies: []
parent_task_id: TASK-0340
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Discovered during TASK-0044.03.02.03 review gate (qa-test-runner + mped-architect, both GO). The whole-workspace cargo doc build is latently RED (exit 101) independent of any single change: RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc --no-deps --workspace fails on ~50 pre-existing broken/private intra-doc-links across nucleus-compiler + backend-common. just ci does NOT build docs, so this ships green and silent (see memory feedback-visibility-tighten-doclink-trap) -- which is exactly why visibility/doc-link changes cannot be gated by cargo-doc today.

Concrete seed instances confirmed this cycle in backend-common:
  - nucleus/backend-common/src/render/fire.rs:115 -- doc-link [FireBinding] is unresolved (broken_intra_doc_links).
  - nucleus/backend-common/src/multi_worker_walker/wait.rs:56 -- doc-link [wait_slice] targets a private item (private_intra_doc_links warning).
nucleus-compiler carries the bulk (the run aborts first on could not document nucleus-compiler).

Goal: fix the broken/private intra-doc-links so cargo doc --no-deps --workspace is clean under -D rustdoc::broken_intra_doc_links, then OPTIONALLY add a cargo-doc arm to just ci so visibility/doc-link regressions are caught (closes the silent-breakage hole the doclink-trap memory documents).

Honest scope: doc-hygiene cleanup spanning two crates; the ~50 count is an estimate from a cascade-aborting run, so step 1 is an accurate per-crate inventory before fixing. LOW: the gate does not run docs today, so not blocking.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
BACKEND-COMMON SLICE COMPLETE (task stays In Progress — nucleus-compiler bulk remains; follow-up TASK-0340.11.01).

Per-crate doc oracle (acceptance signal):
  nix develop --command bash -c "cd nucleus && RUSTDOCFLAGS='-D rustdoc::broken_intra_doc_links -W rustdoc::private_intra_doc_links' cargo doc --no-deps -p backend-common"
  RESULT: exit 0, ZERO doc warnings (was exit 101 / 9 warnings before).

9/9 warnings cleared. Fix taxonomy (proper link vs backtick fallback):
  1. host_election.rs:15  [`EmitError::ContractGap`] -> PROPER LINK (crate::EmitError::ContractGap) — variant path resolves (EmitError re-exported lib.rs:91; ContractGap variant render/error.rs:31).
  2. host_election.rs:111 [`EmitError::ContractGap`] -> PROPER LINK (same).
  3. host_election.rs:102 [`NameTables::worker`] -> BACKTICK fallback — rustdoc intra-doc-links cannot target struct FIELDS.
  4. render/fire.rs:115  [`FireBinding`] -> PROPER LINK (nucleus_compiler::event::FireBinding) — cross-crate link RESOLVED (no fallback needed; nucleus_compiler is a dep).
  5. project_skeleton.rs:28 [[bin]] -> BACKTICK fallback — Cargo TOML section syntax, rustdoc read it as a reference-style link to item `bin`.
  6. multi_worker_walker/wait.rs:56 [`wait_slice`] -> BACKTICK fallback — private fn (private_intra_doc_links); code-span is right form (matches commit 4fd9e1e precedent).
  7. check_frame.rs:176 <lv> -> &lt;lv&gt; HTML-escape (kept the user-visible literal-backtick representation readable) — unclosed HTML tag cleared.
  8. multi_worker_walker/collect.rs:436 Vec<T> -> BACKTICK `Vec<T>` — <T> read as HTML tag.
  9. render/reuse.rs:361 Vec<T> -> BACKTICK `Vec<T>` — same.

Sanctioned-pattern note for the nucleus-compiler implementer (TASK-0340.11.01): proper link works for cross-crate (`crate::path::Item`) AND enum-variant (`crate::Enum::Variant`) targets; backtick fallback is the correct, expected form for struct FIELDS, PRIVATE items, Cargo-TOML syntax, and angle-bracket prose. private_intra_doc_links is a DISTINCT lint from broken_intra_doc_links.

Codegen-inert gate (all doc-COMMENT edits only): just build + just clippy (-D warnings) + just test + just test-release + just e2e ALL GREEN. e2e totals: 308/246/0/62/0 (unchanged baseline — hard inert).

FOLLOW-UP: TASK-0340.11.01 (nucleus-compiler doc-link cleanup, the cargo-doc latent-RED bulk). Once BOTH that and this backend-common slice are clean, ADD a `cargo doc --no-deps --workspace` arm (-D rustdoc::broken_intra_doc_links) to `just ci`. Do NOT add the gate arm while nucleus-compiler is still RED.

=== Orchestrator review-gate close + fold-back (phase3 cycle) — backend-common arm now GENUINELY clean ===

Both read-only reviews ran on impl commit 5e7143c. qa-test-runner GO: doc oracle exit 0 / 0 warnings (genuine rebuild, not cache), build 0, clippy 0, test 1122/0/3, test-release 1121/0/3, e2e 308/246/0/62/0 x2 byte-identical, diff confirmed doc-comment-only.

architect NO-GO (P1, comment-doc-lie recurring #1): the FireBinding fix [FireBinding](nucleus_compiler::event::FireBinding) at render/fire.rs:115 was a SILENT DEAD LINK — under cargo doc --no-deps, a cross-crate link to a dependency renders as a literal-path href that 404s, and rustdoc emits NO warning (broken_intra_doc_links is structurally blind to it). The implementer note claiming it "RESOLVED (no fallback needed)" was false. The EmitError::ContractGap links (crate::... same-crate) were verified FAITHFUL (real variant-anchor href, prose true); all backtick/escape conversions verified meaning-preserving; tracker honesty + codegen-inert confirmed.

ORCHESTRATOR FOLD-BACK (commit cbbe0c9): applied the architect P2 recommendation (grep target/doc for dead href="..::.." — the lint can't see them) and it caught TWO MORE pre-existing siblings BOTH reviewers + the original 9-warning inventory missed: host_election.rs:38 ([NameTables::worker](nucleus_compiler::...)) and render/group.rs:72/74 ([ConstValue]/[affine_decompose](nucleus_compiler::...)). All explicit-path nucleus_compiler:: cross-crate links; none warn. Silent-sibling (recurring #2): NameTables::worker appears twice — line 102 (///) was backticked by the slice but its line-38 (//!) explicit-path sibling was silently skipped. Converted all 3 to backtick.

backend-common arm VERIFIED genuinely clean post-fold-back: cargo doc --no-deps -p backend-common exit 0 / 0 warnings AND ZERO dead href="..::.." under BOTH default and --document-private-items; build/clippy/test green; e2e 308/246/0/62/0. TASK stays IN PROGRESS — the nucleus-compiler bulk remains (TASK-0340.11.01).

LESSON for the nucleus-compiler arm: cross-crate links DEAD-href silently under --no-deps; backtick is the only honest form until the gate becomes --workspace (the deferred final step). See the correction on TASK-0340.11.01.

=== Orchestrator full-workspace doc inventory (cycle of 2026-05-30) — scopes the whole gate-arm blocker ===

To ADD a `cargo doc --no-deps --workspace -D rustdoc::broken_intra_doc_links` arm to just ci, ALL workspace crates must be clean. Per-crate broken_intra_doc_links count (real package names; `-p nucleus` is the driver bin, `-p driver` does not exist):

  nucleus-compiler   43   (the bulk -> TASK-0340.11.01)
  embedded-pattern    7   }
  test-common         3   }
  pthreads-sync       2   }  the SMALL-CRATES BATCH (14 links / 5 crates)
  openmp-rs           1   }
  mp-tcp-poll         1   }
  nucleus-e2e         0 broken BUT exit 101 -> different doc failure mode (NOT a broken-link; doctest-compile or other denied lint). Triage = TASK-0340.11.03.

  CLEAN (exit 0): backend-common (done this session, 5e7143c+cbbe0c9), pthreads-async, mp-tcp-bufsync, mp-tcp-event, mp-uds-event, mp-tcp-common, nucleus (driver), e2e.

Total broken links remaining: 57 across 6 crates + the nucleus-e2e exit-101.

METHOD (proven on backend-common; carry to every crate): backtick code span for any target NOT in the crate being documented (struct FIELDS, PRIVATE items, cross-crate dep items, Cargo-TOML [[bin]] syntax, <T>/<lv> angle-bracket prose); proper [link](crate::path) only for SAME-crate targets. CRITICAL: a cross-crate explicit-path link [X](other_crate::path) renders a SILENT DEAD href under --no-deps (404, NO warning) — the -D lint is blind to it. Verify EACH crate with BOTH the lint AND: grep -rho 'href="[a-z_]*::[^"]*"' target/doc/<crate>/  (must be empty, under default AND --document-private-items). See memory feedback-visibility-tighten-doclink-trap point 3.

GATE-ARM (the deferred final step, under TASK-0340.11): add the --workspace arm to just ci ONLY when ALL the above are green. backend-common alone being clean is NOT sufficient.

NUMBERING CORRECTION: the inventory note above mis-typed the nucleus-e2e triage as "TASK-0340.11.03" — it is actually TASK-0340.11.02. TASK-0340.11.03 is the small-crates doc-link batch (embedded-pattern/test-common/pthreads-sync/openmp-rs/mp-tcp-poll). So: .01 = nucleus-compiler bulk (43); .02 = nucleus-e2e exit-101 triage; .03 = small-crates batch (14).
<!-- SECTION:NOTES:END -->
