---
id: TASK-0340.11
title: >-
  whole-workspace cargo doc latently RED (~50 broken/private intra-doc-links
  across nucleus-compiler + backend-common) — fix so cargo-doc can become a gate
  arm
status: To Do
assignee: []
created_date: '2026-05-30 01:57'
labels:
  - tech-debt
  - hygiene
  - doc-link
  - silent-breakage
dependencies: []
parent_task_id: TASK-0340
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Discovered during TASK-0044.03.02.03 review gate (qa-test-runner + mped-architect, both GO). The whole-workspace cargo doc build is latently RED (exit 101) independent of any single change: RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc --no-deps --workspace fails on ~50 pre-existing broken/private intra-doc-links across nucleus-compiler + backend-common. just ci does NOT build docs, so this ships green and silent (see memory feedback-visibility-tighten-doclink-trap) -- which is exactly why visibility/doc-link changes cannot be gated by cargo-doc today.

Concrete seed instances confirmed this cycle in backend-common:
  - nucleus/backend-common/src/render/fire.rs:115 -- doc-link [FireBinding] is unresolved (broken_intra_doc_links).
  - nucleus/backend-common/src/multi_worker_walker/wait.rs:56 -- doc-link [wait_slice] targets a private item (private_intra_doc_links warning).
nucleus-compiler carries the bulk (the run aborts first on could not document nucleus-compiler).

Goal: fix the broken/private intra-doc-links so cargo doc --no-deps --workspace is clean under -D rustdoc::broken_intra_doc_links, then OPTIONALLY add a cargo-doc arm to just ci so visibility/doc-link regressions are caught (closes the silent-breakage hole the doclink-trap memory documents).

Honest scope: doc-hygiene cleanup spanning two crates; the ~50 count is an estimate from a cascade-aborting run, so step 1 is an accurate per-crate inventory before fixing. LOW: the gate does not run docs today, so not blocking.
<!-- SECTION:DESCRIPTION:END -->
