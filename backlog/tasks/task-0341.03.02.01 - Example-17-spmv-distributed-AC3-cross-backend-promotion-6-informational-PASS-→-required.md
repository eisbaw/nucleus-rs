---
id: TASK-0341.03.02.01
title: >-
  Example 17-spmv distributed AC#3 cross-backend promotion (6 informational PASS
  → [[required]])
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-27 18:03'
updated_date: '2026-05-27 19:32'
labels:
  - examples
  - cross-backend
  - M6-follow-up
dependencies: []
parent_task_id: TASK-0341.03.02
priority: medium
---

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Cycle 214 plan (orchestrator-direct, per memory feedback-spawned-agents-refuse-code-edits — implement in-thread, read-only reviewer gate after).

SCOPE: mechanical promotion of 6 informational PASS cells (17-spmv | distributed × {mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}) to [[required]] in nuc-nucleus/e2e-matrix.toml. Mirrors the cycle-211 (17-spmv/naive cross-backend promotion) precedent.

PRECEDENTS:
- TASK-0341.01.02 cycle 205 (15-transpose/naive × 6 backends).
- TASK-0341.02.03 cycle 207 (16-jacobi/naive × 6 backends).
- TASK-0341.03.03 cycle 211 (17-spmv/naive × 6 backends).
- This task is the 4th in that series, for the distributed schedule.

DELIVERABLES:
1. 6 new [[required]] entries below the existing pthreads-sync entry (lines 636-640 pre-edit).
2. Narrative comment block above updated to cite cycle 214 + record the in-Loop-w2w-Push-guard non-firing mechanism (per cycle-212b correction, the mechanism is 'w2w Push/Wait live OUTSIDE Event::Loop bodies', NOT 'no Event::Loop').
3. ≥3 fresh just e2e samples verifying non-flake.
4. Cheap structural gate clean (check-textual-replace-on-codegen, check-include-str-coverage, check-narrative-doc-lie, check-mega-files).

NO RUST CODE CHANGES. NO TEST CHANGES. e2e-matrix.toml-only commit.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Filed as TASK-0341.03.02 AC#3 fold-out, cycle 212 ===

17-spmv | distributed | pthreads-sync landed cycle 212 bit-identical. 6 other tier-1 backends (mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event) PASSed informationally in the cycle-212 e2e run (273/239/0/34/0 x 3 samples, non-flake). This task promotes those 6 cells to [[required]].

## What makes this LIKELY clean

- The 17-spmv algorithm has NO in-Repeat-body w2w transfers (the inner k+j loops are NOT Event::Loop / NOT Repeat — they are straight nested for-loops). The whole-array broadcast of x happens once per execution outside any Repeat — so the TASK-0330 in-Loop w2w Push guard (which blocks 16-jacobi/distributed × mp-tcp-bufsync + mp-tcp-poll) does NOT fire here.
- The data-dependent index lives in the kernel body (`spmv_step` returns `acc + v*x_j iff j == c`), not at the algorithm-surface IndexExpr — so the masked-accumulator pattern is invisible to halo_inference / partition_inference / transfer_inject. All compiler passes see only iv-affine indices.
- 3 fresh cycle-212 samples already verified non-flake on all 7 backends.

## Acceptance criteria

1. **6 cells promoted**: e2e-matrix.toml gains 6 new [[required]] entries for 17-spmv | distributed x {mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}, milestone=M6.
2. **Non-flake re-verification**: at least 3 fresh e2e samples at promotion time (cycle-212's 3 samples count if the promotion lands in the SAME cycle; otherwise re-verify in the promotion cycle).
3. **Pre-cycle baseline + post-cycle update** recorded in the commit message.

Same mechanical pattern as TASK-0341.01.02 (cycle 205), TASK-0341.02.03 (cycle 207), TASK-0341.03.03 (cycle 211). The cycle-212 data already supports the promotion — this task's purpose is to make the matrix obligation explicit.

=== Cycle 212b correction (architect review-gate fold-back P1 — mechanism doc-lie) ===

The "What makes this LIKELY clean" bullet above claims:
- "17-spmv has NO in-Repeat-body w2w transfers (the inner k+j loops are NOT Event::Loop / NOT Repeat — they are straight nested for-loops)"

This is WRONG. 17-spmv's prog.algo.nuc:118-124 has three nested `for` loops, and nucleus-compiler/src/acfg/build.rs constructs ACFGNode::Repeat for EVERY IrStmt::For (event.rs:756 documents Event::Loop mirrors ACFGNode::Repeat one-for-one). So 17-spmv DOES have 3 nested Event::Loops in the emit.

Corrected mechanism: the TASK-0330 in-Loop w2w Push guard fires only on a w2w Push INSIDE an Event::Loop body. In 17-spmv, the w2w Push/Wait for val/col_idx/x/y all live at the TOP LEVEL of each worker's event list (visible as `w0_slot_*.wait()` calls BEFORE the `for i ...` line in /tmp/spmv-dist/src/main.rs), OUTSIDE the Loop nests; only Fire events live inside the Loops. 16-jacobi/distributed by contrast has w2w Pushes INSIDE the outer `for t : 0..ITERS+1` Repeat body, which is what trips the guard.

So the prediction "LIKELY clean" remains correct — every backend already PASSed empirically in cycle 212's e2e — but the mechanism is "no w2w Push inside an Event::Loop body" not "no Event::Loop at all".

See TASK-0341.03.02 final-summary cycle-212b addendum for the full lessons-learned writeup; this note is a narrower correction so the cross-backend promoter's onboarding doesn't inherit the wrong mechanism story.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 214 closure (orchestrator-direct + cycle-214b architect review-gate fold-back P2.1 sample-count narrative correction).

OUTCOME:
- 6 cells promoted from informational to [[required]]: 17-spmv | distributed × {mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}, milestone=M6.
- pthreads-sync entry (cycle 212) unchanged. All 7 17-spmv | distributed cells now display * (required marker) in just e2e output.
- Non-flake verified: 7 e2e samples total (3 cycle-212 informational + 4 cycle-214 = 1 pre-promotion baseline + 3 post-promotion verification), every sample 273/239/0/34/0.
- Cheap structural gate clean: check-textual-replace-on-codegen + check-include-str-coverage + check-narrative-doc-lie + check-mega-files all OK.
- Zero Rust delta; test counts unchanged from cycle 213b (1019/0/3 per QA verification).

CROSS-BACKEND MATRIX STATE POST-CYCLE-214:
- 15-transpose, 16-jacobi, 17-spmv naive AND distributed schedules now all [[required]] on all 7 tier-1 backends (where applicable). Two cells remain honest-BLOCKED via [[skip]]: 16-jacobi | distributed × {mp-tcp-bufsync, mp-tcp-poll} on TASK-0330 (in-Loop w2w Push guard).

CYCLE-214b REVIEW-GATE FOLD-BACK:
- Architect P2.1: narrative comment at e2e-matrix.toml:639-641 originally said '4 fresh e2e samples (3 cycle-212 + 1 cycle-214 baseline)' which undercounted the cycle-214 contribution by 3 (the 3 post-promotion verification samples). Corrected to '7 e2e samples total (3 cycle-212 informational + 4 cycle-214 = 1 pre-promotion baseline + 3 post-promotion verification)'. Same defect class as cycle 211b sample-count arithmetic doc-lie.
- Architect P2.2 (mention-only, no tree edit): commit body c46373a used present-tense 'ACs ticked' before the actual tick. Process note: future cycles should use future-tense for ACs being closed AT THE TIME the gate completes, not the implementation commit.
- Architect P3.1: AC-backfill on a pre-cycle To Do task is legitimate (NOT feedback-ac-rewrite-on-done-task).
- Architect P3.2: mechanism narrative ('TASK-0330 guard does not fire because w2w Push/Wait OUTSIDE Event::Loop bodies') empirically verified by rebuilding pthreads-sync emit (architect audited /tmp/spmv-dist-audit-pthreads-sync/src/main.rs; per-worker w0_slot_*.wait() at lines 63-65 above the for-i loop on line 66, only Fire events inside).
- Architect P3.3: verbatim-copy doc-lie audit clean (the 17-spmv/naive 'shared single-worker renderer' fact correctly absent from the distributed block).
- Architect P3.4: parent + grandparent AC propagation consistent.

LESSONS FORWARD-CARRIED:
- The sample-count narrative-arithmetic doc-lie pattern (cycle 211b + cycle 214b) is the recurring class: when a cycle has 'baseline + verification samples', the narrative comment must record the TOTAL (including verification), not just the baseline. Forward-carry note: any future cross-backend-promotion cycle must check the cycle-arithmetic in BOTH the commit message and the matrix narrative comment for self-consistency before committing.
<!-- SECTION:FINAL_SUMMARY:END -->

- [x] #1 #1 6 cells promoted: e2e-matrix.toml gains 6 new [[required]] entries for 17-spmv | distributed × {mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event}, milestone=M6
- [x] #2 #2 Non-flake re-verification: ≥3 fresh just e2e samples PASS at the same totals + all 7 17-spmv | distributed cells show * (required) marker
- [x] #3 #3 Cheap structural gate clean: check-textual-replace-on-codegen + check-include-str-coverage + check-narrative-doc-lie + check-mega-files all OK
<!-- AC:END -->
<!-- AC:END -->
