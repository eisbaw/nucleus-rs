---
id: TASK-0451.01
title: >-
  Thesis C1: scaffold paper/ + dissertation outline (flake, justfile, class,
  chapter/front-matter stubs)
status: Done
assignee: []
created_date: '2026-06-05 19:58'
updated_date: '2026-06-05 20:20'
labels:
  - thesis
  - paper
  - scaffold
dependencies: []
parent_task_id: TASK-0451
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Scaffold paper/ as a formal PhD dissertation that builds an (empty-but-compiling) PDF. DELIVERABLES: paper/flake.nix (nix devShell: texlive lualatex+latexmk+biber+pgf/tikz+pgfplots+fonts; pinned nixpkgs; NO verbose shellHook echos per project rule; optionally a buildable 'paper' package). paper/justfile (commented recipes: build = latexmk -lualatex -biber; watch = latexmk -pvc; figures; view; clean — justfile-hygiene: every recipe commented, no one-off bloat). paper/main.tex (memoir or report dissertation class, geometry, \input includes). paper/preamble.tex (fontspec for luatex, tikz/pgfplots, biblatex+biber, hyperref, cleveref, microtype, listings styled for Nuc + Rust). paper/frontmatter/{titlepage,declaration,abstract,acknowledgements}.tex. paper/chapters/01-introduction.tex .. 12-conclusion.tex — each a SKELETON with section headers + 1-2 sentence intent comments (the cohesive outline committed as files). paper/appendices/{A-examples,B-grammar,C-capability-matrix,D-reproducibility}.tex. paper/figures/ (dir + a placeholder TikZ). paper/refs.bib (empty/seed). paper/.gitignore (LaTeX build artefacts). CHAPTER OUTLINE (put as section skeletons): see epic TASK-0451. CONSTRAINT: standalone prose, no TASK/PRD refs. DoD: cd paper && nix develop --command just build produces main.pdf with no LaTeX/biber errors (front matter + empty chapters render); tree clean except intended files; artefacts gitignored. NOTE: paper/ has its own flake/justfile — the Rust workspace just ci is untouched.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
DONE (orchestrator-direct; LaTeX/nix/justfile infra, build-verified by observed run). Scaffold complete: paper/ flake.nix (texlive scheme-full + just, silent shell, flake.lock pinned, nix-build package), justfile (build/watch/figures/view/clean, all commented), main.tex (memoir dissertation class), preamble.tex (LuaTeX fontspec + tikz/pgfplots + biblatex/biber + cleveref + Nuc/Rust listings), front matter (title/declaration/abstract/acknowledgements), 12 chapter + 4 appendix skeletons with section structure + intent comments, refs.bib seed, figures/ placeholder, .gitignore. VERIFIED: nix develop --command just build => exit 0, main.pdf produced (biber resolved, 18 chapters + 59 sections in ToC, no final-pass LaTeX errors). Hygiene: no shellHook echo; every justfile recipe commented; build artifacts gitignored; flake.lock tracked. Commit on master. The transient first-pass "undefined references" warning is the normal multi-pass ToC/hyperref artifact (zero specific undefined ref/cite on the final pass). Content cycles next: TASK-0451.02 (intro/background/prior-art) is unblocked.
<!-- SECTION:NOTES:END -->
