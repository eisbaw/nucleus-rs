---
id: TASK-0340.11.02
title: >-
  nucleus-e2e crate cargo-doc exit 101 with ZERO broken_intra_doc_links —
  different doc failure mode (triage: doctest-compile or other denied rustdoc
  lint)
status: To Do
assignee: []
created_date: '2026-05-30 03:29'
updated_date: '2026-05-30 04:46'
labels:
  - tech-debt
  - hygiene
  - doc-link
  - triage
dependencies: []
parent_task_id: TASK-0340.11
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Found during the TASK-0340.11 full-workspace doc inventory. RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc --no-deps -p nucleus-e2e exits 101 but grep finds ZERO unresolved-link warnings — so the failure is NOT a broken intra-doc-link. Likely a doctest compile error or a different denied rustdoc lint. Triage: run cargo doc --no-deps -p nucleus-e2e WITHOUT the -D flag to see the real error, then fix. Part of the TASK-0340.11 gate-arm blocker set (the --workspace cargo-doc just-ci arm cannot land until nucleus-e2e is also green). LOW: docs are not gated today.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FORWARD-CARRIED from TASK-0340.11.01 (nucleus-compiler arm, commit f7bc624):
1. DUAL-MODE INVENTORY: the default cargo doc --no-deps -p CRATE oracle UNDERCOUNTS broken links — links inside PRIVATE-item doc-comments surface ONLY under --document-private-items. nucleus-compiler went 0-warnings under default, then --document-private-items revealed 4 MORE broken sites. So triage nucleus-e2e under BOTH modes; the default count is a floor.
2. VACUOUS-GREP GUARD (method correction, still load-bearing): assert rustdoc EXIT==0 BEFORE trusting any dead-href grep — an empty grep on an exit-101 build is VACUOUS (no HTML emitted), not clean.
3. nucleus-e2e is a DIFFERENT failure mode (exit 101 with ZERO broken_intra_doc_links per the .02 title) — likely a doctest-compile failure or a different denied rustdoc lint, NOT the intra-doc-link class that .01/.03/backend-common fixed. Run cargo doc -p nucleus-e2e and read the actual error CATEGORY first; do not assume it is the same link-cleanup shape.
4. GATE-ARM DoD (when ALL crates clean): the just-ci cargo-doc arm DoD must include the post-build dead-href grep (search target/doc/CRATE for any href attribute whose value contains a double-colon; must be empty) — the -D broken_intra_doc_links lint is STRUCTURALLY BLIND to dead cross-crate hrefs. Decide explicitly whether the arm runs --document-private-items (if yes, every crate must be exit-0 in that mode too).
<!-- SECTION:NOTES:END -->
