---
id: TASK-0340.11.02
title: >-
  nucleus-e2e crate cargo-doc exit 101 with ZERO broken_intra_doc_links —
  different doc failure mode (triage: doctest-compile or other denied rustdoc
  lint)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-30 03:29'
updated_date: '2026-05-30 05:18'
labels:
  - tech-debt
  - hygiene
  - doc-link
  - triage
dependencies: []
parent_task_id: TASK-0340.11
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Found during the TASK-0340.11 full-workspace doc inventory. RUSTDOCFLAGS=-D rustdoc::broken_intra_doc_links cargo doc --no-deps -p nucleus-e2e exits 101 but grep finds ZERO unresolved-link warnings — so the failure is NOT a broken intra-doc-link. Likely a doctest compile error or a different denied rustdoc lint. Triage: run cargo doc --no-deps -p nucleus-e2e WITHOUT the -D flag to see the real error, then fix. Part of the TASK-0340.11 gate-arm blocker set (the --workspace cargo-doc just-ci arm cannot land until nucleus-e2e is also green). LOW: docs are not gated today.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FORWARD-CARRIED from TASK-0340.11.01 (nucleus-compiler arm, commit f7bc624):
1. DUAL-MODE INVENTORY: the default cargo doc --no-deps -p CRATE oracle UNDERCOUNTS broken links — links inside PRIVATE-item doc-comments surface ONLY under --document-private-items. nucleus-compiler went 0-warnings under default, then --document-private-items revealed 4 MORE broken sites. So triage nucleus-e2e under BOTH modes; the default count is a floor.
2. VACUOUS-GREP GUARD (method correction, still load-bearing): assert rustdoc EXIT==0 BEFORE trusting any dead-href grep — an empty grep on an exit-101 build is VACUOUS (no HTML emitted), not clean.
3. nucleus-e2e is a DIFFERENT failure mode (exit 101 with ZERO broken_intra_doc_links per the .02 title) — likely a doctest-compile failure or a different denied rustdoc lint, NOT the intra-doc-link class that .01/.03/backend-common fixed. Run cargo doc -p nucleus-e2e and read the actual error CATEGORY first; do not assume it is the same link-cleanup shape.
4. GATE-ARM DoD (when ALL crates clean): the just-ci cargo-doc arm DoD must include the post-build dead-href grep (search target/doc/CRATE for any href attribute whose value contains a double-colon; must be empty) — the -D broken_intra_doc_links lint is STRUCTURALLY BLIND to dead cross-crate hrefs. Decide explicitly whether the arm runs --document-private-items (if yes, every crate must be exit-0 in that mode too).

DONE (phase3 cycle). TRIAGE RESULT: the reported "exit 101 with ZERO broken_intra_doc_links" was NOT an intrinsic e2e failure — it was the cascade-abort from nucleus-compiler failing FIRST in the whole-workspace run (fixed by TASK-0340.11.01). The e2e crate (package name is "e2e", bin "nucleus-e2e"; NOT "nucleus-e2e" as a package) documents at EXIT 0 under both default and --document-private-items. Its only issues were 5 invalid_html_tags warnings (angle-bracket prose). The cargo test --doc exit-101 is just "no library targets" (bin crate) — a non-issue.

FIX (commit 04f6dc5, doc-comment-only): e2e/src/main.rs directory-tree docstring -> fenced text block (renders properly + suppresses HTML parsing; text-annotated so NOT a compiling doctest, confirmed by qa); inline angle-bracket prose backticked. Silent-sibling catch: pthreads-async/src/ring_buffer.rs:146 slot_<id> — the same angle-bracket prose the .03 5-crate batch skipped (it covered embedded-pattern/test-common/pthreads-sync/openmp-rs/mp-tcp-poll, not pthreads-async). After these, the WHOLE workspace (all 14 crates) documents EXIT-0 zero-warning under -D broken_intra_doc_links -D invalid_html_tags.

GATE ARM LANDED (commit 37fb74e + fold-back 103a5c2): added check-doc-links recipe to just ci — the deferred capstone of the TASK-0340.11 epic. Closes the silent-doc-breakage hole.

REVIEW GATE (both GO): qa-test-runner ran FULL just ci end-to-end EXIT 0, all 14 arms pass, test 1122/0/3, test-release 1121/0/3, e2e 308/246/0/62/0, determinism + 3 negative arms pass; check-doc-links negative arm PROVEN to bite (broken link -> exit 101); 04f6dc5 codegen-inert verified independently. mped-architect GO with a P1 comment-doc-lie I had introduced (claimed --workspace structurally resolves cross-crate explicit-path links / grep is a no-op net) — DISPROVEN by injection + independently reproduced: an explicit-path [DataId](nucleus_compiler::event::DataId) sibling link renders a DEAD href under --workspace (lint blind, exit 0) and the grep arm is the PRIMARY catch (recipe exit 1). Folded back in 103a5c2 (comment-only; recipe behavior was correct).
<!-- SECTION:NOTES:END -->
