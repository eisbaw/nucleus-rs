---
id: TASK-0434.01
title: Anchor for-loop-var keyword-collision diagnostic on brace-less truncated input
status: Done
assignee:
  - Mark Ruvald Pedersen
created_date: '2026-06-03 08:07'
updated_date: '2026-06-03 09:54'
labels:
  - compiler
  - frontend
  - diagnostics
  - edge-case
  - cycle-251
dependencies: []
parent_task_id: TASK-0434
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0434 cycle-251 architect P3-2. for_loop_var() (algo/parser.rs) anchors the keyword-collision diagnostic at VAR by take_until(just(quote-brace)) so its error out-reaches the trailing block-brace in chumsky 0.9 furthest-position merge. EDGE CASE: on TRUNCATED input with no opening brace (e.g. `for loop : 0 .. N` at EOF), take_until runs to end-of-input and FAILS before the try_map emits the var-anchored custom message, so the user gets a generic "found end of input, expected brace" at EOF instead of a VAR-anchored message. This is a NARROW diagnostic-quality REGRESSION vs the pre-TASK-0434 code (which anchored at VAR on truncated input because there was no brace to out-reach the ident reject). NOT a crash; correctness UNAFFECTED (the reserved VAR is still rejected, never reaches AST/codegen, in both old and new code). Verified against chumsky 0.9.3 TakeUntil::parse_inner (returns Err at EOF before the mapped closure runs). FIX options: take_until(just(brace).or(end())) then emit at var_span; or a bespoke commit-after-for parser that emits the var error without requiring a brace. Add a negative test for the brace-less case asserting VAR-anchored (line,col). LOW priority — malformed-input diagnostic quality only; caveat documented in for_loop_var() docstring + docs/grammar-algo.md note-4.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CORRECTION (cycle-251): the truncated-input caveat is documented ONLY in docs/grammar-algo.md note-4, NOT in the for_loop_var() docstring — a docstring addition was reverted because parser.rs is at 999/1000 LoC and any net line trips the mega-file fence (TASK-0435 split must land first to restore headroom). When TASK-0434.01 is implemented, add the docstring caveat as part of (or after) the TASK-0435 split.

Cycle-253 DONE (commit b52a571). FIX: for_loop_var() collision path (now in algo/tokens.rs post-TASK-0435) terminator changed take_until(just({)) -> take_until(just({).ignored().or(end())) so a reserved for-loop VAR on truncated brace-less input (for loop : 0 .. N at EOF) anchors at VAR (col 5) instead of degrading to a generic UnexpectedEof (col 18). Braced case byte-identical (take_until non-greedy, stops at first {; end() only matches at true EOF - verified against chumsky 0.9.3 primitive.rs by architect). Safety invariant intact: reserved VAR rejected in BOTH cases, never reaches AST/codegen. New negative test for_loop_var_keyword_collision_anchored_on_truncated_braceless_input (algo_parser.rs) covers Rust-reserved (for loop) + grammar-keyword (for const) truncated cases; PROVEN to bite (col 18/UnexpectedEof when fix reverted). grammar-algo.md note-4 updated caveat->resolved + file-location fixed to algo/tokens.rs. GATES: just test 1290/0 (+1); test-release 0 fail; e2e 420/363/0/57/0 unchanged; clippy/build/check-mega-files (tokens.rs 206 LoC)/check-doc-links green. Review: qa GO + architect GO (no P1/P2). SILENT-SIBLING CLEARED: sched loop-var uses plain ident() with ;-termination (no trailing { to out-reach in the furthest-position merge), so the brace-less gap does NOT exist sched-side; asymmetry already pinned by sched_loop_var_keyword_collision_is_anchored_at_the_variable_token. No sibling follow-up needed.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
for_loop_var keyword-collision diagnostic now anchors at VAR on brace-less truncated input too: take_until terminator gained an end() alternative (just({).ignored().or(end())). Braced case byte-identical; reserved VAR still rejected (safety unaffected). New bite-proven negative test; grammar-algo.md note-4 marked resolved. e2e 420/363/0/57/0 unchanged. qa GO + architect GO; silent-sibling (sched) cleared - no analogous gap.
<!-- SECTION:FINAL_SUMMARY:END -->
