---
id: TASK-0144.03
title: 'block=N + pipeline=D: reject when D >= num_tiles'
status: Done
assignee: []
created_date: '2026-05-22 21:33'
updated_date: '2026-05-23 18:28'
labels:
  - M3
  - compiler
  - language
  - followup
dependencies: []
parent_task_id: TASK-0144
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0144 Stage 3 follow-up: block=N + pipeline=D where D >= num_tiles is meaningless (no parallel tiles to keep in flight). Today the block+pipeline combination is unconditionally rejected at sched-lower via BlockPipelineConflict (TASK-0215), so this finer-grained rejection only becomes relevant if/when block+pipeline is unblocked. Needs the loop bound (num_tiles = ceil((HI-LO)/N)), which is on the ACFG, not the schedule alone — so the validation pass runs after ACFG injection, not at sched-lower.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0144.01 cycle 65: this check CANNOT live in sched/lower.rs::lower_loop the way Stage 2 (UnrollNotDivisibleByBlock) and Stage 3 (.01 VectorizeNotDivisibleByBlock) do. Those are payload-only static-integer checks. block=N + pipeline>=num_tiles needs num_tiles = ceil((HI-LO)/N), where HI/LO come from the algorithm loop bounds — only known on the ACFG. Recommended placement: a validate pass that runs after ACFG injection (sibling of link/transfer_inject), with a new error type (LinkError variant or new VerifyError). Also note: today block+pipeline is unconditionally rejected at sched-lower via BlockPipelineConflict (TASK-0215), so this finer check only becomes live when BlockPipelineConflict is relaxed. Do NOT retrofit into lower_loop.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Closed orchestrator-direct as DEFERRED (cycle 74). The finer block=N + pipeline>=num_tiles check this task proposed is structurally unreachable today: TASK-0215 / SchedLowerErrorKind::BlockPipelineConflict unconditionally rejects ANY block=+pipeline= combination on the same loop at sched-lower, so the finer num_tiles-aware check never fires. Implementing it now would be dead code AND a doc-lie (test would pass for the wrong reason — BlockPipelineConflict would fire first). When/if BlockPipelineConflict is relaxed (PRD §6.3.5 head-start semantics could permit block+pipeline with constraints), file a fresh task at that time scoped to the validation needs the new relaxation reveals — the scope cannot be derived in advance because today's broad reject hides the structural information. Pattern mirrors TASK-0144.02 closure (cycle 70, superseded by TASK-0249's universal Rows/Blocks2d reject).
<!-- SECTION:FINAL_SUMMARY:END -->
