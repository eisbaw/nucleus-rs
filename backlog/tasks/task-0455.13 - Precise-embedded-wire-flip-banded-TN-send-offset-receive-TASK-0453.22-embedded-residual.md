---
id: TASK-0455.13
title: >-
  Precise embedded wire flip: banded [T;N] send + offset receive (TASK-0453.22
  embedded residual)
status: To Do
assignee: []
created_date: '2026-06-10 14:28'
labels:
  - production
  - embedded
  - tier3
dependencies: []
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0453.22 flipped tier-1 + MPI cross-worker payloads to the inferred contiguous span; embedded (tier-3) intentionally stays whole-array — embedded derives WireShape via from_tile(None) so contiguous_span is always None, and its receiver fills the whole [T;N] local (no slice-paste), so narrowing the send without a banded receive would corrupt the wire (verified structurally in the wave-5 review: both production from_tile callers pass literal None, render.rs ~:368/:451).

Work: give the embedded render a banded receive shape (offset write into the [T;N] local) + pass the real pair tile into from_tile, then let contiguous_span narrow the same arms tier-1 narrows. Validate byte-exact in Renode (just renode-multimcu-gate + renode-embedded-nrf-all) — UART traffic shrinks, output bytes must not change. Couples naturally with TASK-0455.02 (embedded async DMA) — sequence them in the same area to avoid double-touching render.rs.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Embedded contiguous-span edges transmit only the span; receiver writes at the banded offset; whole-array preserved for non-contiguous arms
- [ ] #2 Renode gates byte-exact on both MCU families (or honest-stop naming the blocker)
- [ ] #3 No split-brain: the from_tile(None) escape is removed or confined with a documented reason
<!-- AC:END -->
