---
id: TASK-0455.11
title: >-
  Pre-work: move render_single_worker_main from pthreads-sync into
  backend-common (cut the last inter-backend arrow)
status: Done
assignee: []
created_date: '2026-06-09 21:58'
updated_date: '2026-06-10 12:10'
labels:
  - production
  - refactor
  - backend-common
dependencies: []
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
From the 2026-06-09 architecture review (Risk 5). All nine hosted backend crates import render_single_worker_main (and siblings) from the pthreads-sync CRATE — verified across backends/*/src/lib.rs, all except embedded-pattern — making one backend a hidden library hub: touching its renderer perturbs nine crates, and TASK-0453.22/TASK-0455.07 will touch emit paths broadly. The function already consumes only backend-common types (per review), and the standing memory project-backend-common-crate identifies this as the LAST remaining inter-backend arrow (semantic delegation).

Work: relocate into backend-common; remove (do not narrow) any re-export shim — visibility-tighten memory says backend-common pub re-exports are often dead, and narrowing doc-linked modules breaks intra-doc links SILENTLY, so run cargo doc as part of the gate. Byte-identity by emit-oracle: emitted artefacts must be byte-identical pre/post move (git-worktree A/B per the tcp_plan/event_plan precedent).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 No backend crate depends on another backend crate (cargo metadata / Cargo.toml witness recorded)
- [x] #2 Emitted code byte-identical pre/post move on all single-worker required cells (e2e green + A/B spot-diff of at least 2 cells)
- [x] #3 cargo doc clean — no broken intra-doc links from the move
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
A/B emit-oracle move. (a) build release driver, emit 3 single-worker cells to /tmp/task045511-before. (b) read render_single_worker_main + _with_kernels_attr + _with_signature in pthreads-sync/src/lib.rs; verify they depend only on backend-common types (no pthreads-sync-private items); move all three (+ private helpers) into backend-common; update all ~8 importer backends + driver doc comments + pthreads-sync tests; DELETE re-export shims (remove not narrow). (c) rebuild release --workspace, emit same 3 cells to /tmp/task045511-after, diff -r byte-identical. (d) cargo doc --no-deps backend-common+pthreads-sync+touched backends, zero broken links. (e) cargo test per touched crate + clippy --workspace --all-targets. Verify no backend depends on another backend via grep over backends/*/Cargo.toml.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
TASK-0455.11 IMPLEMENTED (leaving In Progress for the wave review gate). Moved the SHARED single-worker straight-line main.rs emitter out of pthreads-sync into a new backend-common module single_worker_main/ (mod.rs + events.rs + break_loop.rs). Scope was LARGER than the cycle-onboarding claim: the prior review said the fn "consumes only backend-common types", but render_main_rs depends on a closed self-recursive cluster — events<->break_loop mutual recursion + walk_fire_outputs/collect_pre_init_data/render_array_init helpers — so ALL of it moved together (verified by grep before moving; multi_worker.rs is independent and untouched). crate:: cross-refs rewritten to super::; backend_common:: imports rewritten to crate::. The three public renderers (render_single_worker_main, _with_kernels_attr, _with_signature) are re-exported from pthreads-sync (pub use backend_common::single_worker_main::{...}) so pthreads-sync own tests (break_cond_codegen, reuse_marker) keep resolving. 8 consuming backends re-pointed src/ imports to backend_common::single_worker_main; mpi-blocking/mpi-nonblocking src/tests.rs likewise. AC#1: cargo-metadata witness shows ZERO production backend->backend edges; the 5 pthreads-sync edges demoted to [dev-dependencies] (their only remaining use is the pthreads_sync::emit / ::NameTables byte-identical TEST oracle), and 3 (mp-uds-event, mpi-blocking, mpi-nonblocking) had pthreads-sync REMOVED entirely (no test use). The 2 pre-existing dev-only oracle edges (mp-tcp-poll->mp-tcp-bufsync, mp-uds-event->mp-tcp-event) remain BY DESIGN. AC#2: A/B emit-oracle diff -r on 3 cells (01/pthreads-sync, 05/mp-tcp-bufsync, 09/mp-tcp-event) BYTE-IDENTICAL before/after, re-verified with freshly-rebuilt release binary. AC#3: cargo doc --no-deps -D rustdoc::broken_intra_doc_links clean on all 10 touched crates (the only doc warnings are PRE-EXISTING private_intra_doc_links to mpi multi_worker, outside my diff). Verification: targeted cargo test all 10 crates 73 ok-lines / 0 failed; clippy --workspace --all-targets clean (fixed 1 doc_lazy_continuation I introduced — a continuation line started with "+"); mega-file + include-str + textual-replace fences all OK. pthreads-sync lib.rs 613->299 LoC. Also fixed stale doc-lies in project_skeleton.rs + backend-common lib.rs + driver dispatch.rs that still named the (now-gone) pthreads_sync::render_single_worker_main arrow. NOT RUN (per wave rules): just e2e / just ci. GOTCHA for reviewer: the [dev-dependencies] oracle edges mean cargo-metadata WITH dev-deps still shows backend->backend; AC#1 is satisfied only for the PRODUCTION dependency graph (kind=normal) — recorded in the metadata witness.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Single-worker emitter cluster (render_main_rs + events/break_loop mutual recursion + pre-init helpers) relocated to backend_common::single_worker_main; 8 backends re-pointed; pthreads-sync production dep demoted-to-dev (5 oracle users) or removed (3); zero production backend->backend edges (cargo metadata witness). Onboarding claim that the fn consumed only backend-common types was WRONG — whole cluster moved; recorded. Emit A/B byte-identical 3 cells x2; cargo doc clean; full-matrix e2e confirmed byte-identity at the wave gate (497/428/0/69/0). Dead variant re-exports removed + 24-site prose sweep in fold-in. Landed 0fdbd9c; architect GO.
<!-- SECTION:FINAL_SUMMARY:END -->
