---
id: TASK-0340.03
title: >-
  Architect cycle-178 P3.2: further split
  nucleus/backend-common/src/render/reuse.rs (778 LoC) into
  reuse/{group,discover,codegen}.rs
status: Done
assignee: []
created_date: '2026-05-26 11:31'
updated_date: '2026-05-30 08:18'
labels:
  - tech-debt
  - hygiene
dependencies: []
parent_task_id: TASK-0340
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect cycle-178 P3 finding (low priority — within the 800-LoC smell threshold AND well under the 1000-LoC check-mega-files fence). reuse.rs has three coherent sub-sections after the cycle-178 split:
- Marker emit (~58 LoC + extensive docstring)
- ReuseRewriteGroup struct (~30 LoC)
- 3 cross-module/private helpers (sidecar_consts_to_resolved, try_reuse_axis_offset, canonicalise_outer_axis) (~115 LoC)
- Discovery walkers (3 functions: discover_reuse_groups + walk_event_for_reuse + walk_arg_for_reuse) (~167 LoC)
- Codegen (render_reuse_buf_decls + render_reuse_per_iter_update + 2 synthetic-slice helpers) (~190 LoC)
- _pub shims (~34 LoC)

Natural further split: reuse/{group,marker,discover,codegen}.rs + reuse/mod.rs (with the pub(super) helpers in a small reuse/common.rs). Defer until M6 codegen growth pushes reuse.rs past the 800 LoC smell threshold or 1000 LoC fence.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0340.10 (embedded-pattern mega-file split, cycle): TWO reusable lessons for any crate-root-file -> child-module split. (1) NO visibility widening needed for items the new child module uses: a child/descendant module can already see ancestor-private (`fn`) items, so do NOT reflexively bump to pub(crate)/pub(super) — the implementer did and the review caught it (kernel_name). Widen ONLY for items the PARENT must call back into (those genuinely need pub(crate)/pub(super)). (2) COMMENT-DOC-LIE TRAP (feedback-verbatim-copy-comment-doc-lie): moved code carries location-deixis ('see lib.rs X', intra-doc links [X], 'above/below') that becomes a LIE the instant X moves in the same commit. Pre-commit: grep the moved files for bare 'lib.rs'/'<oldfile>.rs'/[`fn`] links + above/below deixis and re-point each. Low-churn split trick: re-export moved items at the crate root (use child::*;) so tests.rs + sibling references resolve UNCHANGED (embedded-pattern tests.rs only used public emit/emit_bin, so it needed zero edits).

Forward-carried from TASK-0340.02 (impl b1ec0e7, cycle 30-05): the group split this task anticipated is PARTIALLY DONE. render/group.rs already exists (191 LoC) holding ReuseRewriteGroup + sidecar_consts_to_resolved + try_reuse_axis_offset + canonicalise_outer_axis — extracted to break the ctx<->reuse and fire<->reuse sibling cycles. reuse.rs is now 623 LoC (was 778; -157, more than the ~115 estimate because the moved items carried verbose docstrings). So this task narrows to the remaining {discover, codegen} split of reuse.rs — the group.rs leg is taken. Re-scope AC before picking up: the title says reuse/{group,discover,codegen}.rs but group.rs landed as a flat render/group.rs sibling (not a reuse/ submodule dir) because it had to be a leaf BELOW reuse in the DAG, not a child of it.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 212c — closed as SUBSTANTIALLY-ADDRESSED / premise dissolved (NOT a full 3-way split — stated transparently, not AC-gaming).

The task's premise was an architect cycle-178 P3.2 file-size-hygiene concern: reuse.rs at 778 LoC, split into reuse/{group,discover,codegen}.rs. Current state makes that no longer warranted:
- reuse.rs is now 623 LoC, comfortably UNDER the project's check-mega-files hard threshold (>1000 LoC, per justfile:514 awk '$1 > 1000'). It is neither flagged nor allow-listed, so the hygiene gate is satisfied.
- The group arm of the proposed split ALREADY landed as nucleus/backend-common/src/render/group.rs (191 LoC; reuse.rs imports canonicalise_outer_axis / try_reuse_axis_offset / ReuseRewriteGroup from super::group). So one of the three target modules exists; that extraction is what dropped reuse.rs from 778 to 623 (TASK-0340.02, commit b1ec0e7).
- The residual discover (~167 LoC) and codegen (~190 LoC + _pub shims) concerns remain in reuse.rs, but at 623 LoC total this is below the hygiene bar; a further split would be sub-threshold cosmetic churn on a file shared by 4 tier-1 backends (regression surface > benefit).

No code change this cycle. If reuse.rs later regrows past 1000 LoC, file a fresh split task scoped to that regrowth (do not reopen this one). Orchestrator hygiene per memory feedback-opacity-gate-rot (clean up entries whose underlying premise dissolved).
<!-- SECTION:FINAL_SUMMARY:END -->
