---
id: TASK-0341.02.02.01.02
title: >-
  Hoist w2w field transfers out of partitioned inner loop for
  16-jacobi/distributed (per-worker-variable-count deadlock; the REAL blocker
  under the rank-3 wait_slice guard)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-30 05:53'
updated_date: '2026-05-30 09:23'
labels:
  - compiler
  - transfer_inject
  - M6
  - 16-jacobi
  - deadlock
  - gap-probe-followup
dependencies:
  - TASK-0341.02.02.01.01
  - TASK-0341.02.02.01.03
parent_task_id: TASK-0341.02.02.01
priority: medium
---

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 transfer_inject places w2w field Push/Wait once per t-loop body (outside for y / for x), so each worker performs an EQUAL number of cross-worker exchanges regardless of band size; the current emit places them inside the innermost for x loop with per-worker-variable counts.
- [ ] #2 16-jacobi/distributed x pthreads-sync runs to completion (no deadlock) and emits output.bin bit-identical to reference.bin.
- [ ] #3 Sibling promotion: pthreads-async, mp-tcp-event, openmp-rs, mp-uds-event also pass bit-identical; promote the 5 e2e-matrix [[skip]] entries to [[required]].
- [ ] #4 wait_slice N-D extension (TASK-0341.02.02.01.01) is a co-requisite only IF the hoisted exchange constructs a rank-3 banded (not whole-array) tile; verify which tile shape the hoisted design produces before deciding whether N-D dispatch is needed.
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
ROOT CAUSE (empirically reproduced cycle 211b/this cycle, pthreads-sync):

Built 16-jacobi/distributed x pthreads-sync with the rank-3 wait_slice guard temporarily lifted (guard returned Ok(None) for the rank-3 whole-array tile). The single rank-3 tile constructed for field=DataId(0) is the WHOLE-ARRAY case: bounds=[(t,0..5),(y,0..8),(x,0..8)] vs dims=[5,8,8] (all axes full). It collapses cleanly to the whole-array accumulate arm. Codegen then COMPLETES (no compile error). But the emitted binary DEADLOCKS (timeout 124, no output.bin).

The deadlock is upstream of wait_slice, in transfer_inject xfer PLACEMENT:

  for t in 0..5 {
    bar_1.wait();
    for y in <band> { for x in 1..7 {
      { _tmp = wX_slot.wait(); field accumulate }  // recv from EACH peer
      field[...] = jacobi5_or_seed(...);
      wX_slot.push(field.clone());                 // send to EACH peer
    } }
    bar_2.wait();
  }

The w2w field Wait+Push are INSIDE the innermost for x loop, so the number of push/wait ops per worker = band_rows * 6 (x-span). Bands are unequal (floor-with-spillover, 6 rows / 4 workers):
  w0: y 1..3 (2 rows) -> 12 exchanges
  w1: y 3..5 (2 rows) -> 12
  w2: y 5..6 (1 row)  -> 6
  w3: y 6..7 (1 row)  -> 6
Slot is a single-element Mutex<Option<T>>+Condvar (push overwrites, no count). After w2 finishes its 6 iterations and reaches bar_2, w0 is still on iteration 7+ of 12, blocked forever on w0_slot_6.wait() (recv from w2) — w2 will never push again. DEADLOCK.

Barriers are NOT the bug: bar_2 is correctly a 4-party (workers-only) barrier and all 4 workers wait on it.

WHY wait_slice cannot fix this: the placement/frequency of the w2w transfers is decided in transfer_inject (shared compiler layer), BEFORE backend codegen. All 5 wait_slice-using backends inherit the identical broken event structure. No wait_slice change (N-D dispatch or otherwise) alters the loop nesting that causes the count mismatch.

SEMANTIC NOTE: even fixing the count mismatch requires hoisting the exchange to once-per-t (after all workers finish step t, before t+1) so the whole-array accumulate reassembles field[t] coherently. Per-(y,x) whole-array exchange is also a frequency bug, not just a count bug. This is the TASK-0126-class "ACFG-driven xfer placement" work.

RELATION TO TASK-0341.02.02.01.01 (the wait_slice task): that task is NOT the operative blocker. Its rank-3 guard is merely the FIRST error hit; lifting it reveals this deadlock. For 16-jacobi the only rank-3 tile is whole-array (Ok(None)), so the banded N-D nested-loop path is never exercised. Landing a speculative N-D wait_slice extension would add untested codegen surface that promotes ZERO cells. Left TASK-0341.02.02.01.01 In Progress / honest-BLOCKED on THIS task.

RELATION TO TASK-0330 / TASK-0329.01: same in-Loop-w2w-Push family that already blocks mp-tcp-bufsync/poll on this very example, but here it manifests as a runtime deadlock (5 wait_slice backends) rather than a defensive compile guard (bufsync/poll host-relay collector).

=== Cycle 211b orchestrator: root cause INDEPENDENTLY VERIFIED ===

The architect review-gate (read-only) independently reproduced the deadlock (pthreads-sync, exit 124, threads on Slot Condvar) and dumped all tile shapes (48/48 rank-3 tiles whole-array; zero banded). So this tasks root-cause analysis is empirically confirmed, not just narrated.

Forward-carried facts for whoever picks this up:
- The deadlock arithmetic re-derived from the actual emit: w0 runs for y in 1..3 x for x in 1..7 = 12 iters, pushing/waiting w2 12x each; w2 runs for y in 5..6 x 6 = 6 iters, pushing w0 only 6x; w0 blocks on its 7th wait. The per-(y,x) whole-array field.clone()+wrapping_add is ALSO a frequency bug (each element exchanged 12x), so equalizing counts alone is insufficient -- the exchange must hoist to once-per-t.
- AC#4 hedge is CORRECT (architect confirmed): a once-per-t whole-array hoist will very likely keep the rank-3 tile whole-array (still Ok(None)), making N-D wait_slice (.01) unnecessary; but that depends on the unwritten hoist design, so verify the tile shape the hoist produces before deciding whether .01 is co-required.
- Same in-Loop-w2w family as TASK-0330 / TASK-0329.01 (bufsync/poll). The apply_host_data_relay_inject pass (mp-tcp-event, cycle 163) is the closest existing remediation pattern -- but in-Repeat-body scope only; see memory project-cross-backend-differential + project-mp-tcp-event-vs-bufsync-safety-profile.
- The cycle-211b doc-lie fold-back (commit 5ee22a5) already corrected the wait.rs guard comment + the e2e-matrix skip reasons to point here; the pointers you read are now accurate.

=== IMPLEMENTATION PLAN (this cycle) ===

STEP 1 (DONE): Reproduced deadlock empirically. Temp-lifted the wait.rs rank-3 guard (env-gated NUC_TEMP_REPRO_0341 dump + whole-array Ok(None) collapse), built 16-jacobi/distributed x pthreads-sync, compiled+ran the emitted binary: timeout 124, no output.bin. CONFIRMED. The temp dump printed the single rank-3 field tile: bounds=[(IterVar(2),0..5),(IterVar(4),0..8),(IterVar(3),0..8)] vs ty.dims=[5,8,8] => WHOLE-ARRAY (every axis full). Matches cycle-211b 48/48 finding. Emitted main.rs confirms the w2w field Wait/Push (recv = whole-array wrapping_add accumulate over 320 elems; send = field.clone()) sit INSIDE for x inside for y inside for t. Reverted the temp change.

STEP 2 (design): The hoist target is hoist_invariant_waits. Currently a cross-worker Wait whose data is produced inside a Repeat STAYS inside (per-iteration rendezvous, lines 1425-1438). For 16-jacobi field is produced band-wise inside for y/for x so it stays innermost => deadlock (per-(y,x) exchange, unequal band counts).

ROOT FIX: a cross-worker Wait whose data is produced inside a PARTITION-IV loop (for y), where that partition-iv loop is itself nested inside an OUTER (non-partition) Repeat (for t), must hoist OUT of the partition spatial loops and land in the outer Repeat (for t) body, AFTER the band-compute. This fires the whole-array accumulate ONCE per t on every worker (equal counts => no deadlock), and is semantically correct: each worker writes its band of field[t], the accumulate reconstructs whole field[t] on all workers before t+1 reads it.

GATE (must NOT regress 05/06/07/08 distributed): the new hoist fires ONLY when (a) the Wait is cross-worker w2w (src and dst both compute workers, not host), AND (b) the data is produced inside a partition-iv loop, AND (c) that partition-iv loop is nested inside an outer non-partition Repeat. 05-stencil/distributed has partition=rows at TOP LEVEL (no enclosing Repeat) + host<->worker xfers only (no w2w cross-iteration field) => gate (c) false => unchanged.

STEP 3: AC#4 verify the hoisted tile shape (expected whole-array => Ok(None), no N-D wait_slice needed). STEP 4: promote 5 e2e skips. Full gate before commit.

=== PROGRESS cycle 212 ===

Implemented the Wait hoist in hoist_invariant_waits (PartitionHoistCtx + should_hoist_w2w_out_of_partition_band gate, keyed on body_in_partition_band at the Repeat arm) AND deferred the innermost Push splice for w2w partition-band exchanges (splice_pushes_for_waits + enclosing_tile_in_partition_band), so splice_pushes_global re-places the Push after the for y band loop. AND refined the wait.rs rank-3 guard: a whole-array rank-N tile now collapses to Ok(None) ABOVE the BANDED rank-3 reject (the reject now only fires for a genuinely banded rank>=3 tile).

EMIT now (pthreads-sync): recv x3 at TOP of for t body, compute band (for y/for x), send x3 AFTER for y, then bar_2. Equal counts (3 recv + 3 send per t per worker). BUT STILL DEADLOCKS (exit 124).

ROOT CAUSE of residual deadlock: ORDERING. The recv (Wait) lands BEFORE the band compute (top of for t body), the send (Push) AFTER. Every worker does 3 blocking recvs before any send => rendezvous deadlock (Slot is single-element, no buffer; w0 recv(w1) blocks waiting w1.send, w1 recv(w0) blocks waiting w0.send). The exchange must be SEND-then-RECV after the band compute, not RECV(top)-then-SEND(after band).

AC#4 FINDING (verified empirically, temp diag dump): the hoisted exchange tile is bounds=[(IterVar(2),0..5),(IterVar(4),0..8),(IterVar(3),0..8)] vs dims=[5,8,8] => WHOLE-ARRAY. So TASK-0341.02.02.01.01 (N-D wait_slice) is NOT co-required. The guard refinement (whole-array rank-N => Ok(None)) is the right + complete fix for the tile-shape layer.

NEXT: need recv placed AFTER the send, both after the band compute. The standard Wait-before-consumer model places the recv at top-of-body. Re-examining whether the whole-array-accumulate-exchange-once-per-t model is even reproducible bit-identically vs the naive reference, OR whether a different placement (send-then-recv after band) is required. Reasoning before forcing a fix.

=== CRITICAL FINDING cycle 212: whole-array-accumulate model is SEMANTICALLY WRONG for 16-jacobi (xN double-count) ===

After landing the FULL placement hoist (Wait+Push to for t body, send-then-recv order, deadlock-free), the emitted pthreads-sync binary RUNS TO COMPLETION (exit 0, no deadlock) but produces WRONG VALUES: output = EXACTLY 4x reference (4 = worker count). Empirical proof: ref[1,1]=14, out[1,1]=56=4*14; ref[2,2]=48, out=192=4*48; every cell is x4.

ROOT CAUSE of the value error: the whole-array ACCUMULATE (wrapping_add over all 320 elems, from the same-set-elision fan-out + collect_accumulate_waits classifying whole-array tiles as accumulate, TASK-0343) is only correct for DISJOINT-partition single-pass fan-in (05-stencil/distributed: each worker writes its band ONCE, zeros elsewhere, sum reconstructs). For 16-jacobi the field array is CUMULATIVE across t: after the t=0 exchange ALL workers share field[0]; at t=1 each worker already HAS field[0..t-1] (shared) PLUS its new band of field[t]. Summing whole local fields DOUBLE-COUNTS the shared history field[0..t-1] across all N workers => xN. The orchestrator AC-analysis premise (each worker populates only its band/zeros-elsewhere, sum reconstructs) HOLDS ONLY for a single pass, NOT for the cross-iteration shared-state shape.

CORRECT model: each worker must exchange ONLY its NEWLY-COMPUTED BAND ROWS of field[t] via a COPY (slice-paste, OVERWRITE), NOT a whole-array accumulate. That is a BANDED rank-3 tile (bounds carry the y-band, x full, t...) + COPY combine.

CONSEQUENCE for AC#4 (DEFINITIVE, empirically grounded, supersedes the cycle-211b hedge): the hoist+correct-semantics yields a BANDED (non-whole-array) rank-3 tile. Therefore TASK-0341.02.02.01.01 (N-D wait_slice) IS co-required. Per this task brief AC#4: report BLOCKED, do NOT bolt on speculative N-D code. ADDITIONALLY a SECOND prerequisite surfaced: the combine must be COPY (slice-paste), not accumulate (wrapping_add) -- collect_accumulate_waits/render_wait_assign currently force accumulate for whole-array, and the banded model needs copy. Both are needed for value-correctness.

DECISION: the placement hoist + whole-array guard refinement are built on the FALSE whole-array-accumulate premise; landing them makes 16-jacobi run-but-wrong (worse than honest deadlock for a [[skip]] cell, and the guard refinement would let a wrong model through). Reverting to keep tree honest; re-filing the corrected design (banded slice-paste + copy combine + N-D wait_slice) onto .01. AC#1/#2/#3 NOT met; AC#4 = BLOCKED with the concrete banded-tile + copy-combine finding.

=== CYCLE 212 OUTCOME: honest-BLOCKED (deeper than the placement-hoist brief assumed) ===

Per-AC status:
- AC#1 (place w2w field Push/Wait once per for t body, equal counts): the PLACEMENT mechanism was PROTOTYPED and WORKS (transfer_inject hoist + send-then-recv after band; emit verified: compute band -> push x3 -> wait+accumulate x3 -> bar_2 per t; equal 3+3 per worker; deadlock GONE, exit 0). NOT LANDED — see AC#4.
- AC#2 (bit-identical to reference.bin): NOT MET. Runs to completion but output == 4x reference (xN double-count). The whole-array-accumulate VALUE MODEL is wrong for this cross-iteration shape.
- AC#3 (sibling promotion + e2e [[skip]]->[[required]]): NOT MET. Left all 5 [[skip]] entries as-is. e2e baseline UNCHANGED (308/246/0/62/0) — all source reverted.
- AC#4 (tile shape / .01 co-requisite): RESOLVED = BANDED. Under the CORRECT (value-right) banded-copy model the tile is BANDED rank-3, so TASK-0341.02.02.01.01 (N-D wait_slice) IS co-required. PLUS a new prerequisite: copy-not-accumulate combine (filed TASK-0341.02.02.01.03). The cycle-211b "very likely whole-array / .01 unnecessary" hedge is now DISPROVEN by running the emit.

WHAT I LANDED: nothing in source (all reverted). Tree byte-identical to 2c1cf44 + this tasks tracker notes. The prototype hoist design is documented here for the combined .01+.02+.03 cycle.

WHY NOT FORCE A FIX: landing the placement hoist alone yields run-but-xN-wrong, worse than an honest deadlock for a [[skip]] cell, and would require the whole-array guard refinement that masks the wrong model. Per the no-workaround standard, a precise BLOCKED with the corrected design is the honest outcome.

REJECTED APPROACHES: (a) whole-array accumulate once-per-t — xN double-count, the orchestrator AC premise was false for cumulative cross-iteration state. (b) whole-array-rank-N => Ok(None) guard refinement in wait.rs — correct in isolation but only matters on the (wrong) 16-jacobi path; reverted to avoid untested surface that masks the wrong model.

NEXT (combined cycle): land .03 (copy-not-accumulate classification for cross-iteration partition-band exchange) + .01 (N-D banded wait_slice) + .02 (placement hoist, prototype above). All three together make 16-jacobi/distributed bit-identical. The placement-hoist prototype (PartitionHoistCtx + should_hoist_w2w_out_of_partition_band gated on body_in_partition_band; splice_pushes_for_waits Push-deferral via enclosing_tile_in_partition_band; exchange_after recv placement after the band loop) is the reusable design.

=== Cycle 212 ORCHESTRATOR review-gate close ===

Outcome: honest-BLOCKED (correct, accepted). One read-only architect review run (qa-test-runner code gate SKIPPED — justified: this cycle landed ZERO source, git diff vs 2c1cf44 is tracker-md-only, so the e2e baseline 308/246/0/62/0 is provably unchanged by construction; a full e2e re-run would be theater). Architect verdict: NO-GO on the cycle's deliverable (the rewired task graph), with the CORE conclusions independently verified as sound:
  - accumulate-is-wrong / copy-is-right: VERIFIED (architect reproduced the jacobi reference in Python + simulated 4 combine models).
  - AC#4 = BANDED is FORCED: VERIFIED (whole-array copy provably fails — only one band survives).
  - tree revert clean: VERIFIED (no .rs/.toml/Cargo.lock delta).

The NO-GO was on two correctable tracker defects, BOTH now fixed in-thread by the orchestrator:
  P1-1 dependency CYCLE .01->.02->.01 (unschedulable): repaired by repointing .01 to its Done precursor TASK-0294; cluster is now the DAG .02->{.01,.03}, .03->.01, .01 (source).
  P1-2/P1-3 mechanism mis-attribution + MISSING host-gather prerequisite: the xN on the EXTRACTED output enters at the workers->host GATHER (accumulate of identical full field[ITERS] slices), NOT the w2w history re-summation the implementer's note named. A combined-cycle implementer fixing only the w2w exchange would STILL get xN. .03 AC#1 now explicitly covers BOTH the w2w exchange AND the host gather; .01 carries the write-band-vs-halo + host-gather notes.

This task stays In Progress, BLOCKED on {.01, .03}. The placement-hoist prototype design in the notes above is the reusable .02 deliverable; it is value-correct ONLY combined with .01 (N-D banded wait_slice) + .03 (copy combine for w2w AND host gather). No source landed this cycle.

=== CYCLE 213 COMBINED IMPLEMENTATION PLAN (anchor) ===

Empirical dump (NUC_DUMP_EVENTS, pthreads-sync) confirms the prior-cycle analysis EXACTLY:
- name_data: field=DataId(0), result=DataId(1), seed=DataId(2)
- iter_vars: rx=0, ry=1, t=2, x=3, y=4. partition_worker_ranges={IV4(y): w1:1..3, w2:3..5, w3:5..6, w4:6..7}.
- field is indexed dim0=t, dim1=y, dim2=x. Partition iv (y) is at DIM 1; dim0 (t) is UNPARTITIONED => compute_partition_bounds_with_dim_prefix hits a hole at dim0 and drops to whole-array. THAT is why the w2w field tiles are whole-array [(t,0..5),(y,0..8),(x,0..8)] not banded.
- w2w exchange is ALL-TO-ALL inside for x (IV3) inside for y (IV4) inside for t (IV2): each worker Pushes its band to the 3 peers (seqs) AND Waits from the 3 peers, per (y,x) iteration => unequal counts => deadlock (confirmed).
- HOST GATHER: each worker Push field to w0 with tile [] (whole-array), seq16-19; host w0 does 4 whole-array field Waits => collect_accumulate_waits classifies accumulate => xN on extracted output. Architect P1-2 CONFIRMED.

THREE FIXES (land together):
.03 (classifier, collect.rs): a CUMULATIVE array (read across an OUTER Repeat/for t: field[t-1] read while writing field[t]) must NOT be classified accumulate. Compute cumulative_data set from algo-IR (LHS field written under for t, RHS reads field at a DIFFERENT t index) and pass it into collect_accumulate_waits so cumulative arrays fall through to slice-paste. Regression pin: 05/08 stay accumulate.
.01 (wait.rs): lift rank-3 guard; add WaitSlice::NestedRows for a full-leading + banded-middle + full-inner rank-3 tile; emit per-leading-index contiguous copy_from_slice over the banded middle*inner flat range.
.02 (transfer_inject.rs): (a) construct the BANDED w2w + host-gather tile [(t,0..5 FULL),(y,WRITE-band),(x,0..8 FULL)] keyed on the SENDER write band (NOT halo-expanded); (b) hoist the w2w field Push/Wait out of for y/for x to the for t body, send-then-recv (Push then Wait) AFTER the band compute.

VERIFY: pthreads-sync cmp output.bin==reference.bin FIRST, then promote 4 siblings.

=== CYCLE 213 DONE (all 3 fixes landed; pthreads-sync + 4 siblings bit-identical) ===

LANDED commit cc8c4d7. e2e 308/246/0/62/0 -> 308/251/0/57/0. cmp output.bin==reference.bin verified by direct emit+build+run on pthreads-sync, pthreads-async, openmp-rs, mp-tcp-event, mp-uds-event (all BIT-IDENTICAL, ref[1,1]=14 not 56). Also via just e2e differential (double-emit determinism + reference diff).

AC#1 (place w2w Push/Wait once per for t body, equal counts): MET. hoist_cumulative_w2w_to_repeat_body moves the cumulative-array w2w Push/Wait out of for x/for y to the for t body; emit verified: Sync(1), for y{for x{Fire}}, Push x3 (send), Wait x3 (recv), Sync(2). 3 push + 3 wait per worker per t (equal). SEND-then-RECV (Push before Wait) - the cycle-212 recv-before-send deadlock is gone (exit 0).
AC#2 (bit-identical): MET (cmp passes on pthreads-sync + the model is value-correct, not xN).
AC#3 (sibling promotion + e2e skip->required): MET for the 5 wait_slice backends.
AC#4 (tile shape / .01 co-requisite): RESOLVED = BANDED + co-required, exactly as the cycle-212 finding predicted. The banded tile [(t,0..5),(y,band),(x,0..8)] drives the .01 N-D path.

GOTCHAS / SUBTLETIES for a future cold session:
1. WHY the w2w tile was whole-array pre-fix: compute_partition_bounds_with_dim_prefix drops to whole-array because field dim0=t is UNPARTITIONED (a hole at dim0 before the partitioned dim1=y), so the contiguous-prefix-from-dim-0 filter bails. The new rewrite_cumulative_band_tiles builds the band tile DIRECTLY (full leading/trailing, band on the partition dim) bypassing that filter for cumulative symbols only.
2. HALO SUPPRESSION is automatic: rewrite_cumulative_band_tiles runs AFTER extend_xfer_tiles_for_halo and OVERRIDES the tile to the write band (no halo). field IS halo-inferred on y (jacobi reads field[..][y+-1][..]); the halo rows arrive via the union of all bands the worker receives, NOT via halo-widening the exchange. Do NOT halo-widen the cumulative w2w/gather tile (overlap => double-write).
3. The host GATHER was the operative xN source on the EXTRACTED output (architect P1-2 confirmed): each worker pushed full field[ITERS] (tile []) => host accumulate of 4 identical slices => x4. The SAME band-tile rewrite fixes both w2w AND gather (they share the rewrite path; gather Push src=compute worker is in partition_ranges).
4. Slot push is NON-BLOCKING (overwrite + notify_one) on pthreads-sync; the barrier Sync(2) at end of for-t body separates iterations so the per-(src,dst,seq) slot is drained each t before the next overwrite. This is why send-then-recv works without buffering.
5. The decorative iv on a FULL tile axis is whatever cumulative_band_bounds picks (lowest iv in the dim set); field dim2 shows IterVar(0)=rx because field[ITERS][ry][rx] in the result-extraction loop unions rx into dim2 iv set. Harmless - wait_slice only consults RANGES, not the iv, on a full axis. Deterministic (BTreeSet lowest) so e2e determinism holds.

REJECTED APPROACHES (do not retry):
(a) whole-array accumulate once-per-t (cycle 212) - xN double-count.
(b) modifying compute_partition_bounds_with_dim_prefix to emit band tiles - HIGH regression risk on 05/06/07/08; did the dedicated cumulative pass instead (zero-risk additive, no-op when no cumulative array).
(c) halo-widening the w2w exchange - overlapping bands double-write boundary rows.
<!-- SECTION:NOTES:END -->
