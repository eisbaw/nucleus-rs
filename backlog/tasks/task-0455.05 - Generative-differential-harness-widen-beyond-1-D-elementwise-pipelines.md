---
id: TASK-0455.05
title: 'Generative differential harness: widen beyond 1-D elementwise pipelines'
status: Done
assignee: []
created_date: '2026-06-09 21:30'
updated_date: '2026-06-10 19:31'
labels:
  - production
  - validation
  - fuzz
dependencies: []
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The generative arm (TASK-0453.01) samples only 1-D elementwise integer pipelines; the thesis names extension to multi-dimensional, reduction, and bounded data-dependent shapes as the most direct remaining validation work for the correctness claim (sec:disc-threats). For production use, the guarantee should cover the shapes production schedules will actually use.

Widen the generator, keeping determinism-in-seed and the in-process reference cross-check:
(a) 2-D shapes including stencil-like neighbour reads that force halo inference;
(b) combine-based reductions across all 6 operators, with identity-element edge cases (empty bins, min identity = type max);
(c) multi-worker partitioned decompositions — COORDINATE with TASK-0453.01.01, which already owns multi-worker partition generation plus the per-command timeout (hang->FAIL); one owner per sub-feature, cross-referenced, no duplicate implementation;
(d) bounded for..until single-worker shapes.

Keep the honest framing: the in-process reference is intent-coupled (a second transcription of the same operator definitions), so this strengthens compiler-fault detection, not author-intent independence. Thesis threat paragraph gets updated only when the wider arm actually lands — no pre-claiming.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 2-D and halo-forcing shapes generate, compile, and pass mutual byte-identity across all 7 tier-1 backends
- [x] #2 Reduction shapes cover all 6 combine operators including identity-element edge cases
- [x] #3 Multi-worker partitioned generation has exactly one owner (here or TASK-0453.01.01), cross-referenced in both tasks
- [x] #4 A seeded run of >= 100 synthesised programs is green, or every divergence is filed as its own defect task
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Widen the generative diff-fuzz subclass beyond 1-D elementwise pipelines, batched with TASK-0453.01.01 (same owner).
Family enum over Program shapes, each: deterministic-in-seed, in-process reference (2nd transcription, intent-coupled framing kept in docstring), 7-backend byte-identity.
(a) 2-D stencil-like neighbour reads forcing halo inference (3-pt or 3x3, interior-only writes, skip-with-zero boundary like 05-stencil).
(b) combine-based reductions across all 6 ops (sum/or/xor/min/max/and) w/ identity edge cases (empty bins->identity, min identity=type max). Per-worker partials + tree/host combine like 03-reduction.
(c) multi-worker partition decompositions: OWNED by TASK-0453.01.01 (cross-ref). This task consumes that mechanism for the partitioned families.
(d) bounded for..until single-worker (cap + exact integer predicate), modelled on 21-jacobi but minimal (scalar accumulate to a known fixed point within cap).
Keep honest residual framing; do NOT edit paper/ (deferred). Validate: >=100 seeded programs green (record seed+totals; small-N default + documented knob), or file divergences as defect tasks. Watch determinism.rs 958 LoC + diff_fuzz 993 LoC vs 1000-LoC fence -> sub-split bin if crossed.
Files: nucleus/e2e/src/bin/diff_fuzz.rs (+ split modules if needed), test-common if a helper needs it.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0460 (mega-file split): nucleus/e2e/src/main.rs was carved into 9 sibling modules (manifest/cli/paths/plan/run/determinism/report/baseline + main). If this task adds to the CURATED harness (not just diff_fuzz.rs), note that determinism.rs is the closest to the 1000-LoC mega-file fence at 958 LoC — adding to the determinism/perturbation path may re-cross it; sub-split (det-check vs perturbation injectors) if so. All new e2e harness code now lives in the per-concern module, not main.rs; main.rs is the orchestration facade only. The carve uses `use super::*` in each module + `pub(crate) use <mod>::*` re-exports in main.rs, so new cross-module symbols must be pub(crate).

IMPL landed (e2e/src/bin/diff_fuzz/ split into per-concern + per-family modules to stay under the 1000-LoC fence; old 993-LoC diff_fuzz.rs moved to cruft/). Widened from 1 to 5 families: pipeline1d (existing) + stencil2d (AC#1: 2-D vertical 3-point stencil, y-neighbour reads in the partition axis FORCE halo inference; partition=rows + plain sync transfers -> 7-backend, verified vs 05-stencil shape) + reduction (AC#2: partitioned binned reduction over ALL SIX combine ops sum/or/xor/and/min/max via combine=<op>, identity-aware pre-init; empty-bin + min=i32::MAX/max=i32::MIN/and=all-ones edge cases biased so a wrong 0-init diverges; modelled on 26-bin-min distributed) + partition_workers (multi-compute-worker) + for_until (AC: bounded for..until single-worker, cap + exact integer predicate, modelled on 21-jacobi). AC#3: multi-worker partition generation is OWNED by TASK-0453.01.01 (cross-referenced); this task consumes it for the partitioned families. HONEST RESIDUAL: for_until is pthreads-sync ONLY (curated matrix itself skips 21-jacobi on the other 6; cross-backend break = epic S7) -> per-family backend set; checked for self-consistency+reference on that one backend, NOT 7-way. paper/ NOT edited (thesis update deferred until the arm fully lands per task instruction). AC#4: seeded run in progress (seed 12345 k=120), record totals on completion. 44 unit tests + clippy + mega-files green.

AC#4 validation IN PROGRESS: seeded run seed=12345 k=120, at 49/120 GREEN, 0 FAIL (build-lock contention with 2 concurrent agents slows it). All 5 families exercised+passing: pipeline1d, stencil2d(7/7), reduction(7/7), partition_workers(7/7), for_until(1/1 pthreads-sync). All 6 reduction combine ops confirmed 7-backend byte-identical: sum/or/xor/min/max from the main run + AND from targeted prog-seed 3 (op=and N=34 BINS=11, 7/7) — covers the zero-identity AND non-zero-identity (and=all-ones, min=i32::MAX, max=i32::MIN) pre-init paths. Determinism-in-seed pinned by unit test. Run continuing toward >=100.

AC#4 SATISFIED: seeded run seed=12345 reached 101/120 GREEN, 0 FAIL, 0 DISAGREES, 0 TIMED-OUT (run continued toward 120; >=100 bar met). MAIN-RUN coverage alone: all 5 families (pipeline1d 23, stencil2d 24, reduction 21, partition_workers 19, for_until 15) AND all 6 reduction combine ops 7-backend byte-identical (and 3, max 2, min 4, or 4, sum 3, xor 5). Reproduce: just diff-fuzz 12345 120 (or any prefix k). Per-program reproducer via --prog-seed printed on any failure.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Generative diff-fuzz widened 1 -> 5 families: stencil2d (halo-forcing, partition=rows, 7-backend), reduction (ALL 6 combine ops, identity-aware pre-init, empty-bin + non-zero-identity divergence-biased), partition_workers (owner: TASK-0453.01.01, cross-referenced), for_until (honestly single-backend pthreads-sync mirroring the curated skips — widening trips a structural pin), plus pipeline. 993-LoC bin split into modules (fence green). Seeded validation seed=12345: 108/120 GREEN at report time, 0 fail/disagree/timeout, all families + ops exercised; reproduce: just diff-fuzz 12345 120. Determinism-in-seed + intent-coupled reference preserved (review-verified: no Hash/random/time in generation; min/and identity handling spot-verified). Thesis threat-paragraph update deliberately deferred (no pre-claiming) — folded into TASK-0455.17 scope. Landed b376165 + 38fea10; architect GO; wave gate 3090/0 units + e2e 504/431/0/73/0.
<!-- SECTION:FINAL_SUMMARY:END -->
