---
id: TASK-0455.07
title: >-
  Pre-work: WireShape — single chokepoint for cross-worker payload shape (sender
  slice + receiver _tmp basis)
status: Done
assignee: []
created_date: '2026-06-09 21:57'
updated_date: '2026-06-10 13:07'
labels:
  - production
  - refactor
  - backend-common
dependencies: []
parent_task_id: TASK-0455
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
From the 2026-06-09 mped-architect full-codebase review (Risk 2). The whole-array wire assumption is duplicated across >=5 structurally independent sender emit sites plus one receiver chokepoint that assumes a full-shaped _tmp. Witnesses (line numbers as of review date — re-grep symbols on pickup):
- shared walker push of the whole symbol: backend-common/src/multi_worker_walker/event_walker.rs:462-476
- sync-TCP plan whole-symbol encode: backend-common/src/tcp_plan/events.rs:404-424 + encode.rs:37-53
- event backends (mp-tcp-event, mp-uds-event) own walkers in their multi_worker.rs — same shape
- embedded: backends/embedded-pattern/src/render.rs:378-421 (link_push with size_of_val of the whole array)
- MPI buffered-send sizing budgets full-shaped payloads: backend-common/src/mpi_plan/plan.rs:817-850
- receiver chokepoint: render_wait_assign / wait_slice paste arms index _tmp with DESTINATION offsets (multi_worker_walker/wait.rs:188-265), i.e. wire payload assumed full-shaped.

Work: introduce ONE WireShape derivation in backend-common — computed once per (DataId, SeqTag) from pair tiles + sidecar dims — returning (a) the sender slice expression and (b) the receiver _tmp offset basis; make ALL sender sites and render_wait_assign consume it. This task does NOT change wire behaviour (whole-array stays the shape); it consolidates the decision into one place so TASK-0453.22 flips it once instead of patching 5 sites independently (silent-sibling defence). Follow the parametric-substrate pattern (WirePrimitives / EventTransport / MpiRendezvous).

Adjacent, noted not in scope: host relay hops (apply_host_data_relay_inject) and host_mediation must thread the tile when .22 flips the shape; rid-as-MPI-tag ~32767 portable ceiling (mpi_plan/plan.rs:104-111) approaches at production worker counts given O(W^2) pair fan-out (transfer_inject/mod.rs:71-76).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 WireShape exists in backend-common, computed once per (DataId, SeqTag); all sender sites listed above AND render_wait_assign consume it (grep witness recorded in notes)
- [x] #2 No sender site computes payload shape locally any more — grep proves zero whole-symbol clone/encode holdouts on Push paths
- [x] #3 e2e byte-identity matrix unchanged (whole-array behaviour preserved this task)
- [x] #4 TASK-0453.22 re-scoped in its notes to: flip WireShape whole-array -> inferred region (single-site change + per-backend validation)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
DESIGN DECISION (recorded before edits, per ownership rule): WireShape lives in NEW backend-common/src/multi_worker_walker/wire_shape.rs (keeps wait.rs under the 1000-LoC mega-file fence). Derived ONCE per (DataId,SeqTag) via WireShape::derive(sidecar, pair_tiles, data, seq) from pair tiles + sidecar dims — the exact inputs every walker already holds. NO XferFacts field added: the derivation needs only pair_tiles+sidecar (no compiler-side fact the backends lack), so adding a serde Option<XferWire> now would be the dead field .08 deliberately declined. xfer_facts.rs untouched. WireShape carries the receiver _tmp basis (the existing WaitSlice enum, moved alongside) AND exposes sender-side helpers: sender_value_expr(name) [in-proc .push arg], sender_encode_expr(name,ty) [wire enc], extent_elems(ty)/extent_bytes for sizing. Today every pair resolves to WholeArray => BYTE-IDENTICAL emit (whole-array behaviour preserved per AC#3). render_wait_assign + every sender site consume the one derivation so TASK-0453.22 flips WholeArray->inferred in ONE place. Single-worker renderer (single_worker_main, TASK-0455.11) is OUT OF SCOPE: it rejects Event::Push/Wait with ContractGap (events.rs:459) — no cross-worker push emit there. Host-relay (tcp_plan/relay.rs:123, event_plan) forwards bytes verbatim (shape-agnostic) => out of scope for the value derivation, noted for .22.

COMPLETE (2026-06-10). WireShape landed at backend-common/src/multi_worker_walker/wire_shape.rs (one derivation per (DataId,SeqTag) via WireShape::derive(sidecar, pair_tiles, data, seq) + WireShape::from_tile for the single-tile collect-pass callers). It carries recv_basis (Option<RecvBasis> = the moved WaitSlice enum: None/Flat/Rows/NestedRows) + elems extent, and exposes sender_value_expr / sender_encode_expr / sender_byte_len_expr / extent_elems / extent_bytes / is_whole_array. ALL sender sites converted (grep-proof zero whole-symbol holdouts on Push paths): (1) shared walker event_walker.rs Event::Push -> sender_value_expr (serves pthreads-sync/async, openmp-rs, mp-tcp-event, mp-uds-event, mpi-blocking, mpi-nonblocking); (2) tcp_plan/events.rs Event::Push -> sender_encode_expr (mp-tcp-bufsync, mp-tcp-poll); (3) mpi_plan/plan.rs bsend_bytes -> extent_bytes; (4) embedded-pattern/render.rs Push PIO+DMA + Wait PIO+DMA -> sender_byte_len_expr. RECEIVER chokepoint render_wait_assign (wait.rs) + is_whole_array_recv now consume the SAME WireShape derivation. ORDERING-PRESERVED: empty-tile/type-absent whole-array edge classifies whole WITHOUT erroring (recv_basis empty-return precedes the sidecar lookup, exactly as the old wait_slice) — caught by whole_array_classifier.rs accumulate_classifies_empty_tile test which I re-greened by making elems Option<usize>. tcp_plan mod encode raised to pub(crate). Single-worker (single_worker_main) OUT OF SCOPE (rejects Push/Wait w/ ContractGap). Host-relay (relay.rs) forwards bytes verbatim => shape-agnostic, flagged for .22. Embedded host-IO size_of_val drains (save_output/load_input) are peripheral effects, NOT cross-worker => out of scope. VERIFY: backend-common 18/18 ok; nucleus-compiler ok (boundedness perf test flaky on shared tree, GREEN on rerun, file untouched); 7 tier-1 backends ok; mpi-blocking+mpi-nonblocking ok under .#mpi; embedded 60/60 ok (transport_mode_render pins exact size_of_val emit strings = byte-proof). clippy --workspace --all-targets clean; cargo doc -D broken-links clean (fixed one private-item link). A/B emit-oracle byte-IDENTICAL (diff -r exit 0) for 02-split-add/split x{pthreads-sync,mp-tcp-bufsync,mp-tcp-event,mpi-blocking} + 05-stencil/distributed x pthreads-async + 08-histogram/distributed x openmp-rs, re-verified after full rebuild incl doc edits. TASK-0453.22 re-scoped (AC#4).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
WireShape landed: one derivation (wire_shape.rs, RecvBasis = WaitSlice moved verbatim) consumed by every Push/Wait emit site (shared walker, tcp encode, mpi bsend sizing, embedded link/dma arms) + render_wait_assign. Behaviour-neutral verified: A/B emit-oracle byte-identical 6 cells x 6 backends; embedded emit strings pinned 60/60; empty-tile-before-type-lookup ordering preserved. No XferFacts field (dead-field rule). TASK-0453.22 re-scoped to the three sender helpers + recv basis + the two SO_*BUF max_payload_bytes sites (review P2.1) + host-relay tile threading. bsend Err-swallow hardened with a tripwire in fold-in 9f3434b. Landed 4c12d4b; architect GO; gate 2952/0 + e2e 497/428/0/69/0 + e2e-mpi 14/14.
<!-- SECTION:FINAL_SUMMARY:END -->
