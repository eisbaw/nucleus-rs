---
id: TASK-0044.03.02.01
title: >-
  event_plan + tcp_plan: tighten over-wide walker/encode pub re-exports to
  pub(crate) (TASK-0044.03.02 cycle architect P3-1; silent-sibling pair)
status: Done
assignee:
  - '@claude'
created_date: '2026-05-29 23:53'
updated_date: '2026-05-30 00:36'
labels:
  - tech-debt
  - hygiene
  - visibility
  - silent-sibling
dependencies: []
parent_task_id: TASK-0044.03.02
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect P3-1 on the TASK-0044.03.02 event_plan lift: backend-common/src/event_plan/mod.rs re-exports walkers (pub mod) + collect_push_pairs/collect_w2w_pushes/detect_wait_before_push_hazard/relay_phase_insertion_point/RelayHop and encode::{encode_decode_paths,scalar_width} as pub, and the underlying defs are pub. Orchestrator grep confirmed NONE is consumed outside event_plan/ (the only outside hits are comments/docstrings/test-prose, not use-imports; event_plan::walkers path is referenced nowhere outside the module). So pub -> pub(crate) is safe. SILENT SIBLING: the blueprint backend-common/src/tcp_plan/mod.rs made the IDENTICAL over-widening (collect_barriers_by_tag/collect_w2w_pushes/collect_xfer_data/detect_wait_before_push_hazard/relay_phase_insertion_point/RelayHop + decode_expr/encode_expr/scalar_fn_suffix/scalar_width); grep shows its re-exports likewise unconsumed outside tcp_plan (the pthreads-sync/openmp-rs collect_barriers_by_tag calls resolve to multi_worker_walker, a DIFFERENT module). Fix BOTH modules in one sweep. Compiler is the oracle; relax precisely any item the build rejects. Pure visibility narrowing: build+clippy clean, e2e bit-identical 308/246/0/62/0 (codegen-inert). Fold into the TASK-0340.04 visibility-tightening thread.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Orchestrator-direct (low-risk compiler-verified visibility hygiene; precedent TASK-0340.04.04). Grep-confirmed safe set: backends import only Plan/EventTransport (event_plan) + Plan/WirePrimitives (tcp_plan); the walker/encode re-exports are unconsumed outside each module (only comment/test-prose mentions). Narrow in BOTH mod.rs (silent-sibling pair): pub mod walkers -> pub(crate) mod walkers; pub use encode::{...} -> pub(crate) use; pub use walkers::{...} -> pub(crate) use. KEEP pub: plan::{Plan,(X|Chan)Id} + transport::EventTransport / wire_primitives::WirePrimitives (the Plan API the backends consume). Underlying pub fn defs stay pub (no unreachable_pub lint enforced; effective visibility capped by the pub(crate) module). Compiler is oracle; relax any rejected item. e2e bit-identical (codegen-inert).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
CYCLE CLOSE (orchestrator-direct implementation + parallel review gate, both GO). Commit 8cceada.

IMPLEMENTATION DEVIATION (correct, documented): the plan/AC said "pub use ... -> pub(crate) use", but the re-exports turned out to be DEAD (clippy unused_imports on the first narrowing attempt proved no in-crate consumer reaches them via crate::event_plan::X / crate::tcp_plan::X — internal code uses the walkers::/encode:: module paths directly). So they were REMOVED rather than narrowed (an unused pub(crate) use would just be dead code). pub mod walkers -> pub(crate) mod walkers in BOTH substrates. Underlying pub fn defs left pub (effective visibility capped at pub(crate) by the now-pub(crate) module; unreachable_pub lint not enabled, so no churn needed). The stale "-> pub(crate) use" AC wording is tracker-vs-impl drift, not a defect (commit msg + code comments document the actual approach).

REVIEW GATE:
- qa-test-runner GO: build OK; clippy clean -D warnings (FORCED backend-common recheck; no unused_imports/unreachable_pub/dead_code/private_interfaces); dev 1120/0/3, release 1119/0/3; e2e 308/246/0/62/0 byte-identical x3 (codegen-inert confirmed); check-textual-replace + check-include-str-coverage PASS. Independently verified the dead-re-export claim (zero workspace consumers of the removed symbols).
- mped-architect GO: verified narrowing correctness (re-exports genuinely dead; every real consumer reaches helpers via module paths); resolved the collect_barriers_by_tag name-collision subtlety (backends call walker::collect_barriers_by_tag = multi_worker_walker, a DIFFERENT module, never the removed tcp_plan re-export); silent-sibling completeness (both substrates identical, neither skipped); no doc-lie (all 3 added-comment claims verified); minimal-necessary (no over/under-reach).

P3 (architect, OUT of scope, FILED): two MORE backend-common modules carry the same over-exposure pattern -> filed TASK-0044.03.02.03 (multi_worker_walker/mod.rs + render/mod.rs), WITH the critical caveat that render::fire etc. have cross-crate intra-doc-links that would break under narrowing -> that task must run cargo doc, not just build. Always-file-the-gap silent-sibling discipline: the pattern recurs one level up.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE. Closed architect P3-1 from the TASK-0044.03.02 event_plan lift: the shared substrates over-exposed their walker/encode helpers. Fix (commit 8cceada): pub mod walkers -> pub(crate) mod walkers in BOTH event_plan/mod.rs AND tcp_plan/mod.rs (silent-sibling pair), and the top-level pub use walkers::{...}/pub use encode::{...} re-exports were found DEAD (no in-crate consumer — internal code uses the walkers::/encode:: module paths directly) and REMOVED. Plan/EventTransport/WirePrimitives API stays pub. No substrate internals leak across the backend-common crate boundary. Both reviews GO; e2e 308/246/0/62/0 byte-identical x3 (codegen-inert); dev 1120/0/3 release 1119/0/3 clippy clean. AC#1-3 met (the literal "-> pub(crate) use" AC wording was superseded by the correct "remove dead re-export" realization, documented in notes). Silent-sibling continuation filed as TASK-0044.03.02.03 (multi_worker_walker + render carry the same pattern, with a cargo-doc caveat).
<!-- SECTION:FINAL_SUMMARY:END -->
