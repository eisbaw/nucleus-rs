---
id: TASK-0023.03.03
title: 'e2e harness: --emit-timings under --check-determinism (Stage 1.5)'
status: Done
assignee: []
created_date: '2026-05-22 19:59'
updated_date: '2026-05-22 20:45'
labels:
  - tooling
dependencies: []
parent_task_id: TASK-0023.03
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0023.03 Stage 1 (cycle-54) scoped --emit-timings to RUN mode only; under --check-determinism the flag is currently a loud WARNING and noop.

This task wires --emit-timings into the determinism-mode summary block as well. Output schema:

- [ ] One JSON array element per DetCellResult; key shape mirrors CellResult but status is one of "PASS|FAIL|SKIPPED", with `files_compared: N` on PASS and a `det_mismatch` object on FAIL.
- [ ] Single elapsed-ms field (determinism has a single Duration, not phase_times). Match the existing DetCellResult.elapsed semantics.
- [ ] Top-level document key disambiguates: `{"mode": "determinism", "cells": [...]}` vs Stage 1's `{"mode": "run", "cells": [...]}`. Stage 1 should also emit `"mode": "run"` so consumers can branch.
- [ ] Loud warning currently in main.rs (~line 2848) goes away.

Low priority; the perf-regression headline is RUN-mode wall-clock, which Stage 1 already covers. File this only if a determinism-mode regression ever bites in practice.
<!-- SECTION:DESCRIPTION:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 57 (2026-05-22) — closed. Stage 1.5 of perf-regression infra. The Stage-3 trilogy plus this addendum is now fully complete.

Implementation: nucleus/e2e/src/main.rs only (~+348/-14 LoC). Three new emitter functions mirror cycle-54's pattern (det_cell_result_to_json, render_det_timings_json, write_det_timings_json) with atomic tmp+fsync+rename. Run-mode JSON gains top-level "mode": "run" key for consumer disambiguation (cycle-55 parser explicitly skips unknown keys for forward-compat — verified live).

AC closure (all 4):
- AC#1 (per-DetCellResult JSON, status PASS|FAIL|SKIPPED): CLOSED.
- AC#2 (single elapsed_ms; null on SKIPPED — manifest pre-skip carries no signal; honest non-zero): CLOSED. Module-level doc comment (lines 3014-3019) explicitly documents the null-on-SKIPPED contract.
- AC#3 (mode disambiguator at top): CLOSED. Run-mode emits "mode": "run"; det-mode emits "mode": "determinism". Cycle-55 parser tolerates the new run-mode key (architect-verified parse_baseline_json line 3218-3226 silently skips unknown top-level keys for forward-compat). Two negative-fail tests still pin the loud-failure contract for genuinely malformed input.
- AC#4 (loud warning gone): CLOSED. The cycle-54 emit_timings_warning function and its --check-determinism call site are deleted; replaced by the actual emission.

Emit-call placement honestly documented (lines 4170-4185): BEFORE the NUC_NONDET_TEST zero-perturb early-return so a falsifier regression run still produces a diff-able JSON artifact. Cross-references TASK-0187 AC#2.

Gate (cycle 57): just e2e 88/70/0/18 UNCHANGED; --emit-timings run-mode: "mode": "run" present; --check-determinism --emit-timings: warning gone, "mode": "determinism" present, "wrote 88 cell(s)" confirmation; --baseline against new run-mode JSON parses cleanly (backward-compat); just test 0 FAILED + 2 new tests pass; just clippy clean.

Honest limits: det-mode "perturbed" field harmless under normal runs (only ever true under NUC_NONDET_TEST=1); run-mode and det-mode JSON have DIFFERENT per-cell schemas — consumers MUST branch on top-level "mode"; the shared identity triple + required + status keys are stable across both. Parent-dir fsync absent in both emitters — pre-existing tech-debt not specific to this cycle.

Review-gate (parallel read-only): both qa-test-runner + mped-architect GO. Architect MEDIUM (null-for-SKIPPED doc placement) examined in-thread — the contract IS at module-level (lines 3014-3019) + in-arm comment (lines 3083-3086); the architect's reading was based on incomplete file context. No fix needed.

This closes the 3.5-stage perf-regression infrastructure (TASK-0023.03 parent + .01 + .02 + .03 sub-tasks).
<!-- SECTION:FINAL_SUMMARY:END -->
