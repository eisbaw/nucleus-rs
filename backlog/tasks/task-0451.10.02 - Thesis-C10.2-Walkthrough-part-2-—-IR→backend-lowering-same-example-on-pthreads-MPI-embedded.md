---
id: TASK-0451.10.02
title: >-
  Thesis C10.2: Walkthrough part 2 — IR→backend lowering (same example on
  pthreads/MPI/embedded)
status: Done
assignee: []
created_date: '2026-06-05 22:08'
updated_date: '2026-06-05 22:57'
labels:
  - thesis
  - paper
  - content
  - deep-dive
dependencies:
  - TASK-0451.10.01
parent_task_id: TASK-0451.10
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Write the second half of the compilation-walkthrough chapter + deepen ch7. Take the SAME worked example and its EventList from part 1 and show how the single EventList is lowered onto 2-3 backends: the event contract (Fire/Push/Wait/Barrier/Loop), per-worker projection, and presentation codegen. Contrast how the SAME EventList becomes (a) shared-memory pthreads code, (b) MPI SPMD rank-dispatch code, (c) embedded no_std/UART code -- what each backend renders for the same events and why. Verify against the LIVE backend emit paths: nucleus/backend-common/src/ (multi_worker_walker, render, projection) + nucleus/backends/{pthreads-async,mpi-blocking,embedded-pattern}/ emit code. Forward-ref the lowering figures (.06.02). Duplicate-check vs ch7 -- EXTEND. Standalone prose, no TASK/PRD refs. DoD: chapter part written; lowering code-verified; just build clean exit 0.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle C10.2 landed. Second half of the walkthrough chapter (chapters/07b-compilation-walkthrough.tex): IR->backend lowering of the SAME 05-stencil/distributed per-worker EventList. New sections: One event list three renderings; Shared memory (pthreads); Distributed memory (MPI); The capability boundary; Bare metal (embedded); Why the three agree to the byte. CENTRAL VERIFIED FACT (diff of live codegen): the blur3 compute body + all reuse-buffer index arithmetic are BYTE-FOR-BYTE IDENTICAL between pthreads-async and mpi-nonblocking (shared single-worker emitter); only Push/Wait/Sync differ (Ring/Barrier/thread::spawn vs VecChan/MPI_Ibsend/Mprobe/WorldBar/world.rank()). Capability boundary: the async+buffer=2+notify=event schedule is REJECTED at compile time on mpi-blocking AND embedded-pattern (real diagnostics, capabilities.toml supports_async=false/max_buffer=1/no-event). Embedded (no_std): NucleusShim trait, [i32;256] fixed arrays, blur3 body verbatim, load/save->dma_push/dma_wait, cross-worker->link_push/link_recv, barrier->irq_barrier; emitted under the NAIVE (sync) schedule since async is rejected. Built/inspected pthreads-async + mpi-nonblocking + embedded-pattern generated code live. Review gate: paper-correctness GO (central byte-identity argument sound on all 4 premises; no-cross-worker-reduction caveat added); paper-accuracy NO-GO->GO after fixing 3 codegen-mechanic overclaims (embedded loop is NOT the same loop as hosted-only the kernel BODY is verbatim; "same source text" scoped to the two hosted backends; worker sends FULL array and HOST slices the band) - all 3 re-verified against live codegen; paper-clarity advisory folded (Ibsend deadlock gloss, message-tag/trait/sequence-tag glosses, world.rank gloss, half-open offset gloss, synchronous-schedule introduced). Forward-refs fig:wt-lowering (.06.02) + fig:mpi-spmd. PDF: just build exit 0, 75 pages, zero undefined citations. DoD met. Parent TASK-0451.10 now has both children Done.
<!-- SECTION:NOTES:END -->
