---
id: TASK-0453.21
title: Fundamental limitations register (remains honest limitation - NOT planned)
status: Done
assignee: []
created_date: '2026-06-06 22:57'
updated_date: '2026-06-06 22:57'
labels:
  - rigour
  - thesis
  - honest-limitation
  - wontfix
dependencies: []
parent_task_id: TASK-0453
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
REGISTER of shortcomings triaged as FUNDAMENTAL: inherent trade-offs of the central design decision (static, deterministic firing order). These are NOT defects to fix; the thesis's honesty about them is a STRENGTH. They stay documented limitations and are deliberately NOT given implement/thesis-update pairs.

L1 Restricted algorithm class is narrow (ch10:118-132): the affine/const-bounded/single-assignment/no-data-dependent-control-flow/no-recursion/no-higher-order/no-dynamic-scheduling class is the direct cost of the static firing order (ch10:15-50). FUNDAMENTAL. (Bounded data-dependent iteration is the one addressable slice -> IMPL/THESIS P5.)

L1b Sparse iterative solvers (ch11:108-113): need BOTH data-dependent indexing AND convergence termination; sits entirely outside the model. FUNDAMENTAL destination, not a near-term target.

L2/L5 Greedy non-optimal schedule + descriptive-not-prescriptive check assertions (ch10:134-142,179-186): deliberate scoping - the contribution is separation+soundness, not performance. Prescriptive scheduling (ch11:14-34) needs a target cost model the work deliberately avoids; a wrong model silently picks bad schedules. FUNDAMENTAL-by-scope. (A narrow measured-cost single-parameter solver is conceivable future work but is NOT planned here.)

L3 Soundness scoped to coordination (not value-correctness) and to one net class (ch10:144-159): exact-replay gate is sound precisely BECAUSE the nets have no nondeterministic token routing; it is not a general Petri reachability procedure, and it says nothing about arithmetic correctness (that is the differential rig's job). FUNDAMENTAL boundary. (Gate SCALING is the addressable slice -> IMPL/THESIS P4.)

T3 Negative falsifiers could rot (ch10:268-274): already mitigated by injection-signal self-checks; residual regress ('at some level a test apparatus must be trusted') is FUNDAMENTAL.

T4 Toolchain confound pinned but the pin is itself an assumption (ch10:276-282): Nix content-addressing makes it very strong but not absolute. FUNDAMENTAL residual.

W2 Soundness-gate novelty thin over SDF/marked-graph single-order replay (defence W2): this is a PRIOR-ART FRAMING matter, not an implementation gap - resolved in-text by folding the gate into the IR-unification contribution (handled as a thesis-framing clarification, not an implement/thesis pair). 

GPU/NPU tier (ch11:125-140): a research project in its own right, not one more shim; claiming 'extends to GPUs' unbuilt would be exactly the overclaim the document avoids. NOT planned.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Register exists; each fundamental limitation cross-referenced to its thesis location
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Closed as DEFERRED (honest-limitation register; Done-as-DEFERRED convention). Zero implementation intended by design - these remain documented limitations. Addressable SLICES of otherwise-fundamental items are tracked separately: L1->P5, L3-scaling->P4.
<!-- SECTION:NOTES:END -->
