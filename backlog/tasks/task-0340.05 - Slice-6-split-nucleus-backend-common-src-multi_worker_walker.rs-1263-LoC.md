---
id: TASK-0340.05
title: 'Slice 6: split nucleus/backend-common/src/multi_worker_walker.rs (1263 LoC)'
status: In Progress
assignee:
  - '@claude-implementer'
created_date: '2026-05-26 13:14'
updated_date: '2026-05-26 13:30'
labels:
  - tech-debt
  - hygiene
dependencies: []
parent_task_id: TASK-0340
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0340 AC#2 slice 6: split nucleus/backend-common/src/multi_worker_walker.rs (1263 LoC). Shared spine of all tier-1 backends; splitting now BEFORE M6 codegen amplifies it pays substrate-cleanliness dividends for TASK-0044.01/.02/.03 promotion from SKELETON.

## Proposed seams (already named by section/fn boundaries)

- ctx.rs (~113 LoC): pub struct WalkerCtx + impl (L159-271)
- block_tag.rs (~169 LoC): render_block_tag_loop_header + compute_block_tag_abs_exprs (L272-440)
- event_walker.rs (~447 LoC): render_worker_events + render_worker_events_inner (L441-887)
- wait.rs (~223 LoC): render_wait_assign + wait_slice (L888-1110)
- collect.rs (~143 LoC): collect_xfer_pairs / collect_pair_tiles / collect_worker_rendezvous / collect_barriers_by_tag / collect_pre_init_sets (L1120-end)
- mod.rs: pub use re-exports preserving the existing public surface (per cycle-178 / 179 / 180 pattern)

## Discipline (forward-carried from cycles 178-180)

- ONE atomic commit per file (per AC#2). One file = multi_worker_walker.rs.
- pub(crate) on items consumed across backend crates; pub(super) for cross-submodule but in-crate; private otherwise.
- No doc-claim rewriting — every moved fn/struct/doc-comment preserved VERBATIM, including TASK-NNNN / cycle-NNN narrative anchors (AC#6: no NEW citations introduced, existing ones move verbatim).
- Justfile allow-list updated: REMOVE multi_worker_walker.rs from positive-enumeration list (file now <1000 LoC; staleness direction-B would FAIL LOUD if forgotten).
- Verification gate (orchestrator-verified, not implementer self-certified): just build + clippy + test + test-release + e2e (baseline 112/102/0/10/0 per cycle-180b) + check-mega-files (both directions clean).
- E2E baseline preserved BIT-IDENTICAL: assert totals line matches 112/102/0/10/0 exactly.

## Sibling-lessons forward-carried from previous slices

- Slice 4 (179): Multi-file impl<'a> Plan<'a> needed Rust elision to impl Plan<'_> on sub-module impl blocks when the lifetime is unused in signatures.
- Slice 5 (180): Producer-and-consumer co-location matters (RelayHop placed with producer to avoid circular feel).
- Slice 5 (180): #[allow(unused_imports)] use io as _io guard preserved verbatim if present.
- Slice 5 (180): Existing #[cfg(test)] mod tests blocks moved verbatim; not split further unless they're themselves >800 LoC.
- ALL slices: the existing 'pub use multi_worker_walker::{...}' set at lib.rs must be preserved byte-identical via mod.rs re-exports — zero-churn for callers (other backends consume these names; renaming or path-changing breaks the surface).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 181 — slice 6 implementation plan

Splitting nucleus/backend-common/src/multi_worker_walker.rs (1263 LoC) into a directory of cohesive sub-modules per the brief's proposed seams. Empirical re-verification of seam boundaries (the brief's line ranges were approximate; verified against the actual file):

- ctx.rs (~111 LoC): RendezvousId pub type (L102) + pub struct WalkerCtx (L142-184) + impl WalkerCtx (L186-211). WaitSlice enum is private and only consumed by render_wait_assign + wait_slice — moved to wait.rs instead of ctx.rs.
- block_tag.rs (~189 LoC): render_block_tag_loop_header (L213-303) + compute_block_tag_abs_exprs (L305-399).
- event_walker.rs (~422 LoC): render_worker_events (L401-451) + render_worker_events_inner (L453-863).
- wait.rs (~245 LoC): enum WaitSlice (L128-140, moved down from ctx-side neighborhood) + render_wait_assign (L865-942) + wait_slice (L944-1109).
- collect.rs (~150 LoC): collect_xfer_pairs (L1115-1135) + collect_pair_tiles (L1137-1181) + collect_worker_rendezvous (L1183-1205) + collect_barriers_by_tag (L1207-1225) + collect_pre_init_sets (L1227-1263).
- mod.rs: module //! docstring (verbatim L1-80) + use statements + pub mod sub-decls + pub use re-exports preserving the byte-identical surface consumed by 5 callers (4 production backends + tests).

Public surface to preserve verbatim (grep-verified):
- WalkerCtx (used by pthreads-sync, pthreads-async, mp-tcp-event, 4 backend-common/tests)
- RendezvousId (pthreads-sync, pthreads-async, mp-tcp-event)
- render_worker_events (mp-tcp-event/multi_worker/mod.rs + 3 tests)
- render_wait_assign (mp-tcp-bufsync/plan/events.rs)
- compute_block_tag_abs_exprs (mp-tcp-bufsync/plan/events.rs)
- render_block_tag_loop_header (backend-common/tests/block_tag_loop_header.rs)
- collect_pair_tiles (mp-tcp-bufsync/plan/mod.rs + backend-common/tests/collect_pair_tiles.rs)

Visibility scheme (per cycle-179b refined guidance):
- pub for items lib.rs is part of public surface (the existing pub functions/types stay pub via mod.rs re-export).
- pub(super) for cross-sub-module items that aren't externally consumed (e.g. render_worker_events_inner is private fn — but called only from within event_walker.rs, no uplift needed).
- private otherwise.

Lifetime elision (per cycle-179 lesson): impl<'a> WalkerCtx<'a> if the impl block uses 'a in signatures; impl WalkerCtx<'_> otherwise. WalkerCtx::render_ctx returns RenderCtxPub<'_> (anonymous lifetime tied to &self), so impl WalkerCtx<'_> compiles cleanly.

Justfile: remove nucleus/backend-common/src/multi_worker_walker.rs from the printf allow-list at L464.

One atomic commit at end. Verification gate (orchestrator-self-run inside nix develop): just build + clippy + test + test-release + e2e (TWO samples, expect 112/102/0/10/0) + check-mega-files (both directions OK).

## Cycle 181 — slice 6 LANDED (commit pending)

Split nucleus/backend-common/src/multi_worker_walker.rs (1263 LoC) into multi_worker_walker/{mod,ctx,block_tag,event_walker,wait,collect}.rs. AC#2 now 4/6 (render.rs + mp-tcp-bufsync/lib.rs + mp-tcp-event/multi_worker.rs + multi_worker_walker.rs).

Per-file LoC after split (vs old multi_worker_walker.rs 1263):
- ctx.rs           92  RendezvousId pub type + pub struct WalkerCtx + impl WalkerCtx (render_ctx/worker_name/data_name, all pub(super))
- block_tag.rs    200  render_block_tag_loop_header + compute_block_tag_abs_exprs (both pub)
- event_walker.rs 484  render_worker_events (pub) + render_worker_events_inner (private fn, single-file)
- wait.rs         296  enum WaitSlice (private) + render_wait_assign (pub) + wait_slice (private)
- collect.rs      160  collect_xfer_pairs + collect_pair_tiles + collect_worker_rendezvous + collect_barriers_by_tag + collect_pre_init_sets (all pub)
- mod.rs           95  module //! docstring (verbatim L1-80 of old file) + use mod decls + pub use re-exports

(Total 1327 LoC; +64 vs pre-split 1263 = per-file //! orientation docstrings + use statements + sub-module decls + 5 sub-module use lines in mod.rs. No behaviour change.)

Public surface preserved BYTE-IDENTICAL via mod.rs pub use re-exports:
- WalkerCtx + RendezvousId (consumed by pthreads-sync/multi_worker.rs:88, pthreads-async/multi_worker.rs:56, mp-tcp-event/multi_worker/mod.rs:70, mp-tcp-event/multi_worker/worker_program.rs:18, 4 backend-common/tests/*.rs)
- render_worker_events (consumed by mp-tcp-event + 3 tests)
- render_wait_assign (consumed by mp-tcp-bufsync/plan/events.rs:13)
- compute_block_tag_abs_exprs (consumed by mp-tcp-bufsync/plan/events.rs:147)
- render_block_tag_loop_header (consumed by backend-common/tests/block_tag_loop_header.rs)
- collect_pair_tiles (consumed by mp-tcp-bufsync/plan/mod.rs:114 + backend-common/tests/collect_pair_tiles.rs)
- collect_xfer_pairs, collect_worker_rendezvous, collect_barriers_by_tag, collect_pre_init_sets (kept pub for external+test visibility per pre-split file)

No call sites in production backends or tests change. Verified by clean build + clippy + 969-test pass.

Visibility scheme (cycle-179b refined):
- pub for items reachable from outside the crate via the mod.rs re-export surface (preserved EXACT pre-split set).
- pub(super) for cross-sub-module items not in the public re-export set (3 WalkerCtx impl methods: render_ctx, worker_name, data_name — consumed only by event_walker.rs; uplifted from private fn to pub(super)).
- private (default) for items used only within one sub-module (render_worker_events_inner, wait_slice, enum WaitSlice).

Behaviour-equivalent change list (cycle-179b enumerated-disclosure discipline):
(a) Three WalkerCtx impl methods (render_ctx, worker_name, data_name) visibility uplift fn (private) -> pub(super). Required for event_walker.rs to call them across the sub-module boundary. Bodies byte-identical.
(b) enum WaitSlice MOVED from neighbouring WalkerCtx (was at L128-140 in old file; now at the head of wait.rs since it is only consumed by wait_slice + render_wait_assign). Module-private both before and after — same visibility shape; only the file location changed. Bodies byte-identical.

No other reflows, requalifications, or doc-claim edits. Imports re-grouped per sub-module need ( split between ctx.rs / wait.rs / event_walker.rs / collect.rs along the consumer axis; same for  symbol set).

Lifetime elision (cycle-179 lesson applied): event_walker.rs has no impl block (all functions are free); block_tag.rs has no impl block; ctx.rs uses impl WalkerCtx<'_> (no method signature references 'a — render_ctx returns RenderCtxPub<'_> tied to &self, worker_name/data_name return owned String). All compile clippy-clean.

Allow-list: removed 'nucleus/backend-common/src/multi_worker_walker.rs' from justfile check-mega-files printf list. Allow-list shrinks from 11 to 10 entries. just check-mega-files OK both directions (no new mega-file, no stale entry).

Verification gate (orchestrator-self-run, inside nix develop):
- just build (workspace): clean.
- just clippy (-D warnings --all-targets): clean.
- just test (dev): 969 / 0 / 3.
- just test-release: 968 / 0 / 3 (the -1 is the debug_assert-gated #[should_panic] negative test per TASK-0291).
- just e2e: 112 / 102 / 0 / 10 / 0. TWO independent samples both at the same totals line (non-flake). Matches cycle 180 baseline exactly.
- just check-mega-files: OK both directions.

AC status post-cycle-181:
- AC#1: DONE.
- AC#2: 4/6 files split. Remaining originally-named: acfg.rs (1440), link.rs (1290).
- AC#3: PENDING (slice 9 proptest).
- AC#4: PENDING (slice 10 e2e/main.rs report-formatter carve-out).
- AC#5: DONE; allow-list shrank 11 -> 10.
- AC#6: DONE for this slice. No new TASK-NNNN or cycle-NNN citations introduced; the only added text is per-file //! orientation docstrings on the 5 new sub-module files, each repeating EXISTING TASK anchors that appeared in the pre-split file (TASK-0239, TASK-0181, TASK-0270, TASK-0253, TASK-0117, TASK-0294, TASK-0300, TASK-0018 — all grep-verified present in the source before the split).
- AC#7: PENDING (final cycle when AC#2 + AC#3 close).

Subtleties / gotchas forward-carried (slices 7+):
- Brief's seam line ranges (L159-271, L272-440, etc.) were APPROXIMATE — verified actual ranges before splitting. The seam map was correct at the FUNCTION-name level; line ranges drifted +/- 10 from the brief.
- enum WaitSlice was filed under 'ctx.rs neighbours' in the original file (L128-140, just before WalkerCtx) but is consumed only by wait_slice/render_wait_assign — moved to wait.rs head instead of being awkwardly imported back. Mechanical co-location, not a behaviour change.
- The 3 WalkerCtx impl methods (render_ctx, worker_name, data_name) were private fn in pre-split; uplifted to pub(super) since event_walker.rs calls them across the sub-module boundary. Same pattern as cycle 180's (c) visibility uplifts.
- multi_worker_walker is the LAST shared-substrate file split (backend-common spine done after this cycle). acfg.rs (slice 7) and link.rs (slice 8) are nucleus-compiler IR/pass layer — different shape than backend codegen splits 3-6; the mod.rs+sub-module pattern still applies but the natural seams will be transform-family / IR-layer, not Plan-codegen clusters.
- check-mega-files allow-list now down to 10 entries: 8 out-of-original-audit files (transfer_inject.rs 4726, reuse_inference.rs 1676, sched/lower.rs 1546, halo_inference.rs 1525, algo/lower.rs 1328, host_data_relay_inject.rs 1212, sched/ir.rs 1163, pthreads-async/multi_worker.rs 1048) + 2 remaining originally-named (acfg.rs 1440, link.rs 1290).

Status -> Done at end of this cycle.
<!-- SECTION:NOTES:END -->
