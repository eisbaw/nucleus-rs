---
id: TASK-0455.10
title: >-
  Gate memory: lazy Petri place/transition naming + cut whole-net clones in gate
  passes
status: To Do
assignee: []
created_date: '2026-06-09 21:58'
labels:
  - production
  - gate
  - perf
dependencies: []
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
From the 2026-06-09 architecture review (Risk 1, memory half). Behaviour-neutral memory reduction for the expanded-replay gate — shrinks the NeedsExpansion residual that TASK-0455.01 falls back to, and is worth having even if .01 stalls.

Two findings: (1) the O(firings) blowup constant factor is heap strings — every unrolled transition allocates fresh format!-built names (acfg_to_petri.rs:546-558 ctl places, :429-432 push_seq transitions; String fields on Place/Transition at petri.rs:85-110) that are consumed ONLY in error payloads and DOT output; (2) the gate clones the entire net up to 4-5x per build: derive_firing_order clones (boundedness.rs:335), check_bounded clones (boundedness.rs:182), check_deadlock_free clones (deadlock.rs:230), conflict pass clones for BFS or fallback (net_soundness.rs:530, 630) — peak memory ~4-5x one net.

Work: store structured ids (e.g. (WorkerId, u32)) and format names lazily in error/DOT paths only; replay on borrowed marking state instead of cloned nets where possible. Record before/after RSS on the largest distributed matmul N that currently compiles.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Place/Transition carry no eagerly-built String names; DOT output and error messages byte-identical or improved-with-updated-pins
- [ ] #2 Gate passes clone the net at most once per build (audit recorded in notes)
- [ ] #3 Measured RSS reduction on distributed matmul recorded before/after at identical N
- [ ] #4 e2e green; all negative gate unit nets (over-capacity, stall, free-choice) still rejected with correct diagnoses
<!-- AC:END -->
