---
id: TASK-0422.01
title: >-
  Verify PRD §8.3 inv(2) over the POST-mediation EventList for
  mp-tcp-*/mp-uds-event backends (host_mediation_inject +
  host_data_relay_inject)
status: Done
assignee:
  - mped
created_date: '2026-06-02 16:21'
updated_date: '2026-06-02 17:32'
labels:
  - compiler
  - event-contract
  - prd-invariant-audit
  - cycle-242
dependencies: []
parent_task_id: TASK-0422
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Forward-carried from TASK-0428 (architect P3). TASK-0428 proved inv(2) (Push/Wait matched pairs) holds for the ENTIRE example corpus (55/55 schedules) on the backend-AGNOSTIC pre-mediation chain (build_acfg -> ... -> inject_transfers -> acfg_to_events), closing the stale deferral premise. BUT the mp-tcp-{bufsync,event,poll} / mp-uds-event backends run TWO further ACFG passes AFTER inject_transfers: host_mediation_inject + host_data_relay_inject (re-routing Push/Wait through host with host election). inv(2) over THAT post-mediation EventList is NOT proven. This is the live hard-blocker for TASK-0422 (wiring validate_event_lists as a production gate at backend EventList consumption): the gate cannot be wired until the post-mediation EventList is shown inv(2)-clean for these 4 backends. SCOPE: extend the TASK-0428 corpus sweep (tests/petri_to_events.rs task0428_inv2_holds_for_entire_example_corpus) to additionally apply host_mediation_inject + host_data_relay_inject (with correct host election) per mp-* backend, and assert inv(2) on the result. If a violation surfaces, it is a REAL finding to root-cause (do NOT relax the check to make it pass). Only after this passes can TASK-0422 wire the gate. Pointers: src/passes/host_mediation_inject.rs, host_data_relay_inject; backend_common::host_election; the TASK-0428 sweep test as the extension template.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Implementation Plan (cycle-243):
- Add a corpus-sweep integration test in nucleus/driver/tests/ (the driver crate depends on BOTH nucleus-compiler and backend-common, and IS the source of truth for the per-backend pass ordering I am mirroring). nucleus-compiler/tests/ cannot host it (adding backend-common dev-dep there would create a dev-dep cycle).
- The test reuses the TASK-0428 example-corpus discovery + `schedule for "..."` algo resolution VERBATIM, runs the backend-agnostic chain (build_acfg -> block_transforms -> partition_{workers,rows,blocks2d} -> halo -> reuse -> inject_syncs -> inject_transfers), then for EACH of the 4 mp-* backends applies the post-injection mediation passes EXACTLY as driver/src/main.rs ~464-553:
    * {mp-tcp-bufsync, mp-tcp-event, mp-tcp-poll, mp-uds-event}: project once -> used set -> elect_host_from_name_workers -> apply_host_mediation_inject (None => pass through).
    * {mp-tcp-event, mp-uds-event} ONLY (additionally): re-project post-mediation -> re-elect -> apply_host_data_relay_inject (None => pass through).
  bufsync/poll get mediation ONLY; event/uds get BOTH (asymmetry mirrored, no copy-paste uniformity).
- Host election via the SHARED backend_common::elect_host_from_name_workers (NOT hand-replicated — memory feedback-driver-must-mirror-backend-election-exactly).
- Assert validate_event_lists (full surface incl inv(2)) == Ok on the post-mediation EventList for every (backend, schedule) cell. errd==0; ok>=55*<n-mediated-backends-applicable>. Any violation = REAL finding, root-cause; do NOT relax. If out-of-scope to fix, leave In Progress + file a task with dep edge.
- Gate: just build && clippy && test && test-release && e2e; e2e must hold 385/328/0/57/0.

RESULT (cycle-243, commit 62c1c9e): GREEN. inv(2) (matched Push/Wait pairs) holds on the POST-mediation EventList for ALL 4 mp-* backends across the entire corpus. 220 (backend, schedule) cells validated (55 schedules x 4 backends), 0 inv(2) violations, 0 pipeline errors (errd==0).

New file: nucleus/driver/tests/task0422_01_inv2_post_mediation.rs
 - task0422_01_inv2_holds_post_mediation_for_mp_backends: the broad sweep.
 - task0422_01_mediation_is_non_vacuous_for_pipelined: non-vacuity pin (data-relay GENUINELY reshapes EventList for 09-producer-consumer/pipelined under mp-tcp-event; assert_ne! pre vs post).

Gate: just build ok; just clippy clean (-D warnings); just test dev=1258 passed/0 failed; just test-release=1256 passed/0 failed; just e2e=385/328/0/57/0 (baseline held).

GOTCHAS / subtleties hit:
 1. CRATE-HOME: test had to go in driver/tests/ NOT nucleus-compiler/tests/ (backend-common depends on nucleus-compiler => dev-dep cycle). driver depends on both. A bin-only crate STILL exposes its [dependencies] to integration tests, so nucleus_compiler + backend_common both import fine.
 2. PATH: examples live at <repo>/nuc-nucleus/examples but the cargo WORKSPACE root is <repo>/nucleus. From driver/tests CARGO_MANIFEST_DIR=<repo>/nucleus/driver, so .parent().parent() (driver->nucleus->repo). First draft used one .parent() and would have mis-resolved (caught immediately by the examples.is_dir() assert design + isolated run).
 3. HOST ELECTION: used the SHARED backend_common::elect_host_from_name_workers verbatim per the driver call sites (NOT hand-replicated). Mirrored the driver asymmetry exactly via MEDIATED_BACKENDS table (bool applies_data_relay): bufsync/poll=mediation only, event/uds=mediation+relay.
 4. NON-VACUITY was a real risk: a sweep that ran no-op mediation passes would pass while proving nothing beyond TASK-0428. Added the assert_ne! pin to prove the post-mediation artefact is genuinely distinct.

HONEST LIMITS / could-not-verify:
 - host_data_relay_inject is a no-op for many schedules (only rewrites Push/Wait pairs whose BOTH endpoints are non-host); the sweep asserts inv(2) on whatever each pass produces, not that every cell was reshaped. Non-vacuity proven for at least the pipelined case, not per-cell.
 - This validates the COMPILE-time EventList contract only; the e2e bit-identical differential covers runtime.
 - 220-cell count assumes the corpus floor of 55 schedules; the >=55*4 assert guards against a schedule dir moving / schedule-for path breaking.

Cycle-243 review gate (parallel read-only): qa-test-runner GO + mped-architect GO. Numbers independently re-run by qa: dev 1258/0/3, release 1256/0/3, e2e 385/328/0/57/0 (x2 stable), clippy clean, both new tests run+pass (220 cells / 0 errd / 0 violations reproduced). Architect GO with two non-vacuity refinements (P2: relay delta not isolated from mediation; P3: no non-vacuity coverage for host_mediation_inject itself) — both folded back IN-THREAD by orchestrator in commit 91cdadf: the sweep now counts per-pass reshapes corpus-wide and hard-asserts both > 0 (empirically mediation reshaped 16 cells, data-relay 6), and the named 09-pipelined pin now isolates the data-relay delta (med_only vs full). Architect P3-dedup (3 near-verbatim pre-mediation pipeline-chain copies) filed as TASK-0422.01.01 (cross-crate, out of scope here). inv(2) post-mediation is now proven AND proven-non-vacuous for all 4 mp-* backends. Parent TASK-0422 (gate wiring) is unblocked.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Step (1) of TASK-0422 DISCHARGED GREEN. PRD §8.3 inv(2) proven to hold on the POST-mediation EventList for mp-tcp-{bufsync,event,poll}+mp-uds-event across the full example corpus (220 cells, 0 violations) via nucleus/driver/tests/task0422_01_inv2_post_mediation.rs (commit 62c1c9e). Mediation pass set + host election mirror driver/src/main.rs ~464-553 exactly. Non-vacuity pinned. Full gate green; e2e 385/328/0/57/0 held. No findings/blockers; no new tasks filed.
<!-- SECTION:FINAL_SUMMARY:END -->
