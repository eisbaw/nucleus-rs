---
id: TASK-0341.02.01.09
title: >-
  Epic S0b: pin the 16-jacobi codegen invariant the early-exit fix depends on
  (real workspace test, not the cruft spike)
status: To Do
assignee: []
created_date: '2026-06-03 10:36'
labels:
  - grammar-extension
  - regression-pin
  - test
dependencies: []
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect P2 from the S0 spike review (cycle-255): the S0 finding (TASK-0341.02.01.01) rests on the codegen invariant that 16-jacobi field is a FULL (ITERS+1)-slice history array and the final extraction reads a HARD-CODED slice field[ITERS]. That invariant is currently asserted ONLY by the throwaway cruft/spike-0341-02-01-01 standalone rustc file, which nothing in CI compiles — a stale-snapshot/comment-doc-lie trap: if a future cycle changes the field-buffer lowering (e.g. lands the double-buffer optimization, or alters the (t+ITERS)%(ITERS+1) modulus) the spike silently keeps asserting the old shape and the S4 fix premise becomes a lie with no signal. FIX: pin the invariant with a REAL workspace test — either a behavioral/codegen-snapshot assertion or a unit test that greps the emitted 16-jacobi pthreads-sync main.rs for the full-history buffer (vec![0; (ITERS+1)*H*W]) + the hard-coded field[ITERS] extraction shape (field[(ITERS)*H*W + ...]). Gives the S4 implementer a HARD failure when the substrate the fix depends on drifts. Should land before/at S4 (TASK-0341.02.01.05).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A workspace test pins: 16-jacobi single-worker emits a full (ITERS+1)-slice history buffer AND a hard-coded field[ITERS] final extraction
- [ ] #2 Test BITES if the buffer model or the extraction slice index changes (drift detector for the S4 premise)
<!-- AC:END -->
