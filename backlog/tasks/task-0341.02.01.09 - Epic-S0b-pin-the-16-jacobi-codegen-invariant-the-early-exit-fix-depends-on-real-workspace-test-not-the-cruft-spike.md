---
id: TASK-0341.02.01.09
title: >-
  Epic S0b: pin the 16-jacobi codegen invariant the early-exit fix depends on
  (real workspace test, not the cruft spike)
status: Done
assignee:
  - Mark Ruvald Pedersen
created_date: '2026-06-03 10:36'
updated_date: '2026-06-03 10:59'
labels:
  - grammar-extension
  - regression-pin
  - test
dependencies: []
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect P2 from the S0 spike review (cycle-255): the S0 finding (TASK-0341.02.01.01) rests on the codegen invariant that 16-jacobi field is a FULL (ITERS+1)-slice history array and the final extraction reads a HARD-CODED slice field[ITERS]. That invariant is currently asserted ONLY by the throwaway cruft/spike-0341-02-01-01 standalone rustc file, which nothing in CI compiles — a stale-snapshot/comment-doc-lie trap: if a future cycle changes the field-buffer lowering (e.g. lands the double-buffer optimization, or alters the (t+ITERS)%(ITERS+1) modulus) the spike silently keeps asserting the old shape and the S4 fix premise becomes a lie with no signal. FIX: pin the invariant with a REAL workspace test — either a behavioral/codegen-snapshot assertion or a unit test that greps the emitted 16-jacobi pthreads-sync main.rs for the full-history buffer (vec![0; (ITERS+1)*H*W]) + the hard-coded field[ITERS] extraction shape (field[(ITERS)*H*W + ...]). Gives the S4 implementer a HARD failure when the substrate the fix depends on drifts. Should land before/at S4 (TASK-0341.02.01.05).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A workspace test pins: 16-jacobi single-worker emits a full (ITERS+1)-slice history buffer AND a hard-coded field[ITERS] final extraction
- [ ] #2 Test BITES if the buffer model or the extraction slice index changes (drift detector for the S4 premise)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Cycle-256 DONE (commit 9d4247e + review fold-back). Added jacobi_result_extraction_reads_hardcoded_iters_slice to driver/tests/task0348_zero_init_invariant.rs (co-located with the sibling vec![0;320] buffer-SIZE pin — together they pin both halves of the 16-jacobi field[ITERS+1] history-buffer model). Asserts the emitted pthreads-sync main.rs extracts via the hard-coded slice: kernels::ident(field[((4)*64+...]). PROVEN TO BITE: fails on slice (3), passes on (4); architect verified the substring occurs EXACTLY ONCE (line 22) and does NOT match the step reads (modular field[(((t+4)%(4+1)))*64+...]). GATES: just test 1291/0 (+1); clippy clean; e2e 420/363/0/57/0 unchanged (test-only). Review: qa GO + architect GO (no P1/P2). Folded architect P3-1 (broadened the file module-doc to mention the extraction pin) + P3-2 (inline note that the literal (4)/*64 discriminator also trips on a benign ITERS/H/W dims change -> update the literals; the real PREMISE-DRIFT guard is the slice going runtime). NOTE for the S4 implementer (TASK-0341.02.01.05): when you land the runtime break-generation extraction, THIS pin is EXPECTED to fail — update/retire it as part of S4 (its failure then is the feature landing, not a regression).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Real workspace drift-pin for the 16-jacobi hard-coded field[ITERS] final extraction (driver/tests/task0348_zero_init_invariant.rs), guarding the S4/S5 early-exit-fix premise against substrate drift that the cruft/ spike cannot detect. Co-located with the sibling vec![0;320] buffer-size pin (the two halves of the history-buffer model). Bite-proven; qa GO + architect GO; e2e 420/363/0/57/0 unchanged.
<!-- SECTION:FINAL_SUMMARY:END -->
