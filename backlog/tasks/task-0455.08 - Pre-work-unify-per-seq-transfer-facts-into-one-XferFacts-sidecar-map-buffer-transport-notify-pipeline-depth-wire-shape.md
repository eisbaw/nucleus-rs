---
id: TASK-0455.08
title: >-
  Pre-work: unify per-seq transfer facts into one XferFacts sidecar map (buffer,
  transport, notify, pipeline depth, wire shape)
status: To Do
assignee: []
created_date: '2026-06-09 21:57'
labels:
  - production
  - refactor
  - compiler
dependencies: []
parent_task_id: TASK-0455
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
From the 2026-06-09 architecture review (Risk 3). TransferPolicy carries synchronous/buffer/notify/transport (nucleus-compiler/src/acfg/types.rs:353-364), but each fact reaches backends through its OWN parallel BTreeMap<SeqTag,_> in NameSidecar with its own collector, serde-default, and forward-clone (sidecar.rs:237 buffer, :267 transport) — and notify has NO sidecar surface at all (grep over sidecar.rs + sidecar/collectors.rs: zero hits). The KernelSig DIVERGENCE HAZARD comment (sidecar.rs:481-494) records that this copy-mirror pattern already produced one real divergence (purity). TASK-0455.02 needs notify threaded per-seq; TASK-0455.07/TASK-0453.22 add wire shape; pipeline depth currently rides the ACFG separately (pipeline_depth_for_seq).

Work: one BTreeMap<SeqTag, XferFacts> (buffer, transport, notify, pipeline depth, wire shape) populated by ONE collector; migrate all existing consumers; remove the old parallel maps (do not deprecate-and-keep). Serde back-compat via field defaults — note the contract-version caveat from the Event::Sync SyncTag work (required-field changes break old sidecars).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Single XferFacts map replaces the parallel per-seq maps; old fields deleted; grep proves no remaining BTreeMap<SeqTag, primitive> in sidecar.rs
- [ ] #2 notify is carried per-seq end-to-end and readable by backends (consumer may stub until TASK-0455.02)
- [ ] #3 Serde round-trip test covers missing-field defaults; all tests + e2e green
<!-- AC:END -->
