---
id: TASK-0455.08
title: >-
  Pre-work: unify per-seq transfer facts into one XferFacts sidecar map (buffer,
  transport, notify, pipeline depth, wire shape)
status: Done
assignee: []
created_date: '2026-06-09 21:57'
updated_date: '2026-06-10 12:11'
labels:
  - production
  - refactor
  - compiler
dependencies: []
parent_task_id: TASK-0455
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
From the 2026-06-09 architecture review (Risk 3). TransferPolicy carries synchronous/buffer/notify/transport (nucleus-compiler/src/acfg/types.rs:353-364), but each fact reaches backends through its OWN parallel BTreeMap<SeqTag,_> in NameSidecar with its own collector, serde-default, and forward-clone (sidecar.rs:237 buffer, :267 transport) — and notify has NO sidecar surface at all (grep over sidecar.rs + sidecar/collectors.rs: zero hits). The KernelSig DIVERGENCE HAZARD comment (sidecar.rs:481-494) records that this copy-mirror pattern already produced one real divergence (purity). TASK-0455.02 needs notify threaded per-seq; TASK-0455.07/TASK-0453.22 add wire shape; pipeline depth currently rides the ACFG separately (pipeline_depth_for_seq).

Work: one BTreeMap<SeqTag, XferFacts> (buffer, transport, notify, pipeline depth, wire shape) populated by ONE collector; migrate all existing consumers; remove the old parallel maps (do not deprecate-and-keep). Serde back-compat via field defaults — note the contract-version caveat from the Event::Sync SyncTag work (required-field changes break old sidecars).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Single XferFacts map replaces the parallel per-seq maps; old fields deleted; grep proves no remaining BTreeMap<SeqTag, primitive> in sidecar.rs
- [x] #2 notify is carried per-seq end-to-end and readable by backends (consumer may stub until TASK-0455.02)
- [x] #3 Serde round-trip test covers missing-field defaults; all tests + e2e green
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Unify per-seq transfer facts into ONE BTreeMap<SeqTag, XferFacts> on NameSidecar (replaces parallel transfer_buffer_for_seq + transfer_transport_for_seq). XferFacts = { buffer: u64, transport: TransportMode, notify: NotifyMode (NEW backend surface), pipeline_depth: Option<NonZeroU64> (mirror of ACFG, backend-facing only), wire: documented-seam placeholder }. ONE collector collect_xfer_facts walks the post-pass ACFG XferPlaceholder.policy for buffer/transport/notify; pipeline_depth mirrored from acfg.pipeline_depth_for_seq by seq. Migrate all consumers (event_plan/plan.rs cap lookup, embedded render transport, async/bufsync/event tests). Delete old parallel maps. Serde: additive serde(default) on the new map; round-trip + legacy-JSON-missing-field test. Targeted cargo test on every touched crate + clippy. A/B sidecar-internal (no emit-path change other than reading same values via new accessor).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
DESIGN DECISION (pre-impl, per onboarding step 3): pipeline_depth STAYS ACFG-side as source-of-truth; XferFacts carries it as a BACKEND-FACING MIRROR only. Rationale: (1) grep shows NO backend reads pipeline_depth — it is consumed ONLY by middle-end (acfg_to_petri::buffer_place_for initial-marking + host_data_relay_inject mirroring + ~10 pass struct-literals). pthreads-async/tests/multi_worker_codegen.rs:171 even ASSERTS pipeline_depth/pre_fill/initial_marking never appear in emitted code. (2) Ripping pipeline_depth_for_seq out of ACFG would ripple into acfg_to_petri + every pass that struct-literals ACFG. (3) DIVERGENCE HAZARD (sidecar.rs KernelSig comment): two SOURCES rot. Solution: ONE source (ACFG pipeline_depth_for_seq for the model/Petri layer), and XferFacts.pipeline_depth is a pure mirror derived in build_sidecar (like buffer/transport already are mirrors of policy). This makes XferFacts the SINGLE backend-facing surface for ALL per-seq transfer facts while not duplicating the model-layer source. notify gets a NEW backend surface (was zero sidecar hits). wire shape = documented Option<XferWire> seam left None (TASK-0455.07/0453.22 fill it).

IMPLEMENTED + VERIFIED. Single BTreeMap<SeqTag, XferFacts> on NameSidecar replaces transfer_buffer_for_seq + transfer_transport_for_seq (both DELETED). XferFacts = {buffer:u64, transport:TransportMode, notify:NotifyMode, pipeline_depth:Option<NonZeroU64>}; wire-shape seam documented (no dead field added — see struct docs). ONE collector collect_xfer_facts walks XferPlaceholder.policy for buffer/transport/notify + mirrors acfg.pipeline_depth_for_seq. Accessors xfer_buffer/xfer_transport/xfer_notify/xfer_facts_for. notify threaded END-TO-END (policy_from_directive ordering.rs:789 -> policy.notify -> XferFacts.notify), pinned by sidecar_xferfacts::notify_event_directive_threads_into_xfer_facts on 13-cnn pipeline_parallel (notify=event). Serde: xfer_facts is serde(default); pipeline_depth field also serde(default). AC#1 grep proof: only BTreeMap<SeqTag,_> in sidecar.rs is xfer_facts:BTreeMap<SeqTag,XferFacts>. VERIFICATION: cargo test -p nucleus-compiler ALL PASS (incl proptest_serde 16/16 field-completeness gate, sidecar_buffer 5/5 rewritten, sidecar_xferfacts 6/6 NEW); backend-common + 7 backends + mpi + driver + test-common ALL PASS (59+21 ok result lines, zero FAILED); clippy --workspace --all-targets -D warnings exit 0 (forced recompile 14.56s). A/B emit: changes sidecar-internal; empirically confirmed value-preservation — pthreads-async 09-pipelined emits Ring::new(4) (buffer=4 edge) + Ring::new(1) (sync); embedded-pattern 22-dma-pio emits shim.dma_link_arm (mode=dma seqs) + shim.link_push (mode=pio seq) — per-seq buffer/transport divergence preserved via the new accessors.

MEGA-FILE FENCE: adding XferFacts pushed sidecar.rs to 1087 LoC (>1000 hard fail). FIXED by splitting the struct+Default into NEW child module nucleus-compiler/src/sidecar/xfer_facts.rs (pub use re-export; same precedent as collectors/cumulative_tests); sidecar.rs now 983 LoC, just check-mega-files GREEN. cargo doc --no-deps: zero XferFacts/sidecar intra-doc-link warnings (only PRE-EXISTING unrelated ones). check-include-str-coverage + check-textual-replace-on-codegen GREEN. Release-profile test of the sidecar/serde tests PASS (debug_assert divergence guard). FINAL TOUCHED FILES (this task only; the Cargo.toml/lock, backend-common/lib.rs, project_skeleton.rs, mpi/openmp/poll/uds, dispatch.rs, pthreads-sync, single_worker_main/ in git status are CARRIED from TASK-0455.11, NOT mine): nucleus-compiler/src/sidecar.rs, sidecar/xfer_facts.rs (NEW), sidecar/collectors.rs, event.rs, passes/{halo_inference,reuse_inference,host_data_relay_inject}.rs; nucleus-compiler/tests/{proptest_serde,sidecar_buffer,sidecar_halo,sidecar_partition_blocks2d,sidecar_reuse}.rs + sidecar_xferfacts.rs (NEW); backend-common/src/event_plan/plan.rs; backends/embedded-pattern/src/{render.rs,tests/transport_mode_render.rs}; backends/pthreads-async/src/{multi_worker.rs,lib.rs}; backends/mp-tcp-event/src/lib.rs + tests/{loop_body_w2w_push,multi_worker_emit}.rs; backends/mp-tcp-bufsync/tests/{loop_body_w2w_push,wait_before_push}.rs; test-common/src/lib.rs. Left In Progress per wave rules; no git add/commit.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Parallel per-seq maps DELETED, unified into NameSidecar.xfer_facts (XferFacts{buffer,transport,notify,pipeline_depth}, one collector). notify threaded per-seq end-to-end for the first time (inert until TASK-0455.02 consumes; forward-noted). Design decision recorded: pipeline_depth stays ACFG-side single-source, XferFacts is a read-only mirror — single writer + pass-ordering proof verified by architect (collector runs after host_data_relay_inject, the last mutator; consistency pin test). Serde defaults + legacy-JSON fixtures; proptest field-completeness gate extended. Wire-shape seam documented for TASK-0455.07/0453.22. sidecar.rs split under the fence. Landed 0fdbd9c; architect GO; gate 2948/0 + e2e baseline held.
<!-- SECTION:FINAL_SUMMARY:END -->
