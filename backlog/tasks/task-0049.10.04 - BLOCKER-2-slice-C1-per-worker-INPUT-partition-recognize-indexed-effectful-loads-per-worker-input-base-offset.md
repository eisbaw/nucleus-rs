---
id: TASK-0049.10.04
title: >-
  BLOCKER 2 slice C1: per-worker INPUT partition (recognize indexed effectful
  loads + per-worker input base offset)
status: In Progress
assignee:
  - '@me'
created_date: '2026-06-03 23:21'
updated_date: '2026-06-04 00:14'
labels:
  - M11
  - backend
  - codegen
  - transport
  - value-correctness
dependencies: []
parent_task_id: TASK-0049.10
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Slice C1 of TASK-0049.10 (decomposed from slice C; BLOCKER 2 = per-worker input partition). After slices A+B the FIRMWARE reads/drains per-frame indexed in/out correctly, but the multi-MCU .resc/transport classification in multimcu.rs was NOT updated — a SILENT SIBLING of slice A.

TWO sub-gaps:
(1) CLASSIFICATION (silent-sibling of slice A): multimcu.rs is_effectful_load (line ~270) only matches WHOLE-ARRAY output + no inputs ('Some(o) if o.indices.is_empty() && inputs.is_empty()'). ex14's indexed effectful loads (mic_in[frame] <-- fe_capture(), o.indices NON-empty) do NOT match => has_effectful_load false => WorkerPlan.loads_input false for fe AND rf => render_multimachine_resc injects NO $input into their machines (multimcu.rs:614-621) => the shim reads garbage from axiSram 0x24000000. FIX (additive, mirror slice A): add a NEW arm recognizing an indexed effectful load gated on PURITY (the indexed-output no-input shape is structurally identical to a pure indexed compute; needs sidecar.kernel_sig(kernel).purity == Effectful). KEEP the whole-array arm structural+unchanged. is_effectful_load/has_effectful_load/is_effectful_io take the Fire's kernel id (Event::Fire{kernel,bindings}) so thread sidecar: &NameSidecar into TransportPlan::build (lib.rs:453 call site) + the classifiers. REGRESSION-SENSITIVE: is_effectful_io feeds flatten_salients => verify_control_sync_subsumed (the control-Sync guard) AND loads_input/saves_output; 02-split-add (whole-array loads, still matched) + the guard must be unchanged.

(2) PER-WORKER INPUT OFFSET: input.bin = mic_in[0..256] ++ bt_in[256..512] (reference generator nuc-nucleus/examples/14-hearing-aid/reference/src/main.rs:74-87,121-122; i32 LE; N_FRAMES=4 x SAMPLES_PER_FRAME=16 x 4B = 256B each). The shim (skeleton/multimcu.rs:60,146,195-196) has a per-worker input_cursor starting at 0 reading from NUC_INPUT_REGION=0x24000000; the .resc injects the WHOLE input.bin at 0x24000000 into EVERY loader (multimcu.rs:620). So fe (cursor 0) reads mic_in (CORRECT) but rf (cursor 0) ALSO reads mic_in (WRONG; must read bt_in at offset 256). MECHANISM (decided — Mechanism A, partition in codegen NOT recipe): keep injecting the whole input.bin per loader; emit a PER-WORKER input base offset into each worker's shim so input_cursor STARTS at that worker's slice offset. offset[w] = cumulative byte-size (from sidecar.data_types) of all LOADED input symbols ordered BEFORE w's loaded symbol, in canonical input-layout order. Each loader loads exactly ONE symbol = its indexed effectful load Fire's output datum (fe->mic_in, rf->bt_in). CANONICAL ORDER = data-declaration / DataId order (mic_in declared first => DataId lower => offset 0; bt_in => 256). DOCUMENT this coupling to the reference generator's hand-written layout LOUDLY (precedent: project-axis-mapping-assumption) — it is a documented ASSUMPTION that slice D's byte-exact gate is the real validation of; pin fe=0/rf=256 in a test.

SCOPE: INPUT partition only. Output capture (BLOCKER 3, both fe spk_out + rf bt_out drain to same $uartFile) is slice C2. renode-multimcu recipe resolution + ex14 byte-exact is slice D. VERIFY: emit-shape test (fe & rf both loads_input=true + get $input injected; fe shim base offset 0, rf base offset 256); just renode-multimcu 02-split-add still BYTE-EXACT (single-loader regression — Mechanism A must leave it at offset 0); e2e 427/364/0/63/0; just check green. Do NOT claim ex14 byte-exact (slice D).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 multimcu.rs classification recognizes indexed effectful loads via purity (sidecar threaded into TransportPlan::build): fe AND rf get loads_input=true; whole-array load arm unchanged (structural); is_effectful_io/verify_control_sync_subsumed behaviour on 02-split-add unchanged
- [ ] #2 per-worker input base offset (Mechanism A) emitted into each loader's shim: fe cursor starts at 0 (mic_in), rf at 256 (bt_in); offset = cumulative size of loaded input symbols before this worker's symbol in DataId/declaration order; coupling to reference input.bin layout documented loudly + pinned by an emit test (fe=0, rf=256)
- [x] #3 regression: just renode-multimcu 02-split-add BYTE-EXACT (single loader 'host' stays at offset 0); e2e 427/364/0/63/0 byte-for-byte; just check (build/clippy/test/test-release) GREEN; ex14 byte-exact NOT claimed (slice D)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Add scalar_byte_width(&ScalarType)->usize + resolved_type_byte_size(&ResolvedType)->usize to backend-common/render/types.rs (single source of truth; note event_plan private scalar_width as a silent-sibling follow-up). 2. multimcu.rs: thread sidecar:&NameSidecar into TransportPlan::build (+lib.rs:453 call site) + the classifier helpers (has_effectful_load/is_effectful_load/is_effectful_io/flatten_salients/verify_control_sync_subsumed + lib.rs:447 call). Make purity-consulting classifiers fallible (Result<bool,EmitError>) so a missing KernelSig fails loud (ContractGap), mirroring slice A kernel_is_effectful. 3. is_effectful_load NEW arm: keep whole-array (indices empty) arm UNCHANGED; add indexed-output (indices non-empty) gated on purity==Effectful via Fire kernel id. 4. WorkerPlan gains input_base_offset:usize. TransportPlan::build collects all loaded symbols across all workers, sorts by DataId, computes cumulative byte offsets (size from sidecar.data_types); each worker offset = its FIRST loaded symbol offset; ASSERT multi-symbol loaders are contiguous (fail loud EmitError). Single loader -> 0 (02 regression-safe). 5. skeleton/multimcu.rs: emit const NUC_INPUT_BASE = <offset>; init input_cursor to it (NOT 0). Document the input.bin layout coupling LOUDLY. 6. Tests: extend bin_shape.rs with ex14 multi-MCU BIN emit test (fe loads_input + offset 0, rf loads_input + offset 256, both get $input in resc); 02-split regression test asserts host single loader offset 0. 7. Full gate; just renode-multimcu 02-split-add BYTE-EXACT.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPLEMENTED slice C1: classification fix LANDED + single-loader offset LANDED; cross-worker offset BLOCKED on a newly-discovered root cause (TASK-0049.10.06 filed). Full gate GREEN.

PART 1 (CLASSIFICATION, AC#1 DONE): threaded sidecar:&NameSidecar into TransportPlan::build (lib.rs:453) + verify_control_sync_subsumed (lib.rs:447) + the classifier helpers (has_effectful_load / is_effectful_load / is_effectful_io / flatten_salients), which are now fallible (Result<bool,EmitError>) so a missing KernelSig fails loud (ContractGap) mirroring slice A kernel_is_effectful. is_effectful_load gained a NEW indexed-effectful arm gated on purity==Effectful (Some(o) if o.indices.is_empty() => true UNCHANGED whole-array; Some(_) => kernel_is_effectful NEW). Result: ex14 fe(mic_in) AND rf(bt_in) are now both recognised as loaders => both get $input injected. Verified additive: render.rs whole-array load arm (render.rs:456-458) is purely structural NO purity check -> my whole-array arm matches it exactly (no doc-lie). Guard probe confirmed verify_control_sync_subsumed returns Ok(()) for ex14 even with fe/rf indexed loads now counted as IO (barriers still data-subsumed; no spurious trip). 02-split-add guard behaviour unchanged (sync_guard tests all green).

PART 2 (OFFSET, AC#2 BLOCKED -> TASK-0049.10.06): the decided Mechanism A (DataId-order concatenation) is UNSOUND. EMPIRICAL FINDING via probe: DataId is assigned ALPHABETICALLY (acfg/build.rs:60-66 enumerates the name-keyed BTreeMap algo.data.keys()), NOT declaration order. For ex14: bt_in=DataId(0), bt_out=1, mic_in=2, mixed=3, spk_out=4. So sorting loaded symbols by DataId puts bt_in BEFORE mic_in => fe(mic_in)=offset 256, rf(bt_in)=offset 0 -- the REVERSE of the reference input.bin (mic block at byte 0, bt at 256; reference/src/main.rs:74-87). Declaration order is LOST at AlgoIR construction (AST->IR stores data into BTreeMap<String,ResolvedData>) and is ABSENT from NameSidecar/NameTables. So a correct cross-worker offset CANNOT be computed in this backend slice. Per project fail-fast discipline I did NOT emit a byte-wrong offset: compute_input_offsets FAILS LOUD (EmitError::UnsupportedFeature naming the 2 loader workers + DataId-vs-declaration root cause + TASK-0049.10.06) for >=2 distinct loader workers. Single-loader (02-split-add host loads a+b, reads whole input from front in firing order) => offset 0, byte-identical to before. The WorkerPlan.input_base_offset field + shim NUC_INPUT_BASE const + input_cursor:NUC_INPUT_BASE seam are LANDED (currently always 0) so TASK-0049.10.06 only has to populate the value once declaration order is available, not re-plumb the cursor.

REJECTED APPROACHES: (a) emit DataId-order offset fe=256/rf=0 + loud comment -- ships byte-WRONG, guarantees slice D fail, violates AC#2 pin; rejected. (b) rely on schedule transfer-directive order (mic_in,bt_in,...) -- fragile workaround, transfer order not in codegen contract; rejected. (c) hardcode mic_in/bt_in names -- workaround; rejected.

SCOPE-CLEANUP: added then REMOVED scalar_byte_width/resolved_type_byte_size from backend-common/render/types.rs (the byte-sizing they served is deferred with the offset to TASK-0049.10.06; not shipping unused pub API).

GATE: build OK; clippy clean (-D warnings, force-fresh touch); just test 1339 passed 0 failed; just test-release 1337 passed 0 failed; e2e 427/364/0/63/0 byte-identical; just renode-multimcu 02-split-add BYTE-EXACT 1024 bytes (single-loader host stays offset 0 -- regression-safe, the AC#3 cross-MCU proof). ex14 byte-exact NOT claimed (slice D). Review gate: qa-test-runner/mped-architect subagent-spawn tool not available in this env; orchestrator self-performed inline review (doc-lie spot-check, silent-sibling grep of all call sites, panic-not-diagnostic audit -- all typed EmitError, opacity-gate-rot N/A).

NEW TESTS: bin_shape::ex14_multimcu_cross_worker_input_partition_fails_loud_pending_decl_order (asserts the fail-loud message names both loaders + DataId/declaration/TASK-0049.10.06); bin_shape multi_worker_bin test extended to pin host NUC_INPUT_BASE=0 (single-loader regression); sync_guard::indexed_effectful_load_is_classified_as_cross_worker_io + indexed_pure_compute_is_not_cross_worker_io (unit pins for the purity-gated classification arm).

ORCHESTRATOR REVIEW GATE (cycle close, independent parallel read-only). qa-test-runner RE-RAN the full gate + tier-3 (numbers independently reproduced, not transcribed): build clean; clippy clean -D warnings (forced fresh re-Check of embedded-pattern); just test 1339/0/3; just test-release 1337/0/3 (-2 expected should_panic compile-out); just e2e 427/364/0/63/0 x2 identical; AND just renode-multimcu 02-split-add BYTE-EXACT 1024 bytes (single-loader host stayed at offset 0 — Mechanism A regression-safe; no closure download needed). GO. mped-architect (read-only) GO: classification fix ADDITIVE (whole-array arm logically identical to parent ff77577; new purity-gated arm only fires on non-empty-indices+no-inputs+effectful = ex14 captures, cannot re-classify 02/M6/ex1-5-9); control-Sync guard inert + threaded through BOTH (and only) production call sites lib.rs:447/453 with ZERO stale infallible callers and ZERO unwrap defeating fail-loud; the +120-line sync_guard test delta PINS behaviour (Effectful->IO reject / Pure->not-IO accept positive+negative pair), not just compiles; fail-loud SOUND — the dangerous silent rf=0 outcome CANNOT happen (rejection on >=2 distinct loader WORKERS keyed by worker not symbol, so 02 host loading a+b is one entry => offset 0; rejection runs up front in build() before any file write); AC#1/#3 genuinely met, AC#2 honestly BLOCKED, In Progress correct, fail-loud is NOT a workaround. ORCHESTRATOR INDEPENDENTLY CONFIRMED the crux finding (acfg/build.rs:60-66: linked.algo.data is BTreeMap<String,_> so .keys().enumerate() assigns DataId ALPHABETICALLY; ex14 bt_in=DataId(0) < mic_in=DataId(2), the REVERSE of reference input.bin mic@0/bt@256) — the orchestrator-decided Mechanism A premise (DataId==declaration order) was EMPIRICALLY FALSE (feedback-orchestrator-narrative-also-wrong); the implementer correctly refused to emit byte-wrong code and rooted AC#2 to TASK-0049.10.06. FOLDS: filed TASK-0049.10.07 (P3-1 lift shared kernel_is_effectful helper, silent-sibling drift between render.rs:594 + multimcu.rs:363); folded P3-2 scope-note into TASK-0049.10.06 (AlgoAst.items already preserves decl order — .06 is lighter than the full-thread prose implies). TASK STAYS IN PROGRESS: AC#1+AC#3 done (classification silent-sibling fix + seam + regression all landed in 2152572, GATE-VERIFIED), AC#2 BLOCKED on TASK-0049.10.06 (decl-order in the contract).
<!-- SECTION:NOTES:END -->
