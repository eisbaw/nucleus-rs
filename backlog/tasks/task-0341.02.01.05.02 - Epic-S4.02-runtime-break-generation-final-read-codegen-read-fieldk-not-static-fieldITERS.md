---
id: TASK-0341.02.01.05.02
title: >-
  Epic S4.02: runtime break-generation final-read codegen (read field[k] not
  static field[ITERS])
status: Done
assignee:
  - '@Mark Ruvald Pedersen'
created_date: '2026-06-03 14:42'
updated_date: '2026-06-03 18:11'
labels:
  - grammar-extension
  - codegen
  - examples
dependencies:
  - TASK-0341.02.01.05.01
parent_task_id: TASK-0341.02.01.05
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Runtime final-read codegen, gated on the S0 spike (TASK-0341.02.01.01) finding. The shipped 16-jacobi reads a HARD-CODED slice field[ITERS] (generated main.rs:22). On early-exit at k<N, slice ITERS is unwritten (all-zero). The convergence variant must extract from field[k] where k is the captured break generation (loop var t at break). BUFFER-MODEL: the spike PROVED field is a FULL-history array i32[(ITERS+1)*H*W] (NOT a 2-slice double-buffer); generation-k is always materialized in slice k, so the codegen-free lowering (.05.01) needs NO buffer-model change — only THIS slice changes the final-read index to runtime. The step read index needs no change. Thread the captured break-generation into the extraction loop. SCALING: full-history (N+1)*H*W couples to the Petri-unroll bound at large N; a 2-slice double-buffer is a possible follow-up if N is large (would make final-read field[k%2] and require re-checking the cumulative classifier sidecar.rs:704-762) — NOT needed for .05.01 correctness.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Final-extraction slice index is RUNTIME (the captured break-generation k), not the compile-time field[ITERS]; under early-exit at k<N the gather reads generation-k
- [x] #2 Behavioral test early-exits at a known k and asserts the gather is generation-k (the .01 spike finding bites)
- [x] #3 Step cross-iteration read index (t+ITERS)%(ITERS+1) is UNCHANGED (architect P3: always evaluates to t-1, break-point-independent)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Forward-carried from .05.01 cycle-260 architect review (P3-1 + P2-1 residue) ===
DEAD-CODE CLEANUP to fold into this slice: BuildAcfgError::UntilLoopUnsupported is now DEAD after .05.01 lifted the reject (errors.rs:167 def + errors.rs:246 Display arm + errors.rs:190 cross-ref doc). No construct site remains; the Display arm can never fire. It was never serialized, so removal is safe. Remove the variant + Display arm. Also sweep any residual stale references (build.rs:240 + acfg.rs:888 are legitimate HISTORICAL "ex-/was rejected" comments — keep; but re-grep UntilLoopUnsupported after removal to confirm only intended historical mentions remain).
CONTEXT for .05.02 proper: the runtime break-generation final-read. Per the .01 spike, 16-jacobi field is a FULL (ITERS+1)-slice history array; on early-exit at k the final gather must read field[k] (the runtime break generation), NOT the hard-coded field[ITERS] (which is unwritten/stale-zero on early-exit). The break_cond now rides ACFGNode::Repeat.break_cond (.05.01); the EMIT consumer is petri_to_events.rs:317 (binding break_cond:_ today, compiler-forced non-elided so .05.04 wiring cannot be silently skipped). TASK-0436 overflow-safe abs applies to the real reduction kernel (sibling .05.04 concern).

Forward-carried from TASK-0341.02.01.05.04 (cycle-261, commit b8f7686): The break EMIT now lands — Event::Loop.break_cond (3rd additive #[serde(default)] field) carries the predicate and pthreads-sync emits `if <render_bool_expr(cond)> { break; }` as the LAST loop-body stmt. THE BREAK LANDS AFTER THE FULL BODY, so on early-exit at generation k the body for iteration k IS fully executed (the write into field[k] happens) before break — your runtime final-read must extract field[k] where k is the loop var t AT the iteration that broke (the last executed t), NOT t after increment. .05.04 did NOT touch the final-extraction index (still the hard-coded field[ITERS] today) — that is YOUR slice. The break condition operands render via render_int_expr (scalar value renderer); when you thread the captured break-generation k into the extraction loop, render it the same way. No bool-DATA buffer exists (scalar Compare is inline) — you do not inherit a Vec<bool>. block_tag x break_cond: tile loops never carry break_cond (fail-loud guard added) — irrelevant to your full-history field[k] read.

DONE cycle-262 with parent TASK-0341.02.01.06 (commit ee5513b). Runtime break-generation final-read landed in pthreads-sync break_loop.rs: collect_break_loop_info records the break loops cap (range.end-1) + written-DataIds; rewrite_final_reads STRUCTURALLY rewrites the post-loop extraction read of a break-written arrays cap-constant outer index to Ident(__nuc_final_gen) (NOT textual replace). AC#1 (runtime extraction slice) + AC#2 (behavioral test early-exits at known k and gather is gen-k: the 21-jacobi-converge e2e cell breaks at k=30 with NON-ZERO interior, so reading field[ITERS_CAP]=0 would MISMATCH reference.bin; plus unit test runtime_final_read_rewrites_cap_index_to_break_gen) + AC#3 (step read index (t+CAP)%(CAP+1) UNCHANGED - verified in generated main.rs, the in-loop reads are untouched). Also DEAD-CODE residue (BuildAcfgError::UntilLoopUnsupported) cited in the .05.01-forward note was already removed pre-this-cycle (grep confirms no construct site). Verified bit-identical.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Runtime break-generation final-read: extraction reads field[__nuc_final_gen] (captured break gen k), not the hard-coded field[ITERS_CAP]. Structural IrExpr rewrite (not textual). Proven load-bearing via the 21-jacobi-converge e2e cell (breaks at k=30, non-zero interior; reading the cap slice would mismatch) + a unit test. Step read index unchanged.
<!-- SECTION:FINAL_SUMMARY:END -->
