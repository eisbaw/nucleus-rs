---
id: TASK-0049.10.03
title: >-
  Latent: scalar (full-rank-indexed) effectful IO place emits non-compiling
  .as_ptr() — fail loud
status: Done
assignee:
  - '@claude'
created_date: '2026-06-03 22:59'
updated_date: '2026-06-04 06:17'
labels:
  - M11
  - backend
  - codegen
  - hardening
dependencies: []
parent_task_id: TASK-0049.10
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect review of TASK-0049.10.02 (P3-1; shared latent with slice A). backend_common::render::render_indexed_place returns SliceForm::Scalar (D[idx], a single T) when a DataSlice is FULL-RANK indexed. The embedded backend's per-frame effectful arms then emit D[idx].as_ptr() (drain, render.rs effect_drain_place ~640) / D[idx].as_mut_ptr() (fill, render.rs slice-A input arm ~544) — neither compiles on a scalar T (no as_ptr/as_mut_ptr on i32).

Cannot occur for any CURRENT shape: ex14 spk_out/bt_out/mic_in/bt_in are i32[N_FRAMES][SAMPLES_PER_FRAME], so an [frame] index is partial-rank => always SliceForm::SubArray (a [T] slice, valid). But a future effectful kernel with a FULL-RANK-indexed scalar input/output (e.g. 1-D data x:i32[N], emit(x[i])) would emit non-compiling firmware — a latent panic-not-diagnostic / silent-broken-emit gap.

FIX (when picked up): make the per-frame effectful in/out arms reject (or correctly handle) a Scalar place with a typed EmitError::UnsupportedFeature carrying a precise message, instead of emitting code that fails the downstream cross-compile with an opaque rustc error. Likely needs render_indexed_place (or a sibling) to expose the SliceForm Scalar/SubArray distinction the arms currently lose through the String return. Touches BOTH the slice-A fill arm and the slice-B drain arm — own review. e2e-inert (no current cell hits it).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
IMPLEMENTED (commit 23615b6). Added backend_common::render::render_indexed_subarray_place: SubArray -> [start..start+sub_len] place; Scalar (full-rank index) -> typed EmitError::UnsupportedFeature naming the missing .as_ptr()/.as_mut_ptr() + remedy. Both embedded effectful arms (slice-A INPUT fill ~544, slice-B effect_drain_place ~647) now call the guarded helper instead of render_indexed_place. Stale render.rs comments (the guard was "filed as TASK-0049.10.03") reworded to describe the live guard. Two unit tests in render_guard_siblings.rs: full-rank scalar -> Err (BITES: a Scalar fall-through to SubArray formatting would return Ok), partial-rank -> Ok with exact spelling. Gate: e2e 427/364/0/63/0 unchanged (Scalar path unreached); tests 1357 dev / 1355 release (+2 each). Renode byte-exact preserved (02 1024B + 14 512B). cargo doc -p backend-common clean.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE. Fail-loud guard for full-rank-indexed scalar effectful IO place. Commit 23615b6. backend_common::render::render_indexed_subarray_place rejects SliceForm::Scalar (no .as_ptr() on a single T) with EmitError::UnsupportedFeature; both embedded effectful arms delegate to it. e2e 427/364/0/63/0; tests 1357 dev/1355 release (+2); Renode 02 1024B + 14 512B BYTE-EXACT. LIMIT/COLD-SESSION NOTE: the Scalar arm is still UNREACHED by any current shape (ex14 data is 2-D, [frame] is partial-rank). The guard is latent-only; the first source that exercises it would be a 1-D effectful kernel (x:i32[N], emit(x[i])) — there is NO e2e cell proving the emitted firmware error message in situ, only the unit test proving the helper returns Err. If/when such a kernel is admitted, add an e2e/embedded reproducer.
<!-- SECTION:FINAL_SUMMARY:END -->
