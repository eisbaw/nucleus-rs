---
id: TASK-0341.02.02
title: >-
  Example 16-jacobi AC#3: distributed schedule (partition=rows × 4 workers on
  spatial 2D grid)
status: To Do
assignee: []
created_date: '2026-05-27 14:24'
labels:
  - examples
  - coverage
  - M5-follow-up
dependencies: []
parent_task_id: TASK-0341.02
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
=== Filed as TASK-0341.02 AC#3 fold-out, cycle 206 ===

16-jacobi AC#1 (language-sanity, fixed-iteration pthreads-sync naive) landed cycle 206. AC#3 is the multi-worker distributed schedule (partition=rows on the spatial 2D grid).

## What this exercises

- partition=rows on 2D field — partition the spatial dimension across workers. Same shape as 05-stencil/distributed (well-trodden territory).
- halo_inference for the 4-neighbour stencil — should already work since 05-stencil has the same pattern (one row of halo on each side).
- Cross-iteration data edge — each generation t reads field[t-1]; under partition=rows each worker owns its rows of field[t] but reads neighbouring rows from other workers via halo_inference.

## Acceptance criteria

1. **Schedule landed**: nuc-nucleus/examples/16-jacobi/schedules/distributed.sched.nuc with workers={host,w0..w3} and partition=rows on the spatial dimension.
2. **e2e cell PASSES bit-identical OR honest-BLOCKED**: cell `16-jacobi | distributed | pthreads-sync` PASSES bit-identical against the existing reference.bin, OR honest-BLOCKED with a precise filed follow-up naming the gap (e.g. "halo_inference doesn't reach into Repeat body for multi-iteration cross-iteration edges").
3. **Cross-backend follow-up filed**: once pthreads-sync passes, sibling-promotion follow-up for the other tier-1 backends.

## Honest scope LIMITS

- 11-game-of-life landed multi-iter under pipelined.sched.nuc; this is a different schedule shape (spatial partition vs pipeline). The 2D × multi-iter × halo combo is plausibly green but worth empirical verification.
- If halo_inference cannot reach into the Repeat body (the t-outer loop), this is the gap — file precise follow-up; do not silently work around.
<!-- SECTION:DESCRIPTION:END -->
