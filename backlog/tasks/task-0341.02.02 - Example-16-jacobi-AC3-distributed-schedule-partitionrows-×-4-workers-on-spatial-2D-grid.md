---
id: TASK-0341.02.02
title: >-
  Example 16-jacobi AC#3: distributed schedule (partition=rows × 4 workers on
  spatial 2D grid)
status: In Progress
assignee:
  - '@orchestrator'
created_date: '2026-05-27 14:24'
updated_date: '2026-05-27 15:12'
labels:
  - examples
  - coverage
  - M5-follow-up
dependencies: []
parent_task_id: TASK-0341.02
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
=== Filed as TASK-0341.02 AC#3 fold-out, cycle 206 ===

16-jacobi AC#1 (language-sanity, fixed-iteration pthreads-sync naive) landed cycle 206. AC#3 is the multi-worker distributed schedule (partition=rows on the spatial 2D grid).

## What this exercises

- partition=rows on 2D field — partition the spatial dimension across workers. Same shape as 05-stencil/distributed (well-trodden territory).
- halo_inference for the 4-neighbour stencil — should already work since 05-stencil has the same pattern (one row of halo on each side).
- Cross-iteration data edge — each generation t reads field[t-1]; under partition=rows each worker owns its rows of field[t] but reads neighbouring rows from other workers via halo_inference.

## Acceptance criteria

1. **Schedule landed**: nuc-nucleus/examples/16-jacobi/schedules/distributed.sched.nuc with workers={host,w0..w3} and partition=rows on the spatial dimension.
2. **e2e cell PASSES bit-identical OR honest-BLOCKED**: cell `16-jacobi | distributed | pthreads-sync` PASSES bit-identical against the existing reference.bin, OR honest-BLOCKED with a precise filed follow-up naming the gap (e.g. "halo_inference doesn't reach into Repeat body for multi-iteration cross-iteration edges").
3. **Cross-backend follow-up filed**: once pthreads-sync passes, sibling-promotion follow-up for the other tier-1 backends.

## Honest scope LIMITS

- 11-game-of-life landed multi-iter under pipelined.sched.nuc; this is a different schedule shape (spatial partition vs pipeline). The 2D × multi-iter × halo combo is plausibly green but worth empirical verification.
- If halo_inference cannot reach into the Repeat body (the t-outer loop), this is the gap — file precise follow-up; do not silently work around.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Cycle 208 plan (orchestrator-direct, per memory feedback-spawned-agents-refuse-code-edits + cycle-206/207 precedent on the parent).

SCOPE (AC#3 only -- empirical gap probe per task brief):

1. Author nuc-nucleus/examples/16-jacobi/schedules/distributed.sched.nuc
   with workers={host,w0..w3}, partition=rows on spatial y-loop, SYNC
   transfers (pthreads-sync-compatible).

2. Empirically run `nucleus build --backend pthreads-sync` against the
   new schedule; observe outcome.

3. EXPECTED OUTCOMES (per task brief):
   - PASS bit-identical -> promote AC#3 to [[required]], file
     cross-backend follow-up.
   - HONEST-BLOCKED -> file precise gap, add [[skip]] cell, close
     AC#3 with the gap citation.

4. RUN gate: nix develop --command bash -c "just build && just clippy
   && just test && just test-release && just e2e".

5. Commit. Read-only review gate (qa-test-runner + mped-architect)
   parallel, before considering cycle closed.

EMPIRICAL RESULT (cycle 208):

Honest-BLOCKED. `nucleus build --backend pthreads-sync` rejects with:

  halo-inference error (under partitioned iv): kernel call
  `jacobi5_or_seed` reads `field` at axis 0 with a non-affine index
  expression. Halo inference accepts only `iv + b` (b constant) --
  see PRD §13 / TASK-0260 module docs.

Root cause: apply_halo_inference_partition_aware decides fatality
from the FULL enclosing iter-var scope ([t, y, x]) -- `y` is
partitioned -> fatal. But the failing index `(t + ITERS) % (ITERS +
1)` is at axis 0 and references ONLY `t` (NOT partitioned). Halo
strip on a non-partitioned axis is structurally unnecessary; the (B)
rule (TASK-0275 cycle 95) is over-conservative by one axis-mapping
degree of freedom.

Honest-BLOCKED resolution:
- Schedule file landed (documents the attempted shape).
- e2e [[skip]] cell added with precise reason citation.
- Compiler-feature follow-up filed as TASK-0341.02.02.01
  (refinement of the (B) fatality predicate from enclosing-scope to
  index-expression-referenced ivs).

AC#3 closed as honest-BLOCKED per task brief. Cross-backend
follow-up (AC's third item) NOT filed yet -- waits for
TASK-0341.02.02.01 to land first.
<!-- SECTION:PLAN:END -->
