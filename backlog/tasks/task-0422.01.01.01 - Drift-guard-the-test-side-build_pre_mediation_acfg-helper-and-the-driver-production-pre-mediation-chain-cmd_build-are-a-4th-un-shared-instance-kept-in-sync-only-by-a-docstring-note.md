---
id: TASK-0422.01.01.01
title: >-
  Drift guard: the test-side build_pre_mediation_acfg helper and the
  driver-production pre-mediation chain (cmd_build) are a 4th un-shared instance
  kept in sync only by a docstring note
status: To Do
assignee: []
created_date: '2026-06-02 20:13'
labels:
  - compiler
  - test-hygiene
  - silent-sibling-risk
  - cycle-245
dependencies: []
parent_task_id: TASK-0422.01.01
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect P3.2 (cycle-245, TASK-0422.01.01 review). TASK-0422.01.01 consolidated the 3 TEST copies of the pre-mediation pass chain into nucleus_compiler::test_support::build_pre_mediation_acfg. But the DRIVER PRODUCTION chain in driver/src/main.rs cmd_build (~lines 295-427: build_acfg -> block_transforms -> partition_{workers,rows,blocks2d} -> halo(advisory discarded) -> reuse -> inject_syncs -> inject_transfers) remains a SEPARATE 4th instance. The helper docstring instructs keeping the two in sync, but nothing ENFORCES it — a new pass inserted before inject_transfers in the driver would not trip any gate if the helper is not updated, and the corpus sweeps (task0428 + task0422_01) would silently stop exercising the real lowering. This is precisely the feedback-silent-sibling-defect class. SCOPE/OPTIONS: (a) a guard test asserting the driver chain and the helper chain produce an identical ACFG for a sample (algo,sched) — but the driver chain is interleaved with backend-conditional logic + String error mapping, so extracting a comparable unit is the hard part; (b) factor the driver's pre-mediation sequence into a shared fn that BOTH cmd_build and the helper call (the real fix, larger — must preserve the driver's display_with_src error mapping which differs from the helper's {e:?}); (c) accept the docstring note as the guard and close as wont-fix. LOW priority — no current drift; this captures the risk in durable state per the silent-sibling memory rather than relying on a comment. RELATED (architect P3.1, lower): test_support is a #[doc(hidden)] pub fn always compiled into the release nucleus-compiler lib/driver bin; could be #[cfg(any(test, feature=test-support))]-gated (feature enabled ONLY via [dev-dependencies] to avoid the feature-unification-into-prod-bin footgun) if release-surface minimalism ever matters. Negligible cost today. Pointers: nucleus-compiler/src/test_support.rs; driver/src/main.rs:295-427.
<!-- SECTION:DESCRIPTION:END -->
