---
id: TASK-0453.07
title: 'IMPL P7: precise distributed gather/scatter (inspector-executor)'
status: Done
assignee: []
created_date: '2026-06-06 22:55'
updated_date: '2026-06-07 06:10'
labels:
  - rigour
  - compiler
dependencies: []
parent_task_id: TASK-0453
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Addresses L6b (ch11:71-84). Precise distributed gather/scatter moving only touched elements, via a two-pass inspector/executor (saltz1991runtime) or bounded runtime exchange. RISK: admits a runtime-determined communication pattern in tension with the static firing order - open problem is to keep the soundness gate sound, e.g. bound the exchange in a statically sound envelope. Residual likely: only a bounded subclass.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A distributed gather/scatter moves a precise sub-slice rather than whole-array for at least one pattern
- [ ] #2 Soundness gate remains sound (bounded-envelope argument tested)
- [ ] #3 Subclass scope + residual whole-array fallback documented
<!-- AC:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
.07 HONEST-STOP (cycle-7, same disposition as P6 .06: Done-as-DEFERRED, ACs UNTICKED -- no implementation shipped). Precise distributed gather/scatter (moving only touched elements, inspector/executor saltz1991runtime) has NO clean invariant-safe single-cycle slice. STEP-0 duplicate-check + STEP-0b empirical premise verification AGAINST CURRENT GENERATED CODE (not comments/6-day-old memory): distributed gather (TASK-0373) and scatter (TASK-0384) ALREADY ship 7-backend byte-identical, but BOTH conservatively broadcast the data-dependent array WHOLE -- built 17-spmv/distributed_gather on mp-tcp-bufsync and confirmed host emits enc_vec(&x) whole to all 4 workers (and even the affine-banded val/col_idx go whole-array on the wire; only the receiver-side y gather slices); scatter: each worker holds a private whole-width histogram replicate, host element-wise-sums. The gathered/target array goes whole-array BY NECESSITY -- its touched set is runtime-determined. Moving only-touched requires inspector/executor / runtime exchange = admitting a runtime-determined communication pattern = directly violates the standing guardrail (do NOT weaken the static-firing-order invariant / soundness gate). The co-transferred affine-banded arrays are inferred precisely but ALSO transmitted whole-array -- tightening those is the separate large TASK-0453.22 wire-precision project (own project, all-zero-output risk) and does NOT deliver precise GATHER anyway (the gathered array stays whole regardless). A const-index-array subclass would be contrived rigour-theater (AC-gaming, forbidden). KEY INSIGHT (folded into thesis .17): whole-array broadcast IS the trivial statically-sound envelope; the open problem is TIGHTENING it (which elements move under a fixed-structure gate), not BOUNDING exchange size -- the bound is the part P5 for..until already demonstrates (scalar trip count). Closing the gap is a VERIFICATION advance the present soundness argument does not reach. ACs #1-#3 deliberately UNTICKED (no precise sub-slice moved; soundness-envelope NOT newly tested in code -- it is honestly argued in thesis instead). Residual stays documented future work in ch11 sec:fw-affine, sharpened by .17. e2e baseline 497/428/0/69/0 held (no code change).
<!-- SECTION:FINAL_SUMMARY:END -->
