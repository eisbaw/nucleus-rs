---
id: TASK-0341.01.01
title: 'Example 15-transpose AC#2: distributed-rows schedule on output dimension'
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-27 12:44'
updated_date: '2026-05-27 20:32'
labels:
  - examples
  - coverage
  - M5-follow-up
dependencies: []
parent_task_id: TASK-0341.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
=== Filed as TASK-0341.01 AC#2 fold-out, cycle 204 ===

15-transpose AC#1 (language-sanity, pthreads-sync naive) landed cycle 204. AC#2 is the multi-worker distributed-rows schedule that exercises:

- partition=rows on the OUTPUT dimension where the INPUT has a different layout (because output rows correspond to input columns under axis-swap).
- transfer_inject for cross-worker INPUT data fan-in: each output worker writes 4 contiguous output rows, which requires reading 4 columns of input — a strided fan-in shape that no existing example covers (06-separable-filter has 2 sequential ROW-aligned passes, not a row-vs-column dependence).

## Acceptance Criteria
<!-- AC:BEGIN -->
1. **Schedule landed**: nuc-nucleus/examples/15-transpose/schedules/distributed-rows.sched.nuc with workers={host,w0..w3} and partition=rows on the OUTPUT dimension (W-axis under [W][H] layout, i.e. the worker partitions across output ROWS = the input's columns).
2. **e2e cell PASSES bit-identical OR honest-BLOCKED**: cell `15-transpose | distributed-rows | pthreads-sync` PASSES bit-identical against the existing reference.bin, OR an honest-BLOCKED outcome with a precise filed follow-up task naming the compiler-feature gap (e.g. "transfer_inject must permute per-tile bounds under axis-swap output partition", "partition=rows on output dim requires strided input fan-in not yet supported").
3. **Cross-backend follow-up filed**: once the partition lands on pthreads-sync, the same cell on the other tier-1 backends becomes a separate sibling-promotion follow-up (similar to the AC#3 cross-backend promotion that TASK-0341.01.02 covers for the naive schedule).

## Honest scope LIMITS

- This is NOT a milestone-class task. It exercises the existing partition=rows + transfer_inject machinery on a NEW dataflow shape (output-driven partition with axis-swapped input read). The compiler-feature-gap risk is real — the structural test is "is the existing partition machinery layout-aware enough for this".
- If the empirical answer is "no", file the precise gap NAME (not just a "doesn't work" note) — the next session's scoping target depends on the gap being precisely named.
<!-- SECTION:DESCRIPTION:END -->

- [x] #1 Schedule landed: nuc-nucleus/examples/15-transpose/schedules/distributed-rows.sched.nuc with workers={host,w0..w3} and partition=workers on the inner j-loop (output's row axis under [W][H] layout)
- [x] #2 e2e cell PASSES bit-identical: cell 15-transpose | distributed-rows | pthreads-sync PASSES bit-identical against the existing reference.bin (≥3 fresh just e2e samples non-flake at promotion time)
- [x] #3 Cross-backend follow-up filed: TASK-0341.01.01.01 filed for the 6 sibling tier-1 backend promotion (mp-tcp-bufsync, pthreads-async, mp-tcp-event, openmp-rs, mp-tcp-poll, mp-uds-event)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Cycle 215b clarification erratum (architect P2 fold-back) ===

The Description body's proto-AC #1 (filed cycle 204) reads 'with workers={host,w0..w3} and partition=rows on the OUTPUT dimension'. The cycle-215 implementation landed with 'partition=workers on the inner j-loop' because partition=rows is structurally outer-of-2D-only (partition_rows.rs:18-30 + NotOuterOf2DNest variant); the transpose's output-row axis is the INNER j-loop, which partition=workers via find_loop's recursive descent reaches directly.

The structured AC#1 backfilled in cycle 215 (before this addendum) correctly says 'partition=workers on the inner j-loop'. The Description body proto-AC was left as-filed to preserve the audit trail of the cycle-204 framing.

Per memory feedback-ac-rewrite-on-done-task: NOT rewriting the description proto-AC text; this erratum records the wording-vs-reality delta. Same pattern as the cycle-205 / cycle-208b AC-wording-vs-implementation addenda on sibling tasks.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 215 closure (orchestrator-direct, per memory feedback-spawned-agents-refuse-code-edits).

OUTCOME:
- 15-transpose | distributed-rows | pthreads-sync PASSES bit-identical against reference.bin.
- AC#1 (schedule landed): nuc-nucleus/examples/15-transpose/schedules/distributed-rows.sched.nuc (loop j : partition=workers).
- AC#2 (e2e cell PASS): cycle-215 e2e baseline 273/239/0/34/0 → 280/246/0/34/0 (+7 cells = 1 required + 6 informational PASS); 3 fresh samples non-flake.
- AC#3 (cross-backend follow-up filed): TASK-0341.01.01.01 filed for the 6 sibling backend promotion.

KEY MECHANISTIC FINDING:
- The TASK-0301/TASK-0302 axis-mapping filter handles the axis-swap shape correctly via graceful-degradation. For output[j][i] (j leads): dim 0 Some(j_band) → push; dim 1 None → hit_hole → final bounds = [(j, j_band)]. For input[i][j] (i leads, j trails): dim 0 None → hit_hole; dim 1 Some(j_band) AFTER hole → return Some(empty) → whole-array broadcast.
- The asymmetric per-data outcome (output j-banded gather, input whole-array fan-out) is the contiguous-prefix invariant biting on the axis-swap shape. This is the FIRST shipped example to exercise this graceful-degradation path empirically.
- partition_workers.rs::find_loop's recursive descent correctly targets the INNER j-loop (the output's row axis); no shipped schedule before cycle 215 had partitioned an inner loop.

DELIVERABLES (cycle 215):
- nuc-nucleus/examples/15-transpose/schedules/distributed-rows.sched.nuc (76 lines; documents the axis-mapping filter behaviour).
- nuc-nucleus/e2e-matrix.toml: new [[required]] entry 15-transpose | distributed-rows | pthreads-sync milestone=M6 + 31-line narrative block.
- nuc-nucleus/examples/15-transpose/README.md: 'Required schedules' section appended with distributed-rows description.
- TASK-0341.01.01.01 filed (cross-backend promotion follow-up).

GOTCHAS / FORWARD-CARRIED LESSONS:
- The transpose's INNER-loop partition is a structural first. partition_workers.rs::find_loop uses recursive descent so inner-loop targeting works without code changes. Forward-carry: future schedules can target inner loops directly via partition=workers; no need to invent a separate directive.
- Whole-array broadcast on input is the correct outcome here (not a bug). Future schedules where partition iv hits trailing data dims AFTER a non-partitioned hole should ALSO degrade to whole-array broadcast. This is the DESIGNED contract of the cycle-118/121 axis-mapping filter, not a workaround.
- The xpose kernel is identity-passthrough on scalar (i32) -> i32. Each output element depends on ONE input element — no reduction, no halo, no cross-iteration carry. The partition is sound regardless of axis-mapping because the dependence graph is trivial.

NON-ISSUES PROACTIVELY DISCHARGED:
- The 'partition=rows' vs 'partition=workers' choice: chose partition=workers because partition=rows is outer-of-2D-only (partition_rows.rs:18-30 makes this structural pre-condition explicit). Schedule comment block documents the choice with file:line citations.
- The 'AC#3 cross-backend follow-up' shape: AC#3 says 'file the follow-up', not 'land the promotion'. Filed TASK-0341.01.01.01 (same pattern as TASK-0341.03.02.01 cycle 212 → cycle 214).

TEST COUNT IMPACT: 1019/0/3 (no Rust delta this cycle; schedule + TOML + README only).
<!-- SECTION:FINAL_SUMMARY:END -->
