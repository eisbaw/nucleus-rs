---
id: TASK-0341.02.01.08
title: >-
  Epic S7: multi-worker + 7-backend cross-backend differential (collective-break
  invariant)
status: To Do
assignee: []
created_date: '2026-06-03 10:14'
updated_date: '2026-06-03 17:15'
labels:
  - grammar-extension
  - multi-worker
  - cross-backend
dependencies:
  - TASK-0341.02.01.06
  - TASK-0341.02.01.07
parent_task_id: TASK-0341.02.01
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Multi-worker convergence loop + cross-backend differential across all 7 tier-1 backends (TASK-0341.02.01 AC#3). P1 HARD INVARIANT (architect design review cycle-254): the break is a COLLECTIVE decision, NEVER a local one. Each worker computes a partial convergence over its partition; the GLOBAL convergence scalar is all-reduced at a barrier and broadcast, and EVERY worker branches on the IDENTICAL broadcast value — so all workers exit at the same iteration by construction (bit-identity holds; no nondeterministic deadlock). If instead each worker tested its LOCAL diff, worker A could break at iter 7 while worker B waits at the next barrier => deadlock. Reuse the existing barrier/SyncTag machinery for the all-reduce+broadcast. Add a NEGATIVE test that a local-only break predicate is REJECTED fail-loud. Interacts with the cumulative cross-iteration tracking and the host gather (the same place xN-correctness bit other distributed cumulative work — TASK-0366).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Convergence break is a collective all-reduce+broadcast on the global scalar; every worker branches identically (no local-only break)
- [ ] #2 Negative test: a local-only break predicate is rejected fail-loud
- [ ] #3 16-jacobi converge PASSES bit-identical on all 7 tier-1 backends (AC#3)
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0341.02.01.05.04 (cycle review gate): the two multi-worker walkers — backend-common/src/multi_worker_walker/event_walker.rs and backend-common/src/tcp_plan/events.rs — now FAIL LOUD (EmitError::UnsupportedFeature) when an Event::Loop carries a Some(break_cond). This is the guard S7 must REPLACE with real multi-worker break emit. IMPORTANT (the silent-drop near-miss): break_cond IS preserved through the partition path (transfer_inject/partition.rs, sync_inject.rs) and cloned onto every per-worker Loop by petri_to_events.rs:417 — so a for..until inside a partition=workers scope WILL flow a Some into these walkers. The collective-break invariant (S7 AC): all workers must agree on the early-exit generation k (the convergence predicate is a global reduction); a per-worker independent break would desync. Mirror the single-worker emit site pthreads-sync render_events_in Loop arm (if <render_bool_expr(cond)> { break; }) but gate the break on a COLLECTIVE converged signal, not a per-worker-local one. render_bool_expr already exists in backend-common/src/render/expr.rs.
<!-- SECTION:NOTES:END -->
