---
id: TASK-0341.02.01.08
title: >-
  Epic S7: multi-worker + 7-backend cross-backend differential (collective-break
  invariant)
status: In Progress
assignee: []
created_date: '2026-06-03 10:14'
updated_date: '2026-06-10 22:50'
labels:
  - grammar-extension
  - multi-worker
  - cross-backend
dependencies:
  - TASK-0341.02.01.06
  - TASK-0341.02.01.07
parent_task_id: TASK-0341.02.01
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Multi-worker convergence loop + cross-backend differential across all 7 tier-1 backends (TASK-0341.02.01 AC#3). P1 HARD INVARIANT (architect design review cycle-254): the break is a COLLECTIVE decision, NEVER a local one. Each worker computes a partial convergence over its partition; the GLOBAL convergence scalar is all-reduced at a barrier and broadcast, and EVERY worker branches on the IDENTICAL broadcast value — so all workers exit at the same iteration by construction (bit-identity holds; no nondeterministic deadlock). If instead each worker tested its LOCAL diff, worker A could break at iter 7 while worker B waits at the next barrier => deadlock. Reuse the existing barrier/SyncTag machinery for the all-reduce+broadcast. Add a NEGATIVE test that a local-only break predicate is REJECTED fail-loud. Interacts with the cumulative cross-iteration tracking and the host gather (the same place xN-correctness bit other distributed cumulative work — TASK-0366).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Convergence break is a collective all-reduce+broadcast on the global scalar; every worker branches identically (no local-only break)
- [ ] #2 Negative test: a local-only break predicate is rejected fail-loud
- [ ] #3 16-jacobi converge PASSES bit-identical on all 7 tier-1 backends (AC#3)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
STAGED PLAN (cycle S7, fresh-context implementer).

KEY EMPIRICAL FINDING (verified this session): the 12 sibling-backend [[skip]] cells on 21-jacobi-converge/naive + 29-jacobi-cap-hit/naive carry a WRONG skip reason. They say "multi_worker_walker / tcp_plan fail loud on break_cond". But a host-only (naive) schedule has used_workers<=1, so EVERY backend delegates to backend_common::single_worker_main::render_single_worker_main (which already fully emits the break). The multi-worker walker is NEVER reached for these cells. Verified: all 7 backends emit byte-identical main.rs for naive (only the documented #[path]/#[allow(dead_code)] kernels-mod attr differs on src/bin/ backends); openmp-rs built+ran BYTE-EXACT vs reference.bin.

STAGE A (sound flip, no codegen risk): flip the 12 [[skip]] sibling cells -> [[required]] on both examples (6 each). The cross-backend differential genuinely bites (the break/convergence/final-read/cap-hit machinery is shared single-worker emit). Correct the stale skip-reason narrative.

STAGE B (AC#1/#2 collective-break invariant, multi-worker): implement the collective all-reduce+broadcast lowering + multi-worker break emit in the two walkers (lift the fail-loud guards at event_walker.rs:174 + tcp_plan/events.rs:116) + the embedded render.rs guard stays (no_std out of reach). NEGATIVE test: a local-only break predicate rejected fail-loud (AC#2).

HONEST SCOPE on AC#3 (distributed converge PASSES on 7 backends): a partition=workers/rows convergence schedule inherits the 16-jacobi/DISTRIBUTED blockage (itself honest-BLOCKED on ALL 7 backends today: 5 on 3D wait_slice gap TASK-0341.02.02.01.01, 2 on TASK-0330 in-Loop w2w Push) PLUS new collective-break. So a multi-worker converge EXAMPLE cannot be a required e2e cell until 16-jacobi/distributed itself lands. Will assess whether a minimal non-halo multi-worker convergence shape is demonstrable; if not, AC#3 stays honest-BLOCKED with the distributed-base dependency named, and Stage A delivers the single-worker cross-backend differential.

STAGES:
(a) IR/middle-end: collective reducer lowering (per-iter local partial -> reducer -> global decision broadcast as Push/Wait + Sync in Repeat body).
(b) Backend emit: multi-worker break on the SAME received global decision at the SAME generation; lift guards.
(c) Matrix: flip the 12 naive sibling cells; update remaining skip reasons precisely.
(d) Tests: lowering unit pins, per-backend emit pins, negative local-only-break pin, two examples byte-exact on every flipped backend.
(e) FINAL: just e2e (verbatim totals), just e2e-mpi if in reach, falsifiers bite, clippy clean, fences green.

OWNERSHIP: nucleus-compiler/src, backend-common/src, backends/*/src, examples 21/29, e2e-matrix.toml, tests, tracker. NOT paper/ or docs/ (file thesis-sync follow-up).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Forward-carried from TASK-0341.02.01.05.04 (cycle review gate): the two multi-worker walkers — backend-common/src/multi_worker_walker/event_walker.rs and backend-common/src/tcp_plan/events.rs — now FAIL LOUD (EmitError::UnsupportedFeature) when an Event::Loop carries a Some(break_cond). This is the guard S7 must REPLACE with real multi-worker break emit. IMPORTANT (the silent-drop near-miss): break_cond IS preserved through the partition path (transfer_inject/partition.rs, sync_inject.rs) and cloned onto every per-worker Loop by petri_to_events.rs:417 — so a for..until inside a partition=workers scope WILL flow a Some into these walkers. The collective-break invariant (S7 AC): all workers must agree on the early-exit generation k (the convergence predicate is a global reduction); a per-worker independent break would desync. Mirror the single-worker emit site pthreads-sync render_events_in Loop arm (if <render_bool_expr(cond)> { break; }) but gate the break on a COLLECTIVE converged signal, not a per-worker-local one. render_bool_expr already exists in backend-common/src/render/expr.rs.

Forward-carried from TASK-0341.02.01.06 (S5, cycle-262, commit ee5513b): S5 landed example 21-jacobi-converge value-correct on single-worker pthreads-sync ONLY. The single-worker codegen S7 must generalise lives in nucleus/backends/pthreads-sync/: (a) the break EMIT (lib.rs render_event Event::Loop arm: `if <cond> { __nuc_break_gen = t; break; }`), and (b) the NEW break_loop.rs (break-gen capture + runtime final-read rewrite + cap-hit observability). KEY S7 GAPS to solve for multi-worker/7-backend: (1) the break is a COLLECTIVE decision - all workers must agree to halt at the same generation k (the convergence scalar maxdiff[t] is a cross-worker REDUCTION; each worker only has partials, so the halt predicate needs an all-reduce/barrier of maxdiff[t] before the break, else workers diverge on k). (2) backend_common multi_worker_walker + tcp_plan currently FAIL LOUD on break_cond:Some (bind-and-reject); embedded-pattern rejects too - those guards must be lifted + given real multi-worker break emit. (3) the runtime final-read rewrite (break_loop::rewrite_final_reads) is single-worker-EventList-shaped (top-level events, one break loop); multi-worker per-worker EventLists + the host GATHER of field[k] need the break-gen broadcast to the gather site. (4) cap-hit observability must be per-worker-consistent (all workers hit cap together since the predicate is collective). (5) The 21-jacobi-converge example is single-host schedule by design; S7 needs a partition=workers convergence schedule (which today hits the fail-loud guard). e2e baseline after S5: 427/364/0/63/0 (the 6 sibling-backend 21-jacobi-converge cells are [[skip]] pending S7 - promoting them is S7 AC).

Forward-carried from TASK-0341.02.01.06 (cycle review, architect P3-2): the cap-hit-not-converged RUNTIME path (sentinel -1 -> __nuc_final_gen=CAP + stderr [[nuc_converge]] did-NOT-converge diagnostic) currently has ONLY string-level unit-test coverage (cap_hit_resolution_distinguishes_converged_from_cap_hit) — NO e2e cell actually RUNS a non-converging input end-to-end. When S7 lands (or earlier), add a second e2e cell with a tiny cap (or a non-converging seed) so the cap-hit branch is exercised end-to-end, closing .05.03 AC#2 at the e2e layer. The 21-jacobi-converge example converges at k=30<cap=64 so it only exercises the CONVERGED path.

CYCLE-5 UPDATE (TASK-0453.05, rigour epic P5): the cap-hit-not-converged RUNTIME e2e coverage gap forward-carried into this task from the S5 review (P3-2: "no e2e cell RUNS a non-converging input") is now CLOSED by the new single-worker example 29-jacobi-cap-hit (ITERS_CAP=16 < convergence gen 30; runs the full cap; -1 sentinel -> __nuc_final_gen=cap + stderr did-NOT-converge; byte-identical vs reference.bin). So S7 remaining scope NARROWS to purely the MULTI-WORKER collective break: lift the fail-loud break_cond guards (event_walker.rs:163, tcp_plan/events.rs:112, embedded render.rs:188) + implement the collective all-reduce+broadcast of the convergence scalar (so all workers exit at the same generation; preserves static firing order) + per-worker final-read + host gather of field[k] + negative test (local-only break rejected) + promote the 6 [[skip]] siblings on BOTH 21-jacobi-converge AND 29-jacobi-cap-hit.

Raised LOW->MEDIUM 2026-06-09: pulled into the production-usefulness push (epic TASK-0455, leverage item 6). The multi-worker bounded break (collective all-reduce-and-broadcast of the convergence scalar at a barrier, all workers exiting at the same generation) is what admits distributed iterative solvers — the most-requested capability the production class still lacks (thesis sec:fw-affine).

CYCLE-S7 OUTCOME (fresh-context implementer): SINGLE-WORKER 7-backend for..until cross-backend differential LANDED; DISTRIBUTED collective-break remains honest-BLOCKED.

WHAT LANDED (sound, fully e2e-verified):
- Flipped the 12 sibling-backend [[skip]] cells (21-jacobi-converge/naive + 29-jacobi-cap-hit/naive x 6 backends each) to [[required]]. ROOT INSIGHT (verified empirically + by code-trace): the previous S5 skip reason was FACTUALLY WRONG. A naive schedule is host-only (workers={host}, used_workers<=1), so EVERY backend delegates it to backend_common::single_worker_main::render_single_worker_main (which already fully emits the break). The multi-worker walkers (event_walker.rs:174 / tcp_plan/events.rs:116) are NEVER reached for a single-worker schedule — their break_cond fail-loud guard fires only for partition=workers. Verified: all 7 backends emit byte-identical compute main.rs for naive (only the documented #[path]/#[allow(dead_code)] kernels-mod attr differs on src/bin/ backends); openmp-rs converge ran BYTE-EXACT vs reference.bin; mp-uds-event + mp-tcp-bufsync cap-hit ran the FULL 16-gen cap, emitted the [[nuc_converge]] did-NOT-converge stderr, byte-exact vs reference.bin.
- So the full for..until machinery (break EMIT + convergence reduction + runtime break-gen final-read + cap-hit observability) is now exercised bit-identically across all 7 tier-1 backends, BOTH the converged path AND the worst-case full-cap replay path.
- e2e 504/431/0/73/0 -> 504/443/0/61/0 (+12 pass, -12 skip, 0 required-fail); e2e-mpi 14/14 unchanged; clippy clean; 3 structural fences GREEN; new falsifier passes dev+release.
- NEW falsifier: backend-common/tests/multi_worker_break_cond_rejected.rs pins that the shared multi-worker walker REJECTS break_cond:Some fail-loud (the conservative-correct behaviour) + accepts a plain loop. Bites if a future cycle lifts the guard without the collective break.
- Stale narrative corrected in both example READMEs, both naive.sched.nuc, both prog.algo.nuc comment blocks, and the e2e-matrix.toml headers.

WHAT IS HONEST-BLOCKED (the 3 ACs, all about the DISTRIBUTED collective break):
- AC#1 (collective all-reduce+broadcast), AC#2 (local-only break rejected — presumes collective break exists to contrast), AC#3 (distributed converge passes 7 backends): ALL require a partition=workers convergence schedule, which inherits the 16-jacobi/DISTRIBUTED blockage (itself honest-BLOCKED on all 7: 5 wait_slice + 2 TASK-0330) PLUS the collective machinery. Filed as TASK-0341.02.01.08.01 (gated on the 16-jacobi/distributed cluster). NO AC ticked — the distributed invariant is genuinely not delivered; ticking would be fake-complete.
- Empirically confirmed the partition=rows converge schedule trips the combine=max accumulator-fan-in error (the all-reduce shape) before even reaching halo — so the distributed path is a multi-cycle epic, not this task.

THESIS-SYNC: paper/** claims now stale (for..until is 7-backend single-worker, not one-backend) filed as TASK-0467 (ch08:126, ch10:62/326/339, ch11 sec:fw-affine, ch05 sec:lang-limits). paper/ + docs/ were OUT OF SCOPE for this implementer.

TOUCHED: nuc-nucleus/e2e-matrix.toml; nuc-nucleus/examples/21-jacobi-converge/{README.md,prog.algo.nuc,schedules/naive.sched.nuc}; nuc-nucleus/examples/29-jacobi-cap-hit/{README.md,prog.algo.nuc,schedules/naive.sched.nuc}; nucleus/backend-common/tests/multi_worker_break_cond_rejected.rs (NEW). No production codegen changed (guards correctly stay fail-loud).

Orchestrator disposition (wave 10, 2026-06-11): stays In Progress by design — the landed half (single-worker 7-backend differential, 12 cells flipped required, baseline 504/443/0/61/0) is committed as 0c0ba28 with the corrected skip-reason story; the parent ACs are the DISTRIBUTED collective-break invariant, carried by the newly-filed child TASK-0341.02.01.08.01 with its dependency chain (16-jacobi/distributed cluster: 3D wait_slice TASK-0341.02.02.01.01 on 5 backends + in-Loop w2w TASK-0330 on 2). Tick nothing here until the child lands. Precedent: the TASK-0453.05 single-vs-multi-worker split.
<!-- SECTION:NOTES:END -->
