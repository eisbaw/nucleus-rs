---
id: TASK-0045.01
title: M7 — mpi-blocking multi-worker SPMD arm (rank Send/Recv + MPI_Barrier)
status: To Do
assignee: []
created_date: '2026-05-31 07:28'
labels:
  - M7
  - backend
  - mpi
dependencies:
  - TASK-0045
parent_task_id: TASK-0045
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
The multi-worker arm of the mpi-blocking SPMD backend (parent TASK-0045 landed the single-worker arm: one rank-dispatched binary, MPI_Init/Finalize + rank==0 guard, examples 1-6 naive value-correct under mpiexec -n 1).

emit() currently returns EmitError::ContractGap for used_workers > 1 (the loud forward-link this task discharges). Implement the multi-worker SPMD lowering so a multi-worker schedule (e.g. 02-split-add/split) compiles + runs under mpiexec -n N with bit-identical/value-correct output.

Lower the per-worker EventList onto MPI:
- Push  -> blocking world.process_at_rank(dst).send(&payload)
- Wait  -> world.process_at_rank(src).receive::<T>() (or any_process; DECIDE + pin, seq-paired like the smoke)
- Sync  -> world.barrier() for whole-COMM_WORLD; Comm_split + sub-barrier for host-excluding / non-uniform participant sets.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 emit() lowers a multi-worker schedule to a SINGLE SPMD binary whose `fn main` dispatches on world.rank() (match rank { 0 => .., 1 => .., .. } or per-rank fn dispatch) — NOT N separate binaries; the ContractGap rejection for used_workers>1 is removed.
- [ ] #2 Push/Wait lower to blocking MPI Send/Recv with the sidecar element type threaded through rsmpi's Buffer/Equivalence bounds; a payload type lacking mpi::Equivalence yields a TYPED EmitError (not a panic), mirroring the parent's ContractGap discipline.
- [ ] #3 Event::Sync lowers to MPI_Barrier (whole-world) and Comm_split+barrier for host-excluding/non-uniform participant sets; the host-election/participant rule MIRRORS the backend election EXACTLY (memory: feedback-driver-must-mirror-backend-election-exactly), pinned by a test.
- [ ] #4 check-mpi runs each multi-worker example under mpiexec -n N (N = used-worker count, the WORST case — all ranks live, NOT -n 1) and cmps byte-exact vs reference.bin (memory: 16-jacobi — deadlock-free != value-correct; one -n 1 sample hides ordering bugs).
- [ ] #5 A check-loop-carrying schedule (on_violation=count/log) is spot-verified under MPI: the NucCheckCountReporter stderr-summary now lives in nuc_compute()'s body and every rank running compute prints its own tally — confirm that interaction is correct or document it (forward-carried P3-2 from TASK-0045 architect review).
- [ ] #6 Implementation notes record: the SPMD rank-dispatch design (why it cannot route through tcp_plan/WirePrimitives), the Send/Recv typing decision, the barrier participant-set decision, and honest limits.
<!-- AC:END -->
