---
id: TASK-0455.03
title: Non-toy production case study at realistic size (the production witness)
status: Done
assignee: []
created_date: '2026-06-09 21:29'
updated_date: '2026-06-10 20:17'
labels:
  - production
  - case-study
dependencies:
  - TASK-0455.01
parent_task_id: TASK-0455
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Every corpus example is toy-sized (16x16 stencils, 16^3 matmul, ~1ms startup-dominated runtimes); no production witness exists. Pick ONE realistic workload and carry it end-to-end at a size where compute dominates startup (runtime >= 100x the process-spawn floor).

Candidates (pick one at task start): (a) 14-hearing-aid at real audio frame size and frame count — closest to the intended production use, exercises heterogeneous workers + embedded tier; (b) a VGA-resolution (640x480) image pipeline over the stencil/separable-filter algorithms — simplest scaling story.

Scope: tier-1 distributed + at least one more tier (MPI or embedded). Document as the production witness: sizes, schedules, matrix cells, compile cost, gate cost, runtime, memory, transfer volume.

Depends on TASK-0455.01: a realistic iteration/frame count expands the communicating net (Repeat with a cross-worker transfer per iteration), which the current expanded-replay gate cannot hold in memory.

Honest-stop discipline: if a wall is hit (memory, runtime, expressiveness, grammar), FILE THE WALL as its own task and document it in the case-study writeup — do not shrink the case study until it fits and call it realistic.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Chosen workload compiles and runs byte-exact across its required cells at the realistic size (size justified in writing against the startup floor)
- [x] #2 Case-study document committed (sizes, schedules, cells, compile/gate/runtime/memory/transfer-volume numbers)
- [x] #3 Every wall hit is filed as a follow-up task and named in the writeup, not worked around silently
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Workload = VGA 640x480 single-pass 3x3 box-blur stencil (candidate b), the shape the landed symbolic gate (TASK-0455.01) carries FLAT in N (loop-depth-0 single-shot Push/Wait); 16-jacobi/pipelined still expand so explicitly NOT chosen.
S1 fixture OUTSIDE examples/ at docs/case-study/: scaled prog.algo.nuc (H=480,W=640), kernels.rs (i32 box blur, env-var IO), naive+distributed(partition=rows, 4 workers) schedules, INDEPENDENT reference/ crate (std-only, no compiler/backend dep; manual policy-§2 check via the awk).
S2 input gen: deterministic i32 VGA frame (own generator, std-only) -> input.bin under docs/case-study/.
S3 runner scripts/case-study-run.sh + ONE just recipe: builds reference, generates input+reference.bin; nucleus build distributed on pthreads-async (tier-1) + mpi-nonblocking (.#mpi loopback); cargo build --release; run each; byte-diff vs reference.bin; print sizes/cells/compile-ms/gate-RSS/runtime/transfer-volume.
S4 size justification: runtime must dominate ~1ms startup floor (target >=100x); pick frame count / measure actual wall to confirm; if single-frame stencil is too cheap, scale frame COUNT via repeated independent frames (still loop-depth-0, no net expansion) OR larger image; record the honest number.
S5 writeup docs/case-study.md: sizes, schedules, cells, compile cost, gate cost (symbolic flat vs expanded projection), runtime, memory (full-shaped dest = honest residual TASK-0455.14), transfer volume (71.9% wire cut from TASK-0453.22).
S6 HONEST-STOP: any wall (memory/runtime/grammar/per-worker-alloc) -> file a task, name in writeup. Verify clippy untouched (no nucleus/** src edits).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
LANDED (orchestrator, 2026-06-10). Workload = VGA frame-strip single-pass 3x3 box-blur stencil, scaled to 32 stacked VGA frames (H=15360, W=640; 9.83M px, 39_321_600 B/array). Chosen because the landed symbolic gate (TASK-0455.01) carries the loop-depth-0 single-shot distributed net FLAT IN N; 16-jacobi loop-carried + pipelined shapes still expand and were explicitly NOT chosen (stated honestly in prog.algo.nuc + docs/case-study.md sec 1).

OWNERSHIP (all OUTSIDE examples/, paper/, nucleus/ src): NEW docs/case-study/{prog.algo.nuc,kernels.rs,README.md,schedules/{naive,distributed}.sched.nuc,reference/{Cargo.toml,src/main.rs},gen/{Cargo.toml,src/main.rs}}; NEW docs/case-study.md (the writeup); NEW scripts/case-study-run.sh; ONE justfile recipe `case-study`. Zero nucleus/** edits => clippy gate untouched (verified git diff). reference + gen are std-only, code-independent of the compiler (policy 2; check-reference-independence.awk scan exit 0 + re-run in the runner).

AC#1 (byte-exact at realistic size, justified vs floor): 3 cells all BYTE-IDENTICAL to the independent reference at 39_321_600 B: naive x pthreads-async, distributed x pthreads-async (4 workers), distributed x mpi-nonblocking (mpiexec --oversubscribe -n 5 loopback, all ranks live). Size justification: measured nucleus startup floor = ~0.5-0.7 ms (min of 60 runs, 4x4 program); runtimes 77-114 ms naive / 110-182 ms dist-tier1 / 1436-1535 ms dist-mpi => SMALLEST multiple across cells = 168x floor (>=100x target met, stable run-to-run because both numerator and denominator use min). Single VGA frame was only ~7x floor (documented why insufficient + the scaling curve).

AC#2 (writeup committed): docs/case-study.md records sizes, 3 schedules/cells table, compile cost (~0.04-0.06s) + GATE COST KEYSTONE (~88 MB RSS FLAT in N; same as 16x16; expanded net would be ~19.6M nodes / ~100GB projected order-of-magnitude), runtime + x-floor multiples, memory (full-shaped dest = honest residual), transfer volume (75.0% wire reduction per edge from TASK-0453.22, extracted from generated source: img_in 157MB->39MB, img_out 157MB->39MB, combined 314MB->78.6MB).

AC#3 (every wall filed + named): sec 7 names 5 limitations, each mapped to an EXISTING filed task: per-worker full-shaped alloc -> TASK-0455.14; loop-carried/pipelined dont scale + dist-slower-than-baseline + cluster-vs-loopback -> TASK-0455.04 (runtime study); strided/non-prefix edges unnarrowed -> TASK-0455.15. HONEST scaling result reported as-is: distributed (110-182ms) is SLOWER than naive baseline (77-114ms) at this size (coordination + full-shaped buffer overhead outweighs cheap 9-int-op-per-pixel blur on 4 loopback workers); MPI numbers are loopback NOT cluster. No new wall needed filing beyond the existing epic subtasks.

VERIFY: `just case-study` GREEN end-to-end multiple times (scratch cleaned on success); both case-study crates clippy-clean; all doc links resolve; no fragile line-number citations. Fixtures (input/reference ~37.5MiB each) GENERATED by the runner not committed (over the policy few-MB ceiling; reproducible by construction, no RNG/clock).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Production witness landed: 32-frame VGA strip (15360x640, 39MB/array) 3x3 blur, 3 cells byte-identical to an independent std-only oracle (naive + distributed x pthreads-async, distributed x mpi-nonblocking -n 5 loopback) at 168-235x the measured startup floor (min-floor; median-floor sensitivity 46-110x reported after the review caught an inverted conservative-claim). Keystone demonstrated on the witness: gate RSS ~88MB FLAT from 16x16 to 15360x640 (expanded projects ~4-12GB — an earlier ~100GB figure was review-corrected as >10x over-extrapolated); wire narrowing 75.0% per edge verified from generated source. HONEST results prominent: distributed SLOWER than naive at this size (correctness-at-scale claimed, not speedup); walls all mapped to existing tasks (0455.04/.14/.15). Fixture outside examples/ (matrix-proof), fixtures generated-not-committed (+gitignore guard), runner hardened (zero-match hard-fail, MPI timing timeout). just case-study green repeatedly. Landed 8f6f5b2 + b12c58e; architect GO.
<!-- SECTION:FINAL_SUMMARY:END -->
