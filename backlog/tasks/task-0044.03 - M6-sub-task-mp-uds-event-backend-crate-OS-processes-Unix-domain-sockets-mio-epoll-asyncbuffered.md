---
id: TASK-0044.03
title: >-
  M6 sub-task: mp-uds-event backend crate (OS processes + Unix domain sockets +
  mio/epoll, async+buffered)
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 09:24'
updated_date: '2026-05-27 09:59'
labels:
  - M6
  - backend
dependencies: []
parent_task_id: TASK-0044
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land the seventh tier-1 backend crate `nucleus/backends/mp-uds-event`: workers are OS processes, transport is Unix domain sockets, notify is mio/epoll event readiness, supports async + buffer (per-(DataId, SeqTag) bounded queue). PRD §7.1 row 7.

This is the ASYNC-side sibling of mp-tcp-event: same multi-process + mio + ring-buffer substrate, but uses Unix domain sockets (`std::os::unix::net::UnixListener` / mio's `UnixListener` wrapper) instead of TCP loopback. UDS is faster than TCP-loopback (no IP stack), supports SCM_RIGHTS for fd-passing (not exercised in v2), and uses filesystem paths instead of port numbers — eliminating the port-picker dance entirely.

## Closest template

`nucleus/backends/mp-tcp-event` is the structural template:
- Same multi-binary output + multi-worker mio reactor.
- Same per-(DataId, SeqTag) ring buffer + epoll readiness gating.
- Same emit() signature + EmitResult shape (worker_bins + wire_rs + runtime_rs + run_sh).

The diff is the transport layer: UnixListener.bind(\"<path>/worker-N.sock\") instead of TcpListener.bind(\"127.0.0.1:<port>\"). mio's UnixListener / UnixStream share the Source trait with TcpListener / TcpStream so the reactor surface stays identical.

This is also a CANDIDATE FOR LIFTING. If the mp-tcp-event reactor / Plan::build code is parametric over the listener type via a trait, the lift may be cheap. If not, the implementer should ASSESS THE LIFT EFFORT BEFORE COPYING and file a separate lift-task if copy-paste exceeds ~200 lines.

## Capabilities

```toml
schema_version  = 1
name            = \"mp-uds-event\"
tier            = 1
transport       = \"uds\"
notify          = [\"event\"]
supports_async  = true
supports_buffer = true
max_buffer      = 64
worker_classes  = [\"default\"]
memory_regions  = [\"heap\"]
```

(Identical to mp-tcp-event except `transport = \"uds\"`. The schedule-visible capability surface is identical.)

## Acceptance criteria

1. `nucleus/backends/mp-uds-event/` crate exists with Cargo.toml + src/lib.rs + capabilities.toml.
2. `mp_uds_event::emit(...)` returns the same EmitResult shape as mp-tcp-event.
3. Driver dispatch arm in `nucleus/driver/src/main.rs` lit (6→7 backends).
4. capabilities.toml satisfies the capability-compat check the same way mp-tcp-event does — every schedule that compiles for mp-tcp-event compiles for mp-uds-event.
5. e2e-matrix.toml gains mp-uds-event cells parallel to mp-tcp-event — at minimum: 01/naive, 02/split, 03/naive+distributed, 04/naive+blocked, 05/naive+blocked+distributed+distributed-2d, 06/naive+blocked+distributed+distributed2, 07/naive+blocked+distributed+distributed-2d, 09/pipelined, 11/pipelined, 13/pipeline_parallel. Each cell bit-identical against reference.bin.
6. run.sh uses filesystem socket paths in a per-run scratch dir (e.g. `${BUILD_DIR}/sock/worker-N.sock`); cleans up on exit (or trap+EXIT cleanup); names sockets to avoid clash under concurrent matrix cells.
7. Unit + integration tests: smoke test for 02-split + 09/pipelined (the canonical M4 ASYNC examples); pin emit string on at least one multi-worker example.
8. Honest limitation note: UDS path-length cap (~104 chars on macOS, ~108 on Linux). Document the scratch-dir path-length budget; reject or truncate if hit.
9. **TASK-0337 promotion-trigger guard**: mp-uds-event inherits the (D)+(B2) compensating-pass tower from mp-tcp-event. IF a UDS-specific schedule shape forces a 5th compensating pass (a new ACFG pass, a new defensive guard, or a new host-relay variant) to land for mp-uds-event to be bit-identical, that IS the promotion trigger named in TASK-0337's 'Promotion triggers' section. STOP and consult the orchestrator before adding compensating pass #5. The honest outcome is to PROMOTE TASK-0337 (Option E full w↔w mesh, the root-cause TASK-0175 closure) and obsolete the tower, NOT silently grow it.

## Honest scope

- mio's UnixListener wrapper requires the `os-ext` feature in some mio versions. Check Cargo dependency.
- SCM_RIGHTS / fd-passing is NOT in scope. UDS is used as a stream socket, identical surface to TCP.
- The cross-backend differential should gain mp-uds-event vs mp-tcp-event as a fourth ASYNC + BUFFERED row (in addition to pthreads-async). This is genuinely new ground; any divergence will surface defects in either the new UDS code OR the existing reactor that were hidden by TCP-only testing.
- run.sh path-cleanup: the e2e harness deletes `target/e2e-matrix/run-*/` on success (memory project-e2e-harness-scratch-lifecycle); the UDS sockets need to live under that scratch dir so cleanup is automatic.

## Cross-reference

- Template: `nucleus/backends/mp-tcp-event/src/lib.rs`, `nucleus/backends/mp-tcp-event/src/multi_worker.rs`, `nucleus/backends/mp-tcp-event/src/runtime.rs`, `nucleus/backends/mp-tcp-event/capabilities.toml`.
- Shared transport infrastructure: `nucleus/mp-tcp-common/` (the wire framing — needs lifting to be transport-agnostic, or a sibling crate `nucleus/mp-uds-common/` may be acceptable; assess).
- Shared backend infrastructure: `nucleus/backend-common/` (memory project-backend-common-crate).
- Driver dispatch: `nucleus/driver/src/main.rs:745-761` (the mp-tcp-event arm shape).
- M4 sibling: `nucleus/backends/mp-tcp-event/`, TASK-0042.02 + TASK-0042.05.
- PRD: §7.1 row mp-uds-event.

## Forward-carried context (orchestrator briefing for the implementer)

- ASSESS THE LIFT BEFORE COPYING. If mp-tcp-event's reactor is monomorphised over TcpListener / TcpStream, the lift-then-instantiate path may be cheaper than copy-paste. If you copy-paste >200 LoC, file a follow-up `TASK-NNNN: lift transport-layer abstraction from mp-tcp-event + mp-uds-event`.
- The cycle-149 + cycle-163 host-relay machinery (project memory project-cross-backend-differential) lives in mp-tcp-event's multi_worker codegen. UDS inherits both the capability AND the compensating-pass tower — verify the host-relay paths still work over UDS sockets, no UDS-specific assumption broken.
- Per memory project-mp-tcp-event-vs-bufsync-safety-profile: the per-seq demux design mp-tcp-event uses (vs the FIFO read order mp-tcp-bufsync uses) MAY mask contract-violation bugs. UDS inherits the demux design; ensure debug_assert! invariants are present in both backends with parallel coverage (any defensive guard in mp-tcp-event must have a sibling in mp-uds-event — memory feedback-silent-sibling-defect).
- include_str! coverage rule + textual-replace-on-codegen rule + narrative-doc-lie rule all apply (see TASK-0044.01/0044.02 briefs).
- TASK-0337 anchor: the (D)+(B2) compensating-pass tower is the de-facto host-relay; mp-uds-event inherits it. If a new UDS-specific schedule shape needs a 5th compensating pass, that is a TASK-0337 promotion trigger.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 175 — SKELETON slice landed; phased-AC addendum (architect P2.2 fold-back)

Cycle 175 delivered the SKELETON slice — third + last M6 backend skeleton, completing the M6 skeleton sweep (cycles 173/174/175 = openmp-rs + mp-tcp-poll + mp-uds-event). Mirrors the established skeleton pattern.

- AC#1 — `nucleus/backends/mp-uds-event/` crate exists with Cargo.toml + src/lib.rs + capabilities.toml. **DONE.**
- AC#2 — `mp_uds_event::emit(...)` returns the same EmitResult shape as mp-tcp-event (multi-binary 7-field with Option<PathBuf> runtime_rs). **DONE** at the shape level; emit() always returns ContractGap in the skeleton phase.
- AC#3 — Driver dispatch arm in `nucleus/driver/src/main.rs` lit (6→7 backends); BACKENDS help-text + 'unknown backend' help-text both extended; module docstring refreshed. **DONE.**
- AC#4 — capabilities.toml satisfies the capability-compat check (smoke-tested via `nucleus build --backend mp-uds-event ... 01-elementwise-add/naive ...` — compat-check passes, then ContractGap with precise forward-link surfaces). **DONE.** First cap-file in the repo to ship transport="uds".
- AC#7 — Unit tests pin emit-string (cycle-175 skeleton tests: ContractGap variant + message contains `mp-uds-event` + forward-links TASK-0044.03; EmitResult 7-field shape via constructor-as-pin). **DONE for skeleton scope.** Multi-worker emit-string pin lands with codegen.

OPEN ACs (deferred to the codegen cycle of TASK-0044.03):

- AC#5 — e2e-matrix cells for mp-uds-event × {01/naive, 02/split, 03/naive+distributed, 04/naive+blocked, 05/naive+blocked+distributed+distributed-2d, 06/naive+blocked+distributed+distributed2, 07/naive+blocked+distributed+distributed-2d, 09/pipelined, 11/pipelined, 13/pipeline_parallel}, each bit-identical against reference.bin. **PENDING** — every cell would currently ContractGap; matrix wiring lands with codegen.
- AC#6 — Per-run scratch-dir socket-path lifecycle. **PENDING** — runtime concern, lands with codegen.
- AC#7-multi-worker — Real emit-string pin on a multi-worker 02-split or 09/pipelined fixture. **PENDING** — lands with codegen.
- AC#8 — UDS path-length cap honest-scope note. **HALF-DONE** — the lib.rs docstring header (`mp-uds-event/src/lib.rs:62-66`) documents the ~104/~108 char cap; the runtime CHECK + reject lands with codegen.
- AC#9 — TASK-0337 promotion-trigger validation (no 5th compensating pass landed silently). **NOT-YET-APPLICABLE** — the skeleton has no compensating-pass exposure; AC#9 only becomes checkable in the codegen cycle when the (D)+(B2) tower is actually exercised. The docstring at `mp-uds-event/src/lib.rs:45-54` documents the inherited promotion-trigger guard so the codegen-cycle implementer sees it during onboarding.

PHASED-AC POLICY (cycle 175 architect P2.2 fold-back, mirroring cycles 173/174 on TASK-0044.01/0044.02): TASK-0044.03 ACs split into skeleton-cycle (AC#1-#4, AC#7-shape) and codegen-cycle (AC#5/#6/#7-multi-worker/#8-runtime/#9-validation) buckets. Do NOT close TASK-0044.03 until the codegen-cycle ACs also land — `feedback-ac-rewrite-on-done-task` recurrence guard.

## Cycle 194 — SINGLE-WORKER arm landed; phased-AC AC#5-subset closed (NO AC#6 — capability surface is async+buffer+event)

Cycle 194 landed the single-worker codegen arm — third + last M6 backend to do so (cycles 191/192/194: openmp-rs / mp-tcp-poll / mp-uds-event, sibling pattern):

- AC#5 (PARTIAL — single-worker subset DONE; multi-worker portion still PENDING under TASK-0044.03.01):
  - **16 [[required]] mp-uds-event cells**, all bit-identical against reference.bin (verified `just e2e` × 3 stable samples post-cycle-194, total 210/161/0/49/0):
    01/naive, 02/naive, 03/naive, 04/{naive, blocked, blocked-nondiv}, 05/{naive, blocked},
    06/{naive, blocked}, 07/{naive, blocked}, 08/naive, 09/naive, 11/naive, 13/naive.
  - 1 implicit-fan-out informational PASS cell (05-stencil/reuse) — bit-identical.
- AC#6 (NOT APPLICABLE): UNLIKE openmp-rs cycle 191 + mp-tcp-poll cycle 192, mp-uds-event has
  ZERO capability-mismatch [[skip]] entries because its capability surface IS async + buffer +
  event (same as mp-tcp-event). All 13 [[skip]] cells are multi-worker-pending, NOT cap-mismatch.
- AC#7 (PARTIAL): single-worker emit pin LANDED (tests/single_worker_emit.rs — two
  byte-identical-vs-mp-tcp-event tests: empty event-list + 01-elementwise-add/naive real
  fixture). Both tests PASS, empirically confirming that the cycle-191/192 newer typed-
  attribute pattern (`render_single_worker_main_with_kernels_attr`) produces byte-equivalent
  output to mp-tcp-event's older `wrap_single_worker` post-hoc `replacen` pattern (the
  cycle-171 honest-scope plan to migrate mp-tcp-event to the typed API is filed separately).
  Multi-worker emit-string pin still pending under TASK-0044.03.01.

OPEN ACs (carry-forward to TASK-0044.03.01):
- AC#5 (multi-worker subset): 13 [[skip]] cells with 'mp-uds-event multi-worker codegen pending'
  reason — 02/split, 03/distributed, 05/{distributed, distributed-2d},
  06/{distributed, distributed2}, 07/{distributed, distributed-2d}, 08/distributed,
  09/pipelined, 11/pipelined, 13/{batch_parallel, pipeline_parallel}. Promotion target on
  TASK-0044.03.01 close.
- AC#7 (multi-worker pin): mirror mp-tcp-event's tests when codegen lands.
- AC#8 (UDS path-cap honesty): the run.sh check + reject paths exceeding ~104/108 chars
  (per cycle-171 brief AC#8). Lands with TASK-0044.03.01.
- AC#9 (TASK-0337 promotion-trigger guard): if a 5th compensating pass is forced to land
  for mp-uds-event bit-identicality, STOP and consult — that IS the promotion trigger.

## Cycle 194 — implementation details

- nucleus/backends/mp-uds-event/src/lib.rs: replaced SKELETON ContractGap with
  single-vs-multi-worker dispatch. Single-worker arm: pthreads-sync's typed-attribute
  renderer + backend-common's multi_binary skeleton + wire.rs verbatim from
  mp_tcp_common::WIRE_RUNTIME_SRC (TCP-specific code, UNUSED — single-process bin does NOT
  `mod wire;`, so cargo's reachability analysis never compiles it into the bin target;
  swaps to UDS-specific content when TASK-0044.03.01 lands). Multi-worker arm: typed
  EmitError::ContractGap forward-linking TASK-0044.03.01. ~240 LoC total.
- nucleus/backends/mp-uds-event/Cargo.toml: added pthreads-sync + mp-tcp-common (runtime
  deps) + test-common + mp-tcp-event (dev-deps).
- nucleus/backends/mp-uds-event/tests/skeleton.rs: rewrote — multi-worker ContractGap pin
  (creates real kernels.rs stub since emit() reads it upfront, mirroring mp-tcp-event /
  mp-tcp-poll structure) + EmitResult shape pin (7 fields including Option<runtime_rs>).
- nucleus/backends/mp-uds-event/tests/single_worker_emit.rs (NEW): empty event-list +
  01-elementwise-add/naive byte-identical-vs-mp-tcp-event tests + runtime_rs.is_none()
  pin for single-worker.
- nuc-nucleus/e2e-matrix.toml: mp-uds-event added to backends list; 16 [[required]] +
  13 multi-worker-pending [[skip]] cells (NO cap-mismatch entries).
- nucleus/driver/src/main.rs + Cargo.toml + nucleus/Cargo.toml: narrative comments updated
  for all three M6 backends now single-worker-LANDED.

## Cycle 194 — review-gate findings

Parallel review gate (qa-test-runner + mped-architect read-only) BOTH GO with finite findings:

- QA: GO, no P1/P2. P3.2 was a driver/src/main.rs:42-44 file-doc still listing mp-uds-event
  as SKELETON — folded into commit.
- Architect P2.1: doc-precision conflation ("byte-identical to mp-tcp-event ... and
  therefore arithmetic byte-identical to pthreads-sync") — TWO distinct invariants
  collapsed into one claim. Tightened: SOURCE-byte-identical vs mp-tcp-event (pinned by
  unit tests); COMPILED-OUTPUT bit-identical vs pthreads-sync (pinned by e2e gate). Source
  diverges (src/main.rs vs src/bin/nuc-generated.rs).
- Architect P2.2: driver mediation-gate forward-link gap. Lines 503/561/668 of
  driver/src/main.rs gate the 3 mediation passes on `backend == "mp-tcp-event"` (or
  "mp-tcp-bufsync"). When TASK-0044.03.01 lands multi-worker mp-uds-event, each gate
  MUST be widened to include mp-uds-event. FILED as cycle-194 architect P2.2 addendum
  on TASK-0044.03.01 as AC#11 (with concrete line citations).
- Architect P3.1: stamp-twice protocol risk on future module-doc edits — noted, no action.
- Architect P3.2: ContractGap message could reference TASK-0044.03.01 AC# (cycle-192 P2.2
  advisory pattern) — noted, no action (consistent with mp-tcp-poll which also doesn't).

## Lessons forward-carried

For TASK-0044.03.01 (multi-worker arm):
- AC#11 (cycle-194 P2.2 fold-back): widen the 3 driver mediation-gate predicates to
  include mp-uds-event. Sibling-sweep verifies no fourth gate site missed. Memory:
  feedback-driver-must-mirror-backend-election-exactly.
- The mp-tcp-event `wrap_single_worker` migration to typed-attribute API is empirically
  proven byte-equivalent by cycle-194's tests; the migration itself is a separate
  follow-up.
- Cross-transport wire honesty: wire.rs content swaps from `mp_tcp_common::WIRE_RUNTIME_SRC`
  to UDS-specific content as part of multi-worker codegen (AC#7 of TASK-0044.03.01).
  Decide between (a) extending mp_tcp_common to be transport-parametric, (b) introducing
  mp_uds_common sibling crate, (c) inline UDS-specific wire emission in mp-uds-event.

## Cycle 194 stats

- e2e: 210/161/0/49/0 (up from cycle 192's 180/144/0/36/0): +30 total, +17 PASS, +13 SKIP.
  Required-fail: 0. Three stable samples.
- Tests: dev 990/0/3 (was 988 pre-cycle), release 989/0/3 (was 987). +4 new mp-uds-event
  tests.
- Filed follow-ups: TASK-0044.03.01 (multi-worker UDS-reactor codegen, HIGH, 11 ACs
  including AC#11 cycle-194 architect P2.2 driver-mediation-gate widening).

## Cycle-191/192/194 sibling pattern observation (orchestrator memory)

All three M6 single-worker landings (openmp-rs / mp-tcp-poll / mp-uds-event) had the SAME
shape: ~640 LoC, +30 e2e cells, 1 multi-worker follow-up filed, similar review-gate
findings (1-2 P2 narrative-drift items each, 0 P1). Per-cycle delta was identical:
+16 [[required]] + +13 [[skip]] + +1 implicit-fan-out. The pattern is now firmly
established as the single-worker-arm landing template; documented in
project-cross-backend-differential memory.

## Cycle 197b closure (orchestrator-direct, M4-capstone-pattern)

All phased ACs now closed. Sub-task ladder:
- TASK-0044.03.01 (multi-worker UDS-reactor codegen) — Done cycle 197/197b (13 [[required]] cells bit-identical).
- TASK-0044.03.02 (lift mp-tcp-event + mp-uds-event multi_worker/ duplication) — LOW prio follow-up, To Do; NOT a parent-close blocker (hardening lift, not a behavioral gap).

AC closure summary (vs cycle-175 + cycle-194 + cycle-197b implementation notes):
- AC#1-#4 + AC#7-skeleton: cycle 175 (skeleton slice).
- AC#5 single-worker: cycle 194 (16 [[required]] cells bit-identical).
- AC#5 multi-worker (13 cells): cycle 197/197b — 02-split-add/split, 03-reduction/distributed, 05-stencil/{distributed, distributed-2d}, 06/{distributed, distributed2}, 07/{distributed, distributed-2d}, 08-histogram/distributed, 09-producer-consumer/pipelined, 11-game-of-life/pipelined, 13-cnn-inference/{batch_parallel, pipeline_parallel}.
- AC#6: cycle 197 (UDS socket paths under per-run scratch dir + path-length cap workaround in run.sh).
- AC#7 multi-worker emit-string pin: cycle 197 (`tests/multi_worker_emit.rs` UDS vs TCP structural twin).
- AC#8: cycle 197 (UDS path-cap honesty: run.sh check + truncation strategy — sun_path 108-byte Linux cap).
- AC#9 (TASK-0337 promotion-trigger guard): NO 4th compensating pass landed silently for mp-uds-event. mp-uds-event inherits the (D)+(B2) tower from mp-tcp-event via shared backend-common helpers — the 3 existing event-backend-gated compensating passes (`apply_host_mediation_inject` at driver/main.rs:555, `apply_host_data_relay_inject` at :608, `apply_safe_push_reorder` at :717) verified sufficient. TASK-0337 remains LOW deferred-not-cancelled per Option E full w↔w mesh anchor. (Cycle-197b architect fold-back P2.1: original closure narrative said "4 passes" with `host_data_relay_inject` double-listed — verbatim-copy doc-lie recurrence, corrected here. The TASK-0337 promotion-trigger phrasing in the brief at AC#9 said "5th compensating pass"; with 3 actual passes the genuine promotion trigger is "4th compensating pass landing", relabelled.)

E2E baseline at close: 210/190/0/20/0 (cycle 197b, 4 non-flake samples). mp-uds-event holds 29 PASS cells across the matrix.

Honest scope held: cycle-197b review gate caught QA NO-GO P1 (textual-replace) + architect 3xP1 verbatim-copy doc-lies (TCP-specific rendezvous mechanism / test-pin citation / status header strings inherited from mp-tcp-event), all folded back into the same cycle. The `feedback-verbatim-copy-comment-doc-lie` recurrence ran 3 firings in this cycle — discipline is improving but copy-cycle sibling implementers REMAIN the highest-recurrence class for doc-lie introduction.

Parent CLOSED. M6 seventh + final tier-1 backend row LANDED. **M6 backend matrix is now structurally complete; the capstone (TASK-0044.07) close depends on TASK-0044.05 + 0044.06 + TASK-0053 AC#4/#5 + TASK-0054 reopen.**
<!-- SECTION:NOTES:END -->
