---
id: TASK-0042.01
title: >-
  M4 sub-task: pthreads-async backend crate (std threads + condvar + ring
  buffer)
status: To Do
assignee: []
created_date: '2026-05-21 13:10'
labels:
  - M4
  - backend
dependencies:
  - TASK-0134
parent_task_id: TASK-0042
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD §7.1 row pthreads-async: std::thread + condvar + ring buffer; supports_async=true; supports_buffer=true. Codegen produces a runtime where each (DataId, SeqTag) buffer place is a bounded ring of D slots; producer Notify fills, consumer Wait blocks on condvar until non-empty. Initial marking from TASK-0134 means the ring starts with D 'free' slots (or D 'pre-filled' tokens — semantics specified there).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 backends/pthreads-async/{Cargo.toml, capabilities.toml, src/lib.rs} land; capabilities.toml: transport=shared, notify=[event], supports_async=true, supports_buffer=true, max_buffer=<chosen>, worker_classes=[default], memory_regions=[heap].
- [ ] #2 EventList contract: backend consumes only the EventList per PRD §7.4 + TASK-0124 (no direct AlgoIR/ACFG access). If new Event variants are needed (BufferPush vs Push etc.), file the variant work as a sibling sub-task and depend on it; do NOT add a side-channel.
- [ ] #3 Negative gate: a schedule with buffer=N too small for the pipeline depth (impossible after TASK-0134, but synthesise a hand-edited fixture) deadlocks; the boundedness pass catches it at compile time. Wire the negative xbackend test.
- [ ] #4 e2e: examples 9 (producer/consumer pipe) and 11 (Game of Life multi-iter) on pthreads-async × pipelined.sched.nuc bit-identical to their reference.bin. Determinism gate byte-identical x2.
<!-- AC:END -->
