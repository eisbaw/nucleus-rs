---
id: TASK-0421
title: >-
  PRD §8.4(a) enforcement gap: no free-choice/conflict-free structural gate on
  the emitted net (the one tractability-restriction with no per-build check)
status: Done
assignee:
  - '@mped'
created_date: '2026-06-02 02:25'
updated_date: '2026-06-02 07:23'
labels:
  - M6
  - compiler
  - petri
  - fail-loud
  - prd-invariant-audit
  - cycle-241
dependencies: []
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD-invariant audit (cycle-241) finding GAP-1, VERIFIED. PRD §8.4 names FOUR restrictions that keep the net tractable: (a) statically-determined firing order / NO free-choice, NO confusion, NO conflicts; (b) bounded-by-construction; (c) acyclic event DAG; (d) no coloured/stochastic/hierarchical. (b) and (c) have real per-build gates (check_bounded / check_deadlock_free, run every build at driver/src/main.rs:626, both bite-tested). (d) is a TYPE-LEVEL guarantee (Marking=BTreeMap<PlaceId,u32>; no colour/time/probability fields in petri.rs Place/Transition/Arc). But (a) — the precondition the WHOLE single-order-replay soundness argument explicitly rests on (cited verbatim at passes/net_soundness.rs:30-31 and petri.rs:14) — has ZERO asserting code. VERIFIED: grep of nucleus-compiler/src for free.?choice|conflict.?free|conflicting|confusion returns only docstring prose + the UNRELATED SchedLowerErrorKind::ConflictingTransferMode. So the soundness PRECONDITION is assumed-by-construction with no tripwire: a future inject-pass change producing a net where a place feeds two distinct enabled transitions (a free-choice conflict) would silently violate the precondition, and the boundedness/deadlock gates would still pass on their one arbitrary order while the real net is nondeterministic.

SCOPE: add a structural soundness check (e.g. check_statically_ordered / check_conflict_free) that verifies the emitted net actually has the §8.4(a) shape; fail-loud as a new typed error (e.g. PetriAnalysisError::ConflictingChoice { place, transitions }); wire into check_net_sound so it runs EVERY build (mirroring check_bounded/check_deadlock_free). Bite-test with a hand-built free-choice net (mirror the existing tests/net_soundness.rs unsound-net reject pattern).

CRITICAL DESIGN (panic-on-valid-input risk, TASK-0419 precedent): the conflict-free PREDICATE must be defined correctly for v2 statically-ordered nets and MUST NOT false-reject any shipping net. A place feeding >1 transition is NOT automatically a violation if the static firing order serialises them. MANDATORY PRECONDITION: empirically verify NO net in the e2e corpus (385 cells) trips the new check before wiring it into the gate (instrument + run, like TASK-0419 AC#1). If a legitimate multi-out-arc shipping net exists, the predicate must account for the static-order serialisation, not naively reject. 

EXPECTED DISPOSITION: like check_bounded/check_deadlock_free and TASK-0281/0419, this is likely a PROVABLY-DEAD-TODAY tripwire (net_soundness.rs:80-85 argues the inject passes make conflicts structurally impossible today) — that is the accepted project pattern (a conservative soundness tripwire), and the asymmetry that (a) is the ONE §8.4 restriction with no gate is the argument for filing it. Honest value: MEDIUM (closes the §8.4 enforcement set; makes the soundness precondition CHECKED not assumed).

Pointers: nucleus/nucleus-compiler/src/passes/net_soundness.rs (the check_net_sound aggregator + the soundness-precondition prose at :30 and :80-85); src/passes/boundedness.rs + deadlock.rs (the sibling gate pattern to mirror); src/petri.rs (Net/Place/Transition/Arc structure); tests/net_soundness.rs (bite-test template).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Empirical precondition DISCHARGED FIRST (before defining the predicate, TASK-0419 discipline): characterize whether any net in the e2e corpus (385 cells) has a place with PtoT arcs to >=2 distinct transitions (the naive structural-conflict shape). Instrument + run over the corpus; record the finding verbatim in notes. The correct conflict-free predicate is DERIVED from this finding (pure-structural if no shipping net has multi-out places; order-aware/projection-aware if some legitimately do).
- [x] #2 A structural soundness check (e.g. check_conflict_free) verifies the emitted net satisfies PRD §8.4(a) no-free-choice/no-conflict, failing loud as a NEW typed PetriAnalysisError variant (e.g. ConflictingChoice) naming the offending place + the >=2 transitions. The predicate MUST NOT false-reject any shipping net (per AC#1 finding).
- [x] #3 Wired into check_net_sound (net_soundness.rs) so it runs EVERY build, mirroring check_bounded/check_deadlock_free; driver stringifies it (no separate driver wiring beyond the existing check_net_sound call).
- [x] #4 Bite test: a hand-built free-choice/conflict net (a place feeding 2 transitions) trips the check; demonstrably PASSES (no rejection) if the check is removed (prove-the-check-bites). Mirror the tests/net_soundness.rs unsound-net reject pattern.
- [x] #5 Gate green: nix develop -c just build clippy test test-release e2e all pass; e2e baseline 385/328/0/57/0 HELD (the new check rejects ZERO shipping nets — if it would reject one, that is the signal the predicate is wrong, NOT that the net is unsound; refine or return BLOCKED with the data). cargo doc warning count unchanged (10) if a doc-linked symbol is touched.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
AC#1 (FIRST, empirical): derive the conflict-free predicate from reality. Structural reading of acfg_to_petri.rs: (i) control places are consumed by exactly ONE transition each (thread_through_worker allocates a fresh next_pid every call, advances head; seed ctl_*_0 consumed once) -> control places never multi-out by construction; (ii) buffer places (keyed by SeqTag) are consumed (PtoT) by every Wait sharing that seq. Repeat UNROLLS in acfg_to_net (walk body N times) while seq is fixed per Push/Wait pair -> a buffer place inside an unrolled loop legitimately has PtoT arcs to N distinct unrolled Wait transitions. THEREFORE the naive predicate (place with PtoT to >=2 distinct transitions = conflict) WILL false-reject every shipping net with an in-loop transfer. Must instrument the real pipeline to confirm + measure, then choose an ORDER-AWARE predicate: a place is a genuine free-choice CONFLICT only if >=2 of its PtoT-consumer transitions are SIMULTANEOUSLY enabled at some reachable marking under the derived firing order (i.e. token availability, not compile-time order, decides which fires). For v2 statically-ordered nets the derived order serialises the unrolled waits (each waits on its own control-chain predecessor), so they are never co-enabled -> no conflict. Probe: env-gated NUC_DUMP_CONFLICTS dump in driver after gate_net build, drive nucleus build directly per (example,schedule,backend), capture stderr, record verbatim. AC#2: check_conflict_free(net) -> Result<(),PetriAnalysisError::ConflictingChoice{place,place_name,transitions}> using the co-enabled-under-derived-order predicate (reuse derive_firing_order + replay; at each marking, if >=2 PtoT-consumers of one place are enabled, that is the conflict). AC#3: wire into check_net_sound FIRST (before bounded/deadlock — conflict-freedom is the precondition the single-order replay rests on); add variant+Display. AC#4: bite test hand-built free-choice net (one place, marking>=2, two enabled consumers, no control serialisation) trips it; prove it bites (passes if check removed); positive test: shipping-shaped unrolled-loop net passes. AC#5: full gate, e2e 385/328/0/57/0 held x2.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
AC#1 EMPIRICAL FINDING (verbatim). Probe: env-gated NUC_DUMP_CONFLICTS dump in driver (after gate_net = acfg_to_net, before check_net_sound) reporting per build: place/transition/arc counts, naive_multiout_places (places with PtoT arcs to >=2 DISTINCT transitions), and the ORDER-AWARE check_conflict_free verdict. Swept the full corpus: every example x every schedule x 7 tier-1 backends via direct nucleus build --emit-pn (stderr captured directly, NOT through just e2e which discards stderr). Corpus: 316 net-gate builds + 69 capability-skip builds (rc=1, backend rejects schedule BEFORE net construction e.g. async/buffered transfer on a sync backend -> net legitimately never built; these are the e2e SKIPPED cells). RESULT: order_aware_verdict=OK on ALL 316 builds; ZERO CONFLICT verdicts. BUT 13 cells (across 09-producer-consumer/pipelined, 13-cnn-inference/pipeline_parallel, 16-jacobi/distributed) DO have naive structural multi-out places: a buffer place keyed by SeqTag, consumed by up to 16 DISTINCT unrolled Wait transitions (e.g. buf_stream_seq1 consumer_count=16; 16-jacobi has 12 buf_field_seq* places x 5 consumers each). ROOT CAUSE: acfg_to_petri UNROLLS Repeat bodies (TASK-0026) while SeqTag is fixed per Push/Wait pair, so an in-loop transfer emits one buffer place fed by N pushes + consumed by N waits. CONCLUSION: a NAIVE structural predicate (place with PtoT to >=2 distinct transitions = conflict) would FALSE-REJECT all 13 cells (the exact panic-on-valid-input risk). The N unrolled waits are NOT a free-choice conflict: each is gated behind its own per-worker control-place predecessor, so at any reachable marking at most ONE is enabled. THEREFORE the predicate must be ORDER/REACHABILITY-AWARE: replay derive_firing_order and at each reachable marking check no place has >=2 consumer transitions simultaneously consumption-enabled. Verified the derived order fires FULLY on the densest case (16-jacobi/distributed: nucleus ok => bounded+deadlock+conflict all pass => scan covered every reachable marking and still found no co-enabled pair). DISPOSITION: provably-dead-today tripwire (control-place threading makes conflicts structurally impossible on every shipping schedule today) — same family as check_bounded/check_deadlock_free. Probe scaffolding is TEMP and removed before final commit.

GATE (all green, inside nix dev shell). just build: ok. just clippy: clean under -D warnings. just test (dev): 1250 passed / 0 failed / 118 binaries (incl. boundedness 15/15 with the gate_stays_near_linear_under_large_net perf-pin, net_soundness 7/7). just test-release: 1248 passed / 0 failed (profile-gated should_panic delta is expected). just e2e x2: 385/328/0/57/0 BOTH runs (baseline HELD byte-identical; ZERO shipping nets rejected). cargo doc: 10 individual rustdoc warnings (baseline HELD); new ConflictError/ConflictingChoice/check_conflict_free doc-links resolve cleanly, zero new warnings. PERF NOTE (important): first dev-profile just test run hit the gate_stays_near_linear_under_large_net pin (TASK-0377) because the initial check_conflict_free added a redundant 4th full O(T deg) replay (its own derive_firing_order + per-position fire). FIXED: (1) fast path returns Ok in O(A) when no place has >=2 consumers (every shipping net + the perf-pin net) with NO replay; (2) check_net_sound derives the order ONCE and shares it with all three passes via check_conflict_free_with_order; (3) per-transition needs precomputed O(A), so is_consumption_enabled is O(deg) not O(A). Dev-profile perf-pin now 0.40s (ceiling 2s). bite re-proven after refactor. SEPARATE FLAKE (pre-existing, UNRELATED): nucleus/backends/mp-tcp-bufsync/tests/check_frame_emit.rs intermittently fails under parallel just test with NotFound on kernels_stub write (shared-parent scratch_dir race); reproduced on CLEAN tree (stash-pop); different test each run; passes in isolation 3/3. Filed TASK-0426. Not caused by this change (different crate, no fs code).

ORCHESTRATOR REVIEW GATE (phase3-ralph, parallel read-only, range 13e38b8..cbf3419) — both GO. mped-architect: code SOUND (predicate, O(A) fast-path, wiring-first all correct; bite + positive no-false-reject tests genuine; AC#1 reasoning sound) but flagged P1 doc-OVERCLAIM on a soundness gate: check_conflict_free rides the SINGLE derive_firing_order, so it is sound-in-the-safe-direction (never false-rejects) but INCOMPLETE — architect built a concrete counterexample (s1:1,s2:1,p(cap2):0; derived order drains p via cons_x before load2 raises it to 2, so the p=2 co-enabled marking is reachable but never visited -> conflict missed). FOLD-BACK db25239 (in-thread, doc-only): scoped 3 overclaims to the honest under-approximation + added an Honest-limitation section mirroring deadlock.rs; filed TASK-0427 for the coverability-complete version (low, dead-today-inert). qa-test-runner INDEPENDENTLY RE-RAN on the folded-back state: build+clippy clean (forced recompile, large docstring did NOT trip doc_lazy_continuation); just test 1250/0/3 dev; just test-release 1248/0/3; 3 new tests run+pass unfiltered (free_choice_conflict_net_rejected bite + free_choice_net_is_flagged_by_conflict_pass_directly + shipping_shaped_unrolled_loop_buffer_passes_no_false_reject positive); just e2e 385/328/0/57/0 x2 byte-identical, ZERO shipping nets false-rejected (pass count exactly 328, no cell newly errors at net-soundness); TASK-0377 perf-pin gate_stays_near_linear_under_large_net green both profiles (well under 2s); TASK-0426 flake unreproducible in 6 attempts. RECORDED NUMBERS ARE REVIEWER-RE-RUN, not implementer-claimed.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Closed the last PRD §8.4 enforcement gap: §8.4(a) no-free-choice/conflict-free now has a per-build structural gate (was prose-only; b/c had gates, d is type-level). Added check_conflict_free + PetriAnalysisError::ConflictingChoice(ConflictError::FreeChoice{place,place_name,transitions,position}) to passes/net_soundness.rs, wired as the FIRST check in check_net_sound (conflict-freedom is the precondition the single-order bounded/deadlock replay rests on). AC#1-derived predicate is REACHABILITY-AWARE not naive-structural: empirical sweep of the full corpus (316 net-gate builds x 7 tier-1 backends) found 13 shipping cells with naive multi-out buffer places (up to 16 unrolled-Wait consumers from one in-loop transfer SeqTag) that a structural predicate would FALSE-REJECT — but ZERO have two co-enabled consumers under the derived firing order (the unrolled waits are serialised by per-worker control chains). Provably-dead-today conservative tripwire (same family as check_bounded/check_deadlock_free); catches a future inject-pass regression that the order-replay gates would silently pass. Bite test + prove-it-bites (disabling the arm mislabels the free-choice net as Deadlock(Stalled), test FAILS) + positive no-false-reject test on a shipping-shaped unrolled-loop net. Gate: build/clippy/test(1250)/test-release(1248)/e2e 385/328/0/57/0 x2 all green; cargo doc 10 (held). Driver untouched (flows through existing check_net_sound stringify). Filed TASK-0426 for a pre-existing UNRELATED mp-tcp-bufsync test-scratch parallel filesystem flake surfaced during the gate.
<!-- SECTION:FINAL_SUMMARY:END -->
