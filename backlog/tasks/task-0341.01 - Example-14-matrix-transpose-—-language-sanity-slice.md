---
id: TASK-0341.01
title: Example 14 (matrix transpose) — language-sanity slice
status: In Progress
assignee:
  - '@orchestrator'
created_date: '2026-05-26 11:49'
updated_date: '2026-05-27 22:03'
labels:
  - examples
  - coverage
  - M6-companion
dependencies: []
parent_task_id: TASK-0341
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Background

Matrix transpose: out[j][i] = in[i][j]. Pure permutation primitive — affine 2D indexing with axis-swap. Sits between 01-elementwise-add (1D unit-stride) and 07-matmul (full multiply with reduction) in the existing matrix.

Filed under TASK-0341 (showcase example expansion).

## Why this example

Exercises NON-TRIVIAL 2D access patterns that no existing example covers:

- The producer write is unit-stride in axis-0 of `out` (good cache locality).
- The consumer read is unit-stride in axis-0 of `in` (also good).
- BUT producer-write and consumer-read see DIFFERENT row-major linear orders — this is precisely the 'transpose pattern' that surfaces tile-tiling tradeoffs in real implementations.

For a 16x16 input on 4 workers with partition=rows on the OUTPUT, each worker writes 4 contiguous output rows. To populate those, each worker must READ 4 columns of input — a strided access pattern. This is the structural test partition_workers + partition_rows has NOT yet been exercised on (since 06/separable-filter has 2 sequential row-only passes, not a row-vs-column dependence).

## What's substantively new vs existing examples

- vs 01-elementwise-add: 2D instead of 1D; cross-axis access dependency.
- vs 07-matmul: NO reduction; pure permutation. Lighter weight to test scheduling primitives in isolation.
- vs 06-separable-filter: the cross-axis structure makes partition=rows on `out` REQUIRE strided read of `in` (where 06 has both passes row-aligned).

## What this exercises in the compiler

- partition_workers on the OUTPUT dimension where input has different layout.
- transfer_inject for cross-worker INPUT data fan-in (each output worker needs columns from every input worker).
- safe_push_reorder (cycle 162) — likely no impact; transpose has no Loop bodies with w2w Push.
- host_data_relay_inject (cycle 163) — also likely no impact; no in-Repeat-body w2w Push.

If this example lowers and runs bit-identically, it validates that the cycle-147-onward cross-worker transfer machinery handles the 'output-driven partition with strided input' case.

## Honest gotchas

- Determining bit-identical output across backends is trivial here (integer permutation; no FP).
- If the DSL can't express the partition=rows-on-output with cross-column-read pattern cleanly, the naive (single-worker) schedule still passes pthreads-sync, but the multi-worker schedules become follow-ups.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 AC#1 language-sanity: examples/14-transpose/prog.algo.nuc declares a function over a 2D array that swaps axes; examples/14-transpose/schedules/naive.sched.nuc lowers cleanly under pthreads-sync; e2e cell '14-transpose | naive | pthreads-sync' PASSES bit-identical against reference.bin. Defer kernels.rs / reference / binary data authorship to this cycle (smallest deliverable).
- [x] #2 AC#2 multi-worker distributed schedule (partition=rows on OUTPUT): files schedules/distributed-rows.sched.nuc with partition=rows on output dimension and worker count {host,w0..w3}; cell '14-transpose | distributed-rows | pthreads-sync' PASSES bit-identical OR honest-BLOCKED with a precise filed follow-up task naming the DSL feature gap.
- [x] #3 AC#3 cross-backend differential: AC#1 cell expanded to all 4 tier-1 backends (pthreads-sync / pthreads-async / mp-tcp-bufsync / mp-tcp-event); all PASS bit-identical against single reference.bin. The 3 M6 skeleton backends (openmp-rs / mp-tcp-poll / mp-uds-event) remain [[skip]] until their substantive codegen lands per the existing M6 path.
- [x] #4 AC#4 cycle-178 doc-lie-promotion mitigation: any //! module-level docstring in 14-transpose/{prog.algo.nuc,kernels.rs,schedules/*.sched.nuc} is present-tense, cites the landing cycle, and contains NO predictive claims about future schedules. Architect must verify on review.
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Cycle-NNN plan (orchestrator-direct, per memory feedback-spawned-agents-refuse-code-edits — implement in-thread, read-only reviewer gate after).

EXAMPLE NUMBER: 15-transpose (NOT 14-transpose as cycle-178-filed AC#1 said). Example 14 was taken by hearing-aid (TASK-0054 landed cycle 201, after this task was filed). Per parent TASK-0341 'each sub-task should propose its number': 15 is the next slot.

SCOPE (AC#1 only): language-sanity slice — pthreads-sync naive only. Defer AC#2 (distributed schedule), AC#3 (cross-backend differential), AC#4 (doc-lie mitigation review) per follow-up cycles. AC#4 is partly self-enforced in this cycle (any //! docstring authored here will be present-tense + cycle-citation + no predictive claims).

DIMENSIONS: H=8 (input rows), W=16 (input cols); output is W-rows by H-cols (16x8). Non-square so axis-swap is structurally observable. 8*16*4=512 bytes per array → 1 KiB input.bin + 512 B reference.bin. Inspectable by hand.

KERNEL SHAPE: `kernel xpose : (i32) -> i32 pure;` — identity passthrough. Bare-LValue RHS (`output[j][i] <-- input[i][j]`) is REJECTED by intent: acfg::build::build_dataflow skips non-Call RHS (acfg/build.rs:325-327 '// Identity copy or pure-expression RHS: skipped at M1.'), so an identity-copy dataflow would emit NO ACFG operation and the output would never be written. The kernel form is mandatory.

I32 type (not f32): no reduction means f32 would also be deterministic, but PRD §10.1 consistency with examples 01/02/03/05 + integer wraparound documents intent.

DELIVERABLES:
1. examples/15-transpose/prog.algo.nuc — H=8, W=16, data input:i32[H][W] data output:i32[W][H], kernel xpose:(i32)->i32 pure, load_input/save_output effectful, 2D for-nest writing output[j][i] <-- xpose(input[i][j])
2. examples/15-transpose/kernels.rs — xpose identity, load_input (reads H*W i32 LE), save_output (writes W*H i32 LE), wrapping_add not needed (no compute), NUC_INPUT_PATH/NUC_OUTPUT_PATH env-overrides
3. examples/15-transpose/schedules/naive.sched.nuc — single host worker; place xpose/load_input/save_output on host; no loop transforms; no transfers
4. examples/15-transpose/input.bin — H*W i32 LE words; pattern: in[i][j] = (i * 31 + j * 7) & 0xFF (varied across both axes; never constant; not symmetric so an axis-swap bug observable)
5. examples/15-transpose/reference/{Cargo.toml,src/main.rs} — standalone reference (--in --out args), reads H*W i32 LE words row-major in[i][j], writes W*H i32 LE words row-major out[j][i] = in[i][j]
6. examples/15-transpose/reference.bin — committed binary (regenerated from reference crate)
7. examples/15-transpose/README.md — narrative: what/why/I-O format/regen instructions/numeric choice/kernel-vs-bare-LValue rationale; modeled on 01-elementwise-add/README
8. nuc-nucleus/e2e-matrix.toml — runnable_examples += "15-transpose"; [[required]] '15-transpose | naive | pthreads-sync' milestone=M6 (post-M6 hardening wave)

VERIFICATION:
- `nix develop --command bash -c 'just build && just clippy && just test && just test-release && just e2e'`
- e2e baseline pre-cycle (recorded in TASK-0345 cycle 203): expected 120/110/0/10/0 → after this cycle: 121/111/0/10/0 (+1 [[required]] pthreads-sync cell). Defer cross-backend cells to AC#3 follow-up.

HONEST SCOPE LIMITS:
- AC#1 only; AC#2/3/4 deferred to separate cycles (file follow-ups in final summary).
- xpose kernel is identity; the example exercises 2D access-pattern + axis-swap, NOT scalar compute (intentional — the showcase point is the dataflow shape, not arithmetic).
- partition=rows on output (AC#2) is the structural test for output-driven partition with strided input read; deferred — needs precise scoping after AC#1 verifies the naive lowering path.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Cycle 204 closure (orchestrator-direct) ===

AC#1 LANDED — pthreads-sync naive cell PASSES bit-identical.
Empirical observation: 6 other tier-1 backends ALSO PASSed bit-identical informationally in cycle 204's just e2e (shared single-worker renderer makes single-`host` schedules byte-identical by construction). Formal AC#3 promotion deferred to TASK-0341.01.02.

DELIVERABLES:
- nuc-nucleus/examples/15-transpose/{prog.algo.nuc, kernels.rs, schedules/naive.sched.nuc, README.md, input.bin (512 B), reference.bin (512 B), reference/{Cargo.toml, Cargo.lock, src/main.rs}}
- nuc-nucleus/e2e-matrix.toml: runnable_examples += "15-transpose"; [[required]] `15-transpose | naive | pthreads-sync` milestone=M6.

NUMBER RENUMBER: 14 → 15 because hearing-aid took the 14-slot at TASK-0054 cycle 201 (after this task was filed). Title in tracker still says "14" but body and code use 15.

e2e BASELINE: 238/211/0/27/0 (cycle 201 post-hearing-aid baseline; was 120/110/0/10/0 in the Implementation Plan which is now stale) → 245/218/0/27/0 (this cycle, +7 PASS across all 7 backends). Stable across 2 e2e samples.

KERNEL-OVER-BARE-LVALUE RATIONALE: acfg::build::build_dataflow (acfg/build.rs:325-326) skips non-Call RHS at M1; the bare-LValue identity-copy form (`out[j][i] <-- in[i][j]`) produces no ACFG Operation. Per cycle-77 closure note on TASK-0111/0097 ("reopening means filing a single fresh scope-derived task"), the 15-transpose discovery is the trigger; reopened as TASK-0347.

REVIEW GATE RESULTS:
- qa-test-runner: GO. e2e 245/218/0/27/0 stable across 2 samples. Cmp pthreads-sync emit vs reference.bin — IDENTICAL. P3 only (cross-example reference-drift sweep deferred, README TASK-0111 framing minor).
- mped-architect: GO. P2.1+P2.2 (follow-ups not yet filed at gate time; tracker not yet closed) → folded in this commit. P3.1+P3.4 doc-lie fold-ins applied (kernels.rs:14-26 "any future schedule" + README:28-31 "Likely byte-identical" + README:140 "When TASK-0111 lands" all rewritten present-tense). P3.2 acfg/build.rs citation 325-327 → 325-326. P3.3/3.5 cosmetic — left in commit message + tracker; no in-cycle fix.

FORWARD-CARRY LESSONS (next sub-task implementers):
- "any future schedule" / "when TASK-X lands" predictive-tense docstrings are AC#4 violations even in `//`-comments (not just `//!`). Write present-tense + cycle citation only.
- Implementation Plan baseline (120/110/0/10/0 in this task) was stale on land because the plan was filed at cycle 178+ and 13 cycles passed before execution. Land-time addendum (or re-plan before execute) is the discipline.
- The number renumber (14→15) is a pattern future sub-tasks will hit: each TASK-0341.0X should propose its number AT LAND, not at filing time (parent TASK-0341 already says this).

FOLLOW-UPS FILED:
- TASK-0341.01.01 — AC#2 distributed-rows on output dim (multi-worker, partition=rows; structural test for "output-driven partition with axis-swapped input read")
- TASK-0341.01.02 — AC#3 cross-backend promotion (formal [[required]] across the 3 substantively-coded tier-1 backends plus optional 3 M6 skeleton backends)
- TASK-0347 — ACFG + link identity-copy reopen (cycle-77 DEFERRED trigger fired)

AC#4 (doc-lie mitigation): in-cycle compliant after the P3.1 fold-ins. No follow-up filed.

=== Cycle 204 closure status correction ===

Reverted Status to In Progress (was briefly marked Done above). AC#1 [x] is genuinely closed; AC#2 / AC#3 / AC#4 still [ ] with follow-ups filed:
- AC#2 → TASK-0341.01.01 (distributed-rows schedule)
- AC#3 → TASK-0341.01.02 (cross-backend promotion)
- AC#4 → in-cycle compliant after the cycle-204 fold-back (no separate task)

Per honest-failure-discipline (Phase-3 norm + project memory feedback-ac-rewrite-on-done-task), a parent with open ACs stays In Progress until its follow-ups close, NOT Done with redirect annotations. TASK-0341.01 will be marked Done after AC#2 (TASK-0341.01.01) AND AC#3 (TASK-0341.01.02) both close. If a future architect P-finding triggers an in-cycle correction to AC#1, that correction also stays in this same node.

=== Cycle 205 closure addendum (orchestrator-direct, architect P2 fold-back) ===

AC#3 [x] via TASK-0341.01.02 cycle 205. Architect P2 review-gate finding: AC#3's original wording (filed cycle 178, before M6 multi-worker codegen landed) said "the 3 M6 skeleton backends (openmp-rs / mp-tcp-poll / mp-uds-event) remain [[skip]] until their substantive codegen lands per the existing M6 path." That stipulation is now stale:
- openmp-rs multi-worker codegen LANDED cycle 196 (TASK-0044.01.01).
- mp-tcp-poll multi-worker codegen LANDED cycle 195 (TASK-0044.02.02).
- mp-uds-event multi-worker codegen LANDED cycle 197 (TASK-0044.03.01).

So at cycle 205 there are no "M6 skeleton" backends left — all 7 tier-1 backends carry substantive codegen. The 8x non-flake e2e evidence supports promoting all 7 (not just the 4 the original AC#3 stipulated). The wider promotion exceeds the AC's minimum requirement and is justified by the empirical evidence + the cycle-196/197/198 closure of those backends' multi-worker arms.

In-place AC#3 text was NOT rewritten (per project memory feedback-ac-rewrite-on-done-task: prefer closure-addendum over in-place rewrite on AC text drift); this addendum records the wording-vs-reality delta.

=== Cycle 215 closure addendum (TASK-0341.01.01 Done) ===

AC#2 closed by child TASK-0341.01.01 cycle 215. The empirically-landed schedule uses 'partition=workers on the inner j-loop' NOT 'partition=rows on output dimension' as the AC#2 text originally said.

Why the wording mismatch is OK to leave in-place (per memory feedback-ac-rewrite-on-done-task):
- The CONCEPTUAL intent (row-band partition of the output's row axis) is preserved.
- The actual grammar directive 'partition=rows' is structurally outer-of-2D-only per partition_rows.rs:18-30 ('On a 1D loop, partition=rows is a category error per PRD §6.3.3'). The transpose's output-row axis is  = INNER loop, so partition=workers via find_loop's recursive descent is the correct primitive.
- Cycle 215's child task TASK-0341.01.01 used 'partition=workers' in its structured AC#1 — the wording-vs-reality delta is documented in the child's final summary + this addendum.

Cross-backend promotion is the AC follow-out TASK-0341.01.01.01.

NOT in-place rewriting AC#2 text per the cycle-126 AC-rewrite-on-Done-task discipline.

=== Cycle 218 closure addendum (AC#4 doc-lie-promotion mitigation, completed across cycles 217b + 218) ===

AC#4 closed across cycles 217b + 218.

15-transpose //! module-level docstrings (prog.algo.nuc + kernels.rs + schedules/*.sched.nuc + README.md) are now present-tense and cite landing cycles:
- prog.algo.nuc:20-32: cycle 215 (TASK-0341.01.01) + cycle 216 (TASK-0341.01.01.01) + cycle 205 (TASK-0341.01.02) landings.
- schedules/distributed-rows.sched.nuc:72-79: cycle 215 + cycle 216 dual-citation.
- README.md:11-44 + 178-188 (the cycle-217b silent-sibling site at line 178): cycle 215 + cycle 216 dual-citation.

The cycle-217b architect-caught silent-sibling defect at README.md:178 was folded back in cycle 217b commit d35ad6d. Cycle 218 verified via whole-file grep that no stale 'follow-up' / 'yet' claims remain (2 hits, both legitimately not-stale per the cycle-217b ground-truth-evaluation discipline).

The AC#4 text says '14-transpose' but the example landed as 15-transpose (documented in cycle-204b notes per project memory).

NOT in-place rewriting AC#4 text per memory feedback-ac-rewrite-on-done-task; this addendum records closure.
<!-- SECTION:NOTES:END -->
