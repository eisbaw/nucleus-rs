---
id: TASK-0343.05
title: >-
  Lift mp-tcp-bufsync local collect_pre_init_sets to backend_common (architect
  cycle-189 P3.1 silent-sibling-risk)
status: Done
assignee:
  - '@orchestrator'
created_date: '2026-05-26 23:38'
updated_date: '2026-05-26 23:55'
labels:
  - hygiene
  - silent-sibling
dependencies: []
parent_task_id: TASK-0343
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect cycle-189 P3.1 review finding (TASK-0343):

nucleus/backends/mp-tcp-bufsync/src/walkers.rs:370-394 is line-by-line identical to nucleus/backend-common/src/multi_worker_walker/collect.rs:284-308 (collect_pre_init_sets). The other 3 tier-1 backends (pthreads-sync, pthreads-async, mp-tcp-event) consume the shared backend-common helper; mp-tcp-bufsync silently shadows it via its own crate::walkers::collect_pre_init_sets.

Pre-existing (not introduced by cycle 189) but matches the feedback-silent-sibling-defect class. A future fix on backend-common's version would silently skip mp-tcp-bufsync.

Lift mp-tcp-bufsync's local copy to consume the shared backend-common helper, then delete the duplicate (or leave a re-export comment in walkers.rs if the symbol is used elsewhere). Verify no behaviour change end-to-end.

Cross-references:
- TASK-0343 cycle 189 architect P3.1 review finding.
- feedback-silent-sibling-defect (memory).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 190 close — duplicate lifted

Single behaviour-preserving change: deleted 25-LoC duplicate of `collect_pre_init_sets` in `nucleus/backends/mp-tcp-bufsync/src/walkers.rs`; pointed the sole caller (`plan/relay.rs:159`) at the shared `backend_common::multi_worker_walker::collect_pre_init_sets` helper that the other 3 tier-1 backends already consume.

### Gate (orchestrator-self-run, architect-verified)
- cargo build --workspace: OK
- cargo clippy --workspace --all-targets -- -D warnings: 0 warnings
- cargo test --workspace (dev): 984/0/3 PASS (identical to cycle 189 baseline)
- cargo test --workspace --release: 983/0/3 PASS (identical to cycle 189 baseline)
- cargo run --release --bin nucleus-e2e: 120/110/0/10/0 (identical to cycle 189 baseline; required-fail: 0)

The e2e identity vs cycle 189 baseline empirically confirms the lift is behaviour-preserving end-to-end. Architect read-only review (mped-architect, single-agent — QA half orchestrator-self-performed since the change is trivial behaviour-preserving deletion): GO. Byte-equivalence verified (deleted block was line-by-line identical to shared helper modulo `pub` vs `pub(crate)` visibility — pub is strictly more permissive); no missed call site; pointer-comment in walkers.rs is accurate (no comment-doc-lie).

Closes the cycle-189 architect P3.1 silent-sibling-risk: a future fix to the shared helper would no longer silently skip mp-tcp-bufsync.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 190: deleted mp-tcp-bufsync's 25-LoC local duplicate of `collect_pre_init_sets` (walkers.rs:370-394); pointed sole caller (plan/relay.rs:159) at the shared backend-common helper. Behaviour-preserving (e2e 120/110/0/10/0 unchanged); architect GO. Closes the cycle-189 P3.1 silent-sibling risk on the canonical code path.
<!-- SECTION:FINAL_SUMMARY:END -->
