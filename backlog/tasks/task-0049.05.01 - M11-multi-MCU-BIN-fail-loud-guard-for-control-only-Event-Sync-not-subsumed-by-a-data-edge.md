---
id: TASK-0049.05.01
title: >-
  M11 multi-MCU BIN: fail-loud guard for control-only Event::Sync not subsumed
  by a data edge
status: Done
assignee:
  - '@claude'
created_date: '2026-05-31 11:06'
updated_date: '2026-06-03 21:09'
labels: []
dependencies: []
parent_task_id: TASK-0049.05
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Architect P1 on TASK-0049.05 (commit e8ae76d). The multi-MCU BIN MultiMcuShim lowers EVERY Event::Sync to a no-op irq_barrier (render.rs ~:369). This is value-correct ONLY when the barrier ordering is already subsumed by a blocking Push/Wait data edge — true and VERIFIED for 02-split-add (its tag0/1/2 Syncs are control-only but data-edge-redundant; byte-exact diff holds). But the no-op is UNCONDITIONAL: a schedule whose correctness needs a standalone control barrier with NO co-located data edge would be SILENTLY miscompiled. The compiler can distinguish the two cases (happens-before subsumption analysis). Add a typed EmitError on the multi-MCU BIN path that REJECTS a control-only Sync whose ordering is not provably carried by data edges (matching the repo fail-loud-tripwire discipline; cf. TransferInjectError::CumulativeWholeArrayFallback precedent). NB a naive "reject any Sync" is WRONG — it would reject the value-correct 02-split-add; the guard must detect non-redundant barriers. Mitigation today: the tier-3 acceptance is always the byte-exact reference.bin diff, which fails loud if the no-op breaks a schedule — so the risk is bounded to trusting BIN output without that diff.
<!-- SECTION:DESCRIPTION:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
ALGORITHM (orchestrator-derived from 02-split-add GROUND TRUTH event dump + the message-passing model). Key insight: in the multi-MCU model MCUs share NO memory; the ONLY inter-MCU channel is the blocking link_push/link_recv (Push/Wait) which SELF-orders. Therefore a control-only Sync (irq_barrier, lowered to a no-op) can only ever provide an ordering that the data edges do NOT already carry between two workers GLOBALLY-OBSERVABLE EXTERNAL IO side-effects (effectful load/save Fires) — NOT transport (self-ordering) and NOT pure compute (no observable order). GUARD: build a happens-before graph over flattened per-worker salient events (program order within worker + Push{seq}->matching Wait{seq} across workers). For each Sync tag T with participants P: for each ordered distinct pair (Wi,Wj) in P, if Wi has an effectful-IO Fire strictly before its FIRST Sync{T} and Wj has an effectful-IO Fire strictly after its LAST Sync{T} and NOT happens_before(io_i, io_j) under the data+program graph => REJECT loud (EmitError; no transport exists to implement a standalone barrier — future work). e2e-INERT on todays matrix: 02-split-add external IO is single-worker (only host loads/saves; w0 only recv/compute/push) so |IO-workers in P| < 2 => never fires. cf TransferInjectError::CumulativeWholeArrayFallback conservative-tripwire precedent. Tests: (1) 02-split-add multi-MCU emit ACCEPTS (real fixture); (2) synthetic 2-worker schedule with cross-worker IO straddling a bare barrier + no data edge REJECTS; (3) single-participant barrier + barrier with a connecting data edge ACCEPT. Gate: build/clippy/test/test-release/e2e (fast shell) + re-run just renode-multimcu (closure now cached) to confirm 02-split-add still BYTE-EXACT.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
ORCHESTRATOR DESIGN-SCOPING (cycle, no code landed): empirically grounded the subsumption analysis on 02-split-add/split (the one case that MUST NOT be rejected — its no-op irq_barriers are byte-exact). Generated the multi-MCU emit (nucleus build --backend embedded-pattern --shim stm32h7) and read the per-worker run() bodies:

  host: push a(seq0) -> push b(seq1) -> barrier0 -> [loop: barrier1] -> barrier2 -> recv c(seq2) -> save
  w0:   barrier0 -> recv a(seq0) -> recv b(seq1) -> [loop: barrier1, compute] -> push c(seq2)
  data edges (Push->Wait by seq): host.push_a->w0.recv_a; host.push_b->w0.recv_b; w0.push_c->host.recv_c

KEY FINDING — the OBVIOUS formulation is UNSOUND-for-purpose (false-rejects 02-split-add): "barrier S(participants P) is subsumed iff for every ordered pair (a,b) in P, pred_S(a) reaches succ_S(b) in the (program-order + Push->Wait) graph." Test on Sync tag 0 {host,w0}: pred on host = push_b(seq1), succ on w0 = recv_a(seq0). recv_a is reachable only from push_a, which precedes push_b in program order => NOT reachable => this formulation REJECTS Sync tag 0. But 02-split-add is value-correct with the no-op. So the barrier-position-reachability check FALSE-REJECTS the must-pass case and is wrong.

WHY: the barrier provides ALL-TO-ALL ordering, but only a SUBSET of that ordering is needed for correctness. The no-op is safe iff every REAL cross-worker dependency the barrier was incidentally covering (push_a->recv_a, push_b->recv_b, push_c->recv_c) is ALSO carried by a data edge — which it is. A SOUND guard therefore needs a cross-worker DATA-DEPENDENCY model (which RAW/WAR/WAW edges exist across workers, and whether each is independently carried by a blocking Push/Wait), NOT a check that the barriers full ordering is reproduced.

DEEPER OBSERVATION: in the current multi-MCU BIN model there is NO shared memory and the ONLY effect is the single output workers USART1 capture. All cross-worker sharing rides blocking link_recv. So a control-only barrier is essentially ALWAYS redundant here, and a genuinely non-redundant standalone barrier may not be constructible from any sound .nuc schedule today (reachability class (b), IR-constructible only — cf. the TASK-0281/0365 cross-partition-reducer precedent). The guard would need a synthetic non-redundant-barrier fixture to prove-it-bites; there is no in-tree positive case.

RECLASSIFICATION: this is NOT a tail-end "add a guard" cycle. It needs a DESIGN SLICE: (1) define the cross-worker dependency model + the sound subsumption predicate (and whether the safe direction is over-rejection); (2) decide if a non-redundant barrier is even reachable from a sound schedule (if class-(b)-only, the guard mirrors TASK-0281: a conservative tripwire whose positive arm is synthetic-only); (3) build a synthetic prove-it-bites fixture at the multi-MCU emit layer. Until then the documented mitigation stands: the tier-3 acceptance is the byte-exact reference.bin diff, which fails loud if a no-op breaks a SHIPPED schedule. The risk is bounded to trusting BIN output WITHOUT that diff. Natural home for the check: TransportPlan::build (multimcu.rs) which already has the full per_worker view + emits typed EmitError.

CYCLE OUTCOME 2026-06-03 (orchestrator-implemented in-thread per repo policy; GOx2 review gate). LANDED commit 3315247 + review fold-back (pending commit): multimcu::verify_control_sync_subsumed called from emit_bin multi-worker path BEFORE any file write. Rejects (EmitError::UnsupportedFeature) a control-only Event::Sync that orders two workers external IO (effectful load/save Fire) with no connecting Push/Wait data edge — the ONE shape the no-op irq_barrier would silently miscompile. Algorithm: flatten per-worker salients (program order + Push{seq}->Wait{seq} HB edges); per Sync tag, reject iff a participant has IO before its barrier and another has IO after, with no HB path between. e2e-INERT (02-split-add routes all IO through host; w0 has zero effectful IO) so the proven byte-exact path is untouched — re-verified just renode-multimcu 02-split-add BYTE-EXACT (Renode env confirmed NOT blocked; only a one-time .#renode closure download). GATE: build/clippy clean, test 1332/0/3, test-release 1330/0/3, e2e 427/364/0/63/0 (baseline preserved), 6 sync_guard unit tests. REVIEW GO x2 (qa-test-runner + mped-architect). Architect fold-back applied in-thread: P2-1 (the between-barrier-instance comment claimed "conservative" but dropping IO between instances is the false-ACCEPT direction — reworded to the truth + debug_assert!(first_sync==last_sync) tripwire), P2-2 (debug_assert no duplicate Push/Wait SeqTag — silent last-write-wins edge loss = false-accept), P3-1 (documented participant-inference is 1:1 with petri_to_events Sync emission), P3-2 (2 new loop-nested barrier tests: accept-when-data-edge-subsumed + reject-when-standalone, pinning the flatten_salients linearization). GOTCHA for the ex14 path: ex14 fe-capture/rf-transmit are external IO on DIFFERENT MCUs; if a barrier orders them with no data edge this guard will correctly REJECT — that schedule genuinely needs a real UART barrier protocol (parent TASK-0049.05), the no-op is wrong for it. The byte-exact reference.bin diff remains the integration safety net.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
DONE (GOx2 review gate). multimcu::verify_control_sync_subsumed fails loud (EmitError) at emit_bin time on a control-only Event::Sync that orders two workers external IO (effectful load/save) with no subsuming Push/Wait data edge — the ONE shape the multi-MCU no-op irq_barrier would silently miscompile (architect P1 on parent TASK-0049.05). Sound because in the multi-MCU model MCUs share no memory; the only inter-MCU channel is the blocking link_push/link_recv which self-orders every data dependency, so a barrier can only add ordering between two workers peripheral IO side effects. e2e-INERT (02-split-add routes all IO through host) so the proven byte-exact path is untouched — re-verified just renode-multimcu 02-split-add BYTE-EXACT (Renode env confirmed NOT environment-blocked: only a one-time .#renode closure download). Commits 3315247 (guard + 4 tests + skeleton docstring) + 57bfb19 (architect fold-back: P2-1 doc-lie correction + first_sync==last_sync tripwire, P2-2 duplicate-seq tripwire, P3-1 participant-inference doc, P3-2 +2 loop-nested tests). Gate: build/clippy clean, test 1332/0/3, test-release 1330/0/3, e2e 427/364/0/63/0 (baseline preserved), 6 sync_guard tests. LIMITATION/forward: ex14 fe-capture/rf-transmit external IO on different MCUs will correctly TRIP this guard if a barrier orders them with no data edge — that schedule needs a real UART barrier protocol (parent TASK-0049.05 follow-up); byte-exact diff remains the integration safety net (forward-carried to TASK-0049.02).
<!-- SECTION:FINAL_SUMMARY:END -->
