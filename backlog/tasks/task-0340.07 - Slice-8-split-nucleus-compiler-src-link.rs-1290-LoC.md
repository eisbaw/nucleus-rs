---
id: TASK-0340.07
title: 'Slice 8: split nucleus-compiler/src/link.rs (1290 LoC)'
status: Done
assignee:
  - '@claude-implementer'
created_date: '2026-05-26 15:11'
updated_date: '2026-05-26 15:30'
labels:
  - tech-debt
  - hygiene
dependencies: []
parent_task_id: TASK-0340
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
TASK-0340 AC#2 slice 8: split nucleus-compiler/src/link.rs (currently 1290 LoC). The LAST originally-named mega-file in the cycle-175 audit (after slice 7 closed acfg.rs cycle 182). Closes the originally-named 6-file AC#2 scope. Cross-cutting compiler pass (link step between AlgoIR + SchedIR per PRD §5); module-level //! at L1-60 explicitly lists 5 design choices that are CLAIMS not facts and must be doc-audited per cycle-178/181b/182b discipline.

## Proposed seams (already named by section/fn boundaries; verify against current line numbers before splitting)

- link/mod.rs (~70 LoC): module-level //! docstring (verbatim L1-60 of pre-split) + pub mod declarations + pub use re-exports preserving the EXACT lib.rs surface (currently lib.rs:53 has 'pub use link::{link, LinkError, LinkErrorKind, LinkErrorSource, LinkedIR, WorkerEntity};' — 6 public symbols).
- link/types.rs (~120 LoC): Public types section L70-185. WorkerEntity (L88-109) + LinkedIR (L118-185). Both used by all sub-modules.
- link/errors.rs (~470 LoC): Errors section L187-543. LinkErrorKind enum + Display impl + LinkErrorSource enum + LinkError struct + impls (PartialEq/Eq/Display/Error) + write_suggestion helper (L279-289).
- link/build.rs (~215 LoC): Entry point + walk section L545-783. pub fn link(algo, sched).
- link/dataflow.rs (~145 LoC): Dataflow helpers section L784-930. collect_loop_vars + collect_loop_vars_in_stmts + analyse_dataflow + walk_stmts + collect_dataref_consumers.
- link/pipeline.rs (~360 LoC): Pipeline buffer checks section L932-1290. check_pipeline_buffer_constraints + data_is_cross_worker + collect_kernels_touching_data + expr_touches_data + call_callee + find_loop_iter_count + collect_data_in_loop + collect_data_refs.

(Numbers from current HEAD; re-grep before committing — slice 7 / cycle 182b may have shifted citations.)

## Discipline (cycle-178 + 181b + 182b doc-audit discipline mandatory)

THREE audit dimensions per cycle-181b memory generalisation + cycle-182b Dim-2 vocabulary extension:

(1) **Numeric citations** (line N, file:line): `grep -nE ':[0-9]+|line [0-9]+|L[0-9]+' <new sub-files>` — walk each hit; verify post-split line numbers.

(2) **Behaviour-time-bound claims** (cycle-182b vocabulary extension): `grep -nE '(does NOT yet|will be|TODO|future work|deferred|in this cycle|NOT yet|coming|placeholder|stub|skeleton|empty payload|M[0-9]+\b)' <new sub-files>` — milestone-version pins (M1/M2/...) age fastest. The L1-60 module docstring mentions 'TASK-0016+', 'TASK-0019', 'TASK-0088', 'TASK-0096', 'partition=' — each is a claim about deferred work; verify current state.

(3) **File/module deixis** (cycle-181b discovery, cycle-182b reinforced): `grep -nE '(in this file|in this module|this function|the helper (above|below)|nearby|earlier in this file|the helper\\'s docs)' <new sub-files>` — entities referenced may have moved across the new sub-module boundary post-split. L46-50 references '[\`crate::error::suggest\`] helper against the in-hand symbol table' — verify the helper exists.

When a claim was TRUE pre-split but FALSE post-split (deixis class), REWRITE the docstring (do not just relocate) before the commit lands.

## Cycle 182b silent-sibling sweep discipline (mandatory)

If any doc-claim rewrite renames or re-paths a function/struct, run `grep -rn '<OLD-NAME>' nucleus/` BEFORE commit and sweep all hits to current truth. Cycle 182b found 12 sibling sites untouched after a 1-of-1 rewrite — silent siblings shipped until architect review caught them. Specifically: if any link.rs doc references an old pthreads-sync render_call_arg path or similar, sweep it.

## Verification gate (every commit)

Run inside nix develop:
```
nix develop --command bash -c 'just build && just clippy && just test && just test-release && just check-textual-replace-on-codegen && just check-include-str-coverage && just check-mega-files && just e2e'
```

Expected gate:
- just test (dev): no regression. Current baseline 969/0/3 dev.
- just test-release: no regression. Current baseline 968/0/3 release.
- just e2e: 112/102/0/10/0 (preserved cycles 178-182b; non-flake confirmed).
- just check-mega-files: PASS both directions; allow-list shrinks 9 → 8 (link.rs entry removed).

## AC pointer

This subtask closes the link.rs slice of TASK-0340 AC#2 — 6/6 of the originally-named files. After this slice, TASK-0340 has 3 ACs still pending (AC#3 proptest, AC#4 e2e formatter carve-out, AC#7 final commit). The 'should AC#2 extend to all >1000 LoC files (recipe scope), or stop at the original 6?' question deferred from cycle-179b/180b/181b is for the orchestrator to decide AFTER this slice lands, not for the implementer.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 link.rs split into link/{mod,types,errors,build,dataflow,pipeline}.rs along proposed seams; lib.rs pub use surface preserved bit-identical (6 symbols)
- [ ] #2 All 3 audit dimensions (numeric citations / behaviour-time-bound / file-deixis) cleanly pass on the new sub-files; any pre-split claim falsified by the split is REWRITTEN (not relocated)
- [ ] #3 No new TASK-NNNN or cycle-NNN citations in the new sub-files; pre-existing anchors that move with their code are NOT new (lenient AC#6 reading from cycle 179b)
- [ ] #4 Full gate green: build + clippy + test + test-release + check-textual-replace-on-codegen + check-include-str-coverage + check-mega-files + e2e (112/102/0/10/0 preserved)
- [ ] #5 Behaviour-equivalent change list enumerated in commit message (cycle-179b/180/181/182 discipline; no blanket 'verbatim move' claim)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Re-grep current seam-boundary line numbers (slices 3-7 may have shifted)
2. Read each section before moving; identify all pub vs private vs pub(super) needs
3. Create link/ directory + 6 sub-files
4. Move type defs first (types.rs), then errors (errors.rs), then build (build.rs), then dataflow helpers, then pipeline helpers
5. Update src/lib.rs imports — pub mod link → pub mod link (no change in surface)
6. Re-grep three audit dimensions on every new sub-file BEFORE committing
7. Run full gate; commit once everything green
8. Update tracker notes with enumerated change list
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 183 close — slice 8 split landed

Split nucleus-compiler/src/link.rs (1290 LoC) into link/{mod,types,errors,build,dataflow,pipeline}.rs (6 files, sum 1369 LoC; +79 vs pre-split = per-file //! orientation docstrings + use statements + sub-module decls + pub use re-exports; no behaviour change).

Per-file LoC after split:
- link/mod.rs        83  module //! docstring (REWRITTEN — see (R1-R4)) + 5 pub mod decls + 3 pub use re-exports preserving the byte-identical lib.rs surface (link, LinkError, LinkErrorKind, LinkErrorSource, LinkedIR, WorkerEntity — 6 public symbols total).
- link/types.rs      92  WorkerEntity (with co-located pub(super) from_target + pub display) + LinkedIR struct.
- link/errors.rs    408  LinkErrorKind enum + write_suggestion helper + Display impl + LinkErrorSource enum + LinkError struct + impl LinkError + PartialEq/Eq/Display/Error impls.
- link/build.rs     247  pub fn link entry point.
- link/dataflow.rs  162  collect_loop_vars / collect_loop_vars_in_stmts / analyse_dataflow / walk_stmts / collect_dataref_consumers.
- link/pipeline.rs  377  check_pipeline_buffer_constraints (TASK-0134) + data_is_cross_worker (TASK-0214) + collect_kernels_touching_data + expr_touches_data + call_callee + find_loop_iter_count (TASK-0217) + collect_data_in_loop + collect_data_refs.

Three-dimension doc audit (cycle-181b discipline + cycle-182b Dim-2 vocabulary extension):

Dim 1 (numeric citations): grep -nE ':[0-9]+|line [0-9]+|L[0-9]+' returned 1 hit (build.rs:49 'fabricated 0:0' — false positive content phrase, same as pre-split). Clean.

Dim 2 (behaviour-time-bound, extended vocab): pre-split returned 1 hit at L52 'What this pass explicitly DOES NOT do (deferred):'. Walked the 5-item list under that header: 4 of 5 items were stale (TASK-0016+ × 2, TASK-0088, TASK-0019 — all Done). REWROTE (see R1-R4 below). Post-split returned 2 hits in mod.rs (57 'genuinely still deferred' — TRUE; 63 meta-comment 'deferred framing would mislead' — describes the rewrite). Clean.

Dim 3 (file/module deixis): 0 hits pre-split, 0 hits post-split. Clean (link.rs never used file/module deixis).

Doc-claim REWRITES (NOT verbatim moves; enumerated per cycle-178 + cycle-181b discipline):

(R1) mod.rs L22-31 (distributed-placements design choice paragraph): old '(partition= handles the per-element decomposition, scheduled later in TASK-0016+)' → new '(the per-element decomposition is handled in downstream passes: TASK-0016 lands the ACFG layer, TASK-0117 the Push/Wait replication across the set, and TASK-0258 / TASK-0259 / TASK-0249 the per-partition= slicing)'. WHY: TASK-0016 + TASK-0117 + TASK-0258 + TASK-0259 are all DONE; the 'scheduled later' framing is stale. Anchors pre-existing.

(R2) mod.rs L52-78 deferred-items list — REORGANISED into two sections rather than one:
- 'What this pass explicitly DOES NOT do' (2 genuinely-still-deferred items: partition= policy vs placement cardinality + no-producer-data detection).
- 'What this pass also does not do, but where the work has landed in adjacent passes' (3 items moved out of the deferred list: type-check kernel signatures → addressed by contract pass (TASK-0012) + UnknownKernel/UnknownData; per-worker slicing → addressed by partition_workers/rows/blocks2d (TASK-0117 + TASK-0258 + TASK-0259, all Done); transfer/notify capability resolution → addressed by crate::capabilities (TASK-0019, Done)).
WHY: TASK-0088 closed as ADDRESSED in-large-part via contract pass + link UnknownKernel/UnknownData checks (final summary cycle 78); TASK-0016 + TASK-0117 + TASK-0258 + TASK-0259 Done; TASK-0019 Done. The original four 'deferred' bullets had all resolved silently; presenting them as still deferred would mislead a reader.

(R3 / R4 are sub-items of R2 above — folded into the section reorganisation.)

(R5) dataflow.rs analyse_dataflow docstring — Identity-copy carveout paragraph: old 'right semantics for ... are part of the partition/transfer pass proper (TASK-0016+)' → new 'right semantics ... are part of the partition/transfer pass proper (TASK-0117 + TASK-0258 / TASK-0259, the per-partition= consumers — all Done)'. Same stale-anchor pattern as R1. Pre-existing anchors.

Silent-sibling sweep (cycle-182b discipline) for TASK-0016+ across all of nucleus/**/*.rs found TWO source-tree siblings carrying the same stale 'TASK-0016+' deferral framing; both were structurally identical lies (same defect class as R1/R5) and one-line fixes — folded into THIS commit (NOT a separate cycle):
- (S1) passes/acfg_to_petri.rs L231-236 module //! docstring 'Distributed placements treat the set as one entity ... will be revisited once a partition pass exists (TASK-0016+)' → 'the per-worker replication lives in the downstream partition_workers / partition_rows / partition_blocks2d passes (TASK-0117 + TASK-0258 + TASK-0259, all Done) — acfg_to_petri consumes their already-replicated ACFG'.
- (S2) event.rs L651-654 Event enum doc 'A worker's EventList is a Vec<Event> in execution order (TASK-0016+ owns the actual emission; this module just defines the type)' → 'A worker's EventList is a Vec<Event> in execution order (the ACFG layer — TASK-0016, Done — owns the actual emission; this module just defines the type)'.
Post-sweep: grep -rn 'TASK-0016+' nucleus/ --include='*.rs' returns 0 src hits (target/doc/* HTML are auto-regenerated rustdoc output, not source).

Sibling sweep for TASK-0019/0088 surfaced 5+ additional sites in capabilities.rs, sched/ir.rs, acfg/types.rs, transfer_inject.rs, tests/transfer_inject.rs. These are STRUCTURALLY LARGER than the split itself (need per-site re-reading; not a 1-line fix each) — filed as P3 follow-up TASK-0340.07.01 rather than folded in.

Behaviour-equivalent change list (cycle-179b enumerated-disclosure discipline; empirically diffed per moved item):
- (a) Module structure: link.rs (single 1290-LoC file) → link/{mod,types,errors,build,dataflow,pipeline}.rs (6 files). Public surface BYTE-IDENTICAL via mod.rs pub use re-exports (lib.rs:53 'pub use link::{link, LinkError, LinkErrorKind, LinkErrorSource, LinkedIR, WorkerEntity}' unchanged).
- (b) WorkerEntity::from_target visibility uplifted private fn → pub(super) (required because the sole caller — link() in build.rs — now lives in a sibling sub-module).
- (c) collect_loop_vars / analyse_dataflow visibility uplifted private fn → pub(super) (same reason — sole caller is link() in build.rs).
- (d) check_pipeline_buffer_constraints visibility uplifted private fn → pub(super) (same reason).
- (e) Five doc-claim REWRITES (R1, R2 = R3+R4 reorganised, R5) in link/mod.rs + link/dataflow.rs.
- (f) Two silent-sibling sweep edits (S1 in acfg_to_petri.rs, S2 in event.rs) — same stale TASK-0016+ defect class as R1/R5.
- (g) Justfile allow-list: removed 'nucleus/nucleus-compiler/src/link.rs' entry (count 9 → 8). check-mega-files OK both directions confirmed.
- No code-body edits; no fn/struct/enum text changes other than the visibility uplifts (b-d) and docstring rewrites (e-f).

Public surface preserved BYTE-IDENTICAL via mod.rs pub use re-exports. Consumers (grep-verified unchanged import lines):
- nucleus/nucleus-compiler/src/lib.rs:53 — pub use link::{link, LinkError, LinkErrorKind, LinkErrorSource, LinkedIR, WorkerEntity}
- src/{acfg/build.rs, sidecar.rs, passes/{partition_workers, partition_rows, partition_blocks2d, halo_inference, reuse_inference, block_transform, transfer_inject}.rs} — use crate::link::{LinkedIR, WorkerEntity}
- src/passes/{reuse_inference, halo_inference}.rs:883/928 — use crate::link::link (test-only)
- backends/pthreads-sync/tests/emit.rs, tests/{acfg_to_petri, boundedness, sidecar_buffer, sidecar_partition_blocks2d, halo_strip_synth, partition_blocks2d, partition_rows, partition_workers, sync_inject, deadlock, transfer_inject_hoist, petri_to_events}.rs — use nucleus_compiler::link or nucleus_compiler::link::{LinkedIR, WorkerEntity, link}
Zero churn at any call site.

Verification gate (orchestrator-self-run, inside nix develop --command bash -c):
- just build: clean.
- just clippy (-D warnings --all-targets): clean.
- just test (dev): 969 / 0 / 3 — MATCHES cycle-182 baseline.
- just test-release: 968 / 0 / 3 — MATCHES cycle-182 baseline.
- just check-mega-files: OK both directions; allow-list shrank 9 → 8.
- just check-textual-replace-on-codegen: OK.
- just check-include-str-coverage: OK.
- just e2e sample 1: 112 / 102 / 0 / 10 / 0 — MATCHES baseline.
- just e2e sample 2: 112 / 102 / 0 / 10 / 0 — non-flake confirmed (TWO independent samples).

AC delta TASK-0340.07 post-cycle-183:
- AC#1: DONE (6 sub-files per the proposed seam map; public surface preserved bit-identical).
- AC#2: DONE (all 3 audit dimensions clean post-split; 5 doc-claim rewrites enumerated R1/R2/R5; pre-split lies REWRITTEN not relocated).
- AC#3: DONE (no new TASK-NNNN or cycle-NNN citations introduced; all anchors pre-existing per lenient AC#6 reading from cycle 179b).
- AC#4: DONE (full gate green; baselines preserved).
- AC#5: DONE (enumerated behaviour-equivalent change list above; no blanket 'verbatim move' claim).

Forward-carry to TASK-0340 parent cycle-183b summary:
- AC#2 (parent) now 6/6 originally-named files closed — slice 8 was the LAST file in the cycle-175 audit.
- Mega-file count (>1000 LoC, per check-mega-files): 10 (allow-list now 8; transfer_inject.rs et al. remain).
- The cycle-179b/180b/181b/182b deferred AC#2-scope question (originally-named-6 vs all 12 >1000 LoC) is now the orchestrator's call: parent TASK-0340 should either close AC#2 as DONE (originally-named scope, slice 9 = AC#3 proptest next) or widen to recipe scope (transfer_inject.rs 4726 LoC next, biggest remaining).

P3 follow-ups filed:
- TASK-0340.07.01 — TASK-0019/0088 stale-deferral-claim sibling sweep (5+ sites in capabilities.rs / sched/ir.rs / acfg/types.rs / transfer_inject.rs / tests/transfer_inject.rs; structurally larger than slice 8 itself, NOT folded in).

Honest-limits / forward-carries to slice 9+ implementer:
- The 'structured deferral list' pattern (an introducer line like 'X explicitly DOES NOT do (deferred):' followed by N items each carrying a single TASK-NNNN anchor) escapes the cycle-182b Dim-2 vocabulary because each ITEM line doesn't repeat the deferral verb. New audit-dimension forward-carry: when an introducer line tripped Dim-2, scan EVERY item under the introducer (not just the introducer text itself). This caught 4 stale items in the original link.rs L52-60 list.
- Two-sample e2e gate did NOT decompile any divergence between the bit-identical baselines (112/102/0/10/0 both samples). Slice 9 (AC#3 proptest) is fresh shape and should not be blocked by anything from this slice.
- mp-tcp-event's 13-cnn-inference test timed at 3.00s vs the others' ~600ms is a normal observation, not a regression (mp-tcp-event's per-seq-demux is slower at high event count); same shape as cycle 178-182 baselines.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Cycle 183 LANDED commit f518890. Split nucleus-compiler/src/link.rs (1290 LoC) into link/{mod,types,errors,build,dataflow,pipeline}.rs (6 files; sum 1369 LoC; +79 vs pre-split = per-file //! orientation docstrings + use statements + sub-module decls + pub use re-exports). 5 doc-claim REWRITES (R1/R2 = L52-60 list reorganisation/R5) in link/{mod,dataflow}.rs + 2 silent-sibling sweep edits (S1 in acfg_to_petri.rs, S2 in event.rs) folded into this commit. Gate green end-to-end (969/0/3 dev, 968/0/3 release, 112/102/0/10/0 e2e two-sample non-flake, allow-list shrank 9 -> 8). New cycle-183 audit-discipline forward-carry: STRUCTURED-DEFERRAL-LIST pattern (a Dim-2 introducer line followed by N independently-anchored items) requires per-item walk, not just per-grep-hit walk (caught 4 stale items here). P3 follow-up TASK-0340.07.01 filed for the residual TASK-0019/0088 stale-deferral sibling sweep (5+ sites; structurally larger than slice 8 itself). Closes AC#2 6/6 originally-named files for parent TASK-0340; deferred scope question (recipe-scope vs originally-named scope) is now the orchestrator's call for the next session.
<!-- SECTION:FINAL_SUMMARY:END -->
