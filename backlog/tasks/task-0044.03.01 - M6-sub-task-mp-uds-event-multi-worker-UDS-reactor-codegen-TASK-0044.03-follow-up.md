---
id: TASK-0044.03.01
title: >-
  M6 sub-task: mp-uds-event multi-worker UDS-reactor codegen (TASK-0044.03
  follow-up)
status: To Do
assignee: []
created_date: '2026-05-27 02:35'
updated_date: '2026-05-27 03:15'
labels:
  - M6
  - backend
dependencies: []
parent_task_id: TASK-0044.03
priority: high
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
## Goal

Land the multi-worker arm of mp-uds-event: Unix domain socket reactor + per-(DataId, SeqTag) ring buffer + epoll readiness + Plan-shaped per-worker codegen. Same capability surface as mp-tcp-event (async + buffer + event; supports_async=true, supports_buffer=true); every multi-worker schedule that mp-tcp-event handles MUST emit bit-identical output under mp-uds-event.

This unblocks all multi-worker [[skip]] cells filed in TASK-0044.03 cycle 194:
- 02-split-add/split, 03-reduction/distributed, 05-stencil/distributed + distributed-2d, 06-separable-filter/distributed + distributed2, 07-matmul/distributed + distributed-2d, 08-histogram/distributed, 09-producer-consumer/pipelined, 11-game-of-life/pipelined, 13-cnn-inference/batch_parallel + pipeline_parallel

Each promotes to [[required]] once bit-identical is verified.

## Closest template + LIFTING decision (cycle-171 brief AC#9)

Per the cycle-171 brief, ASSESS THE LIFT BEFORE COPYING. mp-tcp-event has ~3000 LoC of multi-worker substrate (lib.rs + multi_worker/{mod,worker_program,relay,walkers,encode}.rs + runtime_src.rs). The DIFF is the transport layer:

- mp-tcp-event uses mio's `TcpListener` / `TcpStream`.
- mp-uds-event must use mio's `UnixListener` / `UnixStream`.

mio's UDS types share the `Source` trait with TCP types, so the reactor surface should be parametric. If mp-tcp-event's reactor is genuinely transport-parametric via a trait, the lift may be cheap (one new instantiation + UDS-specific binding paths). If copy-paste exceeds ~200 lines, FILE a separate lift task (`TASK-NNNN: lift transport-layer abstraction from mp-tcp-event + mp-uds-event`) per the cycle-171 brief.

## Acceptance criteria

1. `mp_uds_event::emit` multi-worker arm (used_workers >= 2) produces a runnable multi-process Cargo project for at least 02-split-add/split.
2. Cross-backend bit-identical against reference.bin on the multi-worker schedules listed under Goal (above).
3. e2e-matrix.toml: each multi-worker [[skip]] cell filed under "mp-uds-event multi-worker codegen pending" in TASK-0044.03 cycle 194 is removed and replaced with [[required]].
4. The multi-worker ContractGap branch in `mp_uds_event::emit` (`used_workers >= 2`) is removed.
5. Unit / integration tests pin the multi-worker emit string on at least one fixture (mirror mp-tcp-event's tests).
6. Cross-backend differential gains a FOURTH async+buffered+event row (pthreads-async + mp-tcp-event + mp-uds-event + the existing 4 sync rows).
7. The wire.rs CONTENT swaps from TCP-specific `mp_tcp_common::WIRE_RUNTIME_SRC` to UDS-specific implementation. Either: (a) extend mp_tcp_common to be transport-parametric, OR (b) introduce `mp_uds_common` (sibling crate), OR (c) inline the UDS-specific wire emission in mp-uds-event. Decide as part of the lifting assessment in AC#1.
8. **TASK-0337 promotion-trigger guard**: if a 5th compensating pass is forced to land here for bit-identicality, STOP and consult — that IS the promotion trigger named in TASK-0337's "Promotion triggers" section.
9. UDS path-length cap honoured: the run.sh checks `SCRATCH_DIR + worker-N.sock` against ~104/~108 char cap and fails loud if exceeded (per cycle-171 brief AC#8).
10. const-in-IndexExpr independent regression pin: mirror `mp-tcp-event/tests/...::const_in_indexexpr_*` (forward-carried from TASK-0044.01.02 cycle 193; the cycle-36 pin pattern bites the moment multi_worker_walker is consumed, which is HERE not at the single-worker arm).

## Honest scope

- mio may need `os-ext` feature for UnixListener depending on mio version. Check Cargo dep.
- SCM_RIGHTS / fd-passing is NOT in scope; UDS used as stream socket, identical surface to TCP.
- run.sh path-cleanup: per project memory `project-e2e-harness-scratch-lifecycle`, the e2e harness deletes `target/e2e-matrix/run-*/` on success; UDS sockets must live under that scratch dir so cleanup is automatic.

## Forward-carried context (cycle 194)

- Single-worker arm delegates to `pthreads_sync::render_single_worker_main_with_kernels_attr` +
  `backend_common::project_skeleton::multi_binary` (byte-identical to mp-tcp-event + mp-tcp-poll
  single-worker emit). The single-process binary does NOT `mod wire;`, so the TCP-specific
  wire.rs sits unused. When this multi-worker arm lands, wire.rs CONTENT changes per AC#7.
- 13 [[skip]] entries in e2e-matrix.toml (cycle 194) are the to-do list: all 13 are
  multi-worker-pending. Unlike openmp-rs + mp-tcp-poll (each had 5 cap-mismatch skips),
  mp-uds-event has ZERO cap-mismatch entries because its capability surface IS
  async + buffer + event (same as mp-tcp-event).
- Per memory `project-mp-tcp-event-vs-bufsync-safety-profile`: every defensive guard
  mp-tcp-event emits MUST have a sibling in mp-uds-event. Cross-grep before promotion.
- Per memory `feedback-silent-sibling-defect`: the (D)+(B2) compensating-pass tower
  (cycles 149/151/162/163/165) lives in mp-tcp-event's multi_worker codegen. mp-uds-event
  INHERITS both the capability AND the tower; verify each pass's UDS interaction.
- The single-worker byte-identical-vs-mp-tcp-event invariant (cycle 194 single_worker_emit.rs)
  is preserved when this multi-worker arm lands; multi-worker bit-identical against
  mp-tcp-event is the cross-backend differential gate (Plan structure / event walker reused).
<!-- SECTION:DESCRIPTION:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Cycle 194 architect P2.2: driver mediation-gate update required

The 3 driver mediation passes gate on `backend == "mp-tcp-event"` or `backend == "mp-tcp-bufsync"`. When TASK-0044.03.01 lands multi-worker mp-uds-event, EACH of these gates MUST be widened to include `"mp-uds-event"` (because mp-uds-event has the same wire/transport-shape requirements that drove the gate predicates):

- `nucleus/driver/src/main.rs:503` — `apply_host_mediation_inject` (CTRL-arm host-mediated barriers; gates on `mp-tcp-bufsync || mp-tcp-event`).
- `nucleus/driver/src/main.rs:561` — `apply_host_data_relay_inject` (DATA-arm w↔w host-relay; gates on `mp-tcp-event` only).
- `nucleus/driver/src/main.rs:668` — `apply_safe_push_reorder` (push-before-wait reorder; gates on `mp-tcp-event` only).

Per project memory `feedback-driver-must-mirror-backend-election-exactly` (cycles 160-164b): driver-level passes mediating against backend-elected entities must use the SAME predicate as the backend, not approximate. Widening these 3 gates is part of the multi-worker arm landing scope — without it, mp-uds-event would emit unmediated w↔w Push/Wait + host-excluding barriers that hit the backend's defense-in-depth ContractGap.

**Acceptance addendum (cycle-194 architect P2.2)**: AC#11 — extend the 3 driver mediation-gate predicates (lines 503/561/668 in driver/src/main.rs at cycle-194 stamp) to include `backend == "mp-uds-event"`. Sibling-sweep verifies no fourth gate site was missed. Sanity test: the host-excluding-barrier test cases for mp-tcp-event (existing e2e cells) MUST also pass for mp-uds-event after the gates widen, end-to-end via the existing e2e gate (not just a unit test of the gate predicate).

**Forward-carried from TASK-0044.01.02 cycle 193** (already in this task's notes earlier; restated here for completeness): when multi-worker codegen lands, ADD an independent const-in-IndexExpr regression pin mirroring mp-tcp-event's (using `test_common::CONST_IN_INDEXEXPR_*` multi-worker fixture). The pin defends against future `pthreads_sync::render_const_expr_pub` regressions that would NOT be caught by byte-identical-vs-mp-tcp-event because both would drift in lockstep.
<!-- SECTION:NOTES:END -->
