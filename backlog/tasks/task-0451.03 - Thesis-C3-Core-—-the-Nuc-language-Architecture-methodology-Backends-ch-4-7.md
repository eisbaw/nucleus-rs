---
id: TASK-0451.03
title: >-
  Thesis C3: Core — the Nuc language + Architecture/methodology + Backends (ch
  4-7)
status: Done
assignee: []
created_date: '2026-06-05 20:00'
updated_date: '2026-06-05 21:27'
labels:
  - thesis
  - paper
  - content
dependencies:
  - TASK-0451.02
parent_task_id: TASK-0451
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Write chapters 4-7 (the technical core). Ch4 Problem statement & solution space: the portability gap precisely stated; the design space (where to put decomposition + IO); the central commitment (algorithm/schedule separation — one algorithm x many schedules x many backends); why a single Petri-net IR (one model for transfers, sync, soundness); design commitments & deliberate non-goals (what makes the claim falsifiable). Ch5 The Nuc language: two sublanguages + one name table; algorithm sublanguage (shape-typed Rust kernels, dataflow, iteration, pure/effectful annotation, single-assignment arrays = no aliasing); schedule sublanguage (workers, mapping, blocking/tiling, reuse/pipeline, IO semantics, check assertions); a worked one-algorithm-many-schedules example (e.g. stencil or separable filter); what the language deliberately cannot express and why (data-dependent writes/scatter, convergence-driven termination, recursion). Ch6 Architecture & methodology: the compilation pipeline (parse -> typed algorithm IR -> link -> ACFG -> transforms vectorise/block/pipeline/reuse -> transfer & halo inference -> Petri-net IR & event contract -> soundness gate -> per-worker projection to EventList -> presentation-layer codegen); the two boundaries (algorithm/schedule; middle-end/presentation); the soundness gate (exact-replay over the deterministic firing order; boundedness + deadlock + conflict-free; WHY sound for the restricted class — not a general model checker). Ch7 Backends & the target ladder: capability matrix & backend orthogonality; tier 1 CPU (7 backends, the falsification rig); tier 2 MPI (SPMD rank dispatch, blocking/non-blocking); tier 3 embedded (no_std, DMA/IRQ shims, multi-MCU Renode co-sim); adding a backend (cost classes, shim model). CONSTRAINTS: standalone prose, no TASK/PRD refs; reference C6 figure labels (pipeline, two boundaries, ACFG, Petri net, projection, target ladder, capability matrix, MPI SPMD, embedded multi-MCU). DoD: just build clean; ch 4-7 written.
<!-- SECTION:DESCRIPTION:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Chapters 4-7 (technical core) written as cohesive standalone prose, no TASK/PRD refs. Ch4 Problem & solution space (gap precisely; 4-option design space; algorithm/schedule commitment; why one Petri-net IR; falsifiability commitments + non-goals). Ch5 The Nuc language (two sublanguages+one name table; algorithm sublang: shape-typed Rust kernels, single-assignment, mandatory pure/effectful, affine const-bounded iteration; schedule sublang: workers/placement/loop-transforms/transfer-IO/check; worked one-algorithm-many-schedules stencil with REAL on-disk listings naive/blocked/distributed; limits: no data-dep control flow, limited gather/scatter, no recursion, no carried scans). Ch6 Architecture (pipeline parse->typed IR->link->ACFG->block/partition->halo+reuse inference->sync+transfer inject->Petri net+soundness gate->per-worker projection->codegen; two boundaries; event contract Fire/Push/Wait/Sync/Loop + Alloc/Free reserved-not-emitted; soundness gate conflict-free+bounded+deadlock by exact-replay over ONE deterministic firing order, every build, sound only for restricted nets). Ch7 Backends (verified 10-backend capability matrix table; 7 tier-1 CPU; 2 MPI SPMD blocking/nonblocking shared mpi_plan rsmpi 0.8; 1 embedded no_std + multi-MCU Renode co-sim UART-hub/boot-order/control-sync, 3 byte-exact examples 02-split-add+14-hearing-aid+22-dma-pio; cost classes + shim model). All facts verified against LIVE codebase via 2 Explore agents + direct checks (capabilities.toml values, petri.rs 790 LoC, pipeline.rs pass order, event.rs+petri_to_events.rs, net_soundness.rs, multimcu module, justfile renode-multimcu-gate). PROVEN: cd paper && nix develop --command just build => exit 0, 55pp, no LaTeX/biber errors, no undefined citations (only C6 fig:* forward-refs). Review gate: paper-accuracy GO (after fixing 2 WRONG: mpi-nonblocking notify table cell was "event" but toml is barrier/blocking/event superset; "two" Renode examples was actually three incl 22-dma-pio; + 3 MISLEADING size claims tightened: petri.rs few-hundred->under-a-thousand, backend-crate-size honest range, faithful full notify lists) + paper-references GO (no new bib entries, no dangling cites). paper-clarity + paper-flow advisory findings folded in (ACFG/sidecar/seq-vs-sync-tag/conflict-freedom/event-contract/SPMD/firing-order/host-election/event-reactor/tier-discipline/boot-order glosses; ch4->ch5 + ch6-close + typed-worker + ch7-open transitions).
<!-- SECTION:FINAL_SUMMARY:END -->
