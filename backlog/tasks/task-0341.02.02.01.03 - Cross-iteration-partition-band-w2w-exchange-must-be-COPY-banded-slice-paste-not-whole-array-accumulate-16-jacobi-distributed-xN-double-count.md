---
id: TASK-0341.02.02.01.03
title: >-
  Cross-iteration partition-band w2w exchange must be COPY (banded slice-paste),
  not whole-array accumulate (16-jacobi/distributed xN double-count)
status: Done
assignee:
  - '@mark'
created_date: '2026-05-30 07:24'
updated_date: '2026-05-30 09:24'
labels:
  - compiler
  - backend-common
  - wait_slice
  - transfer_inject
  - M6
  - 16-jacobi
  - gap-probe-followup
dependencies:
  - TASK-0341.02.02.01.01
parent_task_id: TASK-0341.02.02.01
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Filed from TASK-0341.02.02.01.02 cycle 212. The 16-jacobi/distributed w2w field exchange is currently classified as whole-array ACCUMULATE (wrapping_add; same-set-elision fan-out + collect_accumulate_waits). EMPIRICALLY this xN-double-counts the cumulative cross-iteration field history: with 4 workers the emitted pthreads-sync output == EXACTLY 4x reference (proven cell-by-cell: ref[1,1]=14 -> out=56). The accumulate combine is correct ONLY for DISJOINT single-pass partition fan-in (05-stencil/distributed, 08-histogram/distributed via TASK-0343). For a partition over the OUTPUT rows of a CUMULATIVE array read cross-iteration (field[t] reads field[t-1]), the workers SHARE field[0..t-1]; summing whole local arrays double-counts shared history. CORRECT: copy ONLY each worker band rows of field[t] (banded slice-paste OVERWRITE). AC: (1) classify a cross-iteration partition-band w2w exchange as COPY not accumulate (distinct from TASK-0343 disjoint-accumulate); the discriminator is whether the partitioned data is read across an OUTER Repeat iteration (cumulative) vs written-once-disjoint (single pass). (2) The banded slice-paste needs the N-D wait_slice dispatch (TASK-0341.02.02.01.01) — co-required. (3) The exchange placement hoist (to the for t body, send-then-recv) is TASK-0341.02.02.01.02 — also co-required. All three land together to make 16-jacobi/distributed bit-identical. Cross-ref: TASK-0341.02.02.01.02 implementation notes carry the full empirical chain + reverted prototype design.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 A cross-iteration CUMULATIVE partitioned array's multi-Wait fan-in is classified COPY (band slice-paste OVERWRITE), not accumulate (wrapping_add). Applies to BOTH the w2w exchange AND the workers->host gather (cycle-212 architect: the xN on the EXTRACTED output enters at the host gather of identical full field[ITERS] slices; fixing only the w2w exchange still yields xN). See notes.
- [ ] #2 The cumulative-vs-disjoint discriminator is REAL machinery extending collect_accumulate_waits (today purely structural N>=2 whole-array Waits => accumulate, collect.rs:190-242). Cumulative=read across outer Repeat => COPY; disjoint single-pass write => accumulate. Regression pin: 05-stencil + 08-histogram STAY accumulate; 16-jacobi FLIPS to copy.
- [ ] #3 The banded copy tile uses the WRITE band [source_lo,source_hi), NOT the halo-expanded read range (which overlaps: w0 reads y0..4, w1 reads y2..6 sharing rows 2,3) - copying halo rows double-writes boundaries. Tile construction in .01 keys on the per-worker WRITE band.
- [ ] #4 16-jacobi/distributed x pthreads-sync + the 4 sibling wait_slice backends emit bit-identical to reference.bin ONLY when .01 (N-D banded wait_slice) + .02 (placement hoist) + this task (copy combine for w2w AND host gather) land together as one combined cycle.
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
=== Cycle 212 review-gate (orchestrator + read-only architect) corrections folded in ===

Prose ACs lifted to structured ACs (P2-1) and CORRECTED on the load-bearing point the read-only architect found by independently simulating four combine models in Python against reference.bin:

MECHANISM RE-ATTRIBUTION (memory feedback-implementer-disclosure-mechanism-wrong): the implementer's note located the xN cause in the w2w whole-array history re-summation. The architect proved that re-summation corrupts the HISTORY slices field[0..ITERS-1] (verified: shared field[0] row went x4 after the t=1 exchange) but NOT field[ITERS], which is written last and never re-summed as history. The xN on the EXTRACTED output (out[1,1]=56=4x14, uniform x4) is reproducible ONLY when the workers->host GATHER of field is ALSO a whole-array accumulate of 4 identical full field[ITERS] slices. So the host gather is the operative source of the output error -> a combined-cycle implementer who fixes only the w2w exchange would STILL get xN. AC#1 now explicitly covers BOTH the w2w exchange AND the host gather.

AC=BANDED is FORCED (architect verified): whole-array COPY (last-writer-wins overwrite) is provably wrong (only one worker's band survives; reproduced an all-but-one-band-zero grid). Banded copy is bit-identical. So .01 (N-D banded wait_slice) is genuinely co-required, not one of two viable designs.

Dependency graph repaired (P1-1): the prior .01->.02 edge formed a cycle .01->.02->.01 (unschedulable). .01 was repointed to its true Done precursor TASK-0294 (the 2D row-loop slice-paste it extends to N-D), leaving the acyclic cluster: .02 -> {.01, .03}; .03 -> .01; .01 (source, dep on Done TASK-0294 satisfied).

=== Sibling cross-link (cycle 212) ===

TASK-0343.04 (iterative Repeat-body overlapping-write accumulator, cycle-189 scope LIMIT) is the SIBLING of this task at the collect_accumulate_waits Repeat-body-classification gap. Both involve Waits inside Event::Loop bodies that the cycle-189 whole-array predicate does not classify. They are DISTINCT shapes that must NOT be conflated: 0343.04 = genuine element-wise ACCUMULATE into a shared accumulator across time-steps (no example yet); THIS task = cumulative single-assignment-per-slice array needing band COPY (16-jacobi). When the Repeat-body classifier is extended (either task), the cumulative-vs-disjoint discriminator in this task's AC#2 is the shared mechanism that tells copy from accumulate. See TASK-0343.04 notes for the full distinction.

CYCLE 213 DONE (commit cc8c4d7). Cumulative-array COPY-not-accumulate classifier LANDED. collect_cumulative_data_names (sidecar.rs) computes the cumulative-data set from the algo-IR (a data symbol read at a SHIFTED outer-loop index on its own RHS while being the LHS, nested in a for); carried on NameSidecar::cumulative_data; consumed by collect_accumulate_waits (backend-common/collect.rs) to EXCLUDE cumulative arrays from accumulate. AC#1 (both w2w AND host gather classified COPY) MET - the band-tile rewrite + cumulative exclusion apply to both; the host gather was the operative xN source. AC#2 (real machinery + regression pin 05/08 stay accumulate, 16-jacobi flips to copy) MET - unit tests: sidecar cumulative_tests (jacobi=cumulative, histogram/stencil/top-level=not), whole_array_classifier cumulative_data_whole_array_fan_in_is_excluded_from_accumulate (+ control), banded_rank3_is_not_whole_array_so_not_accumulate. AC#3 (WRITE band not halo-read-range) MET - cumulative_band_bounds keys on partition_ranges[iv][src] (the write band), runs after halo extension and overrides. AC#4 (all three land together => bit-identical) MET - 5 backends cmp bit-identical + e2e 308/251/0/57/0.
<!-- SECTION:NOTES:END -->
