---
id: TASK-0335.02
title: >-
  Investigate Sync-stopping for hoist_invariant_waits::place_or_bubble dedup
  (TASK-0335 cycle-158 architect P2.2 follow-up)
status: To Do
assignee: []
created_date: '2026-05-25 23:11'
updated_date: '2026-05-25 23:11'
labels:
  - compiler
  - transfer_inject
  - silent-sibling-risk
  - forward-carried-from-TASK-0335
dependencies: []
parent_task_id: TASK-0335
priority: low
---

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Background

TASK-0335 cycle 158 architect review P2.2: `hoist_invariant_waits::place_or_bubble` closure at transfer_inject.rs lines 1493-1503 uses the same `out.iter().any(matches!(...))` pattern with a 3-tuple key `(src, dst, data)` (no tile per docstring justification), with NO Sync-stop. Syncs DO exist in the tree at Pass A run time (sync_inject runs before transfer_inject).

## Risk

Same shape as TASK-0336 (P2.1): legitimate cross-epoch hoist target with a matching prior-epoch Wait would be silently suppressed. The 3-tuple key (no tile) makes it slightly more aggressive than the 4-tuple sibling, so its risk surface is BROADER (any same-(src,dst,data) match suppresses regardless of tile).

## In-tree status (cycle 158)

Currently LATENT — Pass A's input is the cycle-158-collapsed output of `inject_in_sequence`. The orchestrator's silent-sibling sweep at cycle-158 close found no in-tree shape that exercises this.

## Acceptance criteria

### AC#1: empirical investigation

Confirm (via grep + ACFG inspection) that no in-tree schedule produces a hoist-target straddling a Sync that this dedup would suppress.

### AC#2: choose remediation

Either widen the dedup to Sync-stopping (the simpler default — same helper as cycle-158), OR assert an invariant + add a structural debug_assert that Pass A's input is already epoch-collapsed.

### AC#3: regression test

Pin whichever choice is taken.

## Cross-reference

- TASK-0335 (cycle 158) — parent fix.
- TASK-0336 sibling.
- feedback-silent-sibling-defect (memory) — would be the 10th firing if this site bites.
- transfer_inject.rs lines 1493-1503 — the silent-sibling site.

## Cross-ref correction

The notes above refer to 'TASK-0336' as the P2.1 sibling, but the actual filed id is **TASK-0335.01** (subtask numbering, not sequential parent id). Treat all in-line 'TASK-0336' tokens above as 'TASK-0335.01'.
<!-- SECTION:NOTES:END -->
