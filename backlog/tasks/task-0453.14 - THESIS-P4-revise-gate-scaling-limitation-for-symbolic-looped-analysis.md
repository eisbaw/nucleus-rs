---
id: TASK-0453.14
title: 'THESIS P4: revise gate-scaling limitation for symbolic looped analysis'
status: In Progress
assignee: []
created_date: '2026-06-06 22:56'
updated_date: '2026-06-07 01:39'
labels:
  - thesis
  - paper
dependencies:
  - TASK-0453.04
parent_task_id: TASK-0453
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Depends on IMPL P4. Revise ch11:172-181 + ch09 gate-scaling + ch10:214-226 to reflect symbolic looped-net analysis lifting the linear-in-firings bound. Ground the scaling claim against measured gate cost. Residual: subclass scope of the symbolic analysis. PDF green.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 ch09/ch10/ch11 reflect symbolic gate analysis, grounded against measurements
- [x] #2 Subclass scope residual documented
- [x] #3 PDF green; paper-accuracy passes
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
THESIS UPDATED (cycle-3) to reflect the P4 symbolic looped-net fast path (TASK-0453.04). Edits: (1) ch09 sec:res-quant-net — rewrote the "honest limitation" paragraph into a TWO-REGIME description: buffer-free programs (single-worker / no cross-worker transfer, incl. the matmul triple loop and the whole tab:quant-net corpus) now proved sound symbolically from the rolled ACFG WITHOUT building the expanded net (cost a function of program text, not iteration count; matmul compile flat as N grows 16->256 / 4k->16.7M firings; the ~2N^3~=33M-node expansion is what the fast path skips); distributed/communicating programs still fall back to the expanded linear-in-firings replay = documented residual, NOT approximated (would trade soundness for scaling). (2) tab:quant-net caption — notes every row is single-worker/buffer-free so the gate now avoids building these nets; counts are the avoided expansion. (3) ch09 sec:res-quant-time — replaced the stale "matmul naive lifts median due to gate linear replay" attribution: single-worker compiles no longer grow with firing count; the distributed matmul schedules are now the heaviest (grow with worker count). (4) ch11 sec:fw-quant — downgraded "left as future work" to "partially closed": buffer-free done, communicating case the concrete open work; explicitly not approximated. ch10:214-226 = the P2 transfer-volume paragraph (parenthetical gate mention only, still accurate) — no gate-scaling claim there, no edit needed (verified). PDF GREEN (122pp, 0 LaTeX warnings, no undefined refs). paper-accuracy + paper-correctness GO after fixes: removed machine-specific absolutes ("five ms"/"eighteen ms") that contradicted the qtmin-qtmax 6-10ms band (P1); fixed "largest compile in set" -> distributed matmul schedules grow with worker count (distributed8 heaviest); softened "Measured sweep" to "compiling at progressively larger N" with 33M framed as computed skipped expansion (P2). Verified-correct: subclass characterization, soundness argument faithful to code theorem, residual framing, flatness=O(rolled-ACFG), fw downgrade, no over-generalization.
<!-- SECTION:NOTES:END -->
