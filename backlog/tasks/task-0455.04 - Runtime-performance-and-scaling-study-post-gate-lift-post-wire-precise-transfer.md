---
id: TASK-0455.04
title: >-
  Runtime performance and scaling study (post gate-lift, post wire-precise
  transfer)
status: Done
assignee: []
created_date: '2026-06-09 21:29'
updated_date: '2026-06-10 22:19'
labels:
  - production
  - perf
dependencies:
  - TASK-0455.01
  - TASK-0453.22
parent_task_id: TASK-0455
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Deferred honestly in TASK-0453.06 because (a) corpus-scale runtime is startup-overhead-dominated, (b) large-N communicating compiles OOM at the gate, (c) loopback != cluster. Becomes feasible once TASK-0455.01 (compile large N) and TASK-0453.22 (stop whole-array broadcast) land — measuring before .22 would mostly measure the known over-communication.

Scope: wall-clock vs worker count x schedule x backend on tier-1, at sizes where compute dominates (>= 100x startup floor); WORST-CASE inputs per the standing perf discipline (memory: feedback-perf-measure-worst-case-and-task-solution-can-be-workaround — a perf number on a friendly input misleads like single-sample correctness); transfer volume measured in bytes on the wire (before/after .22), not estimated; MPI on >= 2 real nodes if hardware is available, otherwise the loopback bound stays explicit in every claim.

Deliverable: methodology doc (warm-up, variance, repetitions, machine spec, input construction) + scaling curves + a short honest-readings section (what the numbers do NOT show). This is the input the prescriptive-scheduling direction would eventually need; that direction stays out of scope here.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Methodology document committed: worst-case inputs, warm-up/variance discipline, machine spec, repetition counts
- [x] #2 Scaling curves for >= 3 examples including distributed matmul and a distributed stencil, across >= 3 worker counts
- [x] #3 Wire transfer-volume reduction from TASK-0453.22 quantified in measured bytes
- [x] #4 No claim exceeds the measured substrate: loopback caveat retained unless run on real multi-node hardware
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
Plan (cycle owner: perf-study):
1. docs/perf-study-methodology.md — machine spec (Core Ultra 7 165U, 10C/14T, powersave/turbo, 32GB, kernel 6.19.6), warm-up=1, min-of-N (N=9 run / N=60 floor), load-avg + cache-warm recorded per row, re-measure dirty rows, input-independence argument (grep kernels for data-dependent branches), explicit CANNOT-claim list (loopback!=cluster, one laptop, heterogeneous P/E cores, shared-machine noise, powersave governor).
2. scripts/perf-study-run.sh + one just recipe (perf-study). Scratch under nucleus/target/perf-study. Parameterized fixtures generated in scratch (cannot edit committed examples): matmul (N sweep), stencil (H sweep, reuse case-study generator/reference crates by INVOCATION only — they hardcode H so I emit my own param gen/ref in scratch), reduction or a third example. Worker counts {2,4,8} via generated schedule variants (workers list edit only). Backends: pthreads-async + one message-passing (mp-tcp-event for async stencil; mp-tcp-bufsync for sync matmul). Plus single-worker naive baseline per size. BYTE-EXACT vs independent reference at every point (case-study oracle pattern).
3. Wire bytes MEASURED on mp-tcp* backend via /proc/<pid>/io read+write deltas on worker procs (least intrusive); baseline = edge-count x array-size computed statically. Document method + overhead.
4. docs/perf-study.md — curves (tables), crossover analysis (locate where distribution pays on loopback or state none exists), honest-slower finding contextualized, wire measurements, what-this-does-NOT-show.
5. Walls -> tasks; loop-carried/pipelined gate-cost wall demonstrated with ONE data point if attempted.
Verification: runner green end-to-end, byte-exact every point, methodology doc complete, zero source edits (clippy untouched).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Progress (cycle owner perf-study): runner + fixtures + wire + baseline helpers written and SMOKE-GREEN end-to-end (17 cells byte-exact, 3 wire rows). Machine: Core Ultra 7 165U 10C/14T powersave+turbo 32GB kernel 6.19.6. KEY METHOD CORRECTION: /proc/<pid>/io wchar route FALSIFIED (Rust TcpStream uses sendto(2), not counted in wchar — measured 20B for a KB transfer); switched to strace -f -e trace=sendto,sendmsg byte-return summing (validated: N=16 matmul=6592B total, decodes exactly to a/c bands + b whole-array broadcast + control). Wire reductions MEASURED: matmul 50.0% (a+c narrowed, b stays whole-array), stencil 75.0% (both edges 4-way band), reduction 75.0% (a scatter). Robustness: mp-tcp-event rendezvous 6s timeout FLAKES under heavy load at large sizes -> added retry + RUNFAIL status (distinct from byte-divergence FAIL; RUNFAIL surfaced loud, does not fail study). N=512 keystone matmul gate compiles 0.04s/88MB RSS (confirms thesis flat-in-N). Full measurement run launched in a quiet window (load 1.4).

Cycle perf-study P1+P2/P3 remediation (review NO-GO fold-in): FIXED the P1 cargo-in-the-timed-loop asymmetry — runner now times a cargo-stripped run-timed.sh (strip_cargo_from_run_sh, zero-match hard-fail; the emitted run.sh cargo no-op is ~28ms/~30-40% of every mp-tcp sample here). Re-measured all affected rows in quiet windows (load ~1.5-2.1); naive+dist taken back-to-back per table so ratios are load-consistent. Key corrected findings: matmul crossover is N=64 floor / N=128 ~1.4x / N=256 ~2.0x (the old 5-25x mp-tcp band was inflated by the cargo no-op AND dirty load; honest band now ~5-7x on meaningful cells, tens-of-x only where pthreads is at floor); the 9.5ms reduction "anomaly" was a dirty row (clean=1.2ms), not a noisy sample; cross-doc 15360 stencil reconciled (case-study 114/182 vs here 96.5/144.2, bounded not exact). CAPSKIP enumeration corrected: 6 cells = stencil 7680 w2/w4 + stencil 15360 w2/w4/w8 (15360 capped at ALL W, emitted NUC_SO_BUF=4920320>4MiB cap — old "w<=4" claim was wrong) + reduction 2^24; predicate now reads the emitted export NUC_SO_BUF line (single source of truth) not hand-dup sizing. Added: gate compile-wall+peak-RSS via time -v (0.03s/88MB, reproduces doc keystone), per-size send histogram, raw TSV retained as artifact on success, stale-driver cargo-build-first guard, RUNFAIL count in summary. wire.py hard-fails on zero data bytes; measure_wire strace-absent/zero-data are loud exit 1 not warnings. Docs: date+commit stamp both docs, methodology static-baseline asymmetry honestly stated (+git-worktree A/B route noted as not-taken), wirebase corroboration scoped to pthreads-only, hundreds-of-GB->paper phrasing, justfile help one-liner. VERIFIED: PERF_SMOKE green; FULL sweep green 0 divergences 6 CAPSKIP 0 RUNFAIL; all hard-fail guards tested (fail-loud on bad input, pass on good).
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Landed after the campaign-strictest review chain (two NO-GOs, both genuine): (1) cargo-in-the-timed-loop contamination of every mp-tcp sample — fixed via cargo-stripped run-timed.sh; (2) published tables untraceable to retained evidence — fixed by re-running in a genuinely quiet window and transcribing PROGRAMMATICALLY from the named artifact (results/wire-20260611-001452.tsv, retained). Findings: matmul distribution pays from N=128 (1.39x) to 2.32x at N=512 (w=4); stencil/reduction never pay on loopback; mp-tcp = fixed ~20-22ms/worker overhead, slowdown 2.2-28.7x amortising with N; wire reductions MEASURED 50/75/75 percent (strace-sendto; /proc-wchar falsified+documented); gate keystone 0.03s/~88MB flat reproduced by the runner. Methodology doc complete with the honest static-baseline asymmetry + worktree-A/B-not-taken note; min/median/spread/load published per row, dirty rows flagged; loopback caveat everywhere. Walls: TASK-0455.04.01 (wmem cap, 6 CAPSKIP cells) + .04.02 (both descriptions corrected so retracted figures cannot be re-imported). Landed 29f5cdc-era commit 29f5dcd; review chain NO-GO->NO-GO->verified.
<!-- SECTION:FINAL_SUMMARY:END -->
