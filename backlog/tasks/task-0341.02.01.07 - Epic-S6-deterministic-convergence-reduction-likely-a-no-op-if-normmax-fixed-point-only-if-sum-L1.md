---
id: TASK-0341.02.01.07
title: >-
  Epic S6: deterministic convergence reduction (likely a no-op if norm=max;
  fixed-point only if sum/L1)
status: Done
assignee: []
created_date: '2026-06-03 10:14'
updated_date: '2026-06-03 19:29'
labels:
  - grammar-extension
  - determinism
  - reduction
dependencies:
  - TASK-0341.02.01.04
  - TASK-0341.02.01.06
parent_task_id: TASK-0341.02.01
priority: low
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Ensure the convergence reduction is bit-identical across schedules. ARCHITECT P3 (design review cycle-254): if the norm is MAX-abs-diff (L-infinity) — the S3 pinned choice — this slice is LARGELY UNNECESSARY: max is associative, commutative, and EXACT in floating point, so the reduction is already schedule-order-independent. Build the integer fixed-point reduction TREE only if the convergence test must use SUM-abs-diff (L1), which IS order-sensitive. Verify empirically: does the max-abs-diff convergence test produce the identical break-iteration across single-worker and (eventually) multi-worker schedules without fixed-point? If yes, close this as a determinism-PIN test (no new machinery) rather than a build. Do not build a fixed-point tree for a reduction that does not need one.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Convergence norm determinism characterised: max=order-free (pin test), sum=needs fixed-point
- [x] #2 If max: a test pins identical break-iteration across schedules with NO fixed-point machinery; slice closes as a pin
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
FORWARD-CARRIED from S3 (TASK-0341.02.01.04, cycle bb1b248): NORM = MAX (L-infinity) is now PINNED, and S6 is confirmed a NO-OP for it. max is associative, commutative, and EXACT on i32 (and in FP — no rounding), so a max-abs-diff reduction is schedule-order-independent with NO fixed-point machinery. Proven by tests/task0341_s3_order_independence.rs (identical scalar under sequential / reversed / 5 xorshift permutations / two-phase-tree == flat-fold). The integer fixed-point + fixed-reduction-tree machinery this task contemplated is ONLY needed for a sum/L1 FLOAT norm — which the epic deliberately does NOT use. RECOMMENDATION: close S6 as a no-op (norm=max), documenting that the order-independence is already pinned at S3; reopen only if a future variant switches to a float L1/L2 norm. The collective all-reduce+broadcast determinism for the multi-worker break is S7 (.08) scope (max-of-maxes is still order-free), not a fixed-point concern.

CLOSED cycle-262 as a NO-OP pin (norm=max), orchestrator in-thread (trivial: one pure-algebra pin test + close; no implementer cycle per skill guidance). AC#1: max norm characterised order-free (vs SUM/L1 which would need fixed-point — but only a FLOAT sum is order-sensitive; integer L1 is also order-free, pinned as contrast); the epic is integer+max per PRD 10.1 so NO fixed-point machinery built. AC#2: new tests/task0341_s6_break_gen_order_independence.rs pins the break-generation k (first t with maxdiff[t]<=tol) identical across sequential/reversed/5 xorshift perms/balanced-tree association (k=8, genuine 1<=k<cap), folding with the OVERFLOW-SAFE abs_diff_i32 byte-identical to the shipped 21-jacobi-converge kernel (NOT the S3 pre-0436 unsafe spelling). Gate: clippy clean; test 1328/0; test-release 1326/0; e2e 427/364/0/63/0 unchanged (tests-only). Multi-worker collective max-of-maxes break determinism is S7 (.08) — still order-free, no fixed-point there either.

Cycle review gate (orchestrator-implemented, so independent reviewers were the only check): qa-test-runner GO + mped-architect GO. qa re-ran: clippy clean, test 1328/0, test-release 1326/0, e2e 427/364/0/63/0 unchanged (21-jacobi-converge/pthreads-sync still PASS), both new tests execute+pass in dev AND release. architect VERIFIED THE PIN BITES (not tautological): substituted an order-sensitive fold (first-nonzero-wins) and confirmed seq->8 vs rev->7 DIVERGE, so a non-order-free op WOULD fail the asserts; confirmed abs_diff_i32 byte-identical to the shipped 21-jacobi-converge kernel (post-0436, not the S3 pre-0436 spelling); confirmed reference_break=8 is a genuine 1<=k<cap early-exit pinned to a concrete value (drift-detection). Findings (all P3, FOLDED in commit 55bb5fc): P3-1 docstring oversold the corollary as new ground -> softened to honest corollary framing; P3-2 cap-hit sentinel path was modelled but unexercised -> added tol=-1 assertion exercising T_CAP across orders. AC-honesty: both ACs genuinely met; the no-op closure is the CORRECT engineering call (max is provably assoc/comm/exact on i32; a fixed-point tree would be over-engineering), correctly-identified non-work with reasoning verified not asserted.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
S6 closed as a no-op for the max norm: order-independence of the convergence break-generation is an algebraic property of max (assoc+comm+exact on i32), pinned directly at the break-generation level (extending the S3 scalar pin). No fixed-point reduction tree was built because none is needed; a float L1/L2 norm would reopen this, which the integer-only epic does not use.
<!-- SECTION:FINAL_SUMMARY:END -->
