---
id: TASK-0052.01
title: >-
  RT sub-task: schedule parser + IR for 'check loop V : latency_max=T,
  on_violation=panic|log|count'
status: Done
assignee:
  - '@mped'
created_date: '2026-05-21 13:11'
updated_date: '2026-05-21 15:04'
labels:
  - M4
  - real-time
  - language
dependencies: []
parent_task_id: TASK-0052
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
PRD §6.3.5. Schedule grammar already supports loop V: pipeline=D; this task adds the CHECK directive (separate top-level form). Units: ns/us/ms/s. Action: panic (default) | log | count. Parser-level test pins each variant; IR carries a ResolvedCheckAssertion typed enum.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 Grammar: 'check loop VAR : latency_max = NUMBER UNIT [, on_violation = panic|log|count] ;' parses for all unit suffixes. Time-unit normalisation to nanoseconds in lowering; tests pin 10ms == 10_000_000ns etc.
- [ ] #2 ResolvedCheckAssertion variant in SchedIR; on_violation default = Panic; multiple checks per loop allowed; duplicate-assertion-kind on same loop is a typed SchedLowerError.
- [ ] #3 Negative tests: 'latency_max=0', missing unit, unknown on_violation value, check-on-undeclared-loop-var, conflicting duplicate latency_max all fail with typed errors and accurate spans.
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
## Orchestrator-direct cycle (skill discipline)

Cycle was tight (2 new SchedLowerErrorKind variants + 4 negative tests + table-doc entry) — handled in-thread by the orchestrator per "small, precise findings ... fix in-thread" guidance. Gate re-run independently by orchestrator.

## What landed

- `nucleus/compiler/src/sched/ir.rs`: two new variants
  - `ZeroLatencyMax { var }` — rejects `latency_max = 0<UNIT>` as semantically degenerate (every iteration violates a zero budget). PRD §6.3.5 framing as upper bound.
  - `DuplicateCheckAssertion { var, kind }` — rejects two `latency_max` (or two `on_violation`) inside one `check loop` directive. Each assertion kind is a unique slot.
- `nucleus/compiler/src/sched/lower.rs`: `lower_check` walks asserts once, surfaces the first offender at the loop_var span before building the resolved Vec; cascade-class table updated.
- `nucleus/compiler/tests/sched_parser.rs`: `negative_check_unknown_on_violation_value_is_rejected` pins parser rejection of `on_violation = foobar`.
- `nucleus/compiler/tests/sched_lower.rs`: 3 new negative tests — zero_latency_max, dup_latency_max, dup_on_violation.

## Per-AC status

- **AC#1** (grammar parses for all unit suffixes; 10ms = 10_000_000ns): DONE — `time_lit()` parser at sched/parser.rs:263, all 4 units in sched_parser.rs:917-920 (ns/us/ms/s) + 10ms pin at sched_parser.rs:399.
- **AC#2** (ResolvedCheckAssertion variant; multiple checks per loop; duplicate-assertion-kind a typed error): DONE — `ResolvedCheckAssert` at ir.rs:243; multi-assert per directive supported + tested (sched_lower.rs:359 has 2 asserts); duplicate-assertion-kind NOW rejected via `DuplicateCheckAssertion`. **`on_violation` default = Panic intentionally deferred to TASK-0052.02 (codegen)**: the IR carries only what was written; codegen materializes the default. Documented at the new variant docstring.
- **AC#3** (negative tests): DONE — `latency_max=0` (this cycle), missing unit (pre-existing `10minutes`), unknown on_violation (this cycle), check-on-undeclared-loop-var (pre-existing via `UnknownLoop` in link.rs), duplicate latency_max within one check (this cycle).

## Gate (orchestrator re-ran post-commit, inside `nix develop`)

- `cargo test --workspace`: **510 pass / 0 fail / 2 ignored** (was 506/0/2 — +4 tests).
- `cargo clippy --workspace --all-targets -- -D warnings`: clean.
- `just e2e`: 36 cells: 29 pass / 0 fail / 7 skipped (unchanged baseline).

## Forward-carry to TASK-0052.02 (codegen)

When TASK-0052.02 (timing codegen) implements `on_violation = panic|log|count`:

1. The IR (`ResolvedCheckDirective.asserts: Vec<ResolvedCheckAssert>`) may have ZERO `OnViolation` entries — codegen must default to `ViolationKind::Panic` per PRD §6.3.5 ("Default: `panic`."). Add this as the codegen-side default, NOT at IR layer (the IR stays faithful to what the user wrote).
2. `latency_max = 0` is now structurally impossible to reach codegen (rejected at sched_lower). Codegen can assume `nanos > 0`.
3. Each `kind` is unique per check (no need to dedupe at codegen).
<!-- SECTION:NOTES:END -->
