---
id: TASK-0438.03
title: >-
  M11 DMA+PIO demo example (22-dma-pio-demo) + Renode platform wiring +
  byte-exact gate
status: To Do
assignee: []
created_date: '2026-06-04 07:03'
labels: []
dependencies:
  - TASK-0438.02
parent_task_id: TASK-0438
priority: medium
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Third sub-slice of TASK-0438. Add the actual demo example exercising BOTH transport modes in one Nuc app, and prove it byte-exact under Renode.

- New example (next free number after 21-jacobi-converge; likely 22-dma-pio-demo): prog.algo.nuc + an embedded multi-MCU schedule with >=1 `transfer X : ..., mode=dma;` AND >=1 `transfer Y : ..., mode=pio;` edge. Audio-style shape mirroring ex14 (bulk frame = DMA, control/status = PIO).
- Renode platform: confirm whether the existing platforms/cpus/stm32h743.repl + generated multimcu.resc suffice (USART fabric reused), or whether a DMA peripheral must be wired. If a .repl change is needed, do it; if not, document why the existing platform is sufficient.
- Wire into the renode-multimcu gate: `just renode-multimcu 22-dma-pio-demo <sched>` must produce BYTE-EXACT output vs a reference.bin. Consider adding it to renode-multimcu-gate (justfile:1669) as a third co-sim, or document why it stays a manual gate.
- HONEST header note in the example .nuc explaining Renode DMA timing is a model (not cycle-accurate); the proof is value-correctness, not timing (parent AC#4).
- tier-1 unaffected: the new example is multi-MCU-only / tier-1-skipped like 14-hearing-aid (parent AC#5).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [ ] #1 new example exists with >=1 mode=dma edge AND >=1 mode=pio edge in one schedule (parent AC#1)
- [ ] #2 just renode-multimcu <new-example> <sched> runs BYTE-EXACT vs reference.bin (parent AC#3); ran >=2x, results recorded
- [ ] #3 Renode platform sufficiency documented: existing .repl reused OR DMA peripheral wired with justification (parent AC#3)
- [ ] #4 HONEST header note in the .nuc: Renode DMA timing is a model, proof is value-correctness not timing (parent AC#4)
- [ ] #5 tier-1 e2e baseline preserved; new example multi-MCU-only/tier-1-skipped (parent AC#5)
- [ ] #6 considered adding to renode-multimcu-gate; decision recorded
<!-- AC:END -->
