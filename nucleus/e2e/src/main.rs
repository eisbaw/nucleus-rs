// Stub entry point for the `nucleus-e2e` differential test harness.
// See nuc-nucleus/PRD.md §10, §12.3.
//
// At M0 this only exists so the workspace builds. The real harness
// will iterate the (example x schedule x backend) matrix and diff
// per-cell outputs against the example's reference.bin.

fn main() {
    // Intentionally empty. Replaced when the first backend lands (M1+).
}
