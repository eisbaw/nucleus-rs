# The end-to-end differential test harness. See PRD §10, §12.3.
#
# At M0 this is a stub. It will eventually drive the full
# (example x schedule x backend) matrix and diff outputs against
# the per-example reference.bin.

[package]
name = "e2e"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "nucleus-e2e"
path = "src/main.rs"
