// Stub entry point for the `nucleus` pre-compiler binary.
// See nuc-nucleus/PRD.md §1, §5, §12.2.
//
// At M0 this only exists so the workspace builds. Real parsing,
// scheduling, and codegen land in subsequent milestones.

fn main() {
    // Intentionally empty. Replaced at M0's end-to-end milestone.
}
